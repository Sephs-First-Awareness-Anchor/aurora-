name: aurora_app
description: Aurora AI Assistant — production Flutter app
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_overlay_window: ^0.3.0
  speech_to_text: ^7.0.0
  flutter_tts: ^4.0.0
  permission_handler: ^11.3.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
