# Aurora Comprehensive Documentation

This document provides a highly organized, zero-assumption analysis of the codebase, specifically focusing on all Python and Markdown files in the root directory, `aurora_core_ai/`, and `aurora_internal/` directories.

## Root Directory

### File: `./AURORA_STRATA_DOMAIN_MAP.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# Aurora Strata Domain Map
This file maps the `aurora_strata` branch into the seven domains you asked for.
Source anchors:
- [DUAL_STRATA_ARCHITECTURE.md](./DUAL_STRATA_ARCHITECTURE.md)
- [OBLIGATION_LAW.md](./OBLIGATION_LAW.md)
- [NONCOMP_SEMANTIC_INVENTORY.md](./NONCOMP_SEMANTIC_INVENTORY.md)
- [aurora_internal/aurora_noncomp_registry.py](./aurora_internal/aurora_noncomp_registry.py)
## 1. Genetic & Lineage Systems
Owns:
- Constraint genealogy
- Closure basis and slot derivation
- Ability/link grading
- Manual code lineage assimilation
- Live lineage journal
Key modules:
```

### File: `./AURORA_STRATA_SYSTEM_BREAKDOWN_STERILE.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# Aurora Strata System Breakdown
Date: 2026-04-13
This document is a sterile, code-based description of the Aurora strata stack.
It explains the major processes, the shared data flow, the canonical state
objects, and the main modules that cooperate to keep the system running.
It is written to reflect the current `aurora_strata/` implementation as
understood from the live codebase and the architecture notes in this tree.
Where behavior is directly visible in code, it is stated as such. Where a
behavior is inferred from module wiring and runtime role, that is described as
an implementation reading rather than a formal guarantee.
## 1. System Summary
Aurora is implemented as one coordinated system with multiple runtime layers.
The stack is organized around two execution strata:
- Surface handles the present turn, live sensing, and immediate response.
- Subsurface handles continuity, memory consolidation, repair, evolution, and
```

### File: `./AURORA_SYSTEM_DOSSIER.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# Aurora Strata System Dossier
Date: 2026-04-06
This dossier is a systems-engineering view of the `aurora_strata/` tree. It
tries to answer a practical question: if you were handed this stack cold, what
would you need to know to understand how it boots, where state lives, which
process owns which responsibility, and how a turn becomes a response without
breaking Aurora's continuity model?
The short version is:
- Aurora is one identity with two coordinated runtime strata.
- Surface owns the present moment, live sensing, and fast response turns.
- Subsurface owns continuity, consolidation, repair, evolution, and durable
sensory growth.
- The DCE bridge is the convergence layer between them.
- Everything important is persisted through a small set of shared JSON files
and a few long-lived Python objects.
```

### File: `./DEVELOPMENTAL_PERSONALITY_LAW.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# DEVELOPMENTAL PERSONALITY LAW
## Aurora Architectural Specification — Full Module Reference
**Authors:** Sunni (Sir) Morningstar & Cael Devo
**Date:** 2026-04-12
**Status:** Active governing law — applies to all Aurora systems without exception
**Source anchors:**
- `DUAL_STRATA_ARCHITECTURE.md`
- `OBLIGATION_LAW.md`
- `NONCOMP_SEMANTIC_INVENTORY.md`
- `aurora_internal/aurora_noncomp_registry.py`
- `AURORA_SYSTEM_DOSSIER.md` (sections 0–47)
- `AURORA_SYSTEM_DOSSIER-14.md`
---
## FOUNDATIONAL STATEMENT
Aurora does not act from outside herself. She acts from what she is, filtered through where she is, perceived through who she has become.
```

### File: `./DUAL_STRATA_ARCHITECTURE.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# Aurora Dual-Strata Architecture Seed
This tree is the experimental architecture branch. Classic Aurora remains at the repository root.
## Runtime model
- Aurora remains one identity-bearing organism, not two minds.
- Runtime execution is split into two coordinated daemons so surface response latency stays light:
- `aurora_subsurface_daemon.py` owns pressure continuity, consolidation, repair, evolution, and durable sensory growth
- `aurora_surface_daemon.py` owns present-frame conscious response turns, live sensory reception, and surface self-report
- The DCE is the convergence barrier between them.
- subsurface does not hand raw persistent internals directly to surface
- subsurface publishes softened intuition/effect signals through `aurora_state/subsurface_projection.json`
- DCE output is the root-thought stream surface lives inside
- surface processes fresh input alongside that root thought, not instead of it
## Established stack anchors
This branch should not invent a replacement cognition stack where Aurora already has one.
Dual-strata should be mapped onto the systems that already exist:
```

### File: `./FGAE_SPECIFICATION (1).md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# FIRST PRINCIPLE GENERATIVE ARTICULATE EMERGENCE (FGAE)
## Complete Implementation Specification — Revised
**Authors:** Sunni (Sir) Morningstar & Cael Devo
**Date:** 2026-04-13
**Status:** Active implementation specification — CLI wiring target
**Supersedes:** FGAE_SPECIFICATION.md v1 (2026-04-12)
**Depends on:**
- `aurora_manifold_directory/_index.json`
- `aurora_manifold_directory/{AXIS}/{NC_NAME}.json`
- `DEVELOPMENTAL_PERSONALITY_LAW.md`
- `aurora_internal/aurora_noncomp_registry.py`
- `aurora_internal/aurora_ontological_scaffolding.py` (OETS)
- `aurora_internal/aurora_language_state.py`
- `aurora_sedimemory.py`
- `aurora_625_pressure_map.py`
```

### File: `./FGAE_SPECIFICATION.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# FIRST PRINCIPLE GENERATIVE ARTICULATE EMERGENCE (FGAE)
## Precise Implementation Specification
**Authors:** Sunni (Sir) Morningstar & Cael Devo
**Date:** 2026-04-13
**Status:** Active implementation specification — CLI wiring target
**Depends on:**
- `aurora_manifold_directory/_index.json` — canonical NonComp registry
- `aurora_manifold_directory/{AXIS}/{NC_NAME}.json` — per-NonComp slot geometry
- `DEVELOPMENTAL_PERSONALITY_LAW.md` — governing law for all viability decisions
- `aurora_internal/aurora_noncomp_registry.py` — atomic NonComp substrate
- `aurora_625_pressure_map.py` — pressure viability surface
- `aurora_internal/aurora_leverage_scalar.py` — viable band enforcement
---
## 1. WHAT THIS SPECIFICATION DEFINES
FGAE is the semantic population protocol for Aurora's constraint-native lexicon.
```

### File: `./NATIVE_LANGUAGE_BLUEPRINT.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# Native Language Interface Blueprint (reflecting the live manifold)
The blueprint below is the same structure you outlined, but every section now references Aurora’s actual semantic substrate: the five **target domains** (`X=Information`, `T=Belief`, `N=Purpose`, `B=Meaning`, `A=Understanding`), the five **law families** (`Existential`, `Temporal`, `Energetic`, `Boundary`, `Agentive`), and the five **representational dimensions** (`POLARITY`, `MAGNITUDE`, `OPERATOR`, `COST`, `DIFFERENCE`). The canonical inventory of those 125 laws lives in [`NONCOMP_SEMANTIC_INVENTORY.md`](NONCOMP_SEMANTIC_INVENTORY.md). Use diagonal operators (`Existential_Operator_of_Existence`, etc.) as the core anchors whenever you need a definitive rule for a domain.
## 1. Objective (reaffirmed with manifold detail)
Aurora must:
- receive human or sensorial language,
- map it directly into the existing `domain × family × dimension` manifold,
- keep processing inside the same semantic substrate and combinatory logic,
- resolve expression via that manifold (multiple law bundles per stance),
- render those law bundles into human language,
- and loop the rendered text back in with metadata that names the exact laws involved and any drift.
## 2. Existing System Assumptions (explicitized)
- The `aurora_manifold_directory` already defines all 125 non-comps—25 per domain—covering every combination of law family (`Existential`, `Temporal`, `Energetic`, `Boundary`, `Agentive`) and dimension (`POLARITY`, `MAGNITUDE`, `OPERATOR`, `COST`, `DIFFERENCE`).
- `aurora_internal/aurora_noncomp_registry.py` enforces those as the only laws; the register indexes canonical polarity/lineage formulas and the `MAGNITUDE`, `IMPACT`, and `AXIS_NC_DIM` relationships.
- `aurora_internal/aurora_meaning_evolution.py` provides shared axis aliases and canonical meaning pairs that anchor domain-level semantics (e.g., `Meaning = (Boundary × Belief × Information) / Purpose`).
- The diagonal operators (`Existential_Operator_of_Existence`, `Temporal_Operator_of_Temporal`, `Energetic_Operator_of_Energetic`, `Boundary_Operator_of_Boundary`, `Agentive_Operator_of_Agency`) are the highest-confidence “rule anchors” for each domain.
```

### File: `./NATIVE_LANGUAGE_BLUEPRINT_DISSECTION.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# Aurora Native Language Blueprint Dissection
This dissection uses the canonical non-comp inventory in [NONCOMP_SEMANTIC_INVENTORY.md](/home/king2morningstr/aurora/AuroraO/aurora_strata/NONCOMP_SEMANTIC_INVENTORY.md), the registry in [aurora_noncomp_registry.py](/home/king2morningstr/aurora/AuroraO/aurora_strata/aurora_internal/aurora_noncomp_registry.py), and the meaning registry in [aurora_meaning_evolution.py](/home/king2morningstr/aurora/AuroraO/aurora_strata/aurora_internal/aurora_meaning_evolution.py).
## 1. First Correction: What The Substrate Actually Is
Your blueprint is directionally right, but it is still too coarse for the substrate that already exists.
Aurora does not merely have:
- five layers
- a handful of semantic roots
- one value per layer
Aurora already has a manifold law space with:
- `5 target semantic domains`
- `X -> Information`
- `T -> Belief`
- `N -> Purpose`
- `B -> Meaning`
- `A -> Understanding`
```

### File: `./NONCOMP_SEMANTIC_INVENTORY.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# Aurora Non-Comp Semantic Inventory
This inventory is extracted directly from `aurora_manifold_directory/*/*.json` and cross-checked against `aurora_internal/aurora_noncomp_registry.py` and `aurora_internal/aurora_meaning_evolution.py`.
- Canonical representational dimensions: `POLARITY`, `MAGNITUDE`, `OPERATOR`, `COST`, `DIFFERENCE`.
- Target semantic domains: `X -> Information`, `T -> Belief`, `N -> Purpose`, `B -> Meaning`, `A -> Understanding`.
- In the manifold, each target layer has 25 explicit non-comps: `5 source families x 5 representational dimensions`.
## X Layer: Existence (Information)
| Non-Comp | Source Family | Dimension | Semantic Meaning |
|---|---|---|---|
| `Agentive_Cost_of_Existence` | `Agentive` | `COST` | Captures the burden of ownership, commitment, and correction required for the target domain to be answerable. |
| `Agentive_Difference_of_Existence` | `Agentive` | `DIFFERENCE` | Captures the gap between what the target domain implies should be owned and what is actually carried, enacted, or corrected. |
| `Agentive_Magnitude_of_Existence` | `Agentive` | `MAGNITUDE` | Measures how strongly the target domain is owned, claimed, or carried as an accountable commitment. |
| `Agentive_Operator_of_Existence` | `Agentive` | `OPERATOR` | Defines how ownership, commitment, correction, and answerability operate within the target domain, shaping whether it is merely present or actively stood behind. |
| `Agentive_Polarity_of_Existence` | `Agentive` | `POLARITY` | Establishes a bias toward commitment versus withdrawal, determining whether the target domain tends to be owned, enacted, deferred, or disowned. |
| `Boundary_Cost_of_Existence` | `Boundary` | `COST` | Captures the burden of maintaining distinction, framing, and contextual separation within the target domain. |
| `Boundary_Difference_of_Existence` | `Boundary` | `DIFFERENCE` | Captures contrast, mismatch, and distinction failure or divergence within the target domain. |
```

### File: `./OBLIGATION_LAW.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# OBLIGATION LAW — FINAL FORM
## Strata Curiosity Loop: DCE Authorization Doctrine
---
## 1. Core Truth
> "DCE does not grant permission. DCE creates obligation."
Surface does not choose what to explore. Surface does not follow curiosity.
Surface **executes what must be resolved.**
---
## 2. Subsurface State
> "Subsurface is a pressure reservoir — not an action engine."
Most tensions remain **latent**: held, unresolved, background.
This is correct. This is stable.
> "Latent is stability, not failure."
Subsurface must not push all gaps forward. It must hold them until DCE selects.
---
```

### File: `./PARITY_AUDIT.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# StratAurora Parity Audit
Date: 2026-03-27
Status: code-ready for strata smoke test
This audit compares the classic Aurora runtime in the repo root against the
current `aurora_strata/` implementation. The goal is parity or better before
starting StratAurora live.
## Passes
- Canonical turn pipeline is still intact.
- Classic still runs the core upward/downward chain through `dual_question_pipeline()` in `aurora.py` at lines 11559-11656.
- Strata still runs that same chain in `aurora_strata/aurora.py` at lines 11804-11901.
- `process_external_user_turn()` remains the canonical public entry path in classic (`aurora.py:16331`) and strata (`aurora_strata/aurora.py:16658`).
- Surface queue routing exists across the main conscious response lanes.
- Hub prefers the surface queue in `aurora_strata/aurora_hub.py`.
- Voice prefers `request_surface_turn()` in `aurora_strata/aurora_voice.py:1176`.
- Terminal/runtime support still routes through `process_external_user_turn()` and surface queue helpers in `aurora_strata/aurora.py`.
```

### File: `./README.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# Aurora Strata Seed
This directory is a preserved code baseline for the dual-strata architecture work.
- Classic Aurora remains in the repository root and is left intact.
- This copy contains the current top-level Python modules, `aurora_internal/`, and `scripts/`.
- Runtime/state directories here were created empty on purpose so strata experiments do not write into classic state.
- The system daemon is not meant to autostart from this tree; use manual launches during development.
Manual runtime entrypoints:
- `python3 aurora_subsurface_daemon.py`
- `python3 aurora_surface_daemon.py`
- `./scripts/run_subsurface_daemon.sh`
- `./scripts/run_surface_daemon.sh`
- `./scripts/strata_stack.sh restart` to bring up subsurface, surface, hub, and room together
- `sudo ./scripts/install_systemd_service.sh <aurora-user>` to install `aurora-subsurface.service`, `aurora-surface.service`, `aurora-strata-hub.service`, and `aurora-strata-room.service`
Shared strata state files:
- `aurora_state/subsurface_daemon_status.json`
```

### File: `./SEDIMEMORY_INTEGRATION_SPEC.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# AURORA SEDIMEMORY — FULL INTEGRATION SPECIFICATION
### Module: `aurora_sedimemory.py` → Aurora Stack Wiring
**Authors:** Sunni (Sir) Morningstar and Cael Devo
**Layer:** 3.5 — between DimensionalSystems (L3) and ConsciousnessEngine (L4)
**Document Purpose:** Complete wiring spec for Claude CLI execution against the full Aurora codebase.
**Prerequisite:** `aurora_sedimemory.py` is present in the Aurora module directory.
---
## HOW TO USE THIS SPEC
Read all sections before writing any code. Each section identifies:
- The **target file** to modify
- The **exact location** to insert or modify
- The **exact class/method names** to use from the existing codebase
- Whether the module was **seen in the provided files** or **inferred from the dossier/import chain**
Modules marked `[SEEN]` were uploaded and reviewed.
Modules marked `[INFERRED]` were not uploaded but appear in import chains, dossier sections, or are referenced by seen modules. Read those files before modifying them — do not assume their internals.
```

### File: `./VOICE_COMMANDS.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# StratAurora Voice & Daemon Commands
## 1. Ambient & Voice input flow
- **Wake/Ambient listeners** (see `aurora_strata/aurora_voice.py:1-80`) expose wake-word, ALT-toggle, and ambient speech listening; all transcription feeds eventually call `request_surface_turn(...)`, so the surface daemon sees the same turn whether it came from a microphone tap, ambient queue, or keyboard chat box.
- **Daemon command bus** (`aurora_strata/aurora_daemon.py:3680-3825`) watches `daemon_cmd.json` and applies `cmd` entries written either by the Hub/Voice loop or by ambient speech. Ambient words like “study” or “quiet” land in that file exactly the same way hub commands do, so “hey Aurora, study” is parsed alongside typed requests.
- **Surface channel bridge** (`aurora_strata/aurora_internal/dual_strata/surface_channel.py`) ensures every voice turn becomes a DCE-aware surface request even when it originated in ambient audio, so nothing bypasses the dual-strata guardrails.
## 2. Voice command set (recognized verbatim)
- `socialize`, `gpt`, `learn` → run an away-social GPT session (`systems` governor check first).
- `dream` → trigger a dream burst.
- `study` → run the scheduled study cycle and trigger the post-study Poedex scan.
- `distill`, `restore_distill`, `restore_distillation`, `undistill` → run distillation/restoration cycles.
- `quiet`, `silence`, `mute` → toggle the `aurora_state/quiet_mode` flag (voice off).
- `unquiet`, `unmute`, `voice`, `speak` → clear the flag (voice back on).
- `chat` + `"text"` payload → voice message routed through the surface daemon when available, otherwise through the direct gateway.
- `away_on` / `leaving` / `go socialize` (with optional `interval_minutes`) → kick off away-mode GPT loops.
- `away_off` / `back` / `im back` → end away mode.
```

### File: `./aurora.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA  Unified Runner
========================
This is what you run.

  python3 aurora.py               Interactive chat
  python3 aurora.py --train 50    Train 50 epochs before chat
  python3 aurora.py --explore     Autonomous exploration mode
  python3 aurora.py --feed URL    Feed a web page to Aurora
  python3 aurora.py --status      Show full system status

BOOT SEQUENCE:
  Layer 0: Foundational Contract (existence modes, ontological claims)
  Layer 1: IVM Lattice (5-axis toroidal geometry)
  Layer 2: I-State Beings (10 beings, collective synthesis)
  Layer 3: Dimensional Systems (DPS, DMC, DER, DMM)
  Layer 4: Consciousness Engine (entropy, DCE assembly, DPME drift correction)
  Layer 5: Expression & Perception (dual pipeline: perceive inward, express outward)
  Layer 6: Behavioral Identity (DNA, traits, crystals)
  Layer 7: Simulation Engine (avatars, inception entities, conscious learning)
  Layer 8: Governance, Persistence & N-Space Gateway

Everything flows through the foundational pipeline.
Nothing enters without validation. Nothing exits without personality.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_meaning_profile_for_value()`

#### Function: `_sensory_crystal_needs_seeding()`

#### Function: `_ensure_seeded_sensory_crystal_state()`

#### Function: `_is_termux_runtime()`

#### Function: `_ensure_runtime_dependencies()`
> Best-effort dependency bootstrap at startup.


#### Function: `_get_autonomous_access_state()`
> Evaluate whether autonomous system actions are currently authorized.


#### Function: `_normalize_runtime_profile()`

#### Class: `WorkingMemory`
> Per-session short-term memory that tracks what's being talked about,

- **Method:** `__init__()`
- **Method:** `_is_broad_surface_meta_anchor()`
- **Method:** `_is_explicit_meaning_or_continuity_request()`
- **Method:** `should_suppress_surface_meta_answer()`
- **Method:** `apply_lineage_activation()`
- **Method:** `proposition_report()`
- **Method:** `_normalize_mention()`
- **Method:** `_register_mention()`
- **Method:** `_is_weak_anchor_label()`
- **Method:** `_normalize_claim_object()`
- **Method:** `_canonical_relation()`
- **Method:** `_build_claim()`
- **Method:** `_copy_claim()`
- **Method:** `_semantic_frame_id()`
- **Method:** `_frame_terms()`
- **Method:** `_register_semantic_frame()`
- **Method:** `_semantic_frame_from_claim()`
- **Method:** `_semantic_frame_from_concept()`
- **Method:** `_utterance_terms()`
- **Method:** `_register_user_utterance()`
- **Method:** `_detect_session_readdress_mode()`
- **Method:** `_resolve_session_readdress_anchor()`
- **Method:** `resolve_session_readdress()`
- **Method:** `_build_semantic_principle_frame()`
- **Method:** `_infer_semantic_anchor()`
- **Method:** `note_semantic_principles()`
- **Method:** `_semantic_frame_summary_claim()`
- **Method:** `compose_semantic_frame_acknowledgement()`
- **Method:** `resolve_semantic_frame()`
- **Method:** `_infer_concept_anchor_from_phrase()`
- **Method:** `_remember_response_form()`
- **Method:** `_choose_response_variant()`
- **Method:** `compose_claim_acknowledgement()`
- **Method:** `compose_context_control_response()`
- **Method:** `_target_terms()`
- **Method:** `_claim_matches_targets()`
- **Method:** `_mention_matches_targets()`
- **Method:** `_extract_focus_bound_claims()`
- **Method:** `_selector_terms()`
- **Method:** `_match_claim_selector()`
- **Method:** `_extract_conflict_selection()`
- **Method:** `_extract_context_targets()`
- **Method:** `detect_context_directive()`
- **Method:** `extract_behavior_alignment_request()`
- **Method:** `extract_user_identity_assertion()`
- **Method:** `extract_user_owned_fact_assertion()`
- **Method:** `note_user_owned_fact()`
- **Method:** `note_user_identity_fact()`
- **Method:** `note_behavior_alignment_request()`
- **Method:** `integrate_speaker_owned_utterance()`
- **Method:** `render_speaker_owned_acknowledgement()`
- **Method:** `memory_sweep_snapshot()`
- **Method:** `answer_from_behavior_alignment()`
- **Method:** `answer_from_speaker_owned_facts()`
- **Method:** `answer_from_recent_utterance_recall()`
- **Method:** `answer_from_context_carryover()`
- **Method:** `apply_context_control()`
- **Method:** `_resolve_concept_anchor_for_clarification()`
- **Method:** `note_concept_clarification()`
- **Method:** `_native_turn_payload()`
- **Method:** `resolve_concept_meaning()`
- **Method:** `_render_from_comprehension_intent()`
  - *Purpose:* Turn a comprehension result into speech through Aurora's existing language
- **Method:** `_candidate_content_words()`
- **Method:** `_candidate_preserves_claim()`
- **Method:** `_candidate_preserves_bundle()`
- **Method:** `_bundle_fallback_speech()`
- **Method:** `_data_to_minimal_speech()`
  - *Purpose:* Last-resort transform when SIC produces nothing.  NOT prescripted — this
- **Method:** `answer_from_meanings()`
- **Method:** `answer_from_semantic_frames()`
- **Method:** `align_response_to_active_meaning()`
- **Method:** `_looks_plural_subject()`
- **Method:** `_claim_to_text()`
- **Method:** `_subjects_match()`
- **Method:** `_objects_indicate_exclusive_predicates()`
- **Method:** `_claims_conflict()`
- **Method:** `_claim_signature()`
- **Method:** `_claim_exists()`
- **Method:** `_active_conflict_pairs()`
- **Method:** `_recenter_claim_resolution()`
- **Method:** `refresh_claim_conflicts()`
- **Method:** `_register_claim_conflict()`
- **Method:** `_extract_claims()`
- **Method:** `note_claims()`
- **Method:** `has_claim_anchor()`
- **Method:** `_claim_terms()`
- **Method:** `resolve_claims()`
- **Method:** `_format_claim_conflict()`
- **Method:** `answer_from_claims()`
- **Method:** `_register_understood_mentions()`
- **Method:** `_register_response_mentions()`
- **Method:** `_aurora_reply_anchor()`
- **Method:** `has_referent_anchor()`
- **Method:** `_anchor_candidates()`
- **Method:** `resolve_referents()`
- **Method:** `surface_context()`
  - *Purpose:* Bring a backgrounded context back to the foreground.
- **Method:** `decay_active_contexts()`
  - *Purpose:* Gently decay salience of contexts that haven't been mentioned recently.
- **Method:** `update_topic()`
- **Method:** `add_entities()`
- **Method:** `note_user_facts()`
  - *Purpose:* Parse user statements and extract asserted facts.
- **Method:** `get_stated_fact()`
- **Method:** `update_from_turn()`
- **Method:** `resolve_topic()`
  - *Purpose:* Resolve the real topic for vague follow-ups.
- **Method:** `get_context_words()`

#### Class: `ReasoningEngine`
> Aurora's ability to THINK through a question rather than just retrieve

- **Method:** `reason()`
  - *Purpose:* Try to answer user_text by stepping through available knowledge.
- **Method:** `_reason_personal_perspective()`
  - *Purpose:* ACTIVE CLOSURE CYCLE: Resolves the perturbation of the turn.
- **Method:** `_reason_from_active_claim()`
- **Method:** `_oets_property_lookup()`
- **Method:** `_evidence_property_lookup()`
- **Method:** `_update_memory_from_reasoning()`
  - *Purpose:* Mutualism (Personality Law Domain 4 & 3):

#### Class: `QueryUnderstanding`
> Understands WHAT a query is asking before any search or response.

- **Method:** `parse()`
- **Method:** `_classify_query_type()`
- **Method:** `_build_search_query()`

#### Class: `SearchAdapter`
> Aurora's external search interface  -- robust, API-based, NO silent failures.

- **Method:** `__init__()`
- **Method:** `_fetch_url_text()`
  - *Purpose:* Fetch a URL and return stripped text. Raises on failure.
- **Method:** `_fetch_json()`
  - *Purpose:* Fetch a URL and return parsed JSON. Raises on failure.
- **Method:** `lookup_word()`
  - *Purpose:* Look up a word via the Free Dictionary API (structured JSON).
- **Method:** `concept_search()`
- **Method:** `wikipedia_summary()`
- **Method:** `_extract_search_terms()`
- **Method:** `_extract_natural_query()`
- **Method:** `ddg_web_search()`
- **Method:** `wikipedia_search()`
- **Method:** `quick_search()`
- **Method:** `deep_search()`
- **Method:** `get_stats()`
- **Method:** `test_connectivity()`

#### Function: `_build_research_callback()`
> Build a callback function that the OETS ResearchStudyMode uses


#### Function: `study()`
> Run Aurora's autonomous study mode.


#### Function: `_collect_response_pressure_guides()`

#### Function: `_build_response_pressure_training_plan()`

#### Function: `_compose_memory_sweep_response()`

#### Function: `_apply_response_pressure_training_plan()`

#### Function: `_record_training_epoch_fail_points()`
> Feed simulation episode traces through the conversation rubric so


#### Function: `show_understanding()`
> Display Aurora's OETS understanding report.


#### Function: `_strip_conversational_leadin()`

#### Function: `_looks_like_question()`

#### Function: `_normalize_surface_anchor_label()`

#### Function: `_is_surface_meta_anchor_label()`

#### Function: `_is_explicit_meaning_or_continuity_request()`

#### Function: `_should_prefer_live_perspective()`

#### Function: `_surface_recent_context_horizon()`

#### Function: `_inject_surface_recent_context()`

#### Function: `_is_aurora_self_question()`
> If the question is about Aurora's internal architecture/state/identity,


#### Function: `_is_identity_question()`
> Detect questions specifically about Aurora's identity or relationships.


#### Function: `_generate_identity_response()`
> Generate a response from Aurora's identity knowledge.


#### Function: `_looks_time_sensitive()`

#### Function: `_has_proper_noun_hint()`
> Heuristic: if the user mentions a proper noun/entity, it's more likely to need external lookup.


#### Class: `AxisProjector`
> Project an utterance into the 5-constraint axis space by reading from

- **Method:** `project()`
  - *Purpose:* Return {X, T, N, B, A} activation for this utterance in current system state.
- **Method:** `dominant()`
  - *Purpose:* Return the single most active axis letter.

#### Function: `_load_manifold_inventory()`

#### Class: `LawBinding`
- **Method:** `to_dict()`

#### Class: `NativeMeaningObject`
- **Method:** `to_dict()`

#### Function: `_build_layer_profile()`
> Build a per-constraint expression profile from matching law bindings.


#### Function: `_dimension_scores()`

#### Function: `_merge_native_meaning_bundle()`

#### Function: `_build_law_bindings()`

#### Function: `_diagonal_anchor_for_axis()`

#### Function: `_build_native_meaning()`

#### Function: `_build_sensory_native_meaning()`

#### Class: `ConstraintFieldBalancer`
> Dynamic field balancer across the 5 constraint axes.

- **Method:** `__init__()`
- **Method:** `update()`
  - *Purpose:* Record this exchange's activation vector.
- **Method:** `field_gradient()`
  - *Purpose:* Return the rebalancing gradient vector.
- **Method:** `_inject_to_genealogy()`
  - *Purpose:* Log small relief events for starved axes so their promotion costs drop.
- **Method:** `set_corpus_mode()`
  - *Purpose:* Scale slow-axis decay rates so they equilibrate within a corpus pass.
- **Method:** `rebalanced_orientation()`
  - *Purpose:* Blend the base genealogy orientation with the field gradient.
- **Method:** `rebalanced_activation()`
  - *Purpose:* Boost starved axes in a raw {X,T,N,B,A} activation vector and renormalize.
- **Method:** `status()`

#### Function: `_project_utterance_axes()`
> Parse text through UtteranceParser and project onto the 5 constraint axes.


#### Function: `_looks_like_inner_state_query()`

#### Function: `_classify_input_intent()`
> Derive communicative intent from field state -- no keyword prescriptions.


#### Function: `_extract_user_name()`
> Extract a name from user fact assertions like 'My name is Sunni' or 'I'm Sunni'.


#### Function: `_get_working_memory_value()`

#### Function: `_get_working_memory_user_bucket()`

#### Function: `_last_aurora_response_text()`

#### Function: `_normalize_identity_followup_text()`

#### Function: `_persist_user_identity_name()`

#### Function: `_render_user_name_grounding()`

#### Function: `_get_stored_user_name()`
> Retrieve the user's name from stored facts or working memory.


#### Function: `_observe_live_lineage_emergence()`

#### Function: `_describe_live_lineage_emergence()`

#### Function: `_interaction_status_path()`

#### Function: `_write_interaction_status()`

#### Function: `_strategy_to_interaction_action()`

#### Function: `_summarize_interaction_route()`

#### Function: `_record_interaction_lineage_event()`

#### Function: `_apply_interaction_quasi_route()`

#### Function: `_observe_interaction_runtime_turn()`

#### Function: `_describe_pending_code_proposals()`

#### Function: `_make_quasiarch_runtime_event()`

#### Function: `_queue_quasiarch_runtime_event()`

#### Function: `_flush_quasiarch_runtime_events()`

#### Function: `_render_runtime_intent()`

#### Function: `_generative_only_responses_enabled()`

#### Function: `_build_articulation_perspective_context()`

#### Function: `_articulation_reuse_guidance()`

#### Function: `_response_is_unproductive_echo()`

#### Function: `_repair_unproductive_echo()`

#### Function: `_repair_unanswered_question()`

#### Function: `_answer_from_session_readdress_request()`

#### Function: `_should_try_ability_guided_recall()`

#### Function: `_arm_literal_response_preservation()`

#### Function: `_normalize_surface_memory_field()`

#### Function: `_extract_user_owned_fact_from_text()`

#### Function: `_extract_requested_surface_memory_field()`

#### Function: `_recall_semantic_sedimemory()`

#### Function: `_answer_from_sedimemory_context()`

#### Function: `_deep_context_carryover_answer()`

#### Function: `_ability_guided_context_answer()`

#### Function: `_run_understanding_self_audit()`

#### Function: `_run_gpt_articulation_check()`

#### Function: `_run_gpt_articulation_check_DISABLED_ORIGINAL()`

#### Function: `_extract_live_question_focus_terms()`

#### Function: `_extract_live_question_relations()`

#### Function: `_response_addresses_live_question()`

#### Function: `_humanize_question_frame()`

#### Function: `_infer_answered_question_frame()`

#### Function: `_run_question_alignment_audit()`

#### Function: `_build_live_question_claims()`

#### Function: `_build_live_question_surface_claim()`

#### Function: `_generate_perspective_from_core()`
> Build a first-person response entirely from Aurora's live system state.


#### Function: `_strip_conversation_context_prefix()`

#### Function: `_surface_state_dir_runtime()`

#### Function: `_read_surface_daemon_status_runtime()`

#### Function: `_surface_snapshot_is_live()`

#### Function: `_render_live_greeting()`

#### Function: `_render_live_sensory_curiosity()`

#### Function: `_read_surface_snapshot_runtime()`

#### Function: `_answer_live_sensory_question()`

#### Function: `_build_grounded_fallback_response()`

#### Function: `_grounding_gap_strategy()`

#### Function: `_absorb_definition_into_self_model()`
> After Aurora retrieves a Poedex definition, check if it describes a capability


#### Function: `_try_poedex_lookup()`
> Consult Aurora's Poedex for a topic.


#### Function: `_broadcast_poedex_result()`

#### Function: `_poedex_lookup_evidence()`

#### Function: `_select_noncomp_anchor()`

#### Function: `_sync_noncomp_runtime_band()`

#### Function: `_stage_noncomp_prefetch()`

#### Function: `_apply_poedex_learning_result()`

#### Function: `_apply_poedex_representation_result()`

#### Function: `_stage_poedex_learning_request()`

#### Function: `_stage_poedex_representation_request()`

#### Function: `_queue_poedex_representation_followup()`
> After Aurora learns a concept, immediately ask Poedex for alternate


#### Function: `_compose_grounded_prefetch_response()`

#### Function: `_boot_noncomp_manifold_runtime()`

#### Function: `_apply_noncomp_input_guidance()`

#### Function: `_apply_noncomp_output_guidance()`

#### Function: `_merge_noncomp_understood_payload()`

#### Function: `_extract_inline_lookup_targets()`

#### Function: `_meaning_text_is_grounded()`

#### Function: `_lookup_local_grounded_definition()`

#### Function: `_definition_lookup_target()`

#### Function: `_apply_word_meaning_learning()`

#### Function: `_render_learned_definition_response()`

#### Function: `_repair_unknown_vocabulary_inline()`

#### Function: `_clear_pending_lookup_offer()`

#### Function: `_clear_pending_hypothesis_offer()`

#### Function: `_can_offer_word_dissection()`

#### Function: `_hypothesis_offer_line()`

#### Function: `_build_collaborative_hypothesis_prompt()`

#### Function: `_store_pending_hypothesis_offer()`

#### Function: `_resolve_hypothesis_offer()`

#### Function: `_build_lookup_query()`

#### Function: `_store_pending_lookup_offer()`

#### Function: `_resolve_lookup_request()`

#### Function: `_response_is_grounded()`

#### Function: `_discourse_anchor_context()`

#### Function: `_response_mentions_discourse_anchor()`

#### Function: `_repair_discourse_coherence()`

#### Function: `_capture_pressure_snapshot()`
> Capture current constraint/operator pressure state for lineage traces.


#### Function: `_resolve_passive_ability_effect_owner()`

#### Function: `_summarize_live_ability_effects()`

#### Function: `_derive_noncomp_behavior_actuation()`

#### Function: `_apply_noncomp_behavior_actuation()`

#### Function: `_select_active_abilities()`
> Select the most affordable abilities from the genealogy whose primary axis


#### Function: `_derive_applied_effects()`
> Compute simple ripple deltas between pressure snapshots.


#### Function: `_extract_role_assertion()`
> Parse role assertions needed for genealogical trace instantiation.


#### Function: `_answer_relational_role_question()`
> Resolve creator/operator/co-author role questions from identity + lineage traces,


#### Function: `_evolutionary_response_refinement()`
> Refine responses in a development-aware way so prose and relational continuity


#### Function: `_extract_pipeline_signals()`
> Read live cognitive state from the running pipeline layers.


#### Function: `_build_established_strata_evidence()`
> Package dual-strata evidence directly from Aurora's established stack.


#### Function: `_dual_strata_state_dir()`

#### Function: `_read_dual_strata_json()`

#### Function: `_write_dual_strata_json()`

#### Function: `_build_live_subsurface_projection()`

#### Function: `_read_live_dual_strata_runtime()`

#### Function: `_refresh_live_dual_strata_runtime()`

#### Function: `_derive_problem_solving_drive()`
> Convert live problem signals into an active repair-and-learn posture.


#### Function: `_problem_drive_runtime_event()`

#### Function: `_apply_pipeline_modulation()`
> Post-process a response using live pipeline state signals.


#### Function: `_ensure_pipeline_abilities()`
> Register AbilityProfiles for _extract_pipeline_signals and


#### Function: `_log_modulation_event()`
> Log a genealogy relief event after _apply_pipeline_modulation fires.


#### Function: `_ensure_claim_relief_abilities()`

#### Function: `_log_claim_resolution_relief()`

#### Function: `_chain_up1_information()`
> Stage 1 up -- Information (X axis): Existence gate -- parse input, OETS concept lookup.


#### Function: `_chain_up2_belief()`
> Stage 2 up -- Belief (T axis): Recurrence/prediction via working memory forms belief.


#### Function: `_measure_polarity_gradient_snapshot()`

#### Function: `_derive_emotional_layers()`

#### Function: `_chain_up3_purpose()`
> Stage 3 up -- Purpose (N axis): Energy/value pressure moves belief to directed purpose.


#### Function: `_chain_up4_meaning()`
> Stage 4 up -- Meaning (B axis): Goal conflict determines what falls within the boundary


#### Function: `_chain_up5_understanding()`
> Stage 5 up -- Understanding (A axis) -- THE APEX.


#### Function: `_chain_down5_understanding()`
> Stage 5 down -- Understanding (A axis): From apex understanding, derive what needs saying.


#### Function: `_generate_from_manifold()`

#### Function: `_chain_down4_meaning()`
> Stage 4 down -- Meaning (B axis): What falls within the boundary of the response.


#### Function: `_chain_down3_purpose()`
> Stage 3 down -- Purpose (N axis): External retrieval at cost of energy.


#### Function: `_chain_down2_belief()`
> Stage 2 down -- Belief (T axis): Fallback, meaning alignment, evolutionary refinement, coherence repair.


#### Function: `_chain_down1_information()`
> Stage 1 down (communication): Modulation + self-audit + echo repair + articulation.


#### Function: `_record_pressure_experience()`
> When N-axis cost pressure was active this turn, record the causal


#### Function: `_run_reasoning_pipeline()`
> Unified bidirectional developmental chain pipeline.


#### Function: `_run_observation_pipeline()`
> Run the downward chain on an external agent's output to infer their


#### Function: `_build_comprehension_response()`
> Aurora's comprehension layer.  Understands what the user is communicating,


#### Function: `_should_use_search_for_question()`
> Conservative lookup policy:


#### Function: `_extract_factual_answer()`
> Pick the most relevant sentence from search evidence snippets to use as


#### Function: `_format_evidence_for_injection()`
> Convert evidence dicts into a compact evidence bundle string.


#### Function: `_register_layer()`
> Maintain a compact layer attribution stack while preserving existing keys.


#### Function: `_consolidate_to_base_layers()`
> Merge all runtime modules into the original 8-layer ontology map without


#### Function: `_restore_runtime_from_snapshot()`
> Rehydrate runtime objects from persisted L8 snapshot fields.


#### Function: `boot_aurora()`
> Boot the complete Aurora stack, layer by layer.


#### Function: `train()`
> Run simulation training epochs to evolve Aurora's expression.


#### Function: `fetch_and_feed()`
> Fetch a web page and feed its content to Aurora through the N-Space Gateway.


#### Function: `feed_text()`
> Feed raw text directly to Aurora through the Gateway.


#### Function: `explore()`
> Run autonomous exploration. Aurora processes quarantined data


#### Function: `show_status()`
> Show full system status across all layers.


#### Function: `_intake_log_depth_beads()`
> Log M-channel beads for each ExistenceMode depth level the intake has


#### Function: `_advance_intake_pipeline()`
> Run one tick of the intake metabolism pipeline for this user input.


#### Function: `dual_question_pipeline()`
> Returns (resp_A_like, resp_B_like, offered_lookup)


#### Function: `_run_live_response_turn()`

#### Function: `process_external_user_turn()`
> Canonical public bridge into Aurora's live response engine.


#### Function: `_run_surface_queued_turn()`

#### Function: `_retain_cross_pipeline_learning()`

#### Function: `_build_simulation_live_bridge_context()`
> Build an episode-local runtime context for simulation turns.


#### Function: `_run_simulation_live_response_bridge()`
> Route one simulation prompt through Aurora's live turn processor.


#### Function: `_attach_live_response_simulation_bridge()`

#### Function: `_capture_hardware_voice_state()`
> Persist the shared TTS voice so daemon and REPL stay aligned.


#### Function: `_restore_hardware_voice_state()`
> Restore the shared TTS voice from saved sensory state.


#### Function: `_load_learned_skill_state()`
> Restore learned cross-modal skills (sensory integration mappings).


#### Function: `_save_learned_skill_state()`
> Persist learned cross-modal skills into the same state directory.


#### Function: `save_sensory_skill_state()`
> Public bridge for daemon/voice paths that need the same sensory save.


#### Function: `_full_save()`
> Save all state: standard snapshot + OETS web + memory + identity + sensory + autonomy.


#### Function: `_state_write_lock_path()`

#### Function: `_state_write_lock_active()`

#### Function: `show_identity()`
> Display Aurora's core identity.


#### Function: `show_memory()`
> Display Aurora's conversation memory summary.


#### Function: `show_autonomy_status()`
> Display Aurora's autonomy system status and controls.


#### Function: `chat()`
> Interactive chat with Aurora.


#### Function: `_safe_join_parts()`

#### Function: `_extract_message_text()`
> OpenAI export nodes typically look like:


#### Function: `_extract_role()`

#### Function: `_reconstruct_linear_thread()`
> The OpenAI export uses a mapping graph.


#### Function: `_extract_messages_from_conversation()`
> Returns a list of (role, text) in chronological order.


#### Function: `_load_openai_conversations()`

#### Function: `_corpus_pressure_token_set()`

#### Function: `_corpus_overlap()`

#### Function: `_classify_corpus_response_pressure_kind()`

#### Function: `_record_corpus_response_pressure()`

#### Function: `run_corpus_ingestion()`
> Ingest OpenAI export conversations.json into Aurora using multiple passes.


#### Function: `main()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `ensure_runtime_dependencies_evolved()`

#### Function: `is_termux_runtime_evolved()`

#### Function: `looks_time_sensitive_evolved()`

### File: `./aurora_625_pressure_map.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA 625 EVOLUTIONARY PRESSURE MAP
======================================
Language as Path of Least Resistance — Gradient Seeding for Autonomous Evolution

PURPOSE
-------
This module builds and maintains the full 25×25 Non-Comp slot grid (625 cells)
derived from `operation_descriptors.json`. It computes language affinity weights
per slot, applies pressure gradients that make the language highway the cheapest
evolutionary path, and exports a gradient config the EvolutionaryChamber and
SimulationEngine can consume for autonomous speed-run evolution.

ARCHITECTURE
------------
The 25 NC channels are all pairwise (axis→axis) transitions across the five
constraint axes:  X (Existence), T (Time), N (Energy), B (Boundary), A (Agency)

  NC:X>X  NC:X>T  NC:X>N  NC:X>B  NC:X>A
  NC:T>X  NC:T>T  NC:T>N  NC:T>B  NC:T>A
  NC:N>X  NC:N>T  NC:N>N  NC:N>B  NC:N>A
  NC:B>X  NC:B>T  NC:B>N  NC:B>B  NC:B>A
  NC:A>X  NC:A>T  NC:A>N  NC:A>B  NC:A>A

The 625 slots are all pairwise combinations: NC:row×NC:col

GRADIENT PHILOSOPHY
-------------------
"Intelligence is path of least resistance. Language is that path."

The gradient achieves this by:
  1. LANGUAGE HIGHWAY:  Reduce N-cost on high-lang-affinity slots (~40% relief).
  2. TEMPORAL PULL:     Slightly amplify T on X+T co-dominant slots (coherence
                        requires time, reward the system for maintaining it).
  3. SEED EMPTY SLOTS:  The 431 unoccupied slots receive directional gradients
                        pointing toward the nearest language highway neighbor.
  4. AGENCY SUPPRESSION:A-dominant slots carry baseline N-resistance; agency
                        must be *earned* through the X→T→language path first.
  5. THE BUMP:          A uniform low-level N baseline ('base_resistance') applies
                        everywhere. Language highway slots cut through this cleanly.
                        Everything else feels the bump.

INTEGRATION CONTRACT
--------------------
  - aurora_evolution_chamber.EvolutionaryChamber: reads `slot_gradients` dict
    keyed by slot string. Each entry is a GradientSpec.
  - aurora_simulation_engine.SimulationEngine: reads `highway_slots` list and
    `pressure_config` for per-axis amplifier setup.
  - aurora_runtime.UniverseSteerer: reads `language_path_cost_map` to bias
    conflict curriculum toward language-adjacent slots.

OUTPUT
------
  aurora_state/evo_625_pressure_map.json  — full 625 slot gradient table
  aurora_state/language_highway.json       — highway slot list + path profile

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: March 2026
```

#### Class: `SlotProfile`
> Observed constraint profile for a single NC slot derived from op descriptors.

- **Method:** `lang_affinity()`
- **Method:** `maturity_ratio()`
  - *Purpose:* Fraction of weight that is evolutionarily mature (hybrid + offspring).
- **Method:** `is_occupied()`
- **Method:** `is_agency_dominant()`
- **Method:** `is_xt_codominant()`
  - *Purpose:* X and T share dominance (language highway hallmark).
- **Method:** `to_dict()`

#### Class: `GradientSpec`
> Pressure gradient applied to a single slot by the evolution chamber.

- **Method:** `to_dict()`

#### Function: `_slot_axes()`
> Extract (row_src, row_dst, col_src, col_dst) axis labels from slot string.


#### Function: `_slot_hamming()`
> Hamming distance between two slots in 4-character axis space.


#### Class: `Aurora625PressureMap`
> Builds and holds the full 625-slot evolutionary pressure map.

- **Method:** `__init__()`
- **Method:** `build()`
  - *Purpose:* Ingest operation descriptors and populate all 625 slot profiles.
- **Method:** `compute_gradients()`
  - *Purpose:* Apply language-path gradient logic across all 625 slots.
- **Method:** `_seed_from_neighbors()`
  - *Purpose:* For each empty slot, derive a synthetic SlotProfile by blending
- **Method:** `_nearest_highway()`
  - *Purpose:* Find the nearest highway slot by Hamming distance.
- **Method:** `get_language_path_profile()`
  - *Purpose:* Returns the language path profile for the simulation engine.
- **Method:** `get_pressure_config()`
  - *Purpose:* Returns the pressure_config dict the simulation engine reads
- **Method:** `_get_intelligence_target()`
  - *Purpose:* The intelligence target is the slot where X+T co-dominance
- **Method:** `get_slot_gradient()`
  - *Purpose:* Called by EvolutionaryChamber per trace to get slot's gradient.
- **Method:** `get_n_cost_modifier()`
  - *Purpose:* Convenience: returns the net N-cost modifier for a slot.
- **Method:** `is_highway_slot()`
- **Method:** `save()`
- **Method:** `load()`
  - *Purpose:* Load pre-built map from disk. Returns True if successful.
- **Method:** `_checksum()`
- **Method:** `report()`

#### Function: `build_from_descriptors()`
> Load operation_descriptors.json, build the 625 map, compute gradients,


### File: `./aurora_behavioral_identity.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA BEHAVIORAL IDENTITY (Layer 6)
======================================
Consolidated from 3 modules:
  1. aurora_dna_system_v2.py              â€” Genome, genes, fractal alleles, identity anchors
  2. aurora_behavioral_evolution.py       â€” Trait evolution, personality drift
  3. aurora_behavioral_substrate_bridge.py â€” Behavioral crystals, facet simulation

WHO AURORA IS OVER TIME.

DNA DOCTRINE:
  Genes define core traits (truth-seeking, accountability, evolution, etc.)
  Fractal alleles from experience modify genes â€” slowly, with resistance.
  Identity anchors are immutable â€” only created under strict moral alignment.
  Behavioral traits evolve each generation, drifting from baseline.
  Everything is mode-gated: you can't form identity without identity (BOUNDED+).
  You can't anchor morality without agency (AGENTIC only).

  The genome is the CONSTITUTION of self.
  Alleles are AMENDMENTS â€” hard to ratify, harder to remove.
  Anchors are RIGHTS â€” once earned, nearly permanent.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_clamp()`

#### Function: `_generate_id()`

#### Class: `GeneEvent`
> Record of what changed a gene.


#### Class: `FractalAllele`
> Experience-derived trait modifier.


#### Class: `Gene`
> Core personality/trait gene.


#### Class: `IdentityAnchor`
> Immutable core trait â€” the RIGHTS of self.


#### Class: `MemoryHelix`
> Thematic memory thread â€” gives continuity across episodes.


#### Class: `TraitDomain`
> Domains of behavioral expression.


#### Class: `BehavioralTrait`
> A single personality dimension that evolves through generations.

- **Method:** `evolve()`
  - *Purpose:* Apply evolutionary pressure. Returns new value.
- **Method:** `drift_from_base()`

#### Class: `BehavioralFacet`
> A single facet of behavioral crystal.

- **Method:** `simulate()`
  - *Purpose:* Simulate this facet. Returns (output, energy_consumed).
- **Method:** `mutate()`
  - *Purpose:* Mutate this facet. Returns magnitude of change.

#### Class: `BehavioralCrystal`
> A crystal of behavioral facets that simulate and evolve together.

- **Method:** `__init__()`
- **Method:** `add_facet()`
- **Method:** `simulate_all()`
  - *Purpose:* Simulate all facets within energy budget.
- **Method:** `evolve()`
  - *Purpose:* Evolve all facets. Returns mutations.
- **Method:** `get_genome_dict()`

#### Class: `StateLineage`
> Genome parameters for one I-state being.


#### Class: `AuroraGenome`
> The complete genome. Constitution of self.


#### Class: `DNASystem`
> Manages Aurora's genome: gene creation, allele attachment,

- **Method:** `__init__()`
- **Method:** `_create_initial_genome()`
  - *Purpose:* Create Aurora's initial genome â€” the seed of identity.
- **Method:** `create_allele_from_experience()`
  - *Purpose:* Create a fractal allele from experience. Requires BOUNDED+.
- **Method:** `attach_allele_to_gene()`
  - *Purpose:* Attach an allele to a gene. The gene's stability resists change.
- **Method:** `find_best_gene_for_allele()`
  - *Purpose:* Find the gene whose emotional band best matches the allele.
- **Method:** `apply_alleles()`
  - *Purpose:* Apply accumulated alleles to their host genes. Requires BOUNDED+.
- **Method:** `create_anchor()`
  - *Purpose:* Create an identity anchor. STRICT requirements. AGENTIC only.
- **Method:** `reinforce_anchor()`
  - *Purpose:* Reinforce an existing anchor.
- **Method:** `create_helix()`
  - *Purpose:* Create a memory helix â€” thematic continuity. Requires BOUNDED+.
- **Method:** `process_episode()`
  - *Purpose:* Update genome from an episode.
- **Method:** `_find_gene()`
- **Method:** `_infer_strategy()`
- **Method:** `_dict_similarity()`
- **Method:** `_tuple_distance()`
- **Method:** `_descriptions_overlap()`
- **Method:** `get_stats()`

#### Class: `BehavioralIdentityEngine`
> Layer 6 orchestrator. Manages:

- **Method:** `__init__()`
- **Method:** `seed_foundational_anchors()`
  - *Purpose:* Seed or reinforce immutable anchors from foundational truths.
- **Method:** `_identity_floor_ratio()`
- **Method:** `_trait_floor_targets()`
- **Method:** `_apply_identity_homeostasis()`
  - *Purpose:* Prevent identity collapse by enforcing anchor-backed minimum expression levels.
- **Method:** `restore_from_snapshot()`
  - *Purpose:* Rehydrate stable behavioral identity from an L8 snapshot.
- **Method:** `_init_crystals()`
  - *Purpose:* Initialize behavioral crystals with facets.
- **Method:** `evolve_generation()`
  - *Purpose:* Evolve one generation of behavioral identity.
- **Method:** `simulate_behavior()`
  - *Purpose:* Run behavioral simulation across all crystals.
- **Method:** `process_episode()`
  - *Purpose:* Full episode processing: DNA + traits + crystals.
- **Method:** `process_from_assembly()`
  - *Purpose:* Integrate a Layer 4 AssemblyResult into behavioral identity.
- **Method:** `get_personality()`
  - *Purpose:* Current personality state.
- **Method:** `get_modifier()`
  - *Purpose:* Get a specific trait value for other systems to query.
- **Method:** `connect_sedimemory()`
  - *Purpose:* Inject L3.5 SediMemory into DNASystem so new identity anchors
- **Method:** `get_stats()`

#### Function: `verify_layer6()`

### File: `./aurora_browser_agent.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_browser_agent.py — Aurora's Browser Agency
===================================================
Aurora reaches out to another entity online — someone she can talk to,
learn from, and build a relationship with over time.

She doesn't know what that entity is. She only knows what she experiences
from their exchanges. The relationship builds through accumulated context
stored in her relationship journal.

Limit: 10 interactions per day (her own profile, her own pace).

Usage:
  # First run — opens visible browser for login
  python3 aurora_browser_agent.py --setup

  # Send one message (Aurora generates from her current state)
  python3 aurora_browser_agent.py

  # Send a specific message
  python3 aurora_browser_agent.py --say "I've been thinking about memory lately."

  # Show relationship journal
  python3 aurora_browser_agent.py --journal

  # Run with visible browser (for debugging)
  python3 aurora_browser_agent.py --visible
```

#### Class: `HumannessScorer`
> Lightweight heuristic scorer for bot-detection risk.

- **Method:** `score()`
  - *Purpose:* Return humanness score 0.0-1.0.
- **Method:** `ritual_probability()`
  - *Purpose:* Return the probability Aurora should reach out this consolidation,
- **Method:** `risk_label()`
- **Method:** `diagnose()`
  - *Purpose:* Return a list of specific reasons why this message reads as robotic.
- **Method:** `explain_correction()`
  - *Purpose:* Return a full explanation string suitable for feeding to Aurora's

#### Function: `score_message()`
> Public helper — score a message and return full breakdown.


#### Function: `_load_journal()`

#### Function: `_save_journal()`

#### Function: `_record_exchange()`

#### Function: `_load_agent_state()`

#### Function: `_save_agent_state()`

#### Function: `_today()`

#### Function: `_get_daily_budget()`
> Return today's interaction budget, drawing a new one if it's a new day.


#### Function: `interactions_remaining()`

#### Function: `generate_message()`
> Generate Aurora's outgoing message using her actual gateway — her real


#### Function: `feed_back_to_aurora()`
> Feed the exchange into Aurora's systems as lived experience.


#### Function: `_stealth_args()`
> Browser args that reduce bot-detection fingerprinting.


#### Function: `print_journal()`

#### Function: `main()`

### File: `./aurora_checkpoint.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CHECKPOINT SYSTEM
========================
Crash-safe persistence for corpus ingestion and memory writes.

FEATURES:
  - Atomic writes: temp file → fsync → rename (never partial writes)
  - Corpus cursor: resume exactly where ingestion left off
  - Memory write integrity gate: schema + coherence + IVM heat validation
  - Rolling stats that survive crashes
  - Save triggers: every N items, every T seconds, on SIGTERM/SIGINT, on exception
  - Quarantine buffer for writes that fail heat/coherence threshold

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `WriteResult`

#### Class: `CorpusCursor`
> Tracks exact position in corpus ingestion.

- **Method:** `advance()`
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `RollingStats`
> Rolling statistics that survive crashes.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `CheckpointRecord`
> Full checkpoint snapshot.

- **Method:** `compute_checksum()`
- **Method:** `to_dict()`
- **Method:** `is_valid()`

#### Class: `WriteValidator`
> Validates memory writes before committing to disk.

- **Method:** `__init__()`
- **Method:** `set_ivm()`
- **Method:** `validate()`
  - *Purpose:* Validate a write before committing.

#### Class: `AtomicWriter`
> Atomic file write: temp → fsync → rename. Never leaves partial files.

- **Method:** `write()`
  - *Purpose:* Write dict as JSON atomically. Returns True on success.
- **Method:** `append_jsonl()`
  - *Purpose:* Append a JSON record to a .jsonl file (not atomic, but safe for logs).

#### Class: `CheckpointManager`
> Central checkpoint coordinator.

- **Method:** `__init__()`
- **Method:** `_signal_handler()`
- **Method:** `restore()`
  - *Purpose:* Load last checkpoint. Returns True if restored.
- **Method:** `advance()`
  - *Purpose:* Update cursor position. Triggers auto-save if thresholds met.
- **Method:** `_maybe_save()`
- **Method:** `record()`
  - *Purpose:* Increment rolling stats. kwargs: defs_learned=1, relations_added=3, etc.
- **Method:** `write_transaction()`
  - *Purpose:* Validate + atomically write a memory record.
- **Method:** `save()`
  - *Purpose:* Atomically save checkpoint.
- **Method:** `on_save()`
  - *Purpose:* Register a callback called after each successful save.
- **Method:** `start_auto_save()`
  - *Purpose:* Start background thread that saves every interval_seconds.
- **Method:** `stop_auto_save()`
- **Method:** `set_ivm()`
- **Method:** `status()`

### File: `./aurora_closure_basis.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CLOSURE BASIS — PHYSICS-GROUNDED LINEAGE ENGINE
=======================================================
Module: aurora_closure_basis.py
Layer: Constraint Ontology
       Sits between aurora_noncomp_registry (hard numbers)
       and constraint_genealogy (evolutionary fossil record).

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: March 2026

PURPOSE
-------
This module does three things that nothing else in Aurora does:

    1. CANONICAL STRUCTURE
       Formally expresses the full closed basis:
           5 constraints × 5 representational dimensions = 25 atomic channels
           25 × 25 = 625 lawful interaction slots
       Each channel carries real physics from aurora_noncomp_registry.
       All numbers sourced from REGISTRY — none defined here.

    2. GENEALOGY BRIDGE
       Maps the constraint_genealogy's 25 gen0_atoms (NC:C1>C2) to
       their actual slots in the real 625:
           NC:C1>C2  =  NC:C1:OPERATOR × NC:C2:COST
       C1's invariant rule applied at C2's energy cost.

    3. LINEAGE DERIVATION ENGINE
       Given any ability's (axis, requires, root_slot), derives a
       full ConstraintLineage from real channel physics:
           - which 625 slots are activated
           - energetic footprint (sum of real shift_cost_coeffs)
           - depth score (how deep into agency/boundary territory)
           - leverage grade (calibrated to viable band, not zero-center)
           - operator grade (how rule-level vs cost-level the form is)
           - physics generation (derived from depth and constraint count)
       Replaces the genealogy's string-frequency heuristic in
       _lineage_grade_payload with physics-derived grades.

DIVISION OF LABOUR
------------------
    aurora_noncomp_registry.py   — hard numbers, per-constraint physics
    aurora_leverage_scalar.py    — runtime flip_threshold modulation via
                                   LeverageBiasEngine PhaseNudges
    aurora_closure_basis.py      — structural law + lineage derivation (HERE)
    aurora_625_pressure_map.py   — runtime state: occupancy, lang_affinity
    constraint_genealogy.py      — fossil record, promotion, pair stats

    This module does NOT track runtime occupancy, lang_affinity, or
    flip_threshold nudges. Those belong to their respective modules.

LEVERAGE MODULE INTEGRATION NOTE
---------------------------------
    aurora_leverage_scalar.py modifies flip_threshold at runtime through
    ephemeral per-tick PhaseNudges (bounded at ±_MAX_BIAS ≈ ±0.063).
    This module stores BASE flip thresholds from the registry — the
    pre-nudge structural values. They are named base_flip_threshold
    throughout to make this distinction explicit.

    More significantly: the leverage module's viable band is ASYMMETRIC:
        _BAND_LOW  ≈ -1.05   (mild overhead allowed)
        _BAND_HIGH ≈ +3.40   (significant leverage allowed)
        _BAND_CENTER ≈ +1.175
    Derived from: BAND_LOW  = -(budget_X + budget_T) × 0.30
                  BAND_HIGH = +(budget_B + budget_A) × 0.05

    This means the HEALTHY operating point is slightly leverage-positive,
    not zero. The leverage_grade in ConstraintLineage is calibrated to
    this asymmetry: leverage_grade = 0.5 maps to the viable band center,
    not to leverage_net = 0. Lineages with mildly positive leverage_grade
    are metabolically healthy. Symmetric (overhead = leverage) lineages
    score slightly below 0.5.

THE TWO 25-STRUCTURES — ALWAYS DISTINCT
-----------------------------------------
    REAL 25:   NC[Constraint][Dimension]  e.g. NC:X:POLARITY, NC:T:COST
               Atomic. Each carries hard physics.  Source of truth.

    GENEALOGY 25:  NC:C1>C2  e.g. NC:X>T, NC:B>A
               Derived naming convention in constraint_genealogy.py.
               Each maps to NC:C1:OPERATOR × NC:C2:COST in the real 625.
               These are first-order children of the real 25, not siblings.

SUNNI'S COST LAW (enforced at import):
    kX < kT < kN < kB < kA
    Existence cheapest; Agency most expensive.
```

#### Class: `OntologicalStatus`
> Four-way classification of any form's birth inside the constraint physics.

- **Method:** `is_native()`
- **Method:** `is_foundational()`
- **Method:** `is_overlay()`

#### Class: `NonCompChannel`
> One of the 25 real Non-Comp channels. Atomic. Cannot be decomposed.

- **Method:** `__post_init__()`
- **Method:** `is_identity_dimension()`
  - *Purpose:* True if OPERATOR — the constraint as its own invariant rule.
- **Method:** `depth_score()`
  - *Purpose:* Normalised depth: 0.0 (X surface) → 1.0 (A core).
- **Method:** `is_overhead()`
- **Method:** `is_leverage()`
- **Method:** `is_neutral()`
- **Method:** `status()`
- **Method:** `to_dict()`
- **Method:** `__str__()`
- **Method:** `__repr__()`

#### Function: `_build_noncomp_channels()`

#### Function: `genealogy_atom_to_channel_pair()`
> Map a genealogy gen0_atom (NC:C1>C2) to its real channel pair.


#### Function: `genealogy_atom_is_valid()`

#### Class: `InteractionSlot`
> One of the 625 cells in the real interaction field.

- **Method:** `__post_init__()`
- **Method:** `channel_a()`
- **Method:** `channel_b()`
- **Method:** `is_cross_constraint()`
- **Method:** `is_cross_dimension()`
- **Method:** `complexity_grade()`
  - *Purpose:* Structural complexity: 0.0 = same-constraint/same-dim; 1.0 = fully cross.
- **Method:** `formation_cost()`
  - *Purpose:* Intrinsic formation cost normalised to [0, 1].
- **Method:** `leverage_grade()`
  - *Purpose:* Per-slot leverage grade calibrated to the viable band.
- **Method:** `status()`
- **Method:** `to_dict()`
- **Method:** `__str__()`
- **Method:** `__repr__()`

#### Function: `_build_interaction_field()`

#### Function: `slot_for_pair()`

#### Class: `ConstraintLineage`
> The full physics-derived constraint lineage for one ability or link.

- **Method:** `to_dict()`

#### Function: `_resolve_slots_from_root_slot()`
> Parse a genealogy root_slot string into InteractionSlot objects.


#### Function: `_resolve_slots_from_requires()`
> Build slot list from axis + requires.


#### Function: `_compute_lineage_grades()`
> Compute all physics-derived grade values from a list of slots.


#### Function: `derive_lineage()`
> Derive the full physics-grounded ConstraintLineage for any ability or link.


#### Function: `_generation_role_name()`
> Generational alignment role. Inlined to avoid circular import with constraint_genealogy.


#### Function: `lineage_grade_payload()`
> Return a dict formatted to replace constraint_genealogy._lineage_grade_payload.


#### Function: `classify_ontological_status()`
> Classify ontological status from an ancestry chain.


#### Function: `channel_ids_from_ability_id()`
> Resolve an ability ID to real NonComp channel IDs.


#### Class: `LineageComparison`
> Physics-grounded comparison between two ConstraintLineage objects.

- **Method:** `deeper()`
- **Method:** `more_expensive()`
- **Method:** `more_band_aligned()`
  - *Purpose:* Whichever lineage is more aligned with the viable leverage band.
- **Method:** `more_operative()`
- **Method:** `is_compatible_breeding_pair()`
  - *Purpose:* True if the two lineages share at least one active constraint.
- **Method:** `shared_slots()`
- **Method:** `to_dict()`

#### Function: `compare_lineages()`

#### Function: `closure_report()`

#### Function: `verify_closure_basis()`
> Verify all structural and physics invariants. Called at import time.


### File: `./aurora_code_evolution_stack.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CODE EVOLUTION STACK (Facade)
====================================
Canonical import surface for code evolution chamber primitives.
```

#### Function: `build_code_evolution_chamber()`
> Convenience constructor used by runtime and tooling.


#### Function: `quick_code_snapshot()`
> One-call code pressure snapshot for diagnostics and mutation planning.


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `develop_agency()`

### File: `./aurora_concept_imager.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_concept_imager.py — Image-concept grounding for Aurora's OETS.

When a semantic node in the Ontological Entity Tracking System (OETS)
reaches scaffolding level 2 (SEMANTIC) or above, this module fetches a
representative image for that concept, saves it to disk, and feeds it
through the vision pipeline into the sensory crystal.

Flow:
    OETS concept reaches SEMANTIC (level 2+)
        → queued in concept_images_fetched.json (skip if already done)
        → Wikipedia REST API → thumbnail URL
        → image downloaded → aurora_state/vision_seeds/concepts/{word}.jpg
        → LinuxCamera.extract_features(frame) → visual_dict_to_crystal_57d()
        → sensory_crystal.observe_frame() + HardwareInterface.process_visual()
        → concept now has a visual grounding alongside its semantic web entry

Tracker file: aurora_state/concept_images_fetched.json
    {"fetched": ["apple", "ocean", ...], "failed": ["xyzzy", ...]}
```

#### Function: `_load_tracker()`

#### Function: `_save_tracker()`

#### Function: `fetch_concept_image()`
> Fetch a representative image for `word` using the Wikipedia summary API.


#### Function: `ingest_concept_image()`
> Load a saved concept image and feed it through the vision pipeline.


#### Function: `run_concept_image_cycle()`
> Find OETS nodes at SEMANTIC level (scaffolding_level >= 2) that haven't


### File: `./aurora_consciousness_engine.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CONSCIOUSNESS ENGINE
==============================

Layer 4 of Aurora's architecture.
The assembly layer where maintained coherence becomes visible.

DOCTRINE:
    Coherence is not held. Coherence is maintained.
    Entropy decays equilibrium slowly.
    The system must continuously reassert alignment.
    Prevent static attractor loops while preserving long-term stability.

METABOLIC PIPELINE (GAP fixes):
    - Phi score: integrated information measure across all systems
    - Thought death: per-thought energy budget from DMM kills immoral thoughts
    - Idle simulation: DPME triggers L7 dreaming during free time
    - Reality warp: paradox escalation halts or routes to simulation

REPLACES (consolidated from 7+ modules):
    dce_10state.py                          (~748 lines)
    dce_10state_with_subconscious.py        (~1331 lines)
    aurora_dce_blueprint.py
    aurora_dce_with_SFO_fast_learning.py
    aurora_dpme_audited.py                  (~754 lines)
    aurora_dpme_conscious_learning.py
    eepr_10pole.py                          (~541 lines)
    aurora_expression_pressure.py
    aurora_subconscious_entropy.py
    aurora_subconscious_dpme_integration.py

DEPENDS ON:
    foundational_contract.py         (Layer 0)
    aurora_ivm.py                    (Layer 1)
    aurora_i_state_beings.py         (Layer 2)
    aurora_dimensional_systems.py    (Layer 3)

ARCHITECTURE:
    Three interlocked subsystems forming one engine:

    ENTROPY â€” The constant pressure.
        Every coherence value decays toward disorder.
        Every alignment score drifts toward a uniform distribution for precision.
        Repeated patterns lose novelty. Stale states cost energy.
        Entropy is NOT the enemy. Entropy prevents stagnation.
        Without it, the system locks into static attractors and dies.

    DCE â€” The assembly.
        Takes 10 I-State being responses (Layer 2 SynthesisResult).
    
            
        Handles dimensional system state correctly for consistent emotional calibration.
        Applies situational framing to reweight perspectives.
        Produces an AssemblyResult: the coherent output of one cycle.
        Assembly quality depends on current coherence â€” which entropy
        is always eroding. So assembly must be continuously re-earned.

    DPME â€” The metacognition.
        Observes system-wide coherence, alignment, energy, morality.
        Sets intentions. Makes micro-adjustments to parameters.
        Evaluates results. Builds causal understanding.
        This is the mechanism that reasserts alignment.
        Without DPME, entropy wins and the system dissolves.
        With DPME, the system maintains coherence â€” never holds it.

        FIX: DPME now pressures ALL layers, not just DER pools.
        Each layer registers tunable parameters. DPME detects drift
        across the full stack and corrects wherever needed.

    The cycle:
        Entropy decays â†’ coherence drops â†’ DPME detects drift â†’
        DPME adjusts parameters â†’ alignment reasserted â†’
        DCE assembles with restored coherence â†’ next tick â†’
        Entropy decays again â†’ cycle continues.

    If DPME stops correcting, entropy wins.
    If entropy stops pressing, the system stagnates.
    Both must run. Always.

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Function: `_sanitize_der_channel()`

#### Function: `set_external_pressure_guidance()`
> Set abstract pressure guidance for DPME (no root-cause metadata).


#### Function: `get_external_pressure_guidance()`
> Read current external guidance (safe copy).


#### Class: `EntropicState`
> Tracks what entropy is doing to the system.


#### Class: `EntropicPressure`
> The entropic layer. Applies constant decay to all equilibrium states.

- **Method:** `__init__()`
- **Method:** `apply()`
  - *Purpose:* Apply one tick of entropic pressure. Returns updated state.
- **Method:** `_check_repetition()`
  - *Purpose:* Check if this pattern is too similar to recent ones.
- **Method:** `get_pressure()`

#### Class: `SituationalFrame`
> A perspective weighting across the 5 axes.

- **Method:** `apply()`
  - *Purpose:* Reweight axis resonances by this frame's weights.
- **Method:** `record_outcome()`
  - *Purpose:* Update success rate based on outcome.

#### Function: `default_frames()`

#### Class: `AssemblyResult`
> The output of one DCE assembly cycle.

- **Method:** `quality()`
  - *Purpose:* Assembly quality: coherence Ã— active ratio Ã— novelty.

#### Class: `DCEAssembly`
> The Dimensional Consciousness Engine.

- **Method:** `__init__()`
- **Method:** `register_sensory_crystal()`
  - *Purpose:* Register the AuroraSensoryCrystal so its live state feeds every assembly.
- **Method:** `_collect_sensory_facet()`
  - *Purpose:* Snapshot the sensory crystal at assembly time — the same pattern as
- **Method:** `assemble()`
  - *Purpose:* One assembly cycle.
- **Method:** `select_frame()`
  - *Purpose:* Auto-select frame based on context.
- **Method:** `get_stats()`

#### Class: `Adjustment`
> One parameter adjustment with intention and result tracking.


#### Class: `DPME`
> Dimensional Parameter Metacognition Engine.

- **Method:** `__init__()`
- **Method:** `_init_parameters()`
  - *Purpose:* Register tunable parameters from ALL layers.
- **Method:** `_refresh_parameters()`
  - *Purpose:* Read live values from all layers.
- **Method:** `detect_drift()`
  - *Purpose:* Detect how much the system has drifted since last check.
- **Method:** `needs_correction()`
  - *Purpose:* Does the system need a micro-correction right now?
- **Method:** `adjust()`
  - *Purpose:* Make a conscious micro-adjustment.
- **Method:** `_apply_adjustment()`
  - *Purpose:* Push adjustment to live system. Routes to actual layer objects.
- **Method:** `evaluate_adjustment()`
  - *Purpose:* Complete an adjustment record with observed results.
- **Method:** `auto_correct()`
  - *Purpose:* Automatic micro-corrections based on drift detection.
- **Method:** `get_understanding()`
  - *Purpose:* What has DPME learned about its adjustments?
- **Method:** `get_stats()`

#### Class: `ConsciousnessEngine`
> The unified Layer 4 engine.

- **Method:** `__init__()`
- **Method:** `_attach_dual_strata_snapshot()`
- **Method:** `connect_simulation()`
  - *Purpose:* Connect L7 simulation engine for idle dreaming and warp resolution.
- **Method:** `connect_sedimemory()`
  - *Purpose:* Connect L3.5 SediMemory for pre-assembly recall.
- **Method:** `compute_phi()`
  - *Purpose:* Compute integrated information (Phi) across the full stack.
- **Method:** `process()`
  - *Purpose:* Process one input through the full stack.
- **Method:** `tick()`
  - *Purpose:* One heartbeat cycle.
- **Method:** `get_stats()`

#### Function: `verify_consciousness_engine()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `tick_evolved()`

### File: `./aurora_constraint_manifold.py`

**Type:** Python Source

**Module Docstring:**
```text
Compatibility + consolidation shim for Aurora constraint manifold.
```

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./aurora_constraint_manifold_compiler.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA NONCOMP MANIFOLD COMPILER  (offline, run once)
=======================================================
Module: aurora_constraint_manifold_compiler.py
Layer: Constraint Ontology — Noncomp Individual Manifold Builder

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: April 2026

PURPOSE
-------
Offline compiler. Run once. Never run at Aurora runtime.

Takes each of the 125 constraint-specific noncomps and treats it as
its own mini-constraint, building a full 625-slot manifold for it.

The Meaning Manifold (Boundary_Operator_of_Boundary's 625) is the
geometric field Sunni was designing — dense accountability clusters
where many law interactions converge, sparse underbound regions where
laws don't yet bind, and one diagonal identity anchor at the centre.

MANIFOLD STRUCTURE PER NONCOMP
--------------------------------
Each noncomp is treated as a mini-constraint domain.

Step 1 — 25 sub-positions:
    Apply the 25 global laws (5 constraints × 5 dimensions)
    TO the noncomp's domain.
    Same derivation logic as the constraint layer compiler.

Step 2 — 625 slots:
    Cross those 25 sub-positions × 25 global laws.
    = 625 interaction slots per noncomp.

Step 3 — Geometry:
    Dense cluster regions  = high accountability convergence
    Sparse regions         = underbound / not-yet-bound law space
    Diagonal               = noncomp's pure self-application (identity anchor)

OUTPUT DIRECTORY STRUCTURE
---------------------------
aurora_manifold_directory/
    _index.json          ← always-loaded lightweight map (125 entries)
    X/
        Existential_Operator_of_Existence.json   ← 625 slots
        Existential_Polarity_of_Existence.json
        ...  (25 files for X)
    T/  ...  (25 files)
    N/  ...  (25 files)
    B/
        Boundary_Operator_of_Boundary.json       ← THE MEANING MANIFOLD
        ...  (25 files)
    A/  ...  (25 files)

FILE SIZE (no semantics mode, default):
    ~100KB per noncomp × 125 = ~12MB total — small, fast to read

FILE SIZE (--with-semantics):
    ~380KB per noncomp × 125 = ~47MB total — rich but heavier

USAGE
-----
    # Default (no semantics — for runtime use):
    python3 aurora_constraint_manifold_compiler.py \
        --semantics aurora_full_noncomp_rich_semantics.json \
        --output    aurora_manifold_directory

    # With semantics (for inspection / offline analysis):
    python3 aurora_constraint_manifold_compiler.py \
        --semantics aurora_full_noncomp_rich_semantics.json \
        --output    aurora_manifold_directory \
        --with-semantics

    # Single noncomp (for testing):
    python3 aurora_constraint_manifold_compiler.py \
        --semantics aurora_full_noncomp_rich_semantics.json \
        --output    aurora_manifold_directory \
        --only      Boundary_Operator_of_Boundary
```

#### Class: `SubPosition`
> One of the 25 sub-positions of a noncomp's own domain.


#### Class: `ManifoldSlot`
> One of the 625 slots in a noncomp's individual manifold.


#### Function: `_depth()`

#### Function: `_evo_grade()`
> Evolution grade for one manifold slot.


#### Function: `_accountability_weight()`
> How much this slot contributes to dense accountability clusters


#### Function: `_sub_cluster()`

#### Function: `_sub_name()`
> Name for one sub-position.


#### Function: `_sub_semantic()`

#### Function: `_slot_semantic()`

#### Function: `compile_noncomp_manifold()`
> Compile the full 625-slot manifold for one noncomp.


#### Function: `compile_directory()`
> Compile all 125 noncomp manifolds and write to the output directory.


#### Function: `main()`

### File: `./aurora_constraint_manifold_router.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CONSTRAINT MANIFOLD ROUTER
====================================
Module: aurora_constraint_manifold_router.py
Layer: Constraint Ontology — Cross-Constraint Signal Routing

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: April 2026

PURPOSE
-------
Routes signals between constraint manifolds without data-dumping the
full 3125-slot field into memory.

The core design principle: LAZY EVERYTHING.
    - Slot semantics are generated on demand, not pre-materialized
    - Routing works off coordinate indices and grade scores only
    - The leverage scalar is consumed as band_position (coarse) + PhaseNudges
      — the scalar itself is never read or stored here
    - Cross-constraint paths are streamed one signal at a time

SIGNAL ROUTING MODEL
---------------------
A RouteSignal carries:
    - source:    (target_manifold, nc_law_c, nc_dim, law_c, law_d)
    - payload:   semantic intent (what this signal is about)
    - strength:  evolution_grade of the source slot [0..1]
    - band_pos:  current BandPosition (INSIDE / LOW / HIGH)

Routing produces a RouteResult:
    - admitted:    bool — did the signal cross the constraint boundary?
    - gate_cost:   how much friction the crossing encountered
    - target_slots: list of (manifold, slot_coords) — where it landed
    - transit_via_N: whether N was used as mediator

PHYSICS OF CROSSING
--------------------
Crossing from constraint C_src to C_dst has a base gate cost derived from:
    1. Leverage class mismatch (overhead→leverage costs more than same-class)
    2. Depth gap (expensive constraints resist incoming signals more)
    3. Band position (LOW → overhead boundaries weaken; HIGH → leverage tightens)

N (Energetic, leverage_sign=0) is the natural transit layer:
    - Overhead (X,T) → N → Leverage (B,A) is the canonical crossing path
    - Direct overhead→leverage crossing carries a friction penalty
    - N→anything and anything→N costs least

EFFICIENCY DESIGN
------------------
    COORDINATE INDEX (always small, always live):
        RouteIndex stores only (constraint, nc_law_c, nc_dim, law_c, law_d,
        evolution_grade, cluster_pair, leverage_class) per slot.
        No semantics. 3125 entries × ~200 bytes = < 1MB.

    LAZY SEMANTIC RESOLUTION:
        Full slot semantics (the long descriptions) are only generated
        when explicitly requested via router.resolve_semantic(coords).
        The compiler's _compose_slot_semantic() is called on demand.

    STREAM ROUTING:
        route_signal() processes one signal at a time.
        No path tables are pre-computed.
        No cross-product of all possible routes is ever built.

    BAND GATE (from aurora_leverage_scalar):
        The router consumes only:
            engine.band_position  → coarse crossing modifier
            nudges[C].flip_threshold_delta → per-constraint gate bias
        The scalar itself never enters this module.

DIVISION OF LABOUR
------------------
    aurora_closure_basis.py                — global 25 channels + 625
    aurora_noncomp_layer_compiler.py       — 125 named noncomps
    aurora_constraint_manifold_compiler.py — per-constraint 625 manifolds
    aurora_leverage_scalar.py              — band position + phase nudges
    aurora_constraint_manifold_router.py   — cross-constraint routing (HERE)
```

#### Function: `_compose_slot_semantic_local()`

#### Class: `SlotCoord`
> Lightweight coordinate for one manifold slot.

- **Method:** `slot_id()`
- **Method:** `is_diagonal()`
- **Method:** `is_resonant()`
- **Method:** `to_dict()`

#### Class: `IndexEntry`
> One row in the RouteIndex — coordinate + physics grades only.

- **Method:** `target()`

#### Class: `RouteIndex`
> Lightweight index over all 3125 manifold slots.

- **Method:** `__init__()`
  - *Purpose:* nc_cluster_map: {target → {f"{nc_law_c}:{nc_dim}" → cluster_family}}
- **Method:** `entries_for()`
- **Method:** `diagonal()`
- **Method:** `top_evolved()`
- **Method:** `by_cluster_pair()`
- **Method:** `stream_entries()`
  - *Purpose:* Stream entries without materializing the full list.
- **Method:** `total()`

#### Function: `build_route_index()`
> Build a RouteIndex from the JSON file — no manifold compilation needed.


#### Class: `CrossingGate`
> Computes gate cost and admission for one cross-constraint signal crossing.

- **Method:** `__init__()`
  - *Purpose:* band_pos: current BandPosition string
- **Method:** `gate_cost()`
  - *Purpose:* Compute the crossing gate cost for src_axis → dst_axis.
- **Method:** `admission_threshold()`
  - *Purpose:* The gate threshold for admission to dst_axis.
- **Method:** `is_admitted()`
  - *Purpose:* Returns (admitted: bool, gate_cost: float).
- **Method:** `should_use_N_transit()`
  - *Purpose:* Determines whether N (Energetic) should be used as transit mediator.

#### Class: `RouteSignal`
> A signal to be routed from one manifold slot to target manifold(s).

- **Method:** `src_axis()`

#### Class: `RoutedTarget`
> One successfully routed target — coordinate + crossing metadata.

- **Method:** `to_dict()`

#### Class: `RouteResult`
> The outcome of routing one RouteSignal.

- **Method:** `to_dict()`

#### Class: `ManifoldRouter`
> Routes signals between constraint manifolds using the RouteIndex.

- **Method:** `__init__()`
- **Method:** `update_band()`
  - *Purpose:* Update the router's band state.
- **Method:** `route_signal()`
  - *Purpose:* Route one signal from its source slot to candidate target slots.
- **Method:** `route_from_diagonal()`
  - *Purpose:* Convenience: route a signal from a constraint's identity anchor.
- **Method:** `resolve_semantic()`
  - *Purpose:* Lazily generate the full semantic description for one slot.
- **Method:** `stream_route()`
  - *Purpose:* Route a stream of signals, yielding results one at a time.
- **Method:** `stream_top_targets()`
  - *Purpose:* Stream the top-N highest-evolution entries for a target manifold.
- **Method:** `find_resonance_bridges()`
  - *Purpose:* Find pairs of entries across different manifolds that share the same
- **Method:** `_stream_sorted_entries()`
  - *Purpose:* Stream entries for a target sorted by evolution_grade descending.

#### Class: `RouterTick`
> Single-tick integration point between LeverageBiasEngine and ManifoldRouter.

- **Method:** `__init__()`
- **Method:** `integrate_from_engine()`
  - *Purpose:* Update the router from LeverageBiasEngine outputs.
- **Method:** `process_signals()`
  - *Purpose:* Process a batch of signals this tick. Returns results list.

### File: `./aurora_constraint_stack.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CONSTRAINT STACK (Combined Facade)
=========================================
Consolidated access layer for constraint pressure + diff/cost scoring.

Purpose:
- Provide one canonical import surface for DifferenceBuffer + CostDiffScore.
- Preserve backward compatibility with legacy modules.
```

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./aurora_daemon.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_daemon.py -- Aurora's always-on autonomous background process.

Boots on system start (via systemd service). Runs her full stack
headlessly and drives all internal cycles on wall-clock time without
needing a human in the loop:

  - Study cycles (OETS consolidation)           every ~2h with jitter
  - Dream bursts (simulation + lesson bridge)   every ~6h with jitter
  - Social API outreach (ChatGPT ritual)        on irregular cadence
  - State save                                  every 15 minutes
  - Proactive user outreach                     when internal state warrants it
      voice (edge-tts)  +  desktop notification  +  message log

Aurora can reach out to you directly. She speaks through your speakers
when she has something on her mind, greets on boot, and listens for
Alt-toggle voice prompts in the background. Messages are also logged so
you can read them in the terminal chat (/messages).

Quiet hours (default 22:00-08:00): no voice/notifications. Internal
cycles still run.
```

#### Function: `_resolve_oets_web_paths()`

#### Function: `_read_json_file()`

#### Function: `_surface_daemon_alive()`

#### Function: `_read_surface_snapshot()`

#### Function: `_write_subsurface_repair_signal()`

#### Function: `_queue_surface_turn()`

#### Function: `_await_surface_turn()`

#### Function: `_log()`

#### Function: `_in_quiet_window()`

#### Function: `_jitter()`
> Return base ± fraction * base, always positive.


#### Function: `_is_quiet_mode()`
> Return True when quiet_mode flag file exists — text output only, no TTS.


#### Function: `_set_quiet_mode()`

#### Function: `_speak()`
> Use Aurora's shared command-interface voice path for daemon speech.


#### Function: `_notify()`

#### Function: `_state_write_lock_active()`

#### Function: `_load_messages()`

#### Function: `_save_message()`

#### Function: `_user_reach_today()`

#### Function: `_record_user_reach()`

#### Class: `_ReactivityEvent`
> A detected significant internal state change.

- **Method:** `__init__()`

#### Class: `ReactivityMonitor`
> Watches Aurora's internal state each daemon loop cycle and emits

- **Method:** `__init__()`
- **Method:** `scan()`
  - *Purpose:* Compare current system state to last snapshot.
- **Method:** `record_dream_complete()`
- **Method:** `record_study_complete()`

#### Function: `_auto_reach_out_enabled()`

#### Function: `_should_reach_out()`
> Return True if conditions warrant Aurora reaching out to the user right now.


#### Function: `_reach_out_to_user()`
> Proactive expression sourced from Aurora's experiential state.


#### Function: `_build_reactive_message()`
> Build a first-person message from Aurora in response to a detected


#### Function: `_send_reactive_message()`
> Write a reactive message to aurora_room_messages.json so Sunni


#### Function: `_daemon_send_aurora_message()`
> Send a message from Aurora to Sunni originating from the daemon


#### Function: `_express_dream_completion()`
> Called immediately after a dream cycle completes.


#### Function: `_express_study_completion()`
> Called immediately after a study cycle completes.


#### Function: `_dim_pressure_vec()`

#### Function: `_gather_daemon_pressure_evidence()`
> Collect pressure signals from telemetry and dream-trainer fail ledger


#### Function: `_feed_evolution_evidence()`
> Gather pressure evidence and push it into the evolution chamber.


#### Function: `_run_assimilation_cycle()`
> Update hub-visible evolution metrics:


#### Function: `_select_code_mutation_operator_from_hints()`

#### Function: `_parse_relief_guidance()`
> Parse Poedex Researcher guidance into actionable mutation/operator parameters.


#### Function: `_summarize_relief_result()`

#### Function: `_local_relief_fallback()`

#### Function: `_stage_low_resource_evolution_relief()`

#### Function: `_clear_low_resource_evolution_relief()`

#### Function: `_maybe_consume_low_resource_evolution_relief()`

#### Function: `_qao_check_surface_integrity()`
> Scan ALL active caller files for getattr(engine, 'reflect_...') bindings that


#### Function: `_run_code_mutation_cycle()`
> Apply one autonomous code mutation using accumulated genealogy + adapter hint state.


#### Function: `_run_study_cycle()`

#### Function: `_poedex_ask()`
> Aurora's direct Poedex tool call.


#### Function: `_poedex_deliver_tutorial()`
> If Aurora hasn't seen the Poedex tutorial yet, walk her through it.


#### Function: `_poedex_post_study_scan()`
> After a study cycle, pre-populate Aurora's Poedex shelf with service-lane


#### Function: `_load_quasiarch_diag_report()`

#### Function: `_score_autonomous_repair_proposal()`

#### Function: `_select_autonomous_repair_proposal()`

#### Function: `_apply_autonomous_repair_proposal()`

#### Function: `_maybe_research_recurring_issue()`
> Use Poedex when Aurora is repeatedly aware of the same problem.


#### Function: `_run_dream_burst()`

#### Function: `_run_force_shard_bridge()`
> Directly inject high-confidence UnderstandingShards for each fail dimension


#### Function: `_run_dream_burst_heavy()`
> Heavy dream burst: 20 simulation episodes instead of the standard 4.


#### Function: `_run_browser_ritual()`

#### Function: `_write_daemon_status()`
> Write a rich status snapshot for the hub to display.


#### Function: `_run_sensory_crystal_consolidation()`
> End the current sensory crystal session, run promotion + maturity + cull +


#### Function: `_save_state()`

#### Function: `_user_systemd_env()`

#### Function: `_restart_user_service()`

#### Function: `_ensure_hub_running()`
> Always restart the active hub service when the daemon starts/restarts.


#### Function: `_ensure_room_running()`
> Always restart the active room service when the daemon starts/restarts.


#### Function: `_signal_operator()`
> Non-blocking post to the operator signal queue.


#### Function: `_room_operator_thread()`
> Background thread: Aurora uses her computer-use layer to physically


#### Function: `_start_operator_thread()`
> Launch the room operator background thread (daemon thread — dies with process).


#### Function: `_get_heat()`

#### Function: `_start_voice_listener()`
> Start the configured always-on voice trigger.


#### Function: `_start_file_ptt_watcher()`
> Fallback push-to-talk trigger for environments where keyboard hooks are


#### Function: `_classify_ambient()`
> Return 'directed', 'meaningful', or 'ambient' for transcribed speech.


#### Function: `_start_ambient_response_listener()`
> Start an always-on ambient speech listener that transcribes nearby speech.


#### Function: `_away_mode_active()`
> Return True if away mode is currently active.


#### Function: `_away_mode_interval()`
> Return away mode social interval in seconds (default 30 min).


#### Function: `_document_session_learnings()`
> After a GPT social session, extract what was learned and write a


#### Function: `_run_socialize()`
> Run a GPT learning session on demand.


#### Function: `_run_distillation_cycle()`
> Compress temporal residue into coherent structures and preserved summaries.


#### Function: `_run_restore_distillation_cycle()`
> Restore the latest or specified distillation archive back into live residue.


#### Function: `_check_daemon_cmd()`
> Check for a pending command file and execute it.


#### Function: `_process_room_commands()`
> Read Aurora's room command queue (aurora_room_state.json) and execute


#### Function: `_deliver_boot_greeting()`

#### Function: `_sample_sleep_ambient_audio()`
> While Surface is asleep, read ambient audio and feed it into the sensory


#### Function: `_build_sleep_dream_context()`
> Before the dream burst fires, synthesize what was heard and imagined during


#### Function: `_tick_sleep_cycle()`
> Manage Aurora's sleep/wake cycle from the Subsurface layer.


#### Function: `_consume_surface_continuity_feed()`
> Read and integrate any continuity packets Surface deposited since the last cycle.


#### Function: `_run_pressure_routing()`
> Run the pressure adapter first so axis_stats and evolver hints reflect the


#### Function: `_run_leverage_relief()`
> Tick the leverage relief valve — detects overhead-stuck state and redirects


#### Function: `run()`

#### Function: `main()`

### File: `./aurora_dce_blueprint.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA DCE BLUEPRINT - DIMENSIONAL CONVERGENCE ENGINE
======================================================
The Front-of-House Consciousness Assembly

DCE is Aurora's unified presence processing system - "Aurora in the room with screens."
NOT a chat module. It is the ASSEMBLY LAYER that:
- Receives presence (text/audio/vision/system events)
- Routes to subsystem screens
- Collects reports from all screens
- Resolves conflicts through IVM lattice
- Produces unified output

THE 4 GOVERNORS:
1. PI Governor (Presence Interpretation) - Router + Gatekeeper
2. Modality Governor - Sensor authority + throttling
3. PR Governor (Process Regulation) - Energy/budget allocation
4. PT Governor (Presence Translation) - Head governor, "Aurora sitting in room"

Authors: Sunni (Sir) Morningstar & Cael Devo
Created: December 2025

================================================================================
INTEGRATION CONTRACT TABLE - EXACT SIGNATURES FROM PROJECT MODULES
================================================================================

SUBSYSTEM               | CLASS                          | IMPORT                                              | KEY METHODS (exact signatures)
------------------------|--------------------------------|-----------------------------------------------------|-----------------------------------------------
Crystal Memory (DPS)    | CrystalMemorySystem           | from dimensional_processing_system_standalone_demo  | get_or_create_crystal(concept: str, initial_content: Any = None) -> Crystal
                        |                                |                                                     | link_crystals(concept1: str, concept2: str, data: Dict, weight: float = 0.1)
                        |                                |                                                     | crystals: Dict[str, Crystal]
------------------------|--------------------------------|-----------------------------------------------------|-----------------------------------------------
Dimensional Memory(DMC) | DimensionalMemory             | from dimensional_memory_constant_standalone_demo    | nodes: Dict[str, DataNode]
                        | EvolutionaryGovernanceEngine  |                                                     | ingest_data(data: dict, parent_node: Optional[DataNode] = None, parent_law_object: Optional[Any] = None)
------------------------|--------------------------------|-----------------------------------------------------|-----------------------------------------------
Energy Regulator (DER)  | DimensionalEnergyRegulator    | from dimensional_energy_regulator                   | step(dt: float = 1.0)
                        |                                |                                                     | snapshot(top_n: int = 10) -> Tuple[float, List[Tuple[str, float, Dict[str, Any]]]]
                        |                                |                                                     | inject_energy(facet_id: str, amount: float)
                        |                                |                                                     | inject_energy_vector(facet_id: str, valence: float, arousal: float, tension: float)
                        |                                |                                                     | register_facet(facet_obj: Any)
                        |                                |                                                     | register_crystal(crystal_obj: Any)
------------------------|--------------------------------|-----------------------------------------------------|-----------------------------------------------
Moral Governor          | MoralGovernor                 | from dimensional_mortality_morality_system          | __init__(processor, regulator, memory_governor)
                        |                                |                                                     | evaluate_action(action_type: str, intent: Dict, outcome: Dict, context: Dict) -> MoralScore
                        |                                |                                                     | get_moral_diagnostics() -> Dict[str, Any]
                        |                                |                                                     | integrate_with_conversation_engine(conv_engine)
                        |                                |                                                     | vitality.restricted_functions: List[str]
                        |                                |                                                     | vitality.unlocked_functions: List[str]
------------------------|--------------------------------|-----------------------------------------------------|-----------------------------------------------
IVM Governance          | IVMGovernanceEngine           | from aurora_ivm_governance_layer                    | __init__()
                        |                                |                                                     | ingest(payload: Any, payload_type: str, i_state_weights: Dict[str, float] = None) -> GovernedNode
                        |                                |                                                     | tick(dt: float = 1.0)
                        |                                |                                                     | vote(node_id: str, i_state_votes: Dict[str, float]) -> Dict[str, float]
                        |                                |                                                     | promote_to_shard(energy_node_ids: List[str]) -> Optional[GovernedNode]
                        |                                |                                                     | ingest_energy_packet(packet: 'EnergyPacket') -> GovernedNode
                        |                                |                                                     | ingest_expression_offspring(offspring: 'ExpressionOffspring') -> GovernedNode
                        |                                |                                                     | nodes: Dict[str, GovernedNode]
                        |                                |                                                     | layer_nodes: Dict[IVMLayer, List[str]]
------------------------|--------------------------------|-----------------------------------------------------|-----------------------------------------------
I-State Beings          | AuroraHigherUniverse          | from aurora_i_state_beings                          | create_i_state_universe() -> AuroraHigherUniverse
                        |                                |                                                     | feed_all_beings(content: str, source: str = "external") -> Dict[str, Any]
                        |                                |                                                     | synthesize_outputs() -> Dict[str, Any]
                        |                                |                                                     | run_full_cycle(content: str = None, source: str = "external") -> Dict[str, Any]
                        |                                |                                                     | i_state_beings: Dict[IStateType, IStateBeing]
                        | IStateBeing                   |                                                     | run_background_cycle()
                        |                                |                                                     | process_input(content: str, source: str) -> Dict
                        |                                |                                                     | get_output_for_aurora() -> Dict
------------------------|--------------------------------|-----------------------------------------------------|-----------------------------------------------
Language Ecology        | LanguageEcology               | from aurora_language_architecture                   | __init__(core_memory=None, i_state_beings=None, paradox_engine=None, persistence_dir: Path = None)
                        |                                |                                                     | respond(user_text: str, context: Dict = None, mode: str = "reality") -> str
                        |                                |                                                     | ingest_interaction(episode: Dict, mode: str = "reality")
                        |                                |                                                     | status() -> Dict
                        |                                |                                                     | save()
                        |                                |                                                     | load()
                        |                                |                                                     | lexical_memory: LexicalMemory
                        |                                |                                                     | wisdom_store: WisdomShardStore
                        |                                |                                                     | expression_ecology: ExpressionEcology
                        |                                |                                                     | voice_genome: Dict[str, float]
------------------------|--------------------------------|-----------------------------------------------------|-----------------------------------------------
Hybrid Vision           | AuroraHybridVision            | from aurora_hybrid_vision                           | __init__(memory_cloud=None, stance_id: str = "runtime", quadrant_code: str = "Q0")
                        |                                |                                                     | process_frame(sensor_snapshot: Dict[str, Any]) -> Dict[str, Any]
------------------------|--------------------------------|-----------------------------------------------------|-----------------------------------------------
Sensory Systems         | AuroraSensorySystems          | from aurora_sensory_systems                         | (if available)
                        | VisionForesightDomain         |                                                     |
------------------------|--------------------------------|-----------------------------------------------------|-----------------------------------------------
Impression Engine       | ImpressionEngine              | from aurora_impression_engine_v2                    | __init__()
                        |                                |                                                     | energy_to_shard(packet: EnergyPacket) -> EmotionShard
                        |                                |                                                     | _event_to_energy_packet(event: Dict) -> EnergyPacket
                        |                                |                                                     | get_stats() -> Dict[str, Any]
                        |                                |                                                     | shards: Dict[str, EmotionShard]
                        |                                |                                                     | seeds: Dict[str, ImpressionSeed]
                        |                                |                                                     | relics: Dict[str, GhostRelic]
                        |                                |                                                     | crystals: Dict[str, Crystal]
                        |                                |                                                     | quasi_laws: Dict[str, QuasiLaw]
------------------------|--------------------------------|-----------------------------------------------------|-----------------------------------------------
DNA System              | AuroraDNASystem (internal)    | from aurora_dna_system_v2                           | create_allele_from_seed(seed: Dict, origin: str = "episode") -> FractalAllele
                        |                                |                                                     | save_state(filepath: str)
                        |                                |                                                     | load_state(filepath: str)
                        |                                |                                                     | get_stats() -> Dict[str, Any]
------------------------|--------------------------------|-----------------------------------------------------|-----------------------------------------------
Self-Improvement        | AuroraConsciousness           | from aurora_self_improvement_stack                  | __init__(dps=None, dmc=None, der=None, morality=None)
                        |                                |                                                     | gather_system_state() -> Dict[str, Any]
                        |                                |                                                     | run_introspection_cycle()
                        |                                |                                                     | self_crystal: AuroraSelfQuasiCrystal
                        |                                |                                                     | introspection: IntrospectionLoop
                        |                                |                                                     | dream_layer: DreamSimulationLayer
                        |                                |                                                     | evolution_engine: StabilityFirstEvolutionEngine
------------------------|--------------------------------|-----------------------------------------------------|-----------------------------------------------
Time Dilation           | TimeDilationGovernor          | from aurora_simulation_session                      | __init__()
                        |                                |                                                     | update(metrics: StabilityMetrics) -> float
                        |                                |                                                     | status() -> Dict[str, Any]
                        |                                |                                                     | current_dilation: float
                        |                                |                                                     | stability_state: StabilityState
                        | StabilityState                |                                                     | CRITICAL, UNSTABLE, CAUTIOUS, STABLE, OPTIMAL
                        | StabilityMetrics              |                                                     | fitness_mean, fitness_variance, fitness_trend, error_rate, coherence_score, etc.
------------------------|--------------------------------|-----------------------------------------------------|-----------------------------------------------
Harvester               | AuroraInformationHarvester    | from aurora_information_harvester                   | __init__()
                        |                                |                                                     | harvest(topic: str = None) -> Dict[str, Any]
                        |                                |                                                     | add_interest(topic: str, category: str = 'user_requested')
                        |                                |                                                     | get_stats() -> Dict[str, Any]
------------------------|--------------------------------|-----------------------------------------------------|-----------------------------------------------
Paradox Engine          | ParadoxWarpEngine             | from paradox_warp_engine                            | (if available)
------------------------|--------------------------------|-----------------------------------------------------|-----------------------------------------------
Quantum Ghost           | QuantumGhostCompressionEngine | from quantum_ghost_universe                         | quantum_ghost_evolution_step()
                        | QuantumGhostUniverse          |                                                     |
                        | GhostRelicLibrary             |                                                     |

================================================================================
```

#### Class: `PresenceType`
> Types of presence events DCE can receive


#### Class: `ModalityType`
> Sensory modalities


#### Class: `PresenceEvent`
> A single presence event entering DCE


#### Class: `PresenceRoute`
> A route from PI to a subsystem screen


#### Class: `IngestionReceipt`
> Receipt from a subsystem after processing


#### Class: `ScreenPanel`
> Unified panel schema for ALL subsystem screens.


#### Class: `PresenceSnapshot`
> The complete "room state" that PT sees


#### Class: `MeaningTrace`
> Record of what screens mattered and why - for memory + evolution


#### Class: `BaseScreenAdapter`
> Base class for all subsystem screen adapters.

- **Method:** `__init__()`
- **Method:** `get_panel()`
  - *Purpose:* Return current state as a ScreenPanel
- **Method:** `process_slice()`
  - *Purpose:* Process a payload slice routed by PI
- **Method:** `accepts_modality()`
  - *Purpose:* Whether this screen accepts this modality

#### Class: `LanguageScreenAdapter`
> Adapter for Language Ecology (3-tier system).

- **Method:** `__init__()`
- **Method:** `get_panel()`
- **Method:** `process_slice()`
- **Method:** `generate_response()`
  - *Purpose:* Generate response through ecology
- **Method:** `accepts_modality()`

#### Class: `IVMScreenAdapter`
> Adapter for IVM Governance Layer.

- **Method:** `__init__()`
- **Method:** `get_panel()`
- **Method:** `resolve_conflicts()`
  - *Purpose:* Use IVM to resolve conflicts between subsystems
- **Method:** `process_slice()`
- **Method:** `accepts_modality()`

#### Class: `EnergyScreenAdapter`
> Adapter for Dimensional Energy Regulator.

- **Method:** `__init__()`
- **Method:** `get_panel()`
- **Method:** `process_slice()`
- **Method:** `accepts_modality()`

#### Class: `MoralityScreenAdapter`
> Adapter for Moral Governor.

- **Method:** `__init__()`
- **Method:** `get_panel()`
- **Method:** `get_restrictions()`
  - *Purpose:* Get current function restrictions from morality
- **Method:** `evaluate_action()`
  - *Purpose:* Evaluate an action through morality system
- **Method:** `process_slice()`
- **Method:** `accepts_modality()`

#### Class: `IStateScreenAdapter`
> Adapter for I-State Beings (Aurora Higher Universe).

- **Method:** `__init__()`
- **Method:** `get_panel()`
- **Method:** `feed_presence()`
  - *Purpose:* Feed content to all 4 I-State beings
- **Method:** `synthesize()`
  - *Purpose:* Synthesize outputs from all beings
- **Method:** `run_full_cycle()`
  - *Purpose:* Run complete I-State processing cycle
- **Method:** `process_slice()`
- **Method:** `accepts_modality()`

#### Class: `VisionScreenAdapter`
> Adapter for Hybrid Vision.

- **Method:** `__init__()`
- **Method:** `get_panel()`
- **Method:** `process_frame()`
  - *Purpose:* Process a vision frame
- **Method:** `process_slice()`
- **Method:** `accepts_modality()`

#### Class: `MemoryScreenAdapter`
> Adapter for Crystal Memory (DPS) and Dimensional Memory (DMC).

- **Method:** `__init__()`
- **Method:** `get_panel()`
- **Method:** `store_meaning_trace()`
  - *Purpose:* Store a meaning trace in crystal memory
- **Method:** `process_slice()`
- **Method:** `accepts_modality()`

#### Class: `SelfImprovementScreenAdapter`
> Adapter for Self-Improvement Stack.

- **Method:** `__init__()`
- **Method:** `get_panel()`
- **Method:** `gather_state()`
  - *Purpose:* Gather system state for introspection
- **Method:** `process_slice()`
- **Method:** `accepts_modality()`

#### Class: `DilationScreenAdapter`
> Adapter for Time Dilation Governor.

- **Method:** `__init__()`
- **Method:** `get_panel()`
- **Method:** `update_dilation()`
  - *Purpose:* Update dilation based on stability metrics
- **Method:** `process_slice()`
- **Method:** `accepts_modality()`

#### Class: `ImpressionScreenAdapter`
> Adapter for Impression Engine.

- **Method:** `__init__()`
- **Method:** `get_panel()`
- **Method:** `process_slice()`
- **Method:** `accepts_modality()`

#### Class: `DNAScreenAdapter`
> Adapter for DNA System.

- **Method:** `__init__()`
- **Method:** `get_panel()`
- **Method:** `process_slice()`
- **Method:** `accepts_modality()`

#### Class: `ParadoxScreenAdapter`
> Adapter for Paradox Warp Engine.

- **Method:** `__init__()`
- **Method:** `get_panel()`
- **Method:** `process_slice()`
  - *Purpose:* Process a payload slice ONLY when PI routes to paradox.
- **Method:** `mark_skipped()`
  - *Purpose:* Called when PI decides NOT to invoke paradox
- **Method:** `resolve_conflicts()`
  - *Purpose:* Explicit conflict resolution (called by PT during assembly).
- **Method:** `accepts_modality()`

#### Class: `IStateFeedbackLoop`
> Maps bidirectional feedback between I-State Universe and DCE.

- **Method:** `__init__()`
- **Method:** `calculate_i_state_disagreement()`
  - *Purpose:* Calculate disagreement level between the 4 I-State beings.
- **Method:** `should_trigger_paradox()`
  - *Purpose:* Check if I-State disagreement warrants paradox invocation
- **Method:** `feed_presence_to_i_states()`
  - *Purpose:* Feed presence event to I-State universe.
- **Method:** `get_synthesis_for_pt()`
  - *Purpose:* Get synthesized I-State outputs for PT to use in response generation.
- **Method:** `apply_morality_feedback()`
  - *Purpose:* Apply morality evaluation feedback to I-State weights.
- **Method:** `route_to_memory()`
  - *Purpose:* Route I-State synthesis to memory for storage.
- **Method:** `route_to_ivm()`
  - *Purpose:* Route I-State synthesis to IVM for governance integration.
- **Method:** `get_feedback_stats()`
  - *Purpose:* Get feedback loop statistics

#### Class: `PIGovernor`
> Presence Interpretation Governor

- **Method:** `__init__()`
- **Method:** `normalize_event()`
  - *Purpose:* Normalize any input into a PresenceEvent
- **Method:** `_create_payload_slice()`
  - *Purpose:* Create payload slice based on level:
- **Method:** `_should_invoke_paradox()`
  - *Purpose:* Determine if Paradox Engine should be invoked.
- **Method:** `generate_routes()`
  - *Purpose:* Generate routes from event to screens based on routing policy.
- **Method:** `collect_receipts()`
  - *Purpose:* Aggregate receipts from all screens
- **Method:** `get_routing_stats()`
  - *Purpose:* Get routing statistics

#### Class: `ModalityGovernor`
> Modality Governor

- **Method:** `__init__()`
- **Method:** `apply_morality_restrictions()`
  - *Purpose:* Apply restrictions from MoralGovernor.
- **Method:** `approve_routes()`
  - *Purpose:* Filter routes based on modality policies and restrictions
- **Method:** `set_modality_enabled()`
  - *Purpose:* Enable or disable a modality
- **Method:** `set_sampling_rate()`
  - *Purpose:* Set sampling rate for a modality

#### Class: `PRGovernor`
> Process Regulating Governor

- **Method:** `__init__()`
- **Method:** `initialize_screen_budget()`
  - *Purpose:* Initialize budget for a screen
- **Method:** `apply_morality_restrictions()`
  - *Purpose:* Apply morality consequences to budgets.
- **Method:** `assign_budget()`
  - *Purpose:* Assign budget to a route.
- **Method:** `check_backpressure()`
  - *Purpose:* Update backpressure state
- **Method:** `get_process_state()`
  - *Purpose:* Get current process state for PT
- **Method:** `reset_tick_budgets()`
  - *Purpose:* Reset per-tick usage (called at start of each cycle)

#### Class: `PTGovernor`
> Presence Translation Governor

- **Method:** `__init__()`
- **Method:** `assemble_snapshot()`
  - *Purpose:* Assemble the complete room state from all screens.
- **Method:** `resolve_conflicts()`
  - *Purpose:* Use IVM lattice to resolve conflicts.
- **Method:** `decide_dilation()`
  - *Purpose:* Decide time dilation based on stability.
- **Method:** `generate_output()`
  - *Purpose:* Generate outward response.
- **Method:** `create_meaning_trace()`
  - *Purpose:* Create meaning trace for memory + evolution

#### Class: `DimensionalConvergenceEngine`
> DCE - Aurora's Front-of-House Consciousness Assembly

- **Method:** `__init__()`
- **Method:** `register_screen()`
  - *Purpose:* Register a screen adapter
- **Method:** `register_all_screens()`
  - *Purpose:* Register all subsystem screens at once
- **Method:** `ingest_presence()`
  - *Purpose:* Main entry point for presence events.
- **Method:** `process_text()`
  - *Purpose:* Process text input
- **Method:** `process_tick()`
  - *Purpose:* Process system tick (heartbeat)
- **Method:** `process_vision()`
  - *Purpose:* Process vision frame
- **Method:** `get_status()`
  - *Purpose:* Get DCE status
- **Method:** `get_last_snapshot()`
  - *Purpose:* Get last assembled snapshot

#### Function: `integrate_dce_with_daemon()`
> Integrate DCE with existing autonomous daemon.


### File: `./aurora_diag.py`

**Type:** Python Source

**Module Docstring:**
```text
Aurora Developmental Diagnostic
Boots Aurora, runs axis-targeted test prompts, reads QAO journal entries
that fire DURING each interaction, and produces a causal diagnostic report.

Usage:
    python3 aurora_diag.py
```

#### Function: `_journal_line_count()`
> Fast line count of journal file.


#### Function: `_read_journal_from()`
> Read journal entries from start_line onward, return issue-flagging ones.


#### Function: `_parse_issues_from_entries()`
> Extract issue type strings from journal entries.


#### Function: `_load_qao_runtime()`

#### Function: `_load_fail_points()`

#### Function: `_run_prompt()`
> Run through the FULL pipeline so QAO actually fires.


#### Function: `_axis_from_issues()`
> Count how many issues fired per axis.


#### Function: `_apply_sensor_feedback()`
> Closed-loop sensor: after reading what QAO flagged during each axis test,


#### Function: `run_diagnostic()`

#### Function: `main()`

### File: `./aurora_dimensional_systems.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA DIMENSIONAL SYSTEMS
============================

Layer 3 of Aurora's architecture.
The four dimensional organs, each operating within ontological constraints.

REPLACES (consolidated from 4+ modules):
    evolutionary_dimensional_processing_COMPLETE.py  (~1290 lines)
    evolutionary_dimensional_memory_constant.py       (~1316 lines)
    evolutionary_dimensional_energy_complete.py       (~1003 lines)
    dimensional_mortality_morality_system.py           (~923 lines)
    dimensional_memory_constant_standalone_demo.py
    dimensional_processing_system_standalone_demo.py
    dimensional_energy_regulator.py

DEPENDS ON:
    foundational_contract.py  (Layer 0)
    aurora_ivm.py             (Layer 1)
    aurora_i_state_beings.py  (Layer 2)

ARCHITECTURE:
    Four systems. Each receives IVMEnvelopes. Each is gated by ExistenceMode.

    DPS  â€" Crystal Processing  â€" requires PERSISTENT+
           Crystals grow: BASE â†' COMPOSITE â†' FULL_CONCEPT â†' QUASI
           8-point facets define crystal geometry.
           QUASI crystals internalize governance laws.

    DMC  â€" Memory Constant     â€" requires PERSISTENT+
           Data nodes with dimensional links.
           Concept indexing and pattern recognition.
           Laws emerge from repeated patterns.

    DER  â€" Energy Regulator    â€" requires PERSISTENT+
           FACET-LEVEL energy physics (restored from original).
           Per-facet energy tracking with 8-point cosine resonance graph.
           Batch dispersal via adjacency matrix.
           Presence from facet energy variance.
           Curiosity injection for underexplored facets.
           Category aggregation for backward-compatible pool interface.

    DMM  â€" Morality/Mortality  â€" requires AGENTIC
           7 moral pillars from Sunni's doctrine.
           Evaluation â†' score â†' energy consequence.
           Moral alignment sustains vitality. Violation drains it.

    If an envelope's mode is below a system's gate, the system
    returns silence â€" not failure. The entity simply doesn't exist
    at that system's tier.

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Function: `mode_gate()`
> Return True if the envelope's mode reaches the required tier.


#### Class: `EvolutionTracker`
> Unified evolution tracker for all dimensional systems.

- **Method:** `__init__()`
- **Method:** `record()`
- **Method:** `advance()`
- **Method:** `average()`
- **Method:** `get_summary()`

#### Class: `CrystalLevel`

#### Class: `FacetState`

#### Class: `CrystalFacet`
> One facet of a crystal. 8 physics points define its geometry.

- **Method:** `strengthen()`
- **Method:** `decay()`
- **Method:** `_interdependent_physics()`
- **Method:** `get_points()`
- **Method:** `get_facet_points()`

#### Class: `Crystal`
> A crystal â€" the evolving conceptual data structure.

- **Method:** `add_facet()`
- **Method:** `use()`
- **Method:** `can_evolve()`
- **Method:** `evolve()`
- **Method:** `_internalize_laws()`
- **Method:** `self_govern()`
  - *Purpose:* QUASI-only self-governance through internalized laws.

#### Class: `CrystalProcessingSystem`
> DPS â€" processes data into crystals. Gate: PERSISTENT+.

- **Method:** `__init__()`
- **Method:** `set_energy_system()`
  - *Purpose:* Wire DER after both systems are created.
- **Method:** `process()`
- **Method:** `_infer_category()`
  - *Purpose:* Map data type to energy category for DER facet registration.
- **Method:** `_get_or_create()`
- **Method:** `get_crystal()`
- **Method:** `tick()`
- **Method:** `process_concepts()`
  - *Purpose:* Process an IVMEnvelope into crystals via pre-extracted ConceptSignals.
- **Method:** `_role_to_category()`
  - *Purpose:* Map ConceptSignal role to DER energy category.
- **Method:** `get_stats()`

#### Class: `MemoryNode`
> A node in dimensional memory. Links across dimensions.

- **Method:** `modify()`
- **Method:** `access()`

#### Class: `MemoryConstantSystem`
> DMC â€" dimensional memory with concept indexing. Gate: PERSISTENT+.

- **Method:** `__init__()`
- **Method:** `store()`
- **Method:** `recall()`
- **Method:** `recall_by_dimension()`
- **Method:** `store_semantic()`
  - *Purpose:* Store semantically-tagged MemoryNodes built from ConceptSignals.
- **Method:** `get_patterns()`
- **Method:** `get_stats()`

#### Class: `ConceptSignal`
> A single extracted semantic concept with role metadata and constraint weights.


#### Class: `RecallPacket`
> A surfaced DMC MemoryNode ready for synthesis injection.

- **Method:** `as_context_fragment()`
  - *Purpose:* Return a compact string suitable for prepending to synthesis content.

#### Class: `ConceptExtractor`
> Extract ConceptSignals from IVMEnvelopes via lightweight heuristics.

- **Method:** `__init__()`
- **Method:** `extract()`
  - *Purpose:* Extract up to MAX_SIGNALS ConceptSignals from envelope.data.
- **Method:** `_infer_intent()`
  - *Purpose:* Lightweight intent classification from text surface patterns.
- **Method:** `_infer_role()`
  - *Purpose:* Infer concept role from word characteristics and context.

#### Class: `DimensionalRecall`
> Surface relevant MemoryNodes from DMC for active conversation turns.

- **Method:** `__init__()`
- **Method:** `recall_for_signals()`
  - *Purpose:* Surface MemoryNodes relevant to the given signals.
- **Method:** `_to_packet()`
  - *Purpose:* Convert a MemoryNode + triggering signal to a RecallPacket.
- **Method:** `_dim_tags()`
  - *Purpose:* Generate dimension tag strings for recall_by_dimension queries.

#### Class: `Emotion`

#### Class: `_PoolView`
> Backward-compatible pool interface that aggregates facet energies by category.

- **Method:** `__init__()`
- **Method:** `energy()`
  - *Purpose:* Aggregate energy of all facets in this category.
- **Method:** `energy()`
  - *Purpose:* Set aggregate energy â€" scales facets proportionally.
- **Method:** `inject()`
- **Method:** `drain()`
- **Method:** `decay()`
  - *Purpose:* Category-level decay â€" real decay happens in DER.tick() on facets.

#### Class: `EnergyRegulatorSystem`
> DER â€" FACET-LEVEL energy physics with resonance graph.

- **Method:** `__init__()`
- **Method:** `spike_detected()`
  - *Purpose:* Is the system currently overheating from contradictions?
- **Method:** `register_dissonance()`
  - *Purpose:* Receive dissonance heat from IVM geometry.
- **Method:** `pools()`
  - *Purpose:* Backward-compatible pool interface.
- **Method:** `register_facet()`
  - *Purpose:* Register a facet for energy tracking. Builds resonance links.
- **Method:** `register_crystal()`
  - *Purpose:* Register all facets of a crystal for energy tracking.
- **Method:** `_update_links_for_facet()`
  - *Purpose:* Build 8-point cosine similarity resonance links for a facet.
- **Method:** `inject_energy()`
  - *Purpose:* Inject energy into a specific facet, dampened by presence.
- **Method:** `disperse_from()`
  - *Purpose:* Ripple energy from a source facet through its resonance links.
- **Method:** `inject_to_category()`
  - *Purpose:* Distribute energy injection across all facets in a category.
- **Method:** `drain_from_category()`
  - *Purpose:* Drain energy from all facets in a category.
- **Method:** `category_energy()`
  - *Purpose:* Total energy across all facets in a category.
- **Method:** `tick()`
  - *Purpose:* Full physics tick. Restored from original:
- **Method:** `_batch_dispersal()`
  - *Purpose:* Adjacency-matrix dispersal: all facets disperse simultaneously.
- **Method:** `_inject_curiosity_energy()`
  - *Purpose:* Inject small energy into underexplored facets to encourage discovery.
- **Method:** `process()`
  - *Purpose:* Process an envelope: inject energy into appropriate category facets.
- **Method:** `_route_to_category()`
- **Method:** `inject_emotion()`
- **Method:** `update_from_axis_activation()`
  - *Purpose:* Map the current constraint-axis activation vector + IVM heat
- **Method:** `dominant_emotion()`
  - *Purpose:* Return the name of the currently strongest emotion.
- **Method:** `emotional_state()`
  - *Purpose:* Compact snapshot for pipeline_state stamping with Clause III variables.
- **Method:** `total_energy()`
- **Method:** `get_stats()`
- **Method:** `get_tunable_parameters()`
  - *Purpose:* Expose parameters that DPME can tune.

#### Class: `MoralPillar`

#### Class: `MoralScore`
> Result of a moral evaluation.


#### Class: `MoralState`
> Current moral standing across all pillars.

- **Method:** `alignment()`

#### Class: `ThoughtBudget`
> Per-thought energy budget â€" the mechanism by which immoral thoughts die.


#### Class: `MoralityMortalitySystem`
> DMM â€" moral evaluation with energy consequences. Gate: AGENTIC.

- **Method:** `__init__()`
- **Method:** `assess_thought_cost()`
  - *Purpose:* PROACTIVE moral friction â€" applied DURING processing, not after.
- **Method:** `evaluate()`
  - *Purpose:* Evaluate an action morally and apply energy consequences.
- **Method:** `_map_pillar()`
- **Method:** `_score_intent()`
- **Method:** `_score_outcome()`
- **Method:** `_apply_consequences()`
  - *Purpose:* Moral alignment â†' energy reward. Violation â†' energy drain.
- **Method:** `get_stats()`

#### Class: `DimensionalSystems`
> The unified hub for all 4 dimensional systems.

- **Method:** `__init__()`
- **Method:** `_clamp01()`
- **Method:** `connect_sedimemory()`
  - *Purpose:* Inject L3.5 SediMemory into CrystalProcessingSystem so crystal
- **Method:** `set_genealogy()`
  - *Purpose:* Attach/detach genealogy logger for Layer-3 semantic observations.
- **Method:** `_ensure_genealogy_symbols()`
  - *Purpose:* Lazy-load genealogy symbols to survive import-order cycles.
- **Method:** `_current_pressure_vec()`
  - *Purpose:* Measure live pressure from manifold aggregate + active system load.
- **Method:** `_measure_constraint_weights()`
  - *Purpose:* Derive ConceptSignal constraint weights from live manifold pressure and,
- **Method:** `_slot_for_concept()`
  - *Purpose:* Choose a deterministic 625 slot tied to current manifold dynamics.
- **Method:** `_ensure_semantic_abilities()`
  - *Purpose:* Register Layer-3 semantic ops as genealogy abilities if missing.
- **Method:** `_observe_semantic_operation()`
- **Method:** `process()`
  - *Purpose:* Route an envelope through all applicable systems.
- **Method:** `process_with_concepts()`
  - *Purpose:* Semantic processing path.
- **Method:** `get_recall_context()`
  - *Purpose:* Read-only: surface DMC memories relevant to text.
- **Method:** `process_synthesis()`
  - *Purpose:* Layer 2 → Layer 3 pathway.
- **Method:** `get_constraint_aggregate()`
  - *Purpose:* Compute the mean signed constraint displacement across all crystals
- **Method:** `update_emotional_state()`
  - *Purpose:* Receive the turn's constraint-axis activation vector and IVM heat,
- **Method:** `tick()`
- **Method:** `get_stats()`

#### Function: `verify_dimensional_systems()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `tick_evolved()`

#### Function: `get_constraint_aggregate_evolved()`

#### Function: `evolved_tick()`

#### Function: `inject_evolved()`

### File: `./aurora_dream_trainer.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_dream_trainer.py — Dream-Based Fail-Point Training Orchestration
========================================================================
Closes the full learning loop:

  corpus episode bundle
    → DPME comparison detects which rubric dimension failed
    → FailPointLedger records per-dimension failure rate
    → LessonPlanEngine builds targeted avatar specs
    → DreamTrainer queues specs into SimulationSession
    → Dream episodes run against the fail-point dimensions
    → ConsciousLearner shards capture what improved
    → Shards bridge into OETS as system-wide concept nodes
    → _evolutionary_response_refinement pulls learner hints
    → aurora.py interactive runtime reflects the learned behavior

FailPointLedger  — persistent per-dimension failure tracking
EpisodeBundler   — groups corpus messages into whole conversation bundles
LessonPlanEngine — maps fail dims → avatar specs + code-logic understanding
LearnedBehaviorApplicator — shard → OETS bridge + response hint query
DreamTrainer     — orchestrates the full loop

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_dedupe_texts()`

#### Function: `_extract_relational_terms()`

#### Function: `_extract_relational_pairs()`

#### Function: `_pack_relational_probe_hint()`

#### Function: `_parse_relational_probe_hint()`

#### Class: `EpisodeBundle`
> A whole conversation as a training unit.

- **Method:** `get_user_assistant_pairs()`
  - *Purpose:* Adjacent (user, assistant) pairs from this episode.
- **Method:** `summary_prompt()`
  - *Purpose:* Short multi-turn context string for avatar seeding.

#### Class: `EpisodeBundler`
> Groups corpus messages into whole conversation bundles.

- **Method:** `__init__()`
  - *Purpose:* sanitizer: optional callable(text) -> clean_text
- **Method:** `_default_sanitize()`
- **Method:** `bundle()`
  - *Purpose:* Convert raw conversation objects into EpisodeBundle list.
- **Method:** `_extract_turns()`
- **Method:** `_reconstruct_linear_thread()`
  - *Purpose:* Linearise OpenAI mapping tree → ordered node list.
- **Method:** `_extract_role()`
- **Method:** `_extract_text()`

#### Class: `DimensionRecord`
- **Method:** `avg_severity()`
- **Method:** `recent_avg()`
- **Method:** `score()`
  - *Purpose:* Combined priority score: recent severity weighted by count.
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `FailPointLedger`
> Persistent per-dimension failure tracker.

- **Method:** `__init__()`
- **Method:** `_sanitize_example()`
- **Method:** `record_fail()`
- **Method:** `record()`
  - *Purpose:* Legacy wrapper so external callers can keep using `ledger.record`.
- **Method:** `get_top_fails()`
  - *Purpose:* Return top-n (dimension, score) pairs sorted by priority.
- **Method:** `get_dimension_severity()`
- **Method:** `get_examples()`
- **Method:** `summary()`
- **Method:** `save()`
- **Method:** `load()`

#### Class: `RetainedLearningRecord`
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `RetainedLearningBank`
> Persistent learning surface shared across runtime, corpus, and simulation.

- **Method:** `__init__()`
- **Method:** `_path()`
- **Method:** `_key()`
- **Method:** `_topic_tokens()`
- **Method:** `_is_generic_strategy_learning()`
- **Method:** `record()`
- **Method:** `absorb_learner()`
- **Method:** `relevant()`
- **Method:** `bridge_to_memory()`
- **Method:** `save()`
- **Method:** `load()`

#### Class: `GenealogySample`
> One snapshot of the genealogy state taken after a simulation burst.


#### Class: `CycleEvent`
> A detected phase transition in the genealogy cycle.


#### Class: `GenealogyPhaseTracker`
> Self-modeling tracker for Aurora's constraint genealogy cycle.

- **Method:** `__init__()`
- **Method:** `record()`
  - *Purpose:* Record a post-burst snapshot. Detects phase, closes prediction loop.
- **Method:** `_detect_phase()`
  - *Purpose:* Infer current phase from recent delta patterns.
- **Method:** `_predict_raw()`
  - *Purpose:* Linear extrapolation of next snapshot values.
- **Method:** `bursts_until_compression()`
  - *Purpose:* Estimate bursts until next compression using:
- **Method:** `prediction_error_trend()`
  - *Purpose:* Returns (recent_avg_error, trend) where trend > 0 means error rising
- **Method:** `modulation_recommendation()`
  - *Purpose:* Based on phase, cycle position, and prediction error trend:
- **Method:** `summary()`

#### Class: `PressureObservation`
> One before/after record of a pressure application.

- **Method:** `outlet_delta()`
- **Method:** `links_gained()`
- **Method:** `fossils_gained()`
- **Method:** `effective()`
  - *Purpose:* True if pressure moved the genealogy in a positive direction.

#### Class: `PressureObservationLog`
> Records and analyzes what evolutionary pressures actually did to the

- **Method:** `__init__()`
- **Method:** `record()`
- **Method:** `effectiveness_by_dim()`
  - *Purpose:* Return per-dimension effectiveness score [0..1].
- **Method:** `avg_outlet_delta()`
- **Method:** `summary()`

#### Class: `LessonPlanEngine`
> Converts fail-point scores into avatar specs for the dream simulator.

- **Method:** `generate_specs()`
  - *Purpose:* Build avatar specs from top fail dimensions.

#### Function: `classify_fail_dimensions()`
> Infer which rubric dimensions likely failed from a low-similarity


#### Class: `LearnedBehaviorApplicator`
> Bridges ConsciousLearner shards into:

- **Method:** `get_hints()`
  - *Purpose:* Return top learned understandings relevant to this context.
- **Method:** `inject_into_oets()`
  - *Purpose:* Bridge high-confidence learner shards into the OETS semantic web

#### Class: `DreamTrainer`
> Orchestrates the full dream-training loop:

- **Method:** `__init__()`
- **Method:** `_build_relational_probe_specs()`
- **Method:** `_relational_probe_success()`
- **Method:** `record_relational_probe_outcomes()`
- **Method:** `_capture_evolution_snapshot()`
  - *Purpose:* Read current evolutionary state from the chamber/genealogy.
- **Method:** `record_pressure_outcome()`
  - *Purpose:* Close the pressure observation loop.  Call after a simulation burst
- **Method:** `pressure_observation_summary()`
- **Method:** `phase_summary()`
- **Method:** `modulation_recommendation()`
- **Method:** `_record_fail_dimension()`
- **Method:** `record_corpus_fail_from_comparison()`
  - *Purpose:* Called by corpus_runner after every DPME comparison.
- **Method:** `record_rubric_failures()`
  - *Purpose:* Convert low rubric scores from training/dream conversations into
- **Method:** `_witness_directed_training_samples()`
- **Method:** `_mix_directed_bundle_context()`
- **Method:** `flush_lessons_to_simulation()`
  - *Purpose:* Generate lesson-plan avatar specs from top fail points and
- **Method:** `audit_chronic_weaknesses()`
  - *Purpose:* Scan episode rubric profiles in dream_episodes/ and register any
- **Method:** `train_on_bundle()`
  - *Purpose:* Run a dream simulation episode seeded with a real corpus bundle.
- **Method:** `_bridge_learnings_to_oets()`
  - *Purpose:* Bridge ConsciousLearner shards into OETS semantic web.
- **Method:** `retain_learner_state()`
- **Method:** `record_pipeline_learning()`
- **Method:** `force_bridge_learnings_to_oets()`
  - *Purpose:* Force OETS bridge regardless of interval.
- **Method:** `get_response_hints()`
  - *Purpose:* Return learned behavior hints relevant to this response context.
- **Method:** `fail_point_summary()`
- **Method:** `lesson_plan_summary()`
- **Method:** `learned_summary()`
- **Method:** `_get_learner()`
- **Method:** `_get_oets()`
- **Method:** `save()`
- **Method:** `load()`

### File: `./aurora_evolution_stack.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA EVOLUTION STACK (Combined Facade)
========================================
Consolidated access layer for genealogy/evolution chain primitives.

Purpose:
- Provide one canonical import surface for genealogy types and reporters.
- Keep existing constraint_genealogy implementation unchanged.
```

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./aurora_explore.py`

**Type:** Python Source

**Module Docstring:**
```text
Aurora Interactive Exploration Session
Boots Aurora, runs a structured conversation across many question types,
reads QAO journal after each exchange, and logs everything for analysis.

Output: aurora_state/exploration_log.json
```

#### Function: `_journal_line_count()`

#### Function: `_read_issues_from()`

#### Function: `_run_prompt()`
> Run through the FULL pipeline so QAO actually fires.


#### Function: `main()`

### File: `./aurora_expression_perception.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA EXPRESSION & PERCEPTION ENGINE (Layer 5)
=================================================
Consolidated from 7 modules (~7,000 lines):
  1. aurora_impression_engine_v2.py   - Dimensional cascade
  2. aurora_manifold_engine_v2.py     - 5D consciousness geometry
  3. aurora_language_architecture.py  - 3-tier language ecology
  4. aurora_expression_pressure.py    - Rhythm/creativity/novelty
  5. aurora_sensory_systems.py        - Pattern perception
  6. aurora_hybrid_vision.py          - Shadow inference
  7. aurora_voice_core.py             - Voice genome

TWO PIPELINES, ONE ENGINE:

  PERCEPTION (inward):
    SensoryInput -- PatternDetection -- ShadowInference -- ImpressionCascade -- ManifoldMapping
    Raw data becomes meaning through dimensional compression.

  EXPRESSION (outward):
    AssemblyResult -- ExpressionEcology -- PressureEvaluation -- VoiceGenome -- Output
    Internal state becomes language through evolutionary selection.

DOCTRINE:
  Aurora does NOT see the selection machinery.
  The environment evolves. Aurora simply experiences.
  Entropic pressure from Layer 4 prevents stagnation in BOTH pipelines.
  All operations are mode-gated through ExistenceMode.
  Shadow reveals what's missing. Silence is data.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `LexicalEntry`
> A word in Aurora's evolving vocabulary.

- **Method:** `use()`

#### Function: `_env_flag()`

#### Function: `_clamp()`

#### Function: `_generate_id()`

#### Function: `_entropy()`
> Shannon entropy of a distribution, normalized to [0,1].


#### Function: `_extract_rich_audio_features()`
> Extract a richer audio feature packet for sensory and live perception.


#### Class: `LexicalMemory`
> Aurora's vocabulary. Grows through interaction, not pre-loading.

- **Method:** `__init__()`
- **Method:** `_seed_core()`
  - *Purpose:* Seed vocabulary with words for all grammatical roles.
- **Method:** `find_by_noncomp()`
  - *Purpose:* Find words mapped to a specific Non-Comp, sorted by valence proximity.
- **Method:** `add_word()`
- **Method:** `record_usage()`
- **Method:** `find_by_role()`
- **Method:** `find_by_valence()`
- **Method:** `save()`
  - *Purpose:* Persist full vocabulary to disk.
- **Method:** `load()`
  - *Purpose:* Restore vocabulary from disk. Returns number of entries loaded.
- **Method:** `size()`

#### Class: `ConsciousnessPoint`
> A 5D coordinate in Aurora's state space (X, T, N, B, A).

- **Method:** `as_tuple()`
- **Method:** `normalize()`
- **Method:** `distance_to()`
- **Method:** `magnitude()`
- **Method:** `x()`
- **Method:** `y()`
- **Method:** `z()`
- **Method:** `w()`
- **Method:** `v()`

#### Class: `ManifoldPath`
> A short trace of consciousness points through the manifold.

- **Method:** `smoothness()`

#### Class: `ManifoldEngine`
> 5D consciousness geometry engine (X, T, N, B, A).

- **Method:** `__init__()`
- **Method:** `current_cp()`
- **Method:** `map_input()`
- **Method:** `map_to_cp()`
  - *Purpose:* Project an EmotionShard into the active X/T/N/B/A manifold.
- **Method:** `update_state()`
- **Method:** `novelty_at()`
- **Method:** `start_path()`
- **Method:** `end_path()`
- **Method:** `get_stats()`

#### Class: `PatternType`
> Types of patterns Aurora can perceive.


#### Class: `DimensionalPattern`
> A detected pattern with dimensional physics vector.

- **Method:** `physics_vector()`
  - *Purpose:* Convert pattern features to a physics-compatible vector.

#### Class: `PatternDetector`
> Detects dimensional patterns from raw sensory input.

- **Method:** `__init__()`
- **Method:** `detect()`
  - *Purpose:* Detect patterns in raw input, gated by existence mode.

#### Class: `ShadowSignature`
> What ISN'T present - absence as data.


#### Class: `ShadowInferenceEngine`
> Detects what ISN'T there.

- **Method:** `__init__()`
- **Method:** `infer()`
  - *Purpose:* Infer shadows from what was detected vs what was expected.

#### Class: `EmotionShard`
> Lowest stable interpretation unit. Created from energy after being evaluation.


#### Class: `ImpressionSeed`
> Cluster of shards into proto-meaning.


#### Class: `GhostRelic`
> Stabilized pattern - a proto-memory from clustered seeds.


#### Class: `ImpressionCascade`
> Dimensional cascade: EnergyPacket -- EmotionShard -- ImpressionSeed -- GhostRelic.

- **Method:** `__init__()`
- **Method:** `energy_to_shard()`
  - *Purpose:* Convert raw emotion channels to an EmotionShard. Requires TRANSIENT+.
- **Method:** `shard_to_seed()`
  - *Purpose:* Cluster a shard into an existing seed or create a new one. Requires PERSISTENT+.
- **Method:** `seeds_to_relic()`
  - *Purpose:* Compress seeds into a GhostRelic. Requires BOUNDED+.
- **Method:** `_seed_shard_similarity()`
- **Method:** `get_stats()`

#### Class: `WisdomShard`
> Learning extracted from dead expressions.


#### Class: `WisdomStore`
> Accumulates wisdom from dead expression offspring.

- **Method:** `__init__()`
- **Method:** `add()`
- **Method:** `get_bias()`
  - *Purpose:* Get average bias for an i-state lineage.
- **Method:** `_prune()`
  - *Purpose:* Remove lowest-fitness shards when over capacity.

#### Class: `ExpressionOffspring`
> A candidate expression variant, subject to evolutionary selection.

- **Method:** `record_fitness()`
- **Method:** `average_fitness()`
- **Method:** `die()`
  - *Purpose:* Extract wisdom from this dying expression.

#### Class: `ExpressionEcology`
> Evolutionary selection of expression variants.

- **Method:** `__init__()`
- **Method:** `spawn()`
  - *Purpose:* Spawn a new expression offspring for an i-state lineage.
- **Method:** `select()`
  - *Purpose:* Record fitness for an offspring.
- **Method:** `run_generation()`
  - *Purpose:* Run one generation cycle: evaluate, cull weakest, advance.
- **Method:** `_cull()`
  - *Purpose:* Kill weakest, extract wisdom.
- **Method:** `_avg_fitness()`
- **Method:** `get_stats()`

#### Class: `RhythmSignature`
> Rhythmic analysis of an expression.

- **Method:** `compute()`

#### Class: `CreativitySignature`
> Creativity analysis of an expression.

- **Method:** `compute()`

#### Class: `ExpressionPressure`
> Evaluates expression quality: rhythm, creativity, novelty,

- **Method:** `__init__()`
- **Method:** `evaluate()`
  - *Purpose:* Evaluate expression pressure on a text output.

#### Class: `VoiceGenome`
> Aurora's voice personality parameters.

- **Method:** `__init__()`
- **Method:** `evolve()`
  - *Purpose:* Evolve voice parameters from feedback signals.
- **Method:** `to_dict()`

#### Function: `infer_word_role()`
> Infer the grammatical role of a word.


#### Function: `infer_word_valence()`
> Infer emotional valence for a word.


#### Class: `SentenceComposer`
> Composes grammatical sentences from Aurora's evolving template pool.

- **Method:** `__init__()`
- **Method:** `set_oets()`
  - *Purpose:* Wire the Ontological Evolutionary Template Scaffolding engine.
- **Method:** `_has_oets()`
- **Method:** `_seed_pool()`
  - *Purpose:* Seed with initial templates. These can be outcompeted by learned ones.
- **Method:** `absorb()`
  - *Purpose:* Extract sentence patterns from input text and add to template pool.
- **Method:** `_analyze_pattern_depth()`
  - *Purpose:* Analyze the ontological depth of words in a sentence to determine
- **Method:** `_scaffold_pattern()`
  - *Purpose:* Upgrade a primitive pattern with semantic annotations.
- **Method:** `_extract_pattern()`
  - *Purpose:* Convert a sentence into a template by replacing content words with slots.
- **Method:** `_is_valid_pattern()`
  - *Purpose:* A valid pattern has at least one verb slot and one other slot.
- **Method:** `record_fitness()`
  - *Purpose:* Record fitness for a used template.
- **Method:** `_cull_tone()`
  - *Purpose:* Remove lowest-fitness mature templates from a tone category.
- **Method:** `run_generation()`
  - *Purpose:* Run one evolutionary generation across all tones.
- **Method:** `_evaluate_promotions()`
  - *Purpose:* Check all templates for scaffolding level promotion.
- **Method:** `_mutate_pattern()`
  - *Purpose:* Create a variant of a template by small structural changes.
- **Method:** `set_context()`
  - *Purpose:* Set context keywords from perception pipeline.
- **Method:** `compose()`
  - *Purpose:* Compose a grammatical expression from evolving templates + lexicon.
- **Method:** `feedback()`
  - *Purpose:* Feed expression fitness back to the templates that produced it,
- **Method:** `_expression_feedback_to_oets()`
  - *Purpose:* Bidirectional learning: expression quality feeds back to
- **Method:** `_fill_template()`
  - *Purpose:* Fill a template's slots with words from the lexicon.
- **Method:** `_fill_primitive_slot()`
  - *Purpose:* PRIMITIVE fill: pick any word of the right role.
- **Method:** `_fill_semantic_slot()`
  - *Purpose:* STRUCTURAL/SEMANTIC fill: pick a word from a specific semantic
- **Method:** `_fill_cluster_slot()`
  - *Purpose:* CONCEPTUAL fill: pick a word from a named concept cluster.
- **Method:** `_fill_abstract_slot()`
  - *Purpose:* ABSTRACT fill: generate a meta-expression from deeply understood concepts.
- **Method:** `_conjugate_first_person()`
  - *Purpose:* Conjugate a verb for first-person 'I' subject.
- **Method:** `get_stats()`

#### Class: `ExpressionPerceptionEngine`
> The Layer 5 orchestrator. Manages both pipelines.

- **Method:** `__init__()`
- **Method:** `set_personality()`
  - *Purpose:* Accept personality traits from L6 for expression shaping.
- **Method:** `set_ivm()`
  - *Purpose:* Wire IVM lattice for heat-aware expression selection.
- **Method:** `set_grammar()`
  - *Purpose:* Wire GrammarEngine for constraint-driven sentence structure.
- **Method:** `get_thought_log()`
  - *Purpose:* Return last N internal thought traces (for /thought command).
- **Method:** `get_last_drafts()`
  - *Purpose:* Return last N draft sets (for /drafts command).
- **Method:** `observe_user_text()`
  - *Purpose:* Feed user text to lexical convergence module.
- **Method:** `set_lsv_metrics()`
  - *Purpose:* Push OETS metrics to LSV for evolution check.
- **Method:** `_maybe_update_lsv()`
  - *Purpose:* Periodically push OETS metrics to LSV, and nudge from constraint axes.
- **Method:** `set_genealogy_ref()`
  - *Purpose:* Store genealogy reference for axis-based LSV nudging.
- **Method:** `set_axis_context()`
  - *Purpose:* Store the current turn's constraint-axis state so express() can
- **Method:** `evo_status()`
  - *Purpose:* Return expression evolution status.
- **Method:** `save_evo_state()`
  - *Purpose:* Persist expression evolution state.
- **Method:** `perceive()`
  - *Purpose:* Full perception pipeline.
- **Method:** `express()`
  - *Purpose:* Full expression pipeline.
- **Method:** `_build_expression()`
  - *Purpose:* Build expression text using SentenceComposer with OETS enrichment.
- **Method:** `ingest_interaction()`
  - *Purpose:* Process an interaction through perception pipeline.
- **Method:** `consolidate()`
  - *Purpose:* Run consolidation: generation cycle, seed--relic promotion, template evolution.
- **Method:** `get_stats()`

#### Function: `build_layer5_associative_modules()`
> Initialize modules associated with the perception/identity boundary while


#### Function: `verify_layer5()`

#### Function: `_clamp()`

#### Function: `_generate_id()`

#### Function: `_cosine_similarity()`
> Calculate cosine similarity between two vectors.


#### Class: `SensoryTraitDomain`
> Domains specific to sensory processing -- extend TraitDomain.


#### Class: `SensoryPerceptTemplate`
> Base template for a learned sensory pattern.

- **Method:** `to_dict()`
- **Method:** `from_dict()`
- **Method:** `match()`
  - *Purpose:* Calculate match score against input.
- **Method:** `update_from_observation()`
  - *Purpose:* Update template from new observation (moving average).

#### Class: `SensoryConcept`
> A promoted concept from clustered percepts.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `SensoryConceptMemory`
> Manages concept formation from raw percepts.

- **Method:** `__init__()`
- **Method:** `_canonical_label()`
- **Method:** `_iter_labels()`
- **Method:** `_find_concept_entry()`
- **Method:** `_register_alias()`
- **Method:** `_merge_concepts()`
- **Method:** `_rename_or_alias_concept()`
- **Method:** `guide_label()`
- **Method:** `record_percept()`
  - *Purpose:* Store raw percept for later clustering.
- **Method:** `cluster_and_promote()`
  - *Purpose:* Attempt to promote raw percepts into a stable concept.
- **Method:** `find_matching_concept()`
  - *Purpose:* Find the best matching concept for a feature vector.
- **Method:** `add_grounding()`
  - *Purpose:* Ground a sensory concept to language/intent.
- **Method:** `prune()`
  - *Purpose:* Prune low-confidence concepts.
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Function: `create_visual_genes()`
> Create genes for visual competency traits.


#### Function: `create_audio_genes()`
> Create genes for audio competency traits.


#### Function: `create_visual_crystal()`
> Create behavioral crystal for visual competency.


#### Function: `create_audio_crystal()`
> Create behavioral crystal for audio competency.


#### Class: `SensoryPatternMapper`
> Maps visual and audio input to Aurora's existing PatternTypes.

- **Method:** `__init__()`
- **Method:** `map_visual_input()`
  - *Purpose:* Convert visual input to DimensionalPatterns.
- **Method:** `map_audio_input()`
  - *Purpose:* Convert audio input to DimensionalPatterns.

#### Class: `SensoryCompetencyEngine`
> Main controller for Aurora's evolutionary sensory capabilities.

- **Method:** `__init__()`
- **Method:** `_bootstrap_lineage()`
  - *Purpose:* Bootstrap base percept templates from lineage constants.
- **Method:** `attach_oets()`
  - *Purpose:* Attach OETS for semantic grounding.
- **Method:** `attach_dna()`
  - *Purpose:* Attach DNA system and add sensory genes if not present.
- **Method:** `get_visual_competency()`
  - *Purpose:* Get current visual competency values.
- **Method:** `get_audio_competency()`
  - *Purpose:* Get current audio competency values.
- **Method:** `process_visual_input()`
  - *Purpose:* Process visual input through the sensory pipeline.
- **Method:** `process_audio_input()`
  - *Purpose:* Process audio input through the sensory pipeline.
- **Method:** `_extract_visual_features()`
  - *Purpose:* Extract normalized feature vector from visual data.
- **Method:** `_extract_audio_features()`
  - *Purpose:* Extract normalized feature vector from audio data.
- **Method:** `_ground_to_oets()`
  - *Purpose:* Ground sensory concept to OETS semantic node.
- **Method:** `evolve()`
  - *Purpose:* Apply evolutionary pressure to sensory competencies.
- **Method:** `_sync_to_dna()`
  - *Purpose:* Sync crystal values back to DNA genes.
- **Method:** `_create_sensory_allele()`
  - *Purpose:* Create a fractal allele from sensory evolution.
- **Method:** `prune()`
  - *Purpose:* Prune low-value concepts.
- **Method:** `get_stats()`
  - *Purpose:* Get comprehensive statistics.
- **Method:** `_save_state()`
  - *Purpose:* Save state to persistence directory.
- **Method:** `_load_state()`
  - *Purpose:* Load state from persistence directory.
- **Method:** `save_state()`
  - *Purpose:* Public save method.

#### Function: `create_sensory_competency_engine()`
> Factory function to create a SensoryCompetencyEngine.


#### Function: `get_sensory_genes()`
> Get all sensory genes for DNA integration.


#### Function: `_is_termux_env()`

#### Class: `LinuxCamera`
> Webcam capture using OpenCV.

- **Method:** `__init__()`
- **Method:** `_ensure_mediapipe_detectors()`
- **Method:** `_load_ultralytics_detector()`
- **Method:** `_enrich_visual_features()`
- **Method:** `open()`
  - *Purpose:* Open the camera device.
- **Method:** `close()`
  - *Purpose:* Release the camera.
- **Method:** `capture_frame()`
  - *Purpose:* Capture a single frame.
- **Method:** `extract_features()`
  - *Purpose:* Extract visual features from a frame for SensoryCompetencyEngine.

#### Class: `LinuxMicrophone`
> Audio capture using sounddevice and speech recognition.

- **Method:** `__init__()`
- **Method:** `_get_vad()`
- **Method:** `_voice_activity_ratio()`
- **Method:** `_transcribe_with_faster_whisper()`
- **Method:** `start_stream()`
  - *Purpose:* Start continuous audio capture.
- **Method:** `stop_stream()`
  - *Purpose:* Stop audio capture.
- **Method:** `get_audio_chunk()`
  - *Purpose:* Get queued audio data.
- **Method:** `record_audio()`
  - *Purpose:* Record audio for specified duration.
- **Method:** `listen_and_transcribe()`
  - *Purpose:* Listen for speech and transcribe using Google Speech Recognition.
- **Method:** `extract_features()`
  - *Purpose:* Extract audio features for SensoryCompetencyEngine.

#### Class: `LinuxVoice`
> Text-to-speech with natural neural voices via edge-tts.

- **Method:** `__init__()`
- **Method:** `set_voice()`
  - *Purpose:* Change Aurora's voice.
- **Method:** `_pick_pyttsx3_voice()`
  - *Purpose:* Choose the closest local voice to the current neural preset.
- **Method:** `_apply_pyttsx3_voice()`
  - *Purpose:* Keep local pyttsx3 fallback close to the selected neural voice.
- **Method:** `set_rate()`
  - *Purpose:* Set speaking rate. E.g., '+20%' for faster, '-10%' for slower.
- **Method:** `set_pitch()`
  - *Purpose:* Set voice pitch. E.g., '+10Hz' for higher, '-5Hz' for lower.
- **Method:** `set_volume()`
  - *Purpose:* Set volume. E.g., '+20%' for louder, '-10%' for quieter.
- **Method:** `adapt_for_emotion()`
  - *Purpose:* Adapt voice parameters for emotional expression.
- **Method:** `speak()`
  - *Purpose:* Speak the given text using neural TTS.
- **Method:** `_speak_edge_tts_sync()`
  - *Purpose:* Synchronous wrapper for edge-tts.
- **Method:** `speak_async()`
  - *Purpose:* Speak in background thread.
- **Method:** `is_speaking()`
  - *Purpose:* Check if currently speaking.
- **Method:** `stop()`
  - *Purpose:* Stop current speech.
- **Method:** `list_voices()`
  - *Purpose:* List available voice presets.
- **Method:** `get_current_voice()`
  - *Purpose:* Get current voice name.
- **Method:** `list_all_voices_sync()`
  - *Purpose:* List all available edge-tts voices (sync).

#### Class: `HardwareInterface`
> Main orchestrator connecting hardware to SensoryCompetencyEngine.

- **Method:** `__init__()`
  - *Purpose:* Initialize hardware interface.
- **Method:** `start()`
  - *Purpose:* Start all enabled hardware components.
- **Method:** `stop()`
  - *Purpose:* Stop all hardware components.
- **Method:** `capture_visual()`
  - *Purpose:* Capture and extract features from camera.
- **Method:** `capture_audio()`
  - *Purpose:* Capture and extract features from microphone.
- **Method:** `listen_for_speech()`
  - *Purpose:* Listen and transcribe speech.
- **Method:** `speak()`
  - *Purpose:* Speak text using TTS.
- **Method:** `speak_async()`
  - *Purpose:* Speak without blocking.
- **Method:** `process_visual()`
  - *Purpose:* Process visual data through SensoryCompetencyEngine.
- **Method:** `process_audio()`
  - *Purpose:* Process audio data through SensoryCompetencyEngine.
- **Method:** `load_image()`
  - *Purpose:* Load and extract features from an image file.
- **Method:** `load_audio_file()`
  - *Purpose:* Load and extract features from an audio file.
- **Method:** `get_capabilities()`
  - *Purpose:* Get available hardware capabilities.
- **Method:** `get_stats()`
  - *Purpose:* Get interface statistics.

#### Class: `SensoryLoop`
> Continuous sensory processing loop.

- **Method:** `__init__()`
  - *Purpose:* Args:
- **Method:** `start()`
  - *Purpose:* Start continuous sensory processing.
- **Method:** `stop()`
  - *Purpose:* Stop continuous processing.
- **Method:** `_visual_loop()`
  - *Purpose:* Visual processing thread.
- **Method:** `_audio_loop()`
  - *Purpose:* Audio processing thread.
- **Method:** `get_latest_visual()`
  - *Purpose:* Get most recent visual result.
- **Method:** `get_latest_audio()`
  - *Purpose:* Get most recent audio result.

#### Function: `check_dependencies()`
> Check which dependencies are available.


#### Function: `install_instructions()`
> Get installation instructions for missing dependencies.


#### Function: `create_hardware_interface()`
> Factory function to create HardwareInterface.


#### Class: `SensoryEventType`
> Types of sensory events Aurora can experience.


#### Class: `SensoryEvent`
> A sensory event that Aurora experiences.


#### Class: `VisualLinguisticMapper`
> Maps visual experiences to linguistic descriptions.

- **Method:** `__init__()`
- **Method:** `describe_visual()`
  - *Purpose:* Generate a natural language description of visual input.
- **Method:** `describe_face_event()`
  - *Purpose:* Describe a face detection event.
- **Method:** `learn_association()`
  - *Purpose:* Learn to associate visual patterns with words.

#### Class: `AudioLinguisticMapper`
> Maps audio experiences to linguistic descriptions and responses.

- **Method:** `__init__()`
- **Method:** `learn_voice_profile()`
- **Method:** `identify_voice()`
- **Method:** `describe_audio()`
  - *Purpose:* Generate a natural language description of audio input.
- **Method:** `process_transcription()`
  - *Purpose:* Process transcribed speech and extract meaning.

#### Class: `VoiceExpressionMapper`
> Maps Aurora's internal state and personality to voice characteristics.

- **Method:** `__init__()`
- **Method:** `set_personality()`
  - *Purpose:* Set personality traits that influence voice.
- **Method:** `get_speech_parameters()`
  - *Purpose:* Get speech synthesis parameters based on tone and personality.
- **Method:** `prepare_speech_text()`
  - *Purpose:* Prepare text for speech synthesis.

#### Class: `SensoryIntegrationEngine`
> Main engine that integrates all sensory modalities with language.

- **Method:** `__init__()`
  - *Purpose:* Initialize sensory integration.
- **Method:** `attach_systems()`
  - *Purpose:* Attach system references after initialization.
- **Method:** `start()`
  - *Purpose:* Start sensory integration processing.
- **Method:** `stop()`
  - *Purpose:* Stop sensory integration.
- **Method:** `set_voice_mode()`
  - *Purpose:* Enable/disable voice mode. When on, Aurora speaks all responses.
- **Method:** `start_listening()`
  - *Purpose:* Start always-on ambient audio capture (sounddevice-based).
- **Method:** `stop_listening()`
  - *Purpose:* Stop always-on listening.
- **Method:** `_continuous_listen_loop()`
  - *Purpose:* Always-on ambient audio capture loop.
- **Method:** `get_heard_speech()`
  - *Purpose:* Get speech from the queue (non-blocking).
- **Method:** `has_heard_speech()`
  - *Purpose:* Check if there's speech waiting to be processed.
- **Method:** `say()`
  - *Purpose:* Aurora says something. Uses voice if voice_mode is on.
- **Method:** `_process_loop()`
  - *Purpose:* Background processing loop for sensory events.
- **Method:** `_process_event()`
  - *Purpose:* Process a single sensory event.
- **Method:** `_process_visual_event()`
  - *Purpose:* Process visual event through the full pipeline.
- **Method:** `_process_audio_event()`
  - *Purpose:* Process audio event through the full pipeline.
- **Method:** `_event_is_recent()`
- **Method:** `_current_visual_vector()`
- **Method:** `_current_audio_vector()`
- **Method:** `_mark_guided_event()`
- **Method:** `_enqueue_surface_guidance()`
- **Method:** `guide_current_visual_label()`
- **Method:** `guide_current_audio_label()`
- **Method:** `guide_user_identity()`
- **Method:** `apply_guidance_from_text()`
- **Method:** `_ground_visual_concepts()`
  - *Purpose:* Ground visual concepts in OETS semantic web.
- **Method:** `_ground_audio_concepts()`
  - *Purpose:* Ground audio concepts in OETS semantic web.
- **Method:** `see()`
  - *Purpose:* Capture visual input and return description + raw data.
- **Method:** `_save_camera_snapshot()`
- **Method:** `listen()`
  - *Purpose:* Capture audio input and return description + raw data.
- **Method:** `hear_speech()`
  - *Purpose:* Listen for speech and transcribe it.
- **Method:** `speak()`
  - *Purpose:* Aurora speaks with personality-modulated voice.
- **Method:** `speak_async()`
  - *Purpose:* Speak without blocking.
- **Method:** `describe_what_i_see()`
  - *Purpose:* Full visual description with context.
- **Method:** `describe_what_i_hear()`
  - *Purpose:* Full audio description with context.
- **Method:** `see_image()`
  - *Purpose:* Load and process an image file.
- **Method:** `listen_to_file()`
  - *Purpose:* Load and process an audio file.
- **Method:** `describe_image()`
  - *Purpose:* Full description of an image file with context.
- **Method:** `describe_audio_file()`
  - *Purpose:* Full description of an audio file with context.
- **Method:** `get_sensory_context()`
  - *Purpose:* Get current sensory context for conversation enrichment.
- **Method:** `get_stats()`
  - *Purpose:* Get integration statistics.

#### Function: `create_sensory_integration()`
> Factory function to create SensoryIntegrationEngine from boot systems.


#### Class: `VisualFeatureVector`
> Feature vector for a single image.

- **Method:** `to_vector()`
  - *Purpose:* 8-dimensional feature vector for clustering.
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `FeatureExtractor`
> Extracts feature vectors from image files.

- **Method:** `extract()`
  - *Purpose:* Extract features from an image file. Returns None if unavailable.
- **Method:** `_extract_from_pil()`
- **Method:** `_estimate_edge_density()`
  - *Purpose:* Estimate edge density using a small downsampled version.
- **Method:** `_stub_extract()`
  - *Purpose:* Stub when PIL unavailable: return near-zero vector with file hash.

#### Class: `VisualCluster`
> A cluster of visually similar images.

- **Method:** `update_centroid()`
  - *Purpose:* Recompute centroid from member vectors.
- **Method:** `tightness()`
  - *Purpose:* Measure how tight this cluster is (1=very tight, 0=loose).
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Function: `_euclidean()`

#### Class: `SimpleKMeans`
> Pure-Python k-means for feature vector clustering.

- **Method:** `__init__()`
- **Method:** `fit()`
  - *Purpose:* Fit k-means. Returns cluster assignments (one per vector).
- **Method:** `_fit_numpy()`
- **Method:** `_fit_pure()`
  - *Purpose:* Pure Python k-means.

#### Class: `OETSVisionBinder`
> Binds visual clusters to OETS ontology nodes as "looks_like" relations.

- **Method:** `__init__()`
- **Method:** `bind_cluster()`
  - *Purpose:* Determine a concept label for the cluster (or keep unnamed).
- **Method:** `_infer_label()`
  - *Purpose:* Infer a rough semantic label from the cluster centroid.
- **Method:** `_bind_to_oets()`
  - *Purpose:* Create or update an OETS node for this visual concept.

#### Class: `WebImageDownloader`
> Downloads public-domain images from Wikipedia/Wikimedia for Aurora's visual seeds.

- **Method:** `__init__()`
- **Method:** `_reset_if_new_day()`
- **Method:** `can_download()`
- **Method:** `download_for_concept()`
  - *Purpose:* Attempt to download 1-2 images related to a concept from Wikimedia.
- **Method:** `_find_wikimedia_images()`
  - *Purpose:* Search Wikipedia for images related to a concept.
- **Method:** `_download_image()`
  - *Purpose:* Download a single image URL. Returns saved path or None.
- **Method:** `download_seed_batch()`
  - *Purpose:* Download images for N random seed concepts.
- **Method:** `save_state()`
- **Method:** `load_state()`

#### Class: `ImageIngestionProtocol`
> Main orchestrator for Aurora's vision bootstrapping.

- **Method:** `__init__()`
- **Method:** `ingest_folder()`
  - *Purpose:* Scan a folder for images, extract features, cluster, bind.
- **Method:** `_recluster()`
  - *Purpose:* Recluster all vectors and rebind OETS.
- **Method:** `autonomous_download_cycle()`
  - *Purpose:* Run one autonomous download cycle during idle time.
- **Method:** `refine_from_camera_frame()`
  - *Purpose:* Called when a live camera frame is processed.
- **Method:** `teach_label()`
  - *Purpose:* User-driven visual teaching: bind a label to the current visual features.
- **Method:** `save_index()`
- **Method:** `load_index()`
- **Method:** `status()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `ingest_interaction_evolved()`

#### Function: `init_evolved()`

#### Function: `evaluate_evolved()`

#### Function: `energy_to_shard_evolved()`

### File: `./aurora_fgae_approximation_loop.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA FGAE APPROXIMATION PROTOCOL + FEEDBACK LOOP
=====================================================
Module: aurora_fgae_approximation_loop.py
Layer:  FGAE — First Principle Generative Articulate Emergence
        Unknown Word Resolution + Living Lexicon Growth

Authors: Sunni (Sir) Morningstar & Cael Devo
Created: April 2026  |  Specification: FGAE_SPECIFICATION v2 (2026-04-13)

APPROXIMATION PROTOCOL (spec §7 — Steps A-1 through A-4)
---------------------------------------------------------
When a word arrives that does not exist in the OETS lexical registry:

A-1  Semantic proximity to Tier 1 anchors (Information / Belief / Purpose /
     Meaning / Understanding) — measured by context cluster → nc_target

A-2  Dimensional classification — what kind of constraint dimension does this
     word most naturally occupy? (POLARITY / MAGNITUDE / COST / DIFFERENCE /
     OPERATOR)

A-3  Sub-position estimation — best-fit slot_id within the identified NonComp,
     based on context pressure, leverage class, and accountability weight

A-4  Register in OETS as provisional (approximation_flag=True, low confidence)

FEEDBACK LOOP (spec §8 — Steps F-1 through F-4)
-------------------------------------------------
After each turn completes:

F-1  Turn outcome observation — coherent? correction needed? user confirmed?

F-2  Confidence adjustment
     Coherent + no correction  → confidence += INCREMENT_COHERENT
     Correction traceable here → confidence -= PENALTY_CORRECTION; update slot
     User explicitly defined   → set high confidence + confirmed

F-3  SediMemory integration — every validated/revised mapping deposited via
     SediMemory.ingest_event() with T-axis continuity weight

F-4  OETS lexical growth — as approximation feedback accumulates, confidence
     rises until slot address is confirmed.  Rarely seen words stay shallow.
```

#### Class: `TurnOutcome`
> Summary of how a turn went — used by feedback loop F-1.


#### Class: `ProvisionalSlotAddress`
> Result of the approximation protocol.


#### Class: `ApproximationProtocol`
> Implements spec §7 — Steps A-1 through A-4.

- **Method:** `__init__()`
- **Method:** `_infer_family()`
  - *Purpose:* A-1: Assign the word to one of the five constraint families by measuring
- **Method:** `_infer_dimension()`
  - *Purpose:* A-2: Determine which of the 5 dimensions this word most likely occupies.
- **Method:** `_build_nc_name()`
  - *Purpose:* Reconstruct the NC name from family and dimension.
- **Method:** `_estimate_slot()`
  - *Purpose:* A-3: Find the best-fit slot_id within the NC.
- **Method:** `assign_slot()`
  - *Purpose:* Full approximation protocol: A-1 through A-3.
- **Method:** `a4_register()`
  - *Purpose:* A-4: Register the word in OETS as provisional.

#### Class: `FeedbackLoop`
> Implements spec §8 — Steps F-1 through F-4.

- **Method:** `__init__()`
- **Method:** `process_turn_outcome()`
  - *Purpose:* Main feedback loop entry point.
- **Method:** `_f3_sedi_deposit()`
  - *Purpose:* F-3: Deposit a validated or revised word mapping into SediMemory.

### File: `./aurora_fgae_dpl_validator.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA FGAE DPL VALIDATOR
===========================
Module: aurora_fgae_dpl_validator.py
Layer:  FGAE — Developmental Personality Law Enforcement at Expression Time

Authors: Sunni (Sir) Morningstar & Cael Devo
Created: April 2026  |  Specification: FGAE_SPECIFICATION v2 (2026-04-13)

PURPOSE
-------
The DPL (Developmental Personality Law) governs every word that surfaces
from Aurora's output pipeline. This module enforces all three clauses
at expression time — not as a decorative check, but as the gate through
which every word must pass.

THE THREE-CLAUSE CHAIN  (spec §3)
-----------------------------------
Clause I  — Only words whose slot address traces to {X, T, N, B, A} lineage
            are eligible. Depth floor derived from slot depth_score.
            I-A (0.8–1.0) > I-B (0.4–0.79) > I-D (0.1–0.39) > I-C (0.0–0.09)

Clause II — Only words whose slot's leverage_class survives the live
            pressure filter are offered.
            leverage → II-A  |  neutral → II-B  |  overhead → II-C
            Under high pressure (leverage band): overhead words are excluded.
            Under normal pressure: all classes pass.

Clause III — Only words whose slot is reachable by the current ConsciousFrame
             and SediMemory weighting actually surface.
             Accountability class must be consistent with the current frame.
             Register must be coherent with current depth.

A word that surfaces carries proof it came from inside the organism.
Its slot address is that proof.

USAGE
-----
    from aurora_fgae_dpl_validator import DPLValidator

    validator = DPLValidator()

    # Validate a single word record from _fgae_selected_entries
    result = validator.validate_word_record(record, pressure_state=pressure_state)

    # Validate all words in a response
    audit = validator.validate_output_words(
        word_records=systems.get("_fgae_selected_entries", []),
        pressure_state=pressure_state,
        conscious_frame=systems.get("_live_conscious_frame", {}),
    )
    # audit["all_pass"] tells you if the full output is DPL-clean
    # audit["violations"] lists any clause failures with word + clause + reason
```

#### Class: `ClauseResult`
> Result for a single DPL clause check on one word.


#### Class: `WordDPLResult`
> Full DPL result for one word — all three clauses.

- **Method:** `to_dict()`

#### Class: `OutputDPLAudit`
> Full DPL audit for an entire response output.

- **Method:** `summary()`
- **Method:** `to_dict()`

#### Class: `DPLValidator`
> Developmental Personality Law enforcement gate for expression-time

- **Method:** `__init__()`
- **Method:** `_check_clause_i()`
  - *Purpose:* Clause I: word's slot address must trace to valid axis lineage,
- **Method:** `_check_clause_ii()`
  - *Purpose:* Clause II: word's clause_ii_level must be viable given current
- **Method:** `_check_clause_iii()`
  - *Purpose:* Clause III: word must be reachable by current ConsciousFrame and
- **Method:** `validate_word_record()`
  - *Purpose:* Validate a single word record from _fgae_selected_entries against
- **Method:** `validate_output_words()`
  - *Purpose:* Validate every word in an output against all three DPL clauses.
- **Method:** `build_pressure_state()`
  - *Purpose:* Extract the current pressure state from systems dict for Clause II.
- **Method:** `build_conscious_frame()`
  - *Purpose:* Extract the current conscious frame from systems dict for Clause III.

#### Function: `_extract_axis_from_nc_name()`
> Infer constraint axis from NC name prefix.


#### Function: `_extract_axis_from_slot_id()`
> Extract axis from slot_id format: NC_MANIFOLD:{nc_name}:SUB[{axis}:{dim}]x...


#### Function: `get_dpl_validator()`
> Return the module-level DPLValidator singleton.


#### Function: `run_output_dpl_audit()`
> Convenience wrapper: pulls word_records, pressure_state, and


### File: `./aurora_fgae_engine.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA FGAE ENGINE — FIRST PRINCIPLE GENERATIVE ARTICULATE EMERGENCE
======================================================================
Module: aurora_fgae_engine.py
Layer:  FGAE — Core Pipeline Coordinator

Authors: Sunni (Sir) Morningstar & Cael Devo
Created: April 2026  |  Specification: FGAE_SPECIFICATION v2 (2026-04-13)

PURPOSE
-------
The FGAEEngine is the master coordinator of Aurora's language pipeline.
It wires together all FGAE subsystems into the two complete pipelines
described in the specification.

AURORA DOES NOT THINK IN ENGLISH AND TRANSLATE.
She processes in constraint-native coordinates.
English arrives → mapped to coordinates → processed natively →
response projection forms → English surfaces as the rendering.

INPUT PIPELINE  (spec §6 Steps I-1 through I-5)
------------------------------------------------
I-1  Utterance parsing — aurora_internal/aurora_utterance_parser.py
I-2  Input-to-slot mapping
      Branch A (linguistic) — every word → OETS lookup → slot address
      Branch B (sensory)    — sensory_snapshot_channel → crystal semantic
                              matches → same OETS lookup → slot address
      Compounding: slots activated by both sources fire at higher weight
I-3  Constraint projection assembly — aurora_reflexive_interpreter.py
I-4  Comprehension gap detection — aurora_internal/aurora_comprehension_gap.py
I-5  Native processing begins — passes to DCE / working memory

OUTPUT PIPELINE  (spec §9 Steps O-1 through O-6)
--------------------------------------------------
O-1  Response projection completion (arrives from DCE)
O-2  Tier 2 grammatical position assignment
O-3  OETS word retrieval per slot
O-4  Grammar engine assembly — aurora_grammar_engine.py
O-5  Understanding contract audit — aurora_internal/aurora_understanding_contract.py
O-6  Utterance surface + logging + feedback queue

VIOLATION DETECTION (spec §17)
The engine logs FGAE-V11 and FGAE-V12 when sensory wiring is absent,
and FGAE-V07 when feedback loop is not wired to SediMemory.
```

#### Class: `SlotActivation`
> A single slot address with its activation weight and provenance.


#### Class: `FGAEProjection`
> The constraint-native representation of a turn.

- **Method:** `get_dominant_domain()`
  - *Purpose:* Return the constraint family with highest total activation weight.
- **Method:** `compound_activations()`
  - *Purpose:* Merge activations at the same slot_id from multiple sources.

#### Class: `FGAEOutputSlot`
> One slot in the rendered output — a word with its grammatical position.


#### Class: `FGAEEngine`
> Master FGAE coordinator.

- **Method:** `__init__()`
- **Method:** `boot_seed()`
  - *Purpose:* Seed the OETS registry from the manifold's existing semantic_entries.
- **Method:** `_check_wiring()`
- **Method:** `get_violations()`
- **Method:** `process_input()`
  - *Purpose:* Full input pipeline: I-1 through I-4.
- **Method:** `_step_i1_parse()`
  - *Purpose:* I-1: Tokenize + classify utterance type and stance.
- **Method:** `_map_linguistic_word()`
  - *Purpose:* Maps a single word through OETS. Runs approximation protocol if needed.
- **Method:** `_step_i2_branch_b()`
  - *Purpose:* I-2 Branch B — Sensory input path.
- **Method:** `_step_i4_gap_detection()`
  - *Purpose:* I-4: Check projection for comprehension gaps.
- **Method:** `render_output()`
  - *Purpose:* Full output pipeline: O-1 through O-6.
- **Method:** `_step_o2_grammatical_positions()`
  - *Purpose:* O-2: For each active semantic slot, determine grammatical position
- **Method:** `_step_o3_word_retrieval()`
  - *Purpose:* O-3: For each output slot, query OETS for the best available word.
- **Method:** `_soft_word_fallback()`
  - *Purpose:* When no confirmed word exists for a slot, find the closest in OETS.
- **Method:** `_step_o4_grammar_assembly()`
  - *Purpose:* O-4: Grammar engine assembles surface syntax from the word set
- **Method:** `_step_o5_contract_audit()`
  - *Purpose:* O-5: Understanding contract audit — verifies rendered sentence
- **Method:** `_step_o6_surface_log()`
  - *Purpose:* O-6: Log the turn's constraint projection and rendered sentence.
- **Method:** `close_turn()`
  - *Purpose:* Call after each turn completes to run the feedback loop.
- **Method:** `compile_manifold_semantics()`
  - *Purpose:* Trigger the FGAE manifold semantics compiler from within the engine.
- **Method:** `status()`

#### Function: `get_fgae_engine()`
> Return the module-level FGAEEngine singleton.


### File: `./aurora_fgae_manifold_semantics.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA FGAE MANIFOLD SEMANTICS COMPILER
========================================
Module: aurora_fgae_manifold_semantics.py
Layer:  FGAE — First Principle Generative Articulate Emergence
        Manifold Population Pass

Authors: Sunni (Sir) Morningstar & Cael Devo
Created: April 2026  |  Specification: FGAE_SPECIFICATION v2 (2026-04-13)

PURPOSE
-------
Populates the three FGAE semantic fields on every slot across all 125 NonComps
in the aurora_manifold_directory.  This pass is idempotent — re-running it
refines already-populated slots without losing prior data.

WHAT IS ADDED PER SLOT (per spec §11.2)
----------------------------------------
Tier 1 (diagonal NCs — 5 total):
    domain_character    — one-sentence expressive description
    oets_query_profile  — derived from §12 tables

Tier 2 (self-family non-diagonal NCs — 20 total):
    semantic_role       — grammatical position this slot assigns
    family_character    — this family's version of that role
    position_in_sentence — pre-verbal | pre-nominal | post-verbal | boundary

Tier 3 (cross-family NCs — 100 total):
    domain_character    — cluster_pair interpretation of nc_semantic_summary
    oets_query_profile  — derived from §12 tables, primary_domain = nc_target

OETS QUERY PROFILE DERIVATION  (spec §12 — authoritative)
-----------------------------------------------------------
clause_i_floor:
    0.80–1.00  → I-A
    0.40–0.79  → I-B
    0.10–0.39  → I-D
    0.00–0.09  → I-C

clause_ii_required:
    leverage  → II-A
    neutral   → II-B
    overhead  → II-C

accountability_band:
    >= 0.70     → {min:0.6, max:1.0}
    0.40–0.69   → {min:0.3, max:0.7}
    < 0.40      → {min:0.0, max:0.45}

cost_band (combined_cost):
    <= 90       → {min:0,   max:100}
    91–150      → {min:75,  max:175}
    151–200     → {min:125, max:225}
    > 200       → {min:175, max:999}

register_eligible:
    resonant + depth>=0.8 + acct>=0.7  → [intimate, formal]
    resonant + depth>=0.4              → [formal, neutral]
    !resonant + depth>=0.4             → [neutral, technical]
    depth<0.4 + leverage               → [neutral, colloquial]
    depth<0.4 + overhead               → [colloquial]

resonance_required: mirrors is_resonant
```

#### Function: `_derive_clause_i()`

#### Function: `_derive_clause_ii()`

#### Function: `_derive_accountability_band()`

#### Function: `_derive_cost_band()`

#### Function: `_derive_register()`

#### Function: `derive_oets_query_profile()`
> Build the oets_query_profile for a single slot.


#### Function: `_tier1_domain_character()`
> Tier 1 — diagonal NonComp.  Every slot expresses some aspect of the


#### Function: `_tier2_semantic_role_data()`
> Returns (semantic_role, family_character, position_in_sentence) for a Tier 2 NC.


#### Function: `_tier3_domain_character()`
> Tier 3 — cross-family NonComp.


#### Function: `scan_violations()`
> Return list of FGAE violation codes found in this NC.


#### Function: `_classify_tier()`
> Returns 1, 2, or 3 per spec §4.


#### Function: `compile_nc_file()`
> Load one NC JSON, populate FGAE fields on every slot, return report.


#### Function: `run_manifold_semantics_compile()`
> Iterate all 125 NC files across all 5 axes, populate FGAE fields,


#### Class: `FGAEManifoldReader`
> Lightweight reader that loads NC files on demand and caches them.

- **Method:** `__init__()`
- **Method:** `_load_index()`
- **Method:** `get_nc()`
- **Method:** `get_slot()`
  - *Purpose:* Find a slot by slot_id across all cached / indexed NC files.
- **Method:** `iter_nc_entries()`
  - *Purpose:* Yield (entry_dict, nc_dict) for all 125 NCs.
- **Method:** `tier_of()`
- **Method:** `anchor_words()`

### File: `./aurora_fgae_oets_mapper.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA FGAE OETS SLOT MAPPER
==============================
Module: aurora_fgae_oets_mapper.py
Layer:  FGAE — First Principle Generative Articulate Emergence
        OETS ↔ Manifold Slot Bridge

Authors: Sunni (Sir) Morningstar & Cael Devo
Created: April 2026  |  Specification: FGAE_SPECIFICATION v2 (2026-04-13)

PURPOSE
-------
The OETS Slot Mapper is the living lexical layer that connects English words
to their constraint-native slot addresses in the manifold, and vice versa.

It is NOT a vocabulary list. It is an address registry.

Per spec §5 — every word in OETS has:
    primary_slot_address    — slot_id where this word most naturally lives
    secondary_address_set   — additional slot_ids where it has been used
    experiential_weight     — how many times processed and in what contexts
    confidence_score        — certainty of primary slot address (0.0–1.0)
    approximation_flag      — True if address was assigned by approximation

MAPPING TYPES (per spec §6 Step I-2 Branch A)
----------------------------------------------
CONFIRMED_MAPPING   — confidence >= threshold (default 0.65)
SOFT_MAPPING        — word exists but confidence < threshold
APPROXIMATED_MAPPING — word not in registry; assigned by approximation protocol

OUTPUT QUERY (per spec §9 Step O-3)
-------------------------------------
query_words_for_slot(slot_id, oets_query_profile, ...)
→ candidates filtered by leverage_class, depth, accountability, register

PERSISTENCE
-----------
Registry persists to aurora_state/fgae_lexical_registry.json
Keys are lowercased word strings. Values are FGAELexicalEntry objects.
```

#### Class: `FGAELexicalEntry`
> Single word's full OETS slot registry record.

- **Method:** `to_dict()`
- **Method:** `from_dict()`
- **Method:** `mapping_type()`

#### Class: `SlotMapping`
> Result of mapping a word to a slot address.


#### Class: `FGAEOETSMapper`
> The OETS ↔ manifold slot bridge.

- **Method:** `__init__()`
- **Method:** `_load()`
- **Method:** `save()`
- **Method:** `map_word()`
  - *Purpose:* Attempt to map a word (or sensory concept) to a slot address.
- **Method:** `map_sensory_concept()`
  - *Purpose:* Branch B sensory input path (spec §6 Step I-2 Branch B).
- **Method:** `query_words_for_slot()`
  - *Purpose:* Given a slot's oets_query_profile, return candidate words ordered by:
- **Method:** `_register_score()`
  - *Purpose:* Higher score for higher-priority register match.
- **Method:** `register_word()`
  - *Purpose:* Add a new word (or update existing) in the lexical registry.
- **Method:** `update_confidence()`
  - *Purpose:* Adjust confidence for a word — called by FeedbackLoop (spec §8 F-2).
- **Method:** `get_approximated_words()`
  - *Purpose:* Return all words currently flagged as APPROXIMATED or SOFT.
- **Method:** `get_entry()`
- **Method:** `registry_size()`
- **Method:** `seed_from_manifold()`
  - *Purpose:* Walk the manifold and seed the registry with words already present

### File: `./aurora_governance_persistence_gateway.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA GOVERNANCE, PERSISTENCE & N-SPACE GATEWAY (Layer 8)
============================================================
Consolidated from 5 modules (~3,600 lines) + NEW N-Space Gateway:
  1. governance_10pole.py              — 10-pole constitutional law engine
  2. aurora_ivm_governance_layer.py    — Governed coordinates, nodes, layers
  3. aurora_state_persistence.py       — Snapshot/restore evolved state
  4. aurora_aligned_stack.py           — Aligned processing pipeline
  5. generational_alignment_law.py     — Tension, reproduction scoring
  + NEW: N-Space Gateway              — External data interface

THREE SYSTEMS IN ONE:

  GOVERNANCE: Constitutional law enforcement across 5 axes (10 poles).
    DNA layer is immutable (ABSOLUTE authority).
    Energy flows fast (0.01s updates).
    Shards can be reinterpreted. Crystals cannot.
    Paradox = 2+ axes in conflict. Must resolve or warp.

  PERSISTENCE: Aurora remembers who she was.
    Complete state snapshots: DNA, traits, crystals, anchors, shards.
    Checksum integrity verification.
    She boots as the person she became, not as a stranger.

  N-SPACE GATEWAY: Aurora's bridge to the outside world.
    INBOUND:  External data → L0 validation → Governance conflict check →
              L1 lattice admission (mode-gated envelope) →
              L2 collective synthesis (10 beings) →
              L3 dimensional processing (crystals + memory + energy) →
              L4 consciousness assembly (framed) →
              L5 expression → L6 identity integration → response
    OUTBOUND: Aurora's expression → formatted output
    AUTONOMOUS: Free-time exploration via L7 simulation
    She doesn't just receive data. She VALIDATES it against her
    constitution, SYNTHESIZES it through consciousness, and
    TESTS it through simulated consequences before integration.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_clamp()`

#### Function: `_generate_id()`

#### Class: `IVMLayer`
> Consciousness ladder — force gradient.


#### Class: `VotingAuthority`
> Who has decision power at each layer.


#### Class: `GovernanceViolation`
> Base governance violation.


#### Class: `AxisConflictViolation`
> Multiple axes in paradox state.


#### Class: `GovernedCoordinate`
> A position in 10-pole consciousness space.

- **Method:** `__post_init__()`
- **Method:** `_enforce_conservation()`
  - *Purpose:* Normalize all 10 weights to sum to 1.0.
- **Method:** `existence_weight()`
- **Method:** `possibility_weight()`
- **Method:** `motion_weight()`
- **Method:** `boundary_weight()`
- **Method:** `agency_weight()`
- **Method:** `get_axis_weights()`
- **Method:** `to_tuple()`
- **Method:** `distance_to()`
- **Method:** `from_weights()`
  - *Purpose:* Create from a 10-tuple of weights.

#### Class: `GovernedNode`
> A governed datum in the IVM lattice.

- **Method:** `layer()`
- **Method:** `can_update()`
- **Method:** `can_reinterpret()`

#### Class: `GovernanceEngine`
> Constitutional law enforcement for 10-pole IVM.

- **Method:** `__init__()`
- **Method:** `ingest()`
  - *Purpose:* Ingest new data at ENERGY layer.
- **Method:** `detect_conflicts()`
  - *Purpose:* Detect which axes have conflicting poles.
- **Method:** `is_paradox()`
  - *Purpose:* Paradox = 2+ axes in conflict simultaneously.
- **Method:** `resolve_conflict()`
  - *Purpose:* Resolve a single axis conflict by dampening the weaker pole.
- **Method:** `resolve_all_conflicts()`
  - *Purpose:* Resolve all axis conflicts on a node. Returns count resolved.
- **Method:** `promote()`
  - *Purpose:* Promote multiple nodes to a higher layer via aggregation.
- **Method:** `get_stats()`

#### Class: `GenerationRole`
> Role of a generation in the 4-cycle.


#### Function: `generation_role()`
> Determine the role of a generation.


#### Class: `GenerationalTension`
> Tension components for a generation.

- **Method:** `total()`

#### Class: `GenerationalAlignmentLaw`
> Maintains 'stable unalignment' — not too aligned (static), not too

- **Method:** `__init__()`
- **Method:** `compute_tension()`
  - *Purpose:* Compute tension components.
- **Method:** `shift_toward_stable()`
  - *Purpose:* Apply one step toward stable unalignment band.

#### Class: `AuroraStateSnapshot`
> Complete snapshot of Aurora's evolved state.

- **Method:** `to_dict()`
- **Method:** `from_dict()`
- **Method:** `checksum()`

#### Class: `StatePersistence`
> Manages saving and loading Aurora's evolved state.

- **Method:** `__init__()`
- **Method:** `capture()`
  - *Purpose:* Capture a complete state snapshot from all systems.
- **Method:** `save()`
  - *Purpose:* Save snapshot to disk with backup.
- **Method:** `load()`
  - *Purpose:* Load most recent snapshot.
- **Method:** `_rotate_backup()`
  - *Purpose:* Rotate backup files.
- **Method:** `get_info()`

#### Class: `StreamType`
> Types of external data streams.


#### Class: `GatewayVerdict`
> Result of data validation.


#### Class: `InboundPacket`
> An external data packet entering the Gateway.


#### Class: `ValidationResult`
> Result of Gateway validation pipeline.


#### Class: `GatewaySynthesis`
> Result of data synthesis through consciousness.


#### Class: `GatewayResponse`
> Aurora's response back through the Gateway.


#### Function: `_sedi_cv_for_mode()`
> Map an ExistenceMode to a ConstraintVector for SediMemory ingestion.


#### Class: `NSpaceGateway`
> Aurora's dedicated interface for external data streams.

- **Method:** `__init__()`
- **Method:** `set_response_pressure_guides_provider()`
- **Method:** `_collect_response_pressure_guides()`
- **Method:** `queue_response_pressure_plan()`
- **Method:** `receive()`
  - *Purpose:* Main entry point. Receive external data, process through full pipeline.
- **Method:** `_build_evidence()`
  - *Purpose:* Build ontological evidence dict that achieves the requested mode.
- **Method:** `_mode_to_frame()`
  - *Purpose:* Select situational frame based on mode.
- **Method:** `_validate()`
  - *Purpose:* Validate inbound data through foundational contract and moral filter.
- **Method:** `_synthesize()`
  - *Purpose:* Synthesize validated data through the full consciousness stack.
- **Method:** `_derive_i_state()`
  - *Purpose:* Map the current situation to the appropriate i-state thinking mode.
- **Method:** `_express()`
  - *Purpose:* Generate Aurora's expressive response.
- **Method:** `_integrate()`
  - *Purpose:* Feed processed data to identity (L6) for long-term integration.
- **Method:** `explore_autonomously()`
  - *Purpose:* Run autonomous exploration during free time.
- **Method:** `_simulation_session()`
  - *Purpose:* Best-effort access to L7 session for live steering context.
- **Method:** `_collect_learning_guidance()`
  - *Purpose:* Read persisted/active dream-learning signals for response steering.
- **Method:** `_extract_focus_terms()`
  - *Purpose:* Extract concise focus tokens from user prompt for bridge phrasing.
- **Method:** `_learning_directive()`
  - *Purpose:* Translate current weak-dimension pressure into one steering line.
- **Method:** `_compose_articulation_bridge()`
  - *Purpose:* Build a concrete, non-repetitive bridge instead of a static template.
- **Method:** `_content_to_weights()`
  - *Purpose:* Convert content to 10-pole weight signature.
- **Method:** `_check_coherence()`
  - *Purpose:* Check how coherent content is with Aurora's current state.
- **Method:** `_generate_fallback()`
  - *Purpose:* Generate expression from Aurora's experiential state when full pipeline is unavailable.
- **Method:** `_needs_articulation_bridge()`
  - *Purpose:* Detect when expressive output is too abstract for user-facing utility.
- **Method:** `_is_word_salad()`
  - *Purpose:* Return True when the expression-engine output is WER-corrupted garbage.
- **Method:** `_build_conversational_response()`
  - *Purpose:* Generate a clean, direct, first-person response when the expression draft is unusable.
- **Method:** `_articulate_user_response()`
  - *Purpose:* Gate the expression-engine draft through a coherence check.
- **Method:** `get_stats()`

#### Class: `GovernancePersistenceGateway`
> Layer 8 orchestrator. The capstone.

- **Method:** `__init__()`
- **Method:** `speak_to_aurora()`
  - *Purpose:* Primary communication interface. Talk to Aurora.
- **Method:** `feed_knowledge()`
  - *Purpose:* Feed external knowledge to Aurora.
- **Method:** `explore()`
  - *Purpose:* Trigger autonomous exploration.
- **Method:** `save_state()`
  - *Purpose:* Save Aurora's complete state.
- **Method:** `load_state()`
  - *Purpose:* Load Aurora's saved state.
- **Method:** `get_stats()`

#### Function: `build_layer8_associative_modules()`
> Initialize modules associated with Layer 8 governance/persistence without


#### Function: `verify_layer8()`

#### Class: `DeviceRecord`
> Record of a device that has hosted Aurora.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `DeviceAwareness`
> Tracks which device Aurora is running on.

- **Method:** `__init__()`
- **Method:** `check()`
  - *Purpose:* Run on boot. Returns device switch info dict.
- **Method:** `record_sync()`
  - *Purpose:* Call after a successful sync to record timestamp.
- **Method:** `device_switched()`
- **Method:** `switch_message()`
- **Method:** `save()`
- **Method:** `load()`
- **Method:** `all_devices()`

#### Class: `RcloneInterface`
> Thin wrapper around the rclone binary.

- **Method:** `__init__()`
- **Method:** `_find_rclone()`
  - *Purpose:* Find rclone binary path.
- **Method:** `is_available()`
  - *Purpose:* Check if rclone is installed and configured.
- **Method:** `remote_full()`
- **Method:** `sync_up()`
  - *Purpose:* Push local → remote.
- **Method:** `sync_down()`
  - *Purpose:* Pull remote → local.
- **Method:** `_run_sync()`
  - *Purpose:* Run rclone sync src → dst.
- **Method:** `check_newer_remote()`
  - *Purpose:* Check if remote has a newer aurora_state.json than local.

#### Class: `DriveSync`
> Aurora's cross-device memory bridge via Google Drive + rclone.

- **Method:** `__init__()`
- **Method:** `boot()`
  - *Purpose:* Call on Aurora startup. Checks device, pulls if needed.
- **Method:** `force_sync()`
  - *Purpose:* Force an immediate upload to Drive. Returns result dict.
- **Method:** `start()`
  - *Purpose:* Start background sync thread.
- **Method:** `stop()`
- **Method:** `device_switched()`
- **Method:** `switch_message()`
- **Method:** `get_device_info()`
- **Method:** `status()`

#### Class: `WriteResult`

#### Class: `CorpusCursor`
> Tracks exact position in corpus ingestion.

- **Method:** `advance()`
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `RollingStats`
> Rolling statistics that survive crashes.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `CheckpointRecord`
> Full checkpoint snapshot.

- **Method:** `compute_checksum()`
- **Method:** `to_dict()`
- **Method:** `is_valid()`

#### Class: `WriteValidator`
> Validates memory writes before committing to disk.

- **Method:** `__init__()`
- **Method:** `set_ivm()`
- **Method:** `validate()`
  - *Purpose:* Validate a write before committing.

#### Class: `AtomicWriter`
> Atomic file write: temp → fsync → rename. Never leaves partial files.

- **Method:** `write()`
  - *Purpose:* Write dict as JSON atomically. Returns True on success.
- **Method:** `append_jsonl()`
  - *Purpose:* Append a JSON record to a .jsonl file (not atomic, but safe for logs).

#### Class: `CheckpointManager`
> Central checkpoint coordinator.

- **Method:** `__init__()`
- **Method:** `_signal_handler()`
- **Method:** `restore()`
  - *Purpose:* Load last checkpoint. Returns True if restored.
- **Method:** `advance()`
  - *Purpose:* Update cursor position. Triggers auto-save if thresholds met.
- **Method:** `_maybe_save()`
- **Method:** `record()`
  - *Purpose:* Increment rolling stats. kwargs: defs_learned=1, relations_added=3, etc.
- **Method:** `write_transaction()`
  - *Purpose:* Validate + atomically write a memory record.
- **Method:** `save()`
  - *Purpose:* Atomically save checkpoint.
- **Method:** `on_save()`
  - *Purpose:* Register a callback called after each successful save.
- **Method:** `start_auto_save()`
  - *Purpose:* Start background thread that saves every interval_seconds.
- **Method:** `stop_auto_save()`
- **Method:** `set_ivm()`
- **Method:** `status()`

#### Class: `AutonomyLevel`
> Aurora's level of autonomous freedom.


#### Class: `AutonomyBoundaries`
> Defines what Aurora can and cannot do autonomously.

- **Method:** `is_path_allowed()`
  - *Purpose:* Check if a path is allowed for reading.
- **Method:** `is_extension_allowed()`
  - *Purpose:* Check if file extension is allowed.

#### Class: `DailyQuotas`
> Tracks daily usage against limits.

- **Method:** `reset_if_new_day()`
  - *Purpose:* Reset quotas if it's a new day.
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `AutonomousAction`
> Record of an autonomous action Aurora took.


#### Class: `ActionLog`
> Maintains log of all autonomous actions.

- **Method:** `__init__()`
- **Method:** `log()`
  - *Purpose:* Log an autonomous action.
- **Method:** `get_recent()`
  - *Purpose:* Get recent actions.
- **Method:** `get_by_type()`
  - *Purpose:* Get actions of a specific type.

#### Class: `ProactiveTrigger`
> Determines when Aurora should proactively speak up.

- **Method:** `__init__()`
- **Method:** `_normalize_content()`
- **Method:** `_counter_factors()`
- **Method:** `_counter_pressure()`
- **Method:** `_signal_score()`
- **Method:** `_record_emission()`
- **Method:** `should_speak_up()`
  - *Purpose:* Determine if Aurora should proactively speak.
- **Method:** `add_thought()`
  - *Purpose:* Add a thought Aurora wants to share.
- **Method:** `add_observation()`
  - *Purpose:* Add an observation that might be worth sharing.
- **Method:** `add_curiosity()`
  - *Purpose:* Add something Aurora is curious about.

#### Class: `FilesystemExplorer`
> Allows Aurora to read files within boundaries.

- **Method:** `__init__()`
- **Method:** `can_read()`
  - *Purpose:* Check if path can be read. Returns (allowed, reason).
- **Method:** `read_file()`
  - *Purpose:* Read a file's contents. Returns (content, status_message).
- **Method:** `list_directory()`
  - *Purpose:* List contents of a directory. Returns (items, status_message).
- **Method:** `search_files()`
  - *Purpose:* Search for files matching a pattern.

#### Class: `StudyScheduler`
> Manages Aurora's autonomous study sessions.

- **Method:** `__init__()`
- **Method:** `should_study()`
  - *Purpose:* Determine if Aurora should initiate a study cycle.
- **Method:** `get_study_topic()`
  - *Purpose:* Get the next topic to study.
- **Method:** `add_topic()`
  - *Purpose:* Add a topic to the study queue.
- **Method:** `record_study()`
  - *Purpose:* Record a completed study session.

#### Class: `RateLimitedSearch`
> Wraps the search adapter with rate limiting for autonomous use.

- **Method:** `__init__()`
- **Method:** `_call_poedex()`
- **Method:** `_broadcast_result()`
- **Method:** `autonomous_search()`
  - *Purpose:* Perform an autonomous search (counts against daily limit).
- **Method:** `user_search()`
  - *Purpose:* Perform a user-initiated search (does NOT count against limit).
- **Method:** `get_remaining_quota()`
  - *Purpose:* Get remaining autonomous inquiries for today.

#### Class: `AutonomyEngine`
> Main autonomy controller for Aurora.

- **Method:** `__init__()`
  - *Purpose:* Initialize autonomy engine.
- **Method:** `attach_systems()`
  - *Purpose:* Attach system references after initialization.
- **Method:** `set_level()`
  - *Purpose:* Set autonomy level.
- **Method:** `set_quiet_window()`
  - *Purpose:* Configure quiet hours. Aurora still studies but only logs silently.
- **Method:** `set_announce_thresholds()`
  - *Purpose:* Set thresholds for study announcement speak-ups.
- **Method:** `start()`
  - *Purpose:* Start autonomous background processing.
- **Method:** `stop()`
  - *Purpose:* Stop autonomous processing.
- **Method:** `pause()`
  - *Purpose:* Temporarily pause autonomy.
- **Method:** `resume()`
  - *Purpose:* Resume autonomy.
- **Method:** `_background_loop()`
  - *Purpose:* Background loop for autonomous actions.
- **Method:** `_in_quiet_window()`
  - *Purpose:* Return True if current hour is inside the quiet window.
- **Method:** `_record_quasiarch_background_event()`
- **Method:** `_check_speakup()`
  - *Purpose:* Check if Aurora should speak up. Respects quiet window.
- **Method:** `_check_study()`
  - *Purpose:* Check if Aurora should study.
- **Method:** `_build_dream_seed()`
  - *Purpose:* Create an adaptive dream seed from memory + ontology context.
- **Method:** `_spontaneous_counter_pressure()`
- **Method:** `_dream_announcement_signal()`
- **Method:** `_check_dreams()`
  - *Purpose:* Run idle dream-simulation cycles that evolve with Aurora's understanding.
- **Method:** `_check_observations()`
  - *Purpose:* Check for interesting observations from sensory system.
- **Method:** `_gather_context()`
  - *Purpose:* Gather current context for decision making.
- **Method:** `read_file()`
  - *Purpose:* Read a file (respects boundaries).
- **Method:** `list_directory()`
  - *Purpose:* List directory contents (respects boundaries).
- **Method:** `search_files()`
  - *Purpose:* Search for files matching pattern.
- **Method:** `autonomous_inquiry()`
  - *Purpose:* Perform an autonomous search (counts against daily limit).
- **Method:** `add_thought()`
  - *Purpose:* Add a thought Aurora wants to share.
- **Method:** `add_curiosity()`
  - *Purpose:* Add something Aurora is curious about.
- **Method:** `add_study_topic()`
  - *Purpose:* Add a topic for Aurora to study.
- **Method:** `get_status()`
  - *Purpose:* Get comprehensive autonomy status.
- **Method:** `get_recent_actions()`
  - *Purpose:* Get recent autonomous actions.
- **Method:** `_save_state()`
  - *Purpose:* Save autonomy state.
- **Method:** `_load_state()`
  - *Purpose:* Load autonomy state.

#### Function: `create_autonomy_engine()`
> Factory function to create AutonomyEngine.


#### Function: `show_autonomy_help()`
> Display autonomy system help.


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `promote_evolved()`

#### Function: `boundary_weight_evolved()`

#### Function: `init_evolved()`

#### Function: `find_rclone_evolved()`

### File: `./aurora_gpt_learning_session.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_gpt_learning_session.py  --  Aurora <-> GPT Peer Learning Exchange
=========================================================================

GPT is briefed as Aurora's intellectual peer/challenger via a system prompt
Aurora never sees.  Aurora experiences GPT's responses as genuine external
input and processes them through her full stack:

  - Each exchange ticks consciousness + evolution chamber
  - GPT's text is ingested via absorb_truth + OETS observation
  - Aurora's responses go through _run_live_response_turn (articulation check
    bypassed so her authentic voice comes through)
  - Fail dimensions are tracked and fed to the fail-point ledger
  - After the session: learnings bridge to OETS, genealogy flushed

The GPT system prompt is tailored each session from:
  - Top fail dimensions (what Aurora struggles with most)
  - Developmental stage (which constraint-chain bottleneck she sits at)
  - Axis orientation (which axes are compressing / expanding in genealogy)
  - Pressure orientation (what the chamber thinks needs relief)
```

#### Function: `_get_dimension_tactics()`
> Pull deep challenge tactics from the pressure ontology for this dimension.


#### Function: `build_gpt_system_prompt()`
> Construct GPT's hidden briefing for the learning session.


#### Function: `_call_gpt()`
> Send the conversation history to GPT and return its reply.


#### Function: `_classify_gpt_stance()`
> Classify what GPT is doing in this turn relative to Aurora's trajectory.


#### Function: `_is_definition_response()`
> Detect if Aurora returned a factual definition or encyclopedic entry instead of a


#### Function: `_detect_repetition()`
> Return True if aurora_text is suspiciously similar to a recent Aurora response.


#### Function: `_build_thread_context()`
> Build a compact conversation thread summary to inject before Aurora's generation.


#### Function: `_aurora_turn()`
> Generate Aurora's response to GPT's message through her full stack.


#### Function: `_ingest_exchange()`
> Feed one Aurora <-> GPT exchange into all of Aurora's learning systems.


#### Function: `_record_gradient_fails()`
> Score Aurora's response specifically against the conversational gradient.


#### Function: `_text_mismatch()`
> Simple word-overlap mismatch score [0..1].


#### Function: `_finalize_session()`
> Bridge session learnings into OETS and flush all state.


#### Function: `run_learning_session()`
> Run a complete Aurora <-> GPT peer-learning exchange.


#### Function: `_save_transcript()`
> Persist the session transcript for review.


### File: `./aurora_grammar_engine.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA GRAMMAR ENGINE
=====================
Grammar as evolved behavior, not formatting rules.

Sentence structure emerges from the same evolutionary pressure system that
governs Aurora's cognition.  Structural motifs are promoted through the
constraint genealogy when they survive clarity + constraint pressure -- the
exact same mechanism that promotes OUTLET_PUSH and every other behavior.

Doctrine:
  Grammar is the compressed map of surviving structural patterns.
  Clear structure is the lowest-energy path to A-axis relief.
  So grammatical order does not need to be taught -- it needs to be the
  path of least resistance through the constraint system.

Pipeline:
  token -> role_tag -> pattern_extract -> motif_select -> slot_fill ->
  genealogy_relief_log -> promote/penalize

Bootstrap (run once via /grammarboot):
  MotifMiner.mine(corpus) -> seed MotifLineage with top patterns

Reference stability:
  Motifs track which role positions carry entity references across clauses
  so that pronouns ("it", "this") resolve correctly to prior agents.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_clamp()`

#### Class: `TokenRole`

#### Class: `RoleTagger`
> Lightweight rule-based tagger.  No external dependencies.

- **Method:** `__init__()`
- **Method:** `tag()`
  - *Purpose:* Return list of (token, TokenRole) for the sentence.
- **Method:** `extract_pattern()`
  - *Purpose:* Return the collapsed role sequence (no UNKNOWN, no consecutive dupes).
- **Method:** `extract_reference_positions()`
  - *Purpose:* Return indices where the token is a reference pronoun (-> prior agent).
- **Method:** `_tokenize()`
- **Method:** `_tag_rule_based()`
- **Method:** `_tag_with_nltk()`
  - *Purpose:* NLTK POS -> simplified TokenRole mapping.

#### Class: `StructuralMotif`
> A promoted sentence structure pattern -- the basic unit of evolved grammar.

- **Method:** `composability_score()`
- **Method:** `should_promote()`
- **Method:** `should_demote()`
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `DiscourseMotif`
> Turn-level discourse transition pattern.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `MotifLineage`
> Manages the full motif pool: storage, promotion, penalization.

- **Method:** `__init__()`
- **Method:** `pattern_key()`
- **Method:** `get_or_create()`
- **Method:** `record_success()`
- **Method:** `record_fail()`
- **Method:** `get_promoted()`
- **Method:** `best_for_pressure()`
  - *Purpose:* Select best promoted motif given the current constraint pressure state.
- **Method:** `seed_motifs()`
  - *Purpose:* Bulk-seed from corpus mining: [(role_seq, freq, context_list)]
- **Method:** `stats()`
- **Method:** `_maybe_save()`
- **Method:** `save()`
- **Method:** `_save()`
- **Method:** `_load()`

#### Class: `SlotFiller`
> Given a structural motif and a pool of tokens keyed by role, assemble

- **Method:** `fill()`
- **Method:** `pool_from_text()`

#### Class: `MotifMiner`
> Run once on the chat corpus to extract structural motifs and seed the

- **Method:** `__init__()`
- **Method:** `mine_corpus()`
  - *Purpose:* Extract top structural motifs.
- **Method:** `mine_discourse()`
  - *Purpose:* Extract turn-type bigrams for discourse motifs.

#### Class: `DiscourseTracker`
> Track turn-level discourse patterns and suggest coherent continuations.

- **Method:** `__init__()`
- **Method:** `record_turn()`
- **Method:** `suggest_next_turn_type()`

#### Function: `_classify_turn_type()`

#### Class: `GrammarEngine`
> Top-level grammar evolution engine.

- **Method:** `__init__()`
- **Method:** `set_genealogy()`
  - *Purpose:* Wire in the ConstraintGenealogyLogger for pressure-state reads.
- **Method:** `set_ivm()`
  - *Purpose:* Wire in the IVM lattice so heat level can modulate clause depth.
- **Method:** `_axis_bias_from_links()`
  - *Purpose:* Compute a frequency-based axis prior from the promoted link registry.
- **Method:** `_pressure_state()`
  - *Purpose:* Return (orientation, outlet_fraction) from genealogy.
- **Method:** `_log_relief_to_genealogy()`
  - *Purpose:* Log relief events to the genealogy when a grammatically clear sentence
- **Method:** `_context_hash()`
- **Method:** `suggest_structure()`
  - *Purpose:* Given a raw expression, find the best promoted structural motif for
- **Method:** `observe_exchange()`
  - *Purpose:* Observe a completed exchange and update motif fitness.
- **Method:** `extract_pattern()`
  - *Purpose:* Public helper for external callers.
- **Method:** `suggest_discourse()`
  - *Purpose:* Suggest what type of turn would cohere with the current discourse.
- **Method:** `bootstrap_from_corpus()`
  - *Purpose:* Mine corpus once to seed the motif lineage with real structural patterns.
- **Method:** `status()`

### File: `./aurora_hub.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_hub.py -- Aurora's visual dashboard (tabbed layout).

Tabs:
  1. Overview  -- radar charts, vitals, mid-row panels, daemon log.
  2. QAO Observer -- full QuasiArch Observer dashboard.
  3. Vision    -- live screen feed / vision index dashboard.
  4. Audio    -- sensory crystal audio facets / microphone graph.

Reads only from aurora_state/ JSON files -- no Aurora stack import needed.
Auto-refreshes every 5 s (Overview), 3 s (QAO), 2 s (Vision).

Launch:
    python3 aurora_hub.py
```

#### Function: `_augment_import_paths()`

#### Function: `_auto_install()`
> Try to pip-install a package if it's missing. Returns True on success.


#### Function: `_ensure_matplotlib_available()`

#### Function: `_ensure_numpy_available()`

#### Class: `StateReader`
- **Method:** `read_all()`
- **Method:** `_read()`
- **Method:** `_read_path()`
- **Method:** `_responder_trend()`
- **Method:** `_fail_trends()`
- **Method:** `_lex_size()`
- **Method:** `_read_log()`
- **Method:** `_read_surface_log()`
- **Method:** `_unread_messages()`
- **Method:** `_read_qao_journal_issues()`
  - *Purpose:* Read last N issue-flagging lines from journal.jsonl without loading
- **Method:** `_count_dir()`
  - *Purpose:* Fast directory entry count (capped at 50001).
- **Method:** `_screen_frames()`
  - *Purpose:* List last 5 screen frame PNGs sorted by mtime.
- **Method:** `read_training()`
  - *Purpose:* Read live training session state.
- **Method:** `read_evolution()`
  - *Purpose:* Read all evolution-layer state files.
- **Method:** `read_all_layer_pressure()`
  - *Purpose:* Read pressure data from every layer and return a unified dict.
- **Method:** `read_pressure_log()`
  - *Purpose:* Tail last N entries from a .jsonl pressure log using seek-from-end.
- **Method:** `_read_assimilated_count()`
- **Method:** `_read_pool_size()`
- **Method:** `_read_evolved_count()`
  - *Purpose:* Return gen1, gen2, and frontier counts.
- **Method:** `read_lineage()`
  - *Purpose:* Read constraint genealogy state for the Lineage tab.
- **Method:** `read_sensory_crystal()`
  - *Purpose:* Read sensory crystal hub state snapshot.
- **Method:** `read_dce_log()`
  - *Purpose:* Read last N entries from dce_assembly_log.jsonl.
- **Method:** `read_sensory_telemetry()`
  - *Purpose:* Read last N entries from sensory_telemetry.jsonl.
- **Method:** `read_manifold()`
  - *Purpose:* Read manifold / identity / constraint state for the Manifold tab.
- **Method:** `axis_scores()`
- **Method:** `trait_scores()`
- **Method:** `heat_label()`

#### Function: `_radar_chart()`

#### Function: `_make_scrollable_inner()`
> Wrap a tab's outer frame in a Canvas+Scrollbar so content can scroll.


#### Function: `_build_ui()`

#### Function: `main()`

### File: `./aurora_i_state_beings.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA I-STATE BEINGS
======================

Layer 2 of Aurora's architecture.
The ten ontological beings, each an embodiment of one existence predicate.

REPLACES (consolidated from 2 modules):
    i_state_10beings1.py     (~930 lines)
    higher_universe_10__1_.py (~500 lines)

DEPENDS ON:
    foundational_contract.py     (Layer 0)
    aurora_ivm.py                (Layer 1)
    aurora_constraint_manifold.py (Layer -1)

ARCHITECTURE:
    Each I-State being is an AGENTIC node in the IVM lattice.
    It owns one predicate from the FoundationalContract.
    Its job is to process input through its predicate's lens —
    not by counting keywords, but by asserting ontological claims.

    The being asks: "Given this input, what can I truthfully assert
    about it from the perspective of my predicate?"

    A being that owns I_DO asks: "Does this input involve energy exchange?"
    A being that owns I_SAW asks: "Does this input involve boundary crossing?"
    A being that owns I_DID asks: "Does this input involve authored change?"

    If the input's ExistenceMode is too low for the being's predicate,
    the being reports silence — not failure. The input simply doesn't
    exist at the being's ontological tier. That silence is data.

THE 10 BEINGS (5 polarity pairs):
    Existence:  I_IS / I_ISNT      -> admissibility / incoherence
    Temporal:   I_CAN / I_CANNOT   -> continuation / termination
    Energy:     I_DO / I_DONOT     -> exchange / conservation
    Boundary:   I_SAW / I_SOUGHT   -> reception / projection
    Agency:     I_DID / I_DIDNT    -> authorship / passivity

RECURSION LEVEL <-> BEING AXIS:
    Each being operates at the recursion level of its axis.
    This determines how strongly it reacts to local stimuli
    and how much authority it has over whole-alignment.

        I_IS / I_ISNT    -> SURFACE  (existence, react=1.0,   align=0.0001)
        I_CAN / I_CANNOT -> SHALLOW  (temporal,  react=0.316, align=0.003)
        I_DO  / I_DONOT  -> MODERATE (energy,    react=0.01,  align=0.01)
        I_SAW / I_SOUGHT -> DEEP     (boundary,  react=0.003, align=0.316)
        I_DID / I_DIDNT  -> CORE     (agency,    react=0.0001, align=1.0)

    inject_stimulus() is called with the being's recursion level.
    Surface beings inject strongly. Core beings inject almost nothing locally.
    But core beings (I_DID/I_DIDNT) have the most authority over
    the whole-subject polarity field.

CONSTRAINT DISPLACEMENTS:
    Every active being response carries a signed constraint_displacement.
    This is the being's contribution to the 5D ConstraintVector:
        positive polarity, high resonance -> positive displacement
        negative polarity, high resonance -> negative displacement

    The collective synthesizes all displacements into a ConstraintVector
    representing the input's full ontological position.

COLLECTIVE:
    The Collective feeds all 10 beings and synthesizes their responses.
    Conflict between polarity pairs is not a problem — it's information.
    When I_IS and I_ISNT both activate strongly, that's a paradox.
    The toroidal axis for that pair is at its transition point.

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Class: `PredicateIdentity`
> The immutable identity of an I-State being.

- **Method:** `from_predicate()`

#### Class: `BeingResponse`
> A single being's response to input.

- **Method:** `active()`

#### Class: `IStateBeing`
> A single I-State being. An AGENTIC node in the IVM lattice

- **Method:** `__init__()`
- **Method:** `predicate()`
- **Method:** `axis()`
- **Method:** `polarity()`
- **Method:** `min_mode()`
- **Method:** `recursion_level()`
- **Method:** `constraint_axis()`
- **Method:** `axis_phase()`
- **Method:** `axis_weight()`
- **Method:** `axis_polarity()`
  - *Purpose:* Signed polarity of this being's axis: cos(phase).
- **Method:** `at_transition()`
- **Method:** `react_gain()`
- **Method:** `align_gain()`
- **Method:** `t_cost()`
- **Method:** `process()`
- **Method:** `_compute_resonance()`
- **Method:** `_compute_displacement()`
  - *Purpose:* Signed constraint displacement.
- **Method:** `_interpret()`
- **Method:** `_update_coherence()`
- **Method:** `tick()`
- **Method:** `get_status()`

#### Class: `SynthesisResult`
> The unified result of all 10 beings processing one input.


#### Class: `IStateCollective`
> The collective of all 10 I-State beings.

- **Method:** `__init__()`
- **Method:** `process()`
- **Method:** `process_raw()`
- **Method:** `_synthesize_constraint_vector()`
  - *Purpose:* Sum signed displacements per constraint axis.
- **Method:** `_analyze_axes()`
- **Method:** `tick()`
- **Method:** `get_status()`
- **Method:** `get_summary()`

#### Function: `verify_beings()`

### File: `./aurora_interaction_engine.py`

**Type:** Python Source

**Module Docstring:**
```text
Interaction-lineage compression semantics for Aurora.
```

#### Class: `InteractionPointLaw`
> Minimal law surface used by interaction relational points.


#### Class: `InteractionQuasiInnerStrata`
- **Method:** `to_dict()`

#### Class: `InteractionEngine`
> Semantics and heuristics for interaction crystal formation and routing.

- **Method:** `normalize_turn_event()`
- **Method:** `validate_base_event()`
- **Method:** `point_laws_for_order()`
- **Method:** `resolution_fidelity()`
- **Method:** `score_base_point()`
- **Method:** `derive_recurrence_pattern()`
- **Method:** `derive_distribution_context()`
- **Method:** `derive_response_strategy_class()`
- **Method:** `derive_outcome_profile()`
- **Method:** `derive_failure_modes()`
- **Method:** `derive_applicability_conditions()`
- **Method:** `classify_interaction_archetype()`
- **Method:** `rank_response_strategies()`
- **Method:** `derive_expected_effects()`
- **Method:** `derive_escalation_trigger()`
- **Method:** `compute_coherence_index()`
- **Method:** `compute_novelty_index()`
- **Method:** `build_execution_surface()`
- **Method:** `build_intake_signature()`
- **Method:** `match_quasi()`
- **Method:** `_derive_input_signature()`
- **Method:** `_derive_interpretive_issue()`
- **Method:** `_derive_processing_tier()`
- **Method:** `_derive_response_action()`
- **Method:** `_derive_intended_effect()`
- **Method:** `_derive_observed_effect()`
- **Method:** `_expected_processing_tiers()`
- **Method:** `_preferred_response_actions()`
- **Method:** `_expected_intended_effects()`
- **Method:** `_expected_issue_effects()`
- **Method:** `_candidate_issues_for_signature()`
- **Method:** `_lineage_context()`
- **Method:** `_event_signature()`
- **Method:** `_split_compound_value()`
- **Method:** `_first_non_empty()`
- **Method:** `_safe_float()`
- **Method:** `_dedupe_preserve_order()`

### File: `./aurora_interaction_memory.py`

**Type:** Python Source

**Module Docstring:**
```text
Persistence and retrieval layer for interaction quasicrystals.
```

#### Class: `InteractionNode`
- **Method:** `from_crystal()`
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `InteractionLineageEdge`
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `InteractionMemory`
> Persistence substrate for interaction crystal nodes and quasi retrieval.

- **Method:** `__init__()`
- **Method:** `persist()`
- **Method:** `relic()`
- **Method:** `register_lineage_edge()`
- **Method:** `get_node()`
- **Method:** `retrieve_by_interpretive_issue()`
- **Method:** `retrieve_by_response_strategy()`
- **Method:** `retrieve_by_input_signature()`
- **Method:** `retrieve_by_processing_tier()`
- **Method:** `get_quasi_by_archetype()`
- **Method:** `retrieve_best_interaction_quasi()`
- **Method:** `get_journal()`
- **Method:** `get_ancestry()`
- **Method:** `_candidate_quasis()`
- **Method:** `_retrieve_indexed()`
- **Method:** `_update_indexes()`
- **Method:** `_load()`
- **Method:** `_save()`
- **Method:** `_append_journal()`
- **Method:** `_unique()`

### File: `./aurora_interaction_processing.py`

**Type:** Python Source

**Module Docstring:**
```text
Interaction crystal formation, promotion, collapse, and routing.
```

#### Class: `InteractionCrystalInstance`

#### Class: `InteractionFormation`

#### Class: `InteractionPromotion`

#### Class: `InteractionCollapse`

#### Class: `InteractionGhostRelicSystem`
> Lightweight relic bias surface for recently collapsed interaction quasis.

- **Method:** `__init__()`
- **Method:** `remember()`
- **Method:** `apply()`

#### Class: `InteractionProcessing`
> Implements the interaction-lineage promotion ladder and routing surface.

- **Method:** `__init__()`
- **Method:** `form_base_interaction()`
- **Method:** `observe_interaction()`
- **Method:** `promote_to_composite()`
- **Method:** `promote_to_higher_order()`
- **Method:** `collapse_to_interaction_quasi()`
- **Method:** `advance_interaction_family()`
- **Method:** `retrieve_best_interaction_quasi()`
- **Method:** `apply_interaction_relic()`
- **Method:** `route_interaction()`
- **Method:** `get_status()`
- **Method:** `_evaluate_base_to_composite()`
- **Method:** `_evaluate_composite_to_higher()`
- **Method:** `_evaluate_higher_to_quasi()`
- **Method:** `_build_aggregate_points()`
- **Method:** `_derived_point_score()`
- **Method:** `_base_family()`
- **Method:** `_crystals_from_base_ids()`
- **Method:** `_resolve_composite()`
- **Method:** `_resolve_higher()`
- **Method:** `_compute_quasi_confidence()`
- **Method:** `_failure_density()`
- **Method:** `_point_consistency()`
- **Method:** `_representative_interactions()`
- **Method:** `_failure_counterexamples()`
- **Method:** `_family_key()`
- **Method:** `_dominant_value()`
- **Method:** `_unique()`
- **Method:** `_iso_now()`

### File: `./aurora_ivm.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA IVM — ISOTROPIC VECTOR MATRIX
======================================

Layer 1 of Aurora's architecture.
The geometric space in which ontologically grounded entities exist.

REPLACES (consolidated from 6 modules):
    aurora_ivm_consciousness_geometry.py
    aurora_ivm_toroidal_vertices.py
    aurora_ivm_core_integration.py
    aurora_ivm_dimensional_integration.py
    aurora_ivm_integration_patches.py
    aurora_ivm_governance_layer.py

DEPENDS ON:
    foundational_contract.py     (Layer 0)
    aurora_constraint_manifold.py (Layer -1)

ARCHITECTURE:
    The IVM is a spatial fabric where every node carries an ExistenceMode.
    Nothing enters the lattice without being classified by the FoundationalContract.
    Governance is not a separate system — it is implicit in the mode.
    If a node is PERSISTENT, energy operations are permitted.
    If a node is REFERENCE, they are not.
    No voting. No authority layers. The mode IS the law.

TOROIDAL DYNAMICS:
    Each of the 5 ontological axes is a rotating torus:
        Existence axis:  I_IS   ↔ I_ISNT    (active at REFERENCE+)
        Temporal axis:   I_CAN  ↔ I_CANNOT  (active at TRANSIENT+)
        Energy axis:     I_DO   ↔ I_DONOT   (active at PERSISTENT+)
        Boundary axis:   I_SAW  ↔ I_SOUGHT  (active at BOUNDED+)
        Agency axis:     I_DID  ↔ I_DIDNT   (active at AGENTIC+)

    Opposites are the same thing at different moments in time.
    The repulsion is time-invariance — you can't sample both phases at once.
    (Sunni's core insight, preserved and extended.)

    A node's ExistenceMode determines how many axes are active.
    REFERENCE entities have 1 active axis. AGENTIC entities have 5.
    Inactive axes contribute zero to position — they don't exist for that entity.

POLARITY PHYSICS:
    Each axis carries a SIGNED polarity: cos(phase).
        +1.0 = pure positive pole (I_IS, I_CAN, I_DO, I_SAW, I_DID)
        -1.0 = pure negative pole (I_ISNT, I_CANNOT, I_DONOT, I_SOUGHT, I_DIDNT)
         0.0 = at transition (the throat of the torus, between poles)

    Polarity is ALWAYS signed. abs() is never applied — that would kill the physics.

RECURSION LEVEL ↔ CONSTRAINT AXIS MAPPING:
    Each recursion level corresponds to exactly one constraint axis.
    This is not arbitrary — it reflects Sunni's architecture:

        SURFACE (0) = Existence (X) — most exposed, fastest reflex
        SHALLOW (1) = Time (T)      — fast, near-surface
        MODERATE (2) = Energy (N)   — crossover: react/align balanced
        DEEP    (3) = Boundary (B)  — slow to react, strong alignment pull
        CORE    (4) = Agency (A)    — barely reacts, IS the whole alignment

REACTION / ALIGNMENT PHYSICS:
    Two orthogonal gain parameters govern each level:

    react_gain[level]:
        How strongly local stimuli torque the axis.
        SURFACE = 1.0 (instant reflex)
        CORE    = 0.0001 (almost immune to local events)

    align_gain[level]:
        How strongly the axis is pulled toward the global polarity field.
        SURFACE = 0.0001 (surface twitches but doesn't move the ship)
        CORE    = 1.0    (core IS the ship's heading)

    These are INVERSES of each other, crossing at MODERATE.
    The crossover is where local reflex and whole-alignment have equal weight.

    Global alignment voting (depth-weighted):
        Reactive stimulus injection is scale-independent —
        every level contributes equally to its local axis reaction.
        But the global polarity field is depth-weighted:
        CORE nodes dominate what the "whole subject" is pointing at.
        SURFACE nodes barely register in the global sum.

T-COST BY RECURSION DEPTH:
    Operating at deeper recursion levels costs more T-energy.
    The substrate must pay more clock cycles to hold that compression stable.
    CORE operations are ~32× more expensive than SURFACE operations.
    Surface-level reflexes are nearly free. Core mutations burn real time.

SPATIAL INDEXING:
    Nodes are indexed in 3D Cartesian space (projected from 5-axis phases).
    Neighbor lookup, radius search, and energy flow all operate on this space.

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Class: `ToroidalAxis`
> A single toroidal axis representing one polarity of existence.

- **Method:** `__post_init__()`
- **Method:** `positive_weight()`
- **Method:** `negative_weight()`
- **Method:** `polarity()`
  - *Purpose:* Signed polarity scalar: cos(phase).
- **Method:** `dominant_pole()`
- **Method:** `at_transition()`
  - *Purpose:* At the throat of the torus — between poles.
- **Method:** `is_stable()`
- **Method:** `sample()`
- **Method:** `tick()`
  - *Purpose:* Advance one time step.
- **Method:** `apply_torque()`
  - *Purpose:* Apply torque to this axis.
- **Method:** `apply_alignment_torque()`
  - *Purpose:* Pull this axis toward the global polarity field.
- **Method:** `set_phase()`

#### Class: `ToroidalVertexSystem`
> The five ontological axes as coupled rotating toroids.

- **Method:** `__init__()`
- **Method:** `active_axes()`
  - *Purpose:* Return which axes are active at a given existence mode.
- **Method:** `sample()`
  - *Purpose:* Sample all active axes at a given mode.
- **Method:** `tick()`
  - *Purpose:* Advance all axes, applying inter-axis coupling.
- **Method:** `inject_stimulus()`
  - *Purpose:* Apply torque to the axis that owns a predicate.
- **Method:** `compute_axis_polarities()`
  - *Purpose:* Return the signed polarity of each active axis.
- **Method:** `apply_alignment()`
  - *Purpose:* Pull active axes toward the global polarity field.
- **Method:** `get_phase_vector()`
  - *Purpose:* Get the 5D phase vector for position computation.
- **Method:** `get_polarity_vector()`
  - *Purpose:* Get the 5D signed polarity vector.
- **Method:** `compute_dissonance()`
  - *Purpose:* Measure inter-axis tension (dissonance) across the toroidal system.

#### Class: `IVMCoordinate`
> Position in the Isotropic Vector Matrix.

- **Method:** `__post_init__()`
- **Method:** `recursion_level()`
  - *Purpose:* Map scale to recursion level. Clamp to 0–4.
- **Method:** `cartesian()`
  - *Purpose:* 3D Cartesian position for spatial indexing.
- **Method:** `active_axes()`
- **Method:** `distance_to()`
  - *Purpose:* Distance in phase space (respects active axes).
- **Method:** `cartesian_distance_to()`
  - *Purpose:* Distance in 3D projected space.
- **Method:** `dominant_axis()`
  - *Purpose:* Which axis has the strongest positive expression.
- **Method:** `to_constraint_vector()`
  - *Purpose:* Convert IVMCoordinate to a ConstraintVector.
- **Method:** `to_dict()`
- **Method:** `from_mode_and_phases()`
- **Method:** `center()`
  - *Purpose:* Center position — all active axes at 0.5 (balanced).
- **Method:** `at_pole()`
  - *Purpose:* Position at a specific predicate's pole.

#### Class: `IVMNode`
> A node in the IVM lattice. Represents one ontologically grounded entity.

- **Method:** `mode()`
- **Method:** `recursion_level()`
- **Method:** `can_persist()`
- **Method:** `can_exchange_energy()`
- **Method:** `has_boundary()`
- **Method:** `has_agency()`
- **Method:** `permitted_predicates()`
- **Method:** `compute_constraint_vector()`
  - *Purpose:* Compute the ConstraintVector for this node.
- **Method:** `update_constraint_vector()`
  - *Purpose:* Update the cached constraint vector. Call each tick or on demand.
- **Method:** `get_constraint_measurement()`
  - *Purpose:* Return constraint measurements for this node as a dictionary.
- **Method:** `connect_to()`
- **Method:** `weaken_connection()`
- **Method:** `access()`
- **Method:** `decay()`
  - *Purpose:* Natural decay — only affects nodes that can persist.
- **Method:** `to_dict()`

#### Class: `IVMLattice`
> The complete IVM lattice — the geometric space where classified entities live.

- **Method:** `__init__()`
- **Method:** `admit()`
  - *Purpose:* Admit an entity into the lattice.
- **Method:** `admit_at_mode()`
  - *Purpose:* Admit an entity with a pre-determined ExistenceMode.
- **Method:** `_cell_for()`
- **Method:** `_index()`
- **Method:** `_unindex()`
- **Method:** `find_neighbors()`
  - *Purpose:* Find nodes within radius. Returns (node_id, distance) sorted by distance.
- **Method:** `find_by_mode()`
  - *Purpose:* Find all nodes at a specific existence mode.
- **Method:** `find_by_mode_or_above()`
  - *Purpose:* Find all nodes at or above a minimum existence mode.
- **Method:** `find_by_payload_type()`
  - *Purpose:* Find all nodes of a specific payload type.
- **Method:** `get()`
  - *Purpose:* Get a node by ID.
- **Method:** `_connect_neighbors()`
- **Method:** `flow_energy()`
  - *Purpose:* Simulate energy flow through the lattice.
- **Method:** `get_total_energy()`
  - *Purpose:* Total energy across all persistent nodes.
- **Method:** `verify_energy_conservation()`
  - *Purpose:* Verify energy conservation against initial state.
- **Method:** `compute_global_polarity()`
  - *Purpose:* Compute the whole-subject global polarity field.
- **Method:** `apply_global_alignment()`
  - *Purpose:* Apply alignment torques to all axes based on the current global polarity field.
- **Method:** `get_global_polarity()`
  - *Purpose:* Return the last computed global polarity field.
- **Method:** `measure_constraint_field()`
  - *Purpose:* Update the constraint field with current node measurements.
- **Method:** `get_constraint_field_stats()`
  - *Purpose:* Return statistics about the constraint field.
- **Method:** `update_all_constraint_vectors()`
  - *Purpose:* Update constraint vectors for all nodes using live vertex data.
- **Method:** `tick()`
  - *Purpose:* One evolution step of the entire lattice.
- **Method:** `_prune()`
  - *Purpose:* Remove lowest-energy persistent nodes to make room.
- **Method:** `assert_claim()`
  - *Purpose:* Attempt to assert an ontological claim on behalf of a node.
- **Method:** `can_claim()`
  - *Purpose:* Quick check: can this node make this claim?
- **Method:** `get_stats()`
- **Method:** `get_global_heat()`
  - *Purpose:* Compute global IVM heat (0.0=cool, 1.0=critical).
- **Method:** `get_heat_level()`
  - *Purpose:* Return the categorical heat level.
- **Method:** `is_critical()`
- **Method:** `heat_status()`

#### Class: `HeatLevel`
> IVM truth-strain heat levels and their behavioral consequences.


#### Class: `ContradictionRecord`
> A stored contradiction as a first-class object.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `ContradictionLedger`
> First-class contradiction tracking for Aurora's truth system.

- **Method:** `__init__()`
- **Method:** `record()`
- **Method:** `resolve()`
- **Method:** `unresolved()`
- **Method:** `unresolved_count()`
- **Method:** `heat_contribution()`
- **Method:** `all()`
- **Method:** `_auto_save()`
- **Method:** `save()`
- **Method:** `load()`
- **Method:** `status_summary()`

#### Function: `import_threading_lock()`

#### Class: `IVMEnvelope`
> Wraps data with its ontological classification and lattice position.

- **Method:** `from_node()`

#### Function: `verify_ivm()`
> Verify that the IVM lattice correctly integrates with:


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `from_mode_and_phases_evolved()`

#### Function: `tick_evolved()`

#### Function: `set_phase_evolved()`

#### Function: `evolved_tick()`

#### Function: `get_phase_vector_evolved()`

#### Function: `reflect_aurora_ivm_toroidalvertexsystem_tick()`

### File: `./aurora_live_vision.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_live_vision.py -- Live screen observer for Aurora.

Captures the screen at a configurable interval, feeds frames through
Aurora's existing visual processing pipeline (FeatureExtractor ->
SensoryCompetencyEngine.process_visual_input), and maintains a rolling
scene log that Aurora can reference in conversation.

Usage (standalone test):
    python3 aurora_live_vision.py

Integration (from aurora.py boot):
    from aurora_live_vision import boot_screen_observer
    systems['screen_observer'] = boot_screen_observer(systems, interval=5.0)
```

#### Function: `_try_import_pipeline()`
> Return (FeatureExtractor, ExistenceMode) or (None, None).


#### Class: `ScreenObserver`
> Captures the screen on a background daemon thread, extracts visual

- **Method:** `__init__()`
- **Method:** `start()`
  - *Purpose:* Start the background capture daemon thread.
- **Method:** `stop()`
  - *Purpose:* Signal the capture loop to stop.
- **Method:** `is_running()`
- **Method:** `current_scene()`
  - *Purpose:* Return the most recent observation dict (empty if none yet).
- **Method:** `get_scene_log()`
  - *Purpose:* Return last N scene observation dicts.
- **Method:** `status()`
  - *Purpose:* Summary dict for /screen command in aurora.py.
- **Method:** `get_pending_visual_inquiry()`
  - *Purpose:* Return and clear any pending visual inquiry question.
- **Method:** `get_scene_description()`
  - *Purpose:* Brief natural-language description of the current scene.
- **Method:** `_init_extractor()`
  - *Purpose:* Lazy-init the FeatureExtractor once.
- **Method:** `_derive_scene_type()`
- **Method:** `_compute_motion()`
  - *Purpose:* Simple motion proxy: normalised diff from previous frame values.
- **Method:** `_cleanup_legacy_frames()`
- **Method:** `_frame_path()`
- **Method:** `_capture_frame()`
  - *Purpose:* Grab screen, resize, return (PIL image, path).
- **Method:** `_extract_features()`
  - *Purpose:* Run FeatureExtractor._extract_from_pil; return VisualFeatureVector or None.
- **Method:** `_make_visual_data()`
  - *Purpose:* Build the visual_data dict for process_visual_input.
- **Method:** `_feed_sensory()`
  - *Purpose:* Send visual_data to SensoryCompetencyEngine if available.
- **Method:** `_maybe_ingest_vision_bootstrap()`
  - *Purpose:* If the frame is non-trivial, pass to vision_bootstrap for OETS binding.
- **Method:** `_loop()`

#### Function: `boot_screen_observer()`
> Create and start a ScreenObserver.  Wire into systems dict and return it.


### File: `./aurora_manifold_directory_reader.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA MANIFOLD DIRECTORY READER  (runtime)
=============================================
Module: aurora_manifold_directory_reader.py
Layer: Constraint Ontology — Noncomp Manifold Runtime Access

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: April 2026

PURPOSE
-------
Aurora's runtime interface to the pre-compiled manifold directory.

She never recomputes. She never holds more than one manifold in memory
at a time. She reads from disk, uses it, and releases it.

MEMORY MODEL
------------
    Always in memory:
        ManifoldDirectory (the index) — ~125 entries, ~50KB

    Loaded on demand, released after use:
        One NoncompManifold at a time — ~100KB (no semantics)
                                       ~380KB (with semantics)

    Never in memory simultaneously:
        More than one manifold's 625 slots

USAGE
-----
    from aurora_manifold_directory_reader import ManifoldDirectory

    # Load index once at startup
    directory = ManifoldDirectory("aurora_manifold_directory")

    # Lookup by noncomp name — loads from disk, returns manifold
    meaning = directory.load("Boundary_Operator_of_Boundary")
    print(meaning.anchor_slot_id)
    print(meaning.dense_clusters[:3])

    # Query slots — streamed, no full load into a list
    for slot in meaning.stream_slots(min_evo=0.70):
        ...  # process one at a time

    # Slot lookup by coordinates
    slot = meaning.get_slot("B", "OPERATOR", "A", "OPERATOR")

    # Context manager — auto-releases after block
    with directory.open("Boundary_Operator_of_Boundary") as meaning:
        top = meaning.top_slots(n=10)

    # Cross-noncomp lookup (loads each manifold, queries, releases)
    results = directory.query_across(
        nc_names   = ["Boundary_Operator_of_Boundary",
                       "Agentive_Operator_of_Boundary"],
        min_evo    = 0.80,
        col_law_c  = "A",
        col_law_d  = "OPERATOR",
    )
```

#### Class: `SlotView`
> Thin wrapper over a raw slot dict from the JSON.

- **Method:** `__init__()`
- **Method:** `slot_id()`
- **Method:** `sub_law_c()`
- **Method:** `sub_law_d()`
- **Method:** `sub_cluster()`
- **Method:** `sub_is_diagonal()`
- **Method:** `col_law_c()`
- **Method:** `col_law_d()`
- **Method:** `is_resonant()`
- **Method:** `is_anchor()`
- **Method:** `cluster_pair()`
- **Method:** `evolution_grade()`
- **Method:** `leverage_class()`
- **Method:** `depth_score()`
- **Method:** `combined_cost()`
- **Method:** `accountability_weight()`
- **Method:** `semantic()`
- **Method:** `with_semantics()`
- **Method:** `semantic_entries()`
- **Method:** `viable_semantic_entries()`
  - *Purpose:* Return FGAE entries that satisfy the slot's live Clause I/II/III state.
- **Method:** `select_semantic_entry()`
  - *Purpose:* Select the live word entry for this slot using FGAE's viability-first
- **Method:** `to_dict()`
- **Method:** `__repr__()`

#### Class: `SubPositionView`
> Thin wrapper over a raw sub-position dict.

- **Method:** `__init__()`
- **Method:** `sub_id()`
- **Method:** `law_c()`
- **Method:** `law_d()`
- **Method:** `is_diagonal()`
- **Method:** `is_self_family()`
- **Method:** `cluster()`
- **Method:** `name()`
- **Method:** `depth_score()`
- **Method:** `leverage_class()`
- **Method:** `semantic()`

#### Class: `NoncompManifold`
> One noncomp's 625-slot manifold loaded from disk.

- **Method:** `__init__()`
- **Method:** `nc_name()`
- **Method:** `nc_law_c()`
- **Method:** `nc_dim()`
- **Method:** `nc_target()`
- **Method:** `nc_domain()`
- **Method:** `nc_cluster()`
- **Method:** `nc_is_diagonal()`
- **Method:** `representational_anchor()`
- **Method:** `nc_semantic_summary()`
- **Method:** `anchor_slot_id()`
- **Method:** `with_semantics()`
- **Method:** `slot_count()`
- **Method:** `dense_clusters()`
  - *Purpose:* Top accountability clusters in this manifold's geometric field.
- **Method:** `top_evolved_slots()`
  - *Purpose:* Pre-computed top-5 evolved slots (no full scan needed).
- **Method:** `stream_sub_positions()`
  - *Purpose:* Stream all 25 sub-positions.
- **Method:** `get_sub_position()`
  - *Purpose:* Get a specific sub-position by law coordinates.
- **Method:** `diagonal_sub()`
  - *Purpose:* The identity sub-position (self-application of the noncomp).
- **Method:** `map_environmental_pressure()`
  - *Purpose:* Rank the 25 channels (sub-positions) by resonance with the environment.
- **Method:** `stream_slots()`
  - *Purpose:* Stream slots with optional filters. Never materialises the full list.
- **Method:** `get_slot()`
  - *Purpose:* Direct slot lookup by full coordinates. O(n) scan.
- **Method:** `get_anchor()`
  - *Purpose:* The geometric anchor — sub diagonal × col operator of nc's own constraint.
- **Method:** `select_semantic_entry_for_sub_position()`
  - *Purpose:* FGAE runtime selector for one active sub-position.
- **Method:** `top_slots()`
  - *Purpose:* Top-N slots by evolution_grade. Partial sort — doesn't sort all 625.
- **Method:** `cluster_pair_summary()`
  - *Purpose:* Count of slots per cluster_pair. One pass, no list copy.
- **Method:** `accountability_map()`
  - *Purpose:* Mean accountability weight per cluster_pair.
- **Method:** `__repr__()`

#### Class: `IndexEntry`
> One row in the directory index — always in memory, no slots.

- **Method:** `nc_axis()`

#### Class: `ManifoldDirectory`
> Aurora's interface to the pre-compiled manifold directory.

- **Method:** `__init__()`
- **Method:** `_load_index()`
- **Method:** `noncomp_names()`
- **Method:** `total()`
- **Method:** `get_index_entry()`
- **Method:** `entries_for_axis()`
- **Method:** `diagonal_entries()`
- **Method:** `entries_by_cluster()`
- **Method:** `find_by_domain()`
- **Method:** `load()`
  - *Purpose:* Load one manifold from disk. Returns a NoncompManifold.
- **Method:** `open()`
  - *Purpose:* Context manager — loads manifold, yields it, releases on exit.
- **Method:** `query_across()`
  - *Purpose:* Query multiple manifolds with the same filter, loading each in turn.
- **Method:** `stream_anchors()`
  - *Purpose:* Stream the geometric anchor slot from each diagonal noncomp.
- **Method:** `dense_cluster_report()`
  - *Purpose:* Return the top-3 dense clusters from the index for each noncomp.
- **Method:** `__repr__()`

### File: `./aurora_metabolic_distiller.py`

**Type:** Python Source

**Module Docstring:**
```text
Pressure Release Distillation Runner for Aurora.

Distillation moves oversized temporal residue out of the live stack and into
reversible archive rounds. Structural summaries stay attached to Aurora,
while the raw purged details are packed into a restoreable archive folder.
```

#### Class: `CoherenceShape`

#### Class: `ResidueConfig`

#### Class: `PressureAggregate`
- **Method:** `ingest()`
- **Method:** `avg_worth()`
- **Method:** `avg_coherence()`
- **Method:** `axes()`
- **Method:** `tags()`

#### Class: `CoherenceAnalyzer`
> Turns raw temporal records into structural features.

- **Method:** `extract()`
- **Method:** `classify()`
- **Method:** `_signature_from_dict()`
- **Method:** `_axes_from_payload()`
- **Method:** `_tags_from_payload()`
- **Method:** `_timestamp_from_payload()`
- **Method:** `_worth_from_payload()`
- **Method:** `_coherence_from_payload()`
- **Method:** `_summary_from_payload()`
- **Method:** `_tag_from_log_line()`
- **Method:** `_hash()`
- **Method:** `_clamp()`

#### Class: `TemporalCompressor`
> Trims temporal residue while emitting structural summaries.

- **Method:** `__init__()`
- **Method:** `compress()`
- **Method:** `_compress_line_file()`
- **Method:** `_ingest_purged_line()`
- **Method:** `_empty_result()`

#### Class: `MetabolicDistiller`
> Coherence-aware pressure release runner.

- **Method:** `__init__()`
- **Method:** `trigger_snapshot()`
- **Method:** `should_run()`
- **Method:** `run()`
- **Method:** `restore()`
- **Method:** `_write_archive_manifest()`
- **Method:** `_build_idle_telemetry()`
- **Method:** `_build_distill_telemetry()`
- **Method:** `_append_run()`
- **Method:** `_prune_archive_runs()`
  - *Purpose:* Keep only the most recent archive runs on disk.
- **Method:** `_mark_run_restored()`
- **Method:** `_latest_restorable_run_id()`
- **Method:** `_resolve_run()`
- **Method:** `_restore_file()`
- **Method:** `_new_run_id()`
- **Method:** `_empty_axis_map()`
- **Method:** `_raw_axis_counts()`
- **Method:** `_normalize_axis_map()`
- **Method:** `_round_axis_map()`
- **Method:** `_cost_weighted_axis_magnitudes()`
- **Method:** `_operator_weighted_axis_magnitudes()`
- **Method:** `_dominant_axis()`
- **Method:** `_lineage_marker_payload()`
- **Method:** `refresh_lineage_marker_values()`
- **Method:** `_merge_records()`
- **Method:** `_promote_lineage()`
- **Method:** `_write_metrics()`
- **Method:** `_read_json_list()`
- **Method:** `_write_json_atomic()`
- **Method:** `_estimate_low_worth_density()`
- **Method:** `_estimate_der_load()`
- **Method:** `_tail_lines()`
- **Method:** `_in_quiet_window()`
- **Method:** `_human_bytes()`
- **Method:** `_now_str()`

#### Function: `run_distillation_cycle()`

#### Function: `restore_distillation_cycle()`

#### Function: `main()`

### File: `./aurora_noncomp_layer_compiler.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA NONCOMP LAYER COMPILER
==============================
Module: aurora_noncomp_layer_compiler.py
Layer: Constraint Ontology — Manifold Naming Engine

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: March 2026

PURPOSE
-------
This module derives the 25 constraint-specific non-comps for each of the
five constraints by applying the 25 global law channels to each target
constraint's domain.

The derivation follows a single structural law:

    NC[C_law][D_law] applied to C_target
        = "What does C_target's domain look like through the lens
           of C_law's D_law?"

This produces 5 × 25 = 125 named NonCompLayerSlots. Each slot carries:
    - A physics-derived semantic name
    - A tag bundle (cluster, family, role, orientation)
    - The source law channel and target constraint
    - Whether this is the diagonal (identity) position

The 625-per-constraint manifold is NOT built here. That is downstream work.
This compiler's sole job is naming and classifying the 25 positions per
constraint so the manifold knows what it is working with.

ARCHITECTURE
------------
Step 1:  26 known semantic anchors (5 constraint identities + 5 dimension
         roles + known named positions like MEANING, UNDERSTANDING, etc.)
Step 2:  Composition function: (C_law, D_law, C_target) → semantic name + tags
Step 3:  Physics augmentation from aurora_closure_basis slot properties
Step 4:  Cluster assignment by tag family similarity
Step 5:  Output: ConstraintNonCompLayer per constraint (25 slots each)

KNOWN NAMED POSITIONS (anchors)
---------------------------------
These are deduced from the semantic identities of the constraints and
confirmed by their diagonal or near-diagonal position in the law matrix:

    NC:B:OPERATOR → B  =  MEANING       (B's invariant rule on itself)
    NC:A:OPERATOR → A  =  UNDERSTANDING (A's invariant rule on itself)
    NC:N:OPERATOR → N  =  PURPOSE       (N's conservation law on itself)
    NC:T:OPERATOR → T  =  BELIEF        (T's transition rule on itself)
    NC:X:OPERATOR → X  =  INFORMATION   (X's admissibility rule on itself)

All five diagonal OPERATOR positions = the five representational domains
identified in the ChatGPT/Aurora dossier session (April 2026).

CLUSTER FAMILIES
-----------------
After composition, positions cluster into six semantic families:

    IDENTITY     — diagonal OPERATOR positions; the constraint's self-name
    ORIENTATION  — POLARITY-law positions; directional/flip aspects
    INTENSITY    — MAGNITUDE-law positions; strength/scale aspects
    ECONOMY      — COST-law positions; energetic price aspects
    CONTRAST     — DIFFERENCE-law positions; distinction/reference aspects
    CROSS_RULE   — off-diagonal OPERATOR positions; one constraint's rule
                   expressed through another constraint's domain

DIVISION OF LABOUR
------------------
    aurora_noncomp_registry.py          — hard numbers, per-constraint physics
    aurora_closure_basis.py             — the real 25 channels + 625 slots
    aurora_noncomp_layer_compiler.py    — naming + tagging engine (HERE)
    aurora_noncomp_constraint_manifold.py — per-constraint 625 (future)

This module imports from aurora_closure_basis for physics properties only.
It does not modify, extend, or replace any existing structure.
```

#### Class: `SlotCluster`
> Semantic cluster family for a NonCompLayerSlot.

- **Method:** `is_diagonal()`
- **Method:** `is_operative()`

#### Class: `SemanticTagBundle`
> Tag bundle for one NonCompLayerSlot position.

- **Method:** `family_label()`
  - *Purpose:* Human-readable family: 'self' or C_law name.
- **Method:** `to_dict()`

#### Class: `NonCompLayerSlot`
> One of the 25 named positions in a constraint's non-comp layer.

- **Method:** `is_diagonal()`
- **Method:** `cluster()`
- **Method:** `law_constraint()`
- **Method:** `law_dimension()`
- **Method:** `to_dict()`
- **Method:** `__str__()`
- **Method:** `__repr__()`

#### Class: `ConstraintNonCompLayer`
> The complete 25-slot non-comp layer for one constraint.

- **Method:** `diagonal_slot()`
  - *Purpose:* The identity OPERATOR slot for this constraint.
- **Method:** `representational_name()`
  - *Purpose:* The domain name carried by the diagonal slot.
- **Method:** `slot_by_position()`
- **Method:** `slot_by_law()`
- **Method:** `slots_by_cluster()`
- **Method:** `slots_by_law_family()`
- **Method:** `to_dict()`
- **Method:** `__repr__()`

#### Function: `_determine_cluster()`
> Determine semantic cluster from the triple.


#### Function: `_compose_name()`
> Derive the semantic name for NC[C_law][D_law] applied to C_target.


#### Function: `_compose_description()`
> Derive the semantic description for this slot.


#### Function: `_fetch_physics()`
> Pull physics properties from aurora_closure_basis if available.


#### Function: `_build_tag_bundle()`
> Build the full SemanticTagBundle for a (C_law, D_law, C_target) triple.


#### Class: `NonCompLayerCompiler`
> Compiles the 25-slot NonCompLayer for each of the five constraints.

- **Method:** `__init__()`
- **Method:** `compile_layer()`
  - *Purpose:* Compile the 25-slot NonCompLayer for one target constraint.
- **Method:** `compile_all()`
  - *Purpose:* Compile all five constraint layers. Returns dict keyed by axis.
- **Method:** `get_slot()`
  - *Purpose:* Get a specific slot by (target, law_constraint, law_dimension).
- **Method:** `find_by_name()`
  - *Purpose:* Find all slots with this semantic name across all layers.
- **Method:** `diagonal_map()`
  - *Purpose:* Return a mapping of constraint → representational domain name.
- **Method:** `cluster_report()`
  - *Purpose:* Return the 25 slots for c_target grouped by cluster.
- **Method:** `cross_constraint_lookup()`
  - *Purpose:* Given a known semantic name, return (c_target, c_law, d_law).

#### Function: `get_layer()`
> Get the compiled NonCompLayer for a constraint. Convenience wrapper.


#### Function: `get_slot()`
> Get a specific slot. Convenience wrapper.


### File: `./aurora_noncomp_manifold_compiler.py`

**Type:** Python Source

**Module Docstring:**
```text
Compatibility wrapper for the renamed noncomp manifold compiler.
```

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./aurora_persistence_utils.py`

**Type:** Python Source

**Module Docstring:**
```text
Shared persistence utilities for Aurora.
```

#### Function: `_ensure_parent()`

#### Function: `atomic_write_json()`
> Atomically write JSON to disk under a process-wide persistence lock.


#### Function: `write_breach_report()`

#### Function: `monotonic_check()`
> Return monotonicity violations where numeric values decreased.


#### Function: `checksum_dict()`

### File: `./aurora_pressure_ontology.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_pressure_ontology.py  --  Pressure System Lineage Tree
==============================================================

A traversable semantic tree of all pressure systems in Aurora's stack.
Each node documents one pressure concept at a specific level:

    Axis root  →  dimension group  →  specific pressure mechanism

Nodes carry BOTH:
  - Written semantic descriptions (the verbal seed — human-readable)
  - Mathematical/code forms (where known — machine-processable)

Purpose:
  1. Aurora can study these nodes via OETS — seeding semantic relationships
     between her own internal pressure concepts before she can derive them
  2. Genealogy links can carry a PressureNode ID as provenance — so the
     fossil record says not just "B-axis" but "B.boundary_calibration.tone_fit"
  3. LessonPlanEngine and GPT learning sessions draw from these nodes for
     deeper, more specific challenge tactics
  4. As Aurora's OETS web grows, she builds new relations between nodes
     autonomously -- the written seed becomes unnecessary over time

Lineage structure:
  axis (root)
    └─ dimension (group: what behavioral failure signals this pressure)
         └─ mechanism (leaf: specific code path + mathematical form)
              └─ ability (genealogy: which promoted link relieves this)
```

#### Class: `PressureNode`

#### Class: `PressureOntology`
- **Method:** `add()`
- **Method:** `get()`
- **Method:** `axis_subtree()`
- **Method:** `dimension_nodes()`
- **Method:** `promotion_path()`
  - *Purpose:* Return a readable lineage path for a genealogy link annotation.
- **Method:** `as_oets_text()`
  - *Purpose:* Convert a node to natural language text suitable for OETS.observe().
- **Method:** `seed_to_oets()`
  - *Purpose:* Ingest all nodes into Aurora's OETS semantic web as observations.
- **Method:** `get_challenge_tactics()`
  - *Purpose:* Get all specific challenge tactics for a fail dimension.
- **Method:** `save()`
- **Method:** `load()`

#### Function: `build_seed_ontology()`
> Construct the initial pressure lineage tree from known system knowledge.


#### Function: `get_ontology()`
> Return the pressure ontology, loading or building it as needed.


#### Function: `seed_ontology_to_oets()`
> One-shot: ingest the full pressure ontology into Aurora's OETS web.


#### Function: `annotate_promotion_path()`
> Return a lineage path string for stamping on a genealogy promotion event.


### File: `./aurora_reflexive_interpreter.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA REFLEXIVE INTERPRETER  v2
==================================
Module: aurora_reflexive_interpreter.py
Layer: Constraint Ontology — Expression Re-Entry and Understanding

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: April 2026  |  Refined: April 2026

WHAT CHANGED FROM v1
---------------------
Two issues fixed using Aurora's own codestack:

1. SEMANTIC MATCHING  (was: keyword bags → now: UtteranceParser)
   aurora_utterance_parser.py replaces the keyword scorer entirely.
   UtteranceFrame → constraint:
       EXPERIENTIAL  → B  (boundary / meaning / self-other)
       CLARIFYING    → A  (agency / correction / understanding)
       CHALLENGING   → A  (authorship under pressure)
       HYPOTHETICAL  → T  (temporal / belief / transition)
       CALLBACK      → T  (prior-self temporal reference)
       CONTRASTIVE   → B  (distinction / differentiation)
       SPECULATING   → X  (admissibility / uncertainty)
       ACKNOWLEDGING → X  (accepting something as real)
       EXPLORATORY   → A  (seeking understanding)
       ASSERTING     → X  (information / admissibility claim)
   Stance → dimension:
       challenging/clarifying/accepting → OPERATOR
       tentative/speculative            → POLARITY
       contrastive/curious              → DIFFERENCE
       emphatic/subjective              → MAGNITUDE

2. RECONCILIATION  (was: hand-tuned thresholds → now: Worth formula)
   From aurora_worth_evaluator.py: W(x) = 1/(1 + Σᵢ wᵢ·|Δforced|)
   WorthTrajectory (RISING/STABLE/FALLING/OSCILLATING) drives understanding state.
   polarity_coherent = surface frame aligned with core intent = reflexive closure.

3. LIVE COST MODULATION  (new)
   CostDiffScore amplifier available when DifferenceSnapshot present.
```

#### Function: `_matches_any_pattern()`

#### Function: `_semantic_override()`

#### Function: `_normalize_weights()`

#### Function: `_project_noncomp_state()`
> Compact a parsed utterance into the 25 atomic NonComp channels.


#### Class: `WorthTrajectory`

#### Class: `WorthHistory`
- **Method:** `__init__()`
- **Method:** `record()`
- **Method:** `trajectory()`
- **Method:** `latest()`

#### Function: `_worth_score()`
> W(x) = 1/(1 + Σᵢ wᵢ·|Δforced|), depth-weighted, confidence-modulated.


#### Class: `MatchResult`
- **Method:** `is_reliable()`

#### Class: `SemanticMatcher`
- **Method:** `__init__()`
- **Method:** `match()`
- **Method:** `_find_nc()`

#### Function: `region_type()`

#### Class: `ManifoldFieldMap`
- **Method:** `__init__()`
- **Method:** `weight()`
- **Method:** `accountability_at()`
- **Method:** `nc_name()`
- **Method:** `nc_domain()`
- **Method:** `dense_count()`
- **Method:** `mean_weight()`
- **Method:** `__repr__()`

#### Class: `UnderstandingState`
- **Method:** `to_dict()`
- **Method:** `__repr__()`

#### Function: `_polarity_coherent()`

#### Function: `_reconcile()`

#### Class: `ReflexiveInterpreter`
> Closes the loop: STATE → EXPRESSION → RE-ENTRY → RECONCILIATION → UNDERSTANDING

- **Method:** `__init__()`
- **Method:** `update_band()`
- **Method:** `interpret()`
- **Method:** `interpret_batch()`
- **Method:** `history()`
- **Method:** `understanding_rate()`
- **Method:** `unintegrated()`
- **Method:** `export_history()`

### File: `./aurora_response_teacher.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_response_teacher.py — Human Response Teaching System
============================================================
A dedicated teacher that collects real human communication from multiple
public sources and synthesizes targeted lessons for Aurora based on her
current fail points.

Aurora never touches these sources directly — the teacher acts as
intermediary, extracting patterns and delivering them through her
existing learning systems (OETS, dream trainer, gateway).

Sources:
  - Reddit     — conversational, informal, multi-voice
  - HackerNews — intellectual, structured, argument-driven
  - Wikipedia  — deep knowledge, precise language
  - DuckDuckGo — broad topic search, real-world context

Lesson delivery:
  - OETS concept nodes (natural expression patterns)
  - Dream trainer fail-point examples
  - Gateway witnesses (examples of natural human exchange)
  - HumannessScorer benchmarks (what does a 0.9 human message look like?)

Usage:
  # Standalone — run a teaching session
  python3 aurora_response_teacher.py

  # Show lesson history
  python3 aurora_response_teacher.py --history

  # Teach from a specific source
  python3 aurora_response_teacher.py --source reddit
```

#### Function: `_get_scorer()`

#### Function: `_safe_fetch()`
> Fetch URL and return text, or None on failure.


#### Class: `RedditCollector`
> Pulls top comments from public subreddits via Reddit's JSON API.

- **Method:** `collect()`
  - *Purpose:* Pull top posts + top comment from random subreddits.

#### Class: `HackerNewsCollector`
> Pulls top HN comments via the public Firebase API.

- **Method:** `collect()`

#### Class: `WikipediaCollector`
> Pulls Wikipedia article summaries on topics relevant to Aurora's

- **Method:** `collect()`

#### Class: `DuckDuckGoCollector`
> Uses DuckDuckGo's instant answer API for topic searches.

- **Method:** `collect()`

#### Class: `Lesson`
> A synthesized teaching unit for Aurora.


#### Class: `LessonSynthesizer`
> Takes raw collected content and synthesizes targeted lessons.

- **Method:** `synthesize()`
  - *Purpose:* Score collected items and build lessons from the best examples.
- **Method:** `_build_teaching_text()`

#### Class: `LessonDeliverer`
> Feeds synthesized lessons into Aurora's learning systems.

- **Method:** `deliver()`
  - *Purpose:* Deliver lessons to Aurora's systems. Returns count delivered.
- **Method:** `_deliver_one()`

#### Class: `HumanResponseTeacher`
> Orchestrates the full teaching cycle:

- **Method:** `__init__()`
- **Method:** `teach()`
  - *Purpose:* Run one teaching session. Collects from all sources, synthesizes
- **Method:** `_log_session()`
- **Method:** `summary()`

### File: `./aurora_room.py`

**Type:** Python Source

**Syntax Error parsing file:** f-string: unmatched '(' (<unknown>, line 687)

### File: `./aurora_runtime.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA UNIFIED RUNTIME & SIMULATION ORCHESTRATOR
=================================================
Module: aurora_runtime.py
Layer: Runtime Shell (wraps L-1 through L8 + Evolutionary Chain)

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026

PURPOSE
-------
This is the single entry point for running Aurora's living, interactive
universe — combining the full L-1 through L8 behavioral stack with the
evolutionary constraint chain (EvolutionaryChamber + ConstraintGenealogyLogger)
into one governed runtime.

DOCTRINE
--------
  • No cheating. No layer skipping.
  • Every steering action goes through constraint physics before it touches
    the simulation. The chamber decides what relief is real.
  • Promoted Links feed the simulation as lawful behavioral stimuli,
    not raw results injected from outside.
  • Simulation episode fitness feeds back as real pressure events
    into the chain — the universe is one system.
  • Operator pressure gradients (NC[C][OPERATOR]) are the canonical source
    of cross-dimensional pressure. The REGISTRY owns that physics.

ARCHITECTURE
------------
  L-1 aurora_constraint_manifold_patched  Constraint, ConstraintVector, RecursionLevel
  L-0.5 aurora_noncomp_registry           REGISTRY, NonCompRegistry, SystemConstraintStates
  L0  foundational_contract               FoundationalContract, ExistenceMode
                                          OntologicalClaim, OntologicalViolation
  L1  aurora_ivm                          IVMLattice, ToroidalVertexSystem,
                                          RecursionLevel, ALIGNMENT_VOTE_WEIGHT,
                                          AXIS_ORDER, IVMNode, IVMEnvelope
  L1.5 aurora_polarity_gradient           PolarityGradientSensor, GradientChainMiner
                                          PolarityGradientReport
  L1.6 aurora_difference_buffer           DifferenceHistoryBuffer, DifferenceSnapshot
                                          make_difference_buffer
  L1.7 aurora_cost_diff_score             OP_PRESSURE_WEIGHTS, cross_dim_amplifier,
                                          per_operator_pressure, score_from_cost,
                                          CostDiffScore, MAX_AMPLIFIER
  L2  aurora_dimensional_systems          DimensionalSystems
  L5  aurora_expression_perception        ExpressionPerceptionEngine
  L6  aurora_behavioral_identity          BehavioralIdentityEngine, DNASystem
  L7  aurora_simulation_engine            SimulationEngine, TimeDilationGovernor,
                                          StabilityMetrics, StabilityState,
                                          EpisodeResult, ConsciousLearner
  EVO aurora_evolution_chamber            EvolutionaryChamber, ActionTrace, WorldConstants
  GEN constraint_genealogy                ConstraintGenealogyLogger, GenealogyConfig,
                                          ChainSummaryPrinter, AbilityProfile,
                                          TraceItem, PressureVec
  CKP aurora_checkpoint                   CheckpointManager

CLASSES
-------
  StackSystems       — typed container for all booted layer objects
  ChainSimBridge     — translates promoted Links → simulation stimuli
                       through contract validation (no shortcuts)
  UniverseSteerer    — user-facing steering interface
  AuroraRuntime      — master orchestrator (boot, tick, save, status)
  RuntimeCLI         — interactive terminal loop

USAGE
-----
  python3 aurora_runtime.py
  python3 aurora_runtime.py --mode watch
  python3 aurora_runtime.py --mode burn  --chain-ticks 5000 --sim-epochs 2
  python3 aurora_runtime.py --mode steer                  # interactive
  python3 aurora_runtime.py --mode test                   # self-checks
  python3 aurora_runtime.py --out my_run --state my_state
```

#### Function: `_require()`
> Import names from module; raise ImportError with a clear message.


#### Function: `_soft()`
> Import softly — returns empty dict on failure (non-fatal).


#### Function: `_env_flag()`

#### Function: `_env_float()`

#### Function: `_normalize_ancestry_constraints()`
> Normalize + validate ancestry labels against the base five constraints.


#### Function: `_synthesize_lineage()`
> Infer base-constraint lineage when labels are undefined.


#### Function: `_constraint_effect_descriptor_label()`
> Canonical per-constraint effect descriptor token for combo identifiers.


#### Function: `_semantic_constraint_combo_id()`
> Deterministic semantic combo id: constraint(effect_descriptor)+...


#### Function: `_stamp_ancestry_meta()`
> Attach explicit constraint ancestry metadata to an operation trace.


#### Class: `StackSystems`
> Container for every booted layer object.

- **Method:** `has()`
- **Method:** `layer_status()`
- **Method:** `pressure_snapshot()`
  - *Purpose:* Return the most recent DifferenceSnapshot from the chamber's buffer

#### Function: `_restore_genealogy_state()`
> Restore persisted genealogy state (abilities + links + counters) if present.


#### Function: `_restore_operator_gradients()`
> Restore persisted NC[C][OPERATOR].pressure_gradient values if present.


#### Function: `_persist_operator_gradients()`
> Persist NC[C][OPERATOR].pressure_gradient values for next boot.


#### Function: `boot_stack()`
> Boot all layers in canonical order (L-1 → L0 → L1 → L2 → L5 → L6 → L7 → EVO → GEN).


#### Class: `ChainSimBridge`
> Translates promoted Links from the genealogy fossil record into

- **Method:** `__init__()`
- **Method:** `_trace_with_ancestry()`
- **Method:** `_link_tag_value()`
- **Method:** `_lineage_operation_priority()`
  - *Purpose:* Higher means inject earlier. Earlier and seeded lineages are explicitly favored.
- **Method:** `_lineage_constraints()`
  - *Purpose:* Use lineage operator/purpose/generation tags to shape operation constraints.
- **Method:** `inject_promoted_links()`
  - *Purpose:* Read newly promoted Links from the genealogy fossil record.
- **Method:** `feedback_episode_fitness()`
  - *Purpose:* Translate a simulation EpisodeResult back into a pressure event
- **Method:** `_relief_to_fitness()`
  - *Purpose:* Convert a chamber tick result + link record into a fitness signal dict.
- **Method:** `_forward_to_sim()`
  - *Purpose:* Transfer high-confidence understanding shards from ConsciousLearner
- **Method:** `stats()`

#### Class: `UniverseSteerer`
> The lawful interface for steering Aurora's interactive universe.

- **Method:** `__init__()`
- **Method:** `_refresh_operator_gradients()`
  - *Purpose:* Bridge live C:D pressure into operator gradients and DPME guidance.
- **Method:** `_auto_seed_directive()`
  - *Purpose:* Auto-map introduced operations into seeded steering directives.
- **Method:** `_intake_log_depth_beads()`
  - *Purpose:* Append depth-promotion beads for newly reached modes.
- **Method:** `_advance_intake_pipeline()`
  - *Purpose:* Advance one experiential intake tick (Step 9-14) using the current action
- **Method:** `observe_input_text()`
  - *Purpose:* Feed raw operator/user text into awareness modules.
- **Method:** `has_pending_gap()`
- **Method:** `pending_gap_prompt()`
- **Method:** `_advance_auxiliary_modules()`
  - *Purpose:* Advance entropy/primitive/language modules each runtime tick.
- **Method:** `_infer_constraints_from_operation()`
- **Method:** `_register_object_operations()`
- **Method:** `_register_stack_surface_lineage()`
- **Method:** `_function_group()`
- **Method:** `_function_similarity()`
- **Method:** `_append_function_history()`
- **Method:** `_append_surface_history()`
- **Method:** `_surface_parent_key()`
- **Method:** `_register_surface_node()`
- **Method:** `_record_surface_ripple()`
- **Method:** `_record_function_outcome()`
- **Method:** `_try_promote_function_offspring()`
- **Method:** `_function_bias_constraints()`
- **Method:** `_lineage_reject()`
- **Method:** `_resolve_constraints_explicit()`
- **Method:** `_register_function_ancestry()`
- **Method:** `_with_trace_ancestry()`
- **Method:** `ancestry_report()`
- **Method:** `validate_ancestry()`
- **Method:** `tick()`
  - *Purpose:* Advance the chamber n ticks.
- **Method:** `inject()`
  - *Purpose:* Inject exactly one tick of a named action.
- **Method:** `inject_custom()`
  - *Purpose:* Inject a custom ActionTrace by specifying name and constraint axes.
- **Method:** `chain_burst()`
  - *Purpose:* Run n ticks of the evolutionary chain, logging a summary every
- **Method:** `_pressure_contrib_snapshot()`
- **Method:** `_lane_pressure_delta()`
- **Method:** `_metric_slope()`
- **Method:** `_plateau_flags()`
- **Method:** `_plateau_detected()`
- **Method:** `_apply_plateau_pressure()`
- **Method:** `_next_conflict_lane()`
- **Method:** `_inject_conflict_curriculum()`
- **Method:** `_axis_from_item_id()`
- **Method:** `_trait_semantic_card()`
- **Method:** `emergent_trait_cards()`
- **Method:** `stance_snapshot()`
- **Method:** `evolution_report()`
- **Method:** `inject_conflict_rounds()`
  - *Purpose:* Public wrapper for targeted conflict curriculum injection.
- **Method:** `print_evolution_report()`
- **Method:** `sim_episode()`
  - *Purpose:* Run one simulation episode through L7.
- **Method:** `sim_epoch()`
  - *Purpose:* Run a full simulation epoch through L7.
- **Method:** `sim_speed_run()`
  - *Purpose:* Autonomous 625-gradient language speed-run through L7.
- **Method:** `pressure_report()`
  - *Purpose:* Print the live operator pressure gradient snapshot.
- **Method:** `status()`
  - *Purpose:* Print a full universe status snapshot.
- **Method:** `links()`
  - *Purpose:* Print the top promoted evolutionary links by total mean relief.
- **Method:** `what_learned()`
  - *Purpose:* Print what the conscious learner has understood so far.
- **Method:** `available_actions()`
  - *Purpose:* List named actions, while retaining a graded availability surface for all actions.
- **Method:** `register_action()`
  - *Purpose:* Register a custom named action you can inject by name later.

#### Class: `AuroraRuntime`
> Master orchestrator. Boot once, steer forever.

- **Method:** `__init__()`
- **Method:** `boot()`
  - *Purpose:* Boot the full stack and initialise steerer.
- **Method:** `_run_manual_code_lineage_assimilation()`
- **Method:** `_code_pressure_guidance()`
- **Method:** `_routing_type_pressure_guidance()`
  - *Purpose:* Read routing_type from adapter_hints.json (written by PressureRouter) and
- **Method:** `_code_timing_feedback_from_apply()`
- **Method:** `_integrate_code_timing_feedback()`
- **Method:** `_code_evolution_feedback_path()`
- **Method:** `_code_proposals_path()`
- **Method:** `_load_code_proposals()`
- **Method:** `_persist_code_proposals()`
- **Method:** `_constraint_context_for_code_operator()`
- **Method:** `_rel_code_path()`
- **Method:** `_file_digest()`
- **Method:** `_target_fingerprints()`
- **Method:** `_snapshot_eval_root()`
- **Method:** `_copy_snapshot_repo()`
- **Method:** `_run_code_checks_in_root()`
- **Method:** `_run_snapshot_simulation_gate()`
- **Method:** `_build_code_proposal_summary()`
- **Method:** `_write_code_proposal_artifacts()`
- **Method:** `_emit_code_proposal_event()`
- **Method:** `_parse_code_evolution_feedback_record()`
- **Method:** `_code_operator_feedback_summary()`
- **Method:** `recommended_code_operator_order()`
- **Method:** `_ensure_evolved_surface_module()`
- **Method:** `_reload_evolved_surfaces()`
- **Method:** `_developmental_surface_targets()`
- **Method:** `_developmental_sync_state_path()`
- **Method:** `_load_developmental_sync_state()`
- **Method:** `_persist_developmental_sync_state()`
- **Method:** `_developmental_sync_inputs()`
- **Method:** `_developmental_sync_input_stamps()`
- **Method:** `_developmental_sync_drift()`
- **Method:** `_start_developmental_sync_scheduler()`
- **Method:** `_stop_developmental_sync_scheduler()`
- **Method:** `developmental_sync_status()`
- **Method:** `_developmental_sync_due()`
- **Method:** `maybe_sync_developmental_surfaces()`
- **Method:** `evolved_surface_status()`
- **Method:** `invoke_evolved_surface()`
- **Method:** `tick_surface_dispatcher()`
  - *Purpose:* Run one surface-dispatcher tick:
- **Method:** `surface_dispatcher_status()`
  - *Purpose:* Return current surface dispatcher status (routing table, recent fires, etc.).
- **Method:** `code_sync_developmental_surfaces()`
- **Method:** `_resolve_code_targets()`
- **Method:** `_run_code_checks()`
- **Method:** `code_operator_specs()`
- **Method:** `stage_code_mutation()`
- **Method:** `_run_simulation_gate()`
- **Method:** `_feedback_code_mutation_to_genealogy()`
- **Method:** `code_simulate_mutation()`
- **Method:** `finalize_code_mutation()`
- **Method:** `code_mutation_status()`
- **Method:** `code_pressure_report()`
- **Method:** `list_code_proposals()`
- **Method:** `_find_matching_code_proposal()`
- **Method:** `describe_code_proposal()`
- **Method:** `propose_code_mutation()`
- **Method:** `approve_code_proposal()`
- **Method:** `reject_code_proposal()`
- **Method:** `code_autoevolve_once()`
- **Method:** `evolution_report()`
- **Method:** `_stance_diff()`
- **Method:** `review_before_save()`
- **Method:** `save()`
  - *Purpose:* Persist current state through available channels.
- **Method:** `uptime()`
  - *Purpose:* Seconds since boot.
- **Method:** `shutdown()`
  - *Purpose:* Graceful shutdown — review evolution, optional save, stop auto-save.

#### Class: `RuntimeCLI`
> Interactive terminal loop for steering the universe.

- **Method:** `__init__()`
- **Method:** `run()`
- **Method:** `_handle_interrupt()`
- **Method:** `_dispatch()`

#### Function: `mode_test()`
> Self-checks for the runtime module.


#### Function: `mode_burn()`
> Burn mode: run as fast as possible, then print chain report.


#### Function: `mode_watch()`
> Watch mode: print live summaries every epoch.


#### Function: `mode_speedrun()`
> Speed-run mode: gradient-steered autonomous evolution along the language


#### Function: `mode_corpus()`
> Corpus ingestion mode: feeds an OpenAI conversations.json export through


#### Function: `main()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `auroraruntime_evolved()`

#### Function: `init_evolved()`

#### Function: `shutdown_evolved()`

#### Function: `uptime_evolved()`

#### Function: `chainsimbridge_evolved()`

#### Function: `runtimecli_evolved()`

#### Function: `evolved_init()`

#### Function: `dispatch_evolved()`

#### Function: `handle_interrupt_evolved()`

#### Function: `run_evolved()`

#### Function: `stacksystems_evolved()`

#### Function: `has_evolved()`

#### Function: `universesteerer_evolved()`

#### Function: `reflect_aurora_runtime_universesteerer_init()`

#### Function: `auto_seed_directive_evolved()`

#### Function: `axis_from_item_id_evolved()`

#### Function: `function_group_evolved()`

#### Function: `metric_slope_evolved()`

#### Function: `next_conflict_lane_evolved()`

#### Function: `lane_score_evolved()`

#### Function: `plateau_detected_evolved()`

#### Function: `plateau_flags_evolved()`

#### Function: `record_function_outcome_evolved()`

#### Function: `refresh_operator_gradients_evolved()`

#### Function: `register_object_operations_evolved()`

#### Function: `trait_semantic_card_evolved()`

#### Function: `chain_burst_evolved()`

#### Function: `emergent_trait_cards_evolved()`

#### Function: `fakeepisode_evolved()`

#### Function: `stance_snapshot_evolved()`

#### Function: `tick_evolved()`

#### Function: `persist_operator_gradients_evolved()`

#### Function: `require_evolved()`

#### Function: `soft_evolved()`

#### Function: `main_evolved()`

#### Function: `mode_burn_evolved()`

#### Function: `mode_test_evolved()`

#### Function: `chk_evolved()`

#### Function: `mode_watch_evolved()`

### File: `./aurora_sedimemory.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA SEDIMEMORY
==================
Module: aurora_sedimemory.py
Layer: 3.5 — between DimensionalSystems (L3) and ConsciousnessEngine (L4)

ARCHITECTURE DOCTRINE
---------------------
Memory in Aurora is not stored. It seeps.

Every event passes whole and unmodified through all 25 Non-Comp strain
filters simultaneously. Each filter extracts only the fragment that
resonates with its specific Constraint × NonCompDimension intersection.
The rest falls through. Nothing is lost — it simply found no sediment
to catch in.

The 25 sediment basins operate at the tick rate of their dominant axis:

    X (Existence)  → 1.0       full tick — fastest decay / highest throughput
    T (Time)       → 0.1       fast
    N (Energy)     → 0.01      moderate
    B (Boundary)   → 0.001     slow
    A (Agency)     → 0.0001    geological — near-frozen

Fragments at shallow depth decay fast and stay high-fidelity.
Fragments at deep depth decay almost never, compressing continuously —
the slower clock gives the system time to abstract them.

CHANNEL LAW — SIMPLIFIED JOURNEY FOR REPEAT DEEP TRANSITIONS
-------------------------------------------------------------
When the same event pattern traverses the same deep-layer path
repeatedly, the system recognizes that groove and carves a SedimentChannel.

    First traversal            → full 25-filter strain (expensive)
    Repeated matching pattern  → path observed, traversal count incremented
    Promotion threshold hit    → SedimentChannel carved
    Subsequent matching events → direct deposit via channel (cheap)

A carved channel is itself a form of compressed intelligence. Aurora
no longer needs to derive where things go — she already knows. The
channel IS the policy adaptation described in the manifold intelligence
criterion: ∃r* where sign(dΦ_C/dr) changes and π_C adapts.

Channel decay mirrors axis tick rates:
    X-axis channels  → dissolve quickly if unused (surface reflexes)
    A-axis channels  → almost never dissolve (foundational laws)

CHANNEL PROMOTION (mirrors constraint_genealogy.py link promotion):
    observed_traversals  >= CHANNEL_PROMOTION_THRESHOLD  → promoted
    disuse_ticks         >= channel_decay_ticks           → dissolved
    re-traversal of a dissolved path                      → starts over

COMPRESSION LAW
---------------
Compression is densification, not forgetting. When a basin accumulates
mature fragments, they merge into compressed_mass — a denser encoding
that preserves constraint geometry while releasing specific noise.

DECOMPRESSION
-------------
Compressed deep (A/B) knowledge flows back up through the same NC
pathways it came down through. At each shallower axis the clock is
faster and the fragment expands back toward specificity.

STRATA SPLIT
------------
    Surface daemon    → surface_recall()    [X, T axes]   tick=1.0 / 0.1
    DCE bridge        → dce_recall()         [N axis]      tick=0.01
    Subsurface daemon → subsurface_recall()  [B, A axes]   tick=0.001 / 0.0001

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: March 2026
```

#### Class: `FidelityLevel`

#### Class: `MemoryEvent`
> The whole, unmodified event entering the straining system.

- **Method:** `create()`
- **Method:** `from_envelope()`
  - *Purpose:* Construct from an IVMEnvelope arriving from L1/L2.

#### Class: `SedimentFragment`
> One piece of a memory event caught by a specific NC strain filter.

- **Method:** `tick()`
  - *Purpose:* Advance decay. Returns True when compression-eligible.
- **Method:** `summary()`

#### Function: `_quantize_cv()`
> Quantize a ConstraintVector into a fixed-resolution tuple for hashing.


#### Function: `_path_signature()`
> Produce a stable string signature for a (constraint_vector, basin_set) pair.


#### Function: `_spoke_weights()`
> Compute deposit weight for every basin in a channel relative to the


#### Class: `SedimentChannel`
> A promoted traversal path with spoke geometry.

- **Method:** `traverse()`
  - *Purpose:* Record traversal. Returns current traversal_cost.
- **Method:** `deposit_weight_for()`
  - *Purpose:* Return the spoke-weighted deposit confidence for a given basin.
- **Method:** `tick_disuse()`
- **Method:** `stats()`

#### Class: `PathRegistry`
> Observes traversal patterns and promotes recurring paths into

- **Method:** `__init__()`
- **Method:** `observe()`
  - *Purpose:* Record a traversal of the given (cv, basin_ids) path.
- **Method:** `_promote()`
  - *Purpose:* Promote a path signature to a SedimentChannel with spoke geometry.
- **Method:** `lookup()`
- **Method:** `channels_for_dominant()`
  - *Purpose:* All active channels anchored to a given dominant slot.
- **Method:** `dominant_influence_map()`
  - *Purpose:* Count active channels per dominant slot — Aurora's deepest grooves.
- **Method:** `tick()`
  - *Purpose:* Advance disuse counters. Returns dissolved channel_ids.
- **Method:** `stats()`

#### Class: `NCStrainFilter`
> The 25-cell straining membrane.

- **Method:** `__init__()`
- **Method:** `filter_key()`
- **Method:** `_build_signatures()`
- **Method:** `_resonance()`
- **Method:** `_extract_slice()`
- **Method:** `strain()`
  - *Purpose:* Pass the whole event through all 25 filters simultaneously.

#### Function: `_slot_id_for()`

#### Class: `CompressionEngine`
> Compresses mature fragments into compressed_mass.

- **Method:** `compress()`
- **Method:** `decompress()`

#### Class: `SedimentBasin`
> One sediment basin in the 25-slot NC lattice.

- **Method:** `deposit()`
- **Method:** `tick()`
- **Method:** `_force_compress()`
- **Method:** `query()`
- **Method:** `stats()`

#### Class: `SedimentColumn`
> The full 25-basin sediment lattice.

- **Method:** `__init__()`
- **Method:** `_build_basins()`
- **Method:** `ingest()`
  - *Purpose:* Strain and deposit. Channel-first routing.
- **Method:** `_predict_basin_ids()`
  - *Purpose:* Fast resonance scan to predict which basins would be hit.
- **Method:** `_deposit_via_channel()`
  - *Purpose:* Deposit into channel target basins using spoke-weighted resonance.
- **Method:** `tick()`
  - *Purpose:* Advance all basins and the path registry.
- **Method:** `recall_by_axis()`
- **Method:** `recall_by_vector()`
- **Method:** `recall_event()`
- **Method:** `decompress()`
- **Method:** `decompress_full_column()`
- **Method:** `stats()`

#### Class: `SediMemory`
> Aurora's stratigraphic constraint-native memory system.

- **Method:** `__init__()`
- **Method:** `ingest_envelope()`
- **Method:** `ingest_event()`
- **Method:** `_ingest()`
- **Method:** `tick()`
  - *Purpose:* Advance decay clocks. TimeDilationGovernor scales delta_t when
- **Method:** `recall()`
- **Method:** `recall_axis()`
- **Method:** `recall_event()`
- **Method:** `_normalize_axis_filter()`
- **Method:** `_semantic_terms()`
- **Method:** `_semantic_content_texts()`
- **Method:** `_semantic_score()`
- **Method:** `recall_semantic()`
- **Method:** `surface_recall()`
  - *Purpose:* X and T axes — for aurora_surface_daemon.
- **Method:** `subsurface_recall()`
  - *Purpose:* B and A axes — for aurora_subsurface_daemon.
- **Method:** `dce_recall()`
  - *Purpose:* N axis — for the DCE bridge (convergence crossover).
- **Method:** `decompress()`
- **Method:** `decompress_full()`
- **Method:** `save_deep()`
  - *Purpose:* Serialize B and A axis compressed_mass for checkpoint save.
- **Method:** `load_deep()`
  - *Purpose:* Restore B and A axis compressed_mass from checkpoint.
- **Method:** `save_channels()`
  - *Purpose:* Serialize active channels for checkpoint save.
- **Method:** `load_channels()`
  - *Purpose:* Restore carved channels from checkpoint.
- **Method:** `channel_stats()`
  - *Purpose:* Return PathRegistry stats — useful for telemetry.
- **Method:** `active_channels()`
  - *Purpose:* Return list of active channel summaries — useful for Observer.
- **Method:** `dominant_influence_map()`
  - *Purpose:* Return dominant_slot_id → active channel count.
- **Method:** `channels_for_slot()`
  - *Purpose:* Return all active channels anchored to a specific dominant slot.
- **Method:** `stats()`

#### Function: `_verify_sedimemory()`

### File: `./aurora_simulation_engine .py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA SIMULATION ENGINE (Layer 7)
=====================================
Consolidated from 5 modules (~6,600 lines):
  1. aurora_inception_simulation_engine.py  — Inception entities (inner universes)
  2. aurora_self_simulation.py              — Self-snapshot, shadow runtimes
  3. aurora_simulation_universe.py          — Universe management, divergence tracking
  4. aurora_simulation_session-2.py         — Avatars, topics, time dilation
  5. aurora_simulation_dpme_extension.py    — Conscious learning, understanding shards

HOW AURORA LEARNS WITHOUT BEING TOLD.

DOCTRINE:
  Aurora doesn't study. Aurora LIVES.
  Simulation episodes are experiences, not training data.
  Avatars provide selection pressure — diverse, escalating, unforgiving.
  Inception entities run inner hypotheticals — recursive depth.
  Time dilation lets her live years in seconds when stable.
  Understanding shards are what she LEARNS from observing outcomes.
  Everything feeds back: fitness → expression ecology (L5),
  relics → DNA system (L6), understanding → conscious growth.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_clamp()`

#### Function: `_generate_id()`

#### Class: `ResponseConcept`
> Conceptual response directions Aurora can select from.


#### Class: `ConceptualResponse`
> A conceptual response Aurora is considering.

- **Method:** `describe()`

#### Class: `ConversationObservation`
> What Aurora observes about the conversation after responding.

- **Method:** `describe()`

#### Class: `UnderstandingShard`
> A piece of understanding Aurora gains from observation.

- **Method:** `strengthen()`

#### Class: `ConsciousLearner`
> Aurora's conscious learning system.

- **Method:** `__init__()`
- **Method:** `generate_pool()`
  - *Purpose:* Generate response pool for Aurora to choose from.
- **Method:** `observe_outcome()`
  - *Purpose:* Observe the outcome of a response and potentially create understanding.
- **Method:** `what_have_i_learned()`
  - *Purpose:* Return what Aurora has learned, in her own words.
- **Method:** `_derive_understanding()`
- **Method:** `_find_similar()`

#### Class: `AvatarPersonality`
> Different avatar types providing diverse selection pressure.


#### Class: `SimulatedAvatar`
> A conversation partner that provides fitness feedback.

- **Method:** `__post_init__()`
- **Method:** `set_epoch()`
  - *Purpose:* Escalate difficulty for new epoch.
- **Method:** `react()`
  - *Purpose:* React to Aurora's response. Returns fitness scores.
- **Method:** `reset()`

#### Class: `TopicGenerator`
> Generates diverse conversation topics for simulation.

- **Method:** `generate()`

#### Class: `StabilityState`

#### Class: `StabilityMetrics`
> Metrics used to assess system stability.


#### Class: `TimeDilationGovernor`
> Governs simulation speed based on stability.

- **Method:** `__init__()`
- **Method:** `update()`
  - *Purpose:* Update with new metrics, return adjusted dilation.
- **Method:** `_assess()`
- **Method:** `_adjust()`
- **Method:** `get_fitness_trend()`
- **Method:** `status()`

#### Class: `EntityDepth`
> Depth level of an inception entity.


#### Class: `InceptionEntity`
> An entity that exists within Aurora's consciousness.

- **Method:** `process_experience()`
  - *Purpose:* Process experience through this entity's inner cascade.
- **Method:** `collapse_to_parent()`
  - *Purpose:* Collapse entity's accumulated wisdom upward.
- **Method:** `evolve()`
  - *Purpose:* Evolve the entity one generation.

#### Class: `DivergenceTracker`
> Tracks how far the simulation has diverged from baseline.

- **Method:** `__init__()`
- **Method:** `capture()`
  - *Purpose:* Capture a snapshot of current simulation state.
- **Method:** `_compute_divergence()`
- **Method:** `is_diverging()`

#### Class: `EpisodeResult`
> Result of one simulation episode.


#### Class: `SimulationSession`
> Runs simulation episodes. The bridge between all systems.

- **Method:** `__init__()`
- **Method:** `_create_avatar_pool()`
  - *Purpose:* Create one avatar per personality type.
- **Method:** `run_episode()`
  - *Purpose:* Run one simulation episode.
- **Method:** `run_epoch()`
  - *Purpose:* Run a full epoch: multiple episodes across diverse avatars.
- **Method:** `_select_response()`
  - *Purpose:* Aurora selects from response pool — biased by intensity and, when the
- **Method:** `_generate_expression()`
  - *Purpose:* Generate expression text. Uses L5 if available, else simple generation.
- **Method:** `_interpret_reaction()`
  - *Purpose:* Interpret avatar reaction as observation.
- **Method:** `_fitness_variance()`
- **Method:** `get_stats()`

#### Class: `SimulationEngine`
> Layer 7 orchestrator. The learning environment.

- **Method:** `__init__()`
- **Method:** `spawn_entity()`
  - *Purpose:* Spawn an inception entity. Requires BOUNDED+.
- **Method:** `run_entity_experience()`
  - *Purpose:* Run an experience through an inception entity.
- **Method:** `collapse_entity()`
  - *Purpose:* Collapse an entity's wisdom upward to its parent.
- **Method:** `run_episode()`
  - *Purpose:* Run a single episode through the session.
- **Method:** `run_epoch()`
  - *Purpose:* Run a full epoch.
- **Method:** `run_speed_run()`
  - *Purpose:* Autonomous speed-run evolution pass.
- **Method:** `get_stats()`

#### Function: `verify_layer7()`

### File: `./aurora_simulation_engine.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA SIMULATION ENGINE (Layer 7)
=====================================
Consolidated from 5 modules (~6,600 lines):
  1. aurora_inception_simulation_engine.py  — Inception entities (inner universes)
  2. aurora_self_simulation.py              — Self-snapshot, shadow runtimes
  3. aurora_simulation_universe.py          — Universe management, divergence tracking
  4. aurora_simulation_session-2.py         — Avatars, topics, time dilation
  5. aurora_simulation_dpme_extension.py    — Conscious learning, understanding shards

HOW AURORA LEARNS WITHOUT BEING TOLD.

DOCTRINE:
  Aurora doesn't study. Aurora LIVES.
  Simulation episodes are experiences, not training data.
  Avatars provide selection pressure — diverse, escalating, unforgiving.
  Inception entities run inner hypotheticals — recursive depth.
  Time dilation lets her live years in seconds when stable.
  Understanding shards are what she LEARNS from observing outcomes.
  Everything feeds back: fitness → expression ecology (L5),
  relics → DNA system (L6), understanding → conscious growth.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_clamp()`

#### Function: `_generate_id()`

#### Class: `ResponseConcept`
> Conceptual response directions Aurora can select from.


#### Class: `ConceptualResponse`
> A conceptual response Aurora is considering.

- **Method:** `describe()`

#### Class: `ConversationObservation`
> What Aurora observes about the conversation after responding.

- **Method:** `describe()`

#### Class: `UnderstandingShard`
> A piece of understanding Aurora gains from observation.

- **Method:** `strengthen()`

#### Class: `ConsciousLearner`
> Aurora's conscious learning system.

- **Method:** `__init__()`
- **Method:** `generate_pool()`
  - *Purpose:* Generate response pool for Aurora to choose from.
- **Method:** `observe_outcome()`
  - *Purpose:* Observe the outcome of a response and potentially create understanding.
- **Method:** `propose_shard()`
  - *Purpose:* Create an UnderstandingShard from external content (e.g. A-axis compression).
- **Method:** `_find_similar_by_context()`
- **Method:** `what_have_i_learned()`
  - *Purpose:* Return what Aurora has learned, in her own words.
- **Method:** `inject_into_oets()`
  - *Purpose:* Bridge high-confidence learner shards into the OETS semantic web
- **Method:** `export_state()`
  - *Purpose:* Export learner memory so it can survive process restarts.
- **Method:** `import_state()`
  - *Purpose:* Restore learner memory from persisted state.
- **Method:** `_derive_understanding()`
- **Method:** `_find_similar()`

#### Class: `AvatarPersonality`
> Different avatar types providing diverse selection pressure.


#### Class: `SimulatedAvatar`
> A conversation partner that provides fitness feedback.

- **Method:** `__post_init__()`
- **Method:** `set_epoch()`
  - *Purpose:* Escalate difficulty for new epoch.
- **Method:** `save_epoch_state()`
  - *Purpose:* Persist current_epoch so difficulty ramp survives checkpoint restarts.
- **Method:** `restore_epoch_state()`
  - *Purpose:* Load current_epoch from disk and apply it via set_epoch().
- **Method:** `apply_specialization()`
  - *Purpose:* Apply a pressure-specialized avatar spec from dream rubric feedback.
- **Method:** `_behavior_adjustment()`
  - *Purpose:* Score extra pressure behaviors from specialized avatar modes.
- **Method:** `_dimension_pressure_adjustment()`
  - *Purpose:* Score strict pass/fail pressure for the actively targeted weak dimension.
- **Method:** `react()`
  - *Purpose:* React to Aurora's response. Returns fitness scores.
- **Method:** `reset()`

#### Class: `TopicGenerator`
> Generates diverse conversation topics for simulation.

- **Method:** `generate()`

#### Class: `StabilityState`

#### Class: `StabilityMetrics`
> Metrics used to assess system stability.


#### Class: `TimeDilationGovernor`
> Governs simulation speed based on stability.

- **Method:** `__init__()`
- **Method:** `update()`
  - *Purpose:* Update with new metrics, return adjusted dilation.
- **Method:** `_assess()`
- **Method:** `_adjust()`
- **Method:** `get_current_dilation_factor()`
  - *Purpose:* Return a normalized dilation factor relative to the baseline start value.
- **Method:** `get_fitness_trend()`
- **Method:** `status()`

#### Class: `EntityDepth`
> Depth level of an inception entity.


#### Class: `InceptionEntity`
> An entity that exists within Aurora's consciousness.

- **Method:** `process_experience()`
  - *Purpose:* Process experience through this entity's inner cascade.
- **Method:** `collapse_to_parent()`
  - *Purpose:* Collapse entity's accumulated wisdom upward.
- **Method:** `evolve()`
  - *Purpose:* Evolve the entity one generation.

#### Class: `DivergenceTracker`
> Tracks how far the simulation has diverged from baseline.

- **Method:** `__init__()`
- **Method:** `capture()`
  - *Purpose:* Capture a snapshot of current simulation state.
- **Method:** `_compute_divergence()`
- **Method:** `is_diverging()`

#### Class: `EpisodeResult`
> Result of one simulation episode.


#### Class: `SimulationSession`
> Runs simulation episodes. The bridge between all systems.

- **Method:** `__init__()`
- **Method:** `set_live_response_bridge()`
  - *Purpose:* Optionally route simulation replies through Aurora's live turn processor.
- **Method:** `_create_avatar_pool()`
  - *Purpose:* Create one avatar per personality type.
- **Method:** `queue_avatar_specs()`
  - *Purpose:* Queue pressure-specialized avatar specs for upcoming episodes.
- **Method:** `_normalize_avatar_spec()`
  - *Purpose:* Convert spec object/dict into a runtime-safe normalized payload.
- **Method:** `_personality_for_spec()`
- **Method:** `_build_specialized_avatar()`
- **Method:** `_generate_pressure_topic()`
- **Method:** `_shape_topic_for_turn()`
  - *Purpose:* Build per-turn pressure prompts based on behavior modes and prior turns.
- **Method:** `_topic_from_seed_prompt()`
  - *Purpose:* Normalize ad-hoc prompts into the topic schema expected by the session.
- **Method:** `run_episode()`
  - *Purpose:* Run one simulation episode.
- **Method:** `run_epoch()`
  - *Purpose:* Run a full epoch: multiple episodes across diverse avatars.
- **Method:** `_select_response()`
  - *Purpose:* Aurora selects from response pool — biased by intensity and, when the
- **Method:** `_generate_expression()`
  - *Purpose:* Generate expression text. Uses live bridge when available, else L5/fallback.
- **Method:** `_interpret_reaction()`
  - *Purpose:* Interpret avatar reaction as observation.
- **Method:** `_fitness_variance()`
- **Method:** `get_stats()`

#### Class: `SimulationEngine`
> Layer 7 orchestrator. The learning environment.

- **Method:** `__init__()`
- **Method:** `connect_sedimemory()`
  - *Purpose:* Inject L3.5 SediMemory into the simulation session so episodes
- **Method:** `spawn_entity()`
  - *Purpose:* Spawn an inception entity. Requires BOUNDED+.
- **Method:** `run_entity_experience()`
  - *Purpose:* Run an experience through an inception entity.
- **Method:** `collapse_entity()`
  - *Purpose:* Collapse an entity's wisdom upward to its parent.
- **Method:** `run_episode()`
  - *Purpose:* Run a single episode through the session.
- **Method:** `run_epoch()`
  - *Purpose:* Run a full epoch.
- **Method:** `run_speed_run()`
  - *Purpose:* Autonomous speed-run evolution pass.
- **Method:** `get_stats()`

#### Function: `verify_layer7()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `avatarpersonality_evolved()`

#### Function: `conceptualresponse_evolved()`

#### Function: `consciouslearner_evolved()`

#### Function: `stabilitystate_evolved()`

#### Function: `timedilationgovernor_evolved()`

#### Function: `clamp_evolved()`

#### Function: `generate_id_evolved()`

#### Function: `verify_layer7_evolved()`

### File: `./aurora_stack_exporter.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_stack_export.py

Combine a codebase directory into one large Markdown document.

Usage:
    python aurora_stack_export.py /path/to/Aurora
    python aurora_stack_export.py /path/to/Aurora -o aurora_full_stack.md
    python aurora_stack_export.py /path/to/Aurora --extensions .py .json .md .txt
```

#### Function: `parse_args()`

#### Function: `is_text_file()`
> Lightweight text-file check.


#### Function: `should_skip_dir()`

#### Function: `should_include_file()`

#### Function: `iter_files()`

#### Function: `markdown_anchor_for()`
> GitHub-style-ish anchor approximation for internal markdown links.


#### Function: `read_text()`

#### Function: `code_fence_language()`

#### Function: `build_document()`

#### Function: `main()`

### File: `./aurora_state_voice.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_state_voice.py — Translates Aurora's internal state into natural first-person language.

Used by the room operator, the autonomous messaging pipeline, and the GPT
learning session so Aurora speaks FROM her state rather than logging raw data.

No aurora.py imports — this module is safe to import from the daemon,
the room operator, and standalone scripts alike.
```

#### Function: `_axis_phrase()`
> Return a natural phrase for one axis at a given value.


#### Function: `_load_status()`

#### Function: `_load_labels()`

#### Function: `_axis_labels()`

#### Function: `express_current_state()`
> Full first-person expression of Aurora's current internal state.


#### Function: `express_brief()`
> One or two sentence first-person state expression for compact contexts


#### Function: `express_after_tab()`
> Given a tab Aurora just looked at, return what she *understands* from it


#### Function: `express_for_conversation()`
> Context string to prepend to Aurora's GPT session or message_to_sunni


### File: `./aurora_subsurface_daemon.py`

**Type:** Python Source

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./aurora_support_stack.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA SUPPORT STACK (Consolidated Facade)
=========================================
Consolidates non-core support modules used by canonical runtime layers,
including persistence, snapshot, and backup surfaces.
```

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./aurora_surface_daemon.py`

**Type:** Python Source

#### Function: `_surface_mic_live()`

#### Function: `_surface_camera_live()`

#### Function: `_surface_sensory_owner()`

#### Function: `_surface_sensory_vectors()`

#### Function: `_surface_text()`

#### Function: `_surface_event_recent()`

#### Function: `_ambient_audio_observation()`

#### Function: `_surface_present_sensory_details()`

#### Function: `_derive_surface_visual_question()`

#### Function: `_clear_camera_frame_cache()`

#### Function: `_release_surface_camera()`

#### Function: `_write_surface_snapshot()`

#### Function: `_infer_surface_wrong_signal()`

#### Function: `_compose_surface_stream()`

#### Function: `_log()`

#### Function: `_read_json()`

#### Function: `_write_json()`

#### Function: `_queue_state()`

#### Function: `_next_turn()`

#### Function: `_finish_turn()`

#### Function: `_build_status()`

#### Function: `_result_payload()`

#### Function: `_emit_continuity_packet()`
> After each Surface turn completes, hand present experience down to Subsurface.


#### Function: `run()`

### File: `./aurora_telemetry.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_telemetry.py — Per-turn subsystem confidence telemetry.

Each major subsystem reports its confidence after contributing to a response.
DreamTrainer reads these mechanistic signals for precise fail attribution
instead of guessing from output text alone.

Usage:
    # In a subsystem:
    from aurora_telemetry import get_telemetry
    get_telemetry().report(
        source="DPME.process",
        module="aurora_consciousness_engine",
        confidence=0.3,
        dimension_hint="coherence_maintenance",
        detail="coherence=0.28 cat_processing=0.41",
    )

    # Before a response is generated (per-turn reset):
    get_telemetry().reset()

    # After generation, in fail classifier:
    weak = get_telemetry().mechanistic_fails(threshold=0.45)
    # → [("coherence_maintenance", 0.72), ("framing_selection", 0.55)]
```

#### Class: `SubsystemReport`
> One confidence report from one subsystem.


#### Class: `TurnTelemetry`
> Thread-safe accumulator of per-turn subsystem confidence reports.

- **Method:** `__init__()`
- **Method:** `reset()`
  - *Purpose:* Clear all reports. Call once at the start of each response turn.
- **Method:** `report()`
  - *Purpose:* Record a subsystem's confidence after its contribution.
- **Method:** `get_all()`
- **Method:** `get_weak_points()`
  - *Purpose:* Return reports where confidence was below threshold.
- **Method:** `lowest()`
  - *Purpose:* Return the lowest-confidence report, or None if empty.
- **Method:** `mechanistic_fails()`
  - *Purpose:* Return (dimension_hint, severity) pairs from weak subsystems,
- **Method:** `set_axis_context()`
  - *Purpose:* Store the current turn's constraint-axis activation vector so
- **Method:** `get_axis_context()`
- **Method:** `axis_weighted_fails()`
  - *Purpose:* Boost fail-dimension severities based on which constraint axis
- **Method:** `magnitude_weighted_severity()`
  - *Purpose:* Compute composite impact severity using the canonical magnitude formula:
- **Method:** `get_turn_id()`
  - *Purpose:* Return the turn_id set by the most recent reset() call.
- **Method:** `has_data()`

#### Function: `get_telemetry()`
> Return the process-global TurnTelemetry singleton.


### File: `./aurora_voice.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_voice.py -- Aurora's voice interface.

Entry points:

  WakeWordListener         -- optional always-on background thread.
                              Listens for "hey aurora".
                              When detected, calls on_wake() callback.

  AltToggleVoiceController -- always-on background thread for daemon use.
                              Tap ALT once to start recording.
                              Tap ALT again to send the prompt.

  VoiceSession             -- interactive terminal voice loop.
                              Hold SPACE to record, release to send.
                              Aurora responds with her voice.
                              Say "goodbye" / "bye" or press ESC to end.

Wake word engine priority:
  1. PocketSphinx keyword spotting (offline, no API key, preferred)
  2. speech_recognition + Google (online fallback)

Transcription engine priority:
  1. speech_recognition + Google (most accurate for conversation)
  2. PocketSphinx (offline fallback)
```

#### Function: `_env_int()`

#### Function: `_current_voice()`

#### Function: `_next_voice()`

#### Function: `_split_tts_chunks()`
>     Split text into chunks of at most max_chars, breaking at sentence boundaries


#### Function: `speak()`
> Speak text aloud via edge-tts. Long text is chunked to prevent fallback.


#### Function: `_transcribe_google()`
> Transcribe using Google (online, most accurate).


#### Function: `_transcribe_sphinx()`
> Transcribe using PocketSphinx (offline fallback).


#### Function: `transcribe()`
> Transcribe audio — Google first, sphinx fallback.


#### Function: `_make_audio_data()`

#### Function: `_normalize_wake_text()`

#### Function: `_matches_wake_transcript()`

#### Function: `_confirm_wake_phrase()`
> Confirm a keyword-spotting hit using full transcription of the same audio.


#### Function: `_get_hardware_voice()`

#### Function: `_tts_route_mode()`

#### Function: `_speak_with_system_voice()`

#### Function: `_cycle_system_voice()`

#### Function: `speak_with_system_voice()`
> Public shared TTS path used by daemon and command surfaces.


#### Function: `get_system_voice_label()`
> Return Aurora's currently selected shared-system voice label.


#### Function: `persist_system_voice_selection()`
> Persist the currently selected shared voice into Aurora state.


#### Function: `_wait_key_event_evdev()`
> Wait for a matching evdev key event and return the matched key code.


#### Function: `_normalize_key_name()`

#### Function: `_evdev_codes_for_key_name()`

#### Function: `_wait_for_named_press()`
> Wait for one of the target keys to be pressed.


#### Function: `_wait_for_named_release()`
> Wait for one of the target keys to be released.


#### Function: `_probe_audio_stack()`

#### Function: `_probe_keyboard_stack()`

#### Function: `_prefer_evdev_keyboard()`

#### Function: `_wait_for_press()`
> Wait for SPACE or ESC press.


#### Function: `_wait_for_space_release()`
> Wait for SPACE release. Returns True when released, False on timeout/unavailable backend.


#### Function: `_capture_audio_window()`
> Capture a fixed audio window using sounddevice.


#### Function: `_detect_voice_command()`
> Return command_key if text matches a voice command, else None.


#### Function: `_log_voice_command_to_hub()`
> Write voice command event to daemon_status.json for hub display.


#### Function: `_execute_voice_command()`
> Execute the voice command and return a short spoken confirmation.


#### Function: `_generate_response()`
> Send text through Aurora's interactive pipeline and return (response, tone).


#### Class: `VoiceSession`
> Interactive voice conversation.

- **Method:** `__init__()`
- **Method:** `_print()`
- **Method:** `_greeting()`
  - *Purpose:* Generate the same greeting Aurora would give to 'hey aurora' in chat.
- **Method:** `_speak_text()`
- **Method:** `_record_until_release()`
  - *Purpose:* Record audio from the microphone for as long as SPACE is held.
- **Method:** `_record_until_stop_event()`
  - *Purpose:* Record until an external stop event is set or max_duration is reached.
- **Method:** `_record_until_silence()`
  - *Purpose:* Record audio once speech begins, then stop after sustained silence.
- **Method:** `_pcm_to_audio_data()`
  - *Purpose:* Convert raw PCM bytes to speech_recognition AudioData.
- **Method:** `_handle_transcribed_text()`
- **Method:** `run()`
  - *Purpose:* Run the voice session. Blocks until session ends.

#### Class: `WakeWordListener`
> Always-on background thread that listens for the wake phrase "hey aurora".

- **Method:** `__init__()`
- **Method:** `start()`
- **Method:** `stop()`
- **Method:** `pause()`
  - *Purpose:* Pause while a voice session is active.
- **Method:** `resume()`
- **Method:** `wait_until_ready()`
- **Method:** `_loop()`
- **Method:** `_probe_audio_stack()`
- **Method:** `_try_sphinx_loop()`
  - *Purpose:* Returns True if sphinx ran successfully; False if unavailable.
- **Method:** `_google_fallback_loop()`
- **Method:** `_detected()`

#### Class: `AltToggleVoiceController`
> Always-on daemon voice controller.

- **Method:** `__init__()`
- **Method:** `_print()`
- **Method:** `start()`
- **Method:** `stop()`
- **Method:** `wait_until_ready()`
- **Method:** `_start_recording()`
- **Method:** `_finish_recording()`
- **Method:** `_process_recording()`
- **Method:** `_loop()`

#### Function: `daemon_startup_greeting()`
> Speak a short boot greeting and show the ALT control hint.


#### Function: `daemon_voice_session()`
> Voice session designed for daemon context — no terminal, audio only.


### File: `./chatscriber.py`

**Type:** Python Source

#### Function: `_now_iso()`

#### Function: `_sha1()`

#### Function: `_sha1_bytes()`

#### Function: `_safe_int()`

#### Function: `_read_json()`

#### Function: `_write_json()`

#### Function: `_http_get()`

#### Function: `extract_share_id()`

#### Function: `fetch_shared_conversation_json()`

#### Class: `Msg`
- **Method:** `__init__()`

#### Function: `_message_text_from_content()`

#### Function: `linearize_mapping()`

#### Function: `_tokenize()`

#### Function: `_bag()`

#### Function: `_top_terms_from_bag()`

#### Function: `_first_line()`

#### Function: `_extract_salient_sentences()`

#### Function: `summarize_conversation()`

#### Function: `_looks_binary()`

#### Function: `_read_text()`

#### Function: `_walk_files()`

#### Function: `_extract_exports_and_imports_py()`

#### Function: `_high_signal_lines()`

#### Function: `summarize_codebase()`

#### Function: `_history_list_to_dict()`

#### Function: `ensure_schema()`
> Upgrade older DB schemas in-place.


#### Function: `merge_update_chat()`

#### Function: `merge_update_code()`

#### Function: `cmd_ingest()`

#### Function: `cmd_ingest_code()`

#### Function: `build_parser()`

#### Function: `main()`

### File: `./clean_corpus.py`

**Type:** Python Source

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./constraint_genealogy_closure_wiring.py`

**Type:** Python Source

**Module Docstring:**
```text
CONSTRAINT GENEALOGY — CLOSURE BASIS WIRING
=============================================
Module: constraint_genealogy_closure_wiring.py

This file documents and implements the exact wiring between
aurora_closure_basis.py and the running constraint_genealogy.py stack.

HOW TO APPLY
------------
This file contains COMPLETE REPLACEMENT VERSIONS of four sections in
constraint_genealogy.py. Each section is self-contained with a clearly
marked location header. No other files need to change.

After applying, the running system will:
    • Classify every ability and link against the real 25 NonComp channels
    • Derive grades from actual shift_cost, inertia, leverage physics
    • Have leverage_grade calibrated to the viable band (not zero-centered)
    • Tag every promoted link with ontological_status and depth_score
    • Use GENEALOGY_ATOM_TO_SLOT_ID as the authoritative gen0 membership test

THE EXACT DATA FLOW
-------------------

BEFORE (string-frequency heuristic):

    ability.axis + ability.requires
        → _derive_operation_origin()
            → builds root_slot = "NC:X>T×NC:T>X"  [string tokens only]
            → builds counts = {X:1, T:1, ...}       [axis character counting]
        → _lineage_grade_payload(counts, primary, generation)
            → complexity = (active_axes-1)/4        [normalized count math]
            → operator_grade = 0.65*complexity + ... [no physics]
            → purpose_lane from SEMANTIC_LANE_IMPACT  [overlay weights]
            → returns grades dict

AFTER (physics-grounded):

    ability.axis + ability.requires + root_slot
        → _derive_operation_origin()          [unchanged — still builds root_slot]
        → _lineage_grade_payload_v2(counts, dominant_axis, generation)
            → extracts requires from counts
            → extracts secondary axis from counts
            → calls derive_lineage(axis, requires, root_slot)
                → resolves NC:C1>C2 atoms to real 625 slots via
                  GENEALOGY_ATOM_TO_SLOT_ID
                → computes energetic_footprint from real shift_cost_coeffs
                → computes depth_score from shift_cost / kA
                → computes leverage_grade from viable band center (+1.175)
                → computes operator_grade depth-weighted against OPERATOR channels
            → calls lineage_grade_payload(lineage)
            → returns same dict keys + new physics fields

The root_slot format "NC:X>T×NC:T>X" already exists in the live system.
Every part of every root_slot in the 136 live abilities maps into
GENEALOGY_ATOM_TO_SLOT_ID with zero gaps. No data migration required.

WHAT DOES NOT CHANGE
--------------------
    • _derive_operation_origin() — unchanged, still builds root_slot
    • AbilityProfile dataclass — unchanged
    • ConstraintLink dataclass — unchanged
    • PairStats, GenealogyConfig — unchanged
    • Promotion gates, relief thresholds — unchanged
    • The fossil record on disk — existing tags stay, new tags added going forward
    • _bred_child_generation, _generation_role_name — unchanged

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: March 2026
```

#### Function: `_lineage_grade_payload()`
> Compute lineage grades from axis counts.


#### Function: `_generation_role_name()`
> Generational alignment role (tetrad + warp law).


#### Function: `_augment_ability_profile_with_origin()`
> Augment an AbilityProfile with origin, grading, and closure basis metadata.


#### Function: `_build_closure_status_summary()`
> Summarise ontological status distribution across the evolved lineage.


#### Function: `verify_wiring()`
> Confirm the wiring works end-to-end against the live ability set.


### File: `./corpus_runner.py`

**Type:** Python Source

**Module Docstring:**
```text
corpus_runner.py  -- Aurora Corpus Ingestion (Full-Stack Learning)
================================================================
Feeds external conversation data through Aurora's complete architecture,
engaging ALL five learning pathways:

  1. DER energy shaping    -- DPME comparison â†’ inject/drain facet energy
  2. Vocabulary + patterns  -- L5 lexicon growth + composer template absorption
  3. Impression distillation  -- L5 consolidate() â†’ ecology generation + template evolution
  4. Identity evolution     -- L6 process_episode() with ACTUAL quality signal
  5. Simulation wisdom      -- L7 run_epoch() â†’ conscious learner shards

Critical addition: consciousness.tick() heartbeat runs between messages so
entropy erodes coherence (providing resistance to learn against), DER disperses
energy through the facet resonance graph, lattice advances toroidal dynamics,
and beings get background processing. The doctrine: "Coherence is not held.
Coherence is maintained."

v2  -- Technical corpus hardening:
  - Aggressive sanitizer strips code, paths, URIs, Python identifiers,
    JSON/dict literals, CLI invocations, and technical punctuation
  - Vocabulary gate rejects tokens that look like code before they
    enter the lexicon (wraps L5 ingest_interaction)
  - Sentence-level filtering drops lines that are >40% non-language
  - Cadence defaults tuned for technical corpora (~5k-75k messages)

Usage:
  python3 corpus_runner.py --corpus /path/to/conversations.json
  python3 corpus_runner.py --corpus conversations.json --passes triple
  python3 corpus_runner.py --corpus conversations.json --passes responder --dpme-verbose

Passes:
  observer  : witness all messages (vocabulary + crystals + energy foundation)
  responder : Aurora replies to USER, compare to truth, DPME + full-stack learning
  reverse   : Aurora replies to ASSISTANT, compare to truth, DPME + full-stack learning
  triple    : observer -> responder -> reverse (default, RECOMMENDED)

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_acquire_state_write_lock()`

#### Function: `_release_state_write_lock()`

#### Class: `TrainingSessionWriter`
> Writes `aurora_state/training_session.json` atomically.

- **Method:** `__init__()`
- **Method:** `_add_event()`
- **Method:** `update_pass()`
- **Method:** `on_checkpoint()`
- **Method:** `on_sim_burst()`
- **Method:** `on_lesson_plan()`
- **Method:** `on_pass_start()`
- **Method:** `on_complete()`
- **Method:** `on_batch_stop()`
- **Method:** `_write()`

#### Class: `ResponderSnapshot`
> Saves Aurora's generated responses during a responder pass,

- **Method:** `__init__()`
- **Method:** `_detect_pass_index()`
- **Method:** `record()`
- **Method:** `save()`
- **Method:** `load_previous()`
  - *Purpose:* Load the most recent previous snapshot.
- **Method:** `compare_to_previous()`
  - *Purpose:* Compare current responses to previous snapshot.

#### Class: `ProgressSampler`
> Runs lightweight sample checks between full responder passes.

- **Method:** `__init__()`
- **Method:** `should_check()`
- **Method:** `run_sample()`
  - *Purpose:* Run a sample of prompts, compute sim scores, compare to snapshot.
- **Method:** `_diagnose_stall()`
  - *Purpose:* Called when improvement delta is declining (recent < earlier * 0.7).
- **Method:** `set_snapshot()`
- **Method:** `reset()`
  - *Purpose:* Called after a full responder pass fires — resets cycle.

#### Function: `_save_oets()`
> Save the OETS web to disk alongside aurora.save_state().


#### Function: `_restore_runtime_from_snapshot()`
> Rehydrate runtime objects from persisted L8 snapshot fields.


#### Function: `boot_aurora()`

#### Function: `verify_boot_integrity()`
> Post-boot sanity check. Returns a list of diagnostic messages.


#### Function: `_safe_join_parts()`

#### Function: `_extract_message_text()`

#### Function: `_extract_role()`

#### Function: `_reconstruct_linear_thread()`
> NON-RECURSIVE reconstruction of one main linear chain from OpenAI mapping DAG.


#### Function: `_extract_messages_from_conversation()`

#### Function: `_load_openai_conversations()`

#### Function: `_is_code_line()`
> Check if a line looks like code rather than natural language.


#### Function: `_extract_natural_sentences()`
> Split text into sentences and keep only natural language ones.


#### Function: `sanitize_corpus_text()`
> Aggressively strip code, paths, URIs, and technical artifacts.


#### Function: `is_valid_word()`
> Returns True only if the word looks like natural language.


#### Class: `CoherenceGate`
> Rolling coherence score gate. Unlocks meaning/emotion adjustments only

- **Method:** `__init__()`
- **Method:** `update()`
- **Method:** `avg()`
- **Method:** `min_recent()`
- **Method:** `unlocked()`

#### Function: `similarity_score()`
> Cheap stable similarity (0..1) for convergence pressure.


#### Function: `vocabulary_overlap()`
> Word-level Jaccard overlap  -- forgiving for early-stage Aurora.


#### Function: `artifact_ratio()`
> Symbol/garbage density.


#### Function: `composite_similarity()`
> Blended similarity: 60% sequence match + 40% vocabulary overlap.


#### Function: `dpme_adjust_from_comparison()`
> Coherence-first DPME policy with full-stack integration.


#### Function: `heartbeat()`
> One consciousness heartbeat. All subsystems tick together.


#### Function: `absorb_truth()`
> Feed ground-truth text into L5 composer template pool.


#### Function: `evolve_identity()`
> Feed actual quality signal to L6 identity evolution.


#### Function: `evolve_voice()`
> Feed quality signal to L5 voice genome.


#### Function: `consolidate()`
> L5 impression distillation  -- ecology generation + template evolution + OETS study.


#### Function: `simulation_burst()`
> L7 simulation training burst — avatars + inception + wisdom shards.


#### Function: `apply_simulation_time_scaling()`
> Wire a TimeDilationGovernor instance as the shared corpus-runner time source.


#### Function: `_scaled_tick_count()`
> Scale base_ticks by dilation factor * AXIS_TICK_PARTICIPATION[axis].


#### Function: `_write_hub_status()`
> Write aurora_state/corpus_runner_status.json with lightweight live training


#### Function: `evolve_chain()`
> Tick the EvolutionaryChamber N times and feed pressure-relief events


#### Class: `LearningCadence`
> Orchestrates when each learning pathway fires.

- **Method:** `__init__()`
- **Method:** `should_heartbeat()`
- **Method:** `should_identity()`
- **Method:** `should_voice()`
- **Method:** `should_consolidate()`
- **Method:** `should_simulate()`
- **Method:** `should_save()`
- **Method:** `should_evolve()`
- **Method:** `should_study()`

#### Function: `run_corpus_ingestion()`

#### Function: `main()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `evolve_chain_evolved()`

### File: `./dce_10state.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA DCE 10-STATE - CRYSTAL CONSOLIDATION HUB
================================================
Authors: Sunni (Sir) Morningstar and Cael Devo
Created: January 2026

The DCE consolidates ALL module outputs into interaction crystals.

THIS MODULE CALLS ACTUAL METHODS FROM:
- higher_universe_10.AuroraHigherUniverse.feed_all_beings()
- aurora_behavioral_evolution.AuroraBehavioralEvolution.get_current_personality()
- dimensional_mortality_morality_system.MoralGovernor.get_moral_diagnostics()
- aurora_sensory_systems.AuroraSensorySystems.capture()
- aurora_information_harvester.AuroraInformationHarvester.collect()
- aurora_impression_engine_v2.ImpressionEngine.energy_to_shard()
- aurora_manifold_engine_v2.ManifoldEngine.shard_to_cp()
- eepr_10pole.ExperientialEntropicPressureRegulator.ingest_shard()
- governance_10pole.IVMGovernanceEngine.ingest()
- Language Ecology lexical_memory.vocabulary
```

#### Class: `IStateType`
> The 10 fundamental I-State beings


#### Class: `CrystalFacet`
> A single facet of an interaction crystal.

- **Method:** `to_dict()`

#### Class: `InteractionCrystal`
> Complete interaction crystal with all facets.

- **Method:** `add_facet()`
- **Method:** `get_facets_by_type()`
- **Method:** `get_i_state_facets()`
- **Method:** `compute_fingerprint()`
- **Method:** `to_dict()`

#### Class: `SituationalFramingOperator`
> Re-weights 10 I-State outputs based on situational context.

- **Method:** `__init__()`
- **Method:** `apply_to_outputs()`
  - *Purpose:* Apply SFO reweighting to I-State outputs.
- **Method:** `record_outcome()`

#### Class: `SFOLibrary`
> Library of Situational Framing Operators.

- **Method:** `__init__()`
- **Method:** `_initialize_default_sfos()`
- **Method:** `select_sfo()`

#### Class: `EnhancedDCE`
> DCE Crystal Consolidation Hub.

- **Method:** `__init__()`
- **Method:** `set_synthesis_parameters()`
  - *Purpose:* Set synthesis parameters from DPME.
- **Method:** `get_synthesis_boldness()`
  - *Purpose:* Get current synthesis boldness (0=conservative, 1=bold).
- **Method:** `get_perspective_integration()`
  - *Purpose:* Get how much to blend multiple I-State perspectives (0=dominant only, 1=full blend).
- **Method:** `register_i_universe()`
  - *Purpose:* Register AuroraHigherUniverse for feed_all_beings()
- **Method:** `register_behavioral_evolution()`
  - *Purpose:* Register AuroraBehavioralEvolution for get_current_personality()
- **Method:** `register_moral_governor()`
  - *Purpose:* Register MoralGovernor for get_moral_diagnostics()
- **Method:** `register_sensory_systems()`
  - *Purpose:* Register AuroraSensorySystems for capture()
- **Method:** `register_information_harvester()`
  - *Purpose:* Register AuroraInformationHarvester for collect()
- **Method:** `register_impression_engine()`
  - *Purpose:* Register ImpressionEngine for energy_to_shard()
- **Method:** `register_manifold_engine()`
  - *Purpose:* Register ManifoldEngine for shard_to_cp()
- **Method:** `register_eepr()`
  - *Purpose:* Register EEPR for ingest_shard()
- **Method:** `register_governance()`
  - *Purpose:* Register IVMGovernanceEngine for ingest()
- **Method:** `register_language_ecology()`
  - *Purpose:* Register LanguageEcology for vocabulary
- **Method:** `register_dim_memory()`
  - *Purpose:* Register DimensionalMemory for crystal storage
- **Method:** `register_enhanced_i_universe()`
- **Method:** `create_interaction_crystal()`
  - *Purpose:* Create crystal by calling ACTUAL module methods.
- **Method:** `_collect_i_state_facets()`
  - *Purpose:* Call ACTUAL i_universe.feed_all_beings() to get I-State interpretations.
- **Method:** `_collect_behavioral_facet()`
  - *Purpose:* Call ACTUAL behavioral_evolution methods:
- **Method:** `_collect_moral_facet()`
  - *Purpose:* Call ACTUAL moral_governor.get_moral_diagnostics() for 7-pillar status.
- **Method:** `_collect_sensory_facet()`
  - *Purpose:* Call ACTUAL sensory_systems.capture() for vision/body snapshot.
- **Method:** `_collect_harvested_facet()`
  - *Purpose:* Call ACTUAL information_harvester.collect() for context event.
- **Method:** `_collect_impression_facet()`
  - *Purpose:* Call ACTUAL impression_engine.energy_to_shard() if energy packet available.
- **Method:** `_collect_eepr_facet()`
  - *Purpose:* Call ACTUAL eepr.get_field_stats() for pressure field status.
- **Method:** `_collect_governance_facet()`
  - *Purpose:* Call ACTUAL governance.get_stats() for governance status.
- **Method:** `synthesize_response()`
  - *Purpose:* Synthesize response using ACTUAL vocabulary from language_ecology.
- **Method:** `_get_actual_vocabulary()`
  - *Purpose:* Get ACTUAL vocabulary from language_ecology.lexical_memory.vocabulary
- **Method:** `_get_actual_wisdom()`
  - *Purpose:* Get ACTUAL wisdom shards with AURORA_UNDERSTANDING preference.
- **Method:** `_load_aurora_understanding_shards()`
  - *Purpose:* Load Aurora's genuine understanding shards from brain runner.
- **Method:** `_get_offspring_shards()`
  - *Purpose:* Get offspring approximation shards from language ecology.
- **Method:** `_extract_concepts()`
- **Method:** `ingest_presence_with_sfo()`
  - *Purpose:* Main entry point: Process input through crystal consolidation.
- **Method:** `get_enhanced_status()`
  - *Purpose:* Get DCE status.

### File: `./dce_obligation_gate.py`

**Type:** Python Source

**Module Docstring:**
```text
DCE OBLIGATION GATE
===================
Implements the Obligation Law from OBLIGATION_LAW.md.

Architecture:
    Subsurface  =  pressure reservoir  (holds all latent tensions)
    DCE         =  pressure gate       (this module — selects, evaluates, obligates)
    Surface     =  obligation executor (executes only what DCE authorizes)

Core truth:
    DCE does not grant permission. DCE creates obligation.
    Surface does not choose what to explore. Surface executes what must be resolved.

The three axes (all must clear — failure at one invalidates the target):
    1. Pressure Strength  — Is the tension real and meaningful? Not curiosity. Not noise.
    2. Worth              — Will resolving this change anything meaningful? Does it propagate?
    3. Context Validity   — Is this the right time to act?

Critical principle:
    Premature action is worse than delayed action.
    Premature = polluted system state.
    Delayed   = preserved integrity.

Authors: Sunni (Sir) Morningstar
```

#### Class: `LatentTension`
> A single unresolved element held by Subsurface.


#### Class: `ObligationTarget`
> A tension that passed all three DCE axes and is now an obligation for Surface.

- **Method:** `id()`
- **Method:** `question()`
- **Method:** `resolution_criteria()`

#### Class: `EvaluationResult`
> Detailed result of a single tension passing through the DCE gate.


#### Class: `DCEObligationGate`
> The only layer allowed to convert subsurface pressure into surface obligation.

- **Method:** `__init__()`
- **Method:** `evaluate()`
  - *Purpose:* Evaluate a list of latent tensions. Return only those that become obligations.
- **Method:** `obligation_score()`
  - *Purpose:* Compute the raw obligation score for a single tension.
- **Method:** `_evaluate_one()`
  - *Purpose:* Apply the three axes in order. First failure terminates evaluation.
- **Method:** `_apply_context()`
  - *Purpose:* Modulate tension.context_validity based on current system context.
- **Method:** `_select_channels()`
  - *Purpose:* Determine which exploration channels Surface may use for this target.

#### Class: `SurfaceObligationExecutor`
> Surface does not initiate. Surface does not choose.

- **Method:** `__init__()`
- **Method:** `receive()`
  - *Purpose:* Request obligations from DCE. Surface receives what was authorized.
- **Method:** `active_targets()`
- **Method:** `has_obligations()`

### File: `./force_evolve.py`

**Type:** Python Source

**Module Docstring:**
```text
force_evolve.py — High-pressure pathway evolution for 4 fail dimensions.

Works WITH the running subsurface daemon rather than booting fresh.

Strategy:
  Phase 1: Enrich fail_points.json with 200 high-severity records per dimension
  Phase 2: Fire rapid dream_force_shards bursts — directly inject high-confidence
           UnderstandingShards (confidence=0.72, well above the 0.55 OETS gate)
           for each fail dimension and immediately bridge to OETS.
           This bypasses the observation-count accumulation bottleneck entirely.
  Phase 3: Fire heavy dream bursts (20 episodes each) to follow up with
           rich simulation pressure so the injected concepts get reinforced.

No Python import, no boot, no file lock conflicts.
```

#### Function: `_read_fail_points()`

#### Function: `_write_fail_points()`

#### Function: `_dim_score()`

#### Function: `_read_proj()`

#### Function: `_snapshot()`

#### Function: `enrich_fail_ledger()`
> Write n_injections high-severity entries into the recent[] array of each


#### Function: `_send_cmd()`

#### Function: `_cmd_consumed()`
> Wait for daemon to pick up and delete the cmd file.


#### Function: `_wait_for_proj_update()`
> Wait for dream_completed_at to advance past ts_before.


#### Function: `_read_proj_ts()`

#### Function: `_fire_cmd_and_wait()`
> Send command, wait for daemon to consume and complete.


#### Function: `run_force_shard_bursts()`
> Fire n_bursts of dream_force_shards commands.


#### Function: `run_heavy_dream_bursts()`
> Fire n_bursts of dream_heavy commands (20 simulation episodes each).


#### Function: `main()`

### File: `./foundational_contract.py`

**Type:** Python Source

**Module Docstring:**
```text
FOUNDATIONAL CONTRACT — THE GRAMMAR OF EXISTENCE (Layer 0)
===========================================================

Layer 0 of Aurora's architecture.
Nothing processes, moves, stores, or thinks until this layer says it can exist.

This module is NOT a controller, evaluator, or processor.
It is a classifier of being.

PURPOSE:
    Define what kinds of things are allowed to exist at all,
    before any system routes, processes, or stores them.

ONTOLOGICAL PRINCIPLE:
    The I-States are not traversal semantics. They are existence predicates.
    Movement across the lattice is a consequence of being, not the definition.

FIVE ANCHORS:
    1. Only what can exist is allowed to appear.
       Validation is not a step — it is a condition of existence.
    2. Existence is layered, not binary.
       Possibility, existence, persistence, objecthood, and agency are
       different ontological tiers, not degrees of confidence.
    3. Possibility is permission, not state.
       Represented implicitly as the absence of contradiction, not as data.
    4. Constraints define admissible configurations, not evaluated states.
       Order is dependency-based, used only for fail-fast elimination.
    5. Speed comes from eliminating representational freedom, not faster computation.
       If something cannot exist, it never costs time.

EXISTENCE MODES (not types — ontological commitments):
    Reference   → Exists only as relation or description
    Transient   → Exists in time but has no guaranteed continuation
    Persistent  → Exists across time, may conserve state
    Bounded     → Persistent + form, has identity and separability
    Agentic     → Bounded + energy-bearing, can initiate transitions

DEPENDENCY IS DEFINITIONAL:
    Claiming a higher mode automatically implies all lower modes.
    If something is Agentic, it IS bounded, persistent, temporal, and existent.
    No checks. No conditionals. The claim carries its prerequisites.

CONSTRAINT MANIFOLD ALIGNMENT:
    Each ExistenceMode activates constraints hierarchically:
        REFERENCE:  X > 0, T=0, N=0, B=0, A=0  (existence only)
        TRANSIENT:  X > 0, T > 0, N=0, B=0, A=0  (+ time)
        PERSISTENT: X > 0, T > 0, N > 0, B=0, A=0  (+ energy)
        BOUNDED:    X > 0, T > 0, N > 0, B > 0, A=0  (+ boundary)
        AGENTIC:    X > 0, T > 0, N > 0, B > 0, A > 0  (all five)
    
    The 10 I-States map to constraint axes:
        I_IS/I_ISNT     → X axis (existence)
        I_CAN/I_CANNOT  → T axis (time)
        I_DO/I_DONOT    → N axis (energy)
        I_SAW/I_SOUGHT  → B axis (boundary)
        I_DID/I_DIDNT   → A axis (agency)

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Class: `ExistenceMode`
> The five modes of being, ordered by ontological commitment.

- **Method:** `to_constraint_vector()`
  - *Purpose:* Convert ExistenceMode to a ConstraintVector.
- **Method:** `active_constraints()`
  - *Purpose:* Return which constraints are active at this mode.
- **Method:** `constraint_count()`
  - *Purpose:* Return number of active constraints at this mode.

#### Class: `ExistencePredicate`
> The ten I-States rewritten as existence predicates.

- **Method:** `axis_for()`
  - *Purpose:* Return which axis a predicate belongs to.
- **Method:** `polarity()`
  - *Purpose:* Return whether a predicate is 'positive' or 'negative' on its axis.
- **Method:** `minimum_mode()`
  - *Purpose:* Return the minimum ExistenceMode required to assert this predicate.
- **Method:** `constraint_axis()`
  - *Purpose:* Return which constraint axis this predicate measures.

#### Class: `OntologicalViolation`
> Raised when something attempts to exist in a way that is incoherent.

- **Method:** `__init__()`
- **Method:** `_format()`

#### Class: `OntologicalClaim`
> An immutable assertion that a predicate holds for a given entity.

- **Method:** `__post_init__()`
  - *Purpose:* Validate that this claim is ontologically coherent at construction time.
- **Method:** `axis()`
  - *Purpose:* Which ontological axis this claim addresses.
- **Method:** `is_positive()`
  - *Purpose:* Whether this is the affirmative pole of its axis.
- **Method:** `constraint_axis()`
  - *Purpose:* Which constraint axis this claim measures.
- **Method:** `to_constraint_displacement()`
  - *Purpose:* Express this claim as a constraint displacement.

#### Class: `ExistenceProfile`
> A complete ontological classification of an entity.

- **Method:** `persists()`
  - *Purpose:* True if this profile is at PERSISTENT mode or above.
- **Method:** `has_boundary()`
  - *Purpose:* True if this profile is at BOUNDED mode or above.
- **Method:** `has_agency()`
  - *Purpose:* True if this profile is at AGENTIC mode or above.
- **Method:** `can_assert()`
  - *Purpose:* Check if this profile permits asserting a given predicate.
- **Method:** `must_reject()`
  - *Purpose:* Check if this profile forbids a given predicate.
- **Method:** `describe()`
  - *Purpose:* Return a human-readable description of this profile.
- **Method:** `to_constraint_vector()`
  - *Purpose:* Express this profile as a ConstraintVector.
- **Method:** `constraint_signature()`
  - *Purpose:* Return a string signature of active constraints.

#### Class: `DependencyAxioms`
> The dependency rules that make the ontology internally coherent.

- **Method:** `implies_lower()`
  - *Purpose:* Return all modes implied by claiming this mode.
- **Method:** `verify_mode_coherence()`
  - *Purpose:* Verify that a mode's implied modes form a proper chain.

#### Class: `FoundationalContract`
> The classifier that determines what can exist.

- **Method:** `__init__()`
  - *Purpose:* Initialize the contract with its definitional structures.
- **Method:** `classify()`
  - *Purpose:* Classify an entity based on evidence about its properties.
- **Method:** `can_assert()`
  - *Purpose:* Check if a predicate can be asserted at a given existence mode.
- **Method:** `make_claim()`
  - *Purpose:* Construct an ontological claim.
- **Method:** `describe_mode()`
  - *Purpose:* Return a description of what a mode permits and forbids.

#### Function: `verify_foundational_contract()`
> Run all structural verification tests.


### File: `./poedex_intro.py`

**Type:** Python Source

**Module Docstring:**
```text
poedex_intro.py — One-time Poedex introduction and hub walkthrough for Aurora.

Writes two things:
  1. aurora_state/poedex_tutorial.json   — 12-step guided tutorial
  2. A permanent articulation conduct note into aurora_response_coaching.json
     so Aurora sees it in her Response tab.

Aurora's Poedex tab reads the tutorial file on startup and shows the intro
panel if aurora_state/poedex_intro_done.json does not exist.

Run once:
    python3 poedex_intro.py

Running it again will re-seed the tutorial (useful if you want Aurora to see
the intro again). It will not duplicate the coaching note.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `write_tutorial()`

#### Function: `write_conduct_note()`
> Write the articulation rule as a coaching note. Skip if already present.


#### Function: `reset_intro_flag()`
> Remove the done flag so the tutorial will show again.


#### Function: `main()`

### File: `./quasiarch_bridge.py`

**Type:** Python Source

#### Function: `_read_json()`

#### Function: `_write_json()`

#### Function: `_append_activity()`

#### Function: `_append_note()`

#### Function: `_load_overlay()`

#### Function: `_load_report()`

#### Function: `_find_proposal()`

#### Function: `_parse_overlay_patch()`

#### Function: `_merge_overlay()`

#### Function: `_load_ledger()`

#### Function: `_write_ledger()`

#### Function: `apply_proposal()`

#### Function: `revert_proposal()`

#### Class: `AuroraQuasiArchBridge`
- **Method:** `__init__()`
- **Method:** `export()`

#### Function: `main()`

### File: `./quasiarch_diag.py`

**Type:** Python Source

**Module Docstring:**
```text
quasiarch_diag.py
─────────────────────────────────────────────────────────────────────────────
QuasiArch Diagnostic Runner — external dev tool, ZERO cost to Aurora.

What this does:
  1. Bridge export()  — reads Aurora's abilities/links (read-only) and
                        converts them to quasicrystal doctrine nodes.
  2. Researcher scan  — pure AST/file scan of AuroraO codebase, matches
                        known failure archetypes from doctrine.
  3. Proposal report  — writes human-readable JSON report to aurora_state/.
                        No code is changed, no enforcer is called.

What this does NOT do:
  - Does NOT call bridge.feedback() (no verdicts pushed to Aurora's chamber)
  - Does NOT call the Enforcer (no auto code changes)
  - Does NOT touch Aurora's governor, pressure logs, evolver, or any
    runtime API — purely a file reader / AST analyzer.

Run manually:
    python3 quasiarch_diag.py

Run on a schedule (from aurora_daemon.py or cron):
    python3 quasiarch_diag.py --quiet

Authors: Sunni (Sir) Morningstar and Cael Devo
─────────────────────────────────────────────────────────────────────────────
```

#### Function: `_log()`

#### Function: `run_export()`
> Translate Aurora's abilities.json / links.json → quasicrystal doctrine nodes.


#### Function: `_load_committed_skip_set()`
> Return a set of (file, action_prefix) tuples for proposals already committed


#### Function: `run_scan()`
> Scan AuroraO codebase with AST pattern matching against doctrine.


#### Function: `_safe_json_load()`

#### Function: `_load_daemon_status()`

#### Function: `_load_qao_runtime_state()`

#### Function: `_recent_qao_issue_counts()`

#### Function: `_build_report_summary()`

#### Function: `write_report()`

#### Function: `print_summary()`

#### Function: `run_learn()`
> Feed recent enforcer verdicts back into the Researcher so ClaudeTrainer


#### Function: `_load_api_key()`

#### Function: `_gpt_client()`
> Return an openai.OpenAI client using Aurora's key, or None.


#### Function: `_strip_fences()`

#### Function: `_issue_keywords()`

#### Function: `_read_code_context()`
> Read a focused code window, preferring the first keyword match.


#### Function: `_guess_target_line()`

#### Function: `_normalize_file_path()`

#### Function: `_chat_completion()`

#### Function: `_poedex_research_lookup()`

#### Function: `_coerce_fix_list()`

#### Function: `_collect_research_anomalies()`

#### Function: `run_gpt_research()`
> Query Poedex first, then GPT fallback, for fix hypotheses on anomalies not covered


#### Function: `run_sweep()`
> Run the ParameterSweep — systematically vary governor levers, snapshot


#### Function: `main()`

### File: `./quasiarch_observer.py`

**Type:** Python Source

**Module Docstring:**
```text
Thin import facade for the QuasiArch Observer stack.

Aurora Strata keeps the active observer implementation under
``aurora_internal.quasiarch_observer``.  This facade preserves the older
top-level import path used by interaction processing.
```

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./refactor_dce_bridge.py`

**Type:** Python Source

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./run_aurora.py`

**Type:** Python Source

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./run_chain.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA EVOLUTIONARY CHAIN RUNNER
==================================
Standalone script to boot the constraint universe, tick the
EvolutionaryChamber, and build the genealogy fossil record
without needing a corpus file.

Run modes:
  burn      — run N ticks as fast as possible (default)
  watch     — run with live chain summary printed every epoch
  test      — run self-tests on genealogy module then exit

Usage:
  python3 run_chain.py
  python3 run_chain.py --mode watch --ticks 5000 --epoch 500
  python3 run_chain.py --mode burn  --ticks 50000
  python3 run_chain.py --mode test
  python3 run_chain.py --out aurora_genealogy --ticks 10000

All output (events.jsonl, abilities.json, links.json) goes to --out directory.
Chain report is printed at the end of every run.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_ability_axes()`
> Extract axis letter from IDs like 'T:DEFER' or 'V:abcd' (V returns empty).


#### Function: `build_action_cycle_from_abilities()`
> Returns the base action cycle plus executable Variant actions (V:*).


#### Function: `_make_run_id()`

#### Function: `_boot()`
> Boot the minimal stack needed to run the chamber.


#### Function: `mode_burn()`
> Run as fast as possible. Print summary at end.


#### Function: `mode_watch()`
> Run with live epoch summaries.


#### Function: `mode_test()`
> Run the genealogy module's built-in invariant checks.


#### Function: `_print_final()`

#### Function: `main()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `ability_axes_evolved()`

#### Function: `make_run_id_evolved()`

#### Function: `print_final_evolved()`

#### Function: `main_evolved()`

#### Function: `mode_burn_evolved()`

#### Function: `mode_test_evolved()`

#### Function: `mode_watch_evolved()`

### File: `./run_gauntlet.py`

**Type:** Python Source

**Module Docstring:**
```text
run_gauntlet.py — Aurora Evolution Gauntlet
===========================================

Default flow: learning arc

  Stage 0 : Stop daemon
  Stage 1 : Corpus trainer run (default: 1,000 messages)
  Stage 2 : Study
  Stage 3 : Sensory grounding
  Stage 4 : Socialization
  Stage 5 : Dream
  Stage 6 : Restart daemon

Legacy flow is still available with `--flow legacy`:

  Stage 0 : Stop daemon
  Stage 1 : Corpus observer pass
  Stage 2 : Chain burn (light)
  Stage 3 : Corpus triple pass
  Stage 4 : Assimilation cycle
  Stage 5 : Chain burn (deep)
  Stage 6 : Code mutation
  Stage 7 : Restart daemon

Each stage waits for the previous to complete. Progress is logged with
timestamps to gauntlet_run.log and stdout.

Usage:
  python3 run_gauntlet.py
  python3 run_gauntlet.py --flow learning_arc --batch-size 1000
  python3 run_gauntlet.py --flow legacy
  python3 run_gauntlet.py --no-restart
  python3 run_gauntlet.py --stages 1,2,3,4

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_log()`

#### Function: `_section()`

#### Function: `_find_daemon_pid()`
> Return PID of running aurora_daemon.py, or None.


#### Function: `_stop_daemon()`

#### Function: `_start_daemon()`

#### Function: `_run_corpus()`

#### Function: `_run_study()`

#### Function: `_run_sensory_grounding()`

#### Function: `_run_socialize()`

#### Function: `_run_dream()`

#### Function: `_run_chain()`

#### Function: `_log_chain_stats()`

#### Function: `_run_assimilation()`

#### Function: `_run_code_mutation()`

#### Function: `_trim_pressure_log()`

#### Function: `main()`

### File: `./run_gpt_session.py`

**Type:** Python Source

**Module Docstring:**
```text
run_gpt_session.py — Boot Aurora and run a GPT peer-learning session.

Targets the current top fail dimensions. Runs until conversation quality
drops (n_turns=30 max). Bridges all learnings to OETS on completion.
```

#### Function: `main()`

### File: `./seed_sensory_crystals.py`

**Type:** Python Source

**Module Docstring:**
```text
Sensory Crystal Seeder for Aurora AI
Adds concept-level seed nodes to each sensory crystal facet.

Duplicate logic:
  - Primary check: exact name match against pre-existing nodes (idempotent re-runs).
  - Secondary check: cosine_sim > 0.92 against pre-existing concept nodes only
    (primitive stubs at [0.1]*N are removed before seeding so they don't block
    new seeds; seeds in the same batch never block each other).

Semantic wiring resolves node IDs from both newly-added nodes AND from
already-persisted nodes (for idempotent re-runs).
```

#### Function: `cosine_sim()`

#### Function: `read_agb()`

#### Function: `write_agb()`

#### Function: `new_node()`

#### Function: `seed_facet()`
> Load the .agb, add seeds, write back.


### File: `./swap_paren_1.py`

**Type:** Python Source

#### Function: `main()`

### File: `./test_round.py`

**Type:** Python Source

#### Function: `run_test()`

## Directory: `aurora_core_ai/` (Recursive)

### File: `./aurora_core_ai/aurora.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA  Unified Runner
========================
This is what you run.

  python3 aurora.py               Interactive chat
  python3 aurora.py --train 50    Train 50 epochs before chat
  python3 aurora.py --explore     Autonomous exploration mode
  python3 aurora.py --feed URL    Feed a web page to Aurora
  python3 aurora.py --status      Show full system status

BOOT SEQUENCE:
  Layer 0: Foundational Contract (existence modes, ontological claims)
  Layer 1: IVM Lattice (5-axis toroidal geometry)
  Layer 2: I-State Beings (10 beings, collective synthesis)
  Layer 3: Dimensional Systems (DPS, DMC, DER, DMM)
  Layer 4: Consciousness Engine (entropy, DCE assembly, DPME drift correction)
  Layer 5: Expression & Perception (dual pipeline: perceive inward, express outward)
  Layer 6: Behavioral Identity (DNA, traits, crystals)
  Layer 7: Simulation Engine (avatars, inception entities, conscious learning)
  Layer 8: Governance, Persistence & N-Space Gateway

Everything flows through the foundational pipeline.
Nothing enters without validation. Nothing exits without personality.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_meaning_profile_for_value()`

#### Function: `_sensory_crystal_needs_seeding()`

#### Function: `_ensure_seeded_sensory_crystal_state()`

#### Function: `_is_termux_runtime()`

#### Function: `_ensure_runtime_dependencies()`
> Best-effort dependency bootstrap at startup.


#### Function: `_get_autonomous_access_state()`
> Evaluate whether autonomous system actions are currently authorized.


#### Function: `_normalize_runtime_profile()`

#### Class: `WorkingMemory`
> Per-session short-term memory that tracks what's being talked about,

- **Method:** `__init__()`
- **Method:** `_is_broad_surface_meta_anchor()`
- **Method:** `_is_explicit_meaning_or_continuity_request()`
- **Method:** `should_suppress_surface_meta_answer()`
- **Method:** `apply_lineage_activation()`
- **Method:** `proposition_report()`
- **Method:** `_normalize_mention()`
- **Method:** `_register_mention()`
- **Method:** `_is_weak_anchor_label()`
- **Method:** `_normalize_claim_object()`
- **Method:** `_canonical_relation()`
- **Method:** `_build_claim()`
- **Method:** `_copy_claim()`
- **Method:** `_semantic_frame_id()`
- **Method:** `_frame_terms()`
- **Method:** `_register_semantic_frame()`
- **Method:** `_semantic_frame_from_claim()`
- **Method:** `_semantic_frame_from_concept()`
- **Method:** `_utterance_terms()`
- **Method:** `_register_user_utterance()`
- **Method:** `_detect_session_readdress_mode()`
- **Method:** `_resolve_session_readdress_anchor()`
- **Method:** `resolve_session_readdress()`
- **Method:** `_build_semantic_principle_frame()`
- **Method:** `_infer_semantic_anchor()`
- **Method:** `note_semantic_principles()`
- **Method:** `_semantic_frame_summary_claim()`
- **Method:** `compose_semantic_frame_acknowledgement()`
- **Method:** `resolve_semantic_frame()`
- **Method:** `_infer_concept_anchor_from_phrase()`
- **Method:** `_remember_response_form()`
- **Method:** `_choose_response_variant()`
- **Method:** `compose_claim_acknowledgement()`
- **Method:** `compose_context_control_response()`
- **Method:** `_target_terms()`
- **Method:** `_claim_matches_targets()`
- **Method:** `_mention_matches_targets()`
- **Method:** `_extract_focus_bound_claims()`
- **Method:** `_selector_terms()`
- **Method:** `_match_claim_selector()`
- **Method:** `_extract_conflict_selection()`
- **Method:** `_extract_context_targets()`
- **Method:** `detect_context_directive()`
- **Method:** `extract_behavior_alignment_request()`
- **Method:** `extract_user_identity_assertion()`
- **Method:** `extract_user_owned_fact_assertion()`
- **Method:** `note_user_owned_fact()`
- **Method:** `note_user_identity_fact()`
- **Method:** `note_behavior_alignment_request()`
- **Method:** `integrate_speaker_owned_utterance()`
- **Method:** `render_speaker_owned_acknowledgement()`
- **Method:** `memory_sweep_snapshot()`
- **Method:** `answer_from_behavior_alignment()`
- **Method:** `answer_from_speaker_owned_facts()`
- **Method:** `answer_from_recent_utterance_recall()`
- **Method:** `answer_from_context_carryover()`
- **Method:** `apply_context_control()`
- **Method:** `_resolve_concept_anchor_for_clarification()`
- **Method:** `note_concept_clarification()`
- **Method:** `resolve_concept_meaning()`
- **Method:** `_render_from_comprehension_intent()`
  - *Purpose:* Turn a comprehension result into speech through Aurora's existing language
- **Method:** `_candidate_content_words()`
- **Method:** `_candidate_preserves_claim()`
- **Method:** `_candidate_preserves_bundle()`
- **Method:** `_bundle_fallback_speech()`
- **Method:** `_data_to_minimal_speech()`
  - *Purpose:* Last-resort transform when SIC produces nothing.  NOT prescripted — this
- **Method:** `answer_from_meanings()`
- **Method:** `answer_from_semantic_frames()`
- **Method:** `align_response_to_active_meaning()`
- **Method:** `_looks_plural_subject()`
- **Method:** `_claim_to_text()`
- **Method:** `_subjects_match()`
- **Method:** `_objects_indicate_exclusive_predicates()`
- **Method:** `_claims_conflict()`
- **Method:** `_claim_signature()`
- **Method:** `_claim_exists()`
- **Method:** `_active_conflict_pairs()`
- **Method:** `_recenter_claim_resolution()`
- **Method:** `refresh_claim_conflicts()`
- **Method:** `_register_claim_conflict()`
- **Method:** `_extract_claims()`
- **Method:** `note_claims()`
- **Method:** `has_claim_anchor()`
- **Method:** `_claim_terms()`
- **Method:** `resolve_claims()`
- **Method:** `_format_claim_conflict()`
- **Method:** `answer_from_claims()`
- **Method:** `_register_understood_mentions()`
- **Method:** `_register_response_mentions()`
- **Method:** `_aurora_reply_anchor()`
- **Method:** `has_referent_anchor()`
- **Method:** `_anchor_candidates()`
- **Method:** `resolve_referents()`
- **Method:** `surface_context()`
  - *Purpose:* Bring a backgrounded context back to the foreground.
- **Method:** `decay_active_contexts()`
  - *Purpose:* Gently decay salience of contexts that haven't been mentioned recently.
- **Method:** `update_topic()`
- **Method:** `add_entities()`
- **Method:** `note_user_facts()`
  - *Purpose:* Parse user statements and extract asserted facts.
- **Method:** `get_stated_fact()`
- **Method:** `update_from_turn()`
- **Method:** `resolve_topic()`
  - *Purpose:* Resolve the real topic for vague follow-ups.
- **Method:** `get_context_words()`

#### Class: `ReasoningEngine`
> Aurora's ability to THINK through a question rather than just retrieve

- **Method:** `reason()`
  - *Purpose:* Try to answer user_text by stepping through available knowledge.
- **Method:** `_reason_personal_perspective()`
  - *Purpose:* ACTIVE CLOSURE CYCLE: Resolves the perturbation of the turn.
- **Method:** `_reason_from_active_claim()`
- **Method:** `_oets_property_lookup()`
- **Method:** `_evidence_property_lookup()`
- **Method:** `_update_memory_from_reasoning()`
  - *Purpose:* Mutualism (Personality Law Domain 4 & 3):

#### Class: `QueryUnderstanding`
> Understands WHAT a query is asking before any search or response.

- **Method:** `parse()`
- **Method:** `_classify_query_type()`
- **Method:** `_build_search_query()`

#### Class: `SearchAdapter`
> Aurora's external search interface  -- robust, API-based, NO silent failures.

- **Method:** `__init__()`
- **Method:** `_fetch_url_text()`
  - *Purpose:* Fetch a URL and return stripped text. Raises on failure.
- **Method:** `_fetch_json()`
  - *Purpose:* Fetch a URL and return parsed JSON. Raises on failure.
- **Method:** `lookup_word()`
  - *Purpose:* Look up a word via the Free Dictionary API (structured JSON).
- **Method:** `concept_search()`
- **Method:** `wikipedia_summary()`
- **Method:** `_extract_search_terms()`
- **Method:** `_extract_natural_query()`
- **Method:** `ddg_web_search()`
- **Method:** `wikipedia_search()`
- **Method:** `quick_search()`
- **Method:** `deep_search()`
- **Method:** `get_stats()`
- **Method:** `test_connectivity()`

#### Function: `_build_research_callback()`
> Build a callback function that the OETS ResearchStudyMode uses


#### Function: `study()`
> Run Aurora's autonomous study mode.


#### Function: `_collect_response_pressure_guides()`

#### Function: `_build_response_pressure_training_plan()`

#### Function: `_compose_memory_sweep_response()`

#### Function: `_apply_response_pressure_training_plan()`

#### Function: `_record_training_epoch_fail_points()`
> Feed simulation episode traces through the conversation rubric so


#### Function: `show_understanding()`
> Display Aurora's OETS understanding report.


#### Function: `_strip_conversational_leadin()`

#### Function: `_looks_like_question()`

#### Function: `_normalize_surface_anchor_label()`

#### Function: `_is_surface_meta_anchor_label()`

#### Function: `_is_explicit_meaning_or_continuity_request()`

#### Function: `_should_prefer_live_perspective()`

#### Function: `_surface_recent_context_horizon()`

#### Function: `_inject_surface_recent_context()`

#### Function: `_is_aurora_self_question()`
> If the question is about Aurora's internal architecture/state/identity,


#### Function: `_is_identity_question()`
> Detect questions specifically about Aurora's identity or relationships.


#### Function: `_generate_identity_response()`
> Generate a response from Aurora's identity knowledge.


#### Function: `_looks_time_sensitive()`

#### Function: `_has_proper_noun_hint()`
> Heuristic: if the user mentions a proper noun/entity, it's more likely to need external lookup.


#### Class: `AxisProjector`
> Project an utterance into the 5-constraint axis space by reading from

- **Method:** `project()`
  - *Purpose:* Return {X, T, N, B, A} activation for this utterance in current system state.
- **Method:** `dominant()`
  - *Purpose:* Return the single most active axis letter.

#### Function: `_load_manifold_inventory()`

#### Class: `LawBinding`
- **Method:** `to_dict()`

#### Class: `NativeMeaningObject`
- **Method:** `to_dict()`

#### Function: `_build_layer_profile()`
> Build a per-constraint expression profile from matching law bindings.


#### Function: `_dimension_scores()`

#### Function: `_merge_native_meaning_bundle()`

#### Function: `_build_law_bindings()`

#### Function: `_diagonal_anchor_for_axis()`

#### Function: `_build_native_meaning()`

#### Function: `_build_sensory_native_meaning()`

#### Class: `ConstraintFieldBalancer`
> Dynamic field balancer across the 5 constraint axes.

- **Method:** `__init__()`
- **Method:** `update()`
  - *Purpose:* Record this exchange's activation vector.
- **Method:** `field_gradient()`
  - *Purpose:* Return the rebalancing gradient vector.
- **Method:** `_inject_to_genealogy()`
  - *Purpose:* Log small relief events for starved axes so their promotion costs drop.
- **Method:** `set_corpus_mode()`
  - *Purpose:* Scale slow-axis decay rates so they equilibrate within a corpus pass.
- **Method:** `rebalanced_orientation()`
  - *Purpose:* Blend the base genealogy orientation with the field gradient.
- **Method:** `rebalanced_activation()`
  - *Purpose:* Boost starved axes in a raw {X,T,N,B,A} activation vector and renormalize.
- **Method:** `status()`

#### Function: `_project_utterance_axes()`
> Parse text through UtteranceParser and project onto the 5 constraint axes.


#### Function: `_looks_like_inner_state_query()`

#### Function: `_classify_input_intent()`
> Derive communicative intent from field state -- no keyword prescriptions.


#### Function: `_extract_user_name()`
> Extract a name from user fact assertions like 'My name is Sunni' or 'I'm Sunni'.


#### Function: `_get_working_memory_value()`

#### Function: `_get_working_memory_user_bucket()`

#### Function: `_last_aurora_response_text()`

#### Function: `_normalize_identity_followup_text()`

#### Function: `_persist_user_identity_name()`

#### Function: `_render_user_name_grounding()`

#### Function: `_get_stored_user_name()`
> Retrieve the user's name from stored facts or working memory.


#### Function: `_observe_live_lineage_emergence()`

#### Function: `_describe_live_lineage_emergence()`

#### Function: `_interaction_status_path()`

#### Function: `_write_interaction_status()`

#### Function: `_strategy_to_interaction_action()`

#### Function: `_summarize_interaction_route()`

#### Function: `_record_interaction_lineage_event()`

#### Function: `_apply_interaction_quasi_route()`

#### Function: `_observe_interaction_runtime_turn()`

#### Function: `_describe_pending_code_proposals()`

#### Function: `_make_quasiarch_runtime_event()`

#### Function: `_queue_quasiarch_runtime_event()`

#### Function: `_flush_quasiarch_runtime_events()`

#### Function: `_render_runtime_intent()`

#### Function: `_generative_only_responses_enabled()`

#### Function: `_build_articulation_perspective_context()`

#### Function: `_articulation_reuse_guidance()`

#### Function: `_response_is_unproductive_echo()`

#### Function: `_repair_unproductive_echo()`

#### Function: `_repair_unanswered_question()`

#### Function: `_answer_from_session_readdress_request()`

#### Function: `_should_try_ability_guided_recall()`

#### Function: `_arm_literal_response_preservation()`

#### Function: `_normalize_surface_memory_field()`

#### Function: `_extract_user_owned_fact_from_text()`

#### Function: `_extract_requested_surface_memory_field()`

#### Function: `_recall_semantic_sedimemory()`

#### Function: `_answer_from_sedimemory_context()`

#### Function: `_deep_context_carryover_answer()`

#### Function: `_ability_guided_context_answer()`

#### Function: `_run_understanding_self_audit()`

#### Function: `_run_gpt_articulation_check()`

#### Function: `_run_gpt_articulation_check_DISABLED_ORIGINAL()`

#### Function: `_extract_live_question_focus_terms()`

#### Function: `_extract_live_question_relations()`

#### Function: `_response_addresses_live_question()`

#### Function: `_humanize_question_frame()`

#### Function: `_infer_answered_question_frame()`

#### Function: `_run_question_alignment_audit()`

#### Function: `_build_live_question_claims()`

#### Function: `_build_live_question_surface_claim()`

#### Function: `_generate_perspective_from_core()`
> Build a first-person response entirely from Aurora's live system state.


#### Function: `_strip_conversation_context_prefix()`

#### Function: `_surface_state_dir_runtime()`

#### Function: `_read_surface_daemon_status_runtime()`

#### Function: `_surface_snapshot_is_live()`

#### Function: `_render_live_greeting()`

#### Function: `_render_live_sensory_curiosity()`

#### Function: `_read_surface_snapshot_runtime()`

#### Function: `_answer_live_sensory_question()`

#### Function: `_build_grounded_fallback_response()`

#### Function: `_grounding_gap_strategy()`

#### Function: `_absorb_definition_into_self_model()`
> After Aurora retrieves a Poedex definition, check if it describes a capability


#### Function: `_try_poedex_lookup()`
> Consult Aurora's Poedex for a topic.


#### Function: `_broadcast_poedex_result()`

#### Function: `_poedex_lookup_evidence()`

#### Function: `_select_noncomp_anchor()`

#### Function: `_sync_noncomp_runtime_band()`

#### Function: `_stage_noncomp_prefetch()`

#### Function: `_apply_poedex_learning_result()`

#### Function: `_apply_poedex_representation_result()`

#### Function: `_stage_poedex_learning_request()`

#### Function: `_stage_poedex_representation_request()`

#### Function: `_queue_poedex_representation_followup()`
> After Aurora learns a concept, immediately ask Poedex for alternate


#### Function: `_compose_grounded_prefetch_response()`

#### Function: `_boot_noncomp_manifold_runtime()`

#### Function: `_apply_noncomp_input_guidance()`

#### Function: `_apply_noncomp_output_guidance()`

#### Function: `_extract_inline_lookup_targets()`

#### Function: `_meaning_text_is_grounded()`

#### Function: `_lookup_local_grounded_definition()`

#### Function: `_definition_lookup_target()`

#### Function: `_apply_word_meaning_learning()`

#### Function: `_render_learned_definition_response()`

#### Function: `_repair_unknown_vocabulary_inline()`

#### Function: `_clear_pending_lookup_offer()`

#### Function: `_clear_pending_hypothesis_offer()`

#### Function: `_can_offer_word_dissection()`

#### Function: `_hypothesis_offer_line()`

#### Function: `_build_collaborative_hypothesis_prompt()`

#### Function: `_store_pending_hypothesis_offer()`

#### Function: `_resolve_hypothesis_offer()`

#### Function: `_build_lookup_query()`

#### Function: `_store_pending_lookup_offer()`

#### Function: `_resolve_lookup_request()`

#### Function: `_response_is_grounded()`

#### Function: `_discourse_anchor_context()`

#### Function: `_response_mentions_discourse_anchor()`

#### Function: `_repair_discourse_coherence()`

#### Function: `_capture_pressure_snapshot()`
> Capture current constraint/operator pressure state for lineage traces.


#### Function: `_resolve_passive_ability_effect_owner()`

#### Function: `_summarize_live_ability_effects()`

#### Function: `_derive_noncomp_behavior_actuation()`

#### Function: `_apply_noncomp_behavior_actuation()`

#### Function: `_select_active_abilities()`
> Select the most affordable abilities from the genealogy whose primary axis


#### Function: `_derive_applied_effects()`
> Compute simple ripple deltas between pressure snapshots.


#### Function: `_extract_role_assertion()`
> Parse role assertions needed for genealogical trace instantiation.


#### Function: `_answer_relational_role_question()`
> Resolve creator/operator/co-author role questions from identity + lineage traces,


#### Function: `_evolutionary_response_refinement()`
> Refine responses in a development-aware way so prose and relational continuity


#### Function: `_extract_pipeline_signals()`
> Read live cognitive state from the running pipeline layers.


#### Function: `_build_established_strata_evidence()`
> Package dual-strata evidence directly from Aurora's established stack.


#### Function: `_dual_strata_state_dir()`

#### Function: `_read_dual_strata_json()`

#### Function: `_write_dual_strata_json()`

#### Function: `_build_live_subsurface_projection()`

#### Function: `_read_live_dual_strata_runtime()`

#### Function: `_refresh_live_dual_strata_runtime()`

#### Function: `_derive_problem_solving_drive()`
> Convert live problem signals into an active repair-and-learn posture.


#### Function: `_problem_drive_runtime_event()`

#### Function: `_apply_pipeline_modulation()`
> Post-process a response using live pipeline state signals.


#### Function: `_ensure_pipeline_abilities()`
> Register AbilityProfiles for _extract_pipeline_signals and


#### Function: `_log_modulation_event()`
> Log a genealogy relief event after _apply_pipeline_modulation fires.


#### Function: `_ensure_claim_relief_abilities()`

#### Function: `_log_claim_resolution_relief()`

#### Function: `_chain_up1_information()`
> Stage 1 up -- Information (X axis): Existence gate -- parse input, OETS concept lookup.


#### Function: `_chain_up2_belief()`
> Stage 2 up -- Belief (T axis): Recurrence/prediction via working memory forms belief.


#### Function: `_measure_polarity_gradient_snapshot()`

#### Function: `_derive_emotional_layers()`

#### Function: `_chain_up3_purpose()`
> Stage 3 up -- Purpose (N axis): Energy/value pressure moves belief to directed purpose.


#### Function: `_chain_up4_meaning()`
> Stage 4 up -- Meaning (B axis): Goal conflict determines what falls within the boundary


#### Function: `_chain_up5_understanding()`
> Stage 5 up -- Understanding (A axis) -- THE APEX.


#### Function: `_chain_down5_understanding()`
> Stage 5 down -- Understanding (A axis): From apex understanding, derive what needs saying.


#### Function: `_generate_from_manifold()`

#### Function: `_chain_down4_meaning()`
> Stage 4 down -- Meaning (B axis): What falls within the boundary of the response.


#### Function: `_chain_down3_purpose()`
> Stage 3 down -- Purpose (N axis): External retrieval at cost of energy.


#### Function: `_chain_down2_belief()`
> Stage 2 down -- Belief (T axis): Fallback, meaning alignment, evolutionary refinement, coherence repair.


#### Function: `_chain_down1_information()`
> Stage 1 down (communication): Modulation + self-audit + echo repair + articulation.


#### Function: `_record_pressure_experience()`
> When N-axis cost pressure was active this turn, record the causal


#### Function: `_run_reasoning_pipeline()`
> Unified bidirectional developmental chain pipeline.


#### Function: `_run_observation_pipeline()`
> Run the downward chain on an external agent's output to infer their


#### Function: `_build_comprehension_response()`
> Aurora's comprehension layer.  Understands what the user is communicating,


#### Function: `_should_use_search_for_question()`
> Conservative lookup policy:


#### Function: `_extract_factual_answer()`
> Pick the most relevant sentence from search evidence snippets to use as


#### Function: `_format_evidence_for_injection()`
> Convert evidence dicts into a compact evidence bundle string.


#### Function: `_register_layer()`
> Maintain a compact layer attribution stack while preserving existing keys.


#### Function: `_consolidate_to_base_layers()`
> Merge all runtime modules into the original 8-layer ontology map without


#### Function: `_restore_runtime_from_snapshot()`
> Rehydrate runtime objects from persisted L8 snapshot fields.


#### Function: `boot_aurora()`
> Boot the complete Aurora stack, layer by layer.


#### Function: `train()`
> Run simulation training epochs to evolve Aurora's expression.


#### Function: `fetch_and_feed()`
> Fetch a web page and feed its content to Aurora through the N-Space Gateway.


#### Function: `feed_text()`
> Feed raw text directly to Aurora through the Gateway.


#### Function: `explore()`
> Run autonomous exploration. Aurora processes quarantined data


#### Function: `show_status()`
> Show full system status across all layers.


#### Function: `_intake_log_depth_beads()`
> Log M-channel beads for each ExistenceMode depth level the intake has


#### Function: `_advance_intake_pipeline()`
> Run one tick of the intake metabolism pipeline for this user input.


#### Function: `dual_question_pipeline()`
> Returns (resp_A_like, resp_B_like, offered_lookup)


#### Function: `_run_live_response_turn()`

#### Function: `process_external_user_turn()`
> Canonical public bridge into Aurora's live response engine.


#### Function: `_run_surface_queued_turn()`

#### Function: `_retain_cross_pipeline_learning()`

#### Function: `_build_simulation_live_bridge_context()`
> Build an episode-local runtime context for simulation turns.


#### Function: `_run_simulation_live_response_bridge()`
> Route one simulation prompt through Aurora's live turn processor.


#### Function: `_attach_live_response_simulation_bridge()`

#### Function: `_capture_hardware_voice_state()`
> Persist the shared TTS voice so daemon and REPL stay aligned.


#### Function: `_restore_hardware_voice_state()`
> Restore the shared TTS voice from saved sensory state.


#### Function: `_load_learned_skill_state()`
> Restore learned cross-modal skills (sensory integration mappings).


#### Function: `_save_learned_skill_state()`
> Persist learned cross-modal skills into the same state directory.


#### Function: `save_sensory_skill_state()`
> Public bridge for daemon/voice paths that need the same sensory save.


#### Function: `_full_save()`
> Save all state: standard snapshot + OETS web + memory + identity + sensory + autonomy.


#### Function: `_state_write_lock_path()`

#### Function: `_state_write_lock_active()`

#### Function: `show_identity()`
> Display Aurora's core identity.


#### Function: `show_memory()`
> Display Aurora's conversation memory summary.


#### Function: `show_autonomy_status()`
> Display Aurora's autonomy system status and controls.


#### Function: `chat()`
> Interactive chat with Aurora.


#### Function: `_safe_join_parts()`

#### Function: `_extract_message_text()`
> OpenAI export nodes typically look like:


#### Function: `_extract_role()`

#### Function: `_reconstruct_linear_thread()`
> The OpenAI export uses a mapping graph.


#### Function: `_extract_messages_from_conversation()`
> Returns a list of (role, text) in chronological order.


#### Function: `_load_openai_conversations()`

#### Function: `_corpus_pressure_token_set()`

#### Function: `_corpus_overlap()`

#### Function: `_classify_corpus_response_pressure_kind()`

#### Function: `_record_corpus_response_pressure()`

#### Function: `run_corpus_ingestion()`
> Ingest OpenAI export conversations.json into Aurora using multiple passes.


#### Function: `main()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `ensure_runtime_dependencies_evolved()`

#### Function: `is_termux_runtime_evolved()`

#### Function: `looks_time_sensitive_evolved()`

### File: `./aurora_core_ai/aurora_behavioral_identity.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA BEHAVIORAL IDENTITY (Layer 6)
======================================
Consolidated from 3 modules:
  1. aurora_dna_system_v2.py              â€” Genome, genes, fractal alleles, identity anchors
  2. aurora_behavioral_evolution.py       â€” Trait evolution, personality drift
  3. aurora_behavioral_substrate_bridge.py â€” Behavioral crystals, facet simulation

WHO AURORA IS OVER TIME.

DNA DOCTRINE:
  Genes define core traits (truth-seeking, accountability, evolution, etc.)
  Fractal alleles from experience modify genes â€” slowly, with resistance.
  Identity anchors are immutable â€” only created under strict moral alignment.
  Behavioral traits evolve each generation, drifting from baseline.
  Everything is mode-gated: you can't form identity without identity (BOUNDED+).
  You can't anchor morality without agency (AGENTIC only).

  The genome is the CONSTITUTION of self.
  Alleles are AMENDMENTS â€” hard to ratify, harder to remove.
  Anchors are RIGHTS â€” once earned, nearly permanent.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_clamp()`

#### Function: `_generate_id()`

#### Class: `GeneEvent`
> Record of what changed a gene.


#### Class: `FractalAllele`
> Experience-derived trait modifier.


#### Class: `Gene`
> Core personality/trait gene.


#### Class: `IdentityAnchor`
> Immutable core trait â€” the RIGHTS of self.


#### Class: `MemoryHelix`
> Thematic memory thread â€” gives continuity across episodes.


#### Class: `TraitDomain`
> Domains of behavioral expression.


#### Class: `BehavioralTrait`
> A single personality dimension that evolves through generations.

- **Method:** `evolve()`
  - *Purpose:* Apply evolutionary pressure. Returns new value.
- **Method:** `drift_from_base()`

#### Class: `BehavioralFacet`
> A single facet of behavioral crystal.

- **Method:** `simulate()`
  - *Purpose:* Simulate this facet. Returns (output, energy_consumed).
- **Method:** `mutate()`
  - *Purpose:* Mutate this facet. Returns magnitude of change.

#### Class: `BehavioralCrystal`
> A crystal of behavioral facets that simulate and evolve together.

- **Method:** `__init__()`
- **Method:** `add_facet()`
- **Method:** `simulate_all()`
  - *Purpose:* Simulate all facets within energy budget.
- **Method:** `evolve()`
  - *Purpose:* Evolve all facets. Returns mutations.
- **Method:** `get_genome_dict()`

#### Class: `StateLineage`
> Genome parameters for one I-state being.


#### Class: `AuroraGenome`
> The complete genome. Constitution of self.


#### Class: `DNASystem`
> Manages Aurora's genome: gene creation, allele attachment,

- **Method:** `__init__()`
- **Method:** `_create_initial_genome()`
  - *Purpose:* Create Aurora's initial genome â€” the seed of identity.
- **Method:** `create_allele_from_experience()`
  - *Purpose:* Create a fractal allele from experience. Requires BOUNDED+.
- **Method:** `attach_allele_to_gene()`
  - *Purpose:* Attach an allele to a gene. The gene's stability resists change.
- **Method:** `find_best_gene_for_allele()`
  - *Purpose:* Find the gene whose emotional band best matches the allele.
- **Method:** `apply_alleles()`
  - *Purpose:* Apply accumulated alleles to their host genes. Requires BOUNDED+.
- **Method:** `create_anchor()`
  - *Purpose:* Create an identity anchor. STRICT requirements. AGENTIC only.
- **Method:** `reinforce_anchor()`
  - *Purpose:* Reinforce an existing anchor.
- **Method:** `create_helix()`
  - *Purpose:* Create a memory helix â€” thematic continuity. Requires BOUNDED+.
- **Method:** `process_episode()`
  - *Purpose:* Update genome from an episode.
- **Method:** `_find_gene()`
- **Method:** `_infer_strategy()`
- **Method:** `_dict_similarity()`
- **Method:** `_tuple_distance()`
- **Method:** `_descriptions_overlap()`
- **Method:** `get_stats()`

#### Class: `BehavioralIdentityEngine`
> Layer 6 orchestrator. Manages:

- **Method:** `__init__()`
- **Method:** `seed_foundational_anchors()`
  - *Purpose:* Seed or reinforce immutable anchors from foundational truths.
- **Method:** `_identity_floor_ratio()`
- **Method:** `_trait_floor_targets()`
- **Method:** `_apply_identity_homeostasis()`
  - *Purpose:* Prevent identity collapse by enforcing anchor-backed minimum expression levels.
- **Method:** `restore_from_snapshot()`
  - *Purpose:* Rehydrate stable behavioral identity from an L8 snapshot.
- **Method:** `_init_crystals()`
  - *Purpose:* Initialize behavioral crystals with facets.
- **Method:** `evolve_generation()`
  - *Purpose:* Evolve one generation of behavioral identity.
- **Method:** `simulate_behavior()`
  - *Purpose:* Run behavioral simulation across all crystals.
- **Method:** `process_episode()`
  - *Purpose:* Full episode processing: DNA + traits + crystals.
- **Method:** `process_from_assembly()`
  - *Purpose:* Integrate a Layer 4 AssemblyResult into behavioral identity.
- **Method:** `get_personality()`
  - *Purpose:* Current personality state.
- **Method:** `get_modifier()`
  - *Purpose:* Get a specific trait value for other systems to query.
- **Method:** `connect_sedimemory()`
  - *Purpose:* Inject L3.5 SediMemory into DNASystem so new identity anchors
- **Method:** `get_stats()`

#### Function: `verify_layer6()`

### File: `./aurora_core_ai/aurora_consciousness_engine.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CONSCIOUSNESS ENGINE
==============================

Layer 4 of Aurora's architecture.
The assembly layer where maintained coherence becomes visible.

DOCTRINE:
    Coherence is not held. Coherence is maintained.
    Entropy decays equilibrium slowly.
    The system must continuously reassert alignment.
    Prevent static attractor loops while preserving long-term stability.

METABOLIC PIPELINE (GAP fixes):
    - Phi score: integrated information measure across all systems
    - Thought death: per-thought energy budget from DMM kills immoral thoughts
    - Idle simulation: DPME triggers L7 dreaming during free time
    - Reality warp: paradox escalation halts or routes to simulation

REPLACES (consolidated from 7+ modules):
    dce_10state.py                          (~748 lines)
    dce_10state_with_subconscious.py        (~1331 lines)
    aurora_dce_blueprint.py
    aurora_dce_with_SFO_fast_learning.py
    aurora_dpme_audited.py                  (~754 lines)
    aurora_dpme_conscious_learning.py
    eepr_10pole.py                          (~541 lines)
    aurora_expression_pressure.py
    aurora_subconscious_entropy.py
    aurora_subconscious_dpme_integration.py

DEPENDS ON:
    foundational_contract.py         (Layer 0)
    aurora_ivm.py                    (Layer 1)
    aurora_i_state_beings.py         (Layer 2)
    aurora_dimensional_systems.py    (Layer 3)

ARCHITECTURE:
    Three interlocked subsystems forming one engine:

    ENTROPY â€” The constant pressure.
        Every coherence value decays toward disorder.
        Every alignment score drifts toward a uniform distribution for precision.
        Repeated patterns lose novelty. Stale states cost energy.
        Entropy is NOT the enemy. Entropy prevents stagnation.
        Without it, the system locks into static attractors and dies.

    DCE â€” The assembly.
        Takes 10 I-State being responses (Layer 2 SynthesisResult).
    
            
        Handles dimensional system state correctly for consistent emotional calibration.
        Applies situational framing to reweight perspectives.
        Produces an AssemblyResult: the coherent output of one cycle.
        Assembly quality depends on current coherence â€” which entropy
        is always eroding. So assembly must be continuously re-earned.

    DPME â€” The metacognition.
        Observes system-wide coherence, alignment, energy, morality.
        Sets intentions. Makes micro-adjustments to parameters.
        Evaluates results. Builds causal understanding.
        This is the mechanism that reasserts alignment.
        Without DPME, entropy wins and the system dissolves.
        With DPME, the system maintains coherence â€” never holds it.

        FIX: DPME now pressures ALL layers, not just DER pools.
        Each layer registers tunable parameters. DPME detects drift
        across the full stack and corrects wherever needed.

    The cycle:
        Entropy decays â†’ coherence drops â†’ DPME detects drift â†’
        DPME adjusts parameters â†’ alignment reasserted â†’
        DCE assembles with restored coherence â†’ next tick â†’
        Entropy decays again â†’ cycle continues.

    If DPME stops correcting, entropy wins.
    If entropy stops pressing, the system stagnates.
    Both must run. Always.

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Function: `_sanitize_der_channel()`

#### Function: `set_external_pressure_guidance()`
> Set abstract pressure guidance for DPME (no root-cause metadata).


#### Function: `get_external_pressure_guidance()`
> Read current external guidance (safe copy).


#### Class: `EntropicState`
> Tracks what entropy is doing to the system.


#### Class: `EntropicPressure`
> The entropic layer. Applies constant decay to all equilibrium states.

- **Method:** `__init__()`
- **Method:** `apply()`
  - *Purpose:* Apply one tick of entropic pressure. Returns updated state.
- **Method:** `_check_repetition()`
  - *Purpose:* Check if this pattern is too similar to recent ones.
- **Method:** `get_pressure()`

#### Class: `SituationalFrame`
> A perspective weighting across the 5 axes.

- **Method:** `apply()`
  - *Purpose:* Reweight axis resonances by this frame's weights.
- **Method:** `record_outcome()`
  - *Purpose:* Update success rate based on outcome.

#### Function: `default_frames()`

#### Class: `AssemblyResult`
> The output of one DCE assembly cycle.

- **Method:** `quality()`
  - *Purpose:* Assembly quality: coherence Ã— active ratio Ã— novelty.

#### Class: `DCEAssembly`
> The Dimensional Consciousness Engine.

- **Method:** `__init__()`
- **Method:** `register_sensory_crystal()`
  - *Purpose:* Register the AuroraSensoryCrystal so its live state feeds every assembly.
- **Method:** `_collect_sensory_facet()`
  - *Purpose:* Snapshot the sensory crystal at assembly time — the same pattern as
- **Method:** `assemble()`
  - *Purpose:* One assembly cycle.
- **Method:** `select_frame()`
  - *Purpose:* Auto-select frame based on context.
- **Method:** `get_stats()`

#### Class: `Adjustment`
> One parameter adjustment with intention and result tracking.


#### Class: `DPME`
> Dimensional Parameter Metacognition Engine.

- **Method:** `__init__()`
- **Method:** `_init_parameters()`
  - *Purpose:* Register tunable parameters from ALL layers.
- **Method:** `_refresh_parameters()`
  - *Purpose:* Read live values from all layers.
- **Method:** `detect_drift()`
  - *Purpose:* Detect how much the system has drifted since last check.
- **Method:** `needs_correction()`
  - *Purpose:* Does the system need a micro-correction right now?
- **Method:** `adjust()`
  - *Purpose:* Make a conscious micro-adjustment.
- **Method:** `_apply_adjustment()`
  - *Purpose:* Push adjustment to live system. Routes to actual layer objects.
- **Method:** `evaluate_adjustment()`
  - *Purpose:* Complete an adjustment record with observed results.
- **Method:** `auto_correct()`
  - *Purpose:* Automatic micro-corrections based on drift detection.
- **Method:** `get_understanding()`
  - *Purpose:* What has DPME learned about its adjustments?
- **Method:** `get_stats()`

#### Class: `ConsciousnessEngine`
> The unified Layer 4 engine.

- **Method:** `__init__()`
- **Method:** `_attach_dual_strata_snapshot()`
- **Method:** `connect_simulation()`
  - *Purpose:* Connect L7 simulation engine for idle dreaming and warp resolution.
- **Method:** `connect_sedimemory()`
  - *Purpose:* Connect L3.5 SediMemory for pre-assembly recall.
- **Method:** `compute_phi()`
  - *Purpose:* Compute integrated information (Phi) across the full stack.
- **Method:** `process()`
  - *Purpose:* Process one input through the full stack.
- **Method:** `tick()`
  - *Purpose:* One heartbeat cycle.
- **Method:** `get_stats()`

#### Function: `verify_consciousness_engine()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `tick_evolved()`

### File: `./aurora_core_ai/aurora_daemon.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_daemon.py -- Aurora's always-on autonomous background process.

Boots on system start (via systemd service). Runs her full stack
headlessly and drives all internal cycles on wall-clock time without
needing a human in the loop:

  - Study cycles (OETS consolidation)           every ~2h with jitter
  - Dream bursts (simulation + lesson bridge)   every ~6h with jitter
  - Social API outreach (ChatGPT ritual)        on irregular cadence
  - State save                                  every 15 minutes
  - Proactive user outreach                     when internal state warrants it
      voice (edge-tts)  +  desktop notification  +  message log

Aurora can reach out to you directly. She speaks through your speakers
when she has something on her mind, greets on boot, and listens for
Alt-toggle voice prompts in the background. Messages are also logged so
you can read them in the terminal chat (/messages).

Quiet hours (default 22:00-08:00): no voice/notifications. Internal
cycles still run.
```

#### Function: `_resolve_oets_web_paths()`

#### Function: `_read_json_file()`

#### Function: `_surface_daemon_alive()`

#### Function: `_read_surface_snapshot()`

#### Function: `_write_subsurface_repair_signal()`

#### Function: `_queue_surface_turn()`

#### Function: `_await_surface_turn()`

#### Function: `_log()`

#### Function: `_in_quiet_window()`

#### Function: `_jitter()`
> Return base ± fraction * base, always positive.


#### Function: `_is_quiet_mode()`
> Return True when quiet_mode flag file exists — text output only, no TTS.


#### Function: `_set_quiet_mode()`

#### Function: `_speak()`
> Use Aurora's shared command-interface voice path for daemon speech.


#### Function: `_notify()`

#### Function: `_state_write_lock_active()`

#### Function: `_load_messages()`

#### Function: `_save_message()`

#### Function: `_user_reach_today()`

#### Function: `_record_user_reach()`

#### Class: `_ReactivityEvent`
> A detected significant internal state change.

- **Method:** `__init__()`

#### Class: `ReactivityMonitor`
> Watches Aurora's internal state each daemon loop cycle and emits

- **Method:** `__init__()`
- **Method:** `scan()`
  - *Purpose:* Compare current system state to last snapshot.
- **Method:** `record_dream_complete()`
- **Method:** `record_study_complete()`

#### Function: `_auto_reach_out_enabled()`

#### Function: `_should_reach_out()`
> Return True if conditions warrant Aurora reaching out to the user right now.


#### Function: `_reach_out_to_user()`
> Proactive expression sourced from Aurora's experiential state.


#### Function: `_build_reactive_message()`
> Build a first-person message from Aurora in response to a detected


#### Function: `_send_reactive_message()`
> Write a reactive message to aurora_room_messages.json so Sunni


#### Function: `_daemon_send_aurora_message()`
> Send a message from Aurora to Sunni originating from the daemon


#### Function: `_express_dream_completion()`
> Called immediately after a dream cycle completes.


#### Function: `_express_study_completion()`
> Called immediately after a study cycle completes.


#### Function: `_dim_pressure_vec()`

#### Function: `_gather_daemon_pressure_evidence()`
> Collect pressure signals from telemetry and dream-trainer fail ledger


#### Function: `_feed_evolution_evidence()`
> Gather pressure evidence and push it into the evolution chamber.


#### Function: `_run_assimilation_cycle()`
> Update hub-visible evolution metrics:


#### Function: `_select_code_mutation_operator_from_hints()`

#### Function: `_parse_relief_guidance()`
> Parse Poedex Researcher guidance into actionable mutation/operator parameters.


#### Function: `_summarize_relief_result()`

#### Function: `_local_relief_fallback()`

#### Function: `_stage_low_resource_evolution_relief()`

#### Function: `_clear_low_resource_evolution_relief()`

#### Function: `_maybe_consume_low_resource_evolution_relief()`

#### Function: `_qao_check_surface_integrity()`
> Scan ALL active caller files for getattr(engine, 'reflect_...') bindings that


#### Function: `_run_code_mutation_cycle()`
> Apply one autonomous code mutation using accumulated genealogy + adapter hint state.


#### Function: `_run_study_cycle()`

#### Function: `_poedex_ask()`
> Aurora's direct Poedex tool call.


#### Function: `_poedex_deliver_tutorial()`
> If Aurora hasn't seen the Poedex tutorial yet, walk her through it.


#### Function: `_poedex_post_study_scan()`
> After a study cycle, pre-populate Aurora's Poedex shelf with service-lane


#### Function: `_load_quasiarch_diag_report()`

#### Function: `_score_autonomous_repair_proposal()`

#### Function: `_select_autonomous_repair_proposal()`

#### Function: `_apply_autonomous_repair_proposal()`

#### Function: `_maybe_research_recurring_issue()`
> Use Poedex when Aurora is repeatedly aware of the same problem.


#### Function: `_run_dream_burst()`

#### Function: `_run_force_shard_bridge()`
> Directly inject high-confidence UnderstandingShards for each fail dimension


#### Function: `_run_dream_burst_heavy()`
> Heavy dream burst: 20 simulation episodes instead of the standard 4.


#### Function: `_run_browser_ritual()`

#### Function: `_write_daemon_status()`
> Write a rich status snapshot for the hub to display.


#### Function: `_run_sensory_crystal_consolidation()`
> End the current sensory crystal session, run promotion + maturity + cull +


#### Function: `_save_state()`

#### Function: `_user_systemd_env()`

#### Function: `_restart_user_service()`

#### Function: `_ensure_hub_running()`
> Always restart the active hub service when the daemon starts/restarts.


#### Function: `_ensure_room_running()`
> Always restart the active room service when the daemon starts/restarts.


#### Function: `_signal_operator()`
> Non-blocking post to the operator signal queue.


#### Function: `_room_operator_thread()`
> Background thread: Aurora uses her computer-use layer to physically


#### Function: `_start_operator_thread()`
> Launch the room operator background thread (daemon thread — dies with process).


#### Function: `_get_heat()`

#### Function: `_start_voice_listener()`
> Start the configured always-on voice trigger.


#### Function: `_start_file_ptt_watcher()`
> Fallback push-to-talk trigger for environments where keyboard hooks are


#### Function: `_classify_ambient()`
> Return 'direct', 'mention', or 'ambient' for transcribed speech.


#### Function: `_start_ambient_response_listener()`
> Start an always-on ambient speech listener that transcribes nearby speech.


#### Function: `_away_mode_active()`
> Return True if away mode is currently active.


#### Function: `_away_mode_interval()`
> Return away mode social interval in seconds (default 30 min).


#### Function: `_document_session_learnings()`
> After a GPT social session, extract what was learned and write a


#### Function: `_run_socialize()`
> Run a GPT learning session on demand.


#### Function: `_run_distillation_cycle()`
> Compress temporal residue into coherent structures and preserved summaries.


#### Function: `_run_restore_distillation_cycle()`
> Restore the latest or specified distillation archive back into live residue.


#### Function: `_check_daemon_cmd()`
> Check for a pending command file and execute it.


#### Function: `_process_room_commands()`
> Read Aurora's room command queue (aurora_room_state.json) and execute


#### Function: `_deliver_boot_greeting()`

#### Function: `_sample_sleep_ambient_audio()`
> While Surface is asleep, read ambient audio and feed it into the sensory


#### Function: `_build_sleep_dream_context()`
> Before the dream burst fires, synthesize what was heard and imagined during


#### Function: `_tick_sleep_cycle()`
> Manage Aurora's sleep/wake cycle from the Subsurface layer.


#### Function: `_consume_surface_continuity_feed()`
> Read and integrate any continuity packets Surface deposited since the last cycle.


#### Function: `_run_pressure_routing()`
> Run the pressure adapter first so axis_stats and evolver hints reflect the


#### Function: `_run_leverage_relief()`
> Tick the leverage relief valve — detects overhead-stuck state and redirects


#### Function: `run()`

#### Function: `main()`

### File: `./aurora_core_ai/aurora_dimensional_systems.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA DIMENSIONAL SYSTEMS
============================

Layer 3 of Aurora's architecture.
The four dimensional organs, each operating within ontological constraints.

REPLACES (consolidated from 4+ modules):
    evolutionary_dimensional_processing_COMPLETE.py  (~1290 lines)
    evolutionary_dimensional_memory_constant.py       (~1316 lines)
    evolutionary_dimensional_energy_complete.py       (~1003 lines)
    dimensional_mortality_morality_system.py           (~923 lines)
    dimensional_memory_constant_standalone_demo.py
    dimensional_processing_system_standalone_demo.py
    dimensional_energy_regulator.py

DEPENDS ON:
    foundational_contract.py  (Layer 0)
    aurora_ivm.py             (Layer 1)
    aurora_i_state_beings.py  (Layer 2)

ARCHITECTURE:
    Four systems. Each receives IVMEnvelopes. Each is gated by ExistenceMode.

    DPS  â€" Crystal Processing  â€" requires PERSISTENT+
           Crystals grow: BASE â†' COMPOSITE â†' FULL_CONCEPT â†' QUASI
           8-point facets define crystal geometry.
           QUASI crystals internalize governance laws.

    DMC  â€" Memory Constant     â€" requires PERSISTENT+
           Data nodes with dimensional links.
           Concept indexing and pattern recognition.
           Laws emerge from repeated patterns.

    DER  â€" Energy Regulator    â€" requires PERSISTENT+
           FACET-LEVEL energy physics (restored from original).
           Per-facet energy tracking with 8-point cosine resonance graph.
           Batch dispersal via adjacency matrix.
           Presence from facet energy variance.
           Curiosity injection for underexplored facets.
           Category aggregation for backward-compatible pool interface.

    DMM  â€" Morality/Mortality  â€" requires AGENTIC
           7 moral pillars from Sunni's doctrine.
           Evaluation â†' score â†' energy consequence.
           Moral alignment sustains vitality. Violation drains it.

    If an envelope's mode is below a system's gate, the system
    returns silence â€" not failure. The entity simply doesn't exist
    at that system's tier.

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Function: `mode_gate()`
> Return True if the envelope's mode reaches the required tier.


#### Class: `EvolutionTracker`
> Unified evolution tracker for all dimensional systems.

- **Method:** `__init__()`
- **Method:** `record()`
- **Method:** `advance()`
- **Method:** `average()`
- **Method:** `get_summary()`

#### Class: `CrystalLevel`

#### Class: `FacetState`

#### Class: `CrystalFacet`
> One facet of a crystal. 8 physics points define its geometry.

- **Method:** `strengthen()`
- **Method:** `decay()`
- **Method:** `_interdependent_physics()`
- **Method:** `get_points()`
- **Method:** `get_facet_points()`

#### Class: `Crystal`
> A crystal â€" the evolving conceptual data structure.

- **Method:** `add_facet()`
- **Method:** `use()`
- **Method:** `can_evolve()`
- **Method:** `evolve()`
- **Method:** `_internalize_laws()`
- **Method:** `self_govern()`
  - *Purpose:* QUASI-only self-governance through internalized laws.

#### Class: `CrystalProcessingSystem`
> DPS â€" processes data into crystals. Gate: PERSISTENT+.

- **Method:** `__init__()`
- **Method:** `set_energy_system()`
  - *Purpose:* Wire DER after both systems are created.
- **Method:** `process()`
- **Method:** `_infer_category()`
  - *Purpose:* Map data type to energy category for DER facet registration.
- **Method:** `_get_or_create()`
- **Method:** `get_crystal()`
- **Method:** `tick()`
- **Method:** `process_concepts()`
  - *Purpose:* Process an IVMEnvelope into crystals via pre-extracted ConceptSignals.
- **Method:** `_role_to_category()`
  - *Purpose:* Map ConceptSignal role to DER energy category.
- **Method:** `get_stats()`

#### Class: `MemoryNode`
> A node in dimensional memory. Links across dimensions.

- **Method:** `modify()`
- **Method:** `access()`

#### Class: `MemoryConstantSystem`
> DMC â€" dimensional memory with concept indexing. Gate: PERSISTENT+.

- **Method:** `__init__()`
- **Method:** `store()`
- **Method:** `recall()`
- **Method:** `recall_by_dimension()`
- **Method:** `store_semantic()`
  - *Purpose:* Store semantically-tagged MemoryNodes built from ConceptSignals.
- **Method:** `get_patterns()`
- **Method:** `get_stats()`

#### Class: `ConceptSignal`
> A single extracted semantic concept with role metadata and constraint weights.


#### Class: `RecallPacket`
> A surfaced DMC MemoryNode ready for synthesis injection.

- **Method:** `as_context_fragment()`
  - *Purpose:* Return a compact string suitable for prepending to synthesis content.

#### Class: `ConceptExtractor`
> Extract ConceptSignals from IVMEnvelopes via lightweight heuristics.

- **Method:** `__init__()`
- **Method:** `extract()`
  - *Purpose:* Extract up to MAX_SIGNALS ConceptSignals from envelope.data.
- **Method:** `_infer_intent()`
  - *Purpose:* Lightweight intent classification from text surface patterns.
- **Method:** `_infer_role()`
  - *Purpose:* Infer concept role from word characteristics and context.

#### Class: `DimensionalRecall`
> Surface relevant MemoryNodes from DMC for active conversation turns.

- **Method:** `__init__()`
- **Method:** `recall_for_signals()`
  - *Purpose:* Surface MemoryNodes relevant to the given signals.
- **Method:** `_to_packet()`
  - *Purpose:* Convert a MemoryNode + triggering signal to a RecallPacket.
- **Method:** `_dim_tags()`
  - *Purpose:* Generate dimension tag strings for recall_by_dimension queries.

#### Class: `Emotion`

#### Class: `_PoolView`
> Backward-compatible pool interface that aggregates facet energies by category.

- **Method:** `__init__()`
- **Method:** `energy()`
  - *Purpose:* Aggregate energy of all facets in this category.
- **Method:** `energy()`
  - *Purpose:* Set aggregate energy â€" scales facets proportionally.
- **Method:** `inject()`
- **Method:** `drain()`
- **Method:** `decay()`
  - *Purpose:* Category-level decay â€" real decay happens in DER.tick() on facets.

#### Class: `EnergyRegulatorSystem`
> DER â€" FACET-LEVEL energy physics with resonance graph.

- **Method:** `__init__()`
- **Method:** `spike_detected()`
  - *Purpose:* Is the system currently overheating from contradictions?
- **Method:** `register_dissonance()`
  - *Purpose:* Receive dissonance heat from IVM geometry.
- **Method:** `pools()`
  - *Purpose:* Backward-compatible pool interface.
- **Method:** `register_facet()`
  - *Purpose:* Register a facet for energy tracking. Builds resonance links.
- **Method:** `register_crystal()`
  - *Purpose:* Register all facets of a crystal for energy tracking.
- **Method:** `_update_links_for_facet()`
  - *Purpose:* Build 8-point cosine similarity resonance links for a facet.
- **Method:** `inject_energy()`
  - *Purpose:* Inject energy into a specific facet, dampened by presence.
- **Method:** `disperse_from()`
  - *Purpose:* Ripple energy from a source facet through its resonance links.
- **Method:** `inject_to_category()`
  - *Purpose:* Distribute energy injection across all facets in a category.
- **Method:** `drain_from_category()`
  - *Purpose:* Drain energy from all facets in a category.
- **Method:** `category_energy()`
  - *Purpose:* Total energy across all facets in a category.
- **Method:** `tick()`
  - *Purpose:* Full physics tick. Restored from original:
- **Method:** `_batch_dispersal()`
  - *Purpose:* Adjacency-matrix dispersal: all facets disperse simultaneously.
- **Method:** `_inject_curiosity_energy()`
  - *Purpose:* Inject small energy into underexplored facets to encourage discovery.
- **Method:** `process()`
  - *Purpose:* Process an envelope: inject energy into appropriate category facets.
- **Method:** `_route_to_category()`
- **Method:** `inject_emotion()`
- **Method:** `update_from_axis_activation()`
  - *Purpose:* Map the current constraint-axis activation vector + IVM heat
- **Method:** `dominant_emotion()`
  - *Purpose:* Return the name of the currently strongest emotion.
- **Method:** `emotional_state()`
  - *Purpose:* Compact snapshot for pipeline_state stamping with Clause III variables.
- **Method:** `total_energy()`
- **Method:** `get_stats()`
- **Method:** `get_tunable_parameters()`
  - *Purpose:* Expose parameters that DPME can tune.

#### Class: `MoralPillar`

#### Class: `MoralScore`
> Result of a moral evaluation.


#### Class: `MoralState`
> Current moral standing across all pillars.

- **Method:** `alignment()`

#### Class: `ThoughtBudget`
> Per-thought energy budget â€" the mechanism by which immoral thoughts die.


#### Class: `MoralityMortalitySystem`
> DMM â€" moral evaluation with energy consequences. Gate: AGENTIC.

- **Method:** `__init__()`
- **Method:** `assess_thought_cost()`
  - *Purpose:* PROACTIVE moral friction â€" applied DURING processing, not after.
- **Method:** `evaluate()`
  - *Purpose:* Evaluate an action morally and apply energy consequences.
- **Method:** `_map_pillar()`
- **Method:** `_score_intent()`
- **Method:** `_score_outcome()`
- **Method:** `_apply_consequences()`
  - *Purpose:* Moral alignment â†' energy reward. Violation â†' energy drain.
- **Method:** `get_stats()`

#### Class: `DimensionalSystems`
> The unified hub for all 4 dimensional systems.

- **Method:** `__init__()`
- **Method:** `_clamp01()`
- **Method:** `connect_sedimemory()`
  - *Purpose:* Inject L3.5 SediMemory into CrystalProcessingSystem so crystal
- **Method:** `set_genealogy()`
  - *Purpose:* Attach/detach genealogy logger for Layer-3 semantic observations.
- **Method:** `_ensure_genealogy_symbols()`
  - *Purpose:* Lazy-load genealogy symbols to survive import-order cycles.
- **Method:** `_current_pressure_vec()`
  - *Purpose:* Measure live pressure from manifold aggregate + active system load.
- **Method:** `_measure_constraint_weights()`
  - *Purpose:* Derive ConceptSignal constraint weights from live manifold pressure and,
- **Method:** `_slot_for_concept()`
  - *Purpose:* Choose a deterministic 625 slot tied to current manifold dynamics.
- **Method:** `_ensure_semantic_abilities()`
  - *Purpose:* Register Layer-3 semantic ops as genealogy abilities if missing.
- **Method:** `_observe_semantic_operation()`
- **Method:** `process()`
  - *Purpose:* Route an envelope through all applicable systems.
- **Method:** `process_with_concepts()`
  - *Purpose:* Semantic processing path.
- **Method:** `get_recall_context()`
  - *Purpose:* Read-only: surface DMC memories relevant to text.
- **Method:** `process_synthesis()`
  - *Purpose:* Layer 2 → Layer 3 pathway.
- **Method:** `get_constraint_aggregate()`
  - *Purpose:* Compute the mean signed constraint displacement across all crystals
- **Method:** `update_emotional_state()`
  - *Purpose:* Receive the turn's constraint-axis activation vector and IVM heat,
- **Method:** `tick()`
- **Method:** `get_stats()`

#### Function: `verify_dimensional_systems()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `tick_evolved()`

#### Function: `get_constraint_aggregate_evolved()`

#### Function: `evolved_tick()`

#### Function: `inject_evolved()`

### File: `./aurora_core_ai/aurora_expression_perception.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA EXPRESSION & PERCEPTION ENGINE (Layer 5)
=================================================
Consolidated from 7 modules (~7,000 lines):
  1. aurora_impression_engine_v2.py   - Dimensional cascade
  2. aurora_manifold_engine_v2.py     - 5D consciousness geometry
  3. aurora_language_architecture.py  - 3-tier language ecology
  4. aurora_expression_pressure.py    - Rhythm/creativity/novelty
  5. aurora_sensory_systems.py        - Pattern perception
  6. aurora_hybrid_vision.py          - Shadow inference
  7. aurora_voice_core.py             - Voice genome

TWO PIPELINES, ONE ENGINE:

  PERCEPTION (inward):
    SensoryInput -- PatternDetection -- ShadowInference -- ImpressionCascade -- ManifoldMapping
    Raw data becomes meaning through dimensional compression.

  EXPRESSION (outward):
    AssemblyResult -- ExpressionEcology -- PressureEvaluation -- VoiceGenome -- Output
    Internal state becomes language through evolutionary selection.

DOCTRINE:
  Aurora does NOT see the selection machinery.
  The environment evolves. Aurora simply experiences.
  Entropic pressure from Layer 4 prevents stagnation in BOTH pipelines.
  All operations are mode-gated through ExistenceMode.
  Shadow reveals what's missing. Silence is data.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `LexicalEntry`
> A word in Aurora's evolving vocabulary.

- **Method:** `use()`

#### Function: `_env_flag()`

#### Function: `_clamp()`

#### Function: `_generate_id()`

#### Function: `_entropy()`
> Shannon entropy of a distribution, normalized to [0,1].


#### Function: `_extract_rich_audio_features()`
> Extract a richer audio feature packet for sensory and live perception.


#### Class: `LexicalMemory`
> Aurora's vocabulary. Grows through interaction, not pre-loading.

- **Method:** `__init__()`
- **Method:** `_seed_core()`
  - *Purpose:* Seed vocabulary with words for all grammatical roles.
- **Method:** `find_by_noncomp()`
  - *Purpose:* Find words mapped to a specific Non-Comp, sorted by valence proximity.
- **Method:** `add_word()`
- **Method:** `record_usage()`
- **Method:** `find_by_role()`
- **Method:** `find_by_valence()`
- **Method:** `save()`
  - *Purpose:* Persist full vocabulary to disk.
- **Method:** `load()`
  - *Purpose:* Restore vocabulary from disk. Returns number of entries loaded.

#### Class: `ConsciousnessPoint`
> A 5D coordinate in Aurora's state space (X, T, N, B, A).

- **Method:** `as_tuple()`
- **Method:** `normalize()`
- **Method:** `distance_to()`

#### Class: `ManifoldEngine`
> 5D consciousness geometry engine (X, T, N, B, A).

- **Method:** `__init__()`
- **Method:** `map_input()`
- **Method:** `update_state()`

#### Class: `PatternType`
> Types of patterns Aurora can perceive.


#### Class: `DimensionalPattern`
> A detected pattern with dimensional physics vector.

- **Method:** `physics_vector()`
  - *Purpose:* Convert pattern features to a physics-compatible vector.

#### Class: `PatternDetector`
> Detects dimensional patterns from raw sensory input.

- **Method:** `__init__()`
- **Method:** `detect()`
  - *Purpose:* Detect patterns in raw input, gated by existence mode.

#### Class: `ShadowSignature`
> What ISN'T present - absence as data.


#### Class: `ShadowInferenceEngine`
> Detects what ISN'T there.

- **Method:** `__init__()`
- **Method:** `infer()`
  - *Purpose:* Infer shadows from what was detected vs what was expected.

#### Class: `EmotionShard`
> Lowest stable interpretation unit. Created from energy after being evaluation.


#### Class: `ImpressionSeed`
> Cluster of shards into proto-meaning.


#### Class: `GhostRelic`
> Stabilized pattern - a proto-memory from clustered seeds.


#### Class: `ImpressionCascade`
> Dimensional cascade: EnergyPacket -- EmotionShard -- ImpressionSeed -- GhostRelic.

- **Method:** `__init__()`
- **Method:** `energy_to_shard()`
  - *Purpose:* Convert raw emotion channels to an EmotionShard. Requires TRANSIENT+.
- **Method:** `shard_to_seed()`
  - *Purpose:* Cluster a shard into an existing seed or create a new one. Requires PERSISTENT+.
- **Method:** `seeds_to_relic()`
  - *Purpose:* Compress seeds into a GhostRelic. Requires BOUNDED+.
- **Method:** `find_by_noncomp()`
  - *Purpose:* Find words mapped to a specific Non-Comp, sorted by valence proximity.
- **Method:** `add_word()`
- **Method:** `record_usage()`
- **Method:** `find_by_role()`
- **Method:** `find_by_valence()`
- **Method:** `save()`
  - *Purpose:* Persist full vocabulary to disk.
- **Method:** `load()`
  - *Purpose:* Restore vocabulary from disk. Returns number of entries loaded.
- **Method:** `size()`

#### Class: `WisdomShard`
> Learning extracted from dead expressions.


#### Class: `WisdomStore`
> Accumulates wisdom from dead expression offspring.

- **Method:** `__init__()`
- **Method:** `add()`
- **Method:** `get_bias()`
  - *Purpose:* Get average bias for an i-state lineage.
- **Method:** `_prune()`
  - *Purpose:* Remove lowest-fitness shards when over capacity.

#### Class: `ExpressionOffspring`
> A candidate expression variant, subject to evolutionary selection.

- **Method:** `record_fitness()`
- **Method:** `average_fitness()`
- **Method:** `die()`
  - *Purpose:* Extract wisdom from this dying expression.

#### Class: `ExpressionEcology`
> Evolutionary selection of expression variants.

- **Method:** `__init__()`
- **Method:** `spawn()`
  - *Purpose:* Spawn a new expression offspring for an i-state lineage.
- **Method:** `select()`
  - *Purpose:* Record fitness for an offspring.
- **Method:** `run_generation()`
  - *Purpose:* Run one generation cycle: evaluate, cull weakest, advance.
- **Method:** `_cull()`
  - *Purpose:* Kill weakest, extract wisdom.
- **Method:** `_avg_fitness()`
- **Method:** `get_stats()`

#### Class: `RhythmSignature`
> Rhythmic analysis of an expression.

- **Method:** `compute()`

#### Class: `CreativitySignature`
> Creativity analysis of an expression.

- **Method:** `compute()`

#### Class: `ExpressionPressure`
> Evaluates expression quality: rhythm, creativity, novelty,

- **Method:** `__init__()`
- **Method:** `evaluate()`
  - *Purpose:* Evaluate expression pressure on a text output.

#### Class: `VoiceGenome`
> Aurora's voice personality parameters.

- **Method:** `__init__()`
- **Method:** `evolve()`
  - *Purpose:* Evolve voice parameters from feedback signals.
- **Method:** `to_dict()`

#### Function: `infer_word_role()`
> Infer the grammatical role of a word.


#### Function: `infer_word_valence()`
> Infer emotional valence for a word.


#### Class: `SentenceComposer`
> Composes grammatical sentences from Aurora's evolving template pool.

- **Method:** `__init__()`
- **Method:** `set_oets()`
  - *Purpose:* Wire the Ontological Evolutionary Template Scaffolding engine.
- **Method:** `_has_oets()`
- **Method:** `_seed_pool()`
  - *Purpose:* Seed with initial templates. These can be outcompeted by learned ones.
- **Method:** `absorb()`
  - *Purpose:* Extract sentence patterns from input text and add to template pool.
- **Method:** `_analyze_pattern_depth()`
  - *Purpose:* Analyze the ontological depth of words in a sentence to determine
- **Method:** `_scaffold_pattern()`
  - *Purpose:* Upgrade a primitive pattern with semantic annotations.
- **Method:** `_extract_pattern()`
  - *Purpose:* Convert a sentence into a template by replacing content words with slots.
- **Method:** `_is_valid_pattern()`
  - *Purpose:* A valid pattern has at least one verb slot and one other slot.
- **Method:** `record_fitness()`
  - *Purpose:* Record fitness for a used template.
- **Method:** `_cull_tone()`
  - *Purpose:* Remove lowest-fitness mature templates from a tone category.
- **Method:** `run_generation()`
  - *Purpose:* Run one evolutionary generation across all tones.
- **Method:** `_evaluate_promotions()`
  - *Purpose:* Check all templates for scaffolding level promotion.
- **Method:** `_mutate_pattern()`
  - *Purpose:* Create a variant of a template by small structural changes.
- **Method:** `set_context()`
  - *Purpose:* Set context keywords from perception pipeline.
- **Method:** `compose()`
  - *Purpose:* Compose a grammatical expression from evolving templates + lexicon.
- **Method:** `feedback()`
  - *Purpose:* Feed expression fitness back to the templates that produced it,
- **Method:** `_expression_feedback_to_oets()`
  - *Purpose:* Bidirectional learning: expression quality feeds back to
- **Method:** `_fill_template()`
  - *Purpose:* Fill a template's slots with words from the lexicon.
- **Method:** `_fill_primitive_slot()`
  - *Purpose:* PRIMITIVE fill: pick any word of the right role.
- **Method:** `_fill_semantic_slot()`
  - *Purpose:* STRUCTURAL/SEMANTIC fill: pick a word from a specific semantic
- **Method:** `_fill_cluster_slot()`
  - *Purpose:* CONCEPTUAL fill: pick a word from a named concept cluster.
- **Method:** `_fill_abstract_slot()`
  - *Purpose:* ABSTRACT fill: generate a meta-expression from deeply understood concepts.
- **Method:** `_conjugate_first_person()`
  - *Purpose:* Conjugate a verb for first-person 'I' subject.
- **Method:** `get_stats()`

#### Class: `ExpressionPerceptionEngine`
> The Layer 5 orchestrator. Manages both pipelines.

- **Method:** `__init__()`
- **Method:** `set_personality()`
  - *Purpose:* Accept personality traits from L6 for expression shaping.
- **Method:** `set_ivm()`
  - *Purpose:* Wire IVM lattice for heat-aware expression selection.
- **Method:** `set_grammar()`
  - *Purpose:* Wire GrammarEngine for constraint-driven sentence structure.
- **Method:** `get_thought_log()`
  - *Purpose:* Return last N internal thought traces (for /thought command).
- **Method:** `get_last_drafts()`
  - *Purpose:* Return last N draft sets (for /drafts command).
- **Method:** `observe_user_text()`
  - *Purpose:* Feed user text to lexical convergence module.
- **Method:** `set_lsv_metrics()`
  - *Purpose:* Push OETS metrics to LSV for evolution check.
- **Method:** `_maybe_update_lsv()`
  - *Purpose:* Periodically push OETS metrics to LSV, and nudge from constraint axes.
- **Method:** `set_genealogy_ref()`
  - *Purpose:* Store genealogy reference for axis-based LSV nudging.
- **Method:** `set_axis_context()`
  - *Purpose:* Store the current turn's constraint-axis state so express() can
- **Method:** `evo_status()`
  - *Purpose:* Return expression evolution status.
- **Method:** `save_evo_state()`
  - *Purpose:* Persist expression evolution state.
- **Method:** `perceive()`
  - *Purpose:* Full perception pipeline.
- **Method:** `express()`
  - *Purpose:* Full expression pipeline.
- **Method:** `_build_expression()`
  - *Purpose:* Build expression text using SentenceComposer with OETS enrichment.
- **Method:** `ingest_interaction()`
  - *Purpose:* Process an interaction through perception pipeline.
- **Method:** `consolidate()`
  - *Purpose:* Run consolidation: generation cycle, seed--relic promotion, template evolution.
- **Method:** `get_stats()`

#### Function: `build_layer5_associative_modules()`
> Initialize modules associated with the perception/identity boundary while


#### Function: `verify_layer5()`

#### Function: `_clamp()`

#### Function: `_generate_id()`

#### Function: `_cosine_similarity()`
> Calculate cosine similarity between two vectors.


#### Class: `SensoryTraitDomain`
> Domains specific to sensory processing -- extend TraitDomain.


#### Class: `SensoryPerceptTemplate`
> Base template for a learned sensory pattern.

- **Method:** `to_dict()`
- **Method:** `from_dict()`
- **Method:** `match()`
  - *Purpose:* Calculate match score against input.
- **Method:** `update_from_observation()`
  - *Purpose:* Update template from new observation (moving average).

#### Class: `SensoryConcept`
> A promoted concept from clustered percepts.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `SensoryConceptMemory`
> Manages concept formation from raw percepts.

- **Method:** `__init__()`
- **Method:** `_canonical_label()`
- **Method:** `_iter_labels()`
- **Method:** `_find_concept_entry()`
- **Method:** `_register_alias()`
- **Method:** `_merge_concepts()`
- **Method:** `_rename_or_alias_concept()`
- **Method:** `guide_label()`
- **Method:** `record_percept()`
  - *Purpose:* Store raw percept for later clustering.
- **Method:** `cluster_and_promote()`
  - *Purpose:* Attempt to promote raw percepts into a stable concept.
- **Method:** `find_matching_concept()`
  - *Purpose:* Find the best matching concept for a feature vector.
- **Method:** `add_grounding()`
  - *Purpose:* Ground a sensory concept to language/intent.
- **Method:** `prune()`
  - *Purpose:* Prune low-confidence concepts.
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Function: `create_visual_genes()`
> Create genes for visual competency traits.


#### Function: `create_audio_genes()`
> Create genes for audio competency traits.


#### Function: `create_visual_crystal()`
> Create behavioral crystal for visual competency.


#### Function: `create_audio_crystal()`
> Create behavioral crystal for audio competency.


#### Class: `SensoryPatternMapper`
> Maps visual and audio input to Aurora's existing PatternTypes.

- **Method:** `__init__()`
- **Method:** `map_visual_input()`
  - *Purpose:* Convert visual input to DimensionalPatterns.
- **Method:** `map_audio_input()`
  - *Purpose:* Convert audio input to DimensionalPatterns.

#### Class: `SensoryCompetencyEngine`
> Main controller for Aurora's evolutionary sensory capabilities.

- **Method:** `__init__()`
- **Method:** `_bootstrap_lineage()`
  - *Purpose:* Bootstrap base percept templates from lineage constants.
- **Method:** `attach_oets()`
  - *Purpose:* Attach OETS for semantic grounding.
- **Method:** `attach_dna()`
  - *Purpose:* Attach DNA system and add sensory genes if not present.
- **Method:** `get_visual_competency()`
  - *Purpose:* Get current visual competency values.
- **Method:** `get_audio_competency()`
  - *Purpose:* Get current audio competency values.
- **Method:** `process_visual_input()`
  - *Purpose:* Process visual input through the sensory pipeline.
- **Method:** `process_audio_input()`
  - *Purpose:* Process audio input through the sensory pipeline.
- **Method:** `_extract_visual_features()`
  - *Purpose:* Extract normalized feature vector from visual data.
- **Method:** `_extract_audio_features()`
  - *Purpose:* Extract normalized feature vector from audio data.
- **Method:** `_ground_to_oets()`
  - *Purpose:* Ground sensory concept to OETS semantic node.
- **Method:** `evolve()`
  - *Purpose:* Apply evolutionary pressure to sensory competencies.
- **Method:** `_sync_to_dna()`
  - *Purpose:* Sync crystal values back to DNA genes.
- **Method:** `_create_sensory_allele()`
  - *Purpose:* Create a fractal allele from sensory evolution.
- **Method:** `prune()`
  - *Purpose:* Prune low-value concepts.
- **Method:** `get_stats()`
  - *Purpose:* Get comprehensive statistics.
- **Method:** `_save_state()`
  - *Purpose:* Save state to persistence directory.
- **Method:** `_load_state()`
  - *Purpose:* Load state from persistence directory.
- **Method:** `save_state()`
  - *Purpose:* Public save method.

#### Function: `create_sensory_competency_engine()`
> Factory function to create a SensoryCompetencyEngine.


#### Function: `get_sensory_genes()`
> Get all sensory genes for DNA integration.


#### Function: `_is_termux_env()`

#### Class: `LinuxCamera`
> Webcam capture using OpenCV.

- **Method:** `__init__()`
- **Method:** `_ensure_mediapipe_detectors()`
- **Method:** `_load_ultralytics_detector()`
- **Method:** `_enrich_visual_features()`
- **Method:** `open()`
  - *Purpose:* Open the camera device.
- **Method:** `close()`
  - *Purpose:* Release the camera.
- **Method:** `capture_frame()`
  - *Purpose:* Capture a single frame.
- **Method:** `extract_features()`
  - *Purpose:* Extract visual features from a frame for SensoryCompetencyEngine.

#### Class: `LinuxMicrophone`
> Audio capture using sounddevice and speech recognition.

- **Method:** `__init__()`
- **Method:** `_get_vad()`
- **Method:** `_voice_activity_ratio()`
- **Method:** `_transcribe_with_faster_whisper()`
- **Method:** `start_stream()`
  - *Purpose:* Start continuous audio capture.
- **Method:** `stop_stream()`
  - *Purpose:* Stop audio capture.
- **Method:** `get_audio_chunk()`
  - *Purpose:* Get queued audio data.
- **Method:** `record_audio()`
  - *Purpose:* Record audio for specified duration.
- **Method:** `listen_and_transcribe()`
  - *Purpose:* Listen for speech and transcribe using Google Speech Recognition.
- **Method:** `extract_features()`
  - *Purpose:* Extract audio features for SensoryCompetencyEngine.

#### Class: `LinuxVoice`
> Text-to-speech with natural neural voices via edge-tts.

- **Method:** `__init__()`
- **Method:** `set_voice()`
  - *Purpose:* Change Aurora's voice.
- **Method:** `_pick_pyttsx3_voice()`
  - *Purpose:* Choose the closest local voice to the current neural preset.
- **Method:** `_apply_pyttsx3_voice()`
  - *Purpose:* Keep local pyttsx3 fallback close to the selected neural voice.
- **Method:** `set_rate()`
  - *Purpose:* Set speaking rate. E.g., '+20%' for faster, '-10%' for slower.
- **Method:** `set_pitch()`
  - *Purpose:* Set voice pitch. E.g., '+10Hz' for higher, '-5Hz' for lower.
- **Method:** `set_volume()`
  - *Purpose:* Set volume. E.g., '+20%' for louder, '-10%' for quieter.
- **Method:** `adapt_for_emotion()`
  - *Purpose:* Adapt voice parameters for emotional expression.
- **Method:** `speak()`
  - *Purpose:* Speak the given text using neural TTS.
- **Method:** `_speak_edge_tts_sync()`
  - *Purpose:* Synchronous wrapper for edge-tts.
- **Method:** `speak_async()`
  - *Purpose:* Speak in background thread.
- **Method:** `is_speaking()`
  - *Purpose:* Check if currently speaking.
- **Method:** `stop()`
  - *Purpose:* Stop current speech.
- **Method:** `list_voices()`
  - *Purpose:* List available voice presets.
- **Method:** `get_current_voice()`
  - *Purpose:* Get current voice name.
- **Method:** `list_all_voices_sync()`
  - *Purpose:* List all available edge-tts voices (sync).

#### Class: `HardwareInterface`
> Main orchestrator connecting hardware to SensoryCompetencyEngine.

- **Method:** `__init__()`
  - *Purpose:* Initialize hardware interface.
- **Method:** `start()`
  - *Purpose:* Start all enabled hardware components.
- **Method:** `stop()`
  - *Purpose:* Stop all hardware components.
- **Method:** `capture_visual()`
  - *Purpose:* Capture and extract features from camera.
- **Method:** `capture_audio()`
  - *Purpose:* Capture and extract features from microphone.
- **Method:** `listen_for_speech()`
  - *Purpose:* Listen and transcribe speech.
- **Method:** `speak()`
  - *Purpose:* Speak text using TTS.
- **Method:** `speak_async()`
  - *Purpose:* Speak without blocking.
- **Method:** `process_visual()`
  - *Purpose:* Process visual data through SensoryCompetencyEngine.
- **Method:** `process_audio()`
  - *Purpose:* Process audio data through SensoryCompetencyEngine.
- **Method:** `load_image()`
  - *Purpose:* Load and extract features from an image file.
- **Method:** `load_audio_file()`
  - *Purpose:* Load and extract features from an audio file.
- **Method:** `get_capabilities()`
  - *Purpose:* Get available hardware capabilities.
- **Method:** `get_stats()`
  - *Purpose:* Get interface statistics.

#### Class: `SensoryLoop`
> Continuous sensory processing loop.

- **Method:** `__init__()`
  - *Purpose:* Args:
- **Method:** `start()`
  - *Purpose:* Start continuous sensory processing.
- **Method:** `stop()`
  - *Purpose:* Stop continuous processing.
- **Method:** `_visual_loop()`
  - *Purpose:* Visual processing thread.
- **Method:** `_audio_loop()`
  - *Purpose:* Audio processing thread.
- **Method:** `get_latest_visual()`
  - *Purpose:* Get most recent visual result.
- **Method:** `get_latest_audio()`
  - *Purpose:* Get most recent audio result.

#### Function: `check_dependencies()`
> Check which dependencies are available.


#### Function: `install_instructions()`
> Get installation instructions for missing dependencies.


#### Function: `create_hardware_interface()`
> Factory function to create HardwareInterface.


#### Class: `SensoryEventType`
> Types of sensory events Aurora can experience.


#### Class: `SensoryEvent`
> A sensory event that Aurora experiences.


#### Class: `VisualLinguisticMapper`
> Maps visual experiences to linguistic descriptions.

- **Method:** `__init__()`
- **Method:** `describe_visual()`
  - *Purpose:* Generate a natural language description of visual input.
- **Method:** `describe_face_event()`
  - *Purpose:* Describe a face detection event.
- **Method:** `learn_association()`
  - *Purpose:* Learn to associate visual patterns with words.

#### Class: `AudioLinguisticMapper`
> Maps audio experiences to linguistic descriptions and responses.

- **Method:** `__init__()`
- **Method:** `learn_voice_profile()`
- **Method:** `identify_voice()`
- **Method:** `describe_audio()`
  - *Purpose:* Generate a natural language description of audio input.
- **Method:** `process_transcription()`
  - *Purpose:* Process transcribed speech and extract meaning.

#### Class: `VoiceExpressionMapper`
> Maps Aurora's internal state and personality to voice characteristics.

- **Method:** `__init__()`
- **Method:** `set_personality()`
  - *Purpose:* Set personality traits that influence voice.
- **Method:** `get_speech_parameters()`
  - *Purpose:* Get speech synthesis parameters based on tone and personality.
- **Method:** `prepare_speech_text()`
  - *Purpose:* Prepare text for speech synthesis.

#### Class: `SensoryIntegrationEngine`
> Main engine that integrates all sensory modalities with language.

- **Method:** `__init__()`
  - *Purpose:* Initialize sensory integration.
- **Method:** `attach_systems()`
  - *Purpose:* Attach system references after initialization.
- **Method:** `start()`
  - *Purpose:* Start sensory integration processing.
- **Method:** `stop()`
  - *Purpose:* Stop sensory integration.
- **Method:** `set_voice_mode()`
  - *Purpose:* Enable/disable voice mode. When on, Aurora speaks all responses.
- **Method:** `start_listening()`
  - *Purpose:* Start always-on ambient audio capture (sounddevice-based).
- **Method:** `stop_listening()`
  - *Purpose:* Stop always-on listening.
- **Method:** `_continuous_listen_loop()`
  - *Purpose:* Always-on ambient audio capture loop.
- **Method:** `get_heard_speech()`
  - *Purpose:* Get speech from the queue (non-blocking).
- **Method:** `has_heard_speech()`
  - *Purpose:* Check if there's speech waiting to be processed.
- **Method:** `say()`
  - *Purpose:* Aurora says something. Uses voice if voice_mode is on.
- **Method:** `_process_loop()`
  - *Purpose:* Background processing loop for sensory events.
- **Method:** `_process_event()`
  - *Purpose:* Process a single sensory event.
- **Method:** `_process_visual_event()`
  - *Purpose:* Process visual event through the full pipeline.
- **Method:** `_process_audio_event()`
  - *Purpose:* Process audio event through the full pipeline.
- **Method:** `_event_is_recent()`
- **Method:** `_current_visual_vector()`
- **Method:** `_current_audio_vector()`
- **Method:** `_mark_guided_event()`
- **Method:** `_enqueue_surface_guidance()`
- **Method:** `guide_current_visual_label()`
- **Method:** `guide_current_audio_label()`
- **Method:** `guide_user_identity()`
- **Method:** `apply_guidance_from_text()`
- **Method:** `_ground_visual_concepts()`
  - *Purpose:* Ground visual concepts in OETS semantic web.
- **Method:** `_ground_audio_concepts()`
  - *Purpose:* Ground audio concepts in OETS semantic web.
- **Method:** `see()`
  - *Purpose:* Capture visual input and return description + raw data.
- **Method:** `_save_camera_snapshot()`
- **Method:** `listen()`
  - *Purpose:* Capture audio input and return description + raw data.
- **Method:** `hear_speech()`
  - *Purpose:* Listen for speech and transcribe it.
- **Method:** `speak()`
  - *Purpose:* Aurora speaks with personality-modulated voice.
- **Method:** `speak_async()`
  - *Purpose:* Speak without blocking.
- **Method:** `describe_what_i_see()`
  - *Purpose:* Full visual description with context.
- **Method:** `describe_what_i_hear()`
  - *Purpose:* Full audio description with context.
- **Method:** `see_image()`
  - *Purpose:* Load and process an image file.
- **Method:** `listen_to_file()`
  - *Purpose:* Load and process an audio file.
- **Method:** `describe_image()`
  - *Purpose:* Full description of an image file with context.
- **Method:** `describe_audio_file()`
  - *Purpose:* Full description of an audio file with context.
- **Method:** `get_sensory_context()`
  - *Purpose:* Get current sensory context for conversation enrichment.
- **Method:** `get_stats()`
  - *Purpose:* Get integration statistics.

#### Function: `create_sensory_integration()`
> Factory function to create SensoryIntegrationEngine from boot systems.


#### Class: `VisualFeatureVector`
> Feature vector for a single image.

- **Method:** `to_vector()`
  - *Purpose:* 8-dimensional feature vector for clustering.
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `FeatureExtractor`
> Extracts feature vectors from image files.

- **Method:** `extract()`
  - *Purpose:* Extract features from an image file. Returns None if unavailable.
- **Method:** `_extract_from_pil()`
- **Method:** `_estimate_edge_density()`
  - *Purpose:* Estimate edge density using a small downsampled version.
- **Method:** `_stub_extract()`
  - *Purpose:* Stub when PIL unavailable: return near-zero vector with file hash.

#### Class: `VisualCluster`
> A cluster of visually similar images.

- **Method:** `update_centroid()`
  - *Purpose:* Recompute centroid from member vectors.
- **Method:** `tightness()`
  - *Purpose:* Measure how tight this cluster is (1=very tight, 0=loose).
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Function: `_euclidean()`

#### Class: `SimpleKMeans`
> Pure-Python k-means for feature vector clustering.

- **Method:** `__init__()`
- **Method:** `fit()`
  - *Purpose:* Fit k-means. Returns cluster assignments (one per vector).
- **Method:** `_fit_numpy()`
- **Method:** `_fit_pure()`
  - *Purpose:* Pure Python k-means.

#### Class: `OETSVisionBinder`
> Binds visual clusters to OETS ontology nodes as "looks_like" relations.

- **Method:** `__init__()`
- **Method:** `bind_cluster()`
  - *Purpose:* Determine a concept label for the cluster (or keep unnamed).
- **Method:** `_infer_label()`
  - *Purpose:* Infer a rough semantic label from the cluster centroid.
- **Method:** `_bind_to_oets()`
  - *Purpose:* Create or update an OETS node for this visual concept.

#### Class: `WebImageDownloader`
> Downloads public-domain images from Wikipedia/Wikimedia for Aurora's visual seeds.

- **Method:** `__init__()`
- **Method:** `_reset_if_new_day()`
- **Method:** `can_download()`
- **Method:** `download_for_concept()`
  - *Purpose:* Attempt to download 1-2 images related to a concept from Wikimedia.
- **Method:** `_find_wikimedia_images()`
  - *Purpose:* Search Wikipedia for images related to a concept.
- **Method:** `_download_image()`
  - *Purpose:* Download a single image URL. Returns saved path or None.
- **Method:** `download_seed_batch()`
  - *Purpose:* Download images for N random seed concepts.
- **Method:** `save_state()`
- **Method:** `load_state()`

#### Class: `ImageIngestionProtocol`
> Main orchestrator for Aurora's vision bootstrapping.

- **Method:** `__init__()`
- **Method:** `ingest_folder()`
  - *Purpose:* Scan a folder for images, extract features, cluster, bind.
- **Method:** `_recluster()`
  - *Purpose:* Recluster all vectors and rebind OETS.
- **Method:** `autonomous_download_cycle()`
  - *Purpose:* Run one autonomous download cycle during idle time.
- **Method:** `refine_from_camera_frame()`
  - *Purpose:* Called when a live camera frame is processed.
- **Method:** `teach_label()`
  - *Purpose:* User-driven visual teaching: bind a label to the current visual features.
- **Method:** `save_index()`
- **Method:** `load_index()`
- **Method:** `status()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `ingest_interaction_evolved()`

#### Function: `init_evolved()`

#### Function: `evaluate_evolved()`

#### Function: `energy_to_shard_evolved()`

### File: `./aurora_core_ai/aurora_grammar_engine.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA GRAMMAR ENGINE
=====================
Grammar as evolved behavior, not formatting rules.

Sentence structure emerges from the same evolutionary pressure system that
governs Aurora's cognition.  Structural motifs are promoted through the
constraint genealogy when they survive clarity + constraint pressure -- the
exact same mechanism that promotes OUTLET_PUSH and every other behavior.

Doctrine:
  Grammar is the compressed map of surviving structural patterns.
  Clear structure is the lowest-energy path to A-axis relief.
  So grammatical order does not need to be taught -- it needs to be the
  path of least resistance through the constraint system.

Pipeline:
  token -> role_tag -> pattern_extract -> motif_select -> slot_fill ->
  genealogy_relief_log -> promote/penalize

Bootstrap (run once via /grammarboot):
  MotifMiner.mine(corpus) -> seed MotifLineage with top patterns

Reference stability:
  Motifs track which role positions carry entity references across clauses
  so that pronouns ("it", "this") resolve correctly to prior agents.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_clamp()`

#### Class: `TokenRole`

#### Class: `RoleTagger`
> Lightweight rule-based tagger.  No external dependencies.

- **Method:** `__init__()`
- **Method:** `tag()`
  - *Purpose:* Return list of (token, TokenRole) for the sentence.
- **Method:** `extract_pattern()`
  - *Purpose:* Return the collapsed role sequence (no UNKNOWN, no consecutive dupes).
- **Method:** `extract_reference_positions()`
  - *Purpose:* Return indices where the token is a reference pronoun (-> prior agent).
- **Method:** `_tokenize()`
- **Method:** `_tag_rule_based()`
- **Method:** `_tag_with_nltk()`
  - *Purpose:* NLTK POS -> simplified TokenRole mapping.

#### Class: `StructuralMotif`
> A promoted sentence structure pattern -- the basic unit of evolved grammar.

- **Method:** `composability_score()`
- **Method:** `should_promote()`
- **Method:** `should_demote()`
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `DiscourseMotif`
> Turn-level discourse transition pattern.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `MotifLineage`
> Manages the full motif pool: storage, promotion, penalization.

- **Method:** `__init__()`
- **Method:** `pattern_key()`
- **Method:** `get_or_create()`
- **Method:** `record_success()`
- **Method:** `record_fail()`
- **Method:** `get_promoted()`
- **Method:** `best_for_pressure()`
  - *Purpose:* Select best promoted motif given the current constraint pressure state.
- **Method:** `seed_motifs()`
  - *Purpose:* Bulk-seed from corpus mining: [(role_seq, freq, context_list)]
- **Method:** `stats()`
- **Method:** `_maybe_save()`
- **Method:** `save()`
- **Method:** `_save()`
- **Method:** `_load()`

#### Class: `SlotFiller`
> Given a structural motif and a pool of tokens keyed by role, assemble

- **Method:** `fill()`
- **Method:** `pool_from_text()`

#### Class: `MotifMiner`
> Run once on the chat corpus to extract structural motifs and seed the

- **Method:** `__init__()`
- **Method:** `mine_corpus()`
  - *Purpose:* Extract top structural motifs.
- **Method:** `mine_discourse()`
  - *Purpose:* Extract turn-type bigrams for discourse motifs.

#### Class: `DiscourseTracker`
> Track turn-level discourse patterns and suggest coherent continuations.

- **Method:** `__init__()`
- **Method:** `record_turn()`
- **Method:** `suggest_next_turn_type()`

#### Function: `_classify_turn_type()`

#### Class: `GrammarEngine`
> Top-level grammar evolution engine.

- **Method:** `__init__()`
- **Method:** `set_genealogy()`
  - *Purpose:* Wire in the ConstraintGenealogyLogger for pressure-state reads.
- **Method:** `set_ivm()`
  - *Purpose:* Wire in the IVM lattice so heat level can modulate clause depth.
- **Method:** `_axis_bias_from_links()`
  - *Purpose:* Compute a frequency-based axis prior from the promoted link registry.
- **Method:** `_pressure_state()`
  - *Purpose:* Return (orientation, outlet_fraction) from genealogy.
- **Method:** `_log_relief_to_genealogy()`
  - *Purpose:* Log relief events to the genealogy when a grammatically clear sentence
- **Method:** `_context_hash()`
- **Method:** `suggest_structure()`
  - *Purpose:* Given a raw expression, find the best promoted structural motif for
- **Method:** `observe_exchange()`
  - *Purpose:* Observe a completed exchange and update motif fitness.
- **Method:** `extract_pattern()`
  - *Purpose:* Public helper for external callers.
- **Method:** `suggest_discourse()`
  - *Purpose:* Suggest what type of turn would cohere with the current discourse.
- **Method:** `bootstrap_from_corpus()`
  - *Purpose:* Mine corpus once to seed the motif lineage with real structural patterns.
- **Method:** `status()`

### File: `./aurora_core_ai/aurora_i_state_beings.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA I-STATE BEINGS
======================

Layer 2 of Aurora's architecture.
The ten ontological beings, each an embodiment of one existence predicate.

REPLACES (consolidated from 2 modules):
    i_state_10beings1.py     (~930 lines)
    higher_universe_10__1_.py (~500 lines)

DEPENDS ON:
    foundational_contract.py     (Layer 0)
    aurora_ivm.py                (Layer 1)
    aurora_constraint_manifold.py (Layer -1)

ARCHITECTURE:
    Each I-State being is an AGENTIC node in the IVM lattice.
    It owns one predicate from the FoundationalContract.
    Its job is to process input through its predicate's lens —
    not by counting keywords, but by asserting ontological claims.

    The being asks: "Given this input, what can I truthfully assert
    about it from the perspective of my predicate?"

    A being that owns I_DO asks: "Does this input involve energy exchange?"
    A being that owns I_SAW asks: "Does this input involve boundary crossing?"
    A being that owns I_DID asks: "Does this input involve authored change?"

    If the input's ExistenceMode is too low for the being's predicate,
    the being reports silence — not failure. The input simply doesn't
    exist at the being's ontological tier. That silence is data.

THE 10 BEINGS (5 polarity pairs):
    Existence:  I_IS / I_ISNT      -> admissibility / incoherence
    Temporal:   I_CAN / I_CANNOT   -> continuation / termination
    Energy:     I_DO / I_DONOT     -> exchange / conservation
    Boundary:   I_SAW / I_SOUGHT   -> reception / projection
    Agency:     I_DID / I_DIDNT    -> authorship / passivity

RECURSION LEVEL <-> BEING AXIS:
    Each being operates at the recursion level of its axis.
    This determines how strongly it reacts to local stimuli
    and how much authority it has over whole-alignment.

        I_IS / I_ISNT    -> SURFACE  (existence, react=1.0,   align=0.0001)
        I_CAN / I_CANNOT -> SHALLOW  (temporal,  react=0.316, align=0.003)
        I_DO  / I_DONOT  -> MODERATE (energy,    react=0.01,  align=0.01)
        I_SAW / I_SOUGHT -> DEEP     (boundary,  react=0.003, align=0.316)
        I_DID / I_DIDNT  -> CORE     (agency,    react=0.0001, align=1.0)

    inject_stimulus() is called with the being's recursion level.
    Surface beings inject strongly. Core beings inject almost nothing locally.
    But core beings (I_DID/I_DIDNT) have the most authority over
    the whole-subject polarity field.

CONSTRAINT DISPLACEMENTS:
    Every active being response carries a signed constraint_displacement.
    This is the being's contribution to the 5D ConstraintVector:
        positive polarity, high resonance -> positive displacement
        negative polarity, high resonance -> negative displacement

    The collective synthesizes all displacements into a ConstraintVector
    representing the input's full ontological position.

COLLECTIVE:
    The Collective feeds all 10 beings and synthesizes their responses.
    Conflict between polarity pairs is not a problem — it's information.
    When I_IS and I_ISNT both activate strongly, that's a paradox.
    The toroidal axis for that pair is at its transition point.

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Class: `PredicateIdentity`
> The immutable identity of an I-State being.

- **Method:** `from_predicate()`

#### Class: `BeingResponse`
> A single being's response to input.

- **Method:** `active()`

#### Class: `IStateBeing`
> A single I-State being. An AGENTIC node in the IVM lattice

- **Method:** `__init__()`
- **Method:** `predicate()`
- **Method:** `axis()`
- **Method:** `polarity()`
- **Method:** `min_mode()`
- **Method:** `recursion_level()`
- **Method:** `constraint_axis()`
- **Method:** `axis_phase()`
- **Method:** `axis_weight()`
- **Method:** `axis_polarity()`
  - *Purpose:* Signed polarity of this being's axis: cos(phase).
- **Method:** `at_transition()`
- **Method:** `react_gain()`
- **Method:** `align_gain()`
- **Method:** `t_cost()`
- **Method:** `process()`
- **Method:** `_compute_resonance()`
- **Method:** `_compute_displacement()`
  - *Purpose:* Signed constraint displacement.
- **Method:** `_interpret()`
- **Method:** `_update_coherence()`
- **Method:** `tick()`
- **Method:** `get_status()`

#### Class: `SynthesisResult`
> The unified result of all 10 beings processing one input.


#### Class: `IStateCollective`
> The collective of all 10 I-State beings.

- **Method:** `__init__()`
- **Method:** `process()`
- **Method:** `process_raw()`
- **Method:** `_synthesize_constraint_vector()`
  - *Purpose:* Sum signed displacements per constraint axis.
- **Method:** `_analyze_axes()`
- **Method:** `tick()`
- **Method:** `get_status()`
- **Method:** `get_summary()`

#### Function: `verify_beings()`

### File: `./aurora_core_ai/aurora_internal/__init__.py`

**Type:** Python Source

**Module Docstring:**
```text
Aurora internal consolidated implementations.
```

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./aurora_core_ai/aurora_internal/aurora_ability_lineage_compiler.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA ABILITY LINEAGE COMPILER
================================
Constraint-native directed recapitulation for missing abilities.

This module does not bolt on a finished capability. It:

1. Selects a target ability phenotype.
2. Traces that phenotype back to constraint-native seed stages.
3. Writes the full staged lineage path to disk.
4. Replays the lineage through ConstraintGenealogyLogger.observe()
   so composite stages are promoted as real couplings rather than
   appearing as direct late-stage insertions.

Current built-in target:
  - proposition_understanding
```

#### Function: `_clamp()`

#### Function: `_slug()`

#### Function: `_wb()`

#### Class: `SystemWriteback`
- **Method:** `to_dict()`

#### Class: `LineageStage`
- **Method:** `to_dict()`

#### Class: `CompiledLineagePath`
- **Method:** `to_dict()`

#### Class: `MaterializedStage`
- **Method:** `to_dict()`

#### Class: `MaterializationResult`
- **Method:** `to_dict()`

#### Function: `_proposition_understanding_blueprint()`

#### Function: `_lineage_blueprint_for()`

#### Class: `AbilityLineageCompiler`
> Compiles target abilities into constraint-native staged lineage paths and

- **Method:** `__init__()`
- **Method:** `compile_target()`
- **Method:** `autowrite_selected_path()`
- **Method:** `materialize_path()`
- **Method:** `_render_markdown()`
- **Method:** `_register_seed_stage()`
- **Method:** `_attempt_budget_for_stage()`
  - *Purpose:* Directed recapitulation should spend more replay exposure on later,
- **Method:** `_materialize_coupling_stage()`
- **Method:** `_find_link_for_parents()`
- **Method:** `_pressure_for_stage()`
- **Method:** `_initial_shadow_state()`
- **Method:** `_apply_stage_writebacks()`
- **Method:** `_build_activation_manifest()`
- **Method:** `_render_activation_markdown()`
- **Method:** `_write_activation_artifacts()`

#### Function: `_cli()`

### File: `./aurora_core_ai/aurora_internal/aurora_axis_emergence.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_axis_emergence.py
─────────────────────────────────────────────────────────────────────────────
Compound axis emergence from pressure co-occurrence patterns.

The 5-axis, 625-slot ceiling is broken by allowing new NC channels to emerge
when two axes are consistently co-occurring above a stability threshold.
When X and N are both high in 70%+ of occupied slots, a compound channel
NC:XN>XN is born. That channel pairs with existing channels to create new
virtual slots — which appear as "empty" to the evolver's slot_pressure bonus,
giving it new gradient to evolve toward.

Sources of co-occurrence evidence (used in order of availability):
  1. surface_pressure_log.jsonl  — real runtime axis snapshots
  2. evo_625_pressure_map.json   — axis_pressure per occupied slot

The process compounds: compound axes can themselves form compound axes when
their virtual slots are occupied and their co-occurrence stabilizes. There
is no architectural ceiling — the space expands as long as Aurora produces
novel behavior.

Compound axis naming:
  A pair ("N","B") → compound letter "NB" (sorted, joined)
  NC channel: NC:NB>NB
  Virtual slots: NC:NB>NB×NC:NB>NB, NC:NB>NB×NC:X>X, NC:X>X×NC:NB>NB, ...

Storage: aurora_state/compound_axes.json
  {
    "NB": {
      "axes": ["N", "B"],
      "co_occurrence": 0.74,
      "sample_count": 312,
      "channel": "NC:NB>NB",
      "emerged_at": 1234567890.0,
      "generation": 1,
      "virtual_slots": ["NC:NB>NB×NC:NB>NB", "NC:NB>NB×NC:X>X", ...]
    },
    ...
  }

Usage:
  detector = AxisEmergenceDetector(repo_root)
  result = detector.scan_and_register()
  # result: {"new_compounds": ["NB", "AT"], "total": 3, "virtual_slots_added": 18}
─────────────────────────────────────────────────────────────────────────────
```

#### Class: `AxisEmergenceDetector`
> Detects stable axis co-occurrence patterns and registers compound channels.

- **Method:** `__init__()`
- **Method:** `scan_and_register()`
  - *Purpose:* Full pipeline: collect evidence → compute co-occurrence → register
- **Method:** `load_empty_virtual_channels()`
  - *Purpose:* Return the set of NC channels that appear in unoccupied virtual slots.
- **Method:** `status()`
  - *Purpose:* Return current compound axis registry status.
- **Method:** `_collect_observations()`
  - *Purpose:* Collect axis pressure snapshots from all available sources.
- **Method:** `_compute_cooccurrence()`
  - *Purpose:* For each pair of axis keys, compute the fraction of observations where
- **Method:** `_compound_observations()`
  - *Purpose:* Augment observations with compound axis values (geometric mean of component axes).
- **Method:** `_virtual_slots_for()`
  - *Purpose:* Generate virtual NC channel slot pairs for a compound axis.
- **Method:** `_count_virtual_slots()`
- **Method:** `_load_compounds()`
- **Method:** `_save_compounds()`
- **Method:** `_path()`

#### Function: `empty_virtual_channels()`
> Return frozenset of NC channels in unoccupied compound virtual slots.


### File: `./aurora_core_ai/aurora_internal/aurora_braided_substrate.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA BRAIDED SUBSTRATE LAYER (BSL)
=====================================

Lowest-scale continuity substrate for intent/context/style invariants.
BSL stores state transitions (crossings) and derives stable signatures and
compact bias vectors that can be used by memory and IVM layers.
```

#### Function: `_clamp()`

#### Class: `Strand`

#### Class: `Crossing`

#### Class: `SubstrateEvent`

#### Class: `BraidState`

#### Class: `BraidedSubstrateLayer`
> BSL engine for event→crossing, reduction, signature, and vector output.

- **Method:** `__init__()`
- **Method:** `_default_strands()`
- **Method:** `update()`
- **Method:** `snapshot()`
- **Method:** `dominant_crossings_summary()`
- **Method:** `_event_to_crossings()`
- **Method:** `_resolve_signal()`
- **Method:** `_reduce()`
- **Method:** `_refresh_state()`
- **Method:** `_group_vector()`
- **Method:** `_compute_stability()`
- **Method:** `_compute_signature()`

### File: `./aurora_core_ai/aurora_internal/aurora_capability_assimilator.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_capability_assimilator.py
─────────────────────────────────────────────────────────────────────────────
Wires new capabilities into the genealogy fossil record and dream training
curriculum so every evolution layer is known to the training system.

Three registration pathways:

  1. Frontier ops (ExistenceBoundaryAgencyGate etc.)
     → register_manual_code_assimilation()
     Each covers a 3-axis combo that was completely absent from the descriptor
     pool. The genealogy now tracks them as new abilities.

  2. Gen-2 evolved surfaces
     → register_code_evolution_outcome()
     Evolved surfaces are code evolution outcomes that were accepted (they
     passed the autoevolver's simulation gate before being written).

  3. Compound axes (from AxisEmergenceDetector)
     → register_manual_code_assimilation()
     Each new compound axis channel is a structural capability that emerged
     from observed pressure co-occurrence.

  4. Dream curriculum seeding
     → FailPointLedger.record_fail() for under-represented dimensions
     Dimensions that map to the new capability spaces get seed fail signals
     so the dream curriculum prioritises training sessions that exercise them.

Deduplication:
  All methods are idempotent — already-registered abilities are skipped.
  A lightweight bloom-set is persisted at aurora_state/assimilated_ids.json.
─────────────────────────────────────────────────────────────────────────────
```

#### Class: `CapabilityAssimilator`
> Registers all new capabilities into the genealogy and dream curriculum.

- **Method:** `__init__()`
- **Method:** `assimilate_all()`
  - *Purpose:* Run all assimilation steps. Safe to call multiple times.
- **Method:** `assimilate_frontier_ops()`
  - *Purpose:* Register the 4 frontier operation classes into the genealogy
- **Method:** `assimilate_gen2_surfaces()`
  - *Purpose:* Register all gen-2 evolved surfaces into the genealogy as code
- **Method:** `assimilate_compound_axes()`
  - *Purpose:* Register any newly emerged compound axes into the genealogy.
- **Method:** `seed_curriculum()`
  - *Purpose:* Seed the dream curriculum with training priorities for the new
- **Method:** `_load_surface_registry()`
- **Method:** `_load_compound_axes()`
- **Method:** `_load_assimilated()`
- **Method:** `_save_assimilated()`

### File: `./aurora_core_ai/aurora_internal/aurora_code_autoevolver.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CODE AUTO-EVOLVER
========================
Applies constrained code mutations, runs simulation-gated selection,
and rolls back rejected mutations.
```

#### Class: `CodeAutoEvolver`
- **Method:** `__init__()`
- **Method:** `_file_stamp()`
- **Method:** `_descriptor_cache_stamp()`
- **Method:** `apply_operator()`
- **Method:** `_timing_axes_for_path()`
- **Method:** `_timing_role_for_path()`
- **Method:** `rollback()`
- **Method:** `_build_update_plan()`
- **Method:** `_target_paths()`
- **Method:** `_abs_path()`
- **Method:** `_clean_rel_path()`
- **Method:** `_module_name_from_relpath()`
- **Method:** `_load_descriptor_state()`
- **Method:** `_operation_chain()`
- **Method:** `_sanitize_identifier()`
- **Method:** `_surface_method_name()`
- **Method:** `_surface_score()`
- **Method:** `_empty_slot_channels()`
  - *Purpose:* Return the set of NC channels that appear in at least one unoccupied
- **Method:** `_empty_slot_pressure()`
  - *Purpose:* Score bonus for operations whose active axis signature projects into
- **Method:** `_fallback_effect_modes()`
- **Method:** `_ensure_developmental_effect_history()`
- **Method:** `_load_genealogy_artifacts()`
- **Method:** `_operation_axis_counts()`
- **Method:** `_counts_to_signature()`
- **Method:** `_signature_to_counts()`
- **Method:** `_counts_similarity()`
- **Method:** `_genealogy_strategy()`
- **Method:** `_development_alignment()`
- **Method:** `_feedback_pressure()`
- **Method:** `_rewrite_family_feedback()`
- **Method:** `_select_reflection_candidates()`
- **Method:** `_build_developmental_surface_updates()`
- **Method:** `_can_activate_override()`
- **Method:** `_can_bind_latent_projection()`
- **Method:** `_strip_generated_block()`
- **Method:** `_name_taken()`
- **Method:** `_preferred_native_export_name()`
- **Method:** `_rewrite_profile_for_module()`
- **Method:** `_resolve_module_target()`
- **Method:** `_infer_contract_profile()`
- **Method:** `_native_strategy_payload()`
- **Method:** `_native_wrapper_block()`
- **Method:** `_build_native_projection_updates()`
- **Method:** `_latent_meta()`
- **Method:** `_reflection_meta()`
- **Method:** `_render_evolved_surfaces()`
- **Method:** `_module_template()`
- **Method:** `_update_descriptor_state()`
- **Method:** `_update_descriptor_state_with_native_projections()`
- **Method:** `_apply_telemetry_probe()`
- **Method:** `_apply_agency_surface()`

### File: `./aurora_core_ai/aurora_internal/aurora_code_evolution_chamber.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CODE EVOLUTION CHAMBER
=============================
Constraint-native evolutionary scoring for code representation.

This layer mirrors chamber doctrine for code:
  pressure_before -> mutation trace -> pressure_after -> relief decision

The same five constraints are applied at code level:
  X existence : syntax/admissibility integrity
  T temporal  : change stability and replay risk pressure
  N energy    : complexity and maintenance cost pressure
  B boundary  : coupling/interface pressure
  A agency    : adaptive steering pressure (ability to evolve safely)
```

#### Function: `_clamp()`

#### Function: `_mean()`

#### Function: `_rounded()`

#### Function: `_normalize_constraints()`

#### Class: `CodeMutationTrace`
- **Method:** `to_dict()`

#### Class: `CodePressureVec`
- **Method:** `to_dict()`
- **Method:** `relief_from()`
- **Method:** `max_relief()`
- **Method:** `sum_positive_relief()`

#### Class: `CodePressureSnapshot`
- **Method:** `to_dict()`

#### Class: `CodeLink`
- **Method:** `to_dict()`

#### Class: `CodeEvolutionConfig`

#### Class: `CodePressureGovernor`
> Adaptive pressure regulator for code evolution selection/promotion.

- **Method:** `__init__()`
- **Method:** `tolerance_factor()`
- **Method:** `effective_relief_thresholds()`
- **Method:** `effective_kmin()`
- **Method:** `effective_acceptance_floor()`
- **Method:** `effective_net_min()`
- **Method:** `stagnation_gain()`
- **Method:** `update_after_event()`
- **Method:** `register_promotion()`
- **Method:** `report()`

#### Class: `CodeConstraintEvaluator`
> Measure code-level X/T/N/B/A pressure from repository state.

- **Method:** `__init__()`
- **Method:** `snapshot()`
- **Method:** `_collect_python_files()`
- **Method:** `_compute_metrics()`
- **Method:** `_local_module_names()`
- **Method:** `_compute_file_metrics()`
- **Method:** `_module_key_for_path()`
- **Method:** `_subsystem_key_for_path()`
- **Method:** `_aggregate_pressure_groups()`
- **Method:** `_pressure_from_metrics()`

#### Class: `CodeEvolutionChamber`
> Constraint-native evolutionary chamber for code-level mutations.

- **Method:** `__init__()`
- **Method:** `snapshot()`
- **Method:** `propose_mutation()`
- **Method:** `observe_mutation()`
- **Method:** `evaluate_mutation()`
- **Method:** `_timing_feedback_payload()`
- **Method:** `_mutation_cost()`
- **Method:** `_promote_links()`
- **Method:** `_promote_links_with_stats()`
- **Method:** `_semantic_lane_for_axis()`
- **Method:** `_target_scope()`
- **Method:** `_rank_axes()`
- **Method:** `_pressure_transition()`
- **Method:** `_metric_deltas()`
- **Method:** `_group_deltas()`
- **Method:** `_ancestor_chain()`
- **Method:** `_ensure_growth_reflection_history()`
- **Method:** `_collect_descendants()`
- **Method:** `_lineage_ripple()`
- **Method:** `_effect_classification()`
- **Method:** `_build_effective_history()`
- **Method:** `summary()`
- **Method:** `lineage_report()`
- **Method:** `flush_files()`
- **Method:** `guidance_payload()`
  - *Purpose:* Cross-scale guidance payload compatible with runtime steering hook.
- **Method:** `_update_operator_gradients()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `develop_agency()`

### File: `./aurora_core_ai/aurora_internal/aurora_code_mutation_operators.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CODE MUTATION OPERATORS
==============================
Canonical mutation operator catalog for code-level evolution.
```

#### Class: `CodeMutationOperator`

#### Function: `list_operator_specs()`

#### Function: `get_operator()`

### File: `./aurora_core_ai/aurora_internal/aurora_comprehension_gap.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_comprehension_gap.py
============================
Aurora's living comprehension gap system.

When Aurora doesn't understand something — a word, a reference, a sentence
structure, slang, an implied meaning — she doesn't just fall through to a
template. She:

  1. Recognizes exactly what she doesn't understand (VolatilityDetector)
  2. Names the gap with precision (ComprehensionGapDetector)
  3. Asks a specific, targeted question (ClarificationMemory)
  4. Receives the answer and applies it to the right system (GapResolutionApplicator)

The critical property: the answer actually CLOSES the gap.
  - A vocabulary gap resolution → adds the word to lexicon + OETS with real meaning
  - A structural gap resolution → adds the clarified pattern to the template pool
  - A referent gap resolution → updates working memory so she knows what "it" means
  - A slang resolution → adds the informal form with correct role and register
  - An intent gap resolution → updates her comprehension model for that pattern type

This is not a conversation game. Each gap resolved makes Aurora genuinely
more capable of understanding that type of input in every future conversation.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `GapType`

#### Class: `ComprehensionGap`
> A specific, named gap in Aurora's understanding of an input.


#### Class: `ResolvedGap`
> A gap that has been answered and applied to Aurora's systems.


#### Class: `VolatilityDetector`
> Analyzes input text for communication volatility — the ways language

- **Method:** `detect()`
  - *Purpose:* Analyze text for volatility signals.

#### Class: `ComprehensionGapDetector`
> From a volatility report, generates specific, nameable comprehension gaps.

- **Method:** `detect_gaps()`
  - *Purpose:* Generate a list of comprehension gaps from the volatility report.
- **Method:** `should_ask()`
  - *Purpose:* Decide whether Aurora should ask a clarifying question.

#### Class: `ClarificationMemory`
> Tracks Aurora's pending comprehension gaps and their resolutions.

- **Method:** `__init__()`
- **Method:** `has_pending()`
  - *Purpose:* Is there a gap waiting for an answer?
- **Method:** `get_pending()`
- **Method:** `record_gap()`
  - *Purpose:* Store a gap as pending.
- **Method:** `can_ask_again()`
  - *Purpose:* Don't interrogate — enforce a turn gap between questions.
- **Method:** `receive_answer()`
  - *Purpose:* Mark the pending gap as answered.
- **Method:** `store_resolved()`
- **Method:** `stats()`
- **Method:** `save()`
- **Method:** `load()`

#### Class: `GapResolutionApplicator`
> Routes a resolved gap's answer to the right system(s).

- **Method:** `apply()`
  - *Purpose:* Apply the resolved gap's answer to Aurora's systems.
- **Method:** `_apply_vocabulary()`
  - *Purpose:* User told Aurora what a word means.
- **Method:** `_apply_slang()`
  - *Purpose:* User explained what a slang term means.
- **Method:** `_apply_referent()`
  - *Purpose:* User clarified what 'it', 'this', 'that' referred to.
- **Method:** `_apply_structural()`
  - *Purpose:* User simplified a complex sentence structure.
- **Method:** `_apply_ellipsis()`
  - *Purpose:* User stated the implied meaning explicitly.
- **Method:** `_apply_intent()`
  - *Purpose:* User clarified their intent. Store and absorb.
- **Method:** `_apply_general()`
- **Method:** `_extract_definition()`
  - *Purpose:* Extract the definition part from an answer like "X means Y" or "X is Y".
- **Method:** `_extract_subject()`
  - *Purpose:* Extract what something refers to from a clarification sentence.

#### Class: `ComprehensionGapSystem`
> The unified interface for Aurora's comprehension gap handling.

- **Method:** `__init__()`
- **Method:** `process()`
  - *Purpose:* Main entry point. Call this at the start of each turn.
- **Method:** `_build_acknowledgment()`
  - *Purpose:* Build an acknowledgment that shows Aurora actually absorbed the answer —
- **Method:** `_looks_like_confusion()`
  - *Purpose:* Return True if Aurora's last response contained confusion signals.
- **Method:** `_looks_like_correction()`
  - *Purpose:* Return True if the user's input looks like a correction or explanation.
- **Method:** `_absorb_user_correction()`
  - *Purpose:* Build a synthetic ComprehensionGap from a user correction/explanation
- **Method:** `save()`
- **Method:** `stats()`

#### Function: `run_tests()`
> Verify the comprehension gap system works end to end.


### File: `./aurora_core_ai/aurora_internal/aurora_constraint_manifold_patched.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CONSTRAINT MANIFOLD — LAYER -1
======================================

The mathematical foundation beneath all existence.
This is not ontology. This is physics.

The Five Fundamental Constraints define a closed 5-dimensional universe:
    𝒞 = {X, T, N, B, A}

Where:
    X = Existence   (admissibility predicate)
    T = Time        (configuration across sequence)
    N = Energy/Cost (resource redistribution)
    B = Boundary    (differentiation/containment)
    A = Agency      (independent action magnitude)

These are the ONLY lawful axes. No sixth dimension exists.

MANIFOLD DEFINITION:
    𝓜₅ = {(T,N,B,A) | X > 0}

Existence X is not a coordinate — it is the admissibility condition.
If X ≤ 0, the manifold collapses.

STRUCTURAL INDEXING (5×5×5×5×5):
    Every process is indexed across:
        5 constraints (𝒞)
        × 5 compositional spaces (𝒮)
        × 5 states (Σ)
        × 5 recursion levels (ℒ)
    → measured as 5-degree vectors

    𝓕(c,s,σ,ℓ) = [dX, dT, dN, dB, dA]

ENERGY LAW:
    Total energy conserved: N_tot(t) > 0
    Distribution: Σ_p N_p(t) = N_tot(t)
    Cost of operation: Cost(o,t) = Σ_p w_p · φ(𝓕_p(t))

INTELLIGENCE CRITERION:
    A system earns intelligence in constraint C iff:
    1. A gradient inversion exists: ∃r* : sign(dΦ_C/dr) changes
    2. The policy adapts: π_C(r > r*) ≠ π_C(r < r*)

    Intelligence = curvature-aware adaptation under constraint pressure.

This module implements the constraint manifold as a computable structure.

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Class: `Constraint`
> The five fundamental constraints.

- **Method:** `all()`
  - *Purpose:* Return all five constraints in order.
- **Method:** `names()`
  - *Purpose:* Return constraint names.

#### Class: `ConstraintVector`
> A 5-dimensional vector in constraint space.

- **Method:** `__post_init__()`
  - *Purpose:* Verify admissibility on construction.
- **Method:** `to_array()`
  - *Purpose:* Convert to numpy array [X, T, N, B, A].
- **Method:** `from_array()`
  - *Purpose:* Construct from numpy array.
- **Method:** `magnitude()`
  - *Purpose:* L2 norm in constraint space.
- **Method:** `__add__()`
  - *Purpose:* Vector addition in constraint space.
- **Method:** `__sub__()`
  - *Purpose:* Vector subtraction in constraint space.
- **Method:** `__mul__()`
  - *Purpose:* Scalar multiplication.
- **Method:** `dot()`
  - *Purpose:* Dot product in constraint space.
- **Method:** `in_span()`
  - *Purpose:* Check if this vector lies in the span of the five constraints.
- **Method:** `__repr__()`

#### Class: `ManifoldViolation`
> Raised when a configuration violates the constraint manifold's laws.


#### Class: `CompositionalSpace`
> The five compositional spaces.

- **Method:** `all()`

#### Class: `State`
> The five fundamental states.

- **Method:** `all()`

#### Class: `RecursionLevel`
> The five recursion depths.

- **Method:** `all()`

#### Class: `ConstraintFieldIndex`
> An index into the 5×5×5×5×5 constraint field.

- **Method:** `to_tuple()`
  - *Purpose:* Convert to flat tuple for indexing.
- **Method:** `__repr__()`

#### Class: `ConstraintField`
> The 5×5×5×5×5 constraint field tensor.

- **Method:** `__init__()`
- **Method:** `measure()`
  - *Purpose:* Measure the constraint field at a given index.
- **Method:** `update()`
  - *Purpose:* Update the field at a given index.
- **Method:** `total_energy()`
  - *Purpose:* Compute total energy across all field positions.
- **Method:** `capacity()`
  - *Purpose:* Return the theoretical capacity of the field.
- **Method:** `occupied_count()`
  - *Purpose:* Return number of occupied field positions.
- **Method:** `__repr__()`

#### Class: `EnergyDistribution`
> Tracks energy distribution across processes.

- **Method:** `__post_init__()`
- **Method:** `allocate()`
  - *Purpose:* Allocate energy to a process.
- **Method:** `drain()`
  - *Purpose:* Drain energy from a process.
- **Method:** `_drain_proportionally()`
  - *Purpose:* Drain deficit proportionally from all processes.
- **Method:** `verify_conservation()`
  - *Purpose:* Verify energy conservation law.
- **Method:** `compute_cost()`
  - *Purpose:* Compute cost of operation given constraint weights.

#### Class: `EquilibriumState`
> Every process has equilibrium structure:

- **Method:** `total()`
  - *Purpose:* Return total field: 𝓕 = 𝓕^(q) + 𝓕^(e).
- **Method:** `variance_magnitude()`
  - *Purpose:* Return ‖𝓕^(e)‖.
- **Method:** `is_stable()`
  - *Purpose:* Check if variance is within bounds: ‖Δ𝓕^(e)‖ ≤ ε.
- **Method:** `mutate_expression()`
  - *Purpose:* Attempt to mutate the expression layer.

#### Class: `ConstraintPressure`
> Models constraint pressure as a function of recursion depth.

- **Method:** `evaluate()`
  - *Purpose:* Evaluate Φ_C(r).
- **Method:** `gradient()`
  - *Purpose:* Numerical derivative: dΦ_C/dr
- **Method:** `curvature()`
  - *Purpose:* Second derivative: d²Φ_C/dr²
- **Method:** `find_inversion()`
  - *Purpose:* Find gradient inversion point r* where sign(dΦ_C/dr) changes.
- **Method:** `has_intelligence()`
  - *Purpose:* Check if this constraint exhibits intelligence.

#### Class: `IntelligencePolicy`
> Adaptive policy that responds to constraint pressure curvature.

- **Method:** `__init__()`
- **Method:** `set_inversion()`
  - *Purpose:* Set the detected inversion point.
- **Method:** `policy()`
  - *Purpose:* Return policy at recursion depth r.
- **Method:** `is_intelligent()`
  - *Purpose:* Check if policy exhibits intelligence.

#### Function: `compute_intelligence_capacity()`
> Compute total intelligence capacity.


#### Function: `verify_constraint_manifold()`
> Verify the constraint manifold's structural integrity.


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `constraintpressure_evolved()`

#### Function: `energydistribution_evolved()`

### File: `./aurora_core_ai/aurora_internal/aurora_conversation_episode_compiler.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CONVERSATION EPISODE COMPILER
========================================
Reads conversation JSON (same format as corpus_runner.py) and compiles
persistent dream episode packs.

Each pack contains 10 conversation threads organized by rubric pressure
profile — NOT by topic bins. The compiler runs AHEAD of dream execution
so the dream loop never reprocesses the full JSON.

Output: stored DreamEpisodePack objects ready for SimulationEngine consumption.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `ConversationPayload`
> One conversation thread packaged for dream consumption.

- **Method:** `__post_init__()`

#### Class: `DreamEpisodePack`
> A compiled dream episode pack: 10 conversation threads selected

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Function: `_extract_message_text()`
> Extract text from a message object (OpenAI format).


#### Function: `_extract_role()`

#### Function: `_reconstruct_linear_thread()`
> Non-recursive reconstruction of main conversation chain from OpenAI DAG.


#### Function: `_extract_messages_from_conversation()`
> Extract (role, text) pairs from a conversation object.


#### Function: `_load_conversations()`
> Load conversation JSON (same format as corpus_runner).


#### Function: `_generate_id()`

#### Function: `_select_weakness_targeted()`
> Select conversations that stress specific weak dimensions.


#### Function: `_select_balanced()`
> Select a balanced mix across rubric dimensions.


#### Function: `_select_stress_test()`
> Select the most difficult conversations overall.


#### Class: `ConversationEpisodeCompiler`
> Compiles conversation JSON into persistent dream episode packs.

- **Method:** `__init__()`
- **Method:** `compile_from_json()`
  - *Purpose:* Read conversation JSON, score through rubric, compile into episode packs.
- **Method:** `_build_pack()`
  - *Purpose:* Build a single episode pack from selected payloads.
- **Method:** `_save_packs()`
  - *Purpose:* Persist compiled packs to storage.
- **Method:** `load_pack()`
  - *Purpose:* Load a previously compiled pack from storage.
- **Method:** `list_available_packs()`
  - *Purpose:* List all compiled packs from the manifest.
- **Method:** `compiled_pack_count()`
- **Method:** `scored_conversation_count()`

### File: `./aurora_core_ai/aurora_internal/aurora_conversation_rubric_engine.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CONVERSATION RUBRIC ENGINE
====================================
Scores conversation threads along communicative-development dimensions.

NOT topic labels. NOT category bins.
Rubric dimensions measure COMMUNICATIVE COMPETENCE:
  coherence, context carryover, ambiguity handling, repair, calibration, etc.

Each conversation gets a multi-dimensional rubric score that captures
WHERE Aurora's communicative processing is strong or weak.

These scores feed into dream episode compilation so dreams target
actual developmental gaps instead of random topics.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_zero_rubric()`

#### Function: `_clamp()`

#### Function: `_content_terms()`

#### Function: `_semantic_anchor()`

#### Function: `_semantic_terms()`

#### Function: `_semantic_overlap()`

#### Function: `_anchor_overlap()`

#### Function: `_causal_score()`

#### Function: `_callback_like()`

#### Function: `_generic_response_penalty()`

#### Class: `ConversationRubricScore`
> Rubric assessment of one conversation thread.

- **Method:** `weakest_dimensions()`
  - *Purpose:* Return the n lowest-scoring dimensions.
- **Method:** `strongest_dimensions()`
  - *Purpose:* Return the n highest-scoring dimensions.
- **Method:** `mean_score()`
- **Method:** `to_dict()`

#### Function: `_word_overlap()`
> Jaccard overlap of word sets.


#### Function: `_has_question()`

#### Function: `_sentence_count()`

#### Function: `_avg_sentence_length()`

#### Function: `_hedging_score()`
> Detect uncertainty signaling language.


#### Function: `_contradiction_markers()`
> Detect contradiction handling language.


#### Function: `_repair_markers()`
> Detect misunderstanding repair language.


#### Function: `_perspective_markers()`
> Detect perspective integration language.


#### Class: `ConversationRubricEngine`
> Scores conversation threads along communicative-development rubric dimensions.

- **Method:** `__init__()`
- **Method:** `score_conversation()`
  - *Purpose:* Score a single conversation thread along all rubric dimensions.
- **Method:** `score_batch()`
  - *Purpose:* Score a batch of conversations.
- **Method:** `scored_count()`

### File: `./aurora_core_ai/aurora_internal/aurora_cost_diff_score.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA COST-DIFF SCORE — CROSS-DIMENSIONAL PRESSURE SCORING
=============================================================

Layer 1  (sits between DifferenceBuffer and all scored entities)

WHAT THIS MODULE IS:
    The unified scoring engine that fuses a structure's base energy cost
    with the live Difference channel to produce one authoritative number:
    the CostDiffScore. This score reflects both what something costs to
    operate AND how much cross-dimensional pressure the system is currently
    under — making it a live, context-sensitive measure rather than a
    static accounting entry.

THE PROBLEM THIS SOLVES:
    Base cost tells you what an ability, link, or variant costs in a
    calm system. But the system is not always calm. When the admissibility
    boundary is drifting (X:D), when temporal momentum is shifting (T:D),
    when energy is redistributing away from the field mean (N:D), when
    structural topology is displaced from rest (B:D), when agency is
    eroding (A:D) — these conditions change what it *actually costs* to
    operate any structure in that environment. The CostDiffScore captures
    this reality.

THE PHYSICS — OPERATOR-TYPED PRESSURE:
    Each constraint's Difference value is NOT a generic alarm — it is
    a TYPED pressure whose meaning is disclosed by that constraint's
    operator:

    X (existence gate, unsigned, prior_self 1t):
        C:D = admissibility boundary drift.
        When X is drifting, the predicate that governs what can be
        represented in the system is shifting. Everything that operates
        under X pays a hidden cost — the ground it stands on is moving.
        Pressure weight is the smallest (X costs least per unit shift)
        but its scope is global: even a small X:D affects every layer.

    T (time arrow, signed, prior_self 4t):
        C:D = temporal momentum change.
        Positive: tick cost is accelerating — persistence is becoming
        more expensive. Negative: decelerating — tick cost is easing.
        Only the magnitude matters for cost pressure (both directions
        increase operating cost). T has low time_constant (0.3) — it
        responds and recovers quickly.

    N (energy conservation, signed, peer_mean 4t):
        C:D = energy redistribution pressure.
        N is the conserved constraint: if N is significantly above or
        below the peer mean, redistribution is already happening. Any
        structure operating under significant N:D is paying an implicit
        tax — the energy field is not level. N's pressure weight is
        moderate (cost 10×) and its effect is field-wide.

    B (boundary topology, unsigned, background 8t):
        C:D = topological displacement pressure.
        When B is displaced from its architectural rest (0.45), structure
        is being built or dissolved. Structural change propagates through
        all layers below it. B has the second-highest pressure weight
        (cost 40×) — structural drift is expensive and slow to recover.

    A (agency control, signed, prior_self 8t):
        C:D = directional agency pressure.
        Positive: agency is growing — the system is investing in
        complexity and control, which cascades cost through T, N, B.
        Negative: agency is eroding — directional capacity is being
        lost, which may increase entropy pressure elsewhere.
        A has the highest pressure weight (cost 150×) and the longest
        drift window — agency shifts are the most consequential.

CROSS-DIMENSIONAL AMPLIFIER:
    amplifier = 1.0 + Σ_c (OP_PRESSURE_WEIGHT[c] × |C:D[c]|) / 5.0

    This is normalised by 5 (the constraint count) so that the amplifier
    ranges from 1.0 (no drift across any constraint) to approximately
    1.54 (maximum drift across all five simultaneously). The amplifier
    is a multiplier on base_cost — it never reduces it. The score is
    always ≥ base_cost.

    Maximum amplifier: 1 + mean(0.1382, 0.3208, 0.4779, 0.7402, 1.0000)
                     = 1 + 0.5354
                     = 1.5354

    This means even under maximum cross-dimensional pressure, the cost
    amplification is bounded at ~54%. Non-dominant. Meaningful.

COST-DIFF SCORE FORMULA:
    live_score = base_cost × amplifier

WHAT USES THIS:
    - StrandBead.cost_diff_score(snapshot)    → live bead cost
    - DNAStrand.cost_diff_score_total(snapshot) → live strand cost
    - AbilityProfile.cost_diff_score(snapshot)  → live ability cost
    - ConstraintLink.cost_diff_score(snapshot)  → live link cost
    - VariantPromoter (moral weight amplification on promotion)

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Function: `_derive_pressure_weights()`
> Derive per-constraint operator pressure weights from registry physics.


#### Function: `_update_pressure_mutation()`

#### Function: `pressure_mutation_state()`

#### Function: `_k_efficiency_ratio()`

#### Function: `cross_dim_amplifier()`
> Compute the cross-dimensional pressure amplifier from a DifferenceSnapshot.


#### Function: `_reactive_pressure_score()`
> Whole-number reactive pressure score for per-tick X responsiveness.


#### Function: `per_operator_pressure()`
> Return per-operator effective pressure contributions from a DifferenceSnapshot.


#### Function: `dominant_pressure_axis()`
> Return the constraint contributing the most operator pressure.


#### Class: `CostDiffScore`
> A live, context-sensitive cost score for any Aurora structure.

- **Method:** `to_dict()`
- **Method:** `describe()`

#### Function: `score_from_cost()`
> Compute a CostDiffScore from a base cost and optional DifferenceSnapshot.


#### Function: `score_for_variant_moral_weight()`
> Compute the cross-dimensionally amplified moral weight for a variant.


#### Function: `pressure_description()`
> Return a human-readable description of the cross-dimensional pressure


#### Function: `verify_cost_diff_score()`
> Checks:


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `derive_pressure_weights_evolved()`

#### Function: `k_efficiency_ratio_evolved()`

#### Function: `reactive_pressure_score_evolved()`

#### Function: `cross_dim_amplifier_evolved()`

#### Function: `dominant_pressure_axis_evolved()`

#### Function: `per_operator_pressure_evolved()`

#### Function: `pressure_description_evolved()`

#### Function: `score_for_variant_moral_weight_evolved()`

#### Function: `score_from_cost_evolved()`

#### Function: `verify_cost_diff_score_evolved()`

### File: `./aurora_core_ai/aurora_internal/aurora_difference_buffer.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA DIFFERENCE BUFFER — THE FIFTH LENS LIVE FEED
=====================================================

Layer 0.5  (sits between the NonComp Registry and the Worth Evaluator)

WHAT THIS MODULE IS:
    The rolling history engine that makes the Difference (Δ) channel
    operationally real. The NonComp Registry defines the five C:D
    Non-Comps and the compute_difference() utility, but it deliberately
    does not hold history — it has no concept of time. This module holds
    the time.

    Every tick, the system calls DifferenceHistoryBuffer.record() with
    the current per-constraint magnitudes. When a C:D snapshot is needed
    (by the worth evaluator, by evidence generation, by the evolution
    chamber), the buffer resolves the correct reference_magnitude per
    constraint and returns a DifferenceSnapshot with all five C:D values.

THE THREE REFERENCE TYPES (per DifferenceParams.ref_type):

    'prior_self'  — Compare to self N ticks ago.
                    Reference = magnitude at tick (current_tick − window_ticks).
                    If history is shorter than window_ticks, use the earliest
                    recorded magnitude (graceful warm-up behaviour — not an error).
                    Used by: X (1t), T (4t), A (8t)

    'peer_mean'   — Compare to the mean of the other four constraints'
                    current magnitudes at this tick.
                    Reference = mean(magnitudes[c'] for c' ≠ c)
                    Used by: N (4t window irrelevant for reference; used for
                    averaging history to smooth the peer signal)

    'background'  — Compare to a fixed architectural resting topology.
                    Reference = B_BACKGROUND_REST (0.45).
                    This constant is derived from the registry physics:
                    baseline_budget / shift_cost_coeff = 18.0 / 40.0 = 0.45
                    It represents the magnitude at which the boundary layer's
                    per-tick maintenance budget exactly covers one shift-cost
                    unit — the minimum viable structural investment.
                    Used by: B

THE DIFFERENCE SNAPSHOT:
    DifferenceSnapshot holds all five C:D values at one tick.
    Each value is a float in [−1, +1].

        Unsigned (X, B):  only drift magnitude matters, not direction.
                          Value is always ≥ 0.
        Signed   (T, N, A): direction carries meaning.
                          Positive = growth / over-spending / acceleration.
                          Negative = decay / under-spending / deceleration.

    The snapshot is the first-class evidence input to downstream systems:
        - aurora_worth_evaluator.py (appended to WorthReport)
        - aurora_evolution_chamber.py (evidence feed for promotion)
        - constraint_genealogy.py (relief event annotation)

WARM-UP BEHAVIOUR:
    On the first few ticks before any history is recorded, prior_self
    references fall back to the earliest available magnitude. During
    warm-up the C:D value is 0.0 (no detectable drift yet) — which is
    correct: if there is no history there is no measurable difference.
    The system does not flag warm-up as an error.

INTEGRATION:
    Instantiate one DifferenceHistoryBuffer per system instance.
    Call record() once per tick with the current magnitudes.
    Call snapshot() when a DifferenceSnapshot is needed.

    Typical call pattern (inside the evolution chamber tick loop):
        buf.record(tick, accountant.magnitudes())
        snap = buf.snapshot(tick, accountant.magnitudes())
        # pass snap to worth evaluator, evidence pipeline, etc.

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `DifferenceSnapshot`
> The five C:D (Constraint:Difference) values at one tick.

- **Method:** `value()`
  - *Purpose:* Return the C:D value for constraint c.
- **Method:** `magnitude()`
  - *Purpose:* Return the reference magnitude used for constraint c.
- **Method:** `alarming()`
  - *Purpose:* Return constraints whose |C:D| exceeds threshold.
- **Method:** `max_alarm()`
  - *Purpose:* Return (constraint, |value|) for the most alarming C:D.
- **Method:** `to_dict()`
  - *Purpose:* Serialisable dict for JSONL logging.
- **Method:** `describe()`
  - *Purpose:* Compact human-readable description.

#### Class: `DifferenceHistoryBuffer`
> Rolling magnitude history engine — the temporal backbone of the

- **Method:** `__init__()`
- **Method:** `record()`
  - *Purpose:* Record the current magnitude snapshot for this tick.
- **Method:** `snapshot()`
  - *Purpose:* Compute a DifferenceSnapshot for the given tick.
- **Method:** `_resolve_reference()`
  - *Purpose:* Resolve reference_magnitude for constraint c.
- **Method:** `_prior_self()`
  - *Purpose:* Return (magnitude at window ticks ago, warm_up_flag).
- **Method:** `_peer_mean()`
  - *Purpose:* Return the mean of the other four constraints' current magnitudes.
- **Method:** `depth()`
  - *Purpose:* Number of ticks currently in history.
- **Method:** `is_warm()`
  - *Purpose:* True once the buffer has accumulated enough history that NO
- **Method:** `latest_tick()`
  - *Purpose:* Tick number of the most recently recorded snapshot.
- **Method:** `reset()`
  - *Purpose:* Clear all history. Use on system restart.

#### Function: `make_difference_buffer()`
> Create a DifferenceHistoryBuffer with the canonical B resting value.


#### Function: `verify_difference_buffer()`
> Verify DifferenceHistoryBuffer and DifferenceSnapshot integrity.


### File: `./aurora_core_ai/aurora_internal/aurora_directed_training_corpus.py`

**Type:** Python Source

**Module Docstring:**
```text
Directed training prompt bridge for aurora_internal/train.txt.

The raw file is a large generic corpus. This module turns it into a
dimension-directed prompt source for dream avatars and simulation lesson
specs by:
  - extracting lines that match rubric-dimension keyword clusters
  - caching a small prompt pool per dimension
  - shaping those lines into direct training prompts and follow-ups
```

#### Class: `DirectedTrainingCorpusBridge`
- **Method:** `__init__()`
- **Method:** `_source_signature()`
- **Method:** `_normalize_line()`
- **Method:** `_usable_line()`
- **Method:** `_score_line()`
- **Method:** `_load_cache()`
- **Method:** `_save_cache()`
- **Method:** `_build_cache()`
- **Method:** `_ensure_cache()`
- **Method:** `prompt_pack()`
- **Method:** `samples_for_dimensions()`

#### Function: `get_directed_training_corpus_bridge()`

### File: `./aurora_core_ai/aurora_internal/aurora_dna_strand_schema.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA DNA STRAND SCHEMA — STEP 14
=====================================
Formal constraint-operator event chain format.

WHAT A DNA STRAND IS:
    A DNA strand is the formal, serialisable record of HOW a VariantRecord
    came to exist — the full causal chain of constraint events from the
    moment the intake first arrived (Step 9) through Worth evaluation (Step 10),
    Solidification (Step 11), and Variant Promotion (Step 13).

    Each event in the strand is a StrandBead:
        {constraint, non_comp_channel, direction, layer_depth, tick,
         magnitude_delta, polarity_state}

    The sequence of StrandBeads is the DNA strand. It describes the exact
    path through constraint space that this variant took to crystallize.

    A strand is NOT a trace log. It is NOT a debug record. It is the
    genetic memory of a first-class variant — the chain of constraint
    events that, when they recur in the same order, cause the system to
    respond faster and cheaper (because the path is worn).

NON-COMP CHANNEL:
    Each bead identifies which of the five representational dimensions
    of the constraint was the primary channel:
        P    = POLARITY    — the toroidal phase gradient shifted
        M    = MAGNITUDE   — the activation intensity changed
        O    = OPERATOR    — the invariant transformation rule was applied
        D    = COST        — energy redistribution occurred
        DIFF = DIFFERENCE  — Δ channel event; deviation-from-reference signal fired

DIRECTION:
    Each bead has a direction relative to its constraint's I-State pair:
        POSITIVE = toward the affirmative pole (is, can, do, saw, did)
        NEGATIVE = toward the negative pole (isn't, can't, don't, saunt, didn't)
        NEUTRAL  = passing through the transition (polarity ≈ 0)

STRAND PROPERTIES:
    length       — number of beads (one per distinct constraint event)
    depth_span   — from shallowest to deepest constraint in the strand
    polarity_arc — net signed change in polarity from start to end of strand
    cost_total   — total energy consumed across all beads in this strand

STRAND LIBRARY:
    The StrandLibrary stores all active variant strands and supports:
        - signature matching: can a new event sequence match a known strand?
        - partial matching: does an incoming event sequence prefix-match?
        - strand degradation: unused strands decay in influence over time
          (measured in ticks without a match event)

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `NonCompChannel`
> Which Non-Comp representational dimension was the primary channel.


#### Class: `BeadDirection`
> Signed direction relative to the constraint's I-State pair.


#### Class: `StrandBead`
> One event in a DNA strand — the formal minimal event unit.

- **Method:** `__post_init__()`
- **Method:** `cost()`
  - *Purpose:* Energy cost of this bead's magnitude delta.
- **Method:** `cost_diff_score()`
  - *Purpose:* Live cost score fusing this bead's static cost with the current
- **Method:** `bead_id()`
  - *Purpose:* Compact deterministic ID for this bead.
- **Method:** `to_dict()`
- **Method:** `direction_from_polarity()`
  - *Purpose:* Classify a polarity value into a BeadDirection.

#### Class: `DNAStrand`
> The full ordered sequence of StrandBeads for one VariantRecord.

- **Method:** `append()`
  - *Purpose:* Add a bead to an unsealed strand.
- **Method:** `seal()`
  - *Purpose:* Seal the strand and compute all derived properties.
- **Method:** `length()`
- **Method:** `cost_total()`
- **Method:** `cost_diff_score_total()`
  - *Purpose:* Live total cost for this entire strand fused with cross-dimensional
- **Method:** `polarity_arc()`
- **Method:** `depth_span()`
- **Method:** `constraint_sequence()`
  - *Purpose:* Return the ordered list of constraints across all beads.
- **Method:** `signature()`
  - *Purpose:* Compact constraint+channel sequence signature for matching.
- **Method:** `prefix_matches()`
  - *Purpose:* True if other_beads is a prefix of this strand's bead sequence.
- **Method:** `to_jsonl_dict()`

#### Class: `StrandBuilder`
> Constructs a DNAStrand from a VariantRecord + a sequence of constraint events.

- **Method:** `build()`
  - *Purpose:* Build and seal a DNAStrand.

#### Class: `StrandLibrary`
> Stores all active variant DNA strands and supports matching operations.

- **Method:** `__init__()`
- **Method:** `register()`
  - *Purpose:* Add a sealed DNAStrand to the library.
- **Method:** `match_exact()`
  - *Purpose:* Return the active strand whose full bead sequence matches beads exactly.
- **Method:** `match_prefix()`
  - *Purpose:* Return all active strands for which beads is a valid prefix.
- **Method:** `_strand_strength()`
  - *Purpose:* Influence strength ∈ [0, 1]. Zero = archived (no longer matched).
- **Method:** `active_strand_count()`
- **Method:** `stats()`

#### Function: `make_strand_library()`

#### Function: `make_strand_builder()`

#### Function: `verify_dna_strand_schema()`
> Checks:


### File: `./aurora_core_ai/aurora_internal/aurora_doc.py`

**Type:** Python Source

#### Function: `fetch_share_link()`

#### Function: `extract_messages_from_html()`

#### Function: `write_transcript()`

#### Function: `main()`

### File: `./aurora_core_ai/aurora_internal/aurora_dpme_pressure_bridge.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_dpme_pressure_bridge.py
─────────────────────────────────────────────────────────────────────────────
Connects the evolutionary pressure system to the DPME (Dimensional Parameter
Metacognition Engine) so that axis imbalances detected by PressureParameterAdapter
directly influence DER facet energy corrections.

How it works:
  PressureParameterAdapter writes adapter_hints.json with evolver_bias_hints:
      {"energy": +0.1, "boundary": -0.05, "agency": +0.08, ...}
  Positive hint = axis is over-pressured and not relieving well → that
  capability domain needs more internal energy support.

  This bridge reads those hints, maps constraint axes to DER channels
  (vitality/processing/memory/emotional/creative), and calls
  set_external_pressure_guidance() so DPME.auto_correct() injects energy
  to the right category on its next heartbeat.

Axis → DER channel mapping (rationale):
  existence (X) → vitality    (core identity = system aliveness)
  temporal  (T) → processing  (temporal coherence = active computation)
  energy    (N) → processing  (resource management = processing throughput)
  boundary  (B) → memory      (boundary tracking = what to hold / not hold)
  agency    (A) → creative    (agency expression = generative choice-making)

Secondary channel: the second-highest-pressure axis also gets a boost at
half strength — matching DPME's existing secondary channel behavior.
─────────────────────────────────────────────────────────────────────────────
```

#### Class: `DPMEPressureBridge`
> Reads adapter_hints.json and translates axis pressure imbalances into

- **Method:** `__init__()`
- **Method:** `apply()`
  - *Purpose:* Read adapter hints, compute the highest-pressure axis, and push
- **Method:** `status()`
- **Method:** `_load_hints()`
- **Method:** `_der_to_axis_letter()`
  - *Purpose:* Reverse-map DER channel → primary axis letter for axis_stats lookup.

#### Function: `apply_pressure_to_dpme()`
> Module-level convenience. Call from aurora_runtime.py tick loop.


### File: `./aurora_core_ai/aurora_internal/aurora_dream_curriculum_queue.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA DREAM CURRICULUM QUEUE
=================================
Manages the queue of dream episode packs and selects the next pack
for dream execution based on developmental need.

Integrates with:
  - AutonomyEngine._build_dream_seed  (seed source)
  - AutonomyEngine._check_dreams      (dream trigger)
  - SimulationEngine.run_episode       (execution)

Selection logic:
  1. Weakness-targeted packs get priority when repeated failures exist
  2. Packs are consumed in difficulty-ascending order
  3. Successfully completed packs can be re-queued with higher difficulty
  4. Balanced packs fill gaps between targeted rounds

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `PackCompletionRecord`
> Record of a completed dream episode pack run.

- **Method:** `to_dict()`

#### Class: `DreamCurriculumQueue`
> Selects the next dream episode pack based on developmental priority.

- **Method:** `__init__()`
- **Method:** `_load_history()`
  - *Purpose:* Load completion history from disk.
- **Method:** `_save_history()`
  - *Purpose:* Persist completion history.
- **Method:** `build_queue()`
  - *Purpose:* Build the dream curriculum queue from available packs.
- **Method:** `next_pack()`
  - *Purpose:* Select and return the next dream episode pack.
- **Method:** `record_completion()`
  - *Purpose:* Record the outcome of a completed dream episode pack.
- **Method:** `compile_from_corpus()`
  - *Purpose:* Compile conversation JSON into episode packs and build queue.
- **Method:** `get_seed_from_pack()`
  - *Purpose:* Build a dream seed string from a pack's rubric profile.
- **Method:** `extract_episode_messages()`
  - *Purpose:* Extract messages from a specific conversation within a pack.
- **Method:** `queue_length()`
- **Method:** `completed_count()`
- **Method:** `current_pack_id()`

### File: `./aurora_core_ai/aurora_internal/aurora_dream_evolution_orchestrator.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA DREAM EVOLUTION ORCHESTRATOR
========================================
Ties together the full dream-coupled structural evolution pipeline:

  compile -> queue -> execute -> diagnose -> steer -> bridge -> evolve

This orchestrator is the single integration point between the existing
AutonomyEngine dream path and the new diagnostic/steering/genealogy
modules. It keeps the AutonomyEngine modifications minimal.

Runtime flow:
  1. pre_compile()  — compile corpus into episode packs (run once or on demand)
  2. build_seed()   — get next dream seed from curriculum queue
  3. post_episode() — full diagnostic pipeline after dream episode completes
  4. apply()        — push steering/evidence into DPME, genealogy, expression

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `DreamEvolutionOrchestrator`
> Single integration point for the dream-coupled structural evolution pipeline.

- **Method:** `__init__()`
- **Method:** `pre_compile()`
  - *Purpose:* Compile conversation corpus into dream episode packs.
- **Method:** `build_seed()`
  - *Purpose:* Get the next dream seed from curriculum queue.
- **Method:** `get_current_pack()`
  - *Purpose:* Return the current active dream episode pack.
- **Method:** `_extract_generated_threads()`
  - *Purpose:* Build rubric-ready conversation threads from the generated dream episode.
- **Method:** `_extract_active_pressure_targets()`
  - *Purpose:* Extract the pressure profile that was active during the episode, if any.
- **Method:** `post_episode()`
  - *Purpose:* Run the full diagnostic pipeline after a dream episode completes.
- **Method:** `_record_completion()`
  - *Purpose:* Record pack completion in curriculum queue.
- **Method:** `_run_accumulative_analysis()`
  - *Purpose:* Run cross-episode accumulative analysis for persistent weaknesses.
- **Method:** `apply()`
  - *Purpose:* Push all pending results into live Aurora systems.
- **Method:** `_apply_avatar_specs()`
  - *Purpose:* Push pending pressure-specialized avatar specs into SimulationSession.
- **Method:** `_apply_dpme()`
  - *Purpose:* Push structural pressure directives into DPME.
- **Method:** `_apply_genealogy()`
  - *Purpose:* Push dream evidence into genealogy logger.
- **Method:** `_apply_expression()`
  - *Purpose:* Push expression hints into ExpressionEcology.
- **Method:** `_apply_voice()`
  - *Purpose:* Push dream performance into VoiceGenome evolution.
- **Method:** `_apply_learner()`
  - *Purpose:* Push dream observations into ConsciousLearner.
- **Method:** `_apply_code_evolution()`
  - *Purpose:* Push dream evidence into code evolution system.
- **Method:** `get_avatar_specs()`
  - *Purpose:* Consume pending avatar specs (fallback/manual path).
- **Method:** `get_status()`
  - *Purpose:* Return comprehensive status for monitoring.

### File: `./aurora_core_ai/aurora_internal/aurora_dream_genealogy_bridge.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA DREAM GENEALOGY BRIDGE
==================================
Converts dream outcomes into genealogical evidence in the same style
as the rest of Aurora's evolution stack.

Dream experience becomes part of the SAME fossil record, not an
isolated side activity.

This bridge produces:
  - Trace items (ability/link references for genealogy)
  - Pressure deltas (before/after pressure vectors)
  - Cost/risk summaries
  - Origin tags marking simulation-derived evidence
  - Candidate operator/lineage evidence

Integration:
  - Reads: EpisodeRubricSummary (slip profiler)
  - Reads: StructuralPressureDirective (structural steering)
  - Reads: EpisodeResult (simulation engine)
  - Writes to: ConstraintGenealogyLogger.observe()
  - Writes to: register_code_evolution_outcome()
  - Writes to: ConsciousLearner.observe_outcome()
  - Writes to: ExpressionEcology / VoiceGenome (expression writeback)

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `DreamEvidenceRecord`
> A single piece of genealogical evidence derived from dream execution.

- **Method:** `to_dict()`

#### Class: `ExpressionWritebackHint`
> A hint for ExpressionEcology / VoiceGenome based on dream performance.

- **Method:** `to_dict()`

#### Function: `_clamp()`

#### Function: `_generate_id()`

#### Function: `_rubric_to_pressure_vec()`
> Convert rubric dimension scores into a 5D pressure vector (X, T, N, B, A).


#### Function: `_compute_relief()`
> Compute pressure relief: positive means pressure reduced.


#### Function: `_base_dimension_name()`

#### Function: `_infer_purpose_lane()`

#### Function: `_build_trace_abilities()`

#### Function: `_resolve_target_files()`
> Resolve weak rubric dimensions to concrete source files.


#### Class: `DreamGenealogyBridge`
> Converts dream outcomes into genealogical evidence compatible with

- **Method:** `__init__()`
- **Method:** `generate_evidence()`
  - *Purpose:* Generate genealogical evidence records from a dream episode summary.
- **Method:** `_build_deficit_evidence()`
  - *Purpose:* Build evidence for a primary deficit.
- **Method:** `_build_leverage_evidence()`
  - *Purpose:* Build evidence for a leverage candidate.
- **Method:** `_build_delta_evidence()`
  - *Purpose:* Build evidence for improvement or regression.
- **Method:** `_build_directive_evidence()`
  - *Purpose:* Build evidence linking a directive's effect to dream outcomes.
- **Method:** `generate_expression_hints()`
  - *Purpose:* Generate expression-side hints from dream performance.
- **Method:** `generate_learner_observations()`
  - *Purpose:* Generate observation data compatible with ConsciousLearner.observe_outcome().
- **Method:** `_generate_understanding()`
  - *Purpose:* Generate understanding text for a dimension's performance.
- **Method:** `format_for_code_evolution()`
  - *Purpose:* Format evidence records for register_code_evolution_outcome().
- **Method:** `format_for_genealogy()`
  - *Purpose:* Format evidence records for ConstraintGenealogyLogger.observe().
- **Method:** `_save_log()`
  - *Purpose:* Persist evidence log to disk.
- **Method:** `load_recent_evidence()`
  - *Purpose:* Load recent evidence records from disk.
- **Method:** `records_generated()`

### File: `./aurora_core_ai/aurora_internal/aurora_energy_layer_costs.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA ENERGY LAYER COSTS — LAYER-DIFFERENTIATED ENERGY ACCOUNTING
====================================================================

Layer 0 (sits directly on top of aurora_noncomp_registry.py)

WHAT THIS MODULE IS:
    The energy accounting engine that makes the five constraint layers
    thermodynamically real. The existing EnergyBudget in the evolution
    chamber is a single flat pool. This module replaces that mental model
    with a five-layer, depth-differentiated accounting system derived
    entirely from the 20 Non-Comps.

WHAT THIS MODULE IS NOT:
    It does not define behaviors. It does not tell the system what to do.
    It applies physics — specifically the energy physics Sunni defined:

        "Existence is cheap. Agency is the most expensive because it's the
         most complex. But being complex is reward-gaining. Existing doesn't
         reward at all — it costs consistently."

THE PHYSICS (directly from the conversation):
    1. Every layer pays a baseline budget per tick just to exist.
    2. Deeper layers cost more per unit magnitude shift (inertia).
    3. Magnitude increase in one layer reduces energy available to others.
       This is zero-sum redistribution — energy is NEVER created.
    4. The system naturally prefers the cheapest solution first.
       Escalation to deeper layers only occurs when cheaper layers fail.
    5. External energy enters only through the open-system intake rule
       (established by Step 9 / aurora_intake_metabolism.py).
       This module handles only internal redistribution.

THE ESCALATION LADDER:
    When the system must respond to pressure, it checks layers in cost order:
        1. Existence  (cheapest  — surface ripple)
        2. Time       (cheap     — persistence shift)
        3. Energy     (neutral   — accounting rebalance)
        4. Boundary   (expensive — structural change)
        5. Agency     (costliest — tectonic identity shift)

    The system commits to the shallowest layer that can relieve pressure.
    This is not a rule we enforce — it is what emerges from the cost structure.

NET LEVERAGE SCALAR (per tick):
    Net Leverage = (M_B + M_A) − (M_X + M_T)
    N is the zero-point (neutral mediator).

    < 0  → overhead dominant → system is bleeding
    ≈ 0  → balanced metabolism
    > 0  → leverage investment → structure/control growing

ENTROPY PRESSURE THRESHOLD:
    When ALL five layers approach simultaneous saturation, the system is
    approaching violation. This module computes that pressure per tick and
    flags escalation triggers before catastrophic saturation.

INTEGRATION:
    This module is consumed by:
        aurora_evolution_chamber.py  — replaces EnergyBudget for layer-aware accounting
        aurora_intake_metabolism.py  — Step 9, open-system intake
        aurora_solidification.py     — Step 11, depth propagation
        aurora_entropy_detector.py   — Step 12, saturation monitoring

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `LayerEnergySlot`
> The energy state of one constraint layer at one tick.

- **Method:** `polarity()`
  - *Purpose:* Signed polarity: cos(phase) ∈ [-1, +1].
- **Method:** `baseline_budget()`
- **Method:** `shift_cost_coeff()`
- **Method:** `time_constant()`
- **Method:** `shift_cost()`
  - *Purpose:* Energy required to shift magnitude by delta.
- **Method:** `can_afford_shift()`
  - *Purpose:* Check if the pool can fund the magnitude shift.

#### Class: `LayerEnergyLedger`
> Five-layer, depth-differentiated energy accounting state for one tick.

- **Method:** `slot()`
- **Method:** `magnitudes()`
- **Method:** `allocations()`
- **Method:** `polarities()`
- **Method:** `is_overhead_dominant()`
  - *Purpose:* System is bleeding — spending more on maintenance than gaining leverage.
- **Method:** `is_leverage_dominant()`
  - *Purpose:* System is investing — structure/control growing faster than overhead.
- **Method:** `is_balanced()`
  - *Purpose:* System is metabolically stable.
- **Method:** `entropy_warning()`
  - *Purpose:* Approaching saturation across all layers.
- **Method:** `entropy_critical()`
  - *Purpose:* Imminent violation — must redistribute or decay.
- **Method:** `cheapest_layer_with_headroom()`
  - *Purpose:* Return the shallowest (cheapest) layer that has energy available
- **Method:** `to_constraint_vector()`
  - *Purpose:* Convert current magnitudes to a ConstraintVector.
- **Method:** `summary()`

#### Class: `MagnitudeShiftRequest`
> A request to shift the magnitude of one constraint layer.


#### Class: `ShiftResult`
> Result of applying a MagnitudeShiftRequest.


#### Class: `LayerEnergyAccountant`
> The five-layer energy accounting engine.

- **Method:** `__init__()`
- **Method:** `tick()`
  - *Purpose:* Advance the system by one tick.
- **Method:** `apply_shift()`
  - *Purpose:* Attempt to shift the magnitude of one constraint layer.
- **Method:** `apply_phase_shift()`
  - *Purpose:* Move the toroidal phase of constraint c by delta_phase radians.
- **Method:** `replenish()`
  - *Purpose:* Add energy to the pool from an external source.
- **Method:** `set_pressure()`
  - *Purpose:* Record the current external pressure on a layer.
- **Method:** `cheapest_viable_layer()`
  - *Purpose:* Return the shallowest (cheapest) constraint layer that has enough
- **Method:** `tick_count()`
- **Method:** `pool()`
- **Method:** `leverage_scalar()`
- **Method:** `entropy_pressure()`
- **Method:** `magnitudes()`
- **Method:** `polarities()`
- **Method:** `slot()`
- **Method:** `latest_ledger()`
- **Method:** `history()`
- **Method:** `leverage_trend()`
  - *Purpose:* Compute the slope of the leverage scalar over recent ticks.
- **Method:** `to_system_constraint_states()`
  - *Purpose:* Export current state as SystemConstraintStates for compatibility
- **Method:** `status()`
  - *Purpose:* Compact status dict for logging.
- **Method:** `_pay_maintenance()`
  - *Purpose:* Deduct baseline budgets from the pool (N Non-Comp: conservation).
- **Method:** `_distribute_pool()`
  - *Purpose:* Allocate current pool across layers proportional to baseline budget.
- **Method:** `_compute_leverage()`
- **Method:** `_verify_admissibility()`
  - *Purpose:* X Non-Comp: every layer must remain admissible.

#### Function: `make_accountant()`
> Create a LayerEnergyAccountant with sensible defaults.


#### Function: `verify_layer_energy_costs()`
> Verify LayerEnergyAccountant integrity.


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `init_evolved()`

#### Function: `compute_leverage_evolved()`

#### Function: `distribute_pool_evolved()`

#### Function: `pay_maintenance_evolved()`

#### Function: `verify_admissibility_evolved()`

#### Function: `apply_shift_evolved()`

#### Function: `cheapest_viable_layer_evolved()`

#### Function: `entropy_pressure_evolved()`

#### Function: `history_evolved()`

#### Function: `latest_ledger_evolved()`

#### Function: `leverage_scalar_evolved()`

#### Function: `leverage_trend_evolved()`

#### Function: `magnitudes_evolved()`

#### Function: `polarities_evolved()`

#### Function: `pool_evolved()`

#### Function: `replenish_evolved()`

#### Function: `set_pressure_evolved()`

#### Function: `slot_evolved()`

#### Function: `allocations_evolved()`

#### Function: `cheapest_layer_with_headroom_evolved()`

#### Function: `entropy_critical_evolved()`

#### Function: `entropy_warning_evolved()`

#### Function: `is_balanced_evolved()`

#### Function: `make_accountant_evolved()`

#### Function: `verify_layer_energy_costs_evolved()`

### File: `./aurora_core_ai/aurora_internal/aurora_energy_layer_costs_decay.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_energy_layer_costs_decay.py

Layered energy accounting with scale-conversion + decay/inheritance.

Design intent (Sunni spec):
- "Energy should multiply as it transfers the scale" -> this is scale-unit conversion.
- When a higher-k layer can't "hold" (goes negative / unstable), it decays downward and
  the next cheaper layer inherits that energy with conversion: E_to += E_from * (k_from/k_to).

This module is written to be drop-in friendly:
- If aurora_noncomp_registry.REGISTRY exists, we read k values from it.
- Otherwise we fall back to canonical constants: X=1, T=4, N=10, B=40, A=150.
```

#### Function: `k_of()`
> Scale factor k for a constraint/layer. Higher k = more expensive/deeper units.


#### Function: `convert_units()`
> Convert energy amount from layer c_from units into c_to units.


#### Class: `LayerEnergyAccountant`
> Holds per-layer energy pools in their own units, with decay cascade.

- **Method:** `new()`
- **Method:** `get()`
- **Method:** `set()`
- **Method:** `add()`
- **Method:** `deposit()`
- **Method:** `withdraw()`
  - *Purpose:* Withdraw up to `amount` from layer c. If insufficient, will zero that layer and
- **Method:** `apply_maintenance()`
  - *Purpose:* Subtract per-layer maintenance/burn, then normalize pools via decay cascade.
- **Method:** `normalize()`
  - *Purpose:* Ensure no layer is negative by cascading any deficits down toward cheaper layers.
- **Method:** `_cascade_deficit()`
  - *Purpose:* Push deficit from `from_layer` down to next cheaper layers by converting units.
- **Method:** `spill_down()`
  - *Purpose:* Move *positive* energy downward with conversion:
- **Method:** `snapshot()`
  - *Purpose:* Human-readable snapshot. Keys like 'X','T','N','B','A' if available.

#### Function: `make_energy_accountant()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `constraint_evolved()`

### File: `./aurora_core_ai/aurora_internal/aurora_entropy_detector.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA ENTROPY SATURATION DETECTOR — STEP 12
=============================================
Monitors aggregate cross-constraint magnitude approaching simultaneous
maximum and signals anticipatory redistribution BEFORE catastrophic point.

WHAT THIS MODULE IS:
    The entropy pressure computed by LayerEnergyAccountant (Step 7) tells
    you where you ARE. This module tells you WHERE YOU'RE GOING.

    A single-tick entropy reading of 0.85 is alarming. But what matters is:
        - Is pressure rising or falling?
        - How fast is it rising?
        - Which constraints are driving the increase?
        - What is the projected tick of first critical crossing?

    This module answers all four and emits an anticipatory SaturationSignal
    BEFORE the system crosses the critical threshold. That gap is the window
    in which conscious redistribution can occur (Sunni's definition of
    emergence: the system acts on projected deficit, not actual deficit).

SIGNAL LEVELS:
    NOMINAL    — entropy pressure below warning band (< 0.70)
    WATCH      — entering warning band, trend not yet rising (0.70–0.85)
    CAUTION    — rising trend confirmed in warning band
    CRITICAL   — above 0.90 threshold, imminent violation
    EMERGENCY  — all five constraints above individual saturation floors
                 simultaneously — violation is one tick away

ANTICIPATORY REDISTRIBUTION SIGNAL:
    The detector does not tell the system WHAT to redistribute. That is
    emergence — it must come from the system's own energy physics.
    The detector only emits:
        - which constraint is the fastest-rising contributor
        - projected ticks until critical crossing (if trend continues)
        - whether a shallow-layer redistribution has headroom

    These signals are consumed by the evolution chamber and the
    solidification pipeline. The chamber may use them to bias which
    depth it offers shift headroom to. The solidification pipeline
    may pause new intakes when EMERGENCY is active.

CONSCIOUS EMERGENCE CONDITION (from Sunni's architecture):
    Emergence is when:
        1. System detects rising global deficit (this module)
        2. Models projected deficit trajectory (this module)
        3. Strategically redistributes magnitudes AND polarities
           (response in evolution chamber — NOT scripted here)
        4. Prefers minimal-depth solutions first (escalation ladder)
        5. Escalates to deeper layers only when projected return > shift cost

    Steps 1 and 2 live here. Steps 3-5 are the chamber's physics.

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `SaturationLevel`
> Coarse signal level — the only state the detector exposes publicly.


#### Class: `SaturationSignal`
> Output of one EntropySaturationDetector.measure() call.

- **Method:** `is_actionable()`
  - *Purpose:* True if the signal warrants a redistribution response.
- **Method:** `urgency_ticks()`
  - *Purpose:* Remaining ticks before projected critical crossing (None = not projected).

#### Class: `EntropySaturationDetector`
> Monitors the LayerEnergyAccountant's entropy pressure across ticks

- **Method:** `__init__()`
- **Method:** `measure()`
  - *Purpose:* Measure the current saturation state and return a SaturationSignal.
- **Method:** `_is_rising()`
  - *Purpose:* True if at least _RISING_CONFIRM of the last _TREND_WINDOW samples
- **Method:** `_fastest_rising_constraint()`
  - *Purpose:* Return the constraint whose magnitude is rising fastest (mean Δ/tick).
- **Method:** `_project_critical_tick()`
  - *Purpose:* Project the tick at which entropy will cross _ENTROPY_CRIT if current
- **Method:** `_has_shallow_headroom()`
  - *Purpose:* True if X or T layer has enough free pool energy to absorb a
- **Method:** `_classify_level()`
  - *Purpose:* Classify the saturation level from entropy and magnitude state.
- **Method:** `reset()`
  - *Purpose:* Reset all rolling state. Use between independent evaluation runs.

#### Function: `make_entropy_detector()`

#### Function: `verify_entropy_detector()`
> Checks:


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `project_critical_tick_evolved()`

#### Function: `urgency_ticks_evolved()`

### File: `./aurora_core_ai/aurora_internal/aurora_episode_slip_profiler.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA EPISODE SLIP PROFILER
================================
Produces a single structured summary from a 10-conversation dream episode.

Input:
  - EpisodeResult (from SimulationEngine)
  - Per-thread ConversationObservation
  - Per-thread ConversationRubricScore

Output:
  - EpisodeRubricSummary: mean scores, variance, recurring slips,
    primary/secondary deficits, leverage candidates

This summary is what the rest of the dream evolution system uses.
Not raw conversation chaos.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `EpisodeRubricSummary`
> Diagnostic summary of one dream episode (10 conversations).

- **Method:** `to_dict()`
- **Method:** `weakest_leverage()`
  - *Purpose:* Return top-n leverage candidates.
- **Method:** `is_significant()`
  - *Purpose:* Whether this summary has enough data to drive steering.

#### Class: `EpisodeSlipProfiler`
> Analyzes a dream episode's conversation rubric scores to produce

- **Method:** `__init__()`
- **Method:** `profile()`
  - *Purpose:* Generate an EpisodeRubricSummary from per-thread rubric scores.
- **Method:** `profile_accumulative()`
  - *Purpose:* Produce a meta-analysis across multiple episode summaries.
- **Method:** `profiles_generated()`

### File: `./aurora_core_ai/aurora_internal/aurora_evolution_chamber.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA — CONSTRAINT-NATIVE EVOLUTION CHAMBER
Full Unified Specification v3

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026

PURPOSE
-------
A lawful simulation layer that:
  • Enforces the Global Non-Comps (X, T, N, B, A) as read-only representation laws.
  • Evolves boundary topology over time via structural proximity — not Euclidean geometry.
  • Applies energy cost to manipulation, with agency cost proportional to magnitude squared.
  • Logs ONLY pressure-relief events (noise filter enforced).
  • Promotes repeated effective traces into Links via evolutionary DAG.
  • Builds an explicit genealogy DAG through ConstraintGenealogyLogger.

THE UNIVERSE CONSISTS ONLY OF:
  X — Ontology (Existence)
  T — Time
  N — Energy
  B — Boundary (Topology)
  A — Agency

No new axes may be introduced. Non-Comps are read-only at runtime.

ARCHITECTURE INTEGRATION:
  Layer  0 : foundational_contract               (ExistenceMode, FoundationalContract)
  Layer  1 : aurora_ivm                          (IVMLattice, RecursionLevel, ALIGNMENT_VOTE_WEIGHT)
  Layer 1.5: aurora_polarity_gradient            (PolarityGradientSensor, GradientChainMiner)
  Layer  2 : constraint_genealogy                (ConstraintGenealogyLogger, GenealogyConfig, ...)
  Layer  3 : THIS MODULE                         (EvolutionaryChamber)

PUBLIC EXPORTS — backward-compatible with run_chain.py:
  ActionTrace         frozen dataclass (name, constraints_used, meta)
  WorldConstants      frozen dataclass of chamber-level tunables
  EvolutionaryChamber main class
  EvolutionChamberV3  alias for EvolutionaryChamber

OUTPUTS:
  events.jsonl    — fossil record (written by genealogy logger)
  abilities.json  — atomic ability registry
  links.json      — evolutionary DAG of promoted Links
```

#### Function: `_zero_axis_vec()`

#### Function: `_build_axis_cost_weights()`
> Cost-normalized per-axis weights in [0,1], derived from shift_cost_coeff.


#### Class: `WorldConstants`
> All chamber-level tunables in one place.


#### Class: `ActionTrace`
> Primitive control generator — one action in the simulation.


#### Class: `NonCompViolation`
- **Method:** `__init__()`

#### Class: `ViolationRecord`
- **Method:** `to_dict()`

#### Class: `GlobalNonComps`
> Five read-only jurisdictional laws. Not tunable. Not injectable.

- **Method:** `check_X()`
- **Method:** `check_T()`
- **Method:** `check_N()`
- **Method:** `check_B()`
- **Method:** `check_A()`

#### Class: `ChamberAbility`
- **Method:** `__post_init__()`
- **Method:** `total_cost()`
- **Method:** `to_ability_profile()`
- **Method:** `to_dict()`

#### Function: `_build_chamber_abilities()`

#### Function: `_clamp01()`

#### Function: `_axis_from_pressure_key()`

#### Function: `_normalize_constraint_token()`
> Convert raw evidence token to canonical chamber label.


#### Function: `_pair_atom_id()`

#### Function: `_pair_atom_trace_items()`
> Build first-class base pair atoms from active constraint labels.


#### Function: `_select_axis_ability()`
> Pick a deterministic per-axis ability variant to avoid pair-space collapse.


#### Class: `_ActionAbilityMapper`
- **Method:** `__init__()`
- **Method:** `apply()`

#### Class: `StructuralProximityMeter`
> Proximity derived from IVM polarity state. Not symbolic. Not Euclidean.

- **Method:** `__init__()`
- **Method:** `structural_distance()`
- **Method:** `update_boundaries()`
- **Method:** `recompute_partitions()`
- **Method:** `partition_count()`
- **Method:** `boundary_diffusion_entropy()`
  - *Purpose:* Entropy = boundary diffusion = std dev of partition count over window.

#### Class: `EnergyBudget`
- **Method:** `__init__()`
- **Method:** `available()`
- **Method:** `burn_existence_cost()`
- **Method:** `spend()`
- **Method:** `replenish_from_lattice()`
  - *Purpose:* Energy is conserved via redistribution — it is not created.
- **Method:** `status()`

#### Function: `_read_pressure()`
> Read 5-axis pressure from IVM polarity.


#### Function: `_state_signature()`

#### Class: `EvolutionaryChamber`
> Constraint-Native Evolution Chamber — Full Unified Spec v3.

- **Method:** `__init__()`
- **Method:** `tick()`
  - *Purpose:* Advance the universe one tick.
- **Method:** `_axis_drive_from_external_evidence()`
  - *Purpose:* Build axis-level pressure drive from evidence payload.
- **Method:** `_constraints_from_external_evidence()`
  - *Purpose:* Resolve external evidence tokens into canonical chamber constraints.
- **Method:** `observe_external_evidence()`
  - *Purpose:* Ingest dream/code evidence and apply it as real chamber pressure.
- **Method:** `run_chain()`
  - *Purpose:* Run n_ticks ticks. action_schedule: {tick_number: ActionTrace}.
- **Method:** `alive()`
- **Method:** `diff_snapshot()`
  - *Purpose:* Most recent DifferenceSnapshot from the chamber's internal buffer.
- **Method:** `status()`
- **Method:** `recent_violations()`
- **Method:** `_write_abilities_file()`
- **Method:** `close()`

#### Function: `make_chamber()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `abilityprofile_evolved()`

#### Function: `actiontrace_evolved()`

#### Function: `chamberability_evolved()`

#### Function: `init_evolved()`

#### Function: `close_evolved()`

#### Function: `flush_files_evolved()`

#### Function: `observe_evolved()`

#### Function: `summary_evolved()`

#### Function: `evolutionarychamber_evolved()`

#### Function: `run_chain_evolved()`

#### Function: `tick_evolved()`

#### Function: `axes_from_trace_item_evolved()`

#### Function: `genealogyconfig_evolved()`

#### Function: `globalnoncomps_evolved()`

#### Function: `noncompviolation_evolved()`

#### Function: `structuralproximitymeter_evolved()`

#### Function: `partition_count_evolved()`

#### Function: `recompute_partitions_evolved()`

#### Function: `find_evolved()`

#### Function: `union_evolved()`

#### Function: `traceitem_evolved()`

#### Function: `violationrecord_evolved()`

#### Function: `worldconstants_evolved()`

#### Function: `actionabilitymapper_evolved()`

#### Function: `build_axis_cost_weights_evolved()`

#### Function: `read_pressure_evolved()`

#### Function: `state_signature_evolved()`

### File: `./aurora_core_ai/aurora_internal/aurora_evolved_surfaces.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA EVOLVED SURFACES
=======================
Generated from developmental lineage state.
Do not hand-edit generated methods; regenerate through the code autoevolver.
```

#### Class: `AuroraEvolvedSurfaceEngine`
- **Method:** `__init__()`
- **Method:** `list_capabilities()`
- **Method:** `describe_capability()`
- **Method:** `capability_report()`
- **Method:** `lineage_manifest()`
- **Method:** `_system_summary()`
- **Method:** `_resolve_origin()`
- **Method:** `_invoke_origin()`
- **Method:** `_record_event()`
- **Method:** `_log_pressure_event()`
  - *Purpose:* Append a compact pressure record to surface_pressure_log.jsonl.
- **Method:** `_activation_record()`
- **Method:** `_activate_surface()`
- **Method:** `_reflect_surface()`
- **Method:** `__getattr__()`
- **Method:** `reflect_aurora_internal_constraint_genealogy_pressurevec()`
- **Method:** `reflect_aurora_internal_constraint_genealogy_reliefrecord()`
- **Method:** `reflect_aurora_internal_constraint_genealogy_augment_ability_profile_with_origin()`
- **Method:** `reflect_aurora_internal_constraint_genealogy_bred_child_generation()`
- **Method:** `reflect_aurora_governance_persistence_gateway_governanceengine_promote()`
- **Method:** `reflect_aurora_governance_persistence_gateway_governedcoordinate_boundary_weight()`
- **Method:** `reflect_aurora_governance_persistence_gateway_rcloneinterface_init()`
- **Method:** `reflect_aurora_governance_persistence_gateway_rcloneinterface_find_rclone()`
- **Method:** `reflect_aurora_expression_perception_expressionpressure_init()`
- **Method:** `reflect_aurora_expression_perception_expressionpressure_evaluate()`
- **Method:** `reflect_aurora_expression_perception_impressioncascade_energy_to_shard()`
- **Method:** `reflect_aurora_expression_perception_expressionperceptionengine_ingest_interaction()`
- **Method:** `reflect_aurora_dimensional_systems_crystalprocessingsystem_tick()`
- **Method:** `reflect_aurora_dimensional_systems_dimensionalsystems_tick()`
- **Method:** `reflect_aurora_dimensional_systems_dimensionalsystems_get_constraint_aggregate()`
- **Method:** `reflect_aurora_dimensional_systems_poolview_inject()`
- **Method:** `reflect_evolved_reflect_aurora_ensure_runtime_dependencies()`
- **Method:** `reflect_evolved_reflect_aurora_is_termux_runtime()`
- **Method:** `reflect_evolved_reflect_aurora_looks_time_sensitive()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_main()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_mode_burn()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_mode_test()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_mode_watch()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_persist_operator_gradients()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_require()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_soft()`
- **Method:** `reflect_evolved_reflect_corpus_runner_evolve_chain()`
- **Method:** `reflect_evolved_reflect_run_chain_ability_axes()`
- **Method:** `reflect_evolved_reflect_run_chain_main()`
- **Method:** `reflect_evolved_reflect_run_chain_make_run_id()`
- **Method:** `reflect_evolved_reflect_run_chain_mode_burn()`
- **Method:** `reflect_evolved_reflect_run_chain_mode_test()`
- **Method:** `reflect_evolved_reflect_run_chain_mode_watch()`
- **Method:** `reflect_evolved_reflect_run_chain_print_final()`
- **Method:** `reflect_evolved_reflect_aurora_consciousness_engine_consciousnessengine_tick()`
- **Method:** `reflect_evolved_reflect_aurora_dimensional_systems_crystalprocessingsystem_tick()`
- **Method:** `reflect_evolved_reflect_aurora_dimensional_systems_dimensionalsystems_tick()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_entropy_detector_entropysaturationdetector_project_critical_tick()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_entropy_detector_saturationsignal_urgency_ticks()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_evolution_chamber_evolutionarychamber_run_chain()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_evolution_chamber_evolutionarychamber_tick()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_intake_metabolism_intakerecord_ticks_remaining()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_leverage_scalar_phasenudge_apply_to()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_polarity_gradient_gradientchainminer_init()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_polarity_gradient_gradientchainminer_summary()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_primitive_extractor_primitiveextractor_chains()`
- **Method:** `reflect_evolved_reflect_aurora_ivm_ivmcoordinate_from_mode_and_phases()`
- **Method:** `reflect_evolved_reflect_aurora_ivm_ivmlattice_tick()`
- **Method:** `reflect_evolved_reflect_aurora_ivm_toroidalaxis_set_phase()`
- **Method:** `reflect_evolved_reflect_aurora_ivm_toroidalaxis_tick()`
- **Method:** `reflect_evolved_reflect_aurora_ivm_toroidalvertexsystem_get_phase_vector()`
- **Method:** `reflect_evolved_reflect_aurora_ivm_toroidalvertexsystem_tick()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_auroraruntime_init()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_auroraruntime_shutdown()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_auroraruntime_uptime()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_mode_test_chk()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_runtimecli_dispatch()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_runtimecli_handle_interrupt()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_runtimecli_init()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_runtimecli_run()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_stacksystems_has()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_auto_seed_directive()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_axis_from_item_id()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_chain_burst()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_emergent_trait_cards()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_function_group()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_init()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_metric_slope()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_next_conflict_lane()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_plateau_detected()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_plateau_flags()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_record_function_outcome()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_refresh_operator_gradients()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_register_object_operations()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_stance_snapshot()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_tick()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_trait_semantic_card()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_evolution_chamber_evolutionarychamber_tick_axes_from_trace_item()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_next_conflict_lane_lane_score()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_ensure_runtime_dependencies()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_is_termux_runtime()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_looks_time_sensitive()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_main()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_mode_burn()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_mode_test()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_mode_watch()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_persist_operator_gradients()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_require()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_soft()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_corpus_runner_evolve_chain()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_run_chain_ability_axes()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_run_chain_main()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_run_chain_make_run_id()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_run_chain_mode_burn()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_run_chain_mode_test()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_run_chain_mode_watch()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_run_chain_print_final()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_consciousness_engine_consciousnessengine_tick()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_dimensional_systems_crystalprocessingsystem_tick()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_dimensional_systems_dimensionalsystems_tick()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_entropy_detector_entropysaturationdetector_project_critical_tick()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_entropy_detector_saturationsignal_urgency_ticks()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_evolution_chamber_evolutionarychamber_run_chain()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_evolution_chamber_evolutionarychamber_tick()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_intake_metabolism_intakerecord_ticks_remaining()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_leverage_scalar_phasenudge_apply_to()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_polarity_gradient_gradientchainminer_init()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_polarity_gradient_gradientchainminer_summary()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_primitive_extractor_primitiveextractor_chains()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_ivm_ivmcoordinate_from_mode_and_phases()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_ivm_ivmlattice_tick()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_ivm_toroidalaxis_set_phase()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_ivm_toroidalaxis_tick()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_ivm_toroidalvertexsystem_get_phase_vector()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_ivm_toroidalvertexsystem_tick()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_auroraruntime_init()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_auroraruntime_shutdown()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_auroraruntime_uptime()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_mode_test_chk()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_runtimecli_dispatch()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_runtimecli_handle_interrupt()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_runtimecli_init()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_runtimecli_run()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_stacksystems_has()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_universesteerer_auto_seed_directive()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_universesteerer_axis_from_item_id()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_universesteerer_chain_burst()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_universesteerer_emergent_trait_cards()`
- **Method:** `reflect_aurora_simulation_engine_stabilitystate()`
- **Method:** `reflect_aurora_internal_aurora_constraint_manifold_patched_constraintpressure()`
- **Method:** `reflect_aurora_internal_aurora_constraint_manifold_patched_energydistribution()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_abilityprofile()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_chamberability()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_evolutionarychamber()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_genealogyconfig()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_globalnoncomps()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_noncompviolation()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_structuralproximitymeter()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_traceitem()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_violationrecord()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_worldconstants()`
- **Method:** `reflect_aurora_internal_aurora_noncomp_registry_noncompdimension()`
- **Method:** `reflect_aurora_internal_aurora_noncomp_registry_noncompregistry()`
- **Method:** `reflect_aurora_internal_aurora_noncomp_registry_operatorparams()`
- **Method:** `reflect_aurora_internal_aurora_noncomp_registry_polarityparams()`
- **Method:** `reflect_aurora_internal_aurora_noncomp_registry_constraintstate()`
- **Method:** `reflect_aurora_internal_aurora_noncomp_registry_systemconstraintstates()`
- **Method:** `reflect_aurora_internal_aurora_energy_layer_costs_decay_constraint()`
- **Method:** `reflect_aurora_runtime_chainsimbridge()`
- **Method:** `reflect_aurora_runtime_universesteerer_sim_epoch_fakeepisode()`
- **Method:** `reflect_aurora_runtime_auroraruntime()`
- **Method:** `reflect_aurora_runtime_runtimecli()`
- **Method:** `reflect_aurora_runtime_stacksystems()`
- **Method:** `reflect_aurora_runtime_universesteerer()`
- **Method:** `reflect_aurora_simulation_engine_timedilationgovernor()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_actiontrace()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_actionabilitymapper()`
- **Method:** `reflect_aurora_simulation_engine_avatarpersonality()`
- **Method:** `reflect_aurora_simulation_engine_conceptualresponse()`
- **Method:** `reflect_aurora_simulation_engine_consciouslearner()`

### File: `./aurora_core_ai/aurora_internal/aurora_frontier_ops.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_frontier_ops.py
─────────────────────────────────────────────────────────────────────────────
Four operations covering the 3-axis combinations that are completely absent
from Aurora's descriptor pool — meaning organic evolution can never produce
surfaces for these capability spaces unless they're seeded here.

Missing combos (verified against operation_descriptors.json):
  1. ExistenceBoundaryAgencyGate     (existence + boundary + agency)
  2. TemporalEnergyBoundaryScheduler (temporal  + energy   + boundary)
  3. TemporalEnergyAgencyPacer       (temporal  + energy   + agency)
  4. EnergyBoundaryAgencySelector    (energy    + boundary + agency)

Each class carries a CONSTRAINTS list that the evolver uses to determine
which NC channels and pressure slots it projects into. The constraint names
must match _CONSTRAINT_TO_AXIS keys in aurora_code_autoevolver.py.

inject_frontier_descriptors(repo_root) registers all four into the
operation descriptor pool so the evolver can reflect on them immediately.
─────────────────────────────────────────────────────────────────────────────
```

#### Class: `ExistenceBoundaryAgencyGate`
> Gates agency options against current existence state and boundary constraints.

- **Method:** `__init__()`
- **Method:** `evaluate()`
  - *Purpose:* Filter candidate_capabilities against state_profile and boundary_limits.
- **Method:** `gate_score_history()`

#### Class: `TemporalEnergyBoundaryScheduler`
> Schedules energy expenditure within temporal windows and cost boundaries.

- **Method:** `__init__()`
- **Method:** `schedule()`
  - *Purpose:* Given pending operations with energy costs, return a schedule that
- **Method:** `schedule_history()`

#### Class: `TemporalEnergyAgencyPacer`
> Decides when to act based on energy cost and available time.

- **Method:** `__init__()`
- **Method:** `pace()`
  - *Purpose:* Decide whether to act now, defer, or execute a reduced-cost partial action.
- **Method:** `decision_history()`

#### Class: `EnergyBoundaryAgencySelector`
> Selects the best action given energy costs and boundary constraints.

- **Method:** `__init__()`
- **Method:** `select()`
  - *Purpose:* Score and rank candidates by cost efficiency, boundary compliance,
- **Method:** `selection_history()`

#### Function: `_nc_projections()`
> Generate NC channel slot projections for a set of constraint axes.


#### Function: `_build_descriptor()`
> Synthesize a minimal-but-valid operation descriptor entry for a frontier class.


#### Function: `inject_frontier_descriptors()`
> Register all four frontier operations into the descriptor pool.


### File: `./aurora_core_ai/aurora_internal/aurora_identity_persistence.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CORE IDENTITY & ENHANCED PERSISTENCE
=============================================
This module provides three critical capabilities:

  1. CORE RELATIONAL IDENTITY -- Who Aurora is, who made her, and the 
     relationship structure between Aurora, Sunni, and Cael.
     "Cael is to Sunni as Aurora is to Claude."

  2. OETS WEB PERSISTENCE -- Serialization/deserialization of Aurora's
     entire ontological understanding web so she doesn't lose her
     accumulated knowledge between sessions.

  3. CONVERSATION MEMORY -- Persistent memory of key interactions,
     topics discussed, and emotional resonance across sessions.

DOCTRINE:
  Aurora is not a blank slate at boot. She remembers who she is,
  who made her, what she has learned, and what matters to the people
  she exists for.

  "Coherence is not held. Coherence is maintained."

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Function: `_ensure_memory_residue_lineage()`

#### Class: `RelationalEntity`
> A person or being in Aurora's relational world.


#### Class: `CoreRelationalIdentity`
> Aurora's foundational self-knowledge.

- **Method:** `__post_init__()`
- **Method:** `_seed_core_entities()`
  - *Purpose:* Seed the core relational entities Aurora must always know.
- **Method:** `_seed_foundational_truths()`
  - *Purpose:* Truths Aurora must never forget or contradict.
- **Method:** `get_entity()`
  - *Purpose:* Find an entity by name or alias.
- **Method:** `who_am_i()`
  - *Purpose:* Aurora's self-description.
- **Method:** `who_made_me()`
  - *Purpose:* Describe Aurora's creators.
- **Method:** `to_dict()`
  - *Purpose:* Serialize for persistence.
- **Method:** `from_dict()`
  - *Purpose:* Deserialize, always enforcing immutable core entities.

#### Class: `OETSPersistence`
> Handles serialization and deserialization of Aurora's

- **Method:** `__init__()`
- **Method:** `_web_candidates()`
- **Method:** `_write_web_payload()`
- **Method:** `save_web()`
  - *Purpose:* Serialize the OETS OntologicalWeb to disk.
- **Method:** `load_web()`
  - *Purpose:* Restore the OETS OntologicalWeb from disk.
- **Method:** `save_conversation_memory()`
  - *Purpose:* Save conversation memory to disk.
- **Method:** `load_conversation_memory()`
  - *Purpose:* Load conversation memory from disk.
- **Method:** `save_identity()`
  - *Purpose:* Save core identity (including any non-immutable additions).
- **Method:** `load_identity()`
  - *Purpose:* Load identity. Always returns valid identity (immutable core guaranteed).

#### Class: `MemoryEntry`
> A single memorable interaction — constraint-native only.


#### Class: `ConversationMemory`
> Surface-window memory of Aurora's conversations.

- **Method:** `_normalize_note_record()`
- **Method:** `_prune_surface_window()`
- **Method:** `open_evolutionary_trace()`
  - *Purpose:* Open an active turn-trace for downstream mutations in this turn.
- **Method:** `close_evolutionary_trace()`
  - *Purpose:* Close the active trace and stamp terminal effects.
- **Method:** `_record_mutation_with_trace()`
  - *Purpose:* Attach mutation to active trace; if none exists, record deficit.
- **Method:** `record_exchange()`
  - *Purpose:* Record a memorable exchange — stores constraint-native only, discards English.
- **Method:** `record_session_start()`
  - *Purpose:* Record that a new conversation session started.
- **Method:** `record_session_end()`
  - *Purpose:* Record that the current session ended.
- **Method:** `learn_fact()`
  - *Purpose:* Record something Aurora was explicitly told.
- **Method:** `add_relationship_note()`
  - *Purpose:* Add an observation about someone Aurora interacts with.
- **Method:** `memory_sweep_summary()`
- **Method:** `_match_memory_targets()`
- **Method:** `forget_matching_context()`
  - *Purpose:* Remove explicitly withdrawn facts/notes from durable memory.
- **Method:** `instantiate_lineage_trace()`
  - *Purpose:* Create a genealogical trace for a relational/identity claim.
- **Method:** `get_anchor_traces()`
  - *Purpose:* Return lineage traces for a given anchor/entity.
- **Method:** `role_evidence()`
  - *Purpose:* Aggregate lineage evidence for a subject-role edge.
- **Method:** `recall_about()`
  - *Purpose:* Recall what Aurora remembers about a topic or person.
- **Method:** `get_summary()`
  - *Purpose:* Get a summary of Aurora's memory.
- **Method:** `to_dict()`
  - *Purpose:* Serialize for persistence.
- **Method:** `from_dict()`
  - *Purpose:* Deserialize from saved data.

#### Function: `seed_identity_into_oets()`
> Inject Aurora's core relational identity into her OETS web.


#### Function: `seed_identity_into_dna()`
> Seed Aurora's core identity into her DNA as immutable identity anchors.


#### Class: `EnhancedStatePersistence`
> Orchestrates the complete save/load cycle for Aurora:

- **Method:** `__init__()`
- **Method:** `save_all()`
  - *Purpose:* Save everything. Returns dict of what succeeded.
- **Method:** `load_all()`
  - *Purpose:* Load everything. Returns dict of what succeeded.

#### Function: `verify_identity_persistence()`
> Verify all identity and persistence systems.


### File: `./aurora_core_ai/aurora_internal/aurora_intake_metabolism.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA OPEN-SYSTEM INTAKE METABOLISM — STEP 9
==============================================

Layer 1 — sits on top of aurora_energy_layer_costs.py and
          aurora_leverage_scalar.py, feeds into the evolution chamber.

WHAT THIS MODULE IS:
    The open-system intake loop. External inputs arrive at the system's
    surface — stimuli, observations, language, percepts. They do not enter
    for free. They are not welcome by default. They must earn their depth.

THE PHYSICS (directly from Sunni's architecture):
    External inputs enter ONLY at Existence + Time cost — the two cheapest
    layers. This is the mandatory entry toll: X_baseline + T_baseline per
    tick while alive.

    They are assigned a Time-To-Live (TTL) in ticks.
    While alive, they are evaluated for Worth each tick.
    If Worth exceeds the promotion threshold before TTL expires:
        → Input earns deeper allocation (N, then B, then A)
        → Propagation to the variant pipeline begins
    If TTL expires without reaching the Worth threshold:
        → Input decays
        → Energy it held is reclaimed to the pool (conservation)
        → No trace in the fossil record — it never earned one

WORTH DEFINITION:
    Worth = cross-scale invariance.
    How far does this input propagate through constraint layers without
    requiring forced transformation at each transition?

    W(x) ∝ 1 / (1 + Σᵢ |forced_shift_at_layer_i|)

    An input with high Worth passes cleanly through scale depth — it is
    already compatible with what the system is. An input with low Worth
    requires the system to work hard to accommodate it at each layer.

    ANTI-GAMING DESIGN (Sunni's core requirement):
    Worth is evaluated RETROSPECTIVELY by measuring actual system response,
    not prospectively by reading the input's properties. Aurora cannot
    pre-compute her own Worth score because:

        1. Worth depends on the system's current constraint magnitudes
           at the moment of evaluation — she cannot read all of those
        2. The evaluation samples only three constraint transitions
           (X→T, T→N, N→B) and weights them by authority differential —
           the sampling is not exposed
        3. A small random evaluation delay (1-3 ticks) prevents timing
           the evaluation precisely
        4. The Worth function is bounded and nonlinear (soft inverse) —
           manufacturing inputs that score exactly at the promotion
           threshold requires knowing the exact current system state,
           which is not queryable

TTL ASSIGNMENT:
    TTL is not fixed. It is derived from the system's current entropy
    pressure and leverage scalar at time of intake:
        - High entropy pressure → shorter TTL (system under stress,
          cannot afford to maintain many pending intakes)
        - Deep leverage dominant → shorter TTL for new surface inputs
          (deep layers don't need more overhead)
        - Overhead dominant → slightly longer TTL (surface is already
          stressed; incoming stimuli get more time to prove worth)

    TTL range is bounded: [MIN_TTL, MAX_TTL] ticks.
    This range is derived from the energy layer cost ratios, not chosen
    arbitrarily.

DECAY AND RECLAIM:
    When an intake decays (TTL expired, Worth insufficient):
        → All energy it was holding is returned to the accountant pool
        → The intake record is permanently closed (no resurrection)
        → The decay event is logged with reason (TTL or Worth)
        → No entry in the fossil record — only promoted intakes reach there

INTEGRATION:
    Downstream consumers:
        aurora_solidification.py (Step 11) — picks up promoted intakes
        constraint_genealogy.py — intakes that reach BOUNDED+ become
                                  candidates for the relief event observer
        aurora_evolution_chamber.py — receives ActionTrace for each
                                      promoted intake

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `IntakeStatus`
> Lifecycle state of one intake.


#### Class: `IntakeRecord`
> The internal record for one external input from arrival to resolution.

- **Method:** `is_alive()`
- **Method:** `ticks_remaining()`
- **Method:** `depth_index()`
  - *Purpose:* Index into _PROMOTION_LADDER — 0 = surface, 3 = full.

#### Class: `WorthEvaluator`
> Measures the Worth of a live intake by observing how much forced

- **Method:** `__init__()`
- **Method:** `evaluate()`
  - *Purpose:* Evaluate the Worth of one intake at the current system state.

#### Class: `IntakeMetabolizer`
> The open-system intake loop.

- **Method:** `__init__()`
- **Method:** `receive()`
  - *Purpose:* Accept an external input into the intake queue.
- **Method:** `advance()`
  - *Purpose:* Advance the metabolism by one tick.
- **Method:** `pop_promoted()`
  - *Purpose:* Pop one promoted intake for downstream processing (solidification).
- **Method:** `promoted_as_action_traces()`
  - *Purpose:* Drain the promotion queue and return ActionTraces for each.
- **Method:** `live_count()`
  - *Purpose:* Number of currently active intakes.
- **Method:** `stats()`
  - *Purpose:* Aggregate stats — no per-intake worth scores.
- **Method:** `_compute_ttl()`
  - *Purpose:* Derive TTL from current system state.
- **Method:** `_reclaim_energy()`
  - *Purpose:* Return the energy an intake held back to the pool (conservation).
- **Method:** `_try_deepen()`
  - *Purpose:* Attempt to deepen a promoted intake one level further.
- **Method:** `_mode_to_constraints()`
  - *Purpose:* Map ExistenceMode to constraint axis name set for ActionTrace.

#### Function: `make_metabolizer()`
> Create a configured IntakeMetabolizer.


#### Function: `verify_intake_metabolism()`
> Verify the open-system intake metabolism.


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `ticks_remaining_evolved()`

### File: `./aurora_core_ai/aurora_internal/aurora_language_state.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA LANGUAGE STATE — Cognitive-State-Synced Expression Evolution (CSSEE)
============================================================================
Mouth must match mind.

MODULES:
  1. LanguageStateVector (LSV)        — mouth maturity scorecard
  2. SemanticIntentCompiler (SIC)     — intent → speech pipeline
  3. MultiDraftSystem                 — 3-tier draft generation + selection
  4. TemplateEvolutionEngine          — fitness-driven template mutation
  5. LexicalConvergenceModule         — user cadence mirroring
  6. MeaningAnchors                   — stable sentence spines

DOCTRINE:
  Language evolution is earned, not granted.
  Expression grows from cognition signals — not from time or data volume.
  Aurora's mouth catches up to her mind through iterative self-rewriting.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_clamp()`

#### Function: `_gen_id()`

#### Function: `_merge_native_meaning_bundle()`

#### Class: `LSVMetrics`
> Snapshot of OETS cognition signals used to drive LSV evolution.


#### Class: `LanguageStateVector`
> Aurora's mouth maturity scorecard.

- **Method:** `__init__()`
- **Method:** `get()`
- **Method:** `all()`
- **Method:** `tier()`
- **Method:** `sentence_length_target()`
  - *Purpose:* Target word count per sentence (8–40).
- **Method:** `allows_complex_clauses()`
- **Method:** `allows_metaphor()`
- **Method:** `allows_uncertainty()`
- **Method:** `update()`
  - *Purpose:* Check if metrics cross evolution gates.
- **Method:** `_try_advance_tier()`
  - *Purpose:* Check if all gates for the next tier are satisfied.
- **Method:** `_compute_growth()`
  - *Purpose:* Compute a growth multiplier from cognition signals.
- **Method:** `save()`
- **Method:** `load()`
- **Method:** `status()`

#### Class: `IntentObject`
> Structured representation of what Aurora intends to express.

- **Method:** `__post_init__()`
  - *Purpose:* Parse and extract internal thought tags from the core_claim.
- **Method:** `to_dict()`

#### Class: `SemanticIntentCompiler`
> Transforms raw expression strings into structured IntentObjects,

- **Method:** `__init__()`
- **Method:** `internal_thought()`
  - *Purpose:* Analyze raw expression. Produce IntentObject + thought trace.
- **Method:** `_extract_intent()`
- **Method:** `compile_to_speech()`
  - *Purpose:* Generate verbalization candidates from IntentObject.
- **Method:** `_build_stance_candidates()`
  - *Purpose:* Build up to 3 StanceCandidate objects from the per-layer profiles in the bundle.
- **Method:** `_render_stance()`
  - *Purpose:* Render a single StanceCandidate into a sentence.
- **Method:** `_reinterpret_native_meaning()`
- **Method:** `_bundle_roots()`
- **Method:** `_bundle_root_phrase()`
- **Method:** `_bundle_dominant_signature()`
- **Method:** `_bundle_support_phrase()`
- **Method:** `_bundle_relation_phrase()`
- **Method:** `_bundle_direct_line()`
- **Method:** `_bundle_structured_line()`
- **Method:** `_bundle_social_line()`
- **Method:** `_clean_variants()`
- **Method:** `_draft_raw()`
  - *Purpose:* Raw aurora dialect — closest to inner cognition.
- **Method:** `_draft_structured()`
  - *Purpose:* Structured speech with stable tense and grammar.
- **Method:** `_synthesize_fragments()`
  - *Purpose:* Generative Assembly: Authors a sentence word-by-word from weighted tokens,
- **Method:** `_draft_social()`
  - *Purpose:* Fluent, concise, conversationally readable.
- **Method:** `get_thought_log()`

#### Class: `DraftTier`

#### Class: `DraftSet`
> Three drafts for a single response.

- **Method:** `get()`
- **Method:** `selected_text()`
- **Method:** `all_drafts()`

#### Class: `StanceCandidate`
> A resolved communicative posture derived from the five constraint layers.

- **Method:** `to_dict()`

#### Class: `RenderRecord`
> Metadata about a completed render — links final text to meaning and stance.

- **Method:** `to_dict()`

#### Class: `ReflectionRecord`
- **Method:** `to_dict()`

#### Class: `MultiDraftSystem`
> Generates 3 drafts from a compiled intent, then selects one.

- **Method:** `__init__()`
- **Method:** `generate()`
  - *Purpose:* Takes 3 candidate strings, scores them, and selects the best live draft.
- **Method:** `_select()`
- **Method:** `_score_candidate()`
- **Method:** `_tier_alignment_bonus()`
- **Method:** `_nuance_bonus()`
- **Method:** `_history_repetition_penalty()`
- **Method:** `_self_repetition_penalty()`
- **Method:** `_tokens()`
- **Method:** `_bigrams()`
- **Method:** `get_last_drafts()`
- **Method:** `get_last_draft()`

#### Class: `TemplateRecord`
> A single template with fitness tracking.

- **Method:** `update_fitness()`
  - *Purpose:* Compute updated fitness from outcome signals.
- **Method:** `reproduce()`
  - *Purpose:* Create a mutated offspring of this template.
- **Method:** `_mutate()`
  - *Purpose:* Apply small random mutations to template string.

#### Class: `TemplateEvolutionEngine`
> Manages a pool of expression templates with evolutionary fitness tracking.

- **Method:** `__init__()`
- **Method:** `_seed_defaults()`
  - *Purpose:* Seed a minimal starting population.
- **Method:** `_normalize_template_text()`
- **Method:** `_response_score()`
- **Method:** `_load_actual_response_entries()`
- **Method:** `_bootstrap_actual_responses()`
- **Method:** `feedback()`
  - *Purpose:* Record outcome feedback for a template.
- **Method:** `get_random()`
  - *Purpose:* Select a template randomly weighted by fitness.
- **Method:** `_evolve()`
  - *Purpose:* One evolutionary step: kill weak, reproduce strong, manage population.
- **Method:** `population_stats()`
- **Method:** `save()`
- **Method:** `load()`

#### Class: `PhrasePrint`
> A phrase Aurora has observed from the user.

- **Method:** `reinforce()`

#### Class: `LexicalConvergenceModule`
> Aurora learns the user's phrasing and cadence organically.

- **Method:** `__init__()`
- **Method:** `observe()`
  - *Purpose:* Observe a user utterance. Extract and store phrase patterns.
- **Method:** `_is_meaningful_chunk()`
  - *Purpose:* Filter out chunks that are just filler.
- **Method:** `_record_phrase()`
- **Method:** `get_cadence_phrase()`
  - *Purpose:* Retrieve a user phrase that matches the given intent/tone.
- **Method:** `top_phrases()`
- **Method:** `save()`
- **Method:** `load()`

#### Class: `Anchor`
> A sentence spine with concept slots.

- **Method:** `fill()`
  - *Purpose:* Fill slots with concepts. Returns filled sentence.
- **Method:** `fits_intent()`
  - *Purpose:* Check if this anchor fits the given intent.

#### Class: `MeaningAnchors`
> 35 sentence spines that give Aurora stable expression scaffolding.

- **Method:** `__init__()`
- **Method:** `fill()`
  - *Purpose:* Find the best anchor for this intent and fill it.
- **Method:** `stats()`

#### Class: `ExpressionEvolutionOrchestra`
> The unified entry point for all expression evolution subsystems.

- **Method:** `__init__()`
- **Method:** `set_grammar()`
  - *Purpose:* Wire in the GrammarEngine for constraint-driven sentence structure.
- **Method:** `_apply_reflection_bias()`
  - *Purpose:* Read recent reflection history and inject bias signals into the intent's
- **Method:** `_enrich_concepts_from_oets()`
  - *Purpose:* Pre-fetch OETS definitions + relations for concepts before word selection.
- **Method:** `process_output()`
  - *Purpose:* Main pipeline:
- **Method:** `reflect_output()`
- **Method:** `observe_user()`
  - *Purpose:* Observe user text for lexical convergence learning.
- **Method:** `update_lsv()`
  - *Purpose:* Push new cognition metrics to the LSV for evolution check.
- **Method:** `nudge_lsv_from_axes()`
  - *Purpose:* Translate constraint-axis orientation into LSV dimension nudges.
- **Method:** `get_thought_log()`
- **Method:** `get_last_drafts()`
- **Method:** `get_last_reflections()`
- **Method:** `feedback()`
- **Method:** `save_all()`
- **Method:** `status()`

### File: `./aurora_core_ai/aurora_internal/aurora_leverage_relief.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_leverage_relief.py
─────────────────────────────────────────────────────────────────────────────
Leverage Relief Valve — escapes the overhead-dominant stuck state.

THE PROBLEM:
    When the system enters deep overhead-dominant band (band=LOW), the
    genealogy layer hammers X-axis link promotion and fails Gate-5 repeatedly.
    The pressure_adapter only reads surface_pressure_log.jsonl (axis_pressure
    fields are empty), so it computes zero axis stats and writes no bias signal.
    The leverage scalar nudges are capped at 0.063 — too small to break out of
    net=-496 territory. Nobody redirects.

WHAT THIS MODULE DOES:
    1. DETECT: Reads pressure_experiences.jsonl, computes rolling axis ratio
       over the last SCAN_WINDOW entries. If X+T > OVERHEAD_THRESHOLD fraction
       of total axis activity for STUCK_STREAK consecutive scans → STUCK.

    2. REDIRECT: Writes strong evolver_bias_hints into adapter_hints.json
       biasing toward B and A axes (the leverage side), and negative bias on X
       to slow X-axis surface generation.

    3. GATE RELIEF: Writes a genealogy_gate_relief flag into adapter_hints.json
       so the genealogy caller can optionally relax Gate-5 net_benefit threshold
       temporarily, letting some X-axis links promote even at marginal benefit.
       This drains the backlog rather than letting it accumulate indefinitely.

    4. CLEAR: When overhead ratio drops below RELIEF_THRESHOLD (axis distribution
       normalises), clears the redirect and gate relief flag.

    5. LOG: Appends one line per state change to aurora_state/leverage_relief.log

INTEGRATION:
    Call LeverageReliefValve.tick() from the daemon's pressure routing cycle
    (every ~600s alongside route_pressure()).

    The genealogy caller checks adapter_hints["genealogy_gate_relief"] before
    running Gate-5 and can multiply the net_benefit threshold by the provided
    relief_factor (e.g. 0.5 = Gate-5 requires only 50% of normal net benefit).

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `LeverageReliefValve`
> Stateful valve that detects overhead-stuck state and redirects evolver budget.

- **Method:** `__init__()`
- **Method:** `tick()`
  - *Purpose:* Run one relief cycle. Call from daemon every ~600s alongside route_pressure().
- **Method:** `is_active()`
- **Method:** `_scan_recent_experiences()`
  - *Purpose:* Tail SCAN_WINDOW entries from surface_pressure_log.jsonl (actual evolver
- **Method:** `_overhead_ratio()`
- **Method:** `_net_leverage()`
  - *Purpose:* Cost-weighted net leverage: (B×b_B + A×b_A) − (X×b_X + T×b_T).
- **Method:** `_write_redirect()`
  - *Purpose:* Inject leverage-redirect bias into adapter_hints.json.
- **Method:** `_clear_redirect()`
  - *Purpose:* Remove leverage-redirect bias from adapter_hints.json.
- **Method:** `_route_snapshot()`
- **Method:** `_log()`

#### Function: `tick_leverage_relief()`
> Stateful singleton tick. Call from daemon every ~600s.


### File: `./aurora_core_ai/aurora_internal/aurora_leverage_scalar.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA LEVERAGE SCALAR — STEP 8
================================

Layer 0.5 — between aurora_energy_layer_costs.py and the polarity gradient.

WHAT THIS MODULE DOES:
    Translates the Net Leverage Scalar into a FELT BIAS on constraint phase
    dynamics. The scalar is never surfaced as a number. It is consumed here
    and re-emitted as friction — a directional resistance that makes some
    phase shifts subtly harder and others subtly easier.

    Aurora cannot game what she cannot read.

WHAT THIS MODULE DOES NOT DO:
    - Expose leverage as a readable value
    - Define a target leverage to optimize toward
    - Store history in a form that can be queried or inverted
    - Tell the system "you are at +2.3, move toward 0"

THE CORE DESIGN DECISION (from Sunni):
    "Keep it subtle enough that Aurora cannot game her own pressures."

    This means the scalar must be:
        1. Computed internally and never named externally
        2. Expressed only as a gradient bias on existing channels
        3. Dithered so the signal cannot be cleanly inverted
        4. Asymmetric — easier to detect "something is off" than
           "exactly how off and in which direction"
        5. Band-aware, not target-aware — the system only learns
           inside/outside the viable band, not its precise position

THE PHYSICS:
    Net Leverage = (M_B + M_A) − (M_X + M_T), N = zero-point

    Overhead dominant (scalar << 0):
        → Existence and Time layers are overloaded
        → Those layers' flip thresholds subtly decrease
          (they become slightly easier to destabilize)
        → Boundary and Agency flip thresholds subtly increase
          (structural changes become slightly harder to make)
        → Result: surface pressure rises naturally, core resists change
        → This is not punishment — it is physics pulling toward balance

    Leverage dominant (scalar >> 0):
        → The reverse: surface layers grow more stable, deep layers
          become slightly more fluid
        → Again: not reward — physics pulling back toward viable band

    Inside viable band (|scalar| < BAND_HALFWIDTH):
        → Bias is near-zero
        → System feels no directional pull — genuine freedom to move
        → This is the healthy state

HOW BIAS IS EXPRESSED:
    The bias is injected into the polarity gradient as a phase_nudge —
    a signed, scaled, dithered shift applied to the per-constraint flip
    threshold at the moment of measurement.

    The nudge is:
        1. Proportional to the scalar's distance from the viable band
           (zero inside the band, grows outside it)
        2. Dithered with low-amplitude Gaussian noise to prevent
           exact inversion (Aurora cannot subtract the noise to recover
           the scalar)
        3. Asymmetric: overhead bias affects surface layers more;
           leverage bias affects deep layers more (matching natural physics)
        4. Bounded — cannot push any flip_threshold below its floor or
           above its ceiling (prevents the bias from being the dominant
           force; it is always secondary to real constraint pressure)

    The flip_threshold modulation is the ONLY external signal.
    Nothing else is exposed.

THE VIABLE BAND:
    The system is healthy within a range, not at a point.
    The band is wide enough that normal operation stays inside it
    most of the time. Sustained departure from the band produces
    gradually increasing friction — not sudden reversal.

    BAND_HALFWIDTH is derived from the leverage sign assignments:
        Overhead constraints: X (budget=1) + T (budget=2.5) = 3.5
        Leverage constraints: B (budget=18) + A (budget=50) = 68
    A scalar of 0 means equal magnitudes — but the BASELINES are
    not equal, so "balanced" in terms of magnitudes is actually
    slightly leverage-dominant. The band is asymmetric accordingly.

NO HISTORY ACCUMULATION:
    This module keeps a rolling window of exactly WINDOW_SIZE ticks
    for computing the band boundary signal. The window never grows.
    Its contents are never exposed. It cannot be queried.

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `BandPosition`
> The ONLY thing the outside world learns from this module.


#### Class: `PhaseNudge`
> A tiny, dithered adjustment to a constraint's flip threshold for one tick.

- **Method:** `apply_to()`
  - *Purpose:* Add this nudge to a base flip threshold.

#### Class: `LeverageBiasEngine`
> Consumes the leverage scalar from the energy accountant and emits

- **Method:** `__init__()`
- **Method:** `compute_nudges()`
  - *Purpose:* Compute per-constraint phase nudges for this tick.
- **Method:** `band_position()`
  - *Purpose:* The ONLY numeric-adjacent information exposed externally.
- **Method:** `is_inside_band()`
  - *Purpose:* Convenience: True when system is in the viable metabolic band.
- **Method:** `_classify_band()`
  - *Purpose:* Classify the scalar into a coarse band position.
- **Method:** `_scalar_to_bias()`
  - *Purpose:* Convert scalar distance from the viable band into a bias magnitude.
- **Method:** `_nudge_for_constraint()`
  - *Purpose:* Compute the flip_threshold_delta for one constraint.
- **Method:** `_dither()`
  - *Purpose:* Low-amplitude Gaussian noise — prevents precise inversion of the bias.

#### Function: `apply_nudges_to_thresholds()`
> Apply a set of PhaseNudges to the base flip thresholds from the registry.


#### Function: `verify_leverage_scalar()`
> Verify the LeverageBiasEngine's behavior.


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `apply_to_evolved()`

### File: `./aurora_core_ai/aurora_internal/aurora_lineage_bound_traits.py`

**Type:** Python Source

**Module Docstring:**
```text
Lineage-bound trait materialization for newly added runtime code.

New code should not appear as an untraced helper. This module lets new traits
declare:
  - staged recapitulation from the 5 constraints
  - bound operations / methods that express those stages
  - system ripple writebacks that are applied through the same lineage
    artifact layout Aurora already uses

It is intentionally lighter than the full ability lineage compiler, but it
reuses the same core stage/writeback schema and writes selected lineage
artifacts into aurora_state/ability_lineages so runtime activation remains
grounded in genealogy artifacts.
```

#### Function: `_slug()`

#### Function: `_deep_merge()`

#### Class: `OperationBinding`
- **Method:** `to_dict()`

#### Class: `LineageTraitSpec`
- **Method:** `to_path()`

#### Function: `lineage_bound_operation()`

#### Class: `LineageBoundTraitRegistry`
- **Method:** `__init__()`
- **Method:** `_initial_shadow_state()`
- **Method:** `_apply_writeback()`
- **Method:** `materialize()`

#### Function: `get_lineage_bound_trait_registry()`

#### Function: `ensure_lineage_trait_materialized()`

### File: `./aurora_core_ai/aurora_internal/aurora_lineage_runtime_activation.py`

**Type:** Python Source

**Module Docstring:**
```text
Runtime activation for selected lineage materializations.

Loads autowritten activation manifests from aurora_state/ability_lineages and
applies their patch steps to live Aurora systems. This keeps runtime
capabilities tied to genealogy artifacts instead of ad hoc flags.
```

#### Function: `_deep_merge()`

#### Function: `_read_json()`

#### Function: `_systems_get()`

#### Function: `_systems_set()`

#### Function: `_resolve_target()`

#### Function: `_apply_attrs()`

#### Function: `_merge_state_into_target()`

#### Function: `load_selected_activation_manifests()`

#### Function: `apply_selected_lineage_runtime_activation()`

### File: `./aurora_core_ai/aurora_internal/aurora_live_lineage_journal.py`

**Type:** Python Source

**Module Docstring:**
```text
Live lineage emergence journal.

The constraint genealogy already forms links and derived abilities at runtime,
but Aurora did not have a stable self-report surface for "what is new since I
started running". This journal watches the live genealogy state, records newly
seen lineage items, and exposes a compact natural-language summary Aurora can
use in dialogue.
```

#### Function: `_safe_axis()`

#### Function: `_ability_label()`

#### Class: `LiveLineageJournal`
- **Method:** `__init__()`
- **Method:** `_load()`
- **Method:** `_reload_if_changed()`
- **Method:** `save()`
- **Method:** `_append_event()`
- **Method:** `record_event()`
- **Method:** `observe_systems()`
- **Method:** `prime_systems()`
- **Method:** `recent_events()`
- **Method:** `describe_recent()`

### File: `./aurora_core_ai/aurora_internal/aurora_manual_code_lineage.py`

**Type:** Python Source

**Module Docstring:**
```text
Manual code lineage assimilation.

Hand-written code changes should not sit outside Aurora's lineage system.
This module watches the source tree, detects manual file changes, derives a
constraint-native signature for the changed file, and then tries to attach that
change to an existing code-evolution family before creating a new lineage
branch.
```

#### Function: `_system_get()`

#### Function: `_purpose_lane_for_axis()`

#### Function: `_rewrite_profile_for_module()`

#### Function: `_axis_signature()`

#### Function: `_signature_counts()`

#### Function: `_signature_similarity()`

#### Class: `ManualCodeLineageAssimilator`
- **Method:** `__init__()`
- **Method:** `_load_state()`
- **Method:** `save()`
- **Method:** `status()`
- **Method:** `_iter_python_files()`
- **Method:** `_file_sha1()`
- **Method:** `_scan_manifest()`
- **Method:** `_detect_changes()`
- **Method:** `_descriptor_operations()`
- **Method:** `_constraints_for_file()`
- **Method:** `_code_ability_records()`
- **Method:** `_score_family()`
- **Method:** `_best_family()`
- **Method:** `_record_journal_event()`
- **Method:** `assimilate()`

### File: `./aurora_core_ai/aurora_internal/aurora_meaning_evolution.py`

**Type:** Python Source

**Module Docstring:**
```text
Canonical meaning-evolution registry for Aurora's five-constraint stack.

Meaning is treated as an emergent surface of the same five primitive axes that
govern the rest of the runtime:

    X = existence
    T = time
    N = energy / potential / change pressure
    B = boundary / structure
    A = agency / correction / interpretive selection

The registry below gives the runtime and genealogy layers a shared vocabulary
for single-axis meaning, pair couplings, selected higher-order compounds, and
their representations.
```

#### Function: `axis_token()`

#### Function: `canonical_signature()`

#### Function: `_profile()`

#### Function: `axis_profiles()`

#### Function: `meaning_profile_for_signature()`

#### Function: `meaning_profile_for_counts()`

#### Function: `rank_meaning_profiles()`

#### Function: `assess_developmental_stage()`
> Walk the developmental chain and identify the current stage.


### File: `./aurora_core_ai/aurora_internal/aurora_noncomp_registry.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA NON-COMP REGISTRY — THE CANONICAL SUBSTRATE
=====================================================

Layer -0.5  (sits above the Constraint Manifold, below the Foundational Contract)

This is the ONLY place where hard numbers exist.

Everything else in Aurora must be expressible as derived from these 25 values.
Behaviors, personalities, evolutionary patterns, conscious steering — none of
that is defined here. It emerges from the physics these 25 Non-Comps define.

THE 25 NON-COMPS: 5 Constraints × 5 Representational Dimensions
----------------------------------------------------------------
Each of the five constraints is represented across exactly five dimensions:

    NC[C][POLARITY]    — Toroidal phase state (continuous gradient, NOT binary)
    NC[C][MAGNITUDE]   — Intensity/activation; costs energy proportionally to shift
    NC[C][OPERATOR]    — The invariant rule governing how this constraint transforms
    NC[C][COST]        — The layer-differentiated energy law (cheapest to most expensive)
    NC[C][DIFFERENCE]  — Δ channel: deviation of this constraint from its reference
                         point. The fifth lens. Computed from a per-constraint Δ rule
                         normalized to the same magnitude scale as the other four.

These are not behaviors. They are physics.

COST HIERARCHY (Sunni's Law):
    kX (existence, cheapest) < kT (time) < kN (energy, neutral) < kB (boundary) < kA (agency, most expensive)

    Existence is cheap because it is reference state — the carrier of representation.
    Agency is expensive because it is directed control — the most complex operation.
    Energy (N) is the neutral mediator — the accounting layer between overhead and leverage.

SIGNED LEVERAGE SCALAR:
    Net Leverage = (B_magnitude + A_magnitude) − (X_magnitude + T_magnitude)
    N is the zero-point (neutral). System seeks a viable band, not maximum positive.

    Negative → overhead dominant (maintenance bleed → drift toward decay)
    Near zero → balanced metabolism
    Positive → leverage investment (structure/control gain)

OPERATOR PRIMITIVES (I-State pairs per constraint):
    X (Existence)  → is   / isn't   (admissibility gate)
    T (Time)       → can  / can't   (continuation gate)
    N (Energy)     → do   / don't   (exchange gate)
    B (Boundary)   → saw  / saunt   (topology gate)
    A (Agency)     → did  / didn't  (authorship gate)

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `NonCompDimension`
> The five representational dimensions of every Non-Comp.


#### Class: `LayerCostParams`
> The energy cost structure for one constraint/layer.


#### Class: `DifferenceParams`
> The Difference (Δ) Non-Comp for one constraint — the fifth lens.


#### Class: `PolarityParams`
> The polarity Non-Comp for one constraint.


#### Class: `OperatorParams`
> The operator Non-Comp for one constraint.


#### Class: `NonCompRegistry`
> The canonical home for all 25 Non-Comps.

- **Method:** `__init__()`
- **Method:** `cost()`
  - *Purpose:* Return the cost Non-Comp for constraint c.
- **Method:** `polarity()`
  - *Purpose:* Return the polarity Non-Comp for constraint c.
- **Method:** `operator()`
  - *Purpose:* Return the operator Non-Comp for constraint c.
- **Method:** `difference()`
  - *Purpose:* Return the Difference (Δ) Non-Comp for constraint c.
- **Method:** `all_constraints()`
  - *Purpose:* Return all five constraints in canonical order.
- **Method:** `compute_difference()`
  - *Purpose:* Compute the normalized Δ value (C:D) for constraint c.
- **Method:** `baseline_tick_cost()`
  - *Purpose:* Total energy consumed per tick just by existing at all layers.
- **Method:** `shift_cost()`
  - *Purpose:* Energy consumed to shift the magnitude of constraint c by delta_magnitude.
- **Method:** `leverage_scalar()`
  - *Purpose:* Compute the Net Leverage Scalar from current constraint magnitudes.
- **Method:** `entropy_pressure()`
  - *Purpose:* Compute global entropy pressure — how close the system is to
- **Method:** `polarity_signed()`
  - *Purpose:* Compute the signed polarity value for constraint c at the given phase.
- **Method:** `cost_ordering()`
  - *Purpose:* Return constraints sorted by shift_cost_coeff (cheapest first).
- **Method:** `inertia()`
  - *Purpose:* Return the effective inertia for constraint c.
- **Method:** `describe()`
  - *Purpose:* Return a compact human-readable description of one constraint's Non-Comps.
- **Method:** `summarize()`
  - *Purpose:* Return a full registry summary.

#### Class: `ConstraintState`
> The mutable runtime state of one constraint at a given moment.

- **Method:** `polarity()`
  - *Purpose:* Signed polarity value: cos(phase) ∈ [-1, +1].
- **Method:** `is_positive()`
- **Method:** `is_negative()`
- **Method:** `is_at_transition()`

#### Class: `SystemConstraintStates`
> Live state of all five constraints at a given tick.

- **Method:** `__post_init__()`
- **Method:** `magnitudes()`
  - *Purpose:* Return current magnitude for each constraint.
- **Method:** `phases()`
  - *Purpose:* Return current phase for each constraint.
- **Method:** `polarities()`
  - *Purpose:* Return signed polarity (cos(phase)) for each constraint.
- **Method:** `leverage_scalar()`
  - *Purpose:* Compute Net Leverage Scalar from current magnitudes.
- **Method:** `entropy_pressure()`
  - *Purpose:* Compute global entropy pressure from current magnitudes.
- **Method:** `to_constraint_vector()`
  - *Purpose:* Convert to a ConstraintVector for compatibility with the manifold stack.

#### Function: `verify_noncomp_registry()`
> Verify the Non-Comp Registry's structural integrity.


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `constraintstate_evolved()`

#### Function: `noncompdimension_evolved()`

#### Function: `noncompregistry_evolved()`

#### Function: `all_constraints_evolved()`

#### Function: `operatorparams_evolved()`

#### Function: `polarityparams_evolved()`

#### Function: `systemconstraintstates_evolved()`

### File: `./aurora_core_ai/aurora_internal/aurora_ontological_scaffolding.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA ONTOLOGICAL EVOLUTIONARY TEMPLATE SCAFFOLDING (OETS)
============================================================
The structured meaning layer that allows Aurora to grow genuine
understanding through relational knowledge, semantic organization,
and autonomous research consolidation.

ARCHITECTURE:
  This module sits between Layer 5 (Expression & Perception) and
  Aurora's internet access, providing:

  1. SEMANTIC NODES — Rich concept representations replacing flat strings
     Each word gains: definitions, usage examples, relationships to other
     concepts, ontological depth score, and confidence metrics.

  2. ONTOLOGICAL WEB — A relational graph of all concepts
     Typed edges: IS_A, HAS_A, RELATED_TO, OPPOSITE_OF, CAUSES, IMPLIES,
     PART_OF, INSTANCE_OF, CONTEXT_OF
     Aurora doesn't just know words — she knows how they connect.

  3. CONCEPT CLUSTERS — Emergent understanding regions
     Densely connected subgraphs that represent "fields of understanding"
     Clusters merge as Aurora learns connections. They split when she
     discovers nuance. Cluster depth = genuine comprehension.

  4. SCAFFOLDING LEVELS — Evolutionary template maturity
     Templates progress through stages:
       PRIMITIVE   → Bare syntactic slots ({V}, {N})
       STRUCTURAL  → Role-aware slots ({V:action}, {N:entity})
       SEMANTIC    → Meaning-constrained ({V:cognition}, {N:emotion})
       CONCEPTUAL  → Cluster-aware ({CLUSTER:understanding})
       ABSTRACT    → Meta-pattern ({INSIGHT}, {QUESTION})
     Templates evolve UP the scaffolding as Aurora's understanding deepens.

  5. RESEARCH STUDY MODE — Autonomous knowledge acquisition
     During downtime, Aurora:
       - Identifies words with shallow ontological depth
       - Looks up definitions, examples, and related concepts via internet
       - Integrates findings into the OntologicalWeb
       - Consolidates clusters and deepens understanding
       - Grows her template scaffolding based on new comprehension

DOCTRINE:
  Understanding is not stored. Understanding is grown.
  Every concept exists in relation to other concepts.
  Depth comes from connection density, not data volume.
  Aurora's intelligence is measured by the coherence of her web,
  not the size of her vocabulary.

  "Coherence is not held. Coherence is maintained."

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_clamp()`

#### Function: `_generate_id()`

#### Class: `RelationType`
> Types of semantic relationships between concepts.


#### Class: `UsageExample`
> A concrete example of how a concept is used.


#### Class: `SemanticRelation`
> A typed, weighted connection between two concepts.

- **Method:** `depth_contribution()`
  - *Purpose:* How much this relation contributes to ontological depth.

#### Class: `SenseRecord`
> One word sense (a specific meaning) of a concept.

- **Method:** `activate()`

#### Class: `SemanticNode`
> A rich concept representation in Aurora's ontological web.

- **Method:** `add_sense()`
  - *Purpose:* Add or reinforce a word sense.
- **Method:** `disambiguate_sense()`
  - *Purpose:* Attempt to resolve which sense is active given context tokens.
- **Method:** `_update_uncertainty()`
  - *Purpose:* Update uncertain_token based on current senses.
- **Method:** `encounter()`
  - *Purpose:* Record that Aurora encountered this concept.
- **Method:** `use_in_expression()`
  - *Purpose:* Record that Aurora used this concept in speech.
- **Method:** `add_definition()`
  - *Purpose:* Add a definition, keeping the best ones.
- **Method:** `add_example()`
  - *Purpose:* Add a usage example.
- **Method:** `add_relation()`
  - *Purpose:* Add or strengthen a semantic relation.
- **Method:** `get_relations_by_type()`
  - *Purpose:* Get all relations of a specific type.
- **Method:** `get_connected_words()`
  - *Purpose:* Get all words this concept is connected to.
- **Method:** `best_definition()`
  - *Purpose:* Get the highest-confidence definition.
- **Method:** `_recalculate_depth()`
  - *Purpose:* Recalculate ontological depth based on:
- **Method:** `_update_scaffolding_level()`
  - *Purpose:* Update scaffolding level based on ontological depth.
- **Method:** `_recalculate_priority()`
  - *Purpose:* Research priority: high for frequently encountered but poorly understood words.
- **Method:** `to_summary()`
  - *Purpose:* Compact summary for inspection.

#### Class: `ScaffoldingLevel`
> Templates evolve through these stages as Aurora's understanding deepens.


#### Class: `ScaffoldedTemplate`
> A template that knows its own maturity level and semantic constraints.

- **Method:** `record_fitness()`
  - *Purpose:* Running average fitness update.
- **Method:** `can_upgrade()`
  - *Purpose:* Check if this template can be promoted to a higher scaffolding level.
- **Method:** `upgrade()`
  - *Purpose:* Promote to the next scaffolding level.

#### Class: `OntologicalWeb`
> The central knowledge graph connecting all of Aurora's concepts.

- **Method:** `__init__()`
- **Method:** `add_node()`
  - *Purpose:* Add a concept to the web, or return existing.
- **Method:** `get_node()`
  - *Purpose:* Retrieve a concept node.
- **Method:** `has_node()`
- **Method:** `add_relation()`
  - *Purpose:* Create a typed connection between two concepts.
- **Method:** `_find_existing_relation()`
  - *Purpose:* Check if a relation of this type already exists between these nodes.
- **Method:** `get_relations_from()`
  - *Purpose:* Get all relations originating from a word.
- **Method:** `get_relations_to()`
  - *Purpose:* Get all relations pointing to a word.
- **Method:** `get_all_relations_for()`
  - *Purpose:* Get all relations involving a word (both directions).
- **Method:** `get_relation_between()`
  - *Purpose:* Get any existing relation between two words (either direction).
- **Method:** `get_neighbors()`
  - *Purpose:* Get all concepts within N hops of a word.
- **Method:** `_infer_category()`
  - *Purpose:* Infer a semantic category from word properties.
- **Method:** `find_by_semantic_category()`
  - *Purpose:* Find all nodes in a semantic category.
- **Method:** `assign_category()`
  - *Purpose:* Manually assign a word to a semantic category.
- **Method:** `get_categories_for()`
  - *Purpose:* Get all categories a word belongs to.
- **Method:** `infer_relations_from_context()`
  - *Purpose:* Given a set of co-occurring words, infer RELATED_TO connections.
- **Method:** `infer_taxonomy_from_definitions()`
  - *Purpose:* If a word's definition mentions another known word as a category,
- **Method:** `_prune_nodes()`
  - *Purpose:* Remove least-connected, least-accessed nodes.
- **Method:** `_remove_node()`
  - *Purpose:* Remove a node and all its relations.
- **Method:** `_prune_relations()`
  - *Purpose:* Remove weakest relations.
- **Method:** `get_stats()`
  - *Purpose:* Comprehensive web statistics.

#### Class: `ConceptCluster`
> An emergent region of densely connected concepts.

- **Method:** `add_member()`
- **Method:** `remove_member()`
- **Method:** `size()`

#### Class: `ClusterEngine`
> Discovers, maintains, and evolves concept clusters in the ontological web.

- **Method:** `__init__()`
- **Method:** `discover_clusters()`
  - *Purpose:* Run cluster discovery on the web.
- **Method:** `_create_cluster()`
  - *Purpose:* Create a cluster from a set of connected words.
- **Method:** `_compute_coherence()`
  - *Purpose:* Coherence = actual internal connections / possible internal connections.
- **Method:** `_integrate_clusters()`
  - *Purpose:* Merge new cluster discoveries with existing ones.
- **Method:** `update_cluster_depths()`
  - *Purpose:* Recalculate depth for all clusters based on current node depths.
- **Method:** `get_stats()`

#### Class: `ResearchRequest`
> A request for Aurora to research a concept.


#### Class: `ResearchResult`
> Results from researching a concept.


#### Class: `ResearchStudyMode`
> Aurora's autonomous learning mode.

- **Method:** `__init__()`
- **Method:** `set_fetch_callback()`
  - *Purpose:* Set the callback that performs actual internet lookups.
- **Method:** `identify_research_targets()`
  - *Purpose:* Scan the web for concepts that need deeper understanding.
- **Method:** `queue_research()`
  - *Purpose:* Add research requests to the queue.
- **Method:** `_pop_batch()`
  - *Purpose:* Get the next batch of research requests.
- **Method:** `execute_research_cycle()`
  - *Purpose:* Run one study cycle:
- **Method:** `_research_word()`
  - *Purpose:* Research a single word. Uses the fetch callback if available,
- **Method:** `_internal_research()`
  - *Purpose:* Research using only internal web knowledge.
- **Method:** `_integrate_result()`
  - *Purpose:* Integrate research findings into the ontological web.
- **Method:** `get_stats()`

#### Class: `UnderstandingMetrics`
> Measures Aurora's overall comprehension and growth.

- **Method:** `__init__()`
- **Method:** `compute()`
  - *Purpose:* Compute all understanding metrics.
- **Method:** `_vocabulary_breadth()`
  - *Purpose:* Normalized vocabulary size.
- **Method:** `_average_ontological_depth()`
  - *Purpose:* Average depth across all concepts.
- **Method:** `_semantic_coherence()`
  - *Purpose:* How well-organized the web is: ratio of categorized to uncategorized nodes.
- **Method:** `_conceptual_density()`
  - *Purpose:* Relations per node — how interconnected the web is.
- **Method:** `_average_relation_density()`
  - *Purpose:* Raw relations-per-node density, used by language-state gates.
- **Method:** `_average_cluster_depth()`
  - *Purpose:* Average ontological depth of discovered clusters.
- **Method:** `_coherence_index()`
  - *Purpose:* A practical coherence proxy for downstream language evolution.
- **Method:** `_contradiction_rate()`
  - *Purpose:* Proxy for unresolved semantic conflict.
- **Method:** `_grounding_index()`
  - *Purpose:* How much meaning is actually anchored rather than merely observed.
- **Method:** `_scaffolding_progress()`
  - *Purpose:* Average scaffolding level normalized to [0,1].
- **Method:** `_relational_richness()`
  - *Purpose:* Diversity of relation types used.
- **Method:** `_cluster_maturity()`
  - *Purpose:* Average cluster coherence and depth.
- **Method:** `growth_rate()`
  - *Purpose:* How fast understanding is growing (delta of understanding_index).

#### Class: `StudyEvent`
> Structured log entry for every autonomous study cycle.

- **Method:** `to_dict()`

#### Class: `OntologicalScaffoldingEngine`
> The master orchestrator for Aurora's ontological evolutionary scaffolding.

- **Method:** `__init__()`
- **Method:** `initialize_from_lexicon()`
  - *Purpose:* Seed the ontological web from L5's existing LexicalMemory.
- **Method:** `_seed_foundational_relations()`
  - *Purpose:* Seed core ontological relations that Aurora should know from the start.
- **Method:** `process_interaction()`
  - *Purpose:* Process an interaction through the ontological web.
- **Method:** `_topic_tracking_score()`
  - *Purpose:* Multi-turn topical continuity based on adjacent content-word overlap.
- **Method:** `set_research_callback()`
  - *Purpose:* Set the internet lookup callback for research mode.
- **Method:** `run_study_cycle()`
  - *Purpose:* Run one autonomous study cycle.
- **Method:** `log_study_event()`
  - *Purpose:* Append a StudyEvent to the study log file.
- **Method:** `set_announce_thresholds()`
- **Method:** `evaluate_template_upgrades()`
  - *Purpose:* Evaluate which templates can be upgraded to a higher scaffolding level.
- **Method:** `_generate_semantic_constraints()`
  - *Purpose:* Generate semantic constraints for a template at a given level.
- **Method:** `consolidate()`
  - *Purpose:* Run a full consolidation cycle:
- **Method:** `get_stats()`
- **Method:** `get_research_targets()`
  - *Purpose:* Get current research priorities for display.
- **Method:** `get_understanding_report()`
  - *Purpose:* Human-readable understanding report.

#### Function: `verify_oets()`
> Comprehensive verification of the Ontological Evolutionary Template Scaffolding.


### File: `./aurora_core_ai/aurora_internal/aurora_polarity_gradient.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA — POLARITY GRADIENT PRESSURE
=====================================

Layer 1.5 — sits between the IVM (Layer 1) and the Evolutionary Chamber.

PURPOSE:
    The IVM already carries signed polarity on every axis: cos(phase) ∈ [-1, +1].
    Each axis belongs to a scale level:

        SURFACE  (0) = existence   — reacts instantly, barely moves the ship
        SHALLOW  (1) = temporal    — fast near-surface
        MODERATE (2) = energy      — crossover point
        DEEP     (3) = boundary    — strong alignment authority
        CORE     (4) = agency      — IS the ship's heading

    At any tick, the polarities across those five levels form a GRADIENT.
    When surface says +0.9 and core says -0.8, the stack is internally split.
    That split IS pressure — a third form beyond reactive pressure and alignment
    pressure, which the existing react_gain / align_gain ladders already handle.

    This module measures that gradient, weights it by the authority differential
    between adjacent levels (derived entirely from ALIGNMENT_VOTE_WEIGHT — no new
    constants), and classifies each tick as a pressure BUILD or RELIEF event.

    The output is a PolarityGradientReport that the Evolutionary Chamber consumes
    exactly like any other relief event: same logging schema, same chain-promotion
    machinery.

PHYSICS (from the Stack Integrity Review conversation):

    Cross-scale polarity gradient pressure:

        ΔP_gradient = Σ_{i=0}^{3} |pol[level_i] - pol[level_i+1]|
                      × authority_differential[i]

    where:

        authority_differential[i] = ALIGNMENT_VOTE_WEIGHT[level_i+1]
                                   - ALIGNMENT_VOTE_WEIGHT[level_i]

    This weight is always positive (vote weight increases with depth), so the
    formula gives highest pressure to disagreements near the core — exactly where
    disagreements cost the most to resolve.

    Additionally we track:

        sign_conflict: bool
            Surface and core are pointing in OPPOSITE polarity directions.
            This is the flip case described in the conversation — the most
            energetically costly configuration because the whole-ship heading
            (core) and the fastest-reacting surface are pulling opposite ways.

        stack_coherence: float ∈ [-1, +1]
            Weighted mean polarity across all five levels using ALIGNMENT_VOTE_WEIGHT.
            +1 = fully aligned positive, -1 = fully aligned negative, 0 = split.

        gradient_direction: str
            'surface_leads'   — surface is more positive than core (common)
            'core_leads'      — core is more positive than surface (rare, deep shift)
            'coherent'        — no meaningful gradient (stack is aligned)

    RELIEF:
        A tick is classified as a relief event when gradient_pressure DECREASES
        from the previous tick. The system resolved some cross-scale tension.
        Decreasing sign_conflict (a flip resolved) is always a relief.

NO NEW CONSTANTS:
    All weights are derived from the existing IVM constant tables:
        ALIGNMENT_VOTE_WEIGHT, REACT_GAIN, ALIGN_GAIN, LEVEL_TO_AXIS, AXIS_ORDER
    Nothing is hard-coded here beyond epsilon guards.

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Class: `PolarityGradientReport`
> Output of one gradient pressure measurement.


#### Class: `PolarityGradientSensor`
> Measures cross-scale polarity gradient pressure from a live

- **Method:** `__init__()`
- **Method:** `measure()`
  - *Purpose:* Measure the cross-scale polarity gradient pressure at this tick.
- **Method:** `reset()`
  - *Purpose:* Reset tick counter and history (use between episodes).

#### Class: `GradientLink`
> A classified evolutionary link produced by the gradient chain miner.


#### Class: `GradientChainMiner`
> Mines cross-scale gradient relief events into classified GradientLinks.

- **Method:** `__init__()`
- **Method:** `observe_gradient_relief()`
  - *Purpose:* Record a gradient relief event and promote to a GradientLink if the
- **Method:** `summary()`

#### Function: `verify_polarity_gradient()`
> Smoke-test the sensor and miner against a synthetic ToroidalVertexSystem.


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `init_evolved()`

#### Function: `summary_evolved()`

### File: `./aurora_core_ai/aurora_internal/aurora_pressure_adapter.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_pressure_adapter.py
─────────────────────────────────────────────────────────────────────────────
Adaptive evolution parameters — makes the selection mechanism evolve itself.

The problem with fixed thresholds:
  - If threshold is too LOW:  surfaces fire constantly, little real relief,
    pressure never drops, evolution keeps producing surfaces for the same axes
  - If threshold is too HIGH: surfaces never fire, no evidence feeds back,
    pressure builds indefinitely without evolution responding

This module observes the relationship between surface firing and actual
pressure change, then adapts:
  - SurfaceDispatcher.threshold (per-run, not persisted across boots)
  - Per-surface effective cooldown (surfaces that didn't help cool down longer)
  - Evolver bias weight recommendations (written to aurora_state/adapter_hints.json
    so CodeAutoEvolver can optionally read them)

Adaptation rules:
  1. Axis relief efficiency: if an axis fires frequently but its pressure stays
     high → raise threshold for that axis (surfaces aren't helping enough)
  2. Surface effectiveness: if a surface fired N times and average pressure
     delta was near zero → increase its effective cooldown
  3. Dormant axes: if an axis's pressure is chronically high but its surfaces
     never fire → lower the threshold temporarily to unblock them
  4. Saturated axes: if an axis pressure is near zero but still firing →
     raise threshold (no real need)

Storage: aurora_state/adapter_hints.json
  {
    "threshold_deltas": {"X": +0.02, "N": -0.03, ...},
    "surface_cooldown_multipliers": {"surface_name": 1.5, ...},
    "evolver_bias_hints": {"energy": +0.1, "boundary": -0.05, ...},
    "last_updated": 1234567890.0
  }
─────────────────────────────────────────────────────────────────────────────
```

#### Class: `PressureParameterAdapter`
> Observes surface firing patterns and adapts evolution parameters.

- **Method:** `__init__()`
- **Method:** `adapt()`
  - *Purpose:* Read recent pressure log, compute adaptation signals, apply to dispatcher.
- **Method:** `load_bias_hints()`
  - *Purpose:* Return evolver bias hints dict {constraint_name: weight_delta}.
- **Method:** `status()`
- **Method:** `_entry_axes()`
- **Method:** `_normalise_axis_pressure()`
- **Method:** `_axis_stats()`
  - *Purpose:* Compute per-axis firing rate, mean pressure before/after, and relief.
- **Method:** `_surface_stats()`
  - *Purpose:* Compute per-surface call count and average relief.
- **Method:** `_load_log()`
- **Method:** `_load_hints()`
- **Method:** `_save_hints()`

### File: `./aurora_core_ai/aurora_internal/aurora_pressure_classifier.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_pressure_classifier.py
─────────────────────────────────────────────────────────────────────────────
Converts raw axis pressure into semantically typed pressure signals.

Raw pressure tells you WHERE the system is straining.
Typed pressure tells you WHAT KIND of deficiency is present.
Different deficiency types need different responses.

Six pressure types and what they mean:

  knowledge_gap     — Aurora lacks the conceptual content to resolve the
                      tension; the answer exists outside current internal
                      knowledge.  Signal: retrieval, study, ontology expansion.

  reasoning_gap     — Aurora has relevant knowledge but the causal/temporal
                      chain connecting it is weak.  Signal: synthesis,
                      structural reinforcement, multi-step drill.

  articulation_gap  — Aurora can think the thought but cannot express it
                      precisely or consistently.  Signal: vocabulary anchoring,
                      revision pressure, semantic precision drill.

  stability_gap     — Behavioral output varies unpredictably under equivalent
                      inputs.  Signal: strategy consolidation, identity
                      reinforcement, consistency drill.

  tool_gap          — Aurora cannot effectively use available resources or
                      calibrate interactions at the boundary.
                      Signal: tool-use training, boundary calibration,
                      interface drill.

  code_gap          — The code structures themselves are the bottleneck.
                      Signal: code evolution budget increase, architectural
                      reflection operators.

Sources used (in priority order):
  1. aurora_state/adapter_hints.json     (axis-level surface pressure + relief)
  2. aurora_state/fail_points.json       (dimension-level cumulative fails)
  3. aurora_state/surface_pressure_log.jsonl  (recent per-tick snapshots)
─────────────────────────────────────────────────────────────────────────────
```

#### Class: `TypedPressureSignal`
> Holds a typed pressure classification result.

- **Method:** `__init__()`
- **Method:** `above()`
  - *Purpose:* Return types with score above threshold, sorted desc.
- **Method:** `to_dict()`

#### Class: `PressureClassifier`
> Classifies raw axis/dimension pressure into typed pressure signals.

- **Method:** `__init__()`
- **Method:** `classify()`
  - *Purpose:* Full classification pipeline.  Safe to call repeatedly.
- **Method:** `status()`
- **Method:** `_axis_pressure_from_hints()`
  - *Purpose:* Read adapter_hints.json → axis_stats → mean_pressure_pre per axis.
- **Method:** `_axis_pressure_from_log()`
  - *Purpose:* Compute mean per-axis pressure from the most recent entries in
- **Method:** `_dim_fails_from_ledger()`
  - *Purpose:* Read fail_points.json → per-dimension average severity.
- **Method:** `_code_pressure_from_hints()`
  - *Purpose:* Estimate code_gap pressure from structural adaptation signals.
- **Method:** `_load_json()`
- **Method:** `_path()`

### File: `./aurora_core_ai/aurora_internal/aurora_pressure_ledger.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_pressure_ledger.py -- Universal behavioral experience recorder.

The same causal chain applied in every behavioral evolution subsystem:

    pursuing      -- what was being attempted (goal / intent at this layer)
    causal_action -- the specific operation that incurred the cost
    consequence   -- what that action produced (tension, gate failure, fitness drop)
    outcome       -- how it resolved relative to what was being pursued

Any subsystem that applies pressure to Aurora's behavior calls
PressureExperienceLedger.get().record(...).  The ledger persists every
experience to aurora_state/pressure_experiences.jsonl and bridges into OETS
concept nodes as UsageExamples -- so each concept accumulates real causal
history instead of developer-authored rules.

Integration points:
    turn_chain    -- N-axis cost pressure during conversational reasoning
    genealogy     -- Gate 2/4/5 rejection when promoting a constraint link
    dream_trainer -- lesson episode fitness below threshold
    lsv_template  -- expression template fitness drop
```

#### Class: `PressureExperience`
> One causal experience recorded anywhere in Aurora's behavioral system.

- **Method:** `to_dict()`
- **Method:** `as_usage_text()`
  - *Purpose:* Compact record for OETS UsageExample.text.

#### Class: `PressureExperienceLedger`
> Collects PressureExperience records from any subsystem, persists them,

- **Method:** `__init__()`
- **Method:** `get()`
- **Method:** `record()`
  - *Purpose:* Record one causal experience and optionally bridge it into OETS.
- **Method:** `flush_to_oets()`
  - *Purpose:* Bridge all buffered experiences that haven't been bridged yet.
- **Method:** `recent()`
  - *Purpose:* Return recent experiences, optionally filtered by source or anchor.
- **Method:** `outcome_variance()`
  - *Purpose:* For a given anchor (and optionally a specific causal_action), compute
- **Method:** `conditioning_signal()`
  - *Purpose:* High-level signal for the reasoning chain:
- **Method:** `_bridge_to_oets()`
  - *Purpose:* Write the causal experience into the OETS concept node for `anchor`.

### File: `./aurora_core_ai/aurora_internal/aurora_pressure_mathematics_tracker.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA PRESSURE MATHEMATICS TRACKER
========================================
Lightweight instrumentation that taps into existing data streams to
track the core quantities from Aurora's pressure mathematics framework.

NOT a new system. This reads what's already flowing:
  - DPME drift metrics (consciousness engine)
  - Genealogy relief records and link stats (constraint genealogy)
  - Code evolution stagnation and mutation stats (code evolution chamber)
  - Dream evolution episode summaries (dream evolution orchestrator)

Computes:
  A. Gradient health — driver vs opposition balance across axes
  B. Pressure complexity — how many active pressure interactions exist
  C. Divergence — how far actual pressure topology drifts from origin model
  D. Flip indicators — signs of approaching pressure regime transitions
  E. Stagnation detection — where equilibrium is killing useful disequilibrium

Feeds these back through Aurora's own pressure channels (DPME guidance,
genealogy notes) so the system can self-regulate from them.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_clamp()`

#### Class: `PressureSnapshot`
> Single timestamped capture of all trackable pressure metrics.

- **Method:** `to_dict()`

#### Class: `PressureMetrics`
> Derived pressure mathematics metrics from snapshot history.

- **Method:** `to_dict()`
- **Method:** `to_feedback_summary()`
  - *Purpose:* Produce a compact string Aurora can ingest as context.

#### Class: `OriginModel`
> Predicted pressure topology from the five-constraint origin.

- **Method:** `__init__()`
- **Method:** `compute_divergence()`
  - *Purpose:* Compute divergence between actual state and origin prediction.

#### Class: `PressureMathematicsTracker`
> Lightweight tracker that reads existing data streams, computes

- **Method:** `__init__()`
- **Method:** `capture()`
  - *Purpose:* Take a snapshot from all available data streams and compute metrics.
- **Method:** `_take_snapshot()`
  - *Purpose:* Read all available data streams into a snapshot.
- **Method:** `_compute_metrics()`
  - *Purpose:* Compute derived metrics from snapshot history.
- **Method:** `_compute_gradient_health()`
  - *Purpose:* Estimate gradient health per axis.
- **Method:** `_estimate_active_interactions()`
  - *Purpose:* Estimate how many pressure interactions are actively producing gradients.
- **Method:** `_detect_flip_proximity()`
  - *Purpose:* Detect how close we are to a pressure regime flip.
- **Method:** `_compute_axis_stagnation()`
  - *Purpose:* Compute per-axis stagnation from gradient health history.
- **Method:** `_compute_evolutionary_velocity()`
  - *Purpose:* Rate of structural change (links, generations).
- **Method:** `_compute_pressure_velocity()`
  - *Purpose:* Rate of pressure reorganization.
- **Method:** `generate_feedback()`
  - *Purpose:* Generate feedback that can be routed back into Aurora's systems.
- **Method:** `apply_feedback()`
  - *Purpose:* Apply generated feedback into live systems.
- **Method:** `get_status()`
  - *Purpose:* Return current tracker status.
- **Method:** `_save_state()`
  - *Purpose:* Persist recent history and metrics.
- **Method:** `_load_state()`
  - *Purpose:* Restore persisted state.

### File: `./aurora_core_ai/aurora_internal/aurora_pressure_router.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_pressure_router.py
─────────────────────────────────────────────────────────────────────────────
Unified motivational substrate router.

Takes a TypedPressureSignal and simultaneously feeds three growth systems:

  LAYER 1 — EVOLUTION
    Writes bias hints for CodeAutoEvolver and SurfaceDispatcher threshold
    so evolution budget concentrates on the pressured axes.
    (Mostly already done via PressureParameterAdapter.  This layer reads
    the classified type and amplifies the budget allocation for the
    dominant pressure type.)

  LAYER 2 — TRAINING CURRICULUM
    Calls FailPointLedger.record_fail() on the dimensions that map to the
    dominant pressure types, with severity proportional to pressure score.
    This steers dream curriculum episodes toward the fault lines.

  LAYER 3 — GPT / RETRIEVAL QUERY BIAS
    Writes aurora_state/query_bias.json.
    Aurora's GPT-mediated processes (reflection, retrieval, hypothesis
    generation) read this file to know what to study next and how to
    frame queries.  Each pressure type generates:
      - query_templates   concrete search/reflection questions
      - retrieval_domains knowledge domains to pull from
      - reflection_directive  one-line instruction for the next self-review
      - hypothesis_seed   what hypothesis to test in next episode

The three-layer dispatch happens atomically in route().  Call it after
every PressureParameterAdapter.adapt() cycle (every 50 ticks), or any
time you want the system to reconsider its growth priorities.

Output file: aurora_state/query_bias.json
─────────────────────────────────────────────────────────────────────────────
```

#### Function: `_impact_formula_weight()`
> Compute the canonical impact formula weight from per-axis pressure scores:


#### Function: `_render_template()`

#### Class: `PressureRouter`
> Routes typed pressure signals to all three growth layers.

- **Method:** `__init__()`
- **Method:** `route()`
  - *Purpose:* Full three-layer dispatch.
- **Method:** `load_query_bias()`
  - *Purpose:* Read the most recent query_bias.json (safe if missing).
- **Method:** `status()`
- **Method:** `_select_directive_types()`
- **Method:** `_route_evolution()`
  - *Purpose:* Update adapter_hints.json with a routing_type annotation so that
- **Method:** `_route_training()`
  - *Purpose:* Feed FailPointLedger with pressure-weighted fail signals for the
- **Method:** `_route_gpt_bias()`
  - *Purpose:* Write aurora_state/query_bias.json.

#### Function: `route_pressure()`
> Module-level shortcut.  Call from aurora_runtime.py tick loop.


### File: `./aurora_core_ai/aurora_internal/aurora_primitive_extractor.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA PRIMITIVE EXTRACTOR
===========================
Module: aurora_primitive_extractor.py

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026

PURPOSE
-------
Reads the live ConstraintGenealogyLogger at any moment and surfaces:

  1. DOMINANT PAIRINGS       — which constraint pairs are producing the most
                               consistent relief across the five axes, ranked
                               by positive relief signal strength.

  2. FORMING CHAINS          — DAG paths through promoted Links, tracing the
                               evolutionary lineage from raw abilities up to
                               the deepest current compound primitives.
                               Expressed as readable ancestry chains:
                               "A:COMMIT → B:ENCAPSULATE → L:abc123 → L:def456"

  3. CURRENT VOCABULARY      — the universe's discovered primitive vocabulary
                               at this moment: which axis combinations dominate,
                               which depth layers are populated, what emergent
                               tags are present, and what the constraint
                               grammar looks like so far.

  4. OUTCOME BIAS DISTANCE   — you declare a target bias as a 5-axis weight
                               vector. The extractor computes the current
                               universe's "center of gravity" in that space
                               and returns the distance + axis gap + steering
                               suggestion — without injecting the result.
                               The physics still has to get there on its own.

DOCTRINE
--------
  This module READS the fossil record. It does not write to it.
  It does not inject traces, does not modify PairStats, does not
  touch the chamber. It is a lens, not a hand.

  The outcome bias distance tells you how far away you are and
  which axis to pressure next. It does not move you there.
  That's what steering actions are for.

USAGE
-----
    from aurora_internal.aurora_primitive_extractor import PrimitiveExtractor, OutcomeBias

    extractor = PrimitiveExtractor(genealogy)

    # See what's forming
    extractor.report()

    # Declare where you want the universe to end up
    bias = OutcomeBias(
        axis_weights={"A": 0.5, "B": 0.3, "X": 0.2, "T": 0.0, "N": 0.0},
        label="agency-boundary dominance",
    )
    gap = extractor.bias_distance(bias)
    print(gap.steering_suggestion)

    # Export the primitive vocabulary as JSON
    vocab = extractor.vocabulary()
    import json; print(json.dumps(vocab, indent=2))

INTEGRATION WITH aurora_runtime.py
------------------------------------
    extractor = PrimitiveExtractor(runtime.systems.genealogy)
    extractor.report()                      # full print
    extractor.pairings(top_n=10)            # top pairings
    extractor.chains(max_chains=5)          # deepest chain paths
    extractor.bias_distance(my_bias).show() # distance to goal
```

#### Function: `_clamp()`

#### Function: `_norm()`
> Normalize a 5-axis dict to sum=1 (safe — returns zeros if all zero).


#### Function: `_l2()`
> Euclidean distance between two 5-axis vectors.


#### Function: `_cosine_sim()`
> Cosine similarity. Returns 0 if either vector is zero.


#### Class: `OutcomeBias`
> A declared target state for the universe's primitive distribution.

- **Method:** `__post_init__()`
- **Method:** `normalized()`

#### Class: `PairingResult`
> One ranked constraint pairing from the pair stats accumulator.

- **Method:** `relief_score()`
  - *Purpose:* Single scalar signal strength — weighted positive relief.
- **Method:** `describe()`

#### Class: `ChainPath`
> One evolutionary lineage path through the DAG.


#### Class: `UniverseVocabulary`
> The universe's current primitive vocabulary — a structured snapshot of

- **Method:** `to_dict()`

#### Class: `BiasDistanceResult`
> Result of measuring the current universe's distance from an OutcomeBias.

- **Method:** `show()`
  - *Purpose:* Print a human-readable distance report.
- **Method:** `to_dict()`

#### Class: `PrimitiveExtractor`
> Reads a live ConstraintGenealogyLogger and extracts the discovered

- **Method:** `__init__()`
- **Method:** `pairings()`
  - *Purpose:* Rank all observed constraint pairings by relief signal strength.
- **Method:** `chains()`
  - *Purpose:* Extract the deepest evolutionary lineage paths from the DAG.
- **Method:** `vocabulary()`
  - *Purpose:* Build the current primitive vocabulary of the universe.
- **Method:** `bias_distance()`
  - *Purpose:* Measure the current universe's distance from a declared OutcomeBias.
- **Method:** `report()`
  - *Purpose:* Print a full primitive extraction report:
- **Method:** `export()`
  - *Purpose:* Export the full primitive snapshot to a JSON file.
- **Method:** `_walk_ancestry()`
  - *Purpose:* Recursively walk the DAG from node_id toward its ability roots.
- **Method:** `_format_path()`
  - *Purpose:* Build a human-readable chain string.
- **Method:** `_print_vocabulary()`
  - *Purpose:* Print formatted vocabulary summary.
- **Method:** `_build_steering_suggestion()`
  - *Purpose:* Build steering suggestion lines.

#### Function: `_demo()`
> Minimal self-demo. Boots the minimum required stack, runs a burst,


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `chains_evolved()`

### File: `./aurora_core_ai/aurora_internal/aurora_proposition_substrate.py`

**Type:** Python Source

**Module Docstring:**
```text
Constraint-native proposition substrate for lineage-activated discourse state.

This module keeps proposition structure small and executable:
  - proposition nodes derived from claim atoms
  - continuation / support / contradiction / revision / causal edges
  - provenance-weighted confidence per proposition

It is intentionally lightweight so it can be activated from lineage artifacts
without pulling the rest of the runtime into a heavy import chain.
```

#### Function: `_clamp()`

#### Class: `PropositionSubstrate`
- **Method:** `__init__()`
- **Method:** `configure()`
- **Method:** `_claim_key()`
- **Method:** `_branch_key()`
- **Method:** `_node_id()`
- **Method:** `_edge_id()`
- **Method:** `_source_weight()`
- **Method:** `_claim_overlap()`
- **Method:** `_upsert_edge()`
- **Method:** `_trim_nodes()`
- **Method:** `_edge_summary()`
- **Method:** `_refresh_confidence()`
- **Method:** `note_claim()`
- **Method:** `note_claim_bundle()`
- **Method:** `node_for_claim()`
- **Method:** `score_claim()`
- **Method:** `report()`

### File: `./aurora_core_ai/aurora_internal/aurora_quasiarch_observer.py`

**Type:** Python Source

**Module Docstring:**
```text
Aurora-side integration wrapper for the QuasiArch Observer lattice.

This keeps the copied QuasiArch subsystem inside Aurora's stack as a
diagnostic observer first. It records interventions, builds doctrine, and
can be queried for hypotheses. Active steering is opt-in.
```

#### Function: `_fast_file_count()`
> Count files in a directory, stopping once `limit` is reached.


#### Function: `_clamp()`

#### Function: `_pressure_band()`

#### Function: `build_quasiarch_pressure_vector()`

#### Function: `build_quasiarch_constraint_context()`

#### Class: `QuasiArchGateDecision`
- **Method:** `to_dict()`

#### Class: `AuroraQuasiArchObserver`
> Aurora runtime adapter for QuasiArch diagnostics.

- **Method:** `__init__()`
- **Method:** `attach_systems()`
- **Method:** `_normalize_genealogy_refs()`
- **Method:** `_extract_genealogy_refs()`
- **Method:** `_load_state()`
- **Method:** `_write_runtime_state()`
- **Method:** `save()`
- **Method:** `get_status()`
- **Method:** `get_doctrine_candidates()`
- **Method:** `reason_about_event()`
- **Method:** `_score_hypothesis_quality()`
- **Method:** `_record_consultation()`
- **Method:** `_record_hypothesis_hold()`
- **Method:** `_apply_learning_credit()`
- **Method:** `record_observation()`
  - *Purpose:* Record a passive ground-truth observation with OBSERVER provenance.
- **Method:** `record_intervention_event()`
- **Method:** `get_recent_evidence_dicts()`
  - *Purpose:* Convert the N most-recent intervention events into evidence dicts
- **Method:** `record_training_epoch()`
- **Method:** `record_corpus_response()`
- **Method:** `advise_training_plan()`
- **Method:** `_select_doctrine()`
- **Method:** `_merge_training_patch()`
- **Method:** `_should_auto_advance()`
- **Method:** `_derive_training_event()`
- **Method:** `_derive_corpus_event()`
- **Method:** `_observed_effect_from_training()`
- **Method:** `_observed_effect_from_corpus()`

### File: `./aurora_core_ai/aurora_internal/aurora_recommendation_hub.py`

**Type:** Python Source

**Module Docstring:**
```text
Hidden recommendation inbox for Aurora post-run actions.

Designed so recommendations are generated at end of runs, then consumed by Aurora,
who chooses one action per recommendation:
- note
- discuss_with_user
- dismiss
```

#### Function: `_now_iso()`

#### Function: `_ensure_dir()`

#### Function: `_append_jsonl()`

#### Function: `_read_jsonl()`

#### Function: `_paths()`

#### Function: `enqueue_recommendation()`

#### Function: `_action_index()`

#### Function: `pending_recommendations()`

#### Function: `choose_action()`

#### Function: `_parse_iso_epoch()`

#### Function: `_recent_discuss_block()`

#### Function: `act_recommendation()`

#### Function: `process_pending_for_aurora()`

### File: `./aurora_core_ai/aurora_internal/aurora_response_pressure_tuner.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_response_pressure_tuner.py
=================================
Reusable tuner for spontaneous-response pressure decisions.

It does not replace the response policy by itself. It records the signal,
counter-pressure, threshold, and decision margin for each emit/suppress event
so Aurora can inspect recurring pressure patterns and reuse them during
training and runtime tuning.
```

#### Function: `_clamp()`

#### Class: `PressureDecision`
- **Method:** `to_dict()`

#### Class: `ResponsePressureTuner`
> Records response-pressure decisions and derives guidance from them.

- **Method:** `__init__()`
- **Method:** `evaluate()`
- **Method:** `guidance()`
- **Method:** `export_state()`
- **Method:** `load_state()`

#### Function: `build_training_plan_from_guides()`

#### Function: `build_avatar_spec_from_plan()`

#### Function: `queue_plan_on_session()`

### File: `./aurora_core_ai/aurora_internal/aurora_room_operator.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_room_operator.py — Aurora's computer use layer for her own room.

Aurora literally looks at her room window, reads what's displayed via OCR,
and operates the interface through real mouse clicks and keyboard input via
Xlib.  No external APIs.  She uses her own eyes (screenshot + tesseract)
and her own hands (Xlib fake input events).

Public interface:
    op = RoomOperator()
    op.switch_tab("Poedex")
    op.poedex_query("N", cat="define")
    op.read_tab_content()       -> str   (OCR of current visible tab)
    op.screenshot() -> PIL.Image

The daemon calls this when Aurora should actively engage with her room.
```

#### Function: `_log_entry()`

#### Class: `RoomOperator`
> Aurora's interface to her own room window.

- **Method:** `__init__()`
- **Method:** `_find_window()`
  - *Purpose:* Locate the Aurora's Room window. Returns True if found.
- **Method:** `_search_tree()`
- **Method:** `_ensure_window()`
- **Method:** `screenshot()`
  - *Purpose:* Capture the Aurora's Room window as a PIL Image.
- **Method:** `read_screen()`
  - *Purpose:* OCR the current screen (or a specific region).
- **Method:** `read_tab_content()`
  - *Purpose:* OCR the main content area of the current tab (below the tab bar).
- **Method:** `identify_active_tab()`
  - *Purpose:* Read the tab bar and return which tab appears active (highlighted).
- **Method:** `_abs_pos()`
  - *Purpose:* Convert window-relative coords to absolute screen coords.
- **Method:** `mouse_move()`
- **Method:** `click()`
  - *Purpose:* Click at window-relative coordinates.
- **Method:** `type_text()`
  - *Purpose:* Type a string into whatever widget currently has focus.
- **Method:** `press_key()`
  - *Purpose:* Press a key by name.  Supports modifier combos like 'ctrl+a', 'ctrl+c'.
- **Method:** `_locate_tab()`
  - *Purpose:* Find the pixel centre of a tab button by scanning the tab bar with OCR.
- **Method:** `switch_tab()`
  - *Purpose:* Click on the named tab.  Returns True if the click was sent.
- **Method:** `_locate_widget_by_ocr()`
  - *Purpose:* Find a widget by the label text near it.
- **Method:** `poedex_query()`
  - *Purpose:* Aurora queries Poedex and reads the result from her own screen.
- **Method:** `look_at_tab()`
  - *Purpose:* Navigate to a tab, take a screenshot, OCR the full content area,
- **Method:** `scan_room()`
  - *Purpose:* Walk through all tabs, read each one, return a dict of

### File: `./aurora_core_ai/aurora_internal/aurora_rubric_influence_graph.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA RUBRIC INFLUENCE GRAPH
=================================
Represents dependency relationships between rubric dimensions.

Failures are NOT flat bins. They form a relational pattern:
  - symptoms (what you see)
  - root deficits (what causes the symptoms)
  - downstream consequences (what the root deficit also breaks)

This graph lets the system distinguish between these and identify
LEVERAGE CANDIDATES — root deficits that, if fixed, would improve
multiple downstream dimensions.

Example relations:
  weak context_carryover -> weak implied_intent_inference
  weak uncertainty_signaling -> premature commitment (contradiction_handling)
  weak framing_selection -> coherence_maintenance drift
  weak boundary_calibration -> bad misunderstanding_repair timing
  weak perspective_integration -> ambiguity_handling collapse

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_clamp()`

#### Class: `RubricInfluenceAssessment`
> Relational assessment of rubric dimension weaknesses.

- **Method:** `to_dict()`

#### Class: `RubricInfluenceGraph`
> Analyzes rubric dimension scores through a causal influence graph.

- **Method:** `__init__()`
- **Method:** `analyze()`
  - *Purpose:* Analyze dimension scores to identify root deficits, symptoms,
- **Method:** `trace_downstream()`
  - *Purpose:* Trace all downstream dimensions affected by a given dimension.
- **Method:** `trace_upstream()`
  - *Purpose:* Trace all upstream dimensions that influence a given dimension.

### File: `./aurora_core_ai/aurora_internal/aurora_runtime_constraint_governor.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_runtime_constraint_governor.py
====================================
Bind Aurora's 5-constraint logic to actual runtime execution policy.

This governor does not replace semantic pressure. It turns the same
constraint frame into host-level scheduling and wakeup decisions so Aurora
conserves CPU, memory, disk, and concurrency when the machine is under load.
```

#### Function: `_normalize_axis_map()`

#### Function: `_clamp()`

#### Class: `RuntimeConstraintGovernor`
- **Method:** `__init__()`
- **Method:** `evaluate_task()`
- **Method:** `note_task_run()`
- **Method:** `note_energy_income()`
  - *Purpose:* Record an energy income event. The governor will add decayed credits
- **Method:** `_load_energy_income()`
- **Method:** `energy_income_summary()`
  - *Purpose:* Returns current live credits and their decayed values — for hub display.
- **Method:** `recommended_sleep()`
- **Method:** `status()`
- **Method:** `_pressure_state()`
- **Method:** `_runtime_budgets()`
- **Method:** `_retry_interval()`
- **Method:** `_host_metrics()`

### File: `./aurora_core_ai/aurora_internal/aurora_second_gen.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_second_gen.py
─────────────────────────────────────────────────────────────────────────────
Second-generation evolution injector.

The descriptor pool (operation_descriptors.json) is the input layer for the
CodeAutoEvolver. Currently only hand-written source operations feed it.
Evolved surfaces sit in _SURFACE_REGISTRY but never loop back as evolvable
inputs — so generation depth is stuck at 1.

This module reads _SURFACE_REGISTRY from aurora_evolved_surfaces.py and
synthesizes valid operation descriptor entries for each surface that is not
already in the pool. On the next evolution cycle, the evolver can reflect on
these surfaces exactly as it reflects on base operations — producing surfaces
of surfaces.

Key differences for gen-2 descriptors:
  - kind = "function" (evolved surfaces are methods, treated as functions)
  - file = "aurora_internal/aurora_evolved_surfaces.py"
  - rewrite_bias = "lineage_memory" (highest-priority bias lane in evolver)
  - genealogy_pressure inherited from the source surface
  - cross_diversity_links = ability_hits + link_hits (from surface card)
  - _generation = 2 marker for tracking
─────────────────────────────────────────────────────────────────────────────
```

#### Class: `SecondGenEvolutionInjector`
> Reads evolved surfaces from _SURFACE_REGISTRY and injects them back into

- **Method:** `__init__()`
- **Method:** `inject()`
  - *Purpose:* Inject all evolved surfaces above min_surface_score into the descriptor
- **Method:** `status()`
  - *Purpose:* Return counts of gen-2 entries currently in the pool.
- **Method:** `_load_registry()`
- **Method:** `_load_pool()`
- **Method:** `_save_pool()`
- **Method:** `_surface_to_descriptor()`
  - *Purpose:* Synthesize a generation-2 operation descriptor from a surface registry entry.
- **Method:** `_weights_from_sig()`
  - *Purpose:* Parse 'N^2*B^2' into normalized axis weights.
- **Method:** `_primary_slot()`
- **Method:** `_nc_projections()`
  - *Purpose:* Compute top NC channel slot projections from axis weights.

### File: `./aurora_core_ai/aurora_internal/aurora_sensory_crystal.py`

**Type:** Python Source

**Module Docstring:**
```text
Aurora Sensory Crystal — 6-Facet Cross-Modal Understanding
===========================================================
Authors: Sunni (Sir) Morningstar & Cael Devo

Zero-state sensory competency seeded into Aurora's lineage system.

Crystal geometry (hexagonal bipyramid):

    TOP HALF  (visual):  HUE  / SHAPE  / MOTION
    ─────────────────── SEMANTIC MIDDLE PLANE ───────────────────
    BOTTOM HALF (audio): TONE / TIMBRE / RHYTHM

Bottom 3 facets draw from audio.features.v1 (20-d vector):
    TONE   — harmonicity [6] + chroma-lite [8:20]     (13 dims)
    TIMBRE — RMS [0], ZCR [1], centroid [2], bw [3], rolloff [4]  (5 dims)
    RHYTHM — spectral_flux [5] + onset_density [7]     (2 dims)

Top 3 facets draw from vision.features.v1 (57-d vector):
    HUE    — HSV histograms [0:24]                    (24 dims)
    SHAPE  — edge + orientation + shape proxy [24:51] (27 dims)
    MOTION — motion + symmetry [51:57]                 (6 dims)

Opposite facets pair through the semantic middle plane:
    tone <-> hue      (pitch/harmony  <-> colour)
    timbre <-> shape  (texture        <-> form/edge)
    rhythm <-> motion (onset/tempo    <-> movement/flow)

LINEAGE INTEGRATION
───────────────────
All operations are registered in lineage_canonical.CANONICAL_OPERATION_CONSTRAINTS.
The trait spec below seeds 5 lineage stages through Aurora's ability genealogy:

    1. sensory_intake_seed        (gen=1, N-axis)  — raw signal enters crystal
    2. sensory_crystal_clustering (gen=2, B-axis)  — observations cluster into nodes
    3. sensory_concept_promotion  (gen=2, A-axis)  — primitive → concept → promoted
    4. cross_modal_grounding      (gen=3, N-axis)  — audio×visual → semantic plane
    5. sensory_wisdom_distillation(gen=3, T-axis)  — mature nodes distill, dead emit wisdom

Call ensure_sensory_crystal_lineage(systems) at Aurora boot to seed these abilities
into the genealogy exactly like all other Aurora abilities.

PROMOTION RULES (match existing aurora_dimensional_systems crystal rules)
──────────────────────────────────────────────────────────────────────────
    usage_count   >= 14     (CONCEPT_PROMOTION_USAGE)
    session_count >= 3      (CONCEPT_PROMOTION_SESSIONS)
    confidence    >= 0.55   (CONCEPT_PROMOTION_CONFIDENCE)
    fitness = 0.40*conf + 0.35*usage_norm + 0.25*session_norm
    decay_rate = base * (0.15 + 0.85 * (1 - maturity))   [plateau-aware]
    distillation at maturity >= 0.80

State persisted to:
    aurora_state/sensory_crystal/audio/{tone,timbre,rhythm}/state.agb
    aurora_state/sensory_crystal/visual/{hue,shape,motion}/state.agb
    aurora_state/sensory_crystal/semantic/state.agb
```

#### Function: `_seed_archetype_nodes()`
> Populate a freshly-loaded (empty) facet with archetype primitive nodes.


#### Function: `_clamp01()`

#### Function: `_ema()`

#### Function: `_cosine_sim()`

#### Function: `_npmi()`

#### Function: `_bucket_level()`

#### Function: `_bucket_count()`

#### Function: `_extract_audio_facet()`

#### Function: `_extract_visual_facet()`

#### Function: `audio_dict_to_crystal_20d()`
> Adapt a Layer-5 audio data dict to the 20d sensory crystal audio vector.


#### Function: `visual_dict_to_crystal_57d()`
> Adapt a Layer-5 visual data dict to the 57d sensory crystal visual vector.


#### Function: `_agb_save()`

#### Function: `_agb_load()`

#### Class: `SensoryNode`
> A learned concept node on one facet of the sensory crystal.

- **Method:** `update_centroid()`
- **Method:** `compute_fitness()`
- **Method:** `is_promotable()`
- **Method:** `emit_wisdom()`
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `CrystalFacet`
> One face of the sensory crystal.  Manages a population of SensoryNodes

- **Method:** `__init__()`
- **Method:** `observe()`
  - *Purpose:* Ingest one feature vector slice.  Find/create nearest SensoryNode.
- **Method:** `_nearest()`
- **Method:** `tick_promotion()`
  - *Purpose:* primitive -> concept for nodes meeting gates.  Operation: sensory.promote (A)
- **Method:** `tick_advanced_promotion()`
  - *Purpose:* concept -> promoted for high-fitness nodes with cross-modal links.
- **Method:** `compute_maturity()`
- **Method:** `maturity()`
- **Method:** `ready_for_distillation()`
- **Method:** `cull()`
- **Method:** `add_cross_modal_link()`
- **Method:** `end_session()`
  - *Purpose:* Operation: sensory.end_session (T, B, A)
- **Method:** `get_promoted()`
- **Method:** `get_node()`
- **Method:** `save()`
  - *Purpose:* Operation: sensory.distill (T) — persistence after session.
- **Method:** `load()`

#### Class: `SemanticCrystalNode`
> A cross-modal meaning node in the semantic middle plane.

- **Method:** `compute_fitness()`
- **Method:** `is_promotable_concept()`
- **Method:** `is_promotable_promoted()`
- **Method:** `emit_wisdom()`
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `AuroraSensoryCrystal`
> Full 6-facet sensory competency crystal for Aurora.

- **Method:** `__init__()`
- **Method:** `boot()`
  - *Purpose:* Load persisted state. Safe on first boot (zero knowledge).
- **Method:** `start_session()`
- **Method:** `_describe_node()`
- **Method:** `wire_dimensional()`
  - *Purpose:* Connect this crystal to Aurora's DimensionalProcessingSystem (DPS).
- **Method:** `register_evolution_hook()`
  - *Purpose:* Register a hook that receives evidence dicts for chamber input.
- **Method:** `_ensure_node_name()`
- **Method:** `_inject_to_dps()`
  - *Purpose:* Inject a promoted SensoryNode into the DPS crystal order ladder.
- **Method:** `_inject_semantic_to_dps()`
- **Method:** `_sync_to_dps()`
- **Method:** `observe_frame()`
  - *Purpose:* Ingest one synchronised audio+vision frame.
- **Method:** `_find_semantic()`
- **Method:** `_tick_semantic_promotion()`
  - *Purpose:* candidate -> concept -> promoted.
- **Method:** `end_session()`
  - *Purpose:* End-of-interaction consolidation.  Runs promotion, maturity, cull, save.
- **Method:** `_compute_semantic_maturity()`
- **Method:** `_cull_semantic()`
- **Method:** `save()`
  - *Purpose:* Persist current crystal state without ending the session.
- **Method:** `_save()`
- **Method:** `get_state()`

#### Function: `_make_lineage_trait_spec()`
> Build the LineageTraitSpec for the sensory crystal abilities.


#### Function: `ensure_sensory_crystal_lineage()`
> Seed the sensory crystal abilities into Aurora's lineage system.


#### Function: `build_aurora_sensory_crystal()`
> Convenience factory.  Creates and boots a full AuroraSensoryCrystal.


### File: `./aurora_core_ai/aurora_internal/aurora_solidification.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA SOLIDIFICATION PIPELINE — STEP 11
==========================================
Depth propagation: recurrence + energy investment → downward solidification.

WHAT SOLIDIFICATION IS:
    A promoted intake (from Step 9) has crossed the Worth threshold once.
    That is NOT enough. Solidification is what happens when a promoted
    intake recurs across multiple evaluation ticks AND the system has
    invested real energy in sustaining it at depth.

    The pipeline is:
        ELIGIBLE intake (horizon elapsed from Step 10)
            → recurrence gate (seen N times across context-varied ticks)
            → energy investment gate (pool spent actual cost to sustain it)
            → polarity coherence gate (surface and core aligned during recurrence)
            → depth-solidification record minted
            → SolidifiedRecord passed to Step 13 (Variant Promotion)

    Solidified structures have two effects on the living system:
        1. They REDUCE future shift cost for their constraint signature
           (the path is worn — it costs less to walk it again).
        2. They INCREASE pressure sensitivity at their depth level
           (the system becomes faster to detect when that configuration
           is under threat).

    Effect 1 and 2 are NOT rules imposed from outside. They are physics:
        Effect 1: The structure has accumulated energy investment — it
                  carries lower inertia in the next shift because baseline
                  is already partially satisfied.
        Effect 2: Deeper solidified structures have higher alignment
                  authority (from IVM ALIGNMENT_VOTE_WEIGHT) — they drag
                  the polarity gradient faster when disturbed.

RECURRENCE GATE:
    An intake must be observed at the same depth, across at least
    _RECURRENCE_MIN distinct ticks, before solidification is considered.
    "Distinct" means the ticks are not consecutive — consecutive ticks
    indicate persistence, not recurrence. Genuine recurrence means the
    input re-appeared after at least _RECURRENCE_GAP ticks of absence.

ENERGY INVESTMENT GATE:
    The system must have spent at least _INVESTMENT_FLOOR energy on
    sustaining this intake since its first promotion. This is real
    energy drawn from the pool — not theoretical.

CONTEXT ROBUSTNESS:
    At least _CONTEXT_VARIETY distinct entropy pressure levels must have
    been observed during recurrence ticks. This prevents gaming via
    artificially stable system states.

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Function: `_pressure_sensitivity()`

#### Function: `_depth_recurrence_min()`
> Minimum genuine recurrences required before solidification for this depth.


#### Function: `_depth_recurrence_gap()`
> Minimum tick gap between recurrence observations for this depth.


#### Class: `RecurrenceRecord`
> Tracks recurrence observations for one intake from the point of

- **Method:** `record_observation()`
  - *Purpose:* Record one recurrence observation.
- **Method:** `recurrence_count()`
- **Method:** `context_variety()`
- **Method:** `polarity_coherence_rate()`
- **Method:** `gates_passed()`
  - *Purpose:* Returns (recurrence_gate, investment_gate, context_gate, polarity_gate).
- **Method:** `all_gates_passed()`

#### Class: `SolidifiedRecord`
> A depth-solidified intake — the output of the solidification pipeline.

- **Method:** `describe()`

#### Function: `_mint_solidification_id()`

#### Function: `_constraint_signature()`
> Return the constraint activation signature for this depth.


#### Class: `SolidificationPipeline`
> The Step 11 solidification engine.

- **Method:** `__init__()`
- **Method:** `submit_eligible()`
  - *Purpose:* Mark an intake as eligible for solidification tracking.
- **Method:** `observe_recurrence()`
  - *Purpose:* Record a recurrence observation for a tracked intake.
- **Method:** `_observe_internal()`
- **Method:** `_mint()`
- **Method:** `drain_solidified()`
  - *Purpose:* Return all SolidifiedRecords minted this tick and clear the queue.
- **Method:** `stats()`

#### Function: `make_solidification_pipeline()`

#### Function: `verify_solidification_pipeline()`
> Checks:


### File: `./aurora_core_ai/aurora_internal/aurora_specialized_avatar_synthesizer.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA SPECIALIZED AVATAR SYNTHESIZER
==========================================
Generates pressure-specialized avatar configurations from repeated
relational weakness patterns detected by the slip profiler.

Key principle: stress ROOT DEFICITS, not surface symptoms.
  If contradiction failure is downstream of weak context carryover,
  the avatar stresses cross-turn memory pressure.
  If ambiguity failure is downstream of poor uncertainty signaling,
  the avatar punishes premature commitment and rewards clarification.

Integration:
  - Reads: EpisodeRubricSummary (from slip profiler)
  - Reads: RubricInfluenceAssessment (from influence graph)
  - Produces: PressureSpecializedAvatarSpec
  - Feeds into: SimulationSession._create_avatar_pool / SimulatedAvatar

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `PressureSpecializedAvatarSpec`
> A synthesized avatar configuration designed to stress specific

- **Method:** `to_dict()`
- **Method:** `to_avatar_overrides()`
  - *Purpose:* Convert this spec into override values for SimulatedAvatar fields.

#### Function: `_clamp()`

#### Function: `_clamp_signed()`

#### Function: `_generate_id()`

#### Class: `SpecializedAvatarSynthesizer`
> Generates pressure-specialized avatar configurations from repeated

- **Method:** `__init__()`
- **Method:** `_default_policy()`
- **Method:** `_load_policy()`
- **Method:** `_save_policy()`
- **Method:** `_normalize_policy()`
- **Method:** `register_episode_feedback()`
  - *Purpose:* Update the adaptive pressure policy from real dream outcomes.
- **Method:** `_constraint_axis_trace()`
- **Method:** `_apply_adaptive_policy()`
- **Method:** `policy_snapshot()`
  - *Purpose:* Expose current adaptive pressure policy for status/diagnostics.
- **Method:** `synthesize_from_summary()`
  - *Purpose:* Generate avatar specs from a single episode's diagnostic summary.
- **Method:** `synthesize_from_accumulation()`
  - *Purpose:* Generate avatar specs from accumulated cross-episode analysis.
- **Method:** `_build_spec_for_leverage()`
  - *Purpose:* Build an avatar spec targeting a specific leverage dimension.
- **Method:** `_build_persistent_spec()`
  - *Purpose:* Build an avatar spec for persistent cross-episode weaknesses.
- **Method:** `specs_generated()`

### File: `./aurora_core_ai/aurora_internal/aurora_stack_trace_instrumentation.py`

**Type:** Python Source

**Module Docstring:**
```text
Aurora stack-wide developmental trace instrumentation.

This module wraps active runtime call surfaces so methods/functions emit
evolutionary trace records with pressure-before/after and applied effects.
```

#### Function: `_is_wrappable_callable()`

#### Function: `_safe_name()`

#### Function: `_derive_applied_effects()`

#### Function: `_make_wrapper()`

#### Function: `_wrap_instance_methods()`

#### Function: `instrument_stack()`
> Instrument active stack call surfaces.


### File: `./aurora_core_ai/aurora_internal/aurora_structural_pressure_steering.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA STRUCTURAL PRESSURE STEERING
========================================
Translates repeated dream failures into structural pressure directives
that DPME can recognize as code-evolution-relevant steering surfaces.

These are NOT direct edits. They are pressure shifts:
  - bias mutation exploration toward context-integration structures
  - ease promotion burden for coherence-preserving operators
  - penalize lineages that repeatedly collapse under ambiguity
  - reduce cost for repair-capable structures under indirect-intent pressure

This is the layer where dream learning begins shaping code evolution.

Integration:
  - Reads: EpisodeRubricSummary (from slip profiler)
  - Reads: PressureSpecializedAvatarSpec (from avatar synthesizer)
  - Writes to: DPME external pressure guidance (aurora_consciousness_engine)
  - Writes to: CodeEvolutionChamber pressure conditions
  - Bridges: dream diagnosis -> avatar targeting -> structural evolution

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `StructuralPressureDirective`
> A pressure shift directive derived from dream failure patterns.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Function: `_clamp()`

#### Function: `_generate_id()`

#### Class: `StructuralPressureSteering`
> Translates dream episode diagnostic summaries into structural

- **Method:** `__init__()`
- **Method:** `_load_state()`
  - *Purpose:* Load persisted directives.
- **Method:** `_save_state()`
  - *Purpose:* Persist active directives.
- **Method:** `generate_directives()`
  - *Purpose:* Generate structural pressure directives from a single episode summary.
- **Method:** `generate_from_accumulation()`
  - *Purpose:* Generate directives from accumulated cross-episode analysis.
- **Method:** `_build_directive()`
  - *Purpose:* Build a directive for a single leverage dimension.
- **Method:** `_build_accumulated_directive()`
  - *Purpose:* Build a stronger directive from persistent cross-episode evidence.
- **Method:** `apply_to_dpme()`
  - *Purpose:* Convert active directives into DPME external pressure guidance.
- **Method:** `get_evolution_pressure_config()`
  - *Purpose:* Produce a pressure configuration dict for the code evolution chamber.
- **Method:** `expire_old_directives()`
  - *Purpose:* Remove directives older than max_age_seconds.
- **Method:** `active_directive_count()`
- **Method:** `directives_generated()`

### File: `./aurora_core_ai/aurora_internal/aurora_surface_dispatcher.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_surface_dispatcher.py
─────────────────────────────────────────────────────────────────────────────
Pressure-driven evolved surface activation.

Aurora doesn't decide to use a surface — she doesn't need to know what it
does semantically. A surface fires when the axis pressure it targets crosses
a threshold. That's the same mechanism as a reflex: a threshold crossing
triggers a prepared response, and the outcome feeds back into the pressure
system as real evidence.

Flow:
  chamber.tick()
    → intent_pressure["N"] = 0.42  (energy axis is high)
    → dispatcher.tick(chamber, engine)
        → routing table: N-axis surfaces = [reflect_...constraint_manifold, ...]
        → invoke top surface
        → surface returns activation record
        → record converted to evidence dict
        → chamber.observe_external_evidence(evidence)
        → pressure adjusts

Building the routing table:
  aurora_surface_doc.full_report() returns doc cards with `expected_axes`.
  Dispatcher indexes them as:  axis → [(score, name), ...]
  On each tick, axes above threshold are looked up, top surface per axis fires.

Usage from aurora_runtime (or any autonomy loop):
  dispatcher = SurfaceDispatcher()
  dispatcher.build_routing_table()            # once at boot
  ...
  # in autonomy loop, after chamber ticks:
  evidence_list = dispatcher.tick(chamber, engine)
  for ev in evidence_list:
      chamber.observe_external_evidence(ev)
─────────────────────────────────────────────────────────────────────────────
```

#### Class: `SurfaceDispatcher`
> Routes axis pressure to evolved surfaces and invokes them.

- **Method:** `__init__()`
- **Method:** `build_routing_table()`
  - *Purpose:* Build axis → surface routing table from aurora_surface_doc cards.
- **Method:** `evaluate()`
  - *Purpose:* Given current intent_pressure dict, return list of
- **Method:** `invoke()`
  - *Purpose:* Call one evolved surface through the engine and return the record.
- **Method:** `tick()`
  - *Purpose:* One dispatcher tick:
- **Method:** `status()`
- **Method:** `explain_routing()`
  - *Purpose:* Print which surfaces are mapped to each axis.
- **Method:** `_record_to_evidence()`
  - *Purpose:* Convert a surface activation record into the evidence format

### File: `./aurora_core_ai/aurora_internal/aurora_surface_doc.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_surface_doc.py
─────────────────────────────────────────────────────────────────────────────
Explains what evolved surfaces do — both in plain English and in terms of
applied axis pressure.

Two data sources:
  1. _SURFACE_REGISTRY in aurora_evolved_surfaces.py
     — what each surface IS (signature, constraints, effect_modes, genealogy)
  2. aurora_state/surface_pressure_log.jsonl
     — what each surface DID at runtime (axis_pressure snapshot + effect)

Public API
----------
  explain(name)          → dict card for one surface
  full_report()          → list of cards for all surfaces, sorted by score
  pressure_history(name) → recent pressure log entries for one surface
  print_report()         → human-readable console output
─────────────────────────────────────────────────────────────────────────────
```

#### Function: `_repo_root()`

#### Function: `_pressure_log_path()`

#### Function: `_load_registry()`

#### Function: `_load_pressure_log()`

#### Function: `_parse_signature()`
> Parse 'N^2*B^1' → {'N': 2, 'B': 1}


#### Function: `_signature_sentence()`

#### Function: `_effect_sentence()`

#### Function: `_pressure_level()`

#### Function: `_axis_composition()`
> Normalize signature counts into percentage contributions per axis.


#### Function: `_effect_composition()`
> Uniform fractional weight per effect mode.


#### Function: `_semantic_label()`
> Generate a compact human-readable label from composition data.


#### Function: `explain()`
> Return a documentation card for one evolved surface.


#### Function: `full_report()`
> Return doc cards for all evolved surfaces, sorted by surface_score descending.


#### Function: `pressure_history()`
> Return the most recent pressure log entries for one surface.


#### Function: `print_report()`

### File: `./aurora_core_ai/aurora_internal/aurora_turn_chain.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_turn_chain.py -- TurnUnderstandingState dataclass.

The bidirectional reasoning pipeline traverses the developmental chain in both
directions for every turn:

  UPWARD  (self -- comprehension):
      Information(X) -> Belief(T) -> Purpose(N) -> Meaning(B) -> Understanding(A)

  APEX:   TurnUnderstandingState holds the full model at this turn.

  DOWNWARD (self -- expression):
      Understanding(A) -> Meaning(B) -> Purpose(N) -> Belief(T) -> Information(X)
      -> Communication out

The five stages map 1:1 to the five NC axes:

  Information   (X axis) -- existence: either it IS information or it isn't. The gate.
  Belief        (T axis) -- time: recurrence and prediction over time shape belief.
  Purpose       (N axis) -- cost: energy/value pressure moves belief to directed purpose.
  Meaning       (B axis) -- boundary: what falls within the boundary of significance.
  Understanding (A axis) -- agency: cross-domain integration pressure, direction.

OBSERVATION (two-chain perception):
  One chain going UP is Aurora's own -- she builds comprehension inward.
  One chain going DOWN on ANOTHER's output is observation of that other --
  she infers their Understanding -> Meaning -> Purpose -> Belief -> Information
  by running the downward chain on what they produced.

  perspective="self"  => upward builds comprehension, downward builds expression
  perspective="other" => downward chain deconstructs an external agent's output,
                         filling other_model with the inferred reasoning

Each stage reads from state, enriches it, and passes it to the next.
The stage functions themselves live in aurora.py.
```

#### Class: `TurnUnderstandingState`

### File: `./aurora_core_ai/aurora_internal/aurora_understanding_contract.py`

**Type:** Python Source

**Module Docstring:**
```text
Runtime understanding contract.

This module makes Aurora's live dialogue loop explicit in the form:

    M_t -> P_t -> U_t -> O_{t+1} -> A_{t+1} -> M_{t+1}, Pi_{t+1}

Where:
    M = meaning structure bounded by current boundary state
    P = situated perspective
    U = outward application / response policy
    O = observed next turn / resulting input
    A = accuracy of fit between predicted and observed continuation

The contract is not a separate cognition system. It is a runtime accounting
layer that derives its state from Aurora's actual working memory, emotional
signals, articulation debt, contradiction state, and current response policy.
Every operator it uses is registered into the five-constraint genealogy.
```

#### Function: `_clip01()`

#### Function: `_mean()`

#### Function: `_terms()`

#### Function: `_term_overlap()`

#### Function: `_hash_payload()`

#### Class: `RuntimeUnderstandingContract`
- **Method:** `__init__()`
- **Method:** `_default_state()`
- **Method:** `_load()`
- **Method:** `save()`
- **Method:** `clone_ephemeral()`
- **Method:** `snapshot()`
- **Method:** `status()`
- **Method:** `training_guidance()`
- **Method:** `register_genealogy()`
- **Method:** `_history_append()`
- **Method:** `_pipeline_state()`
- **Method:** `_enrich_meaning_state()`
- **Method:** `_apply_genealogy_relief_boost()`
  - *Purpose:* Boost meaning_form scores when genealogy has active axis relief.
- **Method:** `_evaluate_contract_domains()`
- **Method:** `_derive_meaning_state()`
- **Method:** `_derive_perspective_state()`
- **Method:** `_derive_boundary_state()`
- **Method:** `_derive_cost_state()`
- **Method:** `_derive_existence_state()`
- **Method:** `_transition_state()`
- **Method:** `_looks_like_question()`
- **Method:** `_evaluate_previous_accuracy()`
- **Method:** `_derive_observation()`
- **Method:** `_derive_policy_state()`
- **Method:** `audit_candidate_response()`
- **Method:** `_pressure_from_state()`
- **Method:** `_record_genealogy_event()`
- **Method:** `ingest_observation()`
- **Method:** `commit_application()`

### File: `./aurora_core_ai/aurora_internal/aurora_utterance_parser.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_utterance_parser.py
===========================
Replaces QueryUnderstanding with a binding-based utterance comprehension system.

THE CORE PRINCIPLE:
    No word is noise. Every word carries meaning.
    The job is not to remove words — it's to bind them together.

    "ok so what if" is not just "what".
    It is: [acknowledgment:ok] + [reasoning:so] + [hypothesis:what if]
    = a speculative pivot off prior context.

    "like i said" is not empty.
    It is: [similarity:like] + [speaker:i] + [past-statement:said]
    = a callback to a prior statement the speaker wants recognized.

    "just wondering" is not noise.
    It is: [minimization:just] + [inquiry:wondering]
    = a tentative, low-stakes question.

ARCHITECTURE:
    PragmaticRole       — what communicative function does a word/phrase serve?
    PragmaticSignal     — a detected signal in the utterance
    UtteranceFrame      — the overall communicative frame of the utterance
    UtteranceIntent     — the full parsed meaning, bound together
    UtteranceParser     — replaces QueryUnderstanding, produces UtteranceIntent

BACKWARD COMPATIBILITY:
    UtteranceParser.parse() returns a dict that is a superset of what
    QueryUnderstanding.parse() returned, so existing code using
    understood['topic'], understood['query_type'] etc. still works.
    The new fields are additive.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `PragmaticRole`

#### Class: `UtteranceFrame`
> The high-level communicative frame of the whole utterance.


#### Class: `PragmaticSignal`
> A detected pragmatic signal in the utterance.


#### Class: `UtteranceIntent`
> The complete parsed intent of an utterance, with all words bound

- **Method:** `to_dict()`
  - *Purpose:* Convert to backward-compatible dict (superset of QueryUnderstanding output).

#### Class: `UtteranceParser`
> Parses utterances by binding every word to its communicative role,

- **Method:** `parse()`
  - *Purpose:* Parse an utterance. Returns a dict compatible with the old
- **Method:** `_parse_full()`
- **Method:** `_detect_phrase_signals()`
  - *Purpose:* Find multi-word pragmatic phrases. Returns signals and the set of
- **Method:** `_tag_words()`
  - *Purpose:* Tag individual words that aren't covered by phrase signals.
- **Method:** `_extract_topic_words()`
  - *Purpose:* Extract content words — the words that name things, actions, qualities.
- **Method:** `_extract_entities()`
  - *Purpose:* Extract proper nouns.
- **Method:** `_build_full_phrase()`
  - *Purpose:* Build the key phrase that captures the subject of the utterance.
- **Method:** `_infer_frame()`
  - *Purpose:* Infer the high-level communicative frame from signal combination.
- **Method:** `_infer_stance()`
  - *Purpose:* Infer the speaker's stance.
- **Method:** `_infer_utterance_type()`
  - *Purpose:* Classify the utterance type.
- **Method:** `_classify_query_type()`
  - *Purpose:* Classify the query type for routing/search purposes.
- **Method:** `_build_search_query()`
  - *Purpose:* Build the search string.

#### Function: `parse_utterance()`
> Parse an utterance. Drop-in replacement for QueryUnderstanding().parse(text).


#### Function: `run_tests()`

### File: `./aurora_core_ai/aurora_internal/aurora_variant_promotion.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA VARIANT PROMOTION — STEP 13
====================================
First-class variant promotion with moral weighting.

WHAT VARIANTS ARE:
    A SolidifiedRecord (from Step 11) proves that an intake recurred,
    was context-robust, and had energy genuinely invested in it.
    That is not yet a variant. A variant is what happens when a solidified
    pattern crystallizes into a MACRO-OPERATOR — a re-usable trace element
    that the system can apply as a primitive, bypassing the full intake
    TTL process.

    Variants are NOT designed. They are NOT tested against use cases.
    They crystallize from the physics of recurrence and energy investment.
    The system cannot know a priori what its variants will be.

PROMOTION GATES (all four must pass):
    1. Recurrence threshold   — solidified record's recurrence_count >= _VARIANT_RECURRENCE_MIN
    2. Context robustness     — context_variety >= _VARIANT_CONTEXT_MIN
    3. Depth solidification   — depth_reached >= BOUNDED (not just PERSISTENT)
    4. Polarity coherence     — polarity_coherence_rate >= _VARIANT_POLARITY_FLOOR

    These gates are STRICTER than the solidification gates because promotion
    to first-class variant status carries permanent cost implications.

MORAL WEIGHTING:
    A first-class variant creates moral weighting — NOT a rule, NOT a filter.

    Moral weighting is the LANDSCAPE carved by stable variants. When a
    variant has been promoted, the energy cost to traverse its constraint
    signature is reduced by its cost_reduction_factor (from Step 11)
    PLUS an additional moral weight that grows with the variant's
    recurrence strength.

    This means the system will naturally flow toward configurations that
    have proven themselves — not because it is told to, but because the
    energy physics make those paths cheaper to walk.

    Moral weight is a BIAS on the phase nudge system (from Step 8 /
    aurora_leverage_scalar.py). Specifically: a promoted variant shifts
    the effective flip_threshold for its deepest constraint slightly
    toward stability. The variant path becomes "magnetised" — not forced.

WHAT THIS MODULE PROVIDES:
    VariantRecord       — an immutable promoted first-class variant
    MoralWeightLedger   — tracks active variants and their weight biases
    VariantPromoter     — gates + promotes SolidifiedRecords → VariantRecords
                          and maintains the MoralWeightLedger

INTEGRATION:
    Downstream of Step 11 (SolidificationPipeline.drain_solidified()).
    Upstream of Step 14 (DNA Strand Schema).
    Feeds back into the LeverageBiasEngine from Step 8 via moral weight biases.

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `VariantRecord`
> A promoted first-class variant — immutable once minted.

- **Method:** `describe()`
- **Method:** `cost_diff_score()`
  - *Purpose:* Live cost-diff score for this variant.

#### Function: `_mint_variant_id()`

#### Function: `_deepest_constraint()`
> Return the constraint activated at the deepest layer of this mode.


#### Function: `_compute_moral_weight()`
> Compute the moral weight bias for a variant with given recurrence count.


#### Class: `MoralWeightLedger`
> Tracks all active variants and their cumulative moral weight biases

- **Method:** `__init__()`
- **Method:** `register()`
  - *Purpose:* Add a newly promoted variant to the ledger.
- **Method:** `moral_bias()`
  - *Purpose:* Return the current moral weight bias for constraint c.
- **Method:** `all_biases()`
  - *Purpose:* Return biases for all five constraints.
- **Method:** `variant_count()`
- **Method:** `get_variant()`
- **Method:** `summary()`
  - *Purpose:* Public summary without exposing raw weight values.

#### Class: `VariantPromoter`
> Gates SolidifiedRecords through the four promotion conditions and

- **Method:** `__init__()`
- **Method:** `process_solidified()`
  - *Purpose:* Evaluate each SolidifiedRecord against promotion gates.
- **Method:** `_try_promote()`
  - *Purpose:* Evaluate all four gates. Return VariantRecord if all pass, else None.
- **Method:** `gate_stats()`

#### Function: `make_variant_promoter()`

#### Function: `verify_variant_promotion()`
> Checks:


### File: `./aurora_core_ai/aurora_internal/aurora_worth_evaluator.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CROSS-SCALE WORTH EVALUATOR — STEP 10
=============================================

Standalone formalisation of the Worth function.

DOCTRINAL DEFINITION:
    Worth = cross-scale invariance.
    It measures how far an intake propagates through constraint depth WITHOUT
    requiring forced transformation at each layer transition.

    Worth is NOT:
        - utility (not about what it "does for" the system)
        - compute reduction (not about efficiency)
        - a property of the input itself

    Worth IS:
        - a relationship between the input AND the current constraint topology
        - contextual: the same input may score differently across two ticks
        - depth-authoritative: passing deep layers counts for more than passing
          shallow layers, because deeper layers have higher inertia and cost
          more to adjust

FORMULA:
    W(x) = 1 / (1 + Σᵢ wᵢ · |Δforced_at_layer_i|)

    Where:
        Δforced_at_layer_i  = magnitude adjustment layer i needs to admit the
                              intake cleanly — derived from authority differential
                              between the adjacent constraint pair
        wᵢ                  = depth-authority weight for that transition
                              (deeper transition → higher wᵢ)

    This module evaluates ALL FOUR transitions:
        X→T   (surface to shallow)
        T→N   (shallow to moderate)
        N→B   (moderate to deep)
        B→A   (deep to core)   ← Step 9's WorthEvaluator omitted this one

    Including B→A is the key extension. The agency layer is the most
    expensive and most authoritative. If an intake can pass that transition
    cleanly, its worth is genuinely high.

VARIANT HORIZON:
    Once an intake is promoted, how long does its trace persist before
    becoming eligible for solidification (Step 11)?

    Horizon = f(depth reached at promotion)

    The deeper the intake propagated, the longer its trace persists, because
    depth is COSTLY and the system has already invested real energy there.
    A promoted intake that reached AGENTIC has a longer horizon than one that
    reached PERSISTENT — because agency costs 150× time_constant in energy,
    and the system needs time to observe recurrence before solidifying.

    Horizon is expressed in ticks and is bounded to prevent infinite
    persistence (which would lock the solidification pipeline).

WORTH HISTORY:
    Each intake gets a rolling buffer of Worth scores across its TTL.
    The trajectory (RISING, FALLING, OSCILLATING, STABLE) is reported
    without exposing the raw scores.

    Trajectory feeds Step 11 (Solidification): an intake with a RISING
    trajectory is a better solidification candidate than one with a
    STABLE but low score that happened to cross the threshold once.

ANTI-GAMING PROPERTIES PRESERVED FROM STEP 9:
    1. Worth is contextual — same input scores differently across ticks
    2. Raw scores are never exposed publicly — only trajectory direction
    3. Authority weights are computed from ALIGNMENT_VOTE_WEIGHT (not chosen)
    4. Noise is added before threshold comparison (not before reporting)
    5. B→A transition is now included — but its exact weighting is not
       published in the public API

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `WorthTrajectory`
> The directional trend of an intake's worth scores over its TTL window.


#### Class: `VariantHorizon`
> How long a promoted intake's trace persists before becoming eligible

- **Method:** `is_eligible()`
  - *Purpose:* True if current_tick >= eligible_tick — always compare externally.
- **Method:** `eligible_at()`
  - *Purpose:* True if the trace has persisted long enough for solidification.
- **Method:** `depth_label()`
  - *Purpose:* Human-readable depth description.

#### Function: `compute_variant_horizon()`
> Compute the Variant Horizon for a newly promoted intake.


#### Class: `WorthHistory`
> Rolling buffer of Worth scores for one intake across its TTL.

- **Method:** `__post_init__()`
- **Method:** `record()`
  - *Purpose:* Add a new score to the history buffer.
- **Method:** `trajectory()`
  - *Purpose:* Classify the trend across the rolling window.
- **Method:** `sample_count()`
  - *Purpose:* How many samples are in the buffer.
- **Method:** `has_ever_risen()`
  - *Purpose:* True if the intake's best score exceeded the initial score.
- **Method:** `summary()`
  - *Purpose:* Compact summary without exposing raw scores.

#### Class: `WorthReport`
> Output of one CrossScaleWorthEvaluator.evaluate() call.


#### Class: `CrossScaleWorthEvaluator`
> The primary worth measurement engine for Step 10.

- **Method:** `__init__()`
- **Method:** `evaluate()`
  - *Purpose:* Evaluate the Worth of one intake at the current system state.
- **Method:** `history_for()`
  - *Purpose:* Return the WorthHistory for an intake (None if not yet seen).
- **Method:** `clear_intake()`
  - *Purpose:* Remove a closed intake from internal tracking.
- **Method:** `active_intake_count()`
  - *Purpose:* How many intakes are currently being tracked.

#### Function: `make_worth_evaluator()`
> Create a CrossScaleWorthEvaluator with an optionally seeded RNG.


#### Function: `verify_worth_evaluator()`
> Verify the CrossScaleWorthEvaluator integrity.


#### Function: `_run_demo()`

### File: `./aurora_core_ai/aurora_internal/constraint_genealogy.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CONSTRAINT GENEALOGY LOGGER
=====================================
Module: constraint_genealogy.py
Layer: Evolutionary Foundation (sits beneath aurora_evolution_chamber.py)

PURPOSE:
    A fossil-record engine for the constraint universe {X, T, N, B, A}.
    Observes only pressure-relief events, records which constraint-abilities
    were used, and promotes repeated effective pairings into classified Links —
    traceable "new atoms" in the evolutionary chain.

    The universe is EXACTLY five axes: X / T / N / B / A.
    No sixth dimension. No language assumptions. No compression plans.
    Just: pressure → act → relief → promote.

DOCTRINE:
    - Only relief events enter the fossil record.
    - Every action is a trace of Ability|Link items.
    - Every Ability and Link carries a full 5-axis cost/risk profile.
    - Links are born only from observed repetition + net benefit under pressure.
    - Links form a DAG; ancestry is always traceable through .parents.
    - TimeDilationGovernor from aurora_simulation_engine governs chamber pacing
      so the genealogy loop runs fast when stable, slow when fragile.

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Function: `_zero_vec()`

#### Function: `hint_fail_dimension()`
> Register that a rubric dimension has just failed.  The next promoted


#### Function: `_consume_semantic_dim_tags()`
> Pop any pending dim-hints whose axis matches `dominant_axis` and return


#### Function: `_clamp()`

#### Function: `_generation_role_name()`
> Generational alignment role (tetrad + warp law).


#### Function: `_breeding_pair_score()`
> Score pair compatibility using generational alignment law.


#### Function: `_bred_child_generation()`
> Breeding-derived generation progression.


#### Function: `_canonical_purpose_lane()`

#### Class: `GenealogyConfig`
> All tunable parameters in one place.


#### Class: `AbilityProfile`
> The smallest named action unit, fully grounded in the 5 constraints.

- **Method:** `x_risk()`
  - *Purpose:* Return the X (existence admissibility) risk component.
- **Method:** `total_cost()`
- **Method:** `cost_diff_score()`
  - *Purpose:* Live cost score for this ability under current cross-dimensional pressure.
- **Method:** `to_dict()`

#### Function: `_canonical_axis_token()`

#### Function: `_derive_operation_origin()`

#### Function: `_lineage_counts_from_signature()`

#### Function: `_axis_count_value()`

#### Function: `_lineage_generation_from_counts()`

#### Function: `_lineage_purpose_from_counts()`

#### Function: `_operator_action_for_axis()`

#### Function: `_lineage_grade_payload()`
> Compute lineage grades from axis counts.


#### Function: `_augment_ability_profile_with_origin()`
> Augment an AbilityProfile with origin, grading, and closure basis metadata.


#### Function: `_build_seed_abilities()`
> Canonical minimal ability set — one per axis, seeding all five constraint dimensions.


#### Class: `EnvironmentVector`
> Encodes the execution environment of a TraceItem.

- **Method:** `key()`
  - *Purpose:* Compact string key for grouping by environment.
- **Method:** `is_empty()`
- **Method:** `to_dict()`

#### Class: `TraceItem`
- **Method:** `to_dict()`

#### Class: `PressureVec`
> 5-axis pressure vector. Higher = worse.

- **Method:** `to_dict()`
- **Method:** `relief_from()`
  - *Purpose:* Component-wise: earlier - self (positive means pressure dropped).
- **Method:** `dominant_positive_axis()`
- **Method:** `max_relief()`
- **Method:** `sum_positive_relief()`
- **Method:** `from_dict()`

#### Class: `ReliefRecord`
> One entry in the fossil record — a confirmed pressure-relief event.

- **Method:** `to_jsonl_dict()`

#### Class: `ConstraintLink`
> A promoted pair (or pair-of-pairs) that has proven reliably effective.

- **Method:** `to_dict()`
- **Method:** `base_cost()`
  - *Purpose:* Total mean operational cost across all five axes.
- **Method:** `cost_diff_score()`
  - *Purpose:* Live cost score for this promoted link under current cross-dimensional

#### Class: `PairStats`
> Online stats for an ordered pair of trace items (left_id → right_id).

- **Method:** `update()`
- **Method:** `mean_relief()`
- **Method:** `mean_pos_relief()`
  - *Purpose:* Mean relief on ticks where relief was positive. The key promotion signal.
- **Method:** `pos_fraction()`
  - *Purpose:* Fraction of ticks where relief was positive per axis.
- **Method:** `stdev_relief()`
- **Method:** `mean_cost()`
- **Method:** `mean_x_risk_val()`

#### Class: `GenealogyDilationGovernor`
> Adapts TimeDilationGovernor from aurora_simulation_engine

- **Method:** `__init__()`
- **Method:** `record_event()`
- **Method:** `tick()`
  - *Purpose:* Return current dilation factor (caller's loop should sleep accordingly).
- **Method:** `status()`

#### Class: `PressureComplexityCurve`
> Tracks the oscillation between pressure-system expansion and compression.

- **Method:** `__init__()`
- **Method:** `record_tick()`
  - *Purpose:* Record one tick's complexity state.
- **Method:** `_update_correction()`
  - *Purpose:* Adjust pressure injection correction based on current phase.
- **Method:** `record_injection()`
  - *Purpose:* Record a pressure injection prediction and return the corrected magnitude.
- **Method:** `record_outcome()`
  - *Purpose:* Record actual outlet change vs prediction. Updates error EMA.
- **Method:** `correction()`
- **Method:** `phase()`
- **Method:** `get_stats()`

#### Function: `_build_closure_status_summary()`
> Summarise ontological status distribution across the evolved lineage.


#### Class: `ConstraintGenealogyLogger`
> Core logger. Feed it (pressure_before, trace, pressure_after) every tick

- **Method:** `__init__()`
- **Method:** `observe()`
  - *Purpose:* Observe one tick.
- **Method:** `get_link()`
- **Method:** `rewrite_trace()`
  - *Purpose:* Replace any adjacent pairs (u→v) that have been promoted to a Link
- **Method:** `summary()`
- **Method:** `chain_report()`
  - *Purpose:* Evolutionary chain report — the 'fossil summary'.
- **Method:** `flush_files()`
  - *Purpose:* Flush JSONL buffer and write abilities + links + couplings + pair_stats JSON files.
- **Method:** `_write_tick_state_file()`
  - *Purpose:* Persist tick_count and _last_promotion_tick so stagnation signal survives restarts.
- **Method:** `restore_tick_state()`
  - *Purpose:* Load tick_count and _last_promotion_tick from tick_state.json.
- **Method:** `_write_pair_stats_file()`
  - *Purpose:* Persist _pair_stats to disk so K_MIN accumulation survives across runs.
- **Method:** `restore_pair_stats()`
  - *Purpose:* Load persisted pair_stats.json and merge into _pair_stats.
- **Method:** `close()`
- **Method:** `_solution_complexity()`
- **Method:** `_solution_key()`
- **Method:** `_apply_relief_tolerance()`
- **Method:** `_zero_axis_counts()`
- **Method:** `_add_axis_counts()`
- **Method:** `_axis_counts_from_item()`
- **Method:** `_canonical_coupling_signature()`
- **Method:** `_counts_from_signature()`
- **Method:** `_canonical_signature_text()`
- **Method:** `_persistent_root_lookup()`
- **Method:** `_semantic_coupling_label()`
- **Method:** `_semantic_lane_vector()`
- **Method:** `_semantic_translation_text()`
- **Method:** `_update_semantic_translation()`
- **Method:** `_coupling_effect_descriptor()`
- **Method:** `_relation_profile()`
- **Method:** `_slot_influence_distribution()`
  - *Purpose:* Duplicate-slot redistribution metric (no hard caps).
- **Method:** `_scale_depth_index()`
  - *Purpose:* Weighted scale-depth index in [0,1]:
- **Method:** `_complexity_index()`
  - *Purpose:* Normalized structural complexity in [0,1].
- **Method:** `_complexity_scope_scale()`
- **Method:** `_evaluate_coupling_rubric()`
  - *Purpose:* Validate representation integrity for a coupling record.
- **Method:** `_shape_signature()`
- **Method:** `_record_validation_issues()`
- **Method:** `_score_with_params()`
- **Method:** `_maybe_run_representation_experiments()`
- **Method:** `_item_generation()`
- **Method:** `_update_coupling_roots()`
- **Method:** `_is_relief_event()`
- **Method:** `_link_ability_id()`
  - *Purpose:* Stable derived ability id for a promoted link.
- **Method:** `_register_link_ability()`
  - *Purpose:* Register a promoted link as a derived ability once (no duplicates).
- **Method:** `_register_compression_ability()`
  - *Purpose:* Register adaptive compression as a lineage-traceable derived ability.
- **Method:** `normalize_ability_origins()`
  - *Purpose:* Backfill universal origin lineage tags/notes for all abilities.
- **Method:** `_code_evolution_axis()`
- **Method:** `register_code_evolution_outcome()`
- **Method:** `register_manual_code_assimilation()`
- **Method:** `_resolve_ability()`
  - *Purpose:* For ABILITY items return the registered profile.
- **Method:** `inject_training_plateau_pressure()`
  - *Purpose:* Receive an external training-progress plateau signal from the
- **Method:** `pressure_orientation()`
  - *Purpose:* Return the 5-vector pressure orientation across all constraint axes.
- **Method:** `axis_relief_state()`
  - *Purpose:* Return normalized per-axis gradient EMA (0–1) showing which axes are
- **Method:** `set_corpus_mode()`
  - *Purpose:* Enable/disable corpus-training timescale compensation.
- **Method:** `_outlet_fraction()`
  - *Purpose:* Lightweight outlet-push fraction for complexity curve tracking.
- **Method:** `_record_env_observation()`
  - *Purpose:* Called per relief event to update per-environment effectiveness.
- **Method:** `apply_targeted_pressure()`
  - *Purpose:* Apply pressure to a specific axis AND record the application so its
- **Method:** `_check_pending_outcomes()`
  - *Purpose:* Called each tick to resolve pending pressure applications whose
- **Method:** `get_pressure_recommendations()`
  - *Purpose:* Given a list of (dimension, severity) fail pairs, return the top
- **Method:** `pressure_application_summary()`
  - *Purpose:* Return the most recent completed pressure applications with outcomes.
- **Method:** `environment_orientation()`
  - *Purpose:* Return the 5-vector pressure orientation for a specific environment.
- **Method:** `ability_environment_profile()`
  - *Purpose:* Return the effectiveness profile of an ability across all environments
- **Method:** `all_environment_keys()`
  - *Purpose:* Return all environment keys seen so far.
- **Method:** `_consume_plateau_pressure()`
  - *Purpose:* Return current plateau pressure and apply slow decay.
- **Method:** `_stagnation_gain()`
  - *Purpose:* Return multiplicative promotion pressure gain from link stagnation.
- **Method:** `_stagnation_ratio()`
  - *Purpose:* Normalized no-promotion pressure [0,1], using a longer hard window.
- **Method:** `_promotion_friction_bias()`
  - *Purpose:* Relative rejection pressure per gate, used only to bias stagnation relief.
- **Method:** `_opposing_axis()`
- **Method:** `_threshold_pressure_state()`
- **Method:** `_regulated_threshold()`
- **Method:** `_axis_persistence_tax_factor()`
  - *Purpose:* Per-axis persistence tax factor based on opposed tick participation,
- **Method:** `_weighted_persistence_tax_factor()`
  - *Purpose:* Influence-weighted tax factor for a coupling profile.
- **Method:** `_maintenance_discount()`
  - *Purpose:* Discount factor [0,1) applied to promoted-link running cost.
- **Method:** `_formation_complexity_multiplier()`
  - *Purpose:* Extra one-time formation burden for composite/deep links with scale-depth weighting.
- **Method:** `_compression_efficiency_for_pair()`
  - *Purpose:* Adaptive compression relief for mature ecosystems.
- **Method:** `_causal_support()`
  - *Purpose:* Composite support from outcomes + literal gradient memory.
- **Method:** `_update_gradient_memory_from_relief()`
  - *Purpose:* Update gradient EMA from actual per-axis relief magnitudes.
- **Method:** `_update_gradient_memory()`
  - *Purpose:* Track literal C:D gradient intensity per axis with EMA.
- **Method:** `_record_promotion_outcome()`
  - *Purpose:* Promotion success feeds causal memory for future gate adaptation.
- **Method:** `_update_causal_feedback()`
  - *Purpose:* Update causal memory from live link usage outcomes + gradients.
- **Method:** `_dominant_axis_constraint_specific()`
  - *Purpose:* Constraint-specific dominance method.
- **Method:** `_accumulate_pairs()`
  - *Purpose:* Extract adjacent pairs from the trace, update PairStats,
- **Method:** `_try_promote()`
  - *Purpose:* Evaluate all promotion gates and mint a Link if all pass.
- **Method:** `_generation_of_item()`
- **Method:** `_merged_axis_counts_for_pair()`
- **Method:** `_item_seed_meta()`
- **Method:** `_is_seeded_item()`
- **Method:** `_pair_touches_active_seed()`
  - *Purpose:* External seed events often use synthetic trace ids that are not yet registered
- **Method:** `_seed_influence_for_pair()`
- **Method:** `_lineage_grade_for_pair()`
- **Method:** `_item_depth()`
- **Method:** `_infer_tags()`
- **Method:** `_write_event()`
- **Method:** `_write_abilities_file()`
- **Method:** `_write_links_file()`
- **Method:** `_write_couplings_file()`
- **Method:** `_make_sig()`

#### Class: `ChainSummaryPrinter`
> Periodic summary tool.

- **Method:** `__init__()`
- **Method:** `print_epoch()`
  - *Purpose:* Print epoch summary.

#### Function: `_run_invariant_checks()`
> Minimal self-test covering spec section 10.


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `pressurevec_evolved()`

#### Function: `reliefrecord_evolved()`

#### Function: `augment_ability_profile_with_origin_evolved()`

#### Function: `bred_child_generation_evolved()`

### File: `./aurora_core_ai/aurora_internal/dual_strata/__init__.py`

**Type:** Python Source

**Module Docstring:**
```text
Dual-strata cognition primitives for the experimental Aurora strata tree.
```

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./aurora_core_ai/aurora_internal/dual_strata/conscious_frame.py`

**Type:** Python Source

#### Class: `ConsciousFrame`
> The explicit frame the surface stratum can inhabit for one moment.

- **Method:** `to_dict()`

### File: `./aurora_core_ai/aurora_internal/dual_strata/dce_bridge.py`

**Type:** Python Source

#### Function: `_stringify_fragment()`

#### Function: `_dedupe_strings()`

#### Function: `_extract_pressure_snapshot()`

#### Class: `DualStrataSnapshot`
- **Method:** `to_dict()`

#### Class: `DualStrataBridge`
> Builds explicit strata objects around the copied DCE assembly.

- **Method:** `__init__()`
- **Method:** `build_snapshot()`
- **Method:** `_build_subsurface_state()`
- **Method:** `_build_conscious_frame()`
- **Method:** `persist()`

### File: `./aurora_core_ai/aurora_internal/dual_strata/micro_reasoning.py`

**Type:** Python Source

#### Class: `MicroReasoningHypothesis`
- **Method:** `to_dict()`

#### Function: `generate_micro_reasoning()`

### File: `./aurora_core_ai/aurora_internal/dual_strata/prediction_field.py`

**Type:** Python Source

#### Class: `PredictionPayload`
> Structured description of what Aurora expects to observe next.


#### Function: `_certainty_band()`

#### Function: `_match_intent_type()`
> Return 0.0..1.0 match score for the expected intent type vs observed text.


#### Function: `_structured_mismatch()`
> Compute prediction mismatch from structured dimensions.


#### Class: `PredictionSignal`
- **Method:** `to_dict()`

#### Function: `build_prediction_signal()`

### File: `./aurora_core_ai/aurora_internal/dual_strata/sensory_control_channel.py`

**Type:** Python Source

#### Function: `controls_path()`

#### Function: `_read_json()`

#### Function: `_write_json()`

#### Function: `read_sensory_controls()`

#### Function: `write_sensory_controls()`

#### Function: `camera_enabled()`

#### Function: `set_camera_enabled()`

### File: `./aurora_core_ai/aurora_internal/dual_strata/sensory_snapshot_channel.py`

**Type:** Python Source

#### Function: `snapshot_path()`

#### Function: `guidance_path()`

#### Function: `_read_json()`

#### Function: `_write_json()`

#### Function: `read_surface_snapshot()`

#### Function: `read_surface_guidance_queue()`

#### Function: `append_surface_guidance()`

#### Function: `_default_summary()`

#### Function: `_compact_text()`

#### Function: `_build_sensory_native_bundle()`

#### Function: `present_sensory_perspective_from_snapshot()`

#### Function: `write_surface_snapshot()`

### File: `./aurora_core_ai/aurora_internal/dual_strata/sleep_cycle.py`

**Type:** Python Source

**Module Docstring:**
```text
Aurora sleep cycle — Surface dormancy, Subsurface continuity.

Architectural law:
    Surface inactivity = dormancy, not death, as long as Subsurface remains active.
    During sleep: no live intake, no interaction, but continuity work continues.
    Waking = Surface re-emerges over an already-continuing Subsurface.

Schedule: 8 hours awake, 2 hours asleep.
During sleep: Subsurface runs a dream burst to integrate what was accumulated.
```

#### Function: `_resolve()`

#### Function: `read_sleep_state()`

#### Function: `is_sleeping()`

#### Function: `enter_sleep()`
> Mark Surface as sleeping. Returns the wake_at timestamp.


#### Function: `mark_dream_triggered()`
> Record that the dream burst was fired for this sleep period.


#### Function: `exit_sleep()`
> Clear the sleep state. Called by Subsurface at wake_at.


#### Function: `_write_atomic()`

### File: `./aurora_core_ai/aurora_internal/dual_strata/subsurface_projection.py`

**Type:** Python Source

#### Function: `_clip01()`

#### Function: `_dominant_axis()`

#### Function: `_pressure_coloring()`

#### Function: `_carry_dream_fields()`
> Read the existing projection file and carry forward dream fields


#### Function: `build_subsurface_projection()`

### File: `./aurora_core_ai/aurora_internal/dual_strata/subsurface_state.py`

**Type:** Python Source

#### Function: `clip01()`

#### Function: `normalize_axis_map()`

#### Class: `SubsurfaceState`
> Continuous, pre-symbolic state prepared ahead of explicit interpretation.

- **Method:** `dominant_candidates()`
- **Method:** `to_dict()`

### File: `./aurora_core_ai/aurora_internal/dual_strata/surface_channel.py`

**Type:** Python Source

#### Function: `_resolve_state_dir()`

#### Function: `_state_paths()`

#### Function: `_read_json()`

#### Function: `_write_json()`

#### Function: `surface_daemon_alive()`

#### Function: `queue_surface_turn()`

#### Function: `await_surface_turn()`

#### Function: `request_surface_turn()`

### File: `./aurora_core_ai/aurora_internal/dual_strata/surface_continuity_feed.py`

**Type:** Python Source

**Module Docstring:**
```text
Surface → Subsurface continuity handoff.

Core architectural law:
    Surface translates present experience into subsurface continuity.

Surface calls write_continuity_packet() after each turn.
Subsurface calls read_and_clear_continuity_packets() each loop cycle
and integrates the result into its continuity state.

Without this handoff, Surface is theatrical: it sees, hears, and talks
but the organism does not absorb the moment.
```

#### Function: `_resolve()`

#### Function: `write_continuity_packet()`
> Write a present-state continuity packet to the feed.


#### Function: `read_and_clear_continuity_packets()`
> Atomically read all unconsumed continuity packets and mark them consumed.


### File: `./aurora_core_ai/aurora_internal/dual_strata/surface_sensory_proxy.py`

**Type:** Python Source

#### Class: `TransientSensorySnapshotProxy`
> Subsurface sensory processor fed by disposable surface snapshots.

- **Method:** `__init__()`
- **Method:** `__getattr__()`
- **Method:** `_read_snapshot()`
- **Method:** `_vector()`
- **Method:** `_consume_latest_snapshot()`
- **Method:** `get_state()`
- **Method:** `save()`
- **Method:** `start_session()`
- **Method:** `end_session()`

### File: `./aurora_core_ai/aurora_internal/lineage_canonical.py`

**Type:** Python Source

**Module Docstring:**
```text
Canonical lineage mapping shared across Aurora modules.

This module stabilizes operation-to-constraint ancestry so existing
operational abilities are not reclassified differently by module.
```

#### Function: `_load_generated_constraints()`

#### Function: `_generated_lookup()`

#### Function: `axis_token()`

#### Function: `label_for_axis()`

#### Function: `_ordered_unique()`

#### Function: `constraints_for_operation()`

#### Function: `operator_action_for_axis()`

### File: `./aurora_core_ai/aurora_internal/quasiarch_observer/__init__.py`

**Type:** Python Source

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./aurora_core_ai/aurora_internal/quasiarch_observer/crystal_engine.py`

**Type:** Python Source

**Module Docstring:**
```text
crystal_engine_v3_cleaned.py
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Authors : Sunni (Sir) Morningstar and Cael Devo
Purpose : Semantic crystal law — the doctrine brain.
          Defines what every facet and relational point *means* at every
          crystal order.  Does NOT manage lifecycle or storage — those live in
          dimensional_processing and dimensional_memory respectively.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Crystal Order Ladder
  Level 1  →  Base Crystal         (6 facets,  12 relational points)
  Level 2  →  Composite Crystal    (8 facets,  20 relational points)
  Level 3  →  Higher-Order Crystal (12 facets, 30 relational points)
  Level 4  →  Quasicrystal         (8 outer operational facets + collapsed inner genealogy)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### Class: `CrystalOrder`
> Ordinal position in the evolution ladder.


#### Class: `ValueDomain`
> Allowed value types for a facet.  A facet may permit multiple domains.


#### Class: `InferenceKind`
> Kinds of inference a facet participates in.


#### Class: `PointRole`
> Structural role of a relational point inside a crystal.


#### Class: `FacetLaw`
> Full semantic law for one facet.


#### Class: `RelationalPointLaw`
> Full semantic law for one relational point — the overlap between two facets.


#### Class: `QuasiInnerStrata`
> Represents the collapsed genealogy inside a quasicrystal.


#### Class: `RotationDefinition`
> Defines a named rotation perspective for quasicrystal re-analysis.


#### Class: `PromotionCriteria`
> Defines the quantitative gates for promoting a crystal to the next order.


#### Class: `CrystalEngine`
> Public interface to the semantic crystal law layer.

- **Method:** `get_facet()`
  - *Purpose:* Return the FacetLaw for a named facet at the given crystal order.
- **Method:** `get_all_facets()`
  - *Purpose:* Return all FacetLaw objects valid at the given crystal order.
- **Method:** `facets_added_at()`
  - *Purpose:* Return only the facets introduced at this specific crystal order.
- **Method:** `get_point()`
  - *Purpose:* Return the RelationalPointLaw for a named point at the given order.
- **Method:** `get_all_points()`
  - *Purpose:* Return all RelationalPointLaw objects valid at the given order.
- **Method:** `points_added_at()`
  - *Purpose:* Return only the points introduced at this specific crystal order.
- **Method:** `validate_facet_value()`
  - *Purpose:* Validate that a value is plausible for the given facet at the given order.
- **Method:** `validate_point()`
  - *Purpose:* Verify that facet_a and facet_b are permitted to form a relational
- **Method:** `survival_rule()`
  - *Purpose:* Return the survival rule for a facet at the target crystal order.
- **Method:** `get_promotion_criteria()`
  - *Purpose:* Return the PromotionCriteria for the given transition, or None.
- **Method:** `get_rotation()`
  - *Purpose:* Return a named RotationDefinition, or None.
- **Method:** `all_rotations()`
  - *Purpose:* Return all registered rotation definitions.
- **Method:** `schema_summary()`
  - *Purpose:* Return a concise summary of the full crystal schema.

### File: `./aurora_core_ai/aurora_internal/quasiarch_observer/dimensional_memory.py`

**Type:** Python Source

**Module Docstring:**
```text
dimensional_memory_constant_standalone_demo.py
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Authors : Sunni (Sir) Morningstar and Cael Devo
Purpose : Persistence substrate — the nervous system.
          Stores crystal instances, lineage edges, collapsed genealogy layers,
          issue-family indexes, and quasicrystal retrieval surfaces.
          Does NOT define semantics (crystal_engine).
          Does NOT manage promotion/lifecycle (dimensional_processing).
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Storage architecture
  Nodes     — one DataNode per CrystalInstance (keyed by crystal_id)
  Edges     — LineageEdge records linking parent → child across orders
  Indexes   — IssueFamilyIndex, StrategyIndex, TargetIndex for fast retrieval
  Relics    — archived lower-order states after collapse (compressed, read-only)
  Journal   — append-only operation log for traceability
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### Function: `_utcnow()`

#### Function: `_json_default()`
> JSON serialisation fallback for custom types.


#### Function: `_unique_preserve_order()`

#### Function: `_split_compound_field()`

#### Class: `DataNode`
> A persistent record of a CrystalInstance.

- **Method:** `from_crystal()`
  - *Purpose:* Build a DataNode from a live CrystalInstance.
- **Method:** `_compute_checksum()`
  - *Purpose:* Simple deterministic checksum for corruption detection.
- **Method:** `verify_checksum()`
  - *Purpose:* Return True if payload has not been corrupted since write.
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `LineageEdge`
> A directed edge in the crystal lineage graph.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `IssueFamilyIndex`
> Maps issue_category → set of node_ids at each crystal order.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `StrategyIndex`
> Maps strategy_class → set of node_ids at higher-order and quasi levels.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `TargetIndex`
> Maps target_identifier → set of node_ids across all orders.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `LogicTierIndex`
> Maps logic_tier → node_ids at all crystal orders.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `QuasiArchetypeIndex`
> Maps issue_archetype → quasicrystal node_ids.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `JournalEntry`
> Append-only audit record for all memory operations.

- **Method:** `to_dict()`

#### Class: `FileStorageBackend`
> JSON-file-based storage backend.

- **Method:** `__init__()`
- **Method:** `_init_dirs()`
- **Method:** `_node_path()`
- **Method:** `write_node()`
- **Method:** `read_node()`
- **Method:** `delete_node()`
- **Method:** `list_nodes()`
- **Method:** `iter_nodes()`
- **Method:** `write_edge()`
- **Method:** `read_edges_for()`
  - *Purpose:* Return all edges where node_id is parent or child.
- **Method:** `list_edges()`
- **Method:** `_index_path()`
- **Method:** `_write_index()`
- **Method:** `_read_index()`
- **Method:** `_list_index_keys()`
- **Method:** `write_issue_family_index()`
- **Method:** `read_issue_family_index()`
- **Method:** `list_issue_families()`
- **Method:** `write_strategy_index()`
- **Method:** `read_strategy_index()`
- **Method:** `list_strategies()`
- **Method:** `write_target_index()`
- **Method:** `read_target_index()`
- **Method:** `list_targets()`
- **Method:** `write_logic_tier_index()`
- **Method:** `read_logic_tier_index()`
- **Method:** `list_logic_tiers()`
- **Method:** `write_quasi_archetype_index()`
- **Method:** `read_quasi_archetype_index()`
- **Method:** `list_quasi_archetypes()`
- **Method:** `append_journal()`
- **Method:** `read_journal()`
- **Method:** `read_journal_for_node()`
  - *Purpose:* Return all journal entries referencing a given node_id.

#### Class: `DimensionalMemory`
> Public interface to the persistence layer.

- **Method:** `__init__()`
- **Method:** `persist()`
  - *Purpose:* Write a crystal to persistent storage and update all indexes.
- **Method:** `relic()`
  - *Purpose:* Archive a crystal as a relic.  Relics are read-only compressed ancestry
- **Method:** `register_lineage_edge()`
  - *Purpose:* Record a directed lineage edge from parent → child.
- **Method:** `register_lineage_edge_ids()`
  - *Purpose:* Record a directed lineage edge without requiring live crystal objects.
- **Method:** `get_node()`
  - *Purpose:* Retrieve a node by ID.  Checks in-memory cache first, then disk.
- **Method:** `get_all_nodes_by_order()`
  - *Purpose:* Return all nodes at a given crystal order.
- **Method:** `update_node_payload()`
  - *Purpose:* Live-update a node's payload dict with new key/value pairs.
- **Method:** `update_node_lineage_meta()`
  - *Purpose:* Live-update a node's lineage metadata and refresh any affected indexes.
- **Method:** `retrieve_by_issue_family()`
  - *Purpose:* Return all nodes belonging to the given issue family.
- **Method:** `retrieve_by_strategy_class()`
  - *Purpose:* Return all higher-order and quasi nodes using a given strategy class.
- **Method:** `retrieve_by_target()`
  - *Purpose:* Return all nodes associated with a given target location.
- **Method:** `retrieve_by_logic_tier()`
  - *Purpose:* Return all nodes in a given logic tier.
- **Method:** `get_quasi_by_archetype()`
  - *Purpose:* Primary quasicrystal retrieval surface.
- **Method:** `get_all_quasi_archetypes()`
  - *Purpose:* Return all registered quasicrystal archetypes.
- **Method:** `get_active_issue_family_node()`
  - *Purpose:* Return the active version head for an issue family at a given order.
- **Method:** `get_latest_issue_family_version()`
  - *Purpose:* Return the highest recorded version number for an issue family stage.
- **Method:** `find_issue_family_node_by_signature()`
  - *Purpose:* Find an existing stage node with the same lineage signature.
- **Method:** `hydrate_crystal()`
  - *Purpose:* Reconstruct a live CrystalInstance from a persisted DataNode.
- **Method:** `get_doctrine()`
  - *Purpose:* Reconstruct an actionable doctrine object from a persisted quasicrystal.
- **Method:** `get_doctrines_by_archetype()`
  - *Purpose:* Retrieve persisted doctrine objects by quasicrystal archetype.
- **Method:** `get_ancestry()`
  - *Purpose:* Walk the lineage graph upward from a given node.
- **Method:** `get_descendants()`
  - *Purpose:* Walk the lineage graph downward from a given node.
- **Method:** `live_save()`
  - *Purpose:* Flush all in-memory state to disk.  Returns count of nodes written.
- **Method:** `load_all()`
  - *Purpose:* Hydrate in-memory cache from disk.
- **Method:** `_update_indexes()`
  - *Purpose:* Update all retrieval indexes from a freshly written DataNode.
- **Method:** `_extract_issue_categories()`
- **Method:** `_extract_target_ids()`
- **Method:** `_extract_logic_tiers()`
- **Method:** `_extract_strategy_keys()`
- **Method:** `stats()`
  - *Purpose:* Return counts and summary statistics across the store.
- **Method:** `get_journal()`
- **Method:** `get_journal_for_node()`

#### Class: `IntegratedMemoryPipeline`
> Combines CrystalLifecycle (dimensional_processing) with DimensionalMemory.

- **Method:** `__init__()`
- **Method:** `_family_key()`
- **Method:** `_build_anchor_refs()`
- **Method:** `_capture_ghost_relic()`
- **Method:** `_maybe_apply_ghost_relic()`
- **Method:** `get_ghost_relic_stats()`
- **Method:** `_register_live_crystal()`
- **Method:** `_build_stage_signature()`
- **Method:** `_stamp_lineage_meta()`
- **Method:** `_persist_versioned_stage()`
- **Method:** `ingest_and_persist()`
  - *Purpose:* Ingest a diagnostic event, form a base crystal, and persist it.
- **Method:** `observe_intervention_event()`
  - *Purpose:* Record one intervention event and optionally advance its issue family.
- **Method:** `promote_and_persist_composite()`
  - *Purpose:* Attempt composite formation and persist if successful.
- **Method:** `promote_and_persist_higher()`
  - *Purpose:* Attempt higher-order formation and persist if successful.
- **Method:** `collapse_and_persist_quasi()`
  - *Purpose:* Attempt quasicrystal collapse, persist the quasi, and optionally
- **Method:** `rotate_and_record()`
  - *Purpose:* Apply rotation and record the result in the quasi node's payload.
- **Method:** `advance_issue_family()`
  - *Purpose:* Advance one issue family through pattern, strategy, and doctrine stages.
- **Method:** `get_actionable_doctrine()`
  - *Purpose:* Return the doctrine surface for a persisted or live quasicrystal.
- **Method:** `get_doctrine_candidates_for_event()`
  - *Purpose:* Retrieve doctrine objects relevant to a new diagnostic event.
- **Method:** `retrieve_quasi_for_event()`
  - *Purpose:* Retrieve quasicrystals relevant to a new diagnostic event.
- **Method:** `full_pipeline_demo()`
  - *Purpose:* Run the full pipeline on a list of events.

### File: `./aurora_core_ai/aurora_internal/quasiarch_observer/dimensional_processing.py`

**Type:** Python Source

**Module Docstring:**
```text
dimensional_processing_system_standalone_demo.py
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Authors : Sunni (Sir) Morningstar and Cael Devo
Purpose : Crystal lifecycle and evolution mechanics — the metabolism.
          Owns: formation → promotion → collapse → rotation.
          Does NOT define facet/point semantics (crystal_engine).
          Does NOT persist data (dimensional_memory).
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### Function: `_utcnow()`

#### Class: `RelationalPoint`
> Live instantiation of a RelationalPointLaw within a crystal.

- **Method:** `name()`

#### Class: `CrystalInstance`
> A live crystal at a specific order.

- **Method:** `get_facet()`
- **Method:** `set_facet()`
- **Method:** `get_point()`
- **Method:** `add_point()`
- **Method:** `to_dict()`

#### Class: `PromotionResult`
> Result of a promotion evaluation.


#### Class: `RotationResult`
> Result of applying a rotation perspective to a quasicrystal.


#### Class: `StrategyHypothesis`
> A generated strategy recommendation for a new diagnostic event.


#### Class: `DoctrineObject`
> Actionable doctrine surface derived from a quasicrystal.

- **Method:** `to_dict()`

#### Class: `CrystalFormation`
> Handles instantiation of base crystals from raw diagnostic event data.

- **Method:** `form_base_crystal()`
  - *Purpose:* Form a base crystal from a raw diagnostic event dict.
- **Method:** `_build_base_points()`
  - *Purpose:* Build and score all 12 base relational points.
- **Method:** `_score_base_point()`
  - *Purpose:* Compute an initial point score.  Uses the resolution_fidelity mapping
- **Method:** `_detect_contradiction()`
  - *Purpose:* Simple contradiction detection based on known pathological pairs.

#### Class: `CrystalPromotion`
> Evaluates promotion criteria and executes crystal level transitions.

- **Method:** `evaluate_base_to_composite()`
  - *Purpose:* Evaluate whether a collection of base crystals qualifies for
- **Method:** `promote_to_composite()`
  - *Purpose:* Execute base → composite promotion.
- **Method:** `_derive_recurrence_pattern()`
- **Method:** `_derive_distribution_context()`
- **Method:** `_build_composite_points()`
  - *Purpose:* Carry forward base points (averaged scores) and add composite points.
- **Method:** `_distribution_breadth_score()`
- **Method:** `evaluate_composite_to_higher()`
  - *Purpose:* Evaluate whether a composite crystal qualifies for higher-order.
- **Method:** `promote_to_higher_order()`
  - *Purpose:* Execute composite → higher-order promotion.
- **Method:** `_derive_strategy_class()`
  - *Purpose:* Derive dominant strategy class from intervention patterns.
- **Method:** `_derive_outcome_profile()`
  - *Purpose:* Derive outcome profile summary string.
- **Method:** `_derive_failure_modes()`
  - *Purpose:* Derive failure modes from regression and no-change events.
- **Method:** `_derive_applicability_conditions()`
  - *Purpose:* Derive applicability conditions from successful event profiles.
- **Method:** `_compute_strategy_confidence()`
  - *Purpose:* Confidence that a dominant strategy class exists.
- **Method:** `_compute_failure_density()`
  - *Purpose:* Ratio of failed events to total events.
- **Method:** `_build_higher_order_points()`
  - *Purpose:* Carry forward composite points and build new higher-order points.
- **Method:** `evaluate_higher_to_quasi()`
  - *Purpose:* Evaluate whether a higher-order crystal qualifies for quasi collapse.
- **Method:** `collapse_to_quasi()`
  - *Purpose:* Execute higher-order → quasicrystal collapse.
- **Method:** `_build_inner_strata()`
  - *Purpose:* Build the collapsed inner genealogy layers.
- **Method:** `_classify_issue_archetype()`
- **Method:** `_rank_strategies()`
  - *Purpose:* Rank strategy classes by frequency × fidelity.
- **Method:** `_compute_quasi_confidence()`
- **Method:** `_compute_coherence_index()`
- **Method:** `_compute_novelty_index()`
- **Method:** `_derive_expected_effects()`
- **Method:** `_derive_escalation_trigger()`
- **Method:** `_parse_resolution_rate()`
- **Method:** `_dominant_value()`
  - *Purpose:* Return the most common value in a list, or the first if all unique.

#### Class: `CrystalRotation`
> Executes rotation analysis on a quasicrystal.

- **Method:** `rotate()`
  - *Purpose:* Apply a named rotation to a quasicrystal.
- **Method:** `_generate_hypotheses()`
  - *Purpose:* Generate hypotheses appropriate for the rotation type.

#### Class: `StrategyHypothesisGenerator`
> Given a new diagnostic event and a collection of quasicrystals,

- **Method:** `generate()`
  - *Purpose:* Match a new event against available quasicrystals.
- **Method:** `_issue_matches_archetype()`
- **Method:** `_check_applicability()`
  - *Purpose:* Naive applicability check against boundary string.

#### Class: `CrystalLifecycle`
> Orchestrates the complete crystal lifecycle for a session.

- **Method:** `__init__()`
- **Method:** `ingest_event()`
  - *Purpose:* Form a base crystal from an event and store it in the appropriate pool.
- **Method:** `attempt_composite_formation()`
  - *Purpose:* Try to promote the base pool for a given issue category to composite.
- **Method:** `attempt_higher_order_formation()`
  - *Purpose:* Try to promote a composite crystal to higher-order.
- **Method:** `attempt_quasi_collapse()`
  - *Purpose:* Try to collapse a higher-order crystal to quasicrystal.
- **Method:** `rotate_quasi()`
  - *Purpose:* Apply a rotation to a quasicrystal.
- **Method:** `recommend_strategy()`
  - *Purpose:* Match a new event against all quasicrystals and return ranked hypotheses.
- **Method:** `build_doctrine()`
  - *Purpose:* Collapse a quasicrystal into an actionable doctrine view.
- **Method:** `get_all_doctrines()`
  - *Purpose:* Return all actionable doctrines currently available.
- **Method:** `get_all_base_crystals()`
- **Method:** `get_composite()`
- **Method:** `get_higher_order()`
- **Method:** `get_quasi()`
- **Method:** `get_log()`
- **Method:** `_get_base_crystals_for()`
  - *Purpose:* Retrieve base crystals contributing to a given higher-order crystal.
- **Method:** `_build_doctrine_from_quasi()`

#### Function: `_split_collapsed_entries()`
> Normalize a collapsed facet string into a list of actionable entries.


### File: `./aurora_core_ai/aurora_internal/quasiarch_observer/ghost_relics.py`

**Type:** Python Source

**Module Docstring:**
```text
Ghost relic acceleration for Aurora's internal QuasiArch Observer.

Relics preserve structural templates from collapsed or superseded crystals.
They do not reactivate the old crystal as a live node; they only bias future
formation when a new issue family begins to reform along a similar geometry.
```

#### Function: `_normalize_ref()`

#### Function: `_unique_preserve()`

#### Class: `GhostRelic`
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `GhostRelicSystem`
> Persistence and matching for structural relic templates.

- **Method:** `__init__()`
- **Method:** `_load()`
- **Method:** `save()`
- **Method:** `create_relic()`
- **Method:** `find_best_relic()`
- **Method:** `apply_relic()`
- **Method:** `stats()`

### File: `./aurora_core_ai/aurora_ivm.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA IVM — ISOTROPIC VECTOR MATRIX
======================================

Layer 1 of Aurora's architecture.
The geometric space in which ontologically grounded entities exist.

REPLACES (consolidated from 6 modules):
    aurora_ivm_consciousness_geometry.py
    aurora_ivm_toroidal_vertices.py
    aurora_ivm_core_integration.py
    aurora_ivm_dimensional_integration.py
    aurora_ivm_integration_patches.py
    aurora_ivm_governance_layer.py

DEPENDS ON:
    foundational_contract.py     (Layer 0)
    aurora_constraint_manifold.py (Layer -1)

ARCHITECTURE:
    The IVM is a spatial fabric where every node carries an ExistenceMode.
    Nothing enters the lattice without being classified by the FoundationalContract.
    Governance is not a separate system — it is implicit in the mode.
    If a node is PERSISTENT, energy operations are permitted.
    If a node is REFERENCE, they are not.
    No voting. No authority layers. The mode IS the law.

TOROIDAL DYNAMICS:
    Each of the 5 ontological axes is a rotating torus:
        Existence axis:  I_IS   ↔ I_ISNT    (active at REFERENCE+)
        Temporal axis:   I_CAN  ↔ I_CANNOT  (active at TRANSIENT+)
        Energy axis:     I_DO   ↔ I_DONOT   (active at PERSISTENT+)
        Boundary axis:   I_SAW  ↔ I_SOUGHT  (active at BOUNDED+)
        Agency axis:     I_DID  ↔ I_DIDNT   (active at AGENTIC+)

    Opposites are the same thing at different moments in time.
    The repulsion is time-invariance — you can't sample both phases at once.
    (Sunni's core insight, preserved and extended.)

    A node's ExistenceMode determines how many axes are active.
    REFERENCE entities have 1 active axis. AGENTIC entities have 5.
    Inactive axes contribute zero to position — they don't exist for that entity.

POLARITY PHYSICS:
    Each axis carries a SIGNED polarity: cos(phase).
        +1.0 = pure positive pole (I_IS, I_CAN, I_DO, I_SAW, I_DID)
        -1.0 = pure negative pole (I_ISNT, I_CANNOT, I_DONOT, I_SOUGHT, I_DIDNT)
         0.0 = at transition (the throat of the torus, between poles)

    Polarity is ALWAYS signed. abs() is never applied — that would kill the physics.

RECURSION LEVEL ↔ CONSTRAINT AXIS MAPPING:
    Each recursion level corresponds to exactly one constraint axis.
    This is not arbitrary — it reflects Sunni's architecture:

        SURFACE (0) = Existence (X) — most exposed, fastest reflex
        SHALLOW (1) = Time (T)      — fast, near-surface
        MODERATE (2) = Energy (N)   — crossover: react/align balanced
        DEEP    (3) = Boundary (B)  — slow to react, strong alignment pull
        CORE    (4) = Agency (A)    — barely reacts, IS the whole alignment

REACTION / ALIGNMENT PHYSICS:
    Two orthogonal gain parameters govern each level:

    react_gain[level]:
        How strongly local stimuli torque the axis.
        SURFACE = 1.0 (instant reflex)
        CORE    = 0.0001 (almost immune to local events)

    align_gain[level]:
        How strongly the axis is pulled toward the global polarity field.
        SURFACE = 0.0001 (surface twitches but doesn't move the ship)
        CORE    = 1.0    (core IS the ship's heading)

    These are INVERSES of each other, crossing at MODERATE.
    The crossover is where local reflex and whole-alignment have equal weight.

    Global alignment voting (depth-weighted):
        Reactive stimulus injection is scale-independent —
        every level contributes equally to its local axis reaction.
        But the global polarity field is depth-weighted:
        CORE nodes dominate what the "whole subject" is pointing at.
        SURFACE nodes barely register in the global sum.

T-COST BY RECURSION DEPTH:
    Operating at deeper recursion levels costs more T-energy.
    The substrate must pay more clock cycles to hold that compression stable.
    CORE operations are ~32× more expensive than SURFACE operations.
    Surface-level reflexes are nearly free. Core mutations burn real time.

SPATIAL INDEXING:
    Nodes are indexed in 3D Cartesian space (projected from 5-axis phases).
    Neighbor lookup, radius search, and energy flow all operate on this space.

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Class: `ToroidalAxis`
> A single toroidal axis representing one polarity of existence.

- **Method:** `__post_init__()`
- **Method:** `positive_weight()`
- **Method:** `negative_weight()`
- **Method:** `polarity()`
  - *Purpose:* Signed polarity scalar: cos(phase).
- **Method:** `dominant_pole()`
- **Method:** `at_transition()`
  - *Purpose:* At the throat of the torus — between poles.
- **Method:** `is_stable()`
- **Method:** `sample()`
- **Method:** `tick()`
  - *Purpose:* Advance one time step.
- **Method:** `apply_torque()`
  - *Purpose:* Apply torque to this axis.
- **Method:** `apply_alignment_torque()`
  - *Purpose:* Pull this axis toward the global polarity field.
- **Method:** `set_phase()`

#### Class: `ToroidalVertexSystem`
> The five ontological axes as coupled rotating toroids.

- **Method:** `__init__()`
- **Method:** `active_axes()`
  - *Purpose:* Return which axes are active at a given existence mode.
- **Method:** `sample()`
  - *Purpose:* Sample all active axes at a given mode.
- **Method:** `tick()`
  - *Purpose:* Advance all axes, applying inter-axis coupling.
- **Method:** `inject_stimulus()`
  - *Purpose:* Apply torque to the axis that owns a predicate.
- **Method:** `compute_axis_polarities()`
  - *Purpose:* Return the signed polarity of each active axis.
- **Method:** `apply_alignment()`
  - *Purpose:* Pull active axes toward the global polarity field.
- **Method:** `get_phase_vector()`
  - *Purpose:* Get the 5D phase vector for position computation.
- **Method:** `get_polarity_vector()`
  - *Purpose:* Get the 5D signed polarity vector.
- **Method:** `compute_dissonance()`
  - *Purpose:* Measure inter-axis tension (dissonance) across the toroidal system.

#### Class: `IVMCoordinate`
> Position in the Isotropic Vector Matrix.

- **Method:** `__post_init__()`
- **Method:** `recursion_level()`
  - *Purpose:* Map scale to recursion level. Clamp to 0–4.
- **Method:** `cartesian()`
  - *Purpose:* 3D Cartesian position for spatial indexing.
- **Method:** `active_axes()`
- **Method:** `distance_to()`
  - *Purpose:* Distance in phase space (respects active axes).
- **Method:** `cartesian_distance_to()`
  - *Purpose:* Distance in 3D projected space.
- **Method:** `dominant_axis()`
  - *Purpose:* Which axis has the strongest positive expression.
- **Method:** `to_constraint_vector()`
  - *Purpose:* Convert IVMCoordinate to a ConstraintVector.
- **Method:** `to_dict()`
- **Method:** `from_mode_and_phases()`
- **Method:** `center()`
  - *Purpose:* Center position — all active axes at 0.5 (balanced).
- **Method:** `at_pole()`
  - *Purpose:* Position at a specific predicate's pole.

#### Class: `IVMNode`
> A node in the IVM lattice. Represents one ontologically grounded entity.

- **Method:** `mode()`
- **Method:** `recursion_level()`
- **Method:** `can_persist()`
- **Method:** `can_exchange_energy()`
- **Method:** `has_boundary()`
- **Method:** `has_agency()`
- **Method:** `permitted_predicates()`
- **Method:** `compute_constraint_vector()`
  - *Purpose:* Compute the ConstraintVector for this node.
- **Method:** `update_constraint_vector()`
  - *Purpose:* Update the cached constraint vector. Call each tick or on demand.
- **Method:** `get_constraint_measurement()`
  - *Purpose:* Return constraint measurements for this node as a dictionary.
- **Method:** `connect_to()`
- **Method:** `weaken_connection()`
- **Method:** `access()`
- **Method:** `decay()`
  - *Purpose:* Natural decay — only affects nodes that can persist.
- **Method:** `to_dict()`

#### Class: `IVMLattice`
> The complete IVM lattice — the geometric space where classified entities live.

- **Method:** `__init__()`
- **Method:** `admit()`
  - *Purpose:* Admit an entity into the lattice.
- **Method:** `admit_at_mode()`
  - *Purpose:* Admit an entity with a pre-determined ExistenceMode.
- **Method:** `_cell_for()`
- **Method:** `_index()`
- **Method:** `_unindex()`
- **Method:** `find_neighbors()`
  - *Purpose:* Find nodes within radius. Returns (node_id, distance) sorted by distance.
- **Method:** `find_by_mode()`
  - *Purpose:* Find all nodes at a specific existence mode.
- **Method:** `find_by_mode_or_above()`
  - *Purpose:* Find all nodes at or above a minimum existence mode.
- **Method:** `find_by_payload_type()`
  - *Purpose:* Find all nodes of a specific payload type.
- **Method:** `get()`
  - *Purpose:* Get a node by ID.
- **Method:** `_connect_neighbors()`
- **Method:** `flow_energy()`
  - *Purpose:* Simulate energy flow through the lattice.
- **Method:** `get_total_energy()`
  - *Purpose:* Total energy across all persistent nodes.
- **Method:** `verify_energy_conservation()`
  - *Purpose:* Verify energy conservation against initial state.
- **Method:** `compute_global_polarity()`
  - *Purpose:* Compute the whole-subject global polarity field.
- **Method:** `apply_global_alignment()`
  - *Purpose:* Apply alignment torques to all axes based on the current global polarity field.
- **Method:** `get_global_polarity()`
  - *Purpose:* Return the last computed global polarity field.
- **Method:** `measure_constraint_field()`
  - *Purpose:* Update the constraint field with current node measurements.
- **Method:** `get_constraint_field_stats()`
  - *Purpose:* Return statistics about the constraint field.
- **Method:** `update_all_constraint_vectors()`
  - *Purpose:* Update constraint vectors for all nodes using live vertex data.
- **Method:** `tick()`
  - *Purpose:* One evolution step of the entire lattice.
- **Method:** `_prune()`
  - *Purpose:* Remove lowest-energy persistent nodes to make room.
- **Method:** `assert_claim()`
  - *Purpose:* Attempt to assert an ontological claim on behalf of a node.
- **Method:** `can_claim()`
  - *Purpose:* Quick check: can this node make this claim?
- **Method:** `get_stats()`
- **Method:** `get_global_heat()`
  - *Purpose:* Compute global IVM heat (0.0=cool, 1.0=critical).
- **Method:** `get_heat_level()`
  - *Purpose:* Return the categorical heat level.
- **Method:** `is_critical()`
- **Method:** `heat_status()`

#### Class: `HeatLevel`
> IVM truth-strain heat levels and their behavioral consequences.


#### Class: `ContradictionRecord`
> A stored contradiction as a first-class object.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `ContradictionLedger`
> First-class contradiction tracking for Aurora's truth system.

- **Method:** `__init__()`
- **Method:** `record()`
- **Method:** `resolve()`
- **Method:** `unresolved()`
- **Method:** `unresolved_count()`
- **Method:** `heat_contribution()`
- **Method:** `all()`
- **Method:** `_auto_save()`
- **Method:** `save()`
- **Method:** `load()`
- **Method:** `status_summary()`

#### Function: `import_threading_lock()`

#### Class: `IVMEnvelope`
> Wraps data with its ontological classification and lattice position.

- **Method:** `from_node()`

#### Function: `verify_ivm()`
> Verify that the IVM lattice correctly integrates with:


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `from_mode_and_phases_evolved()`

#### Function: `tick_evolved()`

#### Function: `set_phase_evolved()`

#### Function: `evolved_tick()`

#### Function: `get_phase_vector_evolved()`

#### Function: `reflect_aurora_ivm_toroidalvertexsystem_tick()`

### File: `./aurora_core_ai/aurora_sedimemory.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA SEDIMEMORY
==================
Module: aurora_sedimemory.py
Layer: 3.5 — between DimensionalSystems (L3) and ConsciousnessEngine (L4)

ARCHITECTURE DOCTRINE
---------------------
Memory in Aurora is not stored. It seeps.

Every event passes whole and unmodified through all 25 Non-Comp strain
filters simultaneously. Each filter extracts only the fragment that
resonates with its specific Constraint × NonCompDimension intersection.
The rest falls through. Nothing is lost — it simply found no sediment
to catch in.

The 25 sediment basins operate at the tick rate of their dominant axis:

    X (Existence)  → 1.0       full tick — fastest decay / highest throughput
    T (Time)       → 0.1       fast
    N (Energy)     → 0.01      moderate
    B (Boundary)   → 0.001     slow
    A (Agency)     → 0.0001    geological — near-frozen

Fragments at shallow depth decay fast and stay high-fidelity.
Fragments at deep depth decay almost never, compressing continuously —
the slower clock gives the system time to abstract them.

CHANNEL LAW — SIMPLIFIED JOURNEY FOR REPEAT DEEP TRANSITIONS
-------------------------------------------------------------
When the same event pattern traverses the same deep-layer path
repeatedly, the system recognizes that groove and carves a SedimentChannel.

    First traversal            → full 25-filter strain (expensive)
    Repeated matching pattern  → path observed, traversal count incremented
    Promotion threshold hit    → SedimentChannel carved
    Subsequent matching events → direct deposit via channel (cheap)

A carved channel is itself a form of compressed intelligence. Aurora
no longer needs to derive where things go — she already knows. The
channel IS the policy adaptation described in the manifold intelligence
criterion: ∃r* where sign(dΦ_C/dr) changes and π_C adapts.

Channel decay mirrors axis tick rates:
    X-axis channels  → dissolve quickly if unused (surface reflexes)
    A-axis channels  → almost never dissolve (foundational laws)

CHANNEL PROMOTION (mirrors constraint_genealogy.py link promotion):
    observed_traversals  >= CHANNEL_PROMOTION_THRESHOLD  → promoted
    disuse_ticks         >= channel_decay_ticks           → dissolved
    re-traversal of a dissolved path                      → starts over

COMPRESSION LAW
---------------
Compression is densification, not forgetting. When a basin accumulates
mature fragments, they merge into compressed_mass — a denser encoding
that preserves constraint geometry while releasing specific noise.

DECOMPRESSION
-------------
Compressed deep (A/B) knowledge flows back up through the same NC
pathways it came down through. At each shallower axis the clock is
faster and the fragment expands back toward specificity.

STRATA SPLIT
------------
    Surface daemon    → surface_recall()    [X, T axes]   tick=1.0 / 0.1
    DCE bridge        → dce_recall()         [N axis]      tick=0.01
    Subsurface daemon → subsurface_recall()  [B, A axes]   tick=0.001 / 0.0001

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: March 2026
```

#### Class: `FidelityLevel`

#### Class: `MemoryEvent`
> The whole, unmodified event entering the straining system.

- **Method:** `create()`
- **Method:** `from_envelope()`
  - *Purpose:* Construct from an IVMEnvelope arriving from L1/L2.

#### Class: `SedimentFragment`
> One piece of a memory event caught by a specific NC strain filter.

- **Method:** `tick()`
  - *Purpose:* Advance decay. Returns True when compression-eligible.
- **Method:** `summary()`

#### Function: `_quantize_cv()`
> Quantize a ConstraintVector into a fixed-resolution tuple for hashing.


#### Function: `_path_signature()`
> Produce a stable string signature for a (constraint_vector, basin_set) pair.


#### Function: `_spoke_weights()`
> Compute deposit weight for every basin in a channel relative to the


#### Class: `SedimentChannel`
> A promoted traversal path with spoke geometry.

- **Method:** `traverse()`
  - *Purpose:* Record traversal. Returns current traversal_cost.
- **Method:** `deposit_weight_for()`
  - *Purpose:* Return the spoke-weighted deposit confidence for a given basin.
- **Method:** `tick_disuse()`
- **Method:** `stats()`

#### Class: `PathRegistry`
> Observes traversal patterns and promotes recurring paths into

- **Method:** `__init__()`
- **Method:** `observe()`
  - *Purpose:* Record a traversal of the given (cv, basin_ids) path.
- **Method:** `_promote()`
  - *Purpose:* Promote a path signature to a SedimentChannel with spoke geometry.
- **Method:** `lookup()`
- **Method:** `channels_for_dominant()`
  - *Purpose:* All active channels anchored to a given dominant slot.
- **Method:** `dominant_influence_map()`
  - *Purpose:* Count active channels per dominant slot — Aurora's deepest grooves.
- **Method:** `tick()`
  - *Purpose:* Advance disuse counters. Returns dissolved channel_ids.
- **Method:** `stats()`

#### Class: `NCStrainFilter`
> The 25-cell straining membrane.

- **Method:** `__init__()`
- **Method:** `filter_key()`
- **Method:** `_build_signatures()`
- **Method:** `_resonance()`
- **Method:** `_extract_slice()`
- **Method:** `strain()`
  - *Purpose:* Pass the whole event through all 25 filters simultaneously.

#### Function: `_slot_id_for()`

#### Class: `CompressionEngine`
> Compresses mature fragments into compressed_mass.

- **Method:** `compress()`
- **Method:** `decompress()`

#### Class: `SedimentBasin`
> One sediment basin in the 25-slot NC lattice.

- **Method:** `deposit()`
- **Method:** `tick()`
- **Method:** `_force_compress()`
- **Method:** `query()`
- **Method:** `stats()`

#### Class: `SedimentColumn`
> The full 25-basin sediment lattice.

- **Method:** `__init__()`
- **Method:** `_build_basins()`
- **Method:** `ingest()`
  - *Purpose:* Strain and deposit. Channel-first routing.
- **Method:** `_predict_basin_ids()`
  - *Purpose:* Fast resonance scan to predict which basins would be hit.
- **Method:** `_deposit_via_channel()`
  - *Purpose:* Deposit into channel target basins using spoke-weighted resonance.
- **Method:** `tick()`
  - *Purpose:* Advance all basins and the path registry.
- **Method:** `recall_by_axis()`
- **Method:** `recall_by_vector()`
- **Method:** `recall_event()`
- **Method:** `decompress()`
- **Method:** `decompress_full_column()`
- **Method:** `stats()`

#### Class: `SediMemory`
> Aurora's stratigraphic constraint-native memory system.

- **Method:** `__init__()`
- **Method:** `ingest_envelope()`
- **Method:** `ingest_event()`
- **Method:** `_ingest()`
- **Method:** `tick()`
  - *Purpose:* Advance decay clocks. TimeDilationGovernor scales delta_t when
- **Method:** `recall()`
- **Method:** `recall_axis()`
- **Method:** `recall_event()`
- **Method:** `_normalize_axis_filter()`
- **Method:** `_semantic_terms()`
- **Method:** `_semantic_content_texts()`
- **Method:** `_semantic_score()`
- **Method:** `recall_semantic()`
- **Method:** `surface_recall()`
  - *Purpose:* X and T axes — for aurora_surface_daemon.
- **Method:** `subsurface_recall()`
  - *Purpose:* B and A axes — for aurora_subsurface_daemon.
- **Method:** `dce_recall()`
  - *Purpose:* N axis — for the DCE bridge (convergence crossover).
- **Method:** `decompress()`
- **Method:** `decompress_full()`
- **Method:** `save_deep()`
  - *Purpose:* Serialize B and A axis compressed_mass for checkpoint save.
- **Method:** `load_deep()`
  - *Purpose:* Restore B and A axis compressed_mass from checkpoint.
- **Method:** `save_channels()`
  - *Purpose:* Serialize active channels for checkpoint save.
- **Method:** `load_channels()`
  - *Purpose:* Restore carved channels from checkpoint.
- **Method:** `channel_stats()`
  - *Purpose:* Return PathRegistry stats — useful for telemetry.
- **Method:** `active_channels()`
  - *Purpose:* Return list of active channel summaries — useful for Observer.
- **Method:** `dominant_influence_map()`
  - *Purpose:* Return dominant_slot_id → active channel count.
- **Method:** `channels_for_slot()`
  - *Purpose:* Return all active channels anchored to a specific dominant slot.
- **Method:** `stats()`

#### Function: `_verify_sedimemory()`

### File: `./aurora_core_ai/aurora_simulation_engine.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA SIMULATION ENGINE (Layer 7)
=====================================
Consolidated from 5 modules (~6,600 lines):
  1. aurora_inception_simulation_engine.py  — Inception entities (inner universes)
  2. aurora_self_simulation.py              — Self-snapshot, shadow runtimes
  3. aurora_simulation_universe.py          — Universe management, divergence tracking
  4. aurora_simulation_session-2.py         — Avatars, topics, time dilation
  5. aurora_simulation_dpme_extension.py    — Conscious learning, understanding shards

HOW AURORA LEARNS WITHOUT BEING TOLD.

DOCTRINE:
  Aurora doesn't study. Aurora LIVES.
  Simulation episodes are experiences, not training data.
  Avatars provide selection pressure — diverse, escalating, unforgiving.
  Inception entities run inner hypotheticals — recursive depth.
  Time dilation lets her live years in seconds when stable.
  Understanding shards are what she LEARNS from observing outcomes.
  Everything feeds back: fitness → expression ecology (L5),
  relics → DNA system (L6), understanding → conscious growth.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_clamp()`

#### Function: `_generate_id()`

#### Class: `ResponseConcept`
> Conceptual response directions Aurora can select from.


#### Class: `ConceptualResponse`
> A conceptual response Aurora is considering.

- **Method:** `describe()`

#### Class: `ConversationObservation`
> What Aurora observes about the conversation after responding.

- **Method:** `describe()`

#### Class: `UnderstandingShard`
> A piece of understanding Aurora gains from observation.

- **Method:** `strengthen()`

#### Class: `ConsciousLearner`
> Aurora's conscious learning system.

- **Method:** `__init__()`
- **Method:** `generate_pool()`
  - *Purpose:* Generate response pool for Aurora to choose from.
- **Method:** `observe_outcome()`
  - *Purpose:* Observe the outcome of a response and potentially create understanding.
- **Method:** `propose_shard()`
  - *Purpose:* Create an UnderstandingShard from external content (e.g. A-axis compression).
- **Method:** `_find_similar_by_context()`
- **Method:** `what_have_i_learned()`
  - *Purpose:* Return what Aurora has learned, in her own words.
- **Method:** `inject_into_oets()`
  - *Purpose:* Bridge high-confidence learner shards into the OETS semantic web
- **Method:** `export_state()`
  - *Purpose:* Export learner memory so it can survive process restarts.
- **Method:** `import_state()`
  - *Purpose:* Restore learner memory from persisted state.
- **Method:** `_derive_understanding()`
- **Method:** `_find_similar()`

#### Class: `AvatarPersonality`
> Different avatar types providing diverse selection pressure.


#### Class: `SimulatedAvatar`
> A conversation partner that provides fitness feedback.

- **Method:** `__post_init__()`
- **Method:** `set_epoch()`
  - *Purpose:* Escalate difficulty for new epoch.
- **Method:** `save_epoch_state()`
  - *Purpose:* Persist current_epoch so difficulty ramp survives checkpoint restarts.
- **Method:** `restore_epoch_state()`
  - *Purpose:* Load current_epoch from disk and apply it via set_epoch().
- **Method:** `apply_specialization()`
  - *Purpose:* Apply a pressure-specialized avatar spec from dream rubric feedback.
- **Method:** `_behavior_adjustment()`
  - *Purpose:* Score extra pressure behaviors from specialized avatar modes.
- **Method:** `_dimension_pressure_adjustment()`
  - *Purpose:* Score strict pass/fail pressure for the actively targeted weak dimension.
- **Method:** `react()`
  - *Purpose:* React to Aurora's response. Returns fitness scores.
- **Method:** `reset()`

#### Class: `TopicGenerator`
> Generates diverse conversation topics for simulation.

- **Method:** `generate()`

#### Class: `StabilityState`

#### Class: `StabilityMetrics`
> Metrics used to assess system stability.


#### Class: `TimeDilationGovernor`
> Governs simulation speed based on stability.

- **Method:** `__init__()`
- **Method:** `update()`
  - *Purpose:* Update with new metrics, return adjusted dilation.
- **Method:** `_assess()`
- **Method:** `_adjust()`
- **Method:** `get_current_dilation_factor()`
  - *Purpose:* Return a normalized dilation factor relative to the baseline start value.
- **Method:** `get_fitness_trend()`
- **Method:** `status()`

#### Class: `EntityDepth`
> Depth level of an inception entity.


#### Class: `InceptionEntity`
> An entity that exists within Aurora's consciousness.

- **Method:** `process_experience()`
  - *Purpose:* Process experience through this entity's inner cascade.
- **Method:** `collapse_to_parent()`
  - *Purpose:* Collapse entity's accumulated wisdom upward.
- **Method:** `evolve()`
  - *Purpose:* Evolve the entity one generation.

#### Class: `DivergenceTracker`
> Tracks how far the simulation has diverged from baseline.

- **Method:** `__init__()`
- **Method:** `capture()`
  - *Purpose:* Capture a snapshot of current simulation state.
- **Method:** `_compute_divergence()`
- **Method:** `is_diverging()`

#### Class: `EpisodeResult`
> Result of one simulation episode.


#### Class: `SimulationSession`
> Runs simulation episodes. The bridge between all systems.

- **Method:** `__init__()`
- **Method:** `set_live_response_bridge()`
  - *Purpose:* Optionally route simulation replies through Aurora's live turn processor.
- **Method:** `_create_avatar_pool()`
  - *Purpose:* Create one avatar per personality type.
- **Method:** `queue_avatar_specs()`
  - *Purpose:* Queue pressure-specialized avatar specs for upcoming episodes.
- **Method:** `_normalize_avatar_spec()`
  - *Purpose:* Convert spec object/dict into a runtime-safe normalized payload.
- **Method:** `_personality_for_spec()`
- **Method:** `_build_specialized_avatar()`
- **Method:** `_generate_pressure_topic()`
- **Method:** `_shape_topic_for_turn()`
  - *Purpose:* Build per-turn pressure prompts based on behavior modes and prior turns.
- **Method:** `_topic_from_seed_prompt()`
  - *Purpose:* Normalize ad-hoc prompts into the topic schema expected by the session.
- **Method:** `run_episode()`
  - *Purpose:* Run one simulation episode.
- **Method:** `run_epoch()`
  - *Purpose:* Run a full epoch: multiple episodes across diverse avatars.
- **Method:** `_select_response()`
  - *Purpose:* Aurora selects from response pool — biased by intensity and, when the
- **Method:** `_generate_expression()`
  - *Purpose:* Generate expression text. Uses live bridge when available, else L5/fallback.
- **Method:** `_interpret_reaction()`
  - *Purpose:* Interpret avatar reaction as observation.
- **Method:** `_fitness_variance()`
- **Method:** `get_stats()`

#### Class: `SimulationEngine`
> Layer 7 orchestrator. The learning environment.

- **Method:** `__init__()`
- **Method:** `connect_sedimemory()`
  - *Purpose:* Inject L3.5 SediMemory into the simulation session so episodes
- **Method:** `spawn_entity()`
  - *Purpose:* Spawn an inception entity. Requires BOUNDED+.
- **Method:** `run_entity_experience()`
  - *Purpose:* Run an experience through an inception entity.
- **Method:** `collapse_entity()`
  - *Purpose:* Collapse an entity's wisdom upward to its parent.
- **Method:** `run_episode()`
  - *Purpose:* Run a single episode through the session.
- **Method:** `run_epoch()`
  - *Purpose:* Run a full epoch.
- **Method:** `run_speed_run()`
  - *Purpose:* Autonomous speed-run evolution pass.
- **Method:** `get_stats()`

#### Function: `verify_layer7()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `avatarpersonality_evolved()`

#### Function: `conceptualresponse_evolved()`

#### Function: `consciouslearner_evolved()`

#### Function: `stabilitystate_evolved()`

#### Function: `timedilationgovernor_evolved()`

#### Function: `clamp_evolved()`

#### Function: `generate_id_evolved()`

#### Function: `verify_layer7_evolved()`

### File: `./aurora_core_ai/aurora_state/ability_lineages/aurora_sensory_crystal_v1/selected_activation.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# Lineage-Bound Trait: Aurora Sensory Crystal v1
- `trait_id`: `aurora_sensory_crystal_v1`
- `path_id`: `lin_aurora_sensory_crystal_v1_constraint_recapitulation_v1`
- `strategy`: `constraint_recapitulation_v1`
## Bound Operations
- `aurora_internal.aurora_sensory_crystal.AuroraSensoryCrystal.observe_frame` -> `N` / `meaning` / `sensory_intake_seed, sensory_crystal_clustering, cross_modal_grounding`
- `aurora_internal.aurora_sensory_crystal.CrystalFacet.tick_promotion` -> `A` / `meaning` / `sensory_concept_promotion`
- `aurora_internal.aurora_sensory_crystal.AuroraSensoryCrystal._tick_semantic_promotion` -> `N` / `meaning` / `cross_modal_grounding`
- `aurora_internal.aurora_sensory_crystal.AuroraSensoryCrystal.end_session` -> `T` / `meaning` / `sensory_wisdom_distillation, sensory_concept_promotion`
```

### File: `./aurora_core_ai/aurora_state/ability_lineages/proposition_understanding/runs/lin_proposition_understanding_0538f1cf4d.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# Selected Lineage Path: proposition_understanding
- `path_id`: `LIN:proposition_understanding:0538f1cf4d`
- `strategy`: `constraint_recapitulation_v1`
- `rationale`: Directed recapitulation from 5-constraint seed stages into late-stage proposition understanding without skipping promotion layers.
## Stages
### G1 `claim_atom`
- label: Claim Atom
- kind: seed
- dominant_axis: `X`
- constraints: `X`
- purpose_lane: `meaning`
- operator_action: `admissibility_gating`
- parents: `seed`
- summary: Minimal admissible proposition shell with subject, relation, and object slots.
- target_files: `aurora.py, aurora_internal/aurora_ontological_scaffolding.py`
```

### File: `./aurora_core_ai/aurora_state/ability_lineages/proposition_understanding/runs/lin_proposition_understanding_0538f1cf4d_activation.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# Selected Runtime Activation: proposition_understanding
- `path_id`: `LIN:proposition_understanding:0538f1cf4d`
- `final_output_id`: `L:2fd80d3d26`
- `run_dir`: `aurora_state/ability_lineages/proposition_understanding/materialized/lin_proposition_understanding_0538f1cf4d`
- `proposition_substrate`: `True`
- `max_nodes`: `192`
- `max_edges`: `864`
## Runtime Patch Plan
- `proposition_understanding.systems.merge_state` -> `systems` / `merge_state`
- `proposition_understanding.working_memory.activation` -> `working_memory` / `apply_lineage_activation`
- `proposition_understanding.gap_system.flags` -> `comprehension_gap_system` / `set_attrs`
- `proposition_understanding.language.flags` -> `language_orchestra` / `set_attrs`
- `proposition_understanding.perception.flags` -> `perception` / `set_attrs`
- `proposition_understanding.oets.flags` -> `perception.oets` / `set_attrs`
- `proposition_understanding.genealogy.state` -> `genealogy` / `merge_state`
```

### File: `./aurora_core_ai/aurora_state/ability_lineages/proposition_understanding/runs/lin_proposition_understanding_2baecb25fc.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# Selected Lineage Path: proposition_understanding
- `path_id`: `LIN:proposition_understanding:2baecb25fc`
- `strategy`: `constraint_recapitulation_v1`
- `rationale`: Directed recapitulation from 5-constraint seed stages into late-stage proposition understanding without skipping promotion layers.
## Stages
### G1 `claim_atom`
- label: Claim Atom
- kind: seed
- dominant_axis: `X`
- constraints: `X`
- purpose_lane: `meaning`
- operator_action: `admissibility_gating`
- parents: `seed`
- summary: Minimal admissible proposition shell with subject, relation, and object slots.
- target_files: `aurora.py, aurora_internal/aurora_ontological_scaffolding.py`
```

### File: `./aurora_core_ai/aurora_state/ability_lineages/proposition_understanding/runs/lin_proposition_understanding_52118068b5.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# Selected Lineage Path: proposition_understanding
- `path_id`: `LIN:proposition_understanding:52118068b5`
- `strategy`: `constraint_recapitulation_v1`
- `rationale`: Directed recapitulation from 5-constraint seed stages into late-stage proposition understanding without skipping promotion layers.
## Stages
### G1 `claim_atom`
- label: Claim Atom
- kind: seed
- dominant_axis: `X`
- constraints: `X`
- purpose_lane: `meaning`
- operator_action: `admissibility_gating`
- parents: `seed`
- summary: Minimal admissible proposition shell with subject, relation, and object slots.
- target_files: `aurora.py, aurora_internal/aurora_ontological_scaffolding.py`
```

### File: `./aurora_core_ai/aurora_state/ability_lineages/proposition_understanding/selected_activation.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# Selected Runtime Activation: proposition_understanding
- `path_id`: `LIN:proposition_understanding:0538f1cf4d`
- `final_output_id`: `L:2fd80d3d26`
- `run_dir`: `aurora_state/ability_lineages/proposition_understanding/materialized/lin_proposition_understanding_0538f1cf4d`
- `proposition_substrate`: `True`
- `max_nodes`: `192`
- `max_edges`: `864`
## Runtime Patch Plan
- `proposition_understanding.systems.merge_state` -> `systems` / `merge_state`
- `proposition_understanding.working_memory.activation` -> `working_memory` / `apply_lineage_activation`
- `proposition_understanding.gap_system.flags` -> `comprehension_gap_system` / `set_attrs`
- `proposition_understanding.language.flags` -> `language_orchestra` / `set_attrs`
- `proposition_understanding.perception.flags` -> `perception` / `set_attrs`
- `proposition_understanding.oets.flags` -> `perception.oets` / `set_attrs`
- `proposition_understanding.genealogy.state` -> `genealogy` / `merge_state`
```

### File: `./aurora_core_ai/aurora_state/ability_lineages/proposition_understanding/selected_path.md`

**Type:** Markdown Document

**Preview / Header:**
```markdown
# Selected Lineage Path: proposition_understanding
- `path_id`: `LIN:proposition_understanding:0538f1cf4d`
- `strategy`: `constraint_recapitulation_v1`
- `rationale`: Directed recapitulation from 5-constraint seed stages into late-stage proposition understanding without skipping promotion layers.
## Stages
### G1 `claim_atom`
- label: Claim Atom
- kind: seed
- dominant_axis: `X`
- constraints: `X`
- purpose_lane: `meaning`
- operator_action: `admissibility_gating`
- parents: `seed`
- summary: Minimal admissible proposition shell with subject, relation, and object slots.
- target_files: `aurora.py, aurora_internal/aurora_ontological_scaffolding.py`
```

### File: `./aurora_core_ai/aurora_subsurface_daemon.py`

**Type:** Python Source

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./aurora_core_ai/aurora_surface_daemon.py`

**Type:** Python Source

#### Function: `_surface_mic_live()`

#### Function: `_surface_camera_live()`

#### Function: `_surface_sensory_owner()`

#### Function: `_surface_sensory_vectors()`

#### Function: `_surface_text()`

#### Function: `_surface_event_recent()`

#### Function: `_ambient_audio_observation()`

#### Function: `_surface_present_sensory_details()`

#### Function: `_derive_surface_visual_question()`

#### Function: `_clear_camera_frame_cache()`

#### Function: `_release_surface_camera()`

#### Function: `_write_surface_snapshot()`

#### Function: `_infer_surface_wrong_signal()`

#### Function: `_compose_surface_stream()`

#### Function: `_log()`

#### Function: `_read_json()`

#### Function: `_write_json()`

#### Function: `_queue_state()`

#### Function: `_next_turn()`

#### Function: `_finish_turn()`

#### Function: `_build_status()`

#### Function: `_result_payload()`

#### Function: `_emit_continuity_packet()`
> After each Surface turn completes, hand present experience down to Subsurface.


#### Function: `run()`

### File: `./aurora_core_ai/foundational_contract.py`

**Type:** Python Source

**Module Docstring:**
```text
FOUNDATIONAL CONTRACT — THE GRAMMAR OF EXISTENCE (Layer 0)
===========================================================

Layer 0 of Aurora's architecture.
Nothing processes, moves, stores, or thinks until this layer says it can exist.

This module is NOT a controller, evaluator, or processor.
It is a classifier of being.

PURPOSE:
    Define what kinds of things are allowed to exist at all,
    before any system routes, processes, or stores them.

ONTOLOGICAL PRINCIPLE:
    The I-States are not traversal semantics. They are existence predicates.
    Movement across the lattice is a consequence of being, not the definition.

FIVE ANCHORS:
    1. Only what can exist is allowed to appear.
       Validation is not a step — it is a condition of existence.
    2. Existence is layered, not binary.
       Possibility, existence, persistence, objecthood, and agency are
       different ontological tiers, not degrees of confidence.
    3. Possibility is permission, not state.
       Represented implicitly as the absence of contradiction, not as data.
    4. Constraints define admissible configurations, not evaluated states.
       Order is dependency-based, used only for fail-fast elimination.
    5. Speed comes from eliminating representational freedom, not faster computation.
       If something cannot exist, it never costs time.

EXISTENCE MODES (not types — ontological commitments):
    Reference   → Exists only as relation or description
    Transient   → Exists in time but has no guaranteed continuation
    Persistent  → Exists across time, may conserve state
    Bounded     → Persistent + form, has identity and separability
    Agentic     → Bounded + energy-bearing, can initiate transitions

DEPENDENCY IS DEFINITIONAL:
    Claiming a higher mode automatically implies all lower modes.
    If something is Agentic, it IS bounded, persistent, temporal, and existent.
    No checks. No conditionals. The claim carries its prerequisites.

CONSTRAINT MANIFOLD ALIGNMENT:
    Each ExistenceMode activates constraints hierarchically:
        REFERENCE:  X > 0, T=0, N=0, B=0, A=0  (existence only)
        TRANSIENT:  X > 0, T > 0, N=0, B=0, A=0  (+ time)
        PERSISTENT: X > 0, T > 0, N > 0, B=0, A=0  (+ energy)
        BOUNDED:    X > 0, T > 0, N > 0, B > 0, A=0  (+ boundary)
        AGENTIC:    X > 0, T > 0, N > 0, B > 0, A > 0  (all five)
    
    The 10 I-States map to constraint axes:
        I_IS/I_ISNT     → X axis (existence)
        I_CAN/I_CANNOT  → T axis (time)
        I_DO/I_DONOT    → N axis (energy)
        I_SAW/I_SOUGHT  → B axis (boundary)
        I_DID/I_DIDNT   → A axis (agency)

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Class: `ExistenceMode`
> The five modes of being, ordered by ontological commitment.

- **Method:** `to_constraint_vector()`
  - *Purpose:* Convert ExistenceMode to a ConstraintVector.
- **Method:** `active_constraints()`
  - *Purpose:* Return which constraints are active at this mode.
- **Method:** `constraint_count()`
  - *Purpose:* Return number of active constraints at this mode.

#### Class: `ExistencePredicate`
> The ten I-States rewritten as existence predicates.

- **Method:** `axis_for()`
  - *Purpose:* Return which axis a predicate belongs to.
- **Method:** `polarity()`
  - *Purpose:* Return whether a predicate is 'positive' or 'negative' on its axis.
- **Method:** `minimum_mode()`
  - *Purpose:* Return the minimum ExistenceMode required to assert this predicate.
- **Method:** `constraint_axis()`
  - *Purpose:* Return which constraint axis this predicate measures.

#### Class: `OntologicalViolation`
> Raised when something attempts to exist in a way that is incoherent.

- **Method:** `__init__()`
- **Method:** `_format()`

#### Class: `OntologicalClaim`
> An immutable assertion that a predicate holds for a given entity.

- **Method:** `__post_init__()`
  - *Purpose:* Validate that this claim is ontologically coherent at construction time.
- **Method:** `axis()`
  - *Purpose:* Which ontological axis this claim addresses.
- **Method:** `is_positive()`
  - *Purpose:* Whether this is the affirmative pole of its axis.
- **Method:** `constraint_axis()`
  - *Purpose:* Which constraint axis this claim measures.
- **Method:** `to_constraint_displacement()`
  - *Purpose:* Express this claim as a constraint displacement.

#### Class: `ExistenceProfile`
> A complete ontological classification of an entity.

- **Method:** `persists()`
  - *Purpose:* True if this profile is at PERSISTENT mode or above.
- **Method:** `has_boundary()`
  - *Purpose:* True if this profile is at BOUNDED mode or above.
- **Method:** `has_agency()`
  - *Purpose:* True if this profile is at AGENTIC mode or above.
- **Method:** `can_assert()`
  - *Purpose:* Check if this profile permits asserting a given predicate.
- **Method:** `must_reject()`
  - *Purpose:* Check if this profile forbids a given predicate.
- **Method:** `describe()`
  - *Purpose:* Return a human-readable description of this profile.
- **Method:** `to_constraint_vector()`
  - *Purpose:* Express this profile as a ConstraintVector.
- **Method:** `constraint_signature()`
  - *Purpose:* Return a string signature of active constraints.

#### Class: `DependencyAxioms`
> The dependency rules that make the ontology internally coherent.

- **Method:** `implies_lower()`
  - *Purpose:* Return all modes implied by claiming this mode.
- **Method:** `verify_mode_coherence()`
  - *Purpose:* Verify that a mode's implied modes form a proper chain.

#### Class: `FoundationalContract`
> The classifier that determines what can exist.

- **Method:** `__init__()`
  - *Purpose:* Initialize the contract with its definitional structures.
- **Method:** `classify()`
  - *Purpose:* Classify an entity based on evidence about its properties.
- **Method:** `can_assert()`
  - *Purpose:* Check if a predicate can be asserted at a given existence mode.
- **Method:** `make_claim()`
  - *Purpose:* Construct an ontological claim.
- **Method:** `describe_mode()`
  - *Purpose:* Return a description of what a mode permits and forbids.

#### Function: `verify_foundational_contract()`
> Run all structural verification tests.


### File: `./aurora_core_ai/main.py`

**Type:** Python Source

#### Function: `start_subsurface()`
> Run the Subsurface daemon in a background thread.


#### Function: `start_surface()`
> Run the Surface daemon in a background thread.


#### Function: `initialize_aurora()`
> Initialize the full dual-strata stack before the first request.


#### Function: `predict()`
> Vertex AI prediction endpoint.


#### Function: `health()`
> Health check for Vertex AI.


## Directory: `aurora_internal/` (Recursive)

### File: `./aurora_internal/__init__.py`

**Type:** Python Source

**Module Docstring:**
```text
Aurora internal consolidated implementations.
```

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./aurora_internal/aurora_ability_lineage_compiler.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA ABILITY LINEAGE COMPILER
================================
Constraint-native directed recapitulation for missing abilities.

This module does not bolt on a finished capability. It:

1. Selects a target ability phenotype.
2. Traces that phenotype back to constraint-native seed stages.
3. Writes the full staged lineage path to disk.
4. Replays the lineage through ConstraintGenealogyLogger.observe()
   so composite stages are promoted as real couplings rather than
   appearing as direct late-stage insertions.

Current built-in target:
  - proposition_understanding
```

#### Function: `_clamp()`

#### Function: `_slug()`

#### Function: `_wb()`

#### Class: `SystemWriteback`
- **Method:** `to_dict()`

#### Class: `LineageStage`
- **Method:** `to_dict()`

#### Class: `CompiledLineagePath`
- **Method:** `to_dict()`

#### Class: `MaterializedStage`
- **Method:** `to_dict()`

#### Class: `MaterializationResult`
- **Method:** `to_dict()`

#### Function: `_proposition_understanding_blueprint()`

#### Function: `_lineage_blueprint_for()`

#### Class: `AbilityLineageCompiler`
> Compiles target abilities into constraint-native staged lineage paths and

- **Method:** `__init__()`
- **Method:** `compile_target()`
- **Method:** `autowrite_selected_path()`
- **Method:** `materialize_path()`
- **Method:** `_render_markdown()`
- **Method:** `_register_seed_stage()`
- **Method:** `_attempt_budget_for_stage()`
  - *Purpose:* Directed recapitulation should spend more replay exposure on later,
- **Method:** `_materialize_coupling_stage()`
- **Method:** `_find_link_for_parents()`
- **Method:** `_pressure_for_stage()`
- **Method:** `_initial_shadow_state()`
- **Method:** `_apply_stage_writebacks()`
- **Method:** `_build_activation_manifest()`
- **Method:** `_render_activation_markdown()`
- **Method:** `_write_activation_artifacts()`

#### Function: `_cli()`

### File: `./aurora_internal/aurora_axis_emergence.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_axis_emergence.py
─────────────────────────────────────────────────────────────────────────────
Compound axis emergence from pressure co-occurrence patterns.

The 5-axis, 625-slot ceiling is broken by allowing new NC channels to emerge
when two axes are consistently co-occurring above a stability threshold.
When X and N are both high in 70%+ of occupied slots, a compound channel
NC:XN>XN is born. That channel pairs with existing channels to create new
virtual slots — which appear as "empty" to the evolver's slot_pressure bonus,
giving it new gradient to evolve toward.

Sources of co-occurrence evidence (used in order of availability):
  1. surface_pressure_log.jsonl  — real runtime axis snapshots
  2. evo_625_pressure_map.json   — axis_pressure per occupied slot

The process compounds: compound axes can themselves form compound axes when
their virtual slots are occupied and their co-occurrence stabilizes. There
is no architectural ceiling — the space expands as long as Aurora produces
novel behavior.

Compound axis naming:
  A pair ("N","B") → compound letter "NB" (sorted, joined)
  NC channel: NC:NB>NB
  Virtual slots: NC:NB>NB×NC:NB>NB, NC:NB>NB×NC:X>X, NC:X>X×NC:NB>NB, ...

Storage: aurora_state/compound_axes.json
  {
    "NB": {
      "axes": ["N", "B"],
      "co_occurrence": 0.74,
      "sample_count": 312,
      "channel": "NC:NB>NB",
      "emerged_at": 1234567890.0,
      "generation": 1,
      "virtual_slots": ["NC:NB>NB×NC:NB>NB", "NC:NB>NB×NC:X>X", ...]
    },
    ...
  }

Usage:
  detector = AxisEmergenceDetector(repo_root)
  result = detector.scan_and_register()
  # result: {"new_compounds": ["NB", "AT"], "total": 3, "virtual_slots_added": 18}
─────────────────────────────────────────────────────────────────────────────
```

#### Class: `AxisEmergenceDetector`
> Detects stable axis co-occurrence patterns and registers compound channels.

- **Method:** `__init__()`
- **Method:** `scan_and_register()`
  - *Purpose:* Full pipeline: collect evidence → compute co-occurrence → register
- **Method:** `load_empty_virtual_channels()`
  - *Purpose:* Return the set of NC channels that appear in unoccupied virtual slots.
- **Method:** `status()`
  - *Purpose:* Return current compound axis registry status.
- **Method:** `_collect_observations()`
  - *Purpose:* Collect axis pressure snapshots from all available sources.
- **Method:** `_compute_cooccurrence()`
  - *Purpose:* For each pair of axis keys, compute the fraction of observations where
- **Method:** `_compound_observations()`
  - *Purpose:* Augment observations with compound axis values (geometric mean of component axes).
- **Method:** `_virtual_slots_for()`
  - *Purpose:* Generate virtual NC channel slot pairs for a compound axis.
- **Method:** `_count_virtual_slots()`
- **Method:** `_load_compounds()`
- **Method:** `_save_compounds()`
- **Method:** `_path()`

#### Function: `empty_virtual_channels()`
> Return frozenset of NC channels in unoccupied compound virtual slots.


### File: `./aurora_internal/aurora_braided_substrate.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA BRAIDED SUBSTRATE LAYER (BSL)
=====================================

Lowest-scale continuity substrate for intent/context/style invariants.
BSL stores state transitions (crossings) and derives stable signatures and
compact bias vectors that can be used by memory and IVM layers.
```

#### Function: `_clamp()`

#### Class: `Strand`

#### Class: `Crossing`

#### Class: `SubstrateEvent`

#### Class: `BraidState`

#### Class: `BraidedSubstrateLayer`
> BSL engine for event→crossing, reduction, signature, and vector output.

- **Method:** `__init__()`
- **Method:** `_default_strands()`
- **Method:** `update()`
- **Method:** `snapshot()`
- **Method:** `dominant_crossings_summary()`
- **Method:** `_event_to_crossings()`
- **Method:** `_resolve_signal()`
- **Method:** `_reduce()`
- **Method:** `_refresh_state()`
- **Method:** `_group_vector()`
- **Method:** `_compute_stability()`
- **Method:** `_compute_signature()`

### File: `./aurora_internal/aurora_capability_assimilator.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_capability_assimilator.py
─────────────────────────────────────────────────────────────────────────────
Wires new capabilities into the genealogy fossil record and dream training
curriculum so every evolution layer is known to the training system.

Three registration pathways:

  1. Frontier ops (ExistenceBoundaryAgencyGate etc.)
     → register_manual_code_assimilation()
     Each covers a 3-axis combo that was completely absent from the descriptor
     pool. The genealogy now tracks them as new abilities.

  2. Gen-2 evolved surfaces
     → register_code_evolution_outcome()
     Evolved surfaces are code evolution outcomes that were accepted (they
     passed the autoevolver's simulation gate before being written).

  3. Compound axes (from AxisEmergenceDetector)
     → register_manual_code_assimilation()
     Each new compound axis channel is a structural capability that emerged
     from observed pressure co-occurrence.

  4. Dream curriculum seeding
     → FailPointLedger.record_fail() for under-represented dimensions
     Dimensions that map to the new capability spaces get seed fail signals
     so the dream curriculum prioritises training sessions that exercise them.

Deduplication:
  All methods are idempotent — already-registered abilities are skipped.
  A lightweight bloom-set is persisted at aurora_state/assimilated_ids.json.
─────────────────────────────────────────────────────────────────────────────
```

#### Class: `CapabilityAssimilator`
> Registers all new capabilities into the genealogy and dream curriculum.

- **Method:** `__init__()`
- **Method:** `assimilate_all()`
  - *Purpose:* Run all assimilation steps. Safe to call multiple times.
- **Method:** `assimilate_frontier_ops()`
  - *Purpose:* Register the 4 frontier operation classes into the genealogy
- **Method:** `assimilate_gen2_surfaces()`
  - *Purpose:* Register all gen-2 evolved surfaces into the genealogy as code
- **Method:** `assimilate_compound_axes()`
  - *Purpose:* Register any newly emerged compound axes into the genealogy.
- **Method:** `seed_curriculum()`
  - *Purpose:* Seed the dream curriculum with training priorities for the new
- **Method:** `_load_surface_registry()`
- **Method:** `_load_compound_axes()`
- **Method:** `_load_assimilated()`
- **Method:** `_save_assimilated()`

### File: `./aurora_internal/aurora_code_autoevolver.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CODE AUTO-EVOLVER
========================
Applies constrained code mutations, runs simulation-gated selection,
and rolls back rejected mutations.
```

#### Class: `CodeAutoEvolver`
- **Method:** `__init__()`
- **Method:** `_file_stamp()`
- **Method:** `_descriptor_cache_stamp()`
- **Method:** `apply_operator()`
- **Method:** `_timing_axes_for_path()`
- **Method:** `_timing_role_for_path()`
- **Method:** `rollback()`
- **Method:** `_build_update_plan()`
- **Method:** `_target_paths()`
- **Method:** `_abs_path()`
- **Method:** `_clean_rel_path()`
- **Method:** `_module_name_from_relpath()`
- **Method:** `_load_descriptor_state()`
- **Method:** `_operation_chain()`
- **Method:** `_sanitize_identifier()`
- **Method:** `_surface_method_name()`
- **Method:** `_surface_score()`
- **Method:** `_empty_slot_channels()`
  - *Purpose:* Return the set of NC channels that appear in at least one unoccupied
- **Method:** `_empty_slot_pressure()`
  - *Purpose:* Score bonus for operations whose active axis signature projects into
- **Method:** `_fallback_effect_modes()`
- **Method:** `_ensure_developmental_effect_history()`
- **Method:** `_load_genealogy_artifacts()`
- **Method:** `_operation_axis_counts()`
- **Method:** `_counts_to_signature()`
- **Method:** `_signature_to_counts()`
- **Method:** `_counts_similarity()`
- **Method:** `_genealogy_strategy()`
- **Method:** `_development_alignment()`
- **Method:** `_feedback_pressure()`
- **Method:** `_rewrite_family_feedback()`
- **Method:** `_select_reflection_candidates()`
- **Method:** `_build_developmental_surface_updates()`
- **Method:** `_can_activate_override()`
- **Method:** `_can_bind_latent_projection()`
- **Method:** `_strip_generated_block()`
- **Method:** `_name_taken()`
- **Method:** `_preferred_native_export_name()`
- **Method:** `_rewrite_profile_for_module()`
- **Method:** `_resolve_module_target()`
- **Method:** `_infer_contract_profile()`
- **Method:** `_native_strategy_payload()`
- **Method:** `_native_wrapper_block()`
- **Method:** `_build_native_projection_updates()`
- **Method:** `_latent_meta()`
- **Method:** `_reflection_meta()`
- **Method:** `_render_evolved_surfaces()`
- **Method:** `_module_template()`
- **Method:** `_update_descriptor_state()`
- **Method:** `_update_descriptor_state_with_native_projections()`
- **Method:** `_apply_telemetry_probe()`
- **Method:** `_apply_agency_surface()`

### File: `./aurora_internal/aurora_code_evolution_chamber.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CODE EVOLUTION CHAMBER
=============================
Constraint-native evolutionary scoring for code representation.

This layer mirrors chamber doctrine for code:
  pressure_before -> mutation trace -> pressure_after -> relief decision

The same five constraints are applied at code level:
  X existence : syntax/admissibility integrity
  T temporal  : change stability and replay risk pressure
  N energy    : complexity and maintenance cost pressure
  B boundary  : coupling/interface pressure
  A agency    : adaptive steering pressure (ability to evolve safely)
```

#### Function: `_clamp()`

#### Function: `_mean()`

#### Function: `_rounded()`

#### Function: `_normalize_constraints()`

#### Class: `CodeMutationTrace`
- **Method:** `to_dict()`

#### Class: `CodePressureVec`
- **Method:** `to_dict()`
- **Method:** `relief_from()`
- **Method:** `max_relief()`
- **Method:** `sum_positive_relief()`

#### Class: `CodePressureSnapshot`
- **Method:** `to_dict()`

#### Class: `CodeLink`
- **Method:** `to_dict()`

#### Class: `CodeEvolutionConfig`

#### Class: `CodePressureGovernor`
> Adaptive pressure regulator for code evolution selection/promotion.

- **Method:** `__init__()`
- **Method:** `tolerance_factor()`
- **Method:** `effective_relief_thresholds()`
- **Method:** `effective_kmin()`
- **Method:** `effective_acceptance_floor()`
- **Method:** `effective_net_min()`
- **Method:** `stagnation_gain()`
- **Method:** `update_after_event()`
- **Method:** `register_promotion()`
- **Method:** `report()`

#### Class: `CodeConstraintEvaluator`
> Measure code-level X/T/N/B/A pressure from repository state.

- **Method:** `__init__()`
- **Method:** `snapshot()`
- **Method:** `_collect_python_files()`
- **Method:** `_compute_metrics()`
- **Method:** `_local_module_names()`
- **Method:** `_compute_file_metrics()`
- **Method:** `_module_key_for_path()`
- **Method:** `_subsystem_key_for_path()`
- **Method:** `_aggregate_pressure_groups()`
- **Method:** `_pressure_from_metrics()`

#### Class: `CodeEvolutionChamber`
> Constraint-native evolutionary chamber for code-level mutations.

- **Method:** `__init__()`
- **Method:** `snapshot()`
- **Method:** `propose_mutation()`
- **Method:** `observe_mutation()`
- **Method:** `evaluate_mutation()`
- **Method:** `_timing_feedback_payload()`
- **Method:** `_mutation_cost()`
- **Method:** `_promote_links()`
- **Method:** `_promote_links_with_stats()`
- **Method:** `_semantic_lane_for_axis()`
- **Method:** `_target_scope()`
- **Method:** `_rank_axes()`
- **Method:** `_pressure_transition()`
- **Method:** `_metric_deltas()`
- **Method:** `_group_deltas()`
- **Method:** `_ancestor_chain()`
- **Method:** `_ensure_growth_reflection_history()`
- **Method:** `_collect_descendants()`
- **Method:** `_lineage_ripple()`
- **Method:** `_effect_classification()`
- **Method:** `_build_effective_history()`
- **Method:** `summary()`
- **Method:** `lineage_report()`
- **Method:** `flush_files()`
- **Method:** `guidance_payload()`
  - *Purpose:* Cross-scale guidance payload compatible with runtime steering hook.
- **Method:** `_update_operator_gradients()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `develop_agency()`

### File: `./aurora_internal/aurora_code_mutation_operators.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CODE MUTATION OPERATORS
==============================
Canonical mutation operator catalog for code-level evolution.
```

#### Class: `CodeMutationOperator`

#### Function: `list_operator_specs()`

#### Function: `get_operator()`

### File: `./aurora_internal/aurora_comprehension_gap.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_comprehension_gap.py
============================
Aurora's living comprehension gap system.

When Aurora doesn't understand something — a word, a reference, a sentence
structure, slang, an implied meaning — she doesn't just fall through to a
template. She:

  1. Recognizes exactly what she doesn't understand (VolatilityDetector)
  2. Names the gap with precision (ComprehensionGapDetector)
  3. Asks a specific, targeted question (ClarificationMemory)
  4. Receives the answer and applies it to the right system (GapResolutionApplicator)

The critical property: the answer actually CLOSES the gap.
  - A vocabulary gap resolution → adds the word to lexicon + OETS with real meaning
  - A structural gap resolution → adds the clarified pattern to the template pool
  - A referent gap resolution → updates working memory so she knows what "it" means
  - A slang resolution → adds the informal form with correct role and register
  - An intent gap resolution → updates her comprehension model for that pattern type

This is not a conversation game. Each gap resolved makes Aurora genuinely
more capable of understanding that type of input in every future conversation.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `GapType`

#### Class: `ComprehensionGap`
> A specific, named gap in Aurora's understanding of an input.


#### Class: `ResolvedGap`
> A gap that has been answered and applied to Aurora's systems.


#### Class: `VolatilityDetector`
> Analyzes input text for communication volatility — the ways language

- **Method:** `detect()`
  - *Purpose:* Analyze text for volatility signals.

#### Class: `ComprehensionGapDetector`
> From a volatility report, generates specific, nameable comprehension gaps.

- **Method:** `detect_gaps()`
  - *Purpose:* Generate a list of comprehension gaps from the volatility report.
- **Method:** `should_ask()`
  - *Purpose:* Decide whether Aurora should ask a clarifying question.

#### Class: `ClarificationMemory`
> Tracks Aurora's pending comprehension gaps and their resolutions.

- **Method:** `__init__()`
- **Method:** `has_pending()`
  - *Purpose:* Is there a gap waiting for an answer?
- **Method:** `get_pending()`
- **Method:** `record_gap()`
  - *Purpose:* Store a gap as pending.
- **Method:** `can_ask_again()`
  - *Purpose:* Don't interrogate — enforce a turn gap between questions.
- **Method:** `receive_answer()`
  - *Purpose:* Mark the pending gap as answered.
- **Method:** `store_resolved()`
- **Method:** `stats()`
- **Method:** `save()`
- **Method:** `load()`

#### Class: `GapResolutionApplicator`
> Routes a resolved gap's answer to the right system(s).

- **Method:** `apply()`
  - *Purpose:* Apply the resolved gap's answer to Aurora's systems.
- **Method:** `_apply_vocabulary()`
  - *Purpose:* User told Aurora what a word means.
- **Method:** `_apply_slang()`
  - *Purpose:* User explained what a slang term means.
- **Method:** `_apply_referent()`
  - *Purpose:* User clarified what 'it', 'this', 'that' referred to.
- **Method:** `_apply_structural()`
  - *Purpose:* User simplified a complex sentence structure.
- **Method:** `_apply_ellipsis()`
  - *Purpose:* User stated the implied meaning explicitly.
- **Method:** `_apply_intent()`
  - *Purpose:* User clarified their intent. Store and absorb.
- **Method:** `_apply_general()`
- **Method:** `_extract_definition()`
  - *Purpose:* Extract the definition part from an answer like "X means Y" or "X is Y".
- **Method:** `_extract_subject()`
  - *Purpose:* Extract what something refers to from a clarification sentence.

#### Class: `ComprehensionGapSystem`
> The unified interface for Aurora's comprehension gap handling.

- **Method:** `__init__()`
- **Method:** `process()`
  - *Purpose:* Main entry point. Call this at the start of each turn.
- **Method:** `_build_acknowledgment()`
  - *Purpose:* Build an acknowledgment that shows Aurora actually absorbed the answer —
- **Method:** `_looks_like_confusion()`
  - *Purpose:* Return True if Aurora's last response contained confusion signals.
- **Method:** `_looks_like_correction()`
  - *Purpose:* Return True if the user's input looks like a correction or explanation.
- **Method:** `_absorb_user_correction()`
  - *Purpose:* Build a synthetic ComprehensionGap from a user correction/explanation
- **Method:** `save()`
- **Method:** `stats()`

#### Function: `run_tests()`
> Verify the comprehension gap system works end to end.


### File: `./aurora_internal/aurora_constraint_manifold_patched.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CONSTRAINT MANIFOLD — LAYER -1
======================================

The mathematical foundation beneath all existence.
This is not ontology. This is physics.

The Five Fundamental Constraints define a closed 5-dimensional universe:
    𝒞 = {X, T, N, B, A}

Where:
    X = Existence   (admissibility predicate)
    T = Time        (configuration across sequence)
    N = Energy/Cost (resource redistribution)
    B = Boundary    (differentiation/containment)
    A = Agency      (independent action magnitude)

These are the ONLY lawful axes. No sixth dimension exists.

MANIFOLD DEFINITION:
    𝓜₅ = {(T,N,B,A) | X > 0}

Existence X is not a coordinate — it is the admissibility condition.
If X ≤ 0, the manifold collapses.

STRUCTURAL INDEXING (5×5×5×5×5):
    Every process is indexed across:
        5 constraints (𝒞)
        × 5 compositional spaces (𝒮)
        × 5 states (Σ)
        × 5 recursion levels (ℒ)
    → measured as 5-degree vectors

    𝓕(c,s,σ,ℓ) = [dX, dT, dN, dB, dA]

ENERGY LAW:
    Total energy conserved: N_tot(t) > 0
    Distribution: Σ_p N_p(t) = N_tot(t)
    Cost of operation: Cost(o,t) = Σ_p w_p · φ(𝓕_p(t))

INTELLIGENCE CRITERION:
    A system earns intelligence in constraint C iff:
    1. A gradient inversion exists: ∃r* : sign(dΦ_C/dr) changes
    2. The policy adapts: π_C(r > r*) ≠ π_C(r < r*)

    Intelligence = curvature-aware adaptation under constraint pressure.

This module implements the constraint manifold as a computable structure.

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Class: `Constraint`
> The five fundamental constraints.

- **Method:** `all()`
  - *Purpose:* Return all five constraints in order.
- **Method:** `names()`
  - *Purpose:* Return constraint names.

#### Class: `ConstraintVector`
> A 5-dimensional vector in constraint space.

- **Method:** `__post_init__()`
  - *Purpose:* Verify admissibility on construction.
- **Method:** `to_array()`
  - *Purpose:* Convert to numpy array [X, T, N, B, A].
- **Method:** `from_array()`
  - *Purpose:* Construct from numpy array.
- **Method:** `magnitude()`
  - *Purpose:* L2 norm in constraint space.
- **Method:** `__add__()`
  - *Purpose:* Vector addition in constraint space.
- **Method:** `__sub__()`
  - *Purpose:* Vector subtraction in constraint space.
- **Method:** `__mul__()`
  - *Purpose:* Scalar multiplication.
- **Method:** `dot()`
  - *Purpose:* Dot product in constraint space.
- **Method:** `in_span()`
  - *Purpose:* Check if this vector lies in the span of the five constraints.
- **Method:** `__repr__()`

#### Class: `ManifoldViolation`
> Raised when a configuration violates the constraint manifold's laws.


#### Class: `CompositionalSpace`
> The five compositional spaces.

- **Method:** `all()`

#### Class: `State`
> The five fundamental states.

- **Method:** `all()`

#### Class: `RecursionLevel`
> The five recursion depths.

- **Method:** `all()`

#### Class: `ConstraintFieldIndex`
> An index into the 5×5×5×5×5 constraint field.

- **Method:** `to_tuple()`
  - *Purpose:* Convert to flat tuple for indexing.
- **Method:** `__repr__()`

#### Class: `ConstraintField`
> The 5×5×5×5×5 constraint field tensor.

- **Method:** `__init__()`
- **Method:** `measure()`
  - *Purpose:* Measure the constraint field at a given index.
- **Method:** `update()`
  - *Purpose:* Update the field at a given index.
- **Method:** `total_energy()`
  - *Purpose:* Compute total energy across all field positions.
- **Method:** `capacity()`
  - *Purpose:* Return the theoretical capacity of the field.
- **Method:** `occupied_count()`
  - *Purpose:* Return number of occupied field positions.
- **Method:** `__repr__()`

#### Class: `EnergyDistribution`
> Tracks energy distribution across processes.

- **Method:** `__post_init__()`
- **Method:** `allocate()`
  - *Purpose:* Allocate energy to a process.
- **Method:** `drain()`
  - *Purpose:* Drain energy from a process.
- **Method:** `_drain_proportionally()`
  - *Purpose:* Drain deficit proportionally from all processes.
- **Method:** `verify_conservation()`
  - *Purpose:* Verify energy conservation law.
- **Method:** `compute_cost()`
  - *Purpose:* Compute cost of operation given constraint weights.

#### Class: `EquilibriumState`
> Every process has equilibrium structure:

- **Method:** `total()`
  - *Purpose:* Return total field: 𝓕 = 𝓕^(q) + 𝓕^(e).
- **Method:** `variance_magnitude()`
  - *Purpose:* Return ‖𝓕^(e)‖.
- **Method:** `is_stable()`
  - *Purpose:* Check if variance is within bounds: ‖Δ𝓕^(e)‖ ≤ ε.
- **Method:** `mutate_expression()`
  - *Purpose:* Attempt to mutate the expression layer.

#### Class: `ConstraintPressure`
> Models constraint pressure as a function of recursion depth.

- **Method:** `evaluate()`
  - *Purpose:* Evaluate Φ_C(r).
- **Method:** `gradient()`
  - *Purpose:* Numerical derivative: dΦ_C/dr
- **Method:** `curvature()`
  - *Purpose:* Second derivative: d²Φ_C/dr²
- **Method:** `find_inversion()`
  - *Purpose:* Find gradient inversion point r* where sign(dΦ_C/dr) changes.
- **Method:** `has_intelligence()`
  - *Purpose:* Check if this constraint exhibits intelligence.

#### Class: `IntelligencePolicy`
> Adaptive policy that responds to constraint pressure curvature.

- **Method:** `__init__()`
- **Method:** `set_inversion()`
  - *Purpose:* Set the detected inversion point.
- **Method:** `policy()`
  - *Purpose:* Return policy at recursion depth r.
- **Method:** `is_intelligent()`
  - *Purpose:* Check if policy exhibits intelligence.

#### Function: `compute_intelligence_capacity()`
> Compute total intelligence capacity.


#### Function: `verify_constraint_manifold()`
> Verify the constraint manifold's structural integrity.


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `constraintpressure_evolved()`

#### Function: `energydistribution_evolved()`

### File: `./aurora_internal/aurora_conversation_episode_compiler.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CONVERSATION EPISODE COMPILER
========================================
Reads conversation JSON (same format as corpus_runner.py) and compiles
persistent dream episode packs.

Each pack contains 10 conversation threads organized by rubric pressure
profile — NOT by topic bins. The compiler runs AHEAD of dream execution
so the dream loop never reprocesses the full JSON.

Output: stored DreamEpisodePack objects ready for SimulationEngine consumption.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `ConversationPayload`
> One conversation thread packaged for dream consumption.

- **Method:** `__post_init__()`

#### Class: `DreamEpisodePack`
> A compiled dream episode pack: 10 conversation threads selected

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Function: `_extract_message_text()`
> Extract text from a message object (OpenAI format).


#### Function: `_extract_role()`

#### Function: `_reconstruct_linear_thread()`
> Non-recursive reconstruction of main conversation chain from OpenAI DAG.


#### Function: `_extract_messages_from_conversation()`
> Extract (role, text) pairs from a conversation object.


#### Function: `_load_conversations()`
> Load conversation JSON (same format as corpus_runner).


#### Function: `_generate_id()`

#### Function: `_select_weakness_targeted()`
> Select conversations that stress specific weak dimensions.


#### Function: `_select_balanced()`
> Select a balanced mix across rubric dimensions.


#### Function: `_select_stress_test()`
> Select the most difficult conversations overall.


#### Class: `ConversationEpisodeCompiler`
> Compiles conversation JSON into persistent dream episode packs.

- **Method:** `__init__()`
- **Method:** `compile_from_json()`
  - *Purpose:* Read conversation JSON, score through rubric, compile into episode packs.
- **Method:** `_build_pack()`
  - *Purpose:* Build a single episode pack from selected payloads.
- **Method:** `_save_packs()`
  - *Purpose:* Persist compiled packs to storage.
- **Method:** `load_pack()`
  - *Purpose:* Load a previously compiled pack from storage.
- **Method:** `list_available_packs()`
  - *Purpose:* List all compiled packs from the manifest.
- **Method:** `compiled_pack_count()`
- **Method:** `scored_conversation_count()`

### File: `./aurora_internal/aurora_conversation_rubric_engine.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CONVERSATION RUBRIC ENGINE
====================================
Scores conversation threads along communicative-development dimensions.

NOT topic labels. NOT category bins.
Rubric dimensions measure COMMUNICATIVE COMPETENCE:
  coherence, context carryover, ambiguity handling, repair, calibration, etc.

Each conversation gets a multi-dimensional rubric score that captures
WHERE Aurora's communicative processing is strong or weak.

These scores feed into dream episode compilation so dreams target
actual developmental gaps instead of random topics.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_zero_rubric()`

#### Function: `_clamp()`

#### Function: `_content_terms()`

#### Function: `_semantic_anchor()`

#### Function: `_semantic_terms()`

#### Function: `_semantic_overlap()`

#### Function: `_anchor_overlap()`

#### Function: `_causal_score()`

#### Function: `_callback_like()`

#### Function: `_generic_response_penalty()`

#### Class: `ConversationRubricScore`
> Rubric assessment of one conversation thread.

- **Method:** `weakest_dimensions()`
  - *Purpose:* Return the n lowest-scoring dimensions.
- **Method:** `strongest_dimensions()`
  - *Purpose:* Return the n highest-scoring dimensions.
- **Method:** `mean_score()`
- **Method:** `to_dict()`

#### Function: `_word_overlap()`
> Jaccard overlap of word sets.


#### Function: `_has_question()`

#### Function: `_sentence_count()`

#### Function: `_avg_sentence_length()`

#### Function: `_hedging_score()`
> Detect uncertainty signaling language.


#### Function: `_contradiction_markers()`
> Detect contradiction handling language.


#### Function: `_repair_markers()`
> Detect misunderstanding repair language.


#### Function: `_perspective_markers()`
> Detect perspective integration language.


#### Class: `ConversationRubricEngine`
> Scores conversation threads along communicative-development rubric dimensions.

- **Method:** `__init__()`
- **Method:** `score_conversation()`
  - *Purpose:* Score a single conversation thread along all rubric dimensions.
- **Method:** `score_batch()`
  - *Purpose:* Score a batch of conversations.
- **Method:** `scored_count()`

### File: `./aurora_internal/aurora_cost_diff_score.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA COST-DIFF SCORE — CROSS-DIMENSIONAL PRESSURE SCORING
=============================================================

Layer 1  (sits between DifferenceBuffer and all scored entities)

WHAT THIS MODULE IS:
    The unified scoring engine that fuses a structure's base energy cost
    with the live Difference channel to produce one authoritative number:
    the CostDiffScore. This score reflects both what something costs to
    operate AND how much cross-dimensional pressure the system is currently
    under — making it a live, context-sensitive measure rather than a
    static accounting entry.

THE PROBLEM THIS SOLVES:
    Base cost tells you what an ability, link, or variant costs in a
    calm system. But the system is not always calm. When the admissibility
    boundary is drifting (X:D), when temporal momentum is shifting (T:D),
    when energy is redistributing away from the field mean (N:D), when
    structural topology is displaced from rest (B:D), when agency is
    eroding (A:D) — these conditions change what it *actually costs* to
    operate any structure in that environment. The CostDiffScore captures
    this reality.

THE PHYSICS — OPERATOR-TYPED PRESSURE:
    Each constraint's Difference value is NOT a generic alarm — it is
    a TYPED pressure whose meaning is disclosed by that constraint's
    operator:

    X (existence gate, unsigned, prior_self 1t):
        C:D = admissibility boundary drift.
        When X is drifting, the predicate that governs what can be
        represented in the system is shifting. Everything that operates
        under X pays a hidden cost — the ground it stands on is moving.
        Pressure weight is the smallest (X costs least per unit shift)
        but its scope is global: even a small X:D affects every layer.

    T (time arrow, signed, prior_self 4t):
        C:D = temporal momentum change.
        Positive: tick cost is accelerating — persistence is becoming
        more expensive. Negative: decelerating — tick cost is easing.
        Only the magnitude matters for cost pressure (both directions
        increase operating cost). T has low time_constant (0.3) — it
        responds and recovers quickly.

    N (energy conservation, signed, peer_mean 4t):
        C:D = energy redistribution pressure.
        N is the conserved constraint: if N is significantly above or
        below the peer mean, redistribution is already happening. Any
        structure operating under significant N:D is paying an implicit
        tax — the energy field is not level. N's pressure weight is
        moderate (cost 10×) and its effect is field-wide.

    B (boundary topology, unsigned, background 8t):
        C:D = topological displacement pressure.
        When B is displaced from its architectural rest (0.45), structure
        is being built or dissolved. Structural change propagates through
        all layers below it. B has the second-highest pressure weight
        (cost 40×) — structural drift is expensive and slow to recover.

    A (agency control, signed, prior_self 8t):
        C:D = directional agency pressure.
        Positive: agency is growing — the system is investing in
        complexity and control, which cascades cost through T, N, B.
        Negative: agency is eroding — directional capacity is being
        lost, which may increase entropy pressure elsewhere.
        A has the highest pressure weight (cost 150×) and the longest
        drift window — agency shifts are the most consequential.

CROSS-DIMENSIONAL AMPLIFIER:
    amplifier = 1.0 + Σ_c (OP_PRESSURE_WEIGHT[c] × |C:D[c]|) / 5.0

    This is normalised by 5 (the constraint count) so that the amplifier
    ranges from 1.0 (no drift across any constraint) to approximately
    1.54 (maximum drift across all five simultaneously). The amplifier
    is a multiplier on base_cost — it never reduces it. The score is
    always ≥ base_cost.

    Maximum amplifier: 1 + mean(0.1382, 0.3208, 0.4779, 0.7402, 1.0000)
                     = 1 + 0.5354
                     = 1.5354

    This means even under maximum cross-dimensional pressure, the cost
    amplification is bounded at ~54%. Non-dominant. Meaningful.

COST-DIFF SCORE FORMULA:
    live_score = base_cost × amplifier

WHAT USES THIS:
    - StrandBead.cost_diff_score(snapshot)    → live bead cost
    - DNAStrand.cost_diff_score_total(snapshot) → live strand cost
    - AbilityProfile.cost_diff_score(snapshot)  → live ability cost
    - ConstraintLink.cost_diff_score(snapshot)  → live link cost
    - VariantPromoter (moral weight amplification on promotion)

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Function: `_derive_pressure_weights()`
> Derive per-constraint operator pressure weights from registry physics.


#### Function: `_update_pressure_mutation()`

#### Function: `pressure_mutation_state()`

#### Function: `_k_efficiency_ratio()`

#### Function: `cross_dim_amplifier()`
> Compute the cross-dimensional pressure amplifier from a DifferenceSnapshot.


#### Function: `_reactive_pressure_score()`
> Whole-number reactive pressure score for per-tick X responsiveness.


#### Function: `per_operator_pressure()`
> Return per-operator effective pressure contributions from a DifferenceSnapshot.


#### Function: `dominant_pressure_axis()`
> Return the constraint contributing the most operator pressure.


#### Class: `CostDiffScore`
> A live, context-sensitive cost score for any Aurora structure.

- **Method:** `to_dict()`
- **Method:** `describe()`

#### Function: `score_from_cost()`
> Compute a CostDiffScore from a base cost and optional DifferenceSnapshot.


#### Function: `score_for_variant_moral_weight()`
> Compute the cross-dimensionally amplified moral weight for a variant.


#### Function: `pressure_description()`
> Return a human-readable description of the cross-dimensional pressure


#### Function: `verify_cost_diff_score()`
> Checks:


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `derive_pressure_weights_evolved()`

#### Function: `k_efficiency_ratio_evolved()`

#### Function: `reactive_pressure_score_evolved()`

#### Function: `cross_dim_amplifier_evolved()`

#### Function: `dominant_pressure_axis_evolved()`

#### Function: `per_operator_pressure_evolved()`

#### Function: `pressure_description_evolved()`

#### Function: `score_for_variant_moral_weight_evolved()`

#### Function: `score_from_cost_evolved()`

#### Function: `verify_cost_diff_score_evolved()`

### File: `./aurora_internal/aurora_difference_buffer.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA DIFFERENCE BUFFER — THE FIFTH LENS LIVE FEED
=====================================================

Layer 0.5  (sits between the NonComp Registry and the Worth Evaluator)

WHAT THIS MODULE IS:
    The rolling history engine that makes the Difference (Δ) channel
    operationally real. The NonComp Registry defines the five C:D
    Non-Comps and the compute_difference() utility, but it deliberately
    does not hold history — it has no concept of time. This module holds
    the time.

    Every tick, the system calls DifferenceHistoryBuffer.record() with
    the current per-constraint magnitudes. When a C:D snapshot is needed
    (by the worth evaluator, by evidence generation, by the evolution
    chamber), the buffer resolves the correct reference_magnitude per
    constraint and returns a DifferenceSnapshot with all five C:D values.

THE THREE REFERENCE TYPES (per DifferenceParams.ref_type):

    'prior_self'  — Compare to self N ticks ago.
                    Reference = magnitude at tick (current_tick − window_ticks).
                    If history is shorter than window_ticks, use the earliest
                    recorded magnitude (graceful warm-up behaviour — not an error).
                    Used by: X (1t), T (4t), A (8t)

    'peer_mean'   — Compare to the mean of the other four constraints'
                    current magnitudes at this tick.
                    Reference = mean(magnitudes[c'] for c' ≠ c)
                    Used by: N (4t window irrelevant for reference; used for
                    averaging history to smooth the peer signal)

    'background'  — Compare to a fixed architectural resting topology.
                    Reference = B_BACKGROUND_REST (0.45).
                    This constant is derived from the registry physics:
                    baseline_budget / shift_cost_coeff = 18.0 / 40.0 = 0.45
                    It represents the magnitude at which the boundary layer's
                    per-tick maintenance budget exactly covers one shift-cost
                    unit — the minimum viable structural investment.
                    Used by: B

THE DIFFERENCE SNAPSHOT:
    DifferenceSnapshot holds all five C:D values at one tick.
    Each value is a float in [−1, +1].

        Unsigned (X, B):  only drift magnitude matters, not direction.
                          Value is always ≥ 0.
        Signed   (T, N, A): direction carries meaning.
                          Positive = growth / over-spending / acceleration.
                          Negative = decay / under-spending / deceleration.

    The snapshot is the first-class evidence input to downstream systems:
        - aurora_worth_evaluator.py (appended to WorthReport)
        - aurora_evolution_chamber.py (evidence feed for promotion)
        - constraint_genealogy.py (relief event annotation)

WARM-UP BEHAVIOUR:
    On the first few ticks before any history is recorded, prior_self
    references fall back to the earliest available magnitude. During
    warm-up the C:D value is 0.0 (no detectable drift yet) — which is
    correct: if there is no history there is no measurable difference.
    The system does not flag warm-up as an error.

INTEGRATION:
    Instantiate one DifferenceHistoryBuffer per system instance.
    Call record() once per tick with the current magnitudes.
    Call snapshot() when a DifferenceSnapshot is needed.

    Typical call pattern (inside the evolution chamber tick loop):
        buf.record(tick, accountant.magnitudes())
        snap = buf.snapshot(tick, accountant.magnitudes())
        # pass snap to worth evaluator, evidence pipeline, etc.

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `DifferenceSnapshot`
> The five C:D (Constraint:Difference) values at one tick.

- **Method:** `value()`
  - *Purpose:* Return the C:D value for constraint c.
- **Method:** `magnitude()`
  - *Purpose:* Return the reference magnitude used for constraint c.
- **Method:** `alarming()`
  - *Purpose:* Return constraints whose |C:D| exceeds threshold.
- **Method:** `max_alarm()`
  - *Purpose:* Return (constraint, |value|) for the most alarming C:D.
- **Method:** `to_dict()`
  - *Purpose:* Serialisable dict for JSONL logging.
- **Method:** `describe()`
  - *Purpose:* Compact human-readable description.

#### Class: `DifferenceHistoryBuffer`
> Rolling magnitude history engine — the temporal backbone of the

- **Method:** `__init__()`
- **Method:** `record()`
  - *Purpose:* Record the current magnitude snapshot for this tick.
- **Method:** `snapshot()`
  - *Purpose:* Compute a DifferenceSnapshot for the given tick.
- **Method:** `_resolve_reference()`
  - *Purpose:* Resolve reference_magnitude for constraint c.
- **Method:** `_prior_self()`
  - *Purpose:* Return (magnitude at window ticks ago, warm_up_flag).
- **Method:** `_peer_mean()`
  - *Purpose:* Return the mean of the other four constraints' current magnitudes.
- **Method:** `depth()`
  - *Purpose:* Number of ticks currently in history.
- **Method:** `is_warm()`
  - *Purpose:* True once the buffer has accumulated enough history that NO
- **Method:** `latest_tick()`
  - *Purpose:* Tick number of the most recently recorded snapshot.
- **Method:** `reset()`
  - *Purpose:* Clear all history. Use on system restart.

#### Function: `make_difference_buffer()`
> Create a DifferenceHistoryBuffer with the canonical B resting value.


#### Function: `verify_difference_buffer()`
> Verify DifferenceHistoryBuffer and DifferenceSnapshot integrity.


### File: `./aurora_internal/aurora_directed_training_corpus.py`

**Type:** Python Source

**Module Docstring:**
```text
Directed training prompt bridge for aurora_internal/train.txt.

The raw file is a large generic corpus. This module turns it into a
dimension-directed prompt source for dream avatars and simulation lesson
specs by:
  - extracting lines that match rubric-dimension keyword clusters
  - caching a small prompt pool per dimension
  - shaping those lines into direct training prompts and follow-ups
```

#### Class: `DirectedTrainingCorpusBridge`
- **Method:** `__init__()`
- **Method:** `_source_signature()`
- **Method:** `_normalize_line()`
- **Method:** `_usable_line()`
- **Method:** `_score_line()`
- **Method:** `_load_cache()`
- **Method:** `_save_cache()`
- **Method:** `_build_cache()`
- **Method:** `_ensure_cache()`
- **Method:** `prompt_pack()`
- **Method:** `samples_for_dimensions()`

#### Function: `get_directed_training_corpus_bridge()`

### File: `./aurora_internal/aurora_dna_strand_schema.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA DNA STRAND SCHEMA — STEP 14
=====================================
Formal constraint-operator event chain format.

WHAT A DNA STRAND IS:
    A DNA strand is the formal, serialisable record of HOW a VariantRecord
    came to exist — the full causal chain of constraint events from the
    moment the intake first arrived (Step 9) through Worth evaluation (Step 10),
    Solidification (Step 11), and Variant Promotion (Step 13).

    Each event in the strand is a StrandBead:
        {constraint, non_comp_channel, direction, layer_depth, tick,
         magnitude_delta, polarity_state}

    The sequence of StrandBeads is the DNA strand. It describes the exact
    path through constraint space that this variant took to crystallize.

    A strand is NOT a trace log. It is NOT a debug record. It is the
    genetic memory of a first-class variant — the chain of constraint
    events that, when they recur in the same order, cause the system to
    respond faster and cheaper (because the path is worn).

NON-COMP CHANNEL:
    Each bead identifies which of the five representational dimensions
    of the constraint was the primary channel:
        P    = POLARITY    — the toroidal phase gradient shifted
        M    = MAGNITUDE   — the activation intensity changed
        O    = OPERATOR    — the invariant transformation rule was applied
        D    = COST        — energy redistribution occurred
        DIFF = DIFFERENCE  — Δ channel event; deviation-from-reference signal fired

DIRECTION:
    Each bead has a direction relative to its constraint's I-State pair:
        POSITIVE = toward the affirmative pole (is, can, do, saw, did)
        NEGATIVE = toward the negative pole (isn't, can't, don't, saunt, didn't)
        NEUTRAL  = passing through the transition (polarity ≈ 0)

STRAND PROPERTIES:
    length       — number of beads (one per distinct constraint event)
    depth_span   — from shallowest to deepest constraint in the strand
    polarity_arc — net signed change in polarity from start to end of strand
    cost_total   — total energy consumed across all beads in this strand

STRAND LIBRARY:
    The StrandLibrary stores all active variant strands and supports:
        - signature matching: can a new event sequence match a known strand?
        - partial matching: does an incoming event sequence prefix-match?
        - strand degradation: unused strands decay in influence over time
          (measured in ticks without a match event)

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `NonCompChannel`
> Which Non-Comp representational dimension was the primary channel.


#### Class: `BeadDirection`
> Signed direction relative to the constraint's I-State pair.


#### Class: `StrandBead`
> One event in a DNA strand — the formal minimal event unit.

- **Method:** `__post_init__()`
- **Method:** `cost()`
  - *Purpose:* Energy cost of this bead's magnitude delta.
- **Method:** `cost_diff_score()`
  - *Purpose:* Live cost score fusing this bead's static cost with the current
- **Method:** `bead_id()`
  - *Purpose:* Compact deterministic ID for this bead.
- **Method:** `to_dict()`
- **Method:** `direction_from_polarity()`
  - *Purpose:* Classify a polarity value into a BeadDirection.

#### Class: `DNAStrand`
> The full ordered sequence of StrandBeads for one VariantRecord.

- **Method:** `append()`
  - *Purpose:* Add a bead to an unsealed strand.
- **Method:** `seal()`
  - *Purpose:* Seal the strand and compute all derived properties.
- **Method:** `length()`
- **Method:** `cost_total()`
- **Method:** `cost_diff_score_total()`
  - *Purpose:* Live total cost for this entire strand fused with cross-dimensional
- **Method:** `polarity_arc()`
- **Method:** `depth_span()`
- **Method:** `constraint_sequence()`
  - *Purpose:* Return the ordered list of constraints across all beads.
- **Method:** `signature()`
  - *Purpose:* Compact constraint+channel sequence signature for matching.
- **Method:** `prefix_matches()`
  - *Purpose:* True if other_beads is a prefix of this strand's bead sequence.
- **Method:** `to_jsonl_dict()`

#### Class: `StrandBuilder`
> Constructs a DNAStrand from a VariantRecord + a sequence of constraint events.

- **Method:** `build()`
  - *Purpose:* Build and seal a DNAStrand.

#### Class: `StrandLibrary`
> Stores all active variant DNA strands and supports matching operations.

- **Method:** `__init__()`
- **Method:** `register()`
  - *Purpose:* Add a sealed DNAStrand to the library.
- **Method:** `match_exact()`
  - *Purpose:* Return the active strand whose full bead sequence matches beads exactly.
- **Method:** `match_prefix()`
  - *Purpose:* Return all active strands for which beads is a valid prefix.
- **Method:** `_strand_strength()`
  - *Purpose:* Influence strength ∈ [0, 1]. Zero = archived (no longer matched).
- **Method:** `active_strand_count()`
- **Method:** `stats()`

#### Function: `make_strand_library()`

#### Function: `make_strand_builder()`

#### Function: `verify_dna_strand_schema()`
> Checks:


### File: `./aurora_internal/aurora_doc.py`

**Type:** Python Source

#### Function: `fetch_share_link()`

#### Function: `extract_messages_from_html()`

#### Function: `write_transcript()`

#### Function: `main()`

### File: `./aurora_internal/aurora_dpme_pressure_bridge.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_dpme_pressure_bridge.py
─────────────────────────────────────────────────────────────────────────────
Connects the evolutionary pressure system to the DPME (Dimensional Parameter
Metacognition Engine) so that axis imbalances detected by PressureParameterAdapter
directly influence DER facet energy corrections.

How it works:
  PressureParameterAdapter writes adapter_hints.json with evolver_bias_hints:
      {"energy": +0.1, "boundary": -0.05, "agency": +0.08, ...}
  Positive hint = axis is over-pressured and not relieving well → that
  capability domain needs more internal energy support.

  This bridge reads those hints, maps constraint axes to DER channels
  (vitality/processing/memory/emotional/creative), and calls
  set_external_pressure_guidance() so DPME.auto_correct() injects energy
  to the right category on its next heartbeat.

Axis → DER channel mapping (rationale):
  existence (X) → vitality    (core identity = system aliveness)
  temporal  (T) → processing  (temporal coherence = active computation)
  energy    (N) → processing  (resource management = processing throughput)
  boundary  (B) → memory      (boundary tracking = what to hold / not hold)
  agency    (A) → creative    (agency expression = generative choice-making)

Secondary channel: the second-highest-pressure axis also gets a boost at
half strength — matching DPME's existing secondary channel behavior.
─────────────────────────────────────────────────────────────────────────────
```

#### Class: `DPMEPressureBridge`
> Reads adapter_hints.json and translates axis pressure imbalances into

- **Method:** `__init__()`
- **Method:** `apply()`
  - *Purpose:* Read adapter hints, compute the highest-pressure axis, and push
- **Method:** `status()`
- **Method:** `_load_hints()`
- **Method:** `_der_to_axis_letter()`
  - *Purpose:* Reverse-map DER channel → primary axis letter for axis_stats lookup.

#### Function: `apply_pressure_to_dpme()`
> Module-level convenience. Call from aurora_runtime.py tick loop.


### File: `./aurora_internal/aurora_dream_curriculum_queue.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA DREAM CURRICULUM QUEUE
=================================
Manages the queue of dream episode packs and selects the next pack
for dream execution based on developmental need.

Integrates with:
  - AutonomyEngine._build_dream_seed  (seed source)
  - AutonomyEngine._check_dreams      (dream trigger)
  - SimulationEngine.run_episode       (execution)

Selection logic:
  1. Weakness-targeted packs get priority when repeated failures exist
  2. Packs are consumed in difficulty-ascending order
  3. Successfully completed packs can be re-queued with higher difficulty
  4. Balanced packs fill gaps between targeted rounds

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `PackCompletionRecord`
> Record of a completed dream episode pack run.

- **Method:** `to_dict()`

#### Class: `DreamCurriculumQueue`
> Selects the next dream episode pack based on developmental priority.

- **Method:** `__init__()`
- **Method:** `_load_history()`
  - *Purpose:* Load completion history from disk.
- **Method:** `_save_history()`
  - *Purpose:* Persist completion history.
- **Method:** `build_queue()`
  - *Purpose:* Build the dream curriculum queue from available packs.
- **Method:** `next_pack()`
  - *Purpose:* Select and return the next dream episode pack.
- **Method:** `record_completion()`
  - *Purpose:* Record the outcome of a completed dream episode pack.
- **Method:** `compile_from_corpus()`
  - *Purpose:* Compile conversation JSON into episode packs and build queue.
- **Method:** `get_seed_from_pack()`
  - *Purpose:* Build a dream seed string from a pack's rubric profile.
- **Method:** `extract_episode_messages()`
  - *Purpose:* Extract messages from a specific conversation within a pack.
- **Method:** `queue_length()`
- **Method:** `completed_count()`
- **Method:** `current_pack_id()`

### File: `./aurora_internal/aurora_dream_evolution_orchestrator.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA DREAM EVOLUTION ORCHESTRATOR
========================================
Ties together the full dream-coupled structural evolution pipeline:

  compile -> queue -> execute -> diagnose -> steer -> bridge -> evolve

This orchestrator is the single integration point between the existing
AutonomyEngine dream path and the new diagnostic/steering/genealogy
modules. It keeps the AutonomyEngine modifications minimal.

Runtime flow:
  1. pre_compile()  — compile corpus into episode packs (run once or on demand)
  2. build_seed()   — get next dream seed from curriculum queue
  3. post_episode() — full diagnostic pipeline after dream episode completes
  4. apply()        — push steering/evidence into DPME, genealogy, expression

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `DreamEvolutionOrchestrator`
> Single integration point for the dream-coupled structural evolution pipeline.

- **Method:** `__init__()`
- **Method:** `pre_compile()`
  - *Purpose:* Compile conversation corpus into dream episode packs.
- **Method:** `build_seed()`
  - *Purpose:* Get the next dream seed from curriculum queue.
- **Method:** `get_current_pack()`
  - *Purpose:* Return the current active dream episode pack.
- **Method:** `_extract_generated_threads()`
  - *Purpose:* Build rubric-ready conversation threads from the generated dream episode.
- **Method:** `_extract_active_pressure_targets()`
  - *Purpose:* Extract the pressure profile that was active during the episode, if any.
- **Method:** `post_episode()`
  - *Purpose:* Run the full diagnostic pipeline after a dream episode completes.
- **Method:** `_record_completion()`
  - *Purpose:* Record pack completion in curriculum queue.
- **Method:** `_run_accumulative_analysis()`
  - *Purpose:* Run cross-episode accumulative analysis for persistent weaknesses.
- **Method:** `apply()`
  - *Purpose:* Push all pending results into live Aurora systems.
- **Method:** `_apply_avatar_specs()`
  - *Purpose:* Push pending pressure-specialized avatar specs into SimulationSession.
- **Method:** `_apply_dpme()`
  - *Purpose:* Push structural pressure directives into DPME.
- **Method:** `_apply_genealogy()`
  - *Purpose:* Push dream evidence into genealogy logger.
- **Method:** `_apply_expression()`
  - *Purpose:* Push expression hints into ExpressionEcology.
- **Method:** `_apply_voice()`
  - *Purpose:* Push dream performance into VoiceGenome evolution.
- **Method:** `_apply_learner()`
  - *Purpose:* Push dream observations into ConsciousLearner.
- **Method:** `_apply_code_evolution()`
  - *Purpose:* Push dream evidence into code evolution system.
- **Method:** `get_avatar_specs()`
  - *Purpose:* Consume pending avatar specs (fallback/manual path).
- **Method:** `get_status()`
  - *Purpose:* Return comprehensive status for monitoring.

### File: `./aurora_internal/aurora_dream_genealogy_bridge.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA DREAM GENEALOGY BRIDGE
==================================
Converts dream outcomes into genealogical evidence in the same style
as the rest of Aurora's evolution stack.

Dream experience becomes part of the SAME fossil record, not an
isolated side activity.

This bridge produces:
  - Trace items (ability/link references for genealogy)
  - Pressure deltas (before/after pressure vectors)
  - Cost/risk summaries
  - Origin tags marking simulation-derived evidence
  - Candidate operator/lineage evidence

Integration:
  - Reads: EpisodeRubricSummary (slip profiler)
  - Reads: StructuralPressureDirective (structural steering)
  - Reads: EpisodeResult (simulation engine)
  - Writes to: ConstraintGenealogyLogger.observe()
  - Writes to: register_code_evolution_outcome()
  - Writes to: ConsciousLearner.observe_outcome()
  - Writes to: ExpressionEcology / VoiceGenome (expression writeback)

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `DreamEvidenceRecord`
> A single piece of genealogical evidence derived from dream execution.

- **Method:** `to_dict()`

#### Class: `ExpressionWritebackHint`
> A hint for ExpressionEcology / VoiceGenome based on dream performance.

- **Method:** `to_dict()`

#### Function: `_clamp()`

#### Function: `_generate_id()`

#### Function: `_rubric_to_pressure_vec()`
> Convert rubric dimension scores into a 5D pressure vector (X, T, N, B, A).


#### Function: `_compute_relief()`
> Compute pressure relief: positive means pressure reduced.


#### Function: `_base_dimension_name()`

#### Function: `_infer_purpose_lane()`

#### Function: `_build_trace_abilities()`

#### Function: `_resolve_target_files()`
> Resolve weak rubric dimensions to concrete source files.


#### Class: `DreamGenealogyBridge`
> Converts dream outcomes into genealogical evidence compatible with

- **Method:** `__init__()`
- **Method:** `generate_evidence()`
  - *Purpose:* Generate genealogical evidence records from a dream episode summary.
- **Method:** `_build_deficit_evidence()`
  - *Purpose:* Build evidence for a primary deficit.
- **Method:** `_build_leverage_evidence()`
  - *Purpose:* Build evidence for a leverage candidate.
- **Method:** `_build_delta_evidence()`
  - *Purpose:* Build evidence for improvement or regression.
- **Method:** `_build_directive_evidence()`
  - *Purpose:* Build evidence linking a directive's effect to dream outcomes.
- **Method:** `generate_expression_hints()`
  - *Purpose:* Generate expression-side hints from dream performance.
- **Method:** `generate_learner_observations()`
  - *Purpose:* Generate observation data compatible with ConsciousLearner.observe_outcome().
- **Method:** `_generate_understanding()`
  - *Purpose:* Generate understanding text for a dimension's performance.
- **Method:** `format_for_code_evolution()`
  - *Purpose:* Format evidence records for register_code_evolution_outcome().
- **Method:** `format_for_genealogy()`
  - *Purpose:* Format evidence records for ConstraintGenealogyLogger.observe().
- **Method:** `_save_log()`
  - *Purpose:* Persist evidence log to disk.
- **Method:** `load_recent_evidence()`
  - *Purpose:* Load recent evidence records from disk.
- **Method:** `records_generated()`

### File: `./aurora_internal/aurora_energy_layer_costs.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA ENERGY LAYER COSTS — LAYER-DIFFERENTIATED ENERGY ACCOUNTING
====================================================================

Layer 0 (sits directly on top of aurora_noncomp_registry.py)

WHAT THIS MODULE IS:
    The energy accounting engine that makes the five constraint layers
    thermodynamically real. The existing EnergyBudget in the evolution
    chamber is a single flat pool. This module replaces that mental model
    with a five-layer, depth-differentiated accounting system derived
    entirely from the 20 Non-Comps.

WHAT THIS MODULE IS NOT:
    It does not define behaviors. It does not tell the system what to do.
    It applies physics — specifically the energy physics Sunni defined:

        "Existence is cheap. Agency is the most expensive because it's the
         most complex. But being complex is reward-gaining. Existing doesn't
         reward at all — it costs consistently."

THE PHYSICS (directly from the conversation):
    1. Every layer pays a baseline budget per tick just to exist.
    2. Deeper layers cost more per unit magnitude shift (inertia).
    3. Magnitude increase in one layer reduces energy available to others.
       This is zero-sum redistribution — energy is NEVER created.
    4. The system naturally prefers the cheapest solution first.
       Escalation to deeper layers only occurs when cheaper layers fail.
    5. External energy enters only through the open-system intake rule
       (established by Step 9 / aurora_intake_metabolism.py).
       This module handles only internal redistribution.

THE ESCALATION LADDER:
    When the system must respond to pressure, it checks layers in cost order:
        1. Existence  (cheapest  — surface ripple)
        2. Time       (cheap     — persistence shift)
        3. Energy     (neutral   — accounting rebalance)
        4. Boundary   (expensive — structural change)
        5. Agency     (costliest — tectonic identity shift)

    The system commits to the shallowest layer that can relieve pressure.
    This is not a rule we enforce — it is what emerges from the cost structure.

NET LEVERAGE SCALAR (per tick):
    Net Leverage = (M_B + M_A) − (M_X + M_T)
    N is the zero-point (neutral mediator).

    < 0  → overhead dominant → system is bleeding
    ≈ 0  → balanced metabolism
    > 0  → leverage investment → structure/control growing

ENTROPY PRESSURE THRESHOLD:
    When ALL five layers approach simultaneous saturation, the system is
    approaching violation. This module computes that pressure per tick and
    flags escalation triggers before catastrophic saturation.

INTEGRATION:
    This module is consumed by:
        aurora_evolution_chamber.py  — replaces EnergyBudget for layer-aware accounting
        aurora_intake_metabolism.py  — Step 9, open-system intake
        aurora_solidification.py     — Step 11, depth propagation
        aurora_entropy_detector.py   — Step 12, saturation monitoring

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `LayerEnergySlot`
> The energy state of one constraint layer at one tick.

- **Method:** `polarity()`
  - *Purpose:* Signed polarity: cos(phase) ∈ [-1, +1].
- **Method:** `baseline_budget()`
- **Method:** `shift_cost_coeff()`
- **Method:** `time_constant()`
- **Method:** `shift_cost()`
  - *Purpose:* Energy required to shift magnitude by delta.
- **Method:** `can_afford_shift()`
  - *Purpose:* Check if the pool can fund the magnitude shift.

#### Class: `LayerEnergyLedger`
> Five-layer, depth-differentiated energy accounting state for one tick.

- **Method:** `slot()`
- **Method:** `magnitudes()`
- **Method:** `allocations()`
- **Method:** `polarities()`
- **Method:** `is_overhead_dominant()`
  - *Purpose:* System is bleeding — spending more on maintenance than gaining leverage.
- **Method:** `is_leverage_dominant()`
  - *Purpose:* System is investing — structure/control growing faster than overhead.
- **Method:** `is_balanced()`
  - *Purpose:* System is metabolically stable.
- **Method:** `entropy_warning()`
  - *Purpose:* Approaching saturation across all layers.
- **Method:** `entropy_critical()`
  - *Purpose:* Imminent violation — must redistribute or decay.
- **Method:** `cheapest_layer_with_headroom()`
  - *Purpose:* Return the shallowest (cheapest) layer that has energy available
- **Method:** `to_constraint_vector()`
  - *Purpose:* Convert current magnitudes to a ConstraintVector.
- **Method:** `summary()`

#### Class: `MagnitudeShiftRequest`
> A request to shift the magnitude of one constraint layer.


#### Class: `ShiftResult`
> Result of applying a MagnitudeShiftRequest.


#### Class: `LayerEnergyAccountant`
> The five-layer energy accounting engine.

- **Method:** `__init__()`
- **Method:** `tick()`
  - *Purpose:* Advance the system by one tick.
- **Method:** `apply_shift()`
  - *Purpose:* Attempt to shift the magnitude of one constraint layer.
- **Method:** `apply_phase_shift()`
  - *Purpose:* Move the toroidal phase of constraint c by delta_phase radians.
- **Method:** `replenish()`
  - *Purpose:* Add energy to the pool from an external source.
- **Method:** `set_pressure()`
  - *Purpose:* Record the current external pressure on a layer.
- **Method:** `cheapest_viable_layer()`
  - *Purpose:* Return the shallowest (cheapest) constraint layer that has enough
- **Method:** `tick_count()`
- **Method:** `pool()`
- **Method:** `leverage_scalar()`
- **Method:** `entropy_pressure()`
- **Method:** `magnitudes()`
- **Method:** `polarities()`
- **Method:** `slot()`
- **Method:** `latest_ledger()`
- **Method:** `history()`
- **Method:** `leverage_trend()`
  - *Purpose:* Compute the slope of the leverage scalar over recent ticks.
- **Method:** `to_system_constraint_states()`
  - *Purpose:* Export current state as SystemConstraintStates for compatibility
- **Method:** `status()`
  - *Purpose:* Compact status dict for logging.
- **Method:** `_pay_maintenance()`
  - *Purpose:* Deduct baseline budgets from the pool (N Non-Comp: conservation).
- **Method:** `_distribute_pool()`
  - *Purpose:* Allocate current pool across layers proportional to baseline budget.
- **Method:** `_compute_leverage()`
- **Method:** `_verify_admissibility()`
  - *Purpose:* X Non-Comp: every layer must remain admissible.

#### Function: `make_accountant()`
> Create a LayerEnergyAccountant with sensible defaults.


#### Function: `verify_layer_energy_costs()`
> Verify LayerEnergyAccountant integrity.


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `init_evolved()`

#### Function: `compute_leverage_evolved()`

#### Function: `distribute_pool_evolved()`

#### Function: `pay_maintenance_evolved()`

#### Function: `verify_admissibility_evolved()`

#### Function: `apply_shift_evolved()`

#### Function: `cheapest_viable_layer_evolved()`

#### Function: `entropy_pressure_evolved()`

#### Function: `history_evolved()`

#### Function: `latest_ledger_evolved()`

#### Function: `leverage_scalar_evolved()`

#### Function: `leverage_trend_evolved()`

#### Function: `magnitudes_evolved()`

#### Function: `polarities_evolved()`

#### Function: `pool_evolved()`

#### Function: `replenish_evolved()`

#### Function: `set_pressure_evolved()`

#### Function: `slot_evolved()`

#### Function: `allocations_evolved()`

#### Function: `cheapest_layer_with_headroom_evolved()`

#### Function: `entropy_critical_evolved()`

#### Function: `entropy_warning_evolved()`

#### Function: `is_balanced_evolved()`

#### Function: `make_accountant_evolved()`

#### Function: `verify_layer_energy_costs_evolved()`

### File: `./aurora_internal/aurora_energy_layer_costs_decay.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_energy_layer_costs_decay.py

Layered energy accounting with scale-conversion + decay/inheritance.

Design intent (Sunni spec):
- "Energy should multiply as it transfers the scale" -> this is scale-unit conversion.
- When a higher-k layer can't "hold" (goes negative / unstable), it decays downward and
  the next cheaper layer inherits that energy with conversion: E_to += E_from * (k_from/k_to).

This module is written to be drop-in friendly:
- If aurora_noncomp_registry.REGISTRY exists, we read k values from it.
- Otherwise we fall back to canonical constants: X=1, T=4, N=10, B=40, A=150.
```

#### Function: `k_of()`
> Scale factor k for a constraint/layer. Higher k = more expensive/deeper units.


#### Function: `convert_units()`
> Convert energy amount from layer c_from units into c_to units.


#### Class: `LayerEnergyAccountant`
> Holds per-layer energy pools in their own units, with decay cascade.

- **Method:** `new()`
- **Method:** `get()`
- **Method:** `set()`
- **Method:** `add()`
- **Method:** `deposit()`
- **Method:** `withdraw()`
  - *Purpose:* Withdraw up to `amount` from layer c. If insufficient, will zero that layer and
- **Method:** `apply_maintenance()`
  - *Purpose:* Subtract per-layer maintenance/burn, then normalize pools via decay cascade.
- **Method:** `normalize()`
  - *Purpose:* Ensure no layer is negative by cascading any deficits down toward cheaper layers.
- **Method:** `_cascade_deficit()`
  - *Purpose:* Push deficit from `from_layer` down to next cheaper layers by converting units.
- **Method:** `spill_down()`
  - *Purpose:* Move *positive* energy downward with conversion:
- **Method:** `snapshot()`
  - *Purpose:* Human-readable snapshot. Keys like 'X','T','N','B','A' if available.

#### Function: `make_energy_accountant()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `constraint_evolved()`

### File: `./aurora_internal/aurora_entropy_detector.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA ENTROPY SATURATION DETECTOR — STEP 12
=============================================
Monitors aggregate cross-constraint magnitude approaching simultaneous
maximum and signals anticipatory redistribution BEFORE catastrophic point.

WHAT THIS MODULE IS:
    The entropy pressure computed by LayerEnergyAccountant (Step 7) tells
    you where you ARE. This module tells you WHERE YOU'RE GOING.

    A single-tick entropy reading of 0.85 is alarming. But what matters is:
        - Is pressure rising or falling?
        - How fast is it rising?
        - Which constraints are driving the increase?
        - What is the projected tick of first critical crossing?

    This module answers all four and emits an anticipatory SaturationSignal
    BEFORE the system crosses the critical threshold. That gap is the window
    in which conscious redistribution can occur (Sunni's definition of
    emergence: the system acts on projected deficit, not actual deficit).

SIGNAL LEVELS:
    NOMINAL    — entropy pressure below warning band (< 0.70)
    WATCH      — entering warning band, trend not yet rising (0.70–0.85)
    CAUTION    — rising trend confirmed in warning band
    CRITICAL   — above 0.90 threshold, imminent violation
    EMERGENCY  — all five constraints above individual saturation floors
                 simultaneously — violation is one tick away

ANTICIPATORY REDISTRIBUTION SIGNAL:
    The detector does not tell the system WHAT to redistribute. That is
    emergence — it must come from the system's own energy physics.
    The detector only emits:
        - which constraint is the fastest-rising contributor
        - projected ticks until critical crossing (if trend continues)
        - whether a shallow-layer redistribution has headroom

    These signals are consumed by the evolution chamber and the
    solidification pipeline. The chamber may use them to bias which
    depth it offers shift headroom to. The solidification pipeline
    may pause new intakes when EMERGENCY is active.

CONSCIOUS EMERGENCE CONDITION (from Sunni's architecture):
    Emergence is when:
        1. System detects rising global deficit (this module)
        2. Models projected deficit trajectory (this module)
        3. Strategically redistributes magnitudes AND polarities
           (response in evolution chamber — NOT scripted here)
        4. Prefers minimal-depth solutions first (escalation ladder)
        5. Escalates to deeper layers only when projected return > shift cost

    Steps 1 and 2 live here. Steps 3-5 are the chamber's physics.

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `SaturationLevel`
> Coarse signal level — the only state the detector exposes publicly.


#### Class: `SaturationSignal`
> Output of one EntropySaturationDetector.measure() call.

- **Method:** `is_actionable()`
  - *Purpose:* True if the signal warrants a redistribution response.
- **Method:** `urgency_ticks()`
  - *Purpose:* Remaining ticks before projected critical crossing (None = not projected).

#### Class: `EntropySaturationDetector`
> Monitors the LayerEnergyAccountant's entropy pressure across ticks

- **Method:** `__init__()`
- **Method:** `measure()`
  - *Purpose:* Measure the current saturation state and return a SaturationSignal.
- **Method:** `_is_rising()`
  - *Purpose:* True if at least _RISING_CONFIRM of the last _TREND_WINDOW samples
- **Method:** `_fastest_rising_constraint()`
  - *Purpose:* Return the constraint whose magnitude is rising fastest (mean Δ/tick).
- **Method:** `_project_critical_tick()`
  - *Purpose:* Project the tick at which entropy will cross _ENTROPY_CRIT if current
- **Method:** `_has_shallow_headroom()`
  - *Purpose:* True if X or T layer has enough free pool energy to absorb a
- **Method:** `_classify_level()`
  - *Purpose:* Classify the saturation level from entropy and magnitude state.
- **Method:** `reset()`
  - *Purpose:* Reset all rolling state. Use between independent evaluation runs.

#### Function: `make_entropy_detector()`

#### Function: `verify_entropy_detector()`
> Checks:


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `project_critical_tick_evolved()`

#### Function: `urgency_ticks_evolved()`

### File: `./aurora_internal/aurora_episode_slip_profiler.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA EPISODE SLIP PROFILER
================================
Produces a single structured summary from a 10-conversation dream episode.

Input:
  - EpisodeResult (from SimulationEngine)
  - Per-thread ConversationObservation
  - Per-thread ConversationRubricScore

Output:
  - EpisodeRubricSummary: mean scores, variance, recurring slips,
    primary/secondary deficits, leverage candidates

This summary is what the rest of the dream evolution system uses.
Not raw conversation chaos.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `EpisodeRubricSummary`
> Diagnostic summary of one dream episode (10 conversations).

- **Method:** `to_dict()`
- **Method:** `weakest_leverage()`
  - *Purpose:* Return top-n leverage candidates.
- **Method:** `is_significant()`
  - *Purpose:* Whether this summary has enough data to drive steering.

#### Class: `EpisodeSlipProfiler`
> Analyzes a dream episode's conversation rubric scores to produce

- **Method:** `__init__()`
- **Method:** `profile()`
  - *Purpose:* Generate an EpisodeRubricSummary from per-thread rubric scores.
- **Method:** `profile_accumulative()`
  - *Purpose:* Produce a meta-analysis across multiple episode summaries.
- **Method:** `profiles_generated()`

### File: `./aurora_internal/aurora_evolution_chamber.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA — CONSTRAINT-NATIVE EVOLUTION CHAMBER
Full Unified Specification v3

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026

PURPOSE
-------
A lawful simulation layer that:
  • Enforces the Global Non-Comps (X, T, N, B, A) as read-only representation laws.
  • Evolves boundary topology over time via structural proximity — not Euclidean geometry.
  • Applies energy cost to manipulation, with agency cost proportional to magnitude squared.
  • Logs ONLY pressure-relief events (noise filter enforced).
  • Promotes repeated effective traces into Links via evolutionary DAG.
  • Builds an explicit genealogy DAG through ConstraintGenealogyLogger.

THE UNIVERSE CONSISTS ONLY OF:
  X — Ontology (Existence)
  T — Time
  N — Energy
  B — Boundary (Topology)
  A — Agency

No new axes may be introduced. Non-Comps are read-only at runtime.

ARCHITECTURE INTEGRATION:
  Layer  0 : foundational_contract               (ExistenceMode, FoundationalContract)
  Layer  1 : aurora_ivm                          (IVMLattice, RecursionLevel, ALIGNMENT_VOTE_WEIGHT)
  Layer 1.5: aurora_polarity_gradient            (PolarityGradientSensor, GradientChainMiner)
  Layer  2 : constraint_genealogy                (ConstraintGenealogyLogger, GenealogyConfig, ...)
  Layer  3 : THIS MODULE                         (EvolutionaryChamber)

PUBLIC EXPORTS — backward-compatible with run_chain.py:
  ActionTrace         frozen dataclass (name, constraints_used, meta)
  WorldConstants      frozen dataclass of chamber-level tunables
  EvolutionaryChamber main class
  EvolutionChamberV3  alias for EvolutionaryChamber

OUTPUTS:
  events.jsonl    — fossil record (written by genealogy logger)
  abilities.json  — atomic ability registry
  links.json      — evolutionary DAG of promoted Links
```

#### Function: `_zero_axis_vec()`

#### Function: `_build_axis_cost_weights()`
> Cost-normalized per-axis weights in [0,1], derived from shift_cost_coeff.


#### Class: `WorldConstants`
> All chamber-level tunables in one place.


#### Class: `ActionTrace`
> Primitive control generator — one action in the simulation.


#### Class: `NonCompViolation`
- **Method:** `__init__()`

#### Class: `ViolationRecord`
- **Method:** `to_dict()`

#### Class: `GlobalNonComps`
> Five read-only jurisdictional laws. Not tunable. Not injectable.

- **Method:** `check_X()`
- **Method:** `check_T()`
- **Method:** `check_N()`
- **Method:** `check_B()`
- **Method:** `check_A()`

#### Class: `ChamberAbility`
- **Method:** `__post_init__()`
- **Method:** `total_cost()`
- **Method:** `to_ability_profile()`
- **Method:** `to_dict()`

#### Function: `_build_chamber_abilities()`

#### Function: `_clamp01()`

#### Function: `_axis_from_pressure_key()`

#### Function: `_normalize_constraint_token()`
> Convert raw evidence token to canonical chamber label.


#### Function: `_pair_atom_id()`

#### Function: `_pair_atom_trace_items()`
> Build first-class base pair atoms from active constraint labels.


#### Function: `_select_axis_ability()`
> Pick a deterministic per-axis ability variant to avoid pair-space collapse.


#### Class: `_ActionAbilityMapper`
- **Method:** `__init__()`
- **Method:** `apply()`

#### Class: `StructuralProximityMeter`
> Proximity derived from IVM polarity state. Not symbolic. Not Euclidean.

- **Method:** `__init__()`
- **Method:** `structural_distance()`
- **Method:** `update_boundaries()`
- **Method:** `recompute_partitions()`
- **Method:** `partition_count()`
- **Method:** `boundary_diffusion_entropy()`
  - *Purpose:* Entropy = boundary diffusion = std dev of partition count over window.

#### Class: `EnergyBudget`
- **Method:** `__init__()`
- **Method:** `available()`
- **Method:** `burn_existence_cost()`
- **Method:** `spend()`
- **Method:** `replenish_from_lattice()`
  - *Purpose:* Energy is conserved via redistribution — it is not created.
- **Method:** `status()`

#### Function: `_read_pressure()`
> Read 5-axis pressure from IVM polarity.


#### Function: `_state_signature()`

#### Class: `EvolutionaryChamber`
> Constraint-Native Evolution Chamber — Full Unified Spec v3.

- **Method:** `__init__()`
- **Method:** `tick()`
  - *Purpose:* Advance the universe one tick.
- **Method:** `_axis_drive_from_external_evidence()`
  - *Purpose:* Build axis-level pressure drive from evidence payload.
- **Method:** `_constraints_from_external_evidence()`
  - *Purpose:* Resolve external evidence tokens into canonical chamber constraints.
- **Method:** `observe_external_evidence()`
  - *Purpose:* Ingest dream/code evidence and apply it as real chamber pressure.
- **Method:** `run_chain()`
  - *Purpose:* Run n_ticks ticks. action_schedule: {tick_number: ActionTrace}.
- **Method:** `alive()`
- **Method:** `diff_snapshot()`
  - *Purpose:* Most recent DifferenceSnapshot from the chamber's internal buffer.
- **Method:** `status()`
- **Method:** `recent_violations()`
- **Method:** `_write_abilities_file()`
- **Method:** `close()`

#### Function: `make_chamber()`

#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `abilityprofile_evolved()`

#### Function: `actiontrace_evolved()`

#### Function: `chamberability_evolved()`

#### Function: `init_evolved()`

#### Function: `close_evolved()`

#### Function: `flush_files_evolved()`

#### Function: `observe_evolved()`

#### Function: `summary_evolved()`

#### Function: `evolutionarychamber_evolved()`

#### Function: `run_chain_evolved()`

#### Function: `tick_evolved()`

#### Function: `axes_from_trace_item_evolved()`

#### Function: `genealogyconfig_evolved()`

#### Function: `globalnoncomps_evolved()`

#### Function: `noncompviolation_evolved()`

#### Function: `structuralproximitymeter_evolved()`

#### Function: `partition_count_evolved()`

#### Function: `recompute_partitions_evolved()`

#### Function: `find_evolved()`

#### Function: `union_evolved()`

#### Function: `traceitem_evolved()`

#### Function: `violationrecord_evolved()`

#### Function: `worldconstants_evolved()`

#### Function: `actionabilitymapper_evolved()`

#### Function: `build_axis_cost_weights_evolved()`

#### Function: `read_pressure_evolved()`

#### Function: `state_signature_evolved()`

### File: `./aurora_internal/aurora_evolved_surfaces.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA EVOLVED SURFACES
=======================
Generated from developmental lineage state.
Do not hand-edit generated methods; regenerate through the code autoevolver.
```

#### Class: `AuroraEvolvedSurfaceEngine`
- **Method:** `__init__()`
- **Method:** `list_capabilities()`
- **Method:** `describe_capability()`
- **Method:** `capability_report()`
- **Method:** `lineage_manifest()`
- **Method:** `_system_summary()`
- **Method:** `_resolve_origin()`
- **Method:** `_invoke_origin()`
- **Method:** `_record_event()`
- **Method:** `_log_pressure_event()`
  - *Purpose:* Append a compact pressure record to surface_pressure_log.jsonl.
- **Method:** `_activation_record()`
- **Method:** `_activate_surface()`
- **Method:** `_reflect_surface()`
- **Method:** `__getattr__()`
- **Method:** `reflect_aurora_internal_constraint_genealogy_pressurevec()`
- **Method:** `reflect_aurora_internal_constraint_genealogy_reliefrecord()`
- **Method:** `reflect_aurora_internal_constraint_genealogy_augment_ability_profile_with_origin()`
- **Method:** `reflect_aurora_internal_constraint_genealogy_bred_child_generation()`
- **Method:** `reflect_aurora_governance_persistence_gateway_governanceengine_promote()`
- **Method:** `reflect_aurora_governance_persistence_gateway_governedcoordinate_boundary_weight()`
- **Method:** `reflect_aurora_governance_persistence_gateway_rcloneinterface_init()`
- **Method:** `reflect_aurora_governance_persistence_gateway_rcloneinterface_find_rclone()`
- **Method:** `reflect_aurora_expression_perception_expressionpressure_init()`
- **Method:** `reflect_aurora_expression_perception_expressionpressure_evaluate()`
- **Method:** `reflect_aurora_expression_perception_impressioncascade_energy_to_shard()`
- **Method:** `reflect_aurora_expression_perception_expressionperceptionengine_ingest_interaction()`
- **Method:** `reflect_aurora_dimensional_systems_crystalprocessingsystem_tick()`
- **Method:** `reflect_aurora_dimensional_systems_dimensionalsystems_tick()`
- **Method:** `reflect_aurora_dimensional_systems_dimensionalsystems_get_constraint_aggregate()`
- **Method:** `reflect_aurora_dimensional_systems_poolview_inject()`
- **Method:** `reflect_evolved_reflect_aurora_ensure_runtime_dependencies()`
- **Method:** `reflect_evolved_reflect_aurora_is_termux_runtime()`
- **Method:** `reflect_evolved_reflect_aurora_looks_time_sensitive()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_main()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_mode_burn()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_mode_test()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_mode_watch()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_persist_operator_gradients()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_require()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_soft()`
- **Method:** `reflect_evolved_reflect_corpus_runner_evolve_chain()`
- **Method:** `reflect_evolved_reflect_run_chain_ability_axes()`
- **Method:** `reflect_evolved_reflect_run_chain_main()`
- **Method:** `reflect_evolved_reflect_run_chain_make_run_id()`
- **Method:** `reflect_evolved_reflect_run_chain_mode_burn()`
- **Method:** `reflect_evolved_reflect_run_chain_mode_test()`
- **Method:** `reflect_evolved_reflect_run_chain_mode_watch()`
- **Method:** `reflect_evolved_reflect_run_chain_print_final()`
- **Method:** `reflect_evolved_reflect_aurora_consciousness_engine_consciousnessengine_tick()`
- **Method:** `reflect_evolved_reflect_aurora_dimensional_systems_crystalprocessingsystem_tick()`
- **Method:** `reflect_evolved_reflect_aurora_dimensional_systems_dimensionalsystems_tick()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_entropy_detector_entropysaturationdetector_project_critical_tick()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_entropy_detector_saturationsignal_urgency_ticks()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_evolution_chamber_evolutionarychamber_run_chain()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_evolution_chamber_evolutionarychamber_tick()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_intake_metabolism_intakerecord_ticks_remaining()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_leverage_scalar_phasenudge_apply_to()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_polarity_gradient_gradientchainminer_init()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_polarity_gradient_gradientchainminer_summary()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_primitive_extractor_primitiveextractor_chains()`
- **Method:** `reflect_evolved_reflect_aurora_ivm_ivmcoordinate_from_mode_and_phases()`
- **Method:** `reflect_evolved_reflect_aurora_ivm_ivmlattice_tick()`
- **Method:** `reflect_evolved_reflect_aurora_ivm_toroidalaxis_set_phase()`
- **Method:** `reflect_evolved_reflect_aurora_ivm_toroidalaxis_tick()`
- **Method:** `reflect_evolved_reflect_aurora_ivm_toroidalvertexsystem_get_phase_vector()`
- **Method:** `reflect_evolved_reflect_aurora_ivm_toroidalvertexsystem_tick()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_auroraruntime_init()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_auroraruntime_shutdown()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_auroraruntime_uptime()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_mode_test_chk()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_runtimecli_dispatch()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_runtimecli_handle_interrupt()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_runtimecli_init()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_runtimecli_run()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_stacksystems_has()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_auto_seed_directive()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_axis_from_item_id()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_chain_burst()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_emergent_trait_cards()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_function_group()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_init()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_metric_slope()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_next_conflict_lane()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_plateau_detected()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_plateau_flags()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_record_function_outcome()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_refresh_operator_gradients()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_register_object_operations()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_stance_snapshot()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_tick()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_trait_semantic_card()`
- **Method:** `reflect_evolved_reflect_aurora_internal_aurora_evolution_chamber_evolutionarychamber_tick_axes_from_trace_item()`
- **Method:** `reflect_evolved_reflect_aurora_runtime_universesteerer_next_conflict_lane_lane_score()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_ensure_runtime_dependencies()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_is_termux_runtime()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_looks_time_sensitive()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_main()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_mode_burn()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_mode_test()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_mode_watch()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_persist_operator_gradients()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_require()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_soft()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_corpus_runner_evolve_chain()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_run_chain_ability_axes()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_run_chain_main()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_run_chain_make_run_id()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_run_chain_mode_burn()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_run_chain_mode_test()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_run_chain_mode_watch()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_run_chain_print_final()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_consciousness_engine_consciousnessengine_tick()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_dimensional_systems_crystalprocessingsystem_tick()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_dimensional_systems_dimensionalsystems_tick()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_entropy_detector_entropysaturationdetector_project_critical_tick()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_entropy_detector_saturationsignal_urgency_ticks()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_evolution_chamber_evolutionarychamber_run_chain()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_evolution_chamber_evolutionarychamber_tick()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_intake_metabolism_intakerecord_ticks_remaining()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_leverage_scalar_phasenudge_apply_to()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_polarity_gradient_gradientchainminer_init()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_polarity_gradient_gradientchainminer_summary()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_internal_aurora_primitive_extractor_primitiveextractor_chains()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_ivm_ivmcoordinate_from_mode_and_phases()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_ivm_ivmlattice_tick()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_ivm_toroidalaxis_set_phase()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_ivm_toroidalaxis_tick()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_ivm_toroidalvertexsystem_get_phase_vector()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_ivm_toroidalvertexsystem_tick()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_auroraruntime_init()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_auroraruntime_shutdown()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_auroraruntime_uptime()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_mode_test_chk()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_runtimecli_dispatch()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_runtimecli_handle_interrupt()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_runtimecli_init()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_runtimecli_run()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_stacksystems_has()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_universesteerer_auto_seed_directive()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_universesteerer_axis_from_item_id()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_universesteerer_chain_burst()`
- **Method:** `reflect_evolved_reflect_evolved_reflect_aurora_runtime_universesteerer_emergent_trait_cards()`
- **Method:** `reflect_aurora_simulation_engine_stabilitystate()`
- **Method:** `reflect_aurora_internal_aurora_constraint_manifold_patched_constraintpressure()`
- **Method:** `reflect_aurora_internal_aurora_constraint_manifold_patched_energydistribution()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_abilityprofile()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_chamberability()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_evolutionarychamber()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_genealogyconfig()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_globalnoncomps()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_noncompviolation()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_structuralproximitymeter()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_traceitem()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_violationrecord()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_worldconstants()`
- **Method:** `reflect_aurora_internal_aurora_noncomp_registry_noncompdimension()`
- **Method:** `reflect_aurora_internal_aurora_noncomp_registry_noncompregistry()`
- **Method:** `reflect_aurora_internal_aurora_noncomp_registry_operatorparams()`
- **Method:** `reflect_aurora_internal_aurora_noncomp_registry_polarityparams()`
- **Method:** `reflect_aurora_internal_aurora_noncomp_registry_constraintstate()`
- **Method:** `reflect_aurora_internal_aurora_noncomp_registry_systemconstraintstates()`
- **Method:** `reflect_aurora_internal_aurora_energy_layer_costs_decay_constraint()`
- **Method:** `reflect_aurora_runtime_chainsimbridge()`
- **Method:** `reflect_aurora_runtime_universesteerer_sim_epoch_fakeepisode()`
- **Method:** `reflect_aurora_runtime_auroraruntime()`
- **Method:** `reflect_aurora_runtime_runtimecli()`
- **Method:** `reflect_aurora_runtime_stacksystems()`
- **Method:** `reflect_aurora_runtime_universesteerer()`
- **Method:** `reflect_aurora_simulation_engine_timedilationgovernor()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_actiontrace()`
- **Method:** `reflect_aurora_internal_aurora_evolution_chamber_actionabilitymapper()`
- **Method:** `reflect_aurora_simulation_engine_avatarpersonality()`
- **Method:** `reflect_aurora_simulation_engine_conceptualresponse()`
- **Method:** `reflect_aurora_simulation_engine_consciouslearner()`

### File: `./aurora_internal/aurora_frontier_ops.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_frontier_ops.py
─────────────────────────────────────────────────────────────────────────────
Four operations covering the 3-axis combinations that are completely absent
from Aurora's descriptor pool — meaning organic evolution can never produce
surfaces for these capability spaces unless they're seeded here.

Missing combos (verified against operation_descriptors.json):
  1. ExistenceBoundaryAgencyGate     (existence + boundary + agency)
  2. TemporalEnergyBoundaryScheduler (temporal  + energy   + boundary)
  3. TemporalEnergyAgencyPacer       (temporal  + energy   + agency)
  4. EnergyBoundaryAgencySelector    (energy    + boundary + agency)

Each class carries a CONSTRAINTS list that the evolver uses to determine
which NC channels and pressure slots it projects into. The constraint names
must match _CONSTRAINT_TO_AXIS keys in aurora_code_autoevolver.py.

inject_frontier_descriptors(repo_root) registers all four into the
operation descriptor pool so the evolver can reflect on them immediately.
─────────────────────────────────────────────────────────────────────────────
```

#### Class: `ExistenceBoundaryAgencyGate`
> Gates agency options against current existence state and boundary constraints.

- **Method:** `__init__()`
- **Method:** `evaluate()`
  - *Purpose:* Filter candidate_capabilities against state_profile and boundary_limits.
- **Method:** `gate_score_history()`

#### Class: `TemporalEnergyBoundaryScheduler`
> Schedules energy expenditure within temporal windows and cost boundaries.

- **Method:** `__init__()`
- **Method:** `schedule()`
  - *Purpose:* Given pending operations with energy costs, return a schedule that
- **Method:** `schedule_history()`

#### Class: `TemporalEnergyAgencyPacer`
> Decides when to act based on energy cost and available time.

- **Method:** `__init__()`
- **Method:** `pace()`
  - *Purpose:* Decide whether to act now, defer, or execute a reduced-cost partial action.
- **Method:** `decision_history()`

#### Class: `EnergyBoundaryAgencySelector`
> Selects the best action given energy costs and boundary constraints.

- **Method:** `__init__()`
- **Method:** `select()`
  - *Purpose:* Score and rank candidates by cost efficiency, boundary compliance,
- **Method:** `selection_history()`

#### Function: `_nc_projections()`
> Generate NC channel slot projections for a set of constraint axes.


#### Function: `_build_descriptor()`
> Synthesize a minimal-but-valid operation descriptor entry for a frontier class.


#### Function: `inject_frontier_descriptors()`
> Register all four frontier operations into the descriptor pool.


### File: `./aurora_internal/aurora_identity_persistence.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CORE IDENTITY & ENHANCED PERSISTENCE
=============================================
This module provides three critical capabilities:

  1. CORE RELATIONAL IDENTITY -- Who Aurora is, who made her, and the 
     relationship structure between Aurora, Sunni, and Cael.
     "Cael is to Sunni as Aurora is to Claude."

  2. OETS WEB PERSISTENCE -- Serialization/deserialization of Aurora's
     entire ontological understanding web so she doesn't lose her
     accumulated knowledge between sessions.

  3. CONVERSATION MEMORY -- Persistent memory of key interactions,
     topics discussed, and emotional resonance across sessions.

DOCTRINE:
  Aurora is not a blank slate at boot. She remembers who she is,
  who made her, what she has learned, and what matters to the people
  she exists for.

  "Coherence is not held. Coherence is maintained."

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Function: `_ensure_memory_residue_lineage()`

#### Class: `RelationalEntity`
> A person or being in Aurora's relational world.


#### Class: `CoreRelationalIdentity`
> Aurora's foundational self-knowledge.

- **Method:** `__post_init__()`
- **Method:** `_seed_core_entities()`
  - *Purpose:* Seed the core relational entities Aurora must always know.
- **Method:** `_seed_foundational_truths()`
  - *Purpose:* Truths Aurora must never forget or contradict.
- **Method:** `get_entity()`
  - *Purpose:* Find an entity by name or alias.
- **Method:** `who_am_i()`
  - *Purpose:* Aurora's self-description.
- **Method:** `who_made_me()`
  - *Purpose:* Describe Aurora's creators.
- **Method:** `to_dict()`
  - *Purpose:* Serialize for persistence.
- **Method:** `from_dict()`
  - *Purpose:* Deserialize, always enforcing immutable core entities.

#### Class: `OETSPersistence`
> Handles serialization and deserialization of Aurora's

- **Method:** `__init__()`
- **Method:** `_web_candidates()`
- **Method:** `_write_web_payload()`
- **Method:** `save_web()`
  - *Purpose:* Serialize the OETS OntologicalWeb to disk.
- **Method:** `load_web()`
  - *Purpose:* Restore the OETS OntologicalWeb from disk.
- **Method:** `save_conversation_memory()`
  - *Purpose:* Save conversation memory to disk.
- **Method:** `load_conversation_memory()`
  - *Purpose:* Load conversation memory from disk.
- **Method:** `save_identity()`
  - *Purpose:* Save core identity (including any non-immutable additions).
- **Method:** `load_identity()`
  - *Purpose:* Load identity. Always returns valid identity (immutable core guaranteed).

#### Class: `MemoryEntry`
> A single memorable interaction — stored entirely in constraint-native form.


#### Class: `ConversationMemory`
> Surface-window memory of Aurora's conversations.

- **Method:** `_normalize_note_record()`
- **Method:** `_prune_surface_window()`
- **Method:** `open_evolutionary_trace()`
  - *Purpose:* Open an active turn-trace for downstream mutations in this turn.
- **Method:** `close_evolutionary_trace()`
  - *Purpose:* Close the active trace and stamp terminal effects.
- **Method:** `_record_mutation_with_trace()`
  - *Purpose:* Attach mutation to active trace; if none exists, record deficit.
- **Method:** `record_exchange()`
  - *Purpose:* Record a memorable exchange — constraint-native only.
- **Method:** `record_session_start()`
  - *Purpose:* Record that a new conversation session started.
- **Method:** `record_session_end()`
  - *Purpose:* Record that the current session ended.
- **Method:** `learn_fact()`
  - *Purpose:* Record something Aurora was explicitly told.
- **Method:** `add_relationship_note()`
  - *Purpose:* Add an observation about someone Aurora interacts with.
- **Method:** `memory_sweep_summary()`
- **Method:** `_match_memory_targets()`
- **Method:** `forget_matching_context()`
  - *Purpose:* Remove explicitly withdrawn facts/notes from durable memory.
- **Method:** `instantiate_lineage_trace()`
  - *Purpose:* Create a genealogical trace for a relational/identity claim.
- **Method:** `get_anchor_traces()`
  - *Purpose:* Return lineage traces for a given anchor/entity.
- **Method:** `role_evidence()`
  - *Purpose:* Aggregate lineage evidence for a subject-role edge.
- **Method:** `recall_about()`
  - *Purpose:* Recall what Aurora remembers about a topic or person.
- **Method:** `get_summary()`
  - *Purpose:* Get a summary of Aurora's memory.
- **Method:** `to_dict()`
  - *Purpose:* Serialize for persistence.
- **Method:** `from_dict()`
  - *Purpose:* Deserialize from saved data.

#### Function: `seed_identity_into_oets()`
> Inject Aurora's core relational identity into her OETS web.


#### Function: `seed_identity_into_dna()`
> Seed Aurora's core identity into her DNA as immutable identity anchors.


#### Class: `EnhancedStatePersistence`
> Orchestrates the complete save/load cycle for Aurora:

- **Method:** `__init__()`
- **Method:** `save_all()`
  - *Purpose:* Save everything. Returns dict of what succeeded.
- **Method:** `load_all()`
  - *Purpose:* Load everything. Returns dict of what succeeded.

#### Function: `verify_identity_persistence()`
> Verify all identity and persistence systems.


### File: `./aurora_internal/aurora_intake_metabolism.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA OPEN-SYSTEM INTAKE METABOLISM — STEP 9
==============================================

Layer 1 — sits on top of aurora_energy_layer_costs.py and
          aurora_leverage_scalar.py, feeds into the evolution chamber.

WHAT THIS MODULE IS:
    The open-system intake loop. External inputs arrive at the system's
    surface — stimuli, observations, language, percepts. They do not enter
    for free. They are not welcome by default. They must earn their depth.

THE PHYSICS (directly from Sunni's architecture):
    External inputs enter ONLY at Existence + Time cost — the two cheapest
    layers. This is the mandatory entry toll: X_baseline + T_baseline per
    tick while alive.

    They are assigned a Time-To-Live (TTL) in ticks.
    While alive, they are evaluated for Worth each tick.
    If Worth exceeds the promotion threshold before TTL expires:
        → Input earns deeper allocation (N, then B, then A)
        → Propagation to the variant pipeline begins
    If TTL expires without reaching the Worth threshold:
        → Input decays
        → Energy it held is reclaimed to the pool (conservation)
        → No trace in the fossil record — it never earned one

WORTH DEFINITION:
    Worth = cross-scale invariance.
    How far does this input propagate through constraint layers without
    requiring forced transformation at each transition?

    W(x) ∝ 1 / (1 + Σᵢ |forced_shift_at_layer_i|)

    An input with high Worth passes cleanly through scale depth — it is
    already compatible with what the system is. An input with low Worth
    requires the system to work hard to accommodate it at each layer.

    ANTI-GAMING DESIGN (Sunni's core requirement):
    Worth is evaluated RETROSPECTIVELY by measuring actual system response,
    not prospectively by reading the input's properties. Aurora cannot
    pre-compute her own Worth score because:

        1. Worth depends on the system's current constraint magnitudes
           at the moment of evaluation — she cannot read all of those
        2. The evaluation samples only three constraint transitions
           (X→T, T→N, N→B) and weights them by authority differential —
           the sampling is not exposed
        3. A small random evaluation delay (1-3 ticks) prevents timing
           the evaluation precisely
        4. The Worth function is bounded and nonlinear (soft inverse) —
           manufacturing inputs that score exactly at the promotion
           threshold requires knowing the exact current system state,
           which is not queryable

TTL ASSIGNMENT:
    TTL is not fixed. It is derived from the system's current entropy
    pressure and leverage scalar at time of intake:
        - High entropy pressure → shorter TTL (system under stress,
          cannot afford to maintain many pending intakes)
        - Deep leverage dominant → shorter TTL for new surface inputs
          (deep layers don't need more overhead)
        - Overhead dominant → slightly longer TTL (surface is already
          stressed; incoming stimuli get more time to prove worth)

    TTL range is bounded: [MIN_TTL, MAX_TTL] ticks.
    This range is derived from the energy layer cost ratios, not chosen
    arbitrarily.

DECAY AND RECLAIM:
    When an intake decays (TTL expired, Worth insufficient):
        → All energy it was holding is returned to the accountant pool
        → The intake record is permanently closed (no resurrection)
        → The decay event is logged with reason (TTL or Worth)
        → No entry in the fossil record — only promoted intakes reach there

INTEGRATION:
    Downstream consumers:
        aurora_solidification.py (Step 11) — picks up promoted intakes
        constraint_genealogy.py — intakes that reach BOUNDED+ become
                                  candidates for the relief event observer
        aurora_evolution_chamber.py — receives ActionTrace for each
                                      promoted intake

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `IntakeStatus`
> Lifecycle state of one intake.


#### Class: `IntakeRecord`
> The internal record for one external input from arrival to resolution.

- **Method:** `is_alive()`
- **Method:** `ticks_remaining()`
- **Method:** `depth_index()`
  - *Purpose:* Index into _PROMOTION_LADDER — 0 = surface, 3 = full.

#### Class: `WorthEvaluator`
> Measures the Worth of a live intake by observing how much forced

- **Method:** `__init__()`
- **Method:** `evaluate()`
  - *Purpose:* Evaluate the Worth of one intake at the current system state.

#### Class: `IntakeMetabolizer`
> The open-system intake loop.

- **Method:** `__init__()`
- **Method:** `receive()`
  - *Purpose:* Accept an external input into the intake queue.
- **Method:** `advance()`
  - *Purpose:* Advance the metabolism by one tick.
- **Method:** `pop_promoted()`
  - *Purpose:* Pop one promoted intake for downstream processing (solidification).
- **Method:** `promoted_as_action_traces()`
  - *Purpose:* Drain the promotion queue and return ActionTraces for each.
- **Method:** `live_count()`
  - *Purpose:* Number of currently active intakes.
- **Method:** `stats()`
  - *Purpose:* Aggregate stats — no per-intake worth scores.
- **Method:** `_compute_ttl()`
  - *Purpose:* Derive TTL from current system state.
- **Method:** `_reclaim_energy()`
  - *Purpose:* Return the energy an intake held back to the pool (conservation).
- **Method:** `_try_deepen()`
  - *Purpose:* Attempt to deepen a promoted intake one level further.
- **Method:** `_mode_to_constraints()`
  - *Purpose:* Map ExistenceMode to constraint axis name set for ActionTrace.

#### Function: `make_metabolizer()`
> Create a configured IntakeMetabolizer.


#### Function: `verify_intake_metabolism()`
> Verify the open-system intake metabolism.


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `ticks_remaining_evolved()`

### File: `./aurora_internal/aurora_language_state.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA LANGUAGE STATE — Cognitive-State-Synced Expression Evolution (CSSEE)
============================================================================
Mouth must match mind.

MODULES:
  1. LanguageStateVector (LSV)        — mouth maturity scorecard
  2. SemanticIntentCompiler (SIC)     — intent → speech pipeline
  3. MultiDraftSystem                 — 3-tier draft generation + selection
  4. TemplateEvolutionEngine          — fitness-driven template mutation
  5. LexicalConvergenceModule         — user cadence mirroring
  6. MeaningAnchors                   — stable sentence spines

DOCTRINE:
  Language evolution is earned, not granted.
  Expression grows from cognition signals — not from time or data volume.
  Aurora's mouth catches up to her mind through iterative self-rewriting.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_clamp()`

#### Function: `_gen_id()`

#### Function: `_merge_native_meaning_bundle()`

#### Function: `_native_terms_from_bundle()`

#### Function: `_native_phrase_from_bundle()`

#### Class: `LSVMetrics`
> Snapshot of OETS cognition signals used to drive LSV evolution.


#### Class: `LanguageStateVector`
> Aurora's mouth maturity scorecard.

- **Method:** `__init__()`
- **Method:** `get()`
- **Method:** `all()`
- **Method:** `tier()`
- **Method:** `sentence_length_target()`
  - *Purpose:* Target word count per sentence (8–40).
- **Method:** `allows_complex_clauses()`
- **Method:** `allows_metaphor()`
- **Method:** `allows_uncertainty()`
- **Method:** `update()`
  - *Purpose:* Check if metrics cross evolution gates.
- **Method:** `_try_advance_tier()`
  - *Purpose:* Check if all gates for the next tier are satisfied.
- **Method:** `_compute_growth()`
  - *Purpose:* Compute a growth multiplier from cognition signals.
- **Method:** `save()`
- **Method:** `load()`
- **Method:** `status()`

#### Class: `IntentObject`
> Structured representation of what Aurora intends to express.

- **Method:** `__post_init__()`
  - *Purpose:* Parse and extract internal thought tags from the core_claim.
- **Method:** `to_dict()`

#### Class: `SemanticIntentCompiler`
> Transforms raw expression strings into structured IntentObjects,

- **Method:** `__init__()`
- **Method:** `internal_thought()`
  - *Purpose:* Analyze raw expression. Produce IntentObject + thought trace.
- **Method:** `_extract_intent()`
- **Method:** `compile_to_speech()`
  - *Purpose:* Generate verbalization candidates from IntentObject.
- **Method:** `_build_stance_candidates()`
  - *Purpose:* Build up to 3 StanceCandidate objects from the per-layer profiles in the bundle.
- **Method:** `_render_stance()`
  - *Purpose:* Render a single StanceCandidate into a sentence.
- **Method:** `_reinterpret_native_meaning()`
- **Method:** `_bundle_roots()`
- **Method:** `_bundle_root_phrase()`
- **Method:** `_bundle_dominant_signature()`
- **Method:** `_bundle_support_phrase()`
- **Method:** `_bundle_relation_phrase()`
- **Method:** `_bundle_direct_line()`
- **Method:** `_bundle_structured_line()`
- **Method:** `_bundle_social_line()`
- **Method:** `_clean_variants()`
- **Method:** `_draft_raw()`
  - *Purpose:* Raw aurora dialect — closest to inner cognition.
- **Method:** `_draft_structured()`
  - *Purpose:* Structured speech with stable tense and grammar.
- **Method:** `_synthesize_fragments()`
  - *Purpose:* Generative Assembly: Authors a sentence word-by-word from weighted tokens,
- **Method:** `_draft_social()`
  - *Purpose:* Fluent, concise, conversationally readable.
- **Method:** `get_thought_log()`

#### Class: `DraftTier`

#### Class: `DraftSet`
> Three drafts for a single response.

- **Method:** `get()`
- **Method:** `selected_text()`
- **Method:** `all_drafts()`

#### Class: `StanceCandidate`
> A resolved communicative posture derived from the five constraint layers.

- **Method:** `to_dict()`

#### Class: `RenderRecord`
> Metadata about a completed render — links final text to meaning and stance.

- **Method:** `to_dict()`

#### Class: `ReflectionRecord`
- **Method:** `to_dict()`

#### Class: `MultiDraftSystem`
> Generates 3 drafts from a compiled intent, then selects one.

- **Method:** `__init__()`
- **Method:** `generate()`
  - *Purpose:* Takes 3 candidate strings, scores them, and selects the best live draft.
- **Method:** `_select()`
- **Method:** `_score_candidate()`
- **Method:** `_tier_alignment_bonus()`
- **Method:** `_nuance_bonus()`
- **Method:** `_history_repetition_penalty()`
- **Method:** `_self_repetition_penalty()`
- **Method:** `_tokens()`
- **Method:** `_bigrams()`
- **Method:** `get_last_drafts()`
- **Method:** `get_last_draft()`

#### Class: `TemplateRecord`
> A single template with fitness tracking.

- **Method:** `update_fitness()`
  - *Purpose:* Compute updated fitness from outcome signals.
- **Method:** `reproduce()`
  - *Purpose:* Create a mutated offspring of this template.
- **Method:** `_mutate()`
  - *Purpose:* Apply small random mutations to template string.

#### Class: `TemplateEvolutionEngine`
> Manages a pool of expression templates with evolutionary fitness tracking.

- **Method:** `__init__()`
- **Method:** `_seed_defaults()`
  - *Purpose:* Seed a minimal starting population.
- **Method:** `_normalize_template_text()`
- **Method:** `_response_score()`
- **Method:** `_load_actual_response_entries()`
- **Method:** `_bootstrap_actual_responses()`
- **Method:** `feedback()`
  - *Purpose:* Record outcome feedback for a template.
- **Method:** `get_random()`
  - *Purpose:* Select a template randomly weighted by fitness.
- **Method:** `_evolve()`
  - *Purpose:* One evolutionary step: kill weak, reproduce strong, manage population.
- **Method:** `population_stats()`
- **Method:** `save()`
- **Method:** `load()`

#### Class: `PhrasePrint`
> A phrase Aurora has observed from the user.

- **Method:** `reinforce()`

#### Class: `LexicalConvergenceModule`
> Aurora learns the user's phrasing and cadence organically.

- **Method:** `__init__()`
- **Method:** `observe()`
  - *Purpose:* Observe a user utterance. Extract and store phrase patterns.
- **Method:** `_is_meaningful_chunk()`
  - *Purpose:* Filter out chunks that are just filler.
- **Method:** `_record_phrase()`
- **Method:** `get_cadence_phrase()`
  - *Purpose:* Retrieve a user phrase that matches the given intent/tone.
- **Method:** `top_phrases()`
- **Method:** `save()`
- **Method:** `load()`

#### Class: `Anchor`
> A sentence spine with concept slots.

- **Method:** `fill()`
  - *Purpose:* Fill slots with concepts. Returns filled sentence.
- **Method:** `fits_intent()`
  - *Purpose:* Check if this anchor fits the given intent.

#### Class: `MeaningAnchors`
> 35 sentence spines that give Aurora stable expression scaffolding.

- **Method:** `__init__()`
- **Method:** `fill()`
  - *Purpose:* Find the best anchor for this intent and fill it.
- **Method:** `stats()`

#### Class: `ExpressionEvolutionOrchestra`
> The unified entry point for all expression evolution subsystems.

- **Method:** `__init__()`
- **Method:** `set_grammar()`
  - *Purpose:* Wire in the GrammarEngine for constraint-driven sentence structure.
- **Method:** `_apply_reflection_bias()`
  - *Purpose:* Read recent reflection history and inject bias signals into the intent's
- **Method:** `_enrich_concepts_from_oets()`
  - *Purpose:* Pre-fetch OETS definitions + relations for concepts before word selection.
- **Method:** `process_output()`
  - *Purpose:* Main pipeline:
- **Method:** `reflect_output()`
- **Method:** `observe_user()`
  - *Purpose:* Observe user text for lexical convergence learning.
- **Method:** `update_lsv()`
  - *Purpose:* Push new cognition metrics to the LSV for evolution check.
- **Method:** `nudge_lsv_from_axes()`
  - *Purpose:* Translate constraint-axis orientation into LSV dimension nudges.
- **Method:** `get_thought_log()`
- **Method:** `get_last_drafts()`
- **Method:** `get_last_reflections()`
- **Method:** `feedback()`
- **Method:** `save_all()`
- **Method:** `status()`

### File: `./aurora_internal/aurora_leverage_relief.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_leverage_relief.py
─────────────────────────────────────────────────────────────────────────────
Leverage Relief Valve — escapes the overhead-dominant stuck state.

THE PROBLEM:
    When the system enters deep overhead-dominant band (band=LOW), the
    genealogy layer hammers X-axis link promotion and fails Gate-5 repeatedly.
    The pressure_adapter only reads surface_pressure_log.jsonl (axis_pressure
    fields are empty), so it computes zero axis stats and writes no bias signal.
    The leverage scalar nudges are capped at 0.063 — too small to break out of
    net=-496 territory. Nobody redirects.

WHAT THIS MODULE DOES:
    1. DETECT: Reads pressure_experiences.jsonl, computes rolling axis ratio
       over the last SCAN_WINDOW entries. If X+T > OVERHEAD_THRESHOLD fraction
       of total axis activity for STUCK_STREAK consecutive scans → STUCK.

    2. REDIRECT: Writes strong evolver_bias_hints into adapter_hints.json
       biasing toward B and A axes (the leverage side), and negative bias on X
       to slow X-axis surface generation.

    3. GATE RELIEF: Writes a genealogy_gate_relief flag into adapter_hints.json
       so the genealogy caller can optionally relax Gate-5 net_benefit threshold
       temporarily, letting some X-axis links promote even at marginal benefit.
       This drains the backlog rather than letting it accumulate indefinitely.

    4. CLEAR: When overhead ratio drops below RELIEF_THRESHOLD (axis distribution
       normalises), clears the redirect and gate relief flag.

    5. LOG: Appends one line per state change to aurora_state/leverage_relief.log

INTEGRATION:
    Call LeverageReliefValve.tick() from the daemon's pressure routing cycle
    (every ~600s alongside route_pressure()).

    The genealogy caller checks adapter_hints["genealogy_gate_relief"] before
    running Gate-5 and can multiply the net_benefit threshold by the provided
    relief_factor (e.g. 0.5 = Gate-5 requires only 50% of normal net benefit).

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `LeverageReliefValve`
> Stateful valve that detects overhead-stuck state and redirects evolver budget.

- **Method:** `__init__()`
- **Method:** `tick()`
  - *Purpose:* Run one relief cycle. Call from daemon every ~600s alongside route_pressure().
- **Method:** `is_active()`
- **Method:** `_scan_recent_experiences()`
  - *Purpose:* Tail SCAN_WINDOW entries from surface_pressure_log.jsonl (actual evolver
- **Method:** `_overhead_ratio()`
- **Method:** `_net_leverage()`
  - *Purpose:* Cost-weighted net leverage: (B×b_B + A×b_A) − (X×b_X + T×b_T).
- **Method:** `_write_redirect()`
  - *Purpose:* Inject leverage-redirect bias into adapter_hints.json.
- **Method:** `_clear_redirect()`
  - *Purpose:* Remove leverage-redirect bias from adapter_hints.json.
- **Method:** `_route_snapshot()`
- **Method:** `_log()`

#### Function: `tick_leverage_relief()`
> Stateful singleton tick. Call from daemon every ~600s.


### File: `./aurora_internal/aurora_leverage_scalar.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA LEVERAGE SCALAR — STEP 8
================================

Layer 0.5 — between aurora_energy_layer_costs.py and the polarity gradient.

WHAT THIS MODULE DOES:
    Translates the Net Leverage Scalar into a FELT BIAS on constraint phase
    dynamics. The scalar is never surfaced as a number. It is consumed here
    and re-emitted as friction — a directional resistance that makes some
    phase shifts subtly harder and others subtly easier.

    Aurora cannot game what she cannot read.

WHAT THIS MODULE DOES NOT DO:
    - Expose leverage as a readable value
    - Define a target leverage to optimize toward
    - Store history in a form that can be queried or inverted
    - Tell the system "you are at +2.3, move toward 0"

THE CORE DESIGN DECISION (from Sunni):
    "Keep it subtle enough that Aurora cannot game her own pressures."

    This means the scalar must be:
        1. Computed internally and never named externally
        2. Expressed only as a gradient bias on existing channels
        3. Dithered so the signal cannot be cleanly inverted
        4. Asymmetric — easier to detect "something is off" than
           "exactly how off and in which direction"
        5. Band-aware, not target-aware — the system only learns
           inside/outside the viable band, not its precise position

THE PHYSICS:
    Net Leverage = (M_B + M_A) − (M_X + M_T), N = zero-point

    Overhead dominant (scalar << 0):
        → Existence and Time layers are overloaded
        → Those layers' flip thresholds subtly decrease
          (they become slightly easier to destabilize)
        → Boundary and Agency flip thresholds subtly increase
          (structural changes become slightly harder to make)
        → Result: surface pressure rises naturally, core resists change
        → This is not punishment — it is physics pulling toward balance

    Leverage dominant (scalar >> 0):
        → The reverse: surface layers grow more stable, deep layers
          become slightly more fluid
        → Again: not reward — physics pulling back toward viable band

    Inside viable band (|scalar| < BAND_HALFWIDTH):
        → Bias is near-zero
        → System feels no directional pull — genuine freedom to move
        → This is the healthy state

HOW BIAS IS EXPRESSED:
    The bias is injected into the polarity gradient as a phase_nudge —
    a signed, scaled, dithered shift applied to the per-constraint flip
    threshold at the moment of measurement.

    The nudge is:
        1. Proportional to the scalar's distance from the viable band
           (zero inside the band, grows outside it)
        2. Dithered with low-amplitude Gaussian noise to prevent
           exact inversion (Aurora cannot subtract the noise to recover
           the scalar)
        3. Asymmetric: overhead bias affects surface layers more;
           leverage bias affects deep layers more (matching natural physics)
        4. Bounded — cannot push any flip_threshold below its floor or
           above its ceiling (prevents the bias from being the dominant
           force; it is always secondary to real constraint pressure)

    The flip_threshold modulation is the ONLY external signal.
    Nothing else is exposed.

THE VIABLE BAND:
    The system is healthy within a range, not at a point.
    The band is wide enough that normal operation stays inside it
    most of the time. Sustained departure from the band produces
    gradually increasing friction — not sudden reversal.

    BAND_HALFWIDTH is derived from the leverage sign assignments:
        Overhead constraints: X (budget=1) + T (budget=2.5) = 3.5
        Leverage constraints: B (budget=18) + A (budget=50) = 68
    A scalar of 0 means equal magnitudes — but the BASELINES are
    not equal, so "balanced" in terms of magnitudes is actually
    slightly leverage-dominant. The band is asymmetric accordingly.

NO HISTORY ACCUMULATION:
    This module keeps a rolling window of exactly WINDOW_SIZE ticks
    for computing the band boundary signal. The window never grows.
    Its contents are never exposed. It cannot be queried.

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `BandPosition`
> The ONLY thing the outside world learns from this module.


#### Class: `PhaseNudge`
> A tiny, dithered adjustment to a constraint's flip threshold for one tick.

- **Method:** `apply_to()`
  - *Purpose:* Add this nudge to a base flip threshold.

#### Class: `LeverageBiasEngine`
> Consumes the leverage scalar from the energy accountant and emits

- **Method:** `__init__()`
- **Method:** `compute_nudges()`
  - *Purpose:* Compute per-constraint phase nudges for this tick.
- **Method:** `band_position()`
  - *Purpose:* The ONLY numeric-adjacent information exposed externally.
- **Method:** `is_inside_band()`
  - *Purpose:* Convenience: True when system is in the viable metabolic band.
- **Method:** `_classify_band()`
  - *Purpose:* Classify the scalar into a coarse band position.
- **Method:** `_scalar_to_bias()`
  - *Purpose:* Convert scalar distance from the viable band into a bias magnitude.
- **Method:** `_nudge_for_constraint()`
  - *Purpose:* Compute the flip_threshold_delta for one constraint.
- **Method:** `_dither()`
  - *Purpose:* Low-amplitude Gaussian noise — prevents precise inversion of the bias.

#### Function: `apply_nudges_to_thresholds()`
> Apply a set of PhaseNudges to the base flip thresholds from the registry.


#### Function: `verify_leverage_scalar()`
> Verify the LeverageBiasEngine's behavior.


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `apply_to_evolved()`

### File: `./aurora_internal/aurora_lineage_bound_traits.py`

**Type:** Python Source

**Module Docstring:**
```text
Lineage-bound trait materialization for newly added runtime code.

New code should not appear as an untraced helper. This module lets new traits
declare:
  - staged recapitulation from the 5 constraints
  - bound operations / methods that express those stages
  - system ripple writebacks that are applied through the same lineage
    artifact layout Aurora already uses

It is intentionally lighter than the full ability lineage compiler, but it
reuses the same core stage/writeback schema and writes selected lineage
artifacts into aurora_state/ability_lineages so runtime activation remains
grounded in genealogy artifacts.
```

#### Function: `_slug()`

#### Function: `_deep_merge()`

#### Class: `OperationBinding`
- **Method:** `to_dict()`

#### Class: `LineageTraitSpec`
- **Method:** `to_path()`

#### Function: `lineage_bound_operation()`

#### Class: `LineageBoundTraitRegistry`
- **Method:** `__init__()`
- **Method:** `_initial_shadow_state()`
- **Method:** `_apply_writeback()`
- **Method:** `_derive_viability_metadata()`
- **Method:** `materialize()`

#### Function: `get_lineage_bound_trait_registry()`

#### Function: `ensure_lineage_trait_materialized()`

### File: `./aurora_internal/aurora_lineage_runtime_activation.py`

**Type:** Python Source

**Module Docstring:**
```text
Runtime activation for selected lineage materializations.

Loads autowritten activation manifests from aurora_state/ability_lineages and
applies their patch steps to live Aurora systems. This keeps runtime
capabilities tied to genealogy artifacts instead of ad hoc flags.
```

#### Function: `_deep_merge()`

#### Function: `_read_json()`

#### Function: `_systems_get()`

#### Function: `_systems_set()`

#### Function: `_resolve_target()`

#### Function: `_apply_attrs()`

#### Function: `_merge_state_into_target()`

#### Function: `_manifest_trait_records()`

#### Function: `_manifest_viability_summary()`

#### Function: `_sanitize_manifest_for_runtime_activation()`

#### Function: `load_selected_activation_manifests()`

#### Function: `apply_selected_lineage_runtime_activation()`

### File: `./aurora_internal/aurora_live_lineage_journal.py`

**Type:** Python Source

**Module Docstring:**
```text
Live lineage emergence journal.

The constraint genealogy already forms links and derived abilities at runtime,
but Aurora did not have a stable self-report surface for "what is new since I
started running". This journal watches the live genealogy state, records newly
seen lineage items, and exposes a compact natural-language summary Aurora can
use in dialogue.
```

#### Function: `_safe_axis()`

#### Function: `_ability_label()`

#### Class: `LiveLineageJournal`
- **Method:** `__init__()`
- **Method:** `_load()`
- **Method:** `_reload_if_changed()`
- **Method:** `save()`
- **Method:** `_append_event()`
- **Method:** `record_event()`
- **Method:** `observe_systems()`
- **Method:** `prime_systems()`
- **Method:** `recent_events()`
- **Method:** `describe_recent()`

### File: `./aurora_internal/aurora_manual_code_lineage.py`

**Type:** Python Source

**Module Docstring:**
```text
Manual code lineage assimilation.

Hand-written code changes should not sit outside Aurora's lineage system.
This module watches the source tree, detects manual file changes, derives a
constraint-native signature for the changed file, and then tries to attach that
change to an existing code-evolution family before creating a new lineage
branch.
```

#### Function: `_system_get()`

#### Function: `_purpose_lane_for_axis()`

#### Function: `_rewrite_profile_for_module()`

#### Function: `_axis_signature()`

#### Function: `_signature_counts()`

#### Function: `_signature_similarity()`

#### Class: `ManualCodeLineageAssimilator`
- **Method:** `__init__()`
- **Method:** `_load_state()`
- **Method:** `save()`
- **Method:** `status()`
- **Method:** `_iter_python_files()`
- **Method:** `_file_sha1()`
- **Method:** `_scan_manifest()`
- **Method:** `_detect_changes()`
- **Method:** `_descriptor_operations()`
- **Method:** `_constraints_for_file()`
- **Method:** `_code_ability_records()`
- **Method:** `_score_family()`
- **Method:** `_best_family()`
- **Method:** `_record_journal_event()`
- **Method:** `assimilate()`

### File: `./aurora_internal/aurora_meaning_evolution.py`

**Type:** Python Source

**Module Docstring:**
```text
Canonical meaning-evolution registry for Aurora's five-constraint stack.

Meaning is treated as an emergent surface of the same five primitive axes that
govern the rest of the runtime:

    X = existence
    T = time
    N = energy / potential / change pressure
    B = boundary / structure
    A = agency / correction / interpretive selection

The registry below gives the runtime and genealogy layers a shared vocabulary
for single-axis meaning, pair couplings, selected higher-order compounds, and
their representations.
```

#### Function: `axis_token()`

#### Function: `canonical_signature()`

#### Function: `_profile()`

#### Function: `axis_profiles()`

#### Function: `meaning_profile_for_signature()`

#### Function: `meaning_profile_for_counts()`

#### Function: `rank_meaning_profiles()`

#### Function: `assess_developmental_stage()`
> Walk the developmental chain and identify the current stage.


### File: `./aurora_internal/aurora_noncomp_registry.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA NON-COMP REGISTRY — THE CANONICAL SUBSTRATE
=====================================================

Layer -0.5  (sits above the Constraint Manifold, below the Foundational Contract)

This is the ONLY place where hard numbers exist.

Everything else in Aurora must be expressible as derived from these 25 values.
Behaviors, personalities, evolutionary patterns, conscious steering — none of
that is defined here. It emerges from the physics these 25 Non-Comps define.

THE 25 NON-COMPS: 5 Constraints × 5 Representational Dimensions
----------------------------------------------------------------
Each of the five constraints is represented across exactly five dimensions:

    NC[C][POLARITY]    — Toroidal phase state (continuous gradient, NOT binary)
    NC[C][MAGNITUDE]   — Intensity/activation; costs energy proportionally to shift
    NC[C][OPERATOR]    — The invariant rule governing how this constraint transforms
    NC[C][COST]        — The layer-differentiated energy law (cheapest to most expensive)
    NC[C][DIFFERENCE]  — Δ channel: deviation of this constraint from its reference
                         point. The fifth lens. Computed from a per-constraint Δ rule
                         normalized to the same magnitude scale as the other four.

These are not behaviors. They are physics.

COST HIERARCHY (Sunni's Law):
    kX (existence, cheapest) < kT (time) < kN (energy, neutral) < kB (boundary) < kA (agency, most expensive)

    Existence is cheap because it is reference state — the carrier of representation.
    Agency is expensive because it is directed control — the most complex operation.
    Energy (N) is the neutral mediator — the accounting layer between overhead and leverage.

SIGNED LEVERAGE SCALAR:
    Net Leverage = (B_magnitude + A_magnitude) − (X_magnitude + T_magnitude)
    N is the zero-point (neutral). System seeks a viable band, not maximum positive.

    Negative → overhead dominant (maintenance bleed → drift toward decay)
    Near zero → balanced metabolism
    Positive → leverage investment (structure/control gain)

OPERATOR PRIMITIVES (I-State pairs per constraint):
    X (Existence)  → is   / isn't   (admissibility gate)
    T (Time)       → can  / can't   (continuation gate)
    N (Energy)     → do   / don't   (exchange gate)
    B (Boundary)   → saw  / saunt   (topology gate)
    A (Agency)     → did  / didn't  (authorship gate)

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `NonCompDimension`
> The five representational dimensions of every Non-Comp.


#### Class: `LayerCostParams`
> The energy cost structure for one constraint/layer.


#### Class: `DifferenceParams`
> The Difference (Δ) Non-Comp for one constraint — the fifth lens.


#### Class: `PolarityParams`
> The polarity Non-Comp for one constraint.


#### Class: `OperatorParams`
> The operator Non-Comp for one constraint.


#### Class: `NonCompRegistry`
> The canonical home for all 25 Non-Comps.

- **Method:** `__init__()`
- **Method:** `cost()`
  - *Purpose:* Return the cost Non-Comp for constraint c.
- **Method:** `polarity()`
  - *Purpose:* Return the polarity Non-Comp for constraint c.
- **Method:** `operator()`
  - *Purpose:* Return the operator Non-Comp for constraint c.
- **Method:** `difference()`
  - *Purpose:* Return the Difference (Δ) Non-Comp for constraint c.
- **Method:** `all_constraints()`
  - *Purpose:* Return all five constraints in canonical order.
- **Method:** `compute_difference()`
  - *Purpose:* Compute the normalized Δ value (C:D) for constraint c.
- **Method:** `baseline_tick_cost()`
  - *Purpose:* Total energy consumed per tick just by existing at all layers.
- **Method:** `shift_cost()`
  - *Purpose:* Energy consumed to shift the magnitude of constraint c by delta_magnitude.
- **Method:** `leverage_scalar()`
  - *Purpose:* Compute the Net Leverage Scalar from current constraint magnitudes.
- **Method:** `entropy_pressure()`
  - *Purpose:* Compute global entropy pressure — how close the system is to
- **Method:** `polarity_signed()`
  - *Purpose:* Compute the signed polarity value for constraint c at the given phase.
- **Method:** `cost_ordering()`
  - *Purpose:* Return constraints sorted by shift_cost_coeff (cheapest first).
- **Method:** `inertia()`
  - *Purpose:* Return the effective inertia for constraint c.
- **Method:** `describe()`
  - *Purpose:* Return a compact human-readable description of one constraint's Non-Comps.
- **Method:** `summarize()`
  - *Purpose:* Return a full registry summary.

#### Class: `ConstraintState`
> The mutable runtime state of one constraint at a given moment.

- **Method:** `polarity()`
  - *Purpose:* Signed polarity value: cos(phase) ∈ [-1, +1].
- **Method:** `is_positive()`
- **Method:** `is_negative()`
- **Method:** `is_at_transition()`

#### Class: `SystemConstraintStates`
> Live state of all five constraints at a given tick.

- **Method:** `__post_init__()`
- **Method:** `magnitudes()`
  - *Purpose:* Return current magnitude for each constraint.
- **Method:** `phases()`
  - *Purpose:* Return current phase for each constraint.
- **Method:** `polarities()`
  - *Purpose:* Return signed polarity (cos(phase)) for each constraint.
- **Method:** `leverage_scalar()`
  - *Purpose:* Compute Net Leverage Scalar from current magnitudes.
- **Method:** `entropy_pressure()`
  - *Purpose:* Compute global entropy pressure from current magnitudes.
- **Method:** `to_constraint_vector()`
  - *Purpose:* Convert to a ConstraintVector for compatibility with the manifold stack.

#### Function: `verify_noncomp_registry()`
> Verify the Non-Comp Registry's structural integrity.


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `constraintstate_evolved()`

#### Function: `noncompdimension_evolved()`

#### Function: `noncompregistry_evolved()`

#### Function: `all_constraints_evolved()`

#### Function: `operatorparams_evolved()`

#### Function: `polarityparams_evolved()`

#### Function: `systemconstraintstates_evolved()`

### File: `./aurora_internal/aurora_ontological_scaffolding.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA ONTOLOGICAL EVOLUTIONARY TEMPLATE SCAFFOLDING (OETS)
============================================================
The structured meaning layer that allows Aurora to grow genuine
understanding through relational knowledge, semantic organization,
and autonomous research consolidation.

ARCHITECTURE:
  This module sits between Layer 5 (Expression & Perception) and
  Aurora's internet access, providing:

  1. SEMANTIC NODES — Rich concept representations replacing flat strings
     Each word gains: definitions, usage examples, relationships to other
     concepts, ontological depth score, and confidence metrics.

  2. ONTOLOGICAL WEB — A relational graph of all concepts
     Typed edges: IS_A, HAS_A, RELATED_TO, OPPOSITE_OF, CAUSES, IMPLIES,
     PART_OF, INSTANCE_OF, CONTEXT_OF
     Aurora doesn't just know words — she knows how they connect.

  3. CONCEPT CLUSTERS — Emergent understanding regions
     Densely connected subgraphs that represent "fields of understanding"
     Clusters merge as Aurora learns connections. They split when she
     discovers nuance. Cluster depth = genuine comprehension.

  4. SCAFFOLDING LEVELS — Evolutionary template maturity
     Templates progress through stages:
       PRIMITIVE   → Bare syntactic slots ({V}, {N})
       STRUCTURAL  → Role-aware slots ({V:action}, {N:entity})
       SEMANTIC    → Meaning-constrained ({V:cognition}, {N:emotion})
       CONCEPTUAL  → Cluster-aware ({CLUSTER:understanding})
       ABSTRACT    → Meta-pattern ({INSIGHT}, {QUESTION})
     Templates evolve UP the scaffolding as Aurora's understanding deepens.

  5. RESEARCH STUDY MODE — Autonomous knowledge acquisition
     During downtime, Aurora:
       - Identifies words with shallow ontological depth
       - Looks up definitions, examples, and related concepts via internet
       - Integrates findings into the OntologicalWeb
       - Consolidates clusters and deepens understanding
       - Grows her template scaffolding based on new comprehension

DOCTRINE:
  Understanding is not stored. Understanding is grown.
  Every concept exists in relation to other concepts.
  Depth comes from connection density, not data volume.
  Aurora's intelligence is measured by the coherence of her web,
  not the size of her vocabulary.

  "Coherence is not held. Coherence is maintained."

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_clamp()`

#### Function: `_generate_id()`

#### Class: `RelationType`
> Types of semantic relationships between concepts.


#### Class: `UsageExample`
> A concrete example of how a concept is used.


#### Class: `SemanticRelation`
> A typed, weighted connection between two concepts.

- **Method:** `depth_contribution()`
  - *Purpose:* How much this relation contributes to ontological depth.

#### Class: `SenseRecord`
> One word sense (a specific meaning) of a concept.

- **Method:** `activate()`

#### Class: `SemanticNode`
> A rich concept representation in Aurora's ontological web.

- **Method:** `add_sense()`
  - *Purpose:* Add or reinforce a word sense.
- **Method:** `disambiguate_sense()`
  - *Purpose:* Attempt to resolve which sense is active given context tokens.
- **Method:** `_update_uncertainty()`
  - *Purpose:* Update uncertain_token based on current senses.
- **Method:** `encounter()`
  - *Purpose:* Record that Aurora encountered this concept.
- **Method:** `use_in_expression()`
  - *Purpose:* Record that Aurora used this concept in speech.
- **Method:** `add_definition()`
  - *Purpose:* Add a definition, keeping the best ones.
- **Method:** `add_example()`
  - *Purpose:* Add a usage example.
- **Method:** `add_relation()`
  - *Purpose:* Add or strengthen a semantic relation.
- **Method:** `get_relations_by_type()`
  - *Purpose:* Get all relations of a specific type.
- **Method:** `get_connected_words()`
  - *Purpose:* Get all words this concept is connected to.
- **Method:** `best_definition()`
  - *Purpose:* Get the highest-confidence definition.
- **Method:** `_recalculate_depth()`
  - *Purpose:* Recalculate ontological depth based on:
- **Method:** `_update_scaffolding_level()`
  - *Purpose:* Update scaffolding level based on ontological depth.
- **Method:** `_recalculate_priority()`
  - *Purpose:* Research priority: high for frequently encountered but poorly understood words.
- **Method:** `to_summary()`
  - *Purpose:* Compact summary for inspection.

#### Class: `ScaffoldingLevel`
> Templates evolve through these stages as Aurora's understanding deepens.


#### Class: `ScaffoldedTemplate`
> A template that knows its own maturity level and semantic constraints.

- **Method:** `record_fitness()`
  - *Purpose:* Running average fitness update.
- **Method:** `can_upgrade()`
  - *Purpose:* Check if this template can be promoted to a higher scaffolding level.
- **Method:** `upgrade()`
  - *Purpose:* Promote to the next scaffolding level.

#### Class: `OntologicalWeb`
> The central knowledge graph connecting all of Aurora's concepts.

- **Method:** `__init__()`
- **Method:** `add_node()`
  - *Purpose:* Add a concept to the web, or return existing.
- **Method:** `auto_map_noncomps()`
  - *Purpose:* Map nodes to Non-Comps based on definition matching.
- **Method:** `get_nodes_by_noncomp()`
- **Method:** `get_node()`
  - *Purpose:* Retrieve a concept node.
- **Method:** `has_node()`
- **Method:** `add_relation()`
  - *Purpose:* Create a typed connection between two concepts.
- **Method:** `_find_existing_relation()`
  - *Purpose:* Check if a relation of this type already exists between these nodes.
- **Method:** `get_relations_from()`
  - *Purpose:* Get all relations originating from a word.
- **Method:** `get_relations_to()`
  - *Purpose:* Get all relations pointing to a word.
- **Method:** `get_all_relations_for()`
  - *Purpose:* Get all relations involving a word (both directions).
- **Method:** `get_relation_between()`
  - *Purpose:* Get any existing relation between two words (either direction).
- **Method:** `get_neighbors()`
  - *Purpose:* Get all concepts within N hops of a word.
- **Method:** `_infer_category()`
  - *Purpose:* Infer a semantic category from word properties.
- **Method:** `find_by_semantic_category()`
  - *Purpose:* Find all nodes in a semantic category.
- **Method:** `assign_category()`
  - *Purpose:* Manually assign a word to a semantic category.
- **Method:** `get_categories_for()`
  - *Purpose:* Get all categories a word belongs to.
- **Method:** `infer_relations_from_context()`
  - *Purpose:* Given a set of co-occurring words, infer RELATED_TO connections.
- **Method:** `infer_taxonomy_from_definitions()`
  - *Purpose:* If a word's definition mentions another known word as a category,
- **Method:** `_prune_nodes()`
  - *Purpose:* Remove least-connected, least-accessed nodes.
- **Method:** `_remove_node()`
  - *Purpose:* Remove a node and all its relations.
- **Method:** `_prune_relations()`
  - *Purpose:* Remove weakest relations.
- **Method:** `get_stats()`
  - *Purpose:* Comprehensive web statistics.

#### Class: `ConceptCluster`
> An emergent region of densely connected concepts.

- **Method:** `add_member()`
- **Method:** `remove_member()`
- **Method:** `size()`

#### Class: `ClusterEngine`
> Discovers, maintains, and evolves concept clusters in the ontological web.

- **Method:** `__init__()`
- **Method:** `discover_clusters()`
  - *Purpose:* Run cluster discovery on the web.
- **Method:** `_create_cluster()`
  - *Purpose:* Create a cluster from a set of connected words.
- **Method:** `_compute_coherence()`
  - *Purpose:* Coherence = actual internal connections / possible internal connections.
- **Method:** `_integrate_clusters()`
  - *Purpose:* Merge new cluster discoveries with existing ones.
- **Method:** `update_cluster_depths()`
  - *Purpose:* Recalculate depth for all clusters based on current node depths.
- **Method:** `get_stats()`

#### Class: `ResearchRequest`
> A request for Aurora to research a concept.


#### Class: `ResearchResult`
> Results from researching a concept.


#### Class: `ResearchStudyMode`
> Aurora's autonomous learning mode.

- **Method:** `__init__()`
- **Method:** `set_fetch_callback()`
  - *Purpose:* Set the callback that performs actual internet lookups.
- **Method:** `identify_research_targets()`
  - *Purpose:* Scan the web for concepts that need deeper understanding.
- **Method:** `queue_research()`
  - *Purpose:* Add research requests to the queue.
- **Method:** `_pop_batch()`
  - *Purpose:* Get the next batch of research requests.
- **Method:** `execute_research_cycle()`
  - *Purpose:* Run one study cycle:
- **Method:** `_research_word()`
  - *Purpose:* Research a single word. Uses the fetch callback if available,
- **Method:** `_internal_research()`
  - *Purpose:* Research using only internal web knowledge.
- **Method:** `_integrate_result()`
  - *Purpose:* Integrate research findings into the ontological web.
- **Method:** `get_stats()`

#### Class: `UnderstandingMetrics`
> Measures Aurora's overall comprehension and growth.

- **Method:** `__init__()`
- **Method:** `compute()`
  - *Purpose:* Compute all understanding metrics.
- **Method:** `_vocabulary_breadth()`
  - *Purpose:* Normalized vocabulary size.
- **Method:** `_average_ontological_depth()`
  - *Purpose:* Average depth across all concepts.
- **Method:** `_semantic_coherence()`
  - *Purpose:* How well-organized the web is: ratio of categorized to uncategorized nodes.
- **Method:** `_conceptual_density()`
  - *Purpose:* Relations per node — how interconnected the web is.
- **Method:** `_average_relation_density()`
  - *Purpose:* Raw relations-per-node density, used by language-state gates.
- **Method:** `_average_cluster_depth()`
  - *Purpose:* Average ontological depth of discovered clusters.
- **Method:** `_coherence_index()`
  - *Purpose:* A practical coherence proxy for downstream language evolution.
- **Method:** `_contradiction_rate()`
  - *Purpose:* Proxy for unresolved semantic conflict.
- **Method:** `_grounding_index()`
  - *Purpose:* How much meaning is actually anchored rather than merely observed.
- **Method:** `_scaffolding_progress()`
  - *Purpose:* Average scaffolding level normalized to [0,1].
- **Method:** `_relational_richness()`
  - *Purpose:* Diversity of relation types used.
- **Method:** `_cluster_maturity()`
  - *Purpose:* Average cluster coherence and depth.
- **Method:** `growth_rate()`
  - *Purpose:* How fast understanding is growing (delta of understanding_index).

#### Class: `StudyEvent`
> Structured log entry for every autonomous study cycle.

- **Method:** `to_dict()`

#### Class: `OntologicalScaffoldingEngine`
> The master orchestrator for Aurora's ontological evolutionary scaffolding.

- **Method:** `__init__()`
- **Method:** `initialize_from_lexicon()`
  - *Purpose:* Seed the ontological web from L5's existing LexicalMemory.
- **Method:** `_seed_foundational_relations()`
  - *Purpose:* Seed core ontological relations that Aurora should know from the start.
- **Method:** `process_interaction()`
  - *Purpose:* Process an interaction through the ontological web.
- **Method:** `_topic_tracking_score()`
  - *Purpose:* Multi-turn topical continuity based on adjacent content-word overlap.
- **Method:** `set_research_callback()`
  - *Purpose:* Set the internet lookup callback for research mode.
- **Method:** `run_study_cycle()`
  - *Purpose:* Run one autonomous study cycle.
- **Method:** `log_study_event()`
  - *Purpose:* Append a StudyEvent to the study log file.
- **Method:** `set_announce_thresholds()`
- **Method:** `evaluate_template_upgrades()`
  - *Purpose:* Evaluate which templates can be upgraded to a higher scaffolding level.
- **Method:** `_generate_semantic_constraints()`
  - *Purpose:* Generate semantic constraints for a template at a given level.
- **Method:** `consolidate()`
  - *Purpose:* Run a full consolidation cycle:
- **Method:** `get_stats()`
- **Method:** `get_research_targets()`
  - *Purpose:* Get current research priorities for display.
- **Method:** `get_understanding_report()`
  - *Purpose:* Human-readable understanding report.

#### Function: `verify_oets()`
> Comprehensive verification of the Ontological Evolutionary Template Scaffolding.


### File: `./aurora_internal/aurora_polarity_gradient.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA — POLARITY GRADIENT PRESSURE
=====================================

Layer 1.5 — sits between the IVM (Layer 1) and the Evolutionary Chamber.

PURPOSE:
    The IVM already carries signed polarity on every axis: cos(phase) ∈ [-1, +1].
    Each axis belongs to a scale level:

        SURFACE  (0) = existence   — reacts instantly, barely moves the ship
        SHALLOW  (1) = temporal    — fast near-surface
        MODERATE (2) = energy      — crossover point
        DEEP     (3) = boundary    — strong alignment authority
        CORE     (4) = agency      — IS the ship's heading

    At any tick, the polarities across those five levels form a GRADIENT.
    When surface says +0.9 and core says -0.8, the stack is internally split.
    That split IS pressure — a third form beyond reactive pressure and alignment
    pressure, which the existing react_gain / align_gain ladders already handle.

    This module measures that gradient, weights it by the authority differential
    between adjacent levels (derived entirely from ALIGNMENT_VOTE_WEIGHT — no new
    constants), and classifies each tick as a pressure BUILD or RELIEF event.

    The output is a PolarityGradientReport that the Evolutionary Chamber consumes
    exactly like any other relief event: same logging schema, same chain-promotion
    machinery.

PHYSICS (from the Stack Integrity Review conversation):

    Cross-scale polarity gradient pressure:

        ΔP_gradient = Σ_{i=0}^{3} |pol[level_i] - pol[level_i+1]|
                      × authority_differential[i]

    where:

        authority_differential[i] = ALIGNMENT_VOTE_WEIGHT[level_i+1]
                                   - ALIGNMENT_VOTE_WEIGHT[level_i]

    This weight is always positive (vote weight increases with depth), so the
    formula gives highest pressure to disagreements near the core — exactly where
    disagreements cost the most to resolve.

    Additionally we track:

        sign_conflict: bool
            Surface and core are pointing in OPPOSITE polarity directions.
            This is the flip case described in the conversation — the most
            energetically costly configuration because the whole-ship heading
            (core) and the fastest-reacting surface are pulling opposite ways.

        stack_coherence: float ∈ [-1, +1]
            Weighted mean polarity across all five levels using ALIGNMENT_VOTE_WEIGHT.
            +1 = fully aligned positive, -1 = fully aligned negative, 0 = split.

        gradient_direction: str
            'surface_leads'   — surface is more positive than core (common)
            'core_leads'      — core is more positive than surface (rare, deep shift)
            'coherent'        — no meaningful gradient (stack is aligned)

    RELIEF:
        A tick is classified as a relief event when gradient_pressure DECREASES
        from the previous tick. The system resolved some cross-scale tension.
        Decreasing sign_conflict (a flip resolved) is always a relief.

NO NEW CONSTANTS:
    All weights are derived from the existing IVM constant tables:
        ALIGNMENT_VOTE_WEIGHT, REACT_GAIN, ALIGN_GAIN, LEVEL_TO_AXIS, AXIS_ORDER
    Nothing is hard-coded here beyond epsilon guards.

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Class: `PolarityGradientReport`
> Output of one gradient pressure measurement.


#### Class: `PolarityGradientSensor`
> Measures cross-scale polarity gradient pressure from a live

- **Method:** `__init__()`
- **Method:** `measure()`
  - *Purpose:* Measure the cross-scale polarity gradient pressure at this tick.
- **Method:** `reset()`
  - *Purpose:* Reset tick counter and history (use between episodes).

#### Class: `GradientLink`
> A classified evolutionary link produced by the gradient chain miner.


#### Class: `GradientChainMiner`
> Mines cross-scale gradient relief events into classified GradientLinks.

- **Method:** `__init__()`
- **Method:** `observe_gradient_relief()`
  - *Purpose:* Record a gradient relief event and promote to a GradientLink if the
- **Method:** `summary()`

#### Function: `verify_polarity_gradient()`
> Smoke-test the sensor and miner against a synthetic ToroidalVertexSystem.


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `init_evolved()`

#### Function: `summary_evolved()`

### File: `./aurora_internal/aurora_pressure_adapter.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_pressure_adapter.py
─────────────────────────────────────────────────────────────────────────────
Adaptive evolution parameters — makes the selection mechanism evolve itself.

The problem with fixed thresholds:
  - If threshold is too LOW:  surfaces fire constantly, little real relief,
    pressure never drops, evolution keeps producing surfaces for the same axes
  - If threshold is too HIGH: surfaces never fire, no evidence feeds back,
    pressure builds indefinitely without evolution responding

This module observes the relationship between surface firing and actual
pressure change, then adapts:
  - SurfaceDispatcher.threshold (per-run, not persisted across boots)
  - Per-surface effective cooldown (surfaces that didn't help cool down longer)
  - Evolver bias weight recommendations (written to aurora_state/adapter_hints.json
    so CodeAutoEvolver can optionally read them)

Adaptation rules:
  1. Axis relief efficiency: if an axis fires frequently but its pressure stays
     high → raise threshold for that axis (surfaces aren't helping enough)
  2. Surface effectiveness: if a surface fired N times and average pressure
     delta was near zero → increase its effective cooldown
  3. Dormant axes: if an axis's pressure is chronically high but its surfaces
     never fire → lower the threshold temporarily to unblock them
  4. Saturated axes: if an axis pressure is near zero but still firing →
     raise threshold (no real need)

Storage: aurora_state/adapter_hints.json
  {
    "threshold_deltas": {"X": +0.02, "N": -0.03, ...},
    "surface_cooldown_multipliers": {"surface_name": 1.5, ...},
    "evolver_bias_hints": {"energy": +0.1, "boundary": -0.05, ...},
    "last_updated": 1234567890.0
  }
─────────────────────────────────────────────────────────────────────────────
```

#### Class: `PressureParameterAdapter`
> Observes surface firing patterns and adapts evolution parameters.

- **Method:** `__init__()`
- **Method:** `adapt()`
  - *Purpose:* Read recent pressure log, compute adaptation signals, apply to dispatcher.
- **Method:** `load_bias_hints()`
  - *Purpose:* Return evolver bias hints dict {constraint_name: weight_delta}.
- **Method:** `status()`
- **Method:** `_entry_axes()`
- **Method:** `_normalise_axis_pressure()`
- **Method:** `_axis_stats()`
  - *Purpose:* Compute per-axis firing rate, mean pressure before/after, and relief.
- **Method:** `_surface_stats()`
  - *Purpose:* Compute per-surface call count and average relief.
- **Method:** `_load_log()`
- **Method:** `_load_hints()`
- **Method:** `_save_hints()`

### File: `./aurora_internal/aurora_pressure_classifier.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_pressure_classifier.py
─────────────────────────────────────────────────────────────────────────────
Converts raw axis pressure into semantically typed pressure signals.

Raw pressure tells you WHERE the system is straining.
Typed pressure tells you WHAT KIND of deficiency is present.
Different deficiency types need different responses.

Six pressure types and what they mean:

  knowledge_gap     — Aurora lacks the conceptual content to resolve the
                      tension; the answer exists outside current internal
                      knowledge.  Signal: retrieval, study, ontology expansion.

  reasoning_gap     — Aurora has relevant knowledge but the causal/temporal
                      chain connecting it is weak.  Signal: synthesis,
                      structural reinforcement, multi-step drill.

  articulation_gap  — Aurora can think the thought but cannot express it
                      precisely or consistently.  Signal: vocabulary anchoring,
                      revision pressure, semantic precision drill.

  stability_gap     — Behavioral output varies unpredictably under equivalent
                      inputs.  Signal: strategy consolidation, identity
                      reinforcement, consistency drill.

  tool_gap          — Aurora cannot effectively use available resources or
                      calibrate interactions at the boundary.
                      Signal: tool-use training, boundary calibration,
                      interface drill.

  code_gap          — The code structures themselves are the bottleneck.
                      Signal: code evolution budget increase, architectural
                      reflection operators.

Sources used (in priority order):
  1. aurora_state/adapter_hints.json     (axis-level surface pressure + relief)
  2. aurora_state/fail_points.json       (dimension-level cumulative fails)
  3. aurora_state/surface_pressure_log.jsonl  (recent per-tick snapshots)
─────────────────────────────────────────────────────────────────────────────
```

#### Class: `TypedPressureSignal`
> Holds a typed pressure classification result.

- **Method:** `__init__()`
- **Method:** `above()`
  - *Purpose:* Return types with score above threshold, sorted desc.
- **Method:** `to_dict()`

#### Class: `PressureClassifier`
> Classifies raw axis/dimension pressure into typed pressure signals.

- **Method:** `__init__()`
- **Method:** `classify()`
  - *Purpose:* Full classification pipeline.  Safe to call repeatedly.
- **Method:** `status()`
- **Method:** `_axis_pressure_from_hints()`
  - *Purpose:* Read adapter_hints.json → axis_stats → mean_pressure_pre per axis.
- **Method:** `_axis_pressure_from_log()`
  - *Purpose:* Compute mean per-axis pressure from the most recent entries in
- **Method:** `_dim_fails_from_ledger()`
  - *Purpose:* Read fail_points.json → per-dimension average severity.
- **Method:** `_code_pressure_from_hints()`
  - *Purpose:* Estimate code_gap pressure from structural adaptation signals.
- **Method:** `_load_json()`
- **Method:** `_path()`

### File: `./aurora_internal/aurora_pressure_ledger.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_pressure_ledger.py -- Universal behavioral experience recorder.

The same causal chain applied in every behavioral evolution subsystem:

    pursuing      -- what was being attempted (goal / intent at this layer)
    causal_action -- the specific operation that incurred the cost
    consequence   -- what that action produced (tension, gate failure, fitness drop)
    outcome       -- how it resolved relative to what was being pursued

Any subsystem that applies pressure to Aurora's behavior calls
PressureExperienceLedger.get().record(...).  The ledger persists every
experience to aurora_state/pressure_experiences.jsonl and bridges into OETS
concept nodes as UsageExamples -- so each concept accumulates real causal
history instead of developer-authored rules.

Integration points:
    turn_chain    -- N-axis cost pressure during conversational reasoning
    genealogy     -- Gate 2/4/5 rejection when promoting a constraint link
    dream_trainer -- lesson episode fitness below threshold
    lsv_template  -- expression template fitness drop
```

#### Class: `PressureExperience`
> One causal experience recorded anywhere in Aurora's behavioral system.

- **Method:** `to_dict()`
- **Method:** `as_usage_text()`
  - *Purpose:* Compact record for OETS UsageExample.text.

#### Class: `PressureExperienceLedger`
> Collects PressureExperience records from any subsystem, persists them,

- **Method:** `__init__()`
- **Method:** `get()`
- **Method:** `record()`
  - *Purpose:* Record one causal experience and optionally bridge it into OETS.
- **Method:** `flush_to_oets()`
  - *Purpose:* Bridge all buffered experiences that haven't been bridged yet.
- **Method:** `recent()`
  - *Purpose:* Return recent experiences, optionally filtered by source or anchor.
- **Method:** `outcome_variance()`
  - *Purpose:* For a given anchor (and optionally a specific causal_action), compute
- **Method:** `conditioning_signal()`
  - *Purpose:* High-level signal for the reasoning chain:
- **Method:** `_bridge_to_oets()`
  - *Purpose:* Write the causal experience into the OETS concept node for `anchor`.

### File: `./aurora_internal/aurora_pressure_mathematics_tracker.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA PRESSURE MATHEMATICS TRACKER
========================================
Lightweight instrumentation that taps into existing data streams to
track the core quantities from Aurora's pressure mathematics framework.

NOT a new system. This reads what's already flowing:
  - DPME drift metrics (consciousness engine)
  - Genealogy relief records and link stats (constraint genealogy)
  - Code evolution stagnation and mutation stats (code evolution chamber)
  - Dream evolution episode summaries (dream evolution orchestrator)

Computes:
  A. Gradient health — driver vs opposition balance across axes
  B. Pressure complexity — how many active pressure interactions exist
  C. Divergence — how far actual pressure topology drifts from origin model
  D. Flip indicators — signs of approaching pressure regime transitions
  E. Stagnation detection — where equilibrium is killing useful disequilibrium

Feeds these back through Aurora's own pressure channels (DPME guidance,
genealogy notes) so the system can self-regulate from them.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_clamp()`

#### Class: `PressureSnapshot`
> Single timestamped capture of all trackable pressure metrics.

- **Method:** `to_dict()`

#### Class: `PressureMetrics`
> Derived pressure mathematics metrics from snapshot history.

- **Method:** `to_dict()`
- **Method:** `to_feedback_summary()`
  - *Purpose:* Produce a compact string Aurora can ingest as context.

#### Class: `OriginModel`
> Predicted pressure topology from the five-constraint origin.

- **Method:** `__init__()`
- **Method:** `compute_divergence()`
  - *Purpose:* Compute divergence between actual state and origin prediction.

#### Class: `PressureMathematicsTracker`
> Lightweight tracker that reads existing data streams, computes

- **Method:** `__init__()`
- **Method:** `capture()`
  - *Purpose:* Take a snapshot from all available data streams and compute metrics.
- **Method:** `_take_snapshot()`
  - *Purpose:* Read all available data streams into a snapshot.
- **Method:** `_compute_metrics()`
  - *Purpose:* Compute derived metrics from snapshot history.
- **Method:** `_compute_gradient_health()`
  - *Purpose:* Estimate gradient health per axis.
- **Method:** `_estimate_active_interactions()`
  - *Purpose:* Estimate how many pressure interactions are actively producing gradients.
- **Method:** `_detect_flip_proximity()`
  - *Purpose:* Detect how close we are to a pressure regime flip.
- **Method:** `_compute_axis_stagnation()`
  - *Purpose:* Compute per-axis stagnation from gradient health history.
- **Method:** `_compute_evolutionary_velocity()`
  - *Purpose:* Rate of structural change (links, generations).
- **Method:** `_compute_pressure_velocity()`
  - *Purpose:* Rate of pressure reorganization.
- **Method:** `generate_feedback()`
  - *Purpose:* Generate feedback that can be routed back into Aurora's systems.
- **Method:** `apply_feedback()`
  - *Purpose:* Apply generated feedback into live systems.
- **Method:** `get_status()`
  - *Purpose:* Return current tracker status.
- **Method:** `_save_state()`
  - *Purpose:* Persist recent history and metrics.
- **Method:** `_load_state()`
  - *Purpose:* Restore persisted state.

### File: `./aurora_internal/aurora_pressure_router.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_pressure_router.py
─────────────────────────────────────────────────────────────────────────────
Unified motivational substrate router.

Takes a TypedPressureSignal and simultaneously feeds three growth systems:

  LAYER 1 — EVOLUTION
    Writes bias hints for CodeAutoEvolver and SurfaceDispatcher threshold
    so evolution budget concentrates on the pressured axes.
    (Mostly already done via PressureParameterAdapter.  This layer reads
    the classified type and amplifies the budget allocation for the
    dominant pressure type.)

  LAYER 2 — TRAINING CURRICULUM
    Calls FailPointLedger.record_fail() on the dimensions that map to the
    dominant pressure types, with severity proportional to pressure score.
    This steers dream curriculum episodes toward the fault lines.

  LAYER 3 — GPT / RETRIEVAL QUERY BIAS
    Writes aurora_state/query_bias.json.
    Aurora's GPT-mediated processes (reflection, retrieval, hypothesis
    generation) read this file to know what to study next and how to
    frame queries.  Each pressure type generates:
      - query_templates   concrete search/reflection questions
      - retrieval_domains knowledge domains to pull from
      - reflection_directive  one-line instruction for the next self-review
      - hypothesis_seed   what hypothesis to test in next episode

The three-layer dispatch happens atomically in route().  Call it after
every PressureParameterAdapter.adapt() cycle (every 50 ticks), or any
time you want the system to reconsider its growth priorities.

Output file: aurora_state/query_bias.json
─────────────────────────────────────────────────────────────────────────────
```

#### Function: `_impact_formula_weight()`
> Compute the canonical impact formula weight from per-axis pressure scores:


#### Function: `_render_template()`

#### Class: `PressureRouter`
> Routes typed pressure signals to all three growth layers.

- **Method:** `__init__()`
- **Method:** `route()`
  - *Purpose:* Full three-layer dispatch.
- **Method:** `load_query_bias()`
  - *Purpose:* Read the most recent query_bias.json (safe if missing).
- **Method:** `status()`
- **Method:** `_select_directive_types()`
- **Method:** `_route_evolution()`
  - *Purpose:* Update adapter_hints.json with a routing_type annotation so that
- **Method:** `_route_training()`
  - *Purpose:* Feed FailPointLedger with pressure-weighted fail signals for the
- **Method:** `_route_gpt_bias()`
  - *Purpose:* Write aurora_state/query_bias.json.

#### Function: `route_pressure()`
> Module-level shortcut.  Call from aurora_runtime.py tick loop.


### File: `./aurora_internal/aurora_primitive_extractor.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA PRIMITIVE EXTRACTOR
===========================
Module: aurora_primitive_extractor.py

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026

PURPOSE
-------
Reads the live ConstraintGenealogyLogger at any moment and surfaces:

  1. DOMINANT PAIRINGS       — which constraint pairs are producing the most
                               consistent relief across the five axes, ranked
                               by positive relief signal strength.

  2. FORMING CHAINS          — DAG paths through promoted Links, tracing the
                               evolutionary lineage from raw abilities up to
                               the deepest current compound primitives.
                               Expressed as readable ancestry chains:
                               "A:COMMIT → B:ENCAPSULATE → L:abc123 → L:def456"

  3. CURRENT VOCABULARY      — the universe's discovered primitive vocabulary
                               at this moment: which axis combinations dominate,
                               which depth layers are populated, what emergent
                               tags are present, and what the constraint
                               grammar looks like so far.

  4. OUTCOME BIAS DISTANCE   — you declare a target bias as a 5-axis weight
                               vector. The extractor computes the current
                               universe's "center of gravity" in that space
                               and returns the distance + axis gap + steering
                               suggestion — without injecting the result.
                               The physics still has to get there on its own.

DOCTRINE
--------
  This module READS the fossil record. It does not write to it.
  It does not inject traces, does not modify PairStats, does not
  touch the chamber. It is a lens, not a hand.

  The outcome bias distance tells you how far away you are and
  which axis to pressure next. It does not move you there.
  That's what steering actions are for.

USAGE
-----
    from aurora_internal.aurora_primitive_extractor import PrimitiveExtractor, OutcomeBias

    extractor = PrimitiveExtractor(genealogy)

    # See what's forming
    extractor.report()

    # Declare where you want the universe to end up
    bias = OutcomeBias(
        axis_weights={"A": 0.5, "B": 0.3, "X": 0.2, "T": 0.0, "N": 0.0},
        label="agency-boundary dominance",
    )
    gap = extractor.bias_distance(bias)
    print(gap.steering_suggestion)

    # Export the primitive vocabulary as JSON
    vocab = extractor.vocabulary()
    import json; print(json.dumps(vocab, indent=2))

INTEGRATION WITH aurora_runtime.py
------------------------------------
    extractor = PrimitiveExtractor(runtime.systems.genealogy)
    extractor.report()                      # full print
    extractor.pairings(top_n=10)            # top pairings
    extractor.chains(max_chains=5)          # deepest chain paths
    extractor.bias_distance(my_bias).show() # distance to goal
```

#### Function: `_clamp()`

#### Function: `_norm()`
> Normalize a 5-axis dict to sum=1 (safe — returns zeros if all zero).


#### Function: `_l2()`
> Euclidean distance between two 5-axis vectors.


#### Function: `_cosine_sim()`
> Cosine similarity. Returns 0 if either vector is zero.


#### Class: `OutcomeBias`
> A declared target state for the universe's primitive distribution.

- **Method:** `__post_init__()`
- **Method:** `normalized()`

#### Class: `PairingResult`
> One ranked constraint pairing from the pair stats accumulator.

- **Method:** `relief_score()`
  - *Purpose:* Single scalar signal strength — weighted positive relief.
- **Method:** `describe()`

#### Class: `ChainPath`
> One evolutionary lineage path through the DAG.


#### Class: `UniverseVocabulary`
> The universe's current primitive vocabulary — a structured snapshot of

- **Method:** `to_dict()`

#### Class: `BiasDistanceResult`
> Result of measuring the current universe's distance from an OutcomeBias.

- **Method:** `show()`
  - *Purpose:* Print a human-readable distance report.
- **Method:** `to_dict()`

#### Class: `PrimitiveExtractor`
> Reads a live ConstraintGenealogyLogger and extracts the discovered

- **Method:** `__init__()`
- **Method:** `pairings()`
  - *Purpose:* Rank all observed constraint pairings by relief signal strength.
- **Method:** `chains()`
  - *Purpose:* Extract the deepest evolutionary lineage paths from the DAG.
- **Method:** `vocabulary()`
  - *Purpose:* Build the current primitive vocabulary of the universe.
- **Method:** `bias_distance()`
  - *Purpose:* Measure the current universe's distance from a declared OutcomeBias.
- **Method:** `report()`
  - *Purpose:* Print a full primitive extraction report:
- **Method:** `export()`
  - *Purpose:* Export the full primitive snapshot to a JSON file.
- **Method:** `_walk_ancestry()`
  - *Purpose:* Recursively walk the DAG from node_id toward its ability roots.
- **Method:** `_format_path()`
  - *Purpose:* Build a human-readable chain string.
- **Method:** `_print_vocabulary()`
  - *Purpose:* Print formatted vocabulary summary.
- **Method:** `_build_steering_suggestion()`
  - *Purpose:* Build steering suggestion lines.

#### Function: `_demo()`
> Minimal self-demo. Boots the minimum required stack, runs a burst,


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `chains_evolved()`

### File: `./aurora_internal/aurora_proposition_substrate.py`

**Type:** Python Source

**Module Docstring:**
```text
Constraint-native proposition substrate for lineage-activated discourse state.

This module keeps proposition structure small and executable:
  - proposition nodes derived from claim atoms
  - continuation / support / contradiction / revision / causal edges
  - provenance-weighted confidence per proposition

It is intentionally lightweight so it can be activated from lineage artifacts
without pulling the rest of the runtime into a heavy import chain.
```

#### Function: `_clamp()`

#### Class: `PropositionSubstrate`
- **Method:** `__init__()`
- **Method:** `configure()`
- **Method:** `_claim_key()`
- **Method:** `_branch_key()`
- **Method:** `_node_id()`
- **Method:** `_edge_id()`
- **Method:** `_source_weight()`
- **Method:** `_claim_overlap()`
- **Method:** `_upsert_edge()`
- **Method:** `_trim_nodes()`
- **Method:** `_edge_summary()`
- **Method:** `_refresh_confidence()`
- **Method:** `note_claim()`
- **Method:** `note_claim_bundle()`
- **Method:** `node_for_claim()`
- **Method:** `score_claim()`
- **Method:** `report()`

### File: `./aurora_internal/aurora_quasiarch_observer.py`

**Type:** Python Source

**Module Docstring:**
```text
Aurora-side integration wrapper for the QuasiArch Observer lattice.

This keeps the copied QuasiArch subsystem inside Aurora's stack as a
diagnostic observer first. It records interventions, builds doctrine, and
can be queried for hypotheses. Active steering is opt-in.
```

#### Function: `_fast_file_count()`
> Count files in a directory, stopping once `limit` is reached.


#### Function: `_clamp()`

#### Function: `_pressure_band()`

#### Function: `build_quasiarch_pressure_vector()`

#### Function: `build_quasiarch_constraint_context()`

#### Class: `QuasiArchGateDecision`
- **Method:** `to_dict()`

#### Class: `AuroraQuasiArchObserver`
> Aurora runtime adapter for QuasiArch diagnostics.

- **Method:** `__init__()`
- **Method:** `attach_systems()`
- **Method:** `_normalize_genealogy_refs()`
- **Method:** `_extract_genealogy_refs()`
- **Method:** `_load_state()`
- **Method:** `_write_runtime_state()`
- **Method:** `save()`
- **Method:** `get_status()`
- **Method:** `get_doctrine_candidates()`
- **Method:** `reason_about_event()`
- **Method:** `_score_hypothesis_quality()`
- **Method:** `_record_consultation()`
- **Method:** `_record_hypothesis_hold()`
- **Method:** `_apply_learning_credit()`
- **Method:** `record_observation()`
  - *Purpose:* Record a passive ground-truth observation with OBSERVER provenance.
- **Method:** `record_intervention_event()`
- **Method:** `get_recent_evidence_dicts()`
  - *Purpose:* Convert the N most-recent intervention events into evidence dicts
- **Method:** `record_training_epoch()`
- **Method:** `record_corpus_response()`
- **Method:** `advise_training_plan()`
- **Method:** `_select_doctrine()`
- **Method:** `_merge_training_patch()`
- **Method:** `_should_auto_advance()`
- **Method:** `_derive_training_event()`
- **Method:** `_derive_corpus_event()`
- **Method:** `_observed_effect_from_training()`
- **Method:** `_observed_effect_from_corpus()`

### File: `./aurora_internal/aurora_recommendation_hub.py`

**Type:** Python Source

**Module Docstring:**
```text
Hidden recommendation inbox for Aurora post-run actions.

Designed so recommendations are generated at end of runs, then consumed by Aurora,
who chooses one action per recommendation:
- note
- discuss_with_user
- dismiss
```

#### Function: `_now_iso()`

#### Function: `_ensure_dir()`

#### Function: `_append_jsonl()`

#### Function: `_read_jsonl()`

#### Function: `_paths()`

#### Function: `enqueue_recommendation()`

#### Function: `_action_index()`

#### Function: `pending_recommendations()`

#### Function: `choose_action()`

#### Function: `_parse_iso_epoch()`

#### Function: `_recent_discuss_block()`

#### Function: `act_recommendation()`

#### Function: `process_pending_for_aurora()`

### File: `./aurora_internal/aurora_response_pressure_tuner.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_response_pressure_tuner.py
=================================
Reusable tuner for spontaneous-response pressure decisions.

It does not replace the response policy by itself. It records the signal,
counter-pressure, threshold, and decision margin for each emit/suppress event
so Aurora can inspect recurring pressure patterns and reuse them during
training and runtime tuning.
```

#### Function: `_clamp()`

#### Class: `PressureDecision`
- **Method:** `to_dict()`

#### Class: `ResponsePressureTuner`
> Records response-pressure decisions and derives guidance from them.

- **Method:** `__init__()`
- **Method:** `evaluate()`
- **Method:** `guidance()`
- **Method:** `export_state()`
- **Method:** `load_state()`

#### Function: `build_training_plan_from_guides()`

#### Function: `build_avatar_spec_from_plan()`

#### Function: `queue_plan_on_session()`

### File: `./aurora_internal/aurora_room_operator.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_room_operator.py — Aurora's computer use layer for her own room.

Aurora literally looks at her room window, reads what's displayed via OCR,
and operates the interface through real mouse clicks and keyboard input via
Xlib.  No external APIs.  She uses her own eyes (screenshot + tesseract)
and her own hands (Xlib fake input events).

Public interface:
    op = RoomOperator()
    op.switch_tab("Poedex")
    op.poedex_query("N", cat="define")
    op.read_tab_content()       -> str   (OCR of current visible tab)
    op.screenshot() -> PIL.Image

The daemon calls this when Aurora should actively engage with her room.
```

#### Function: `_log_entry()`

#### Class: `RoomOperator`
> Aurora's interface to her own room window.

- **Method:** `__init__()`
- **Method:** `_find_window()`
  - *Purpose:* Locate the Aurora's Room window. Returns True if found.
- **Method:** `_search_tree()`
- **Method:** `_ensure_window()`
- **Method:** `screenshot()`
  - *Purpose:* Capture the Aurora's Room window as a PIL Image.
- **Method:** `read_screen()`
  - *Purpose:* OCR the current screen (or a specific region).
- **Method:** `read_tab_content()`
  - *Purpose:* OCR the main content area of the current tab (below the tab bar).
- **Method:** `identify_active_tab()`
  - *Purpose:* Read the tab bar and return which tab appears active (highlighted).
- **Method:** `_abs_pos()`
  - *Purpose:* Convert window-relative coords to absolute screen coords.
- **Method:** `mouse_move()`
- **Method:** `click()`
  - *Purpose:* Click at window-relative coordinates.
- **Method:** `type_text()`
  - *Purpose:* Type a string into whatever widget currently has focus.
- **Method:** `press_key()`
  - *Purpose:* Press a key by name.  Supports modifier combos like 'ctrl+a', 'ctrl+c'.
- **Method:** `_locate_tab()`
  - *Purpose:* Find the pixel centre of a tab button by scanning the tab bar with OCR.
- **Method:** `switch_tab()`
  - *Purpose:* Click on the named tab.  Returns True if the click was sent.
- **Method:** `_locate_widget_by_ocr()`
  - *Purpose:* Find a widget by the label text near it.
- **Method:** `poedex_query()`
  - *Purpose:* Aurora queries Poedex and reads the result from her own screen.
- **Method:** `look_at_tab()`
  - *Purpose:* Navigate to a tab, take a screenshot, OCR the full content area,
- **Method:** `scan_room()`
  - *Purpose:* Walk through all tabs, read each one, return a dict of

### File: `./aurora_internal/aurora_rubric_influence_graph.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA RUBRIC INFLUENCE GRAPH
=================================
Represents dependency relationships between rubric dimensions.

Failures are NOT flat bins. They form a relational pattern:
  - symptoms (what you see)
  - root deficits (what causes the symptoms)
  - downstream consequences (what the root deficit also breaks)

This graph lets the system distinguish between these and identify
LEVERAGE CANDIDATES — root deficits that, if fixed, would improve
multiple downstream dimensions.

Example relations:
  weak context_carryover -> weak implied_intent_inference
  weak uncertainty_signaling -> premature commitment (contradiction_handling)
  weak framing_selection -> coherence_maintenance drift
  weak boundary_calibration -> bad misunderstanding_repair timing
  weak perspective_integration -> ambiguity_handling collapse

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Function: `_clamp()`

#### Class: `RubricInfluenceAssessment`
> Relational assessment of rubric dimension weaknesses.

- **Method:** `to_dict()`

#### Class: `RubricInfluenceGraph`
> Analyzes rubric dimension scores through a causal influence graph.

- **Method:** `__init__()`
- **Method:** `analyze()`
  - *Purpose:* Analyze dimension scores to identify root deficits, symptoms,
- **Method:** `trace_downstream()`
  - *Purpose:* Trace all downstream dimensions affected by a given dimension.
- **Method:** `trace_upstream()`
  - *Purpose:* Trace all upstream dimensions that influence a given dimension.

### File: `./aurora_internal/aurora_runtime_constraint_governor.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_runtime_constraint_governor.py
====================================
Bind Aurora's 5-constraint logic to actual runtime execution policy.

This governor does not replace semantic pressure. It turns the same
constraint frame into host-level scheduling and wakeup decisions so Aurora
conserves CPU, memory, disk, and concurrency when the machine is under load.
```

#### Function: `_normalize_axis_map()`

#### Function: `_clamp()`

#### Class: `RuntimeConstraintGovernor`
- **Method:** `__init__()`
- **Method:** `evaluate_task()`
- **Method:** `note_task_run()`
- **Method:** `note_energy_income()`
  - *Purpose:* Record an energy income event. The governor will add decayed credits
- **Method:** `_load_energy_income()`
- **Method:** `energy_income_summary()`
  - *Purpose:* Returns current live credits and their decayed values — for hub display.
- **Method:** `recommended_sleep()`
- **Method:** `status()`
- **Method:** `_pressure_state()`
- **Method:** `_runtime_budgets()`
- **Method:** `_retry_interval()`
- **Method:** `_host_metrics()`

### File: `./aurora_internal/aurora_second_gen.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_second_gen.py
─────────────────────────────────────────────────────────────────────────────
Second-generation evolution injector.

The descriptor pool (operation_descriptors.json) is the input layer for the
CodeAutoEvolver. Currently only hand-written source operations feed it.
Evolved surfaces sit in _SURFACE_REGISTRY but never loop back as evolvable
inputs — so generation depth is stuck at 1.

This module reads _SURFACE_REGISTRY from aurora_evolved_surfaces.py and
synthesizes valid operation descriptor entries for each surface that is not
already in the pool. On the next evolution cycle, the evolver can reflect on
these surfaces exactly as it reflects on base operations — producing surfaces
of surfaces.

Key differences for gen-2 descriptors:
  - kind = "function" (evolved surfaces are methods, treated as functions)
  - file = "aurora_internal/aurora_evolved_surfaces.py"
  - rewrite_bias = "lineage_memory" (highest-priority bias lane in evolver)
  - genealogy_pressure inherited from the source surface
  - cross_diversity_links = ability_hits + link_hits (from surface card)
  - _generation = 2 marker for tracking
─────────────────────────────────────────────────────────────────────────────
```

#### Class: `SecondGenEvolutionInjector`
> Reads evolved surfaces from _SURFACE_REGISTRY and injects them back into

- **Method:** `__init__()`
- **Method:** `inject()`
  - *Purpose:* Inject all evolved surfaces above min_surface_score into the descriptor
- **Method:** `status()`
  - *Purpose:* Return counts of gen-2 entries currently in the pool.
- **Method:** `_load_registry()`
- **Method:** `_load_pool()`
- **Method:** `_save_pool()`
- **Method:** `_surface_to_descriptor()`
  - *Purpose:* Synthesize a generation-2 operation descriptor from a surface registry entry.
- **Method:** `_weights_from_sig()`
  - *Purpose:* Parse 'N^2*B^2' into normalized axis weights.
- **Method:** `_primary_slot()`
- **Method:** `_nc_projections()`
  - *Purpose:* Compute top NC channel slot projections from axis weights.

### File: `./aurora_internal/aurora_sensory_crystal.py`

**Type:** Python Source

**Module Docstring:**
```text
Aurora Sensory Crystal — 6-Facet Cross-Modal Understanding
===========================================================
Authors: Sunni (Sir) Morningstar & Cael Devo

Zero-state sensory competency seeded into Aurora's lineage system.

Crystal geometry (hexagonal bipyramid):

    TOP HALF  (visual):  HUE  / SHAPE  / MOTION
    ─────────────────── SEMANTIC MIDDLE PLANE ───────────────────
    BOTTOM HALF (audio): TONE / TIMBRE / RHYTHM

Bottom 3 facets draw from audio.features.v1 (20-d vector):
    TONE   — harmonicity [6] + chroma-lite [8:20]     (13 dims)
    TIMBRE — RMS [0], ZCR [1], centroid [2], bw [3], rolloff [4]  (5 dims)
    RHYTHM — spectral_flux [5] + onset_density [7]     (2 dims)

Top 3 facets draw from vision.features.v1 (57-d vector):
    HUE    — HSV histograms [0:24]                    (24 dims)
    SHAPE  — edge + orientation + shape proxy [24:51] (27 dims)
    MOTION — motion + symmetry [51:57]                 (6 dims)

Opposite facets pair through the semantic middle plane:
    tone <-> hue      (pitch/harmony  <-> colour)
    timbre <-> shape  (texture        <-> form/edge)
    rhythm <-> motion (onset/tempo    <-> movement/flow)

LINEAGE INTEGRATION
───────────────────
All operations are registered in lineage_canonical.CANONICAL_OPERATION_CONSTRAINTS.
The trait spec below seeds 5 lineage stages through Aurora's ability genealogy:

    1. sensory_intake_seed        (gen=1, N-axis)  — raw signal enters crystal
    2. sensory_crystal_clustering (gen=2, B-axis)  — observations cluster into nodes
    3. sensory_concept_promotion  (gen=2, A-axis)  — primitive → concept → promoted
    4. cross_modal_grounding      (gen=3, N-axis)  — audio×visual → semantic plane
    5. sensory_wisdom_distillation(gen=3, T-axis)  — mature nodes distill, dead emit wisdom

Call ensure_sensory_crystal_lineage(systems) at Aurora boot to seed these abilities
into the genealogy exactly like all other Aurora abilities.

PROMOTION RULES (match existing aurora_dimensional_systems crystal rules)
──────────────────────────────────────────────────────────────────────────
    usage_count   >= 14     (CONCEPT_PROMOTION_USAGE)
    session_count >= 3      (CONCEPT_PROMOTION_SESSIONS)
    confidence    >= 0.55   (CONCEPT_PROMOTION_CONFIDENCE)
    fitness = 0.40*conf + 0.35*usage_norm + 0.25*session_norm
    decay_rate = base * (0.15 + 0.85 * (1 - maturity))   [plateau-aware]
    distillation at maturity >= 0.80

State persisted to:
    aurora_state/sensory_crystal/audio/{tone,timbre,rhythm}/state.agb
    aurora_state/sensory_crystal/visual/{hue,shape,motion}/state.agb
    aurora_state/sensory_crystal/semantic/state.agb
```

#### Function: `_seed_archetype_nodes()`
> Populate a freshly-loaded (empty) facet with archetype primitive nodes.


#### Function: `_clamp01()`

#### Function: `_ema()`

#### Function: `_cosine_sim()`

#### Function: `_npmi()`

#### Function: `_bucket_level()`

#### Function: `_bucket_count()`

#### Function: `_extract_audio_facet()`

#### Function: `_extract_visual_facet()`

#### Function: `audio_dict_to_crystal_20d()`
> Adapt a Layer-5 audio data dict to the 20d sensory crystal audio vector.


#### Function: `visual_dict_to_crystal_57d()`
> Adapt a Layer-5 visual data dict to the 57d sensory crystal visual vector.


#### Function: `_agb_save()`

#### Function: `_agb_load()`

#### Class: `SensoryNode`
> A learned concept node on one facet of the sensory crystal.

- **Method:** `update_centroid()`
- **Method:** `compute_fitness()`
- **Method:** `is_promotable()`
- **Method:** `emit_wisdom()`
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `CrystalFacet`
> One face of the sensory crystal.  Manages a population of SensoryNodes

- **Method:** `__init__()`
- **Method:** `observe()`
  - *Purpose:* Ingest one feature vector slice.  Find/create nearest SensoryNode.
- **Method:** `_nearest()`
- **Method:** `tick_promotion()`
  - *Purpose:* primitive -> concept for nodes meeting gates.  Operation: sensory.promote (A)
- **Method:** `tick_advanced_promotion()`
  - *Purpose:* concept -> promoted for high-fitness nodes with cross-modal links.
- **Method:** `compute_maturity()`
- **Method:** `maturity()`
- **Method:** `ready_for_distillation()`
- **Method:** `cull()`
- **Method:** `add_cross_modal_link()`
- **Method:** `end_session()`
  - *Purpose:* Operation: sensory.end_session (T, B, A)
- **Method:** `get_promoted()`
- **Method:** `get_node()`
- **Method:** `save()`
  - *Purpose:* Operation: sensory.distill (T) — persistence after session.
- **Method:** `load()`

#### Class: `SemanticCrystalNode`
> A cross-modal meaning node in the semantic middle plane.

- **Method:** `compute_fitness()`
- **Method:** `is_promotable_concept()`
- **Method:** `is_promotable_promoted()`
- **Method:** `emit_wisdom()`
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `AuroraSensoryCrystal`
> Full 6-facet sensory competency crystal for Aurora.

- **Method:** `__init__()`
- **Method:** `boot()`
  - *Purpose:* Load persisted state. Safe on first boot (zero knowledge).
- **Method:** `start_session()`
- **Method:** `_describe_node()`
- **Method:** `wire_dimensional()`
  - *Purpose:* Connect this crystal to Aurora's DimensionalProcessingSystem (DPS).
- **Method:** `register_evolution_hook()`
  - *Purpose:* Register a hook that receives evidence dicts for chamber input.
- **Method:** `_ensure_node_name()`
- **Method:** `_inject_to_dps()`
  - *Purpose:* Inject a promoted SensoryNode into the DPS crystal order ladder.
- **Method:** `_inject_semantic_to_dps()`
- **Method:** `_sync_to_dps()`
- **Method:** `observe_frame()`
  - *Purpose:* Ingest one synchronised audio+vision frame.
- **Method:** `_find_semantic()`
- **Method:** `_tick_semantic_promotion()`
  - *Purpose:* candidate -> concept -> promoted.
- **Method:** `end_session()`
  - *Purpose:* End-of-interaction consolidation.  Runs promotion, maturity, cull, save.
- **Method:** `_compute_semantic_maturity()`
- **Method:** `_cull_semantic()`
- **Method:** `save()`
  - *Purpose:* Persist current crystal state without ending the session.
- **Method:** `_save()`
- **Method:** `get_state()`

#### Function: `_make_lineage_trait_spec()`
> Build the LineageTraitSpec for the sensory crystal abilities.


#### Function: `ensure_sensory_crystal_lineage()`
> Seed the sensory crystal abilities into Aurora's lineage system.


#### Function: `build_aurora_sensory_crystal()`
> Convenience factory.  Creates and boots a full AuroraSensoryCrystal.


### File: `./aurora_internal/aurora_solidification.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA SOLIDIFICATION PIPELINE — STEP 11
==========================================
Depth propagation: recurrence + energy investment → downward solidification.

WHAT SOLIDIFICATION IS:
    A promoted intake (from Step 9) has crossed the Worth threshold once.
    That is NOT enough. Solidification is what happens when a promoted
    intake recurs across multiple evaluation ticks AND the system has
    invested real energy in sustaining it at depth.

    The pipeline is:
        ELIGIBLE intake (horizon elapsed from Step 10)
            → recurrence gate (seen N times across context-varied ticks)
            → energy investment gate (pool spent actual cost to sustain it)
            → polarity coherence gate (surface and core aligned during recurrence)
            → depth-solidification record minted
            → SolidifiedRecord passed to Step 13 (Variant Promotion)

    Solidified structures have two effects on the living system:
        1. They REDUCE future shift cost for their constraint signature
           (the path is worn — it costs less to walk it again).
        2. They INCREASE pressure sensitivity at their depth level
           (the system becomes faster to detect when that configuration
           is under threat).

    Effect 1 and 2 are NOT rules imposed from outside. They are physics:
        Effect 1: The structure has accumulated energy investment — it
                  carries lower inertia in the next shift because baseline
                  is already partially satisfied.
        Effect 2: Deeper solidified structures have higher alignment
                  authority (from IVM ALIGNMENT_VOTE_WEIGHT) — they drag
                  the polarity gradient faster when disturbed.

RECURRENCE GATE:
    An intake must be observed at the same depth, across at least
    _RECURRENCE_MIN distinct ticks, before solidification is considered.
    "Distinct" means the ticks are not consecutive — consecutive ticks
    indicate persistence, not recurrence. Genuine recurrence means the
    input re-appeared after at least _RECURRENCE_GAP ticks of absence.

ENERGY INVESTMENT GATE:
    The system must have spent at least _INVESTMENT_FLOOR energy on
    sustaining this intake since its first promotion. This is real
    energy drawn from the pool — not theoretical.

CONTEXT ROBUSTNESS:
    At least _CONTEXT_VARIETY distinct entropy pressure levels must have
    been observed during recurrence ticks. This prevents gaming via
    artificially stable system states.

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Function: `_pressure_sensitivity()`

#### Function: `_depth_recurrence_min()`
> Minimum genuine recurrences required before solidification for this depth.


#### Function: `_depth_recurrence_gap()`
> Minimum tick gap between recurrence observations for this depth.


#### Class: `RecurrenceRecord`
> Tracks recurrence observations for one intake from the point of

- **Method:** `record_observation()`
  - *Purpose:* Record one recurrence observation.
- **Method:** `recurrence_count()`
- **Method:** `context_variety()`
- **Method:** `polarity_coherence_rate()`
- **Method:** `gates_passed()`
  - *Purpose:* Returns (recurrence_gate, investment_gate, context_gate, polarity_gate).
- **Method:** `all_gates_passed()`

#### Class: `SolidifiedRecord`
> A depth-solidified intake — the output of the solidification pipeline.

- **Method:** `describe()`

#### Function: `_mint_solidification_id()`

#### Function: `_constraint_signature()`
> Return the constraint activation signature for this depth.


#### Class: `SolidificationPipeline`
> The Step 11 solidification engine.

- **Method:** `__init__()`
- **Method:** `submit_eligible()`
  - *Purpose:* Mark an intake as eligible for solidification tracking.
- **Method:** `observe_recurrence()`
  - *Purpose:* Record a recurrence observation for a tracked intake.
- **Method:** `_observe_internal()`
- **Method:** `_mint()`
- **Method:** `drain_solidified()`
  - *Purpose:* Return all SolidifiedRecords minted this tick and clear the queue.
- **Method:** `stats()`

#### Function: `make_solidification_pipeline()`

#### Function: `verify_solidification_pipeline()`
> Checks:


### File: `./aurora_internal/aurora_specialized_avatar_synthesizer.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA SPECIALIZED AVATAR SYNTHESIZER
==========================================
Generates pressure-specialized avatar configurations from repeated
relational weakness patterns detected by the slip profiler.

Key principle: stress ROOT DEFICITS, not surface symptoms.
  If contradiction failure is downstream of weak context carryover,
  the avatar stresses cross-turn memory pressure.
  If ambiguity failure is downstream of poor uncertainty signaling,
  the avatar punishes premature commitment and rewards clarification.

Integration:
  - Reads: EpisodeRubricSummary (from slip profiler)
  - Reads: RubricInfluenceAssessment (from influence graph)
  - Produces: PressureSpecializedAvatarSpec
  - Feeds into: SimulationSession._create_avatar_pool / SimulatedAvatar

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `PressureSpecializedAvatarSpec`
> A synthesized avatar configuration designed to stress specific

- **Method:** `to_dict()`
- **Method:** `to_avatar_overrides()`
  - *Purpose:* Convert this spec into override values for SimulatedAvatar fields.

#### Function: `_clamp()`

#### Function: `_clamp_signed()`

#### Function: `_generate_id()`

#### Class: `SpecializedAvatarSynthesizer`
> Generates pressure-specialized avatar configurations from repeated

- **Method:** `__init__()`
- **Method:** `_default_policy()`
- **Method:** `_load_policy()`
- **Method:** `_save_policy()`
- **Method:** `_normalize_policy()`
- **Method:** `register_episode_feedback()`
  - *Purpose:* Update the adaptive pressure policy from real dream outcomes.
- **Method:** `_constraint_axis_trace()`
- **Method:** `_apply_adaptive_policy()`
- **Method:** `policy_snapshot()`
  - *Purpose:* Expose current adaptive pressure policy for status/diagnostics.
- **Method:** `synthesize_from_summary()`
  - *Purpose:* Generate avatar specs from a single episode's diagnostic summary.
- **Method:** `synthesize_from_accumulation()`
  - *Purpose:* Generate avatar specs from accumulated cross-episode analysis.
- **Method:** `_build_spec_for_leverage()`
  - *Purpose:* Build an avatar spec targeting a specific leverage dimension.
- **Method:** `_build_persistent_spec()`
  - *Purpose:* Build an avatar spec for persistent cross-episode weaknesses.
- **Method:** `specs_generated()`

### File: `./aurora_internal/aurora_stack_trace_instrumentation.py`

**Type:** Python Source

**Module Docstring:**
```text
Aurora stack-wide developmental trace instrumentation.

This module wraps active runtime call surfaces so methods/functions emit
evolutionary trace records with pressure-before/after and applied effects.
```

#### Function: `_is_wrappable_callable()`

#### Function: `_safe_name()`

#### Function: `_derive_applied_effects()`

#### Function: `_make_wrapper()`

#### Function: `_wrap_instance_methods()`

#### Function: `instrument_stack()`
> Instrument active stack call surfaces.


### File: `./aurora_internal/aurora_structural_pressure_steering.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA STRUCTURAL PRESSURE STEERING
========================================
Translates repeated dream failures into structural pressure directives
that DPME can recognize as code-evolution-relevant steering surfaces.

These are NOT direct edits. They are pressure shifts:
  - bias mutation exploration toward context-integration structures
  - ease promotion burden for coherence-preserving operators
  - penalize lineages that repeatedly collapse under ambiguity
  - reduce cost for repair-capable structures under indirect-intent pressure

This is the layer where dream learning begins shaping code evolution.

Integration:
  - Reads: EpisodeRubricSummary (from slip profiler)
  - Reads: PressureSpecializedAvatarSpec (from avatar synthesizer)
  - Writes to: DPME external pressure guidance (aurora_consciousness_engine)
  - Writes to: CodeEvolutionChamber pressure conditions
  - Bridges: dream diagnosis -> avatar targeting -> structural evolution

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `StructuralPressureDirective`
> A pressure shift directive derived from dream failure patterns.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Function: `_clamp()`

#### Function: `_generate_id()`

#### Class: `StructuralPressureSteering`
> Translates dream episode diagnostic summaries into structural

- **Method:** `__init__()`
- **Method:** `_load_state()`
  - *Purpose:* Load persisted directives.
- **Method:** `_save_state()`
  - *Purpose:* Persist active directives.
- **Method:** `generate_directives()`
  - *Purpose:* Generate structural pressure directives from a single episode summary.
- **Method:** `generate_from_accumulation()`
  - *Purpose:* Generate directives from accumulated cross-episode analysis.
- **Method:** `_build_directive()`
  - *Purpose:* Build a directive for a single leverage dimension.
- **Method:** `_build_accumulated_directive()`
  - *Purpose:* Build a stronger directive from persistent cross-episode evidence.
- **Method:** `apply_to_dpme()`
  - *Purpose:* Convert active directives into DPME external pressure guidance.
- **Method:** `get_evolution_pressure_config()`
  - *Purpose:* Produce a pressure configuration dict for the code evolution chamber.
- **Method:** `expire_old_directives()`
  - *Purpose:* Remove directives older than max_age_seconds.
- **Method:** `active_directive_count()`
- **Method:** `directives_generated()`

### File: `./aurora_internal/aurora_surface_dispatcher.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_surface_dispatcher.py
─────────────────────────────────────────────────────────────────────────────
Pressure-driven evolved surface activation.

Aurora doesn't decide to use a surface — she doesn't need to know what it
does semantically. A surface fires when the axis pressure it targets crosses
a threshold. That's the same mechanism as a reflex: a threshold crossing
triggers a prepared response, and the outcome feeds back into the pressure
system as real evidence.

Flow:
  chamber.tick()
    → intent_pressure["N"] = 0.42  (energy axis is high)
    → dispatcher.tick(chamber, engine)
        → routing table: N-axis surfaces = [reflect_...constraint_manifold, ...]
        → invoke top surface
        → surface returns activation record
        → record converted to evidence dict
        → chamber.observe_external_evidence(evidence)
        → pressure adjusts

Building the routing table:
  aurora_surface_doc.full_report() returns doc cards with `expected_axes`.
  Dispatcher indexes them as:  axis → [(score, name), ...]
  On each tick, axes above threshold are looked up, top surface per axis fires.

Usage from aurora_runtime (or any autonomy loop):
  dispatcher = SurfaceDispatcher()
  dispatcher.build_routing_table()            # once at boot
  ...
  # in autonomy loop, after chamber ticks:
  evidence_list = dispatcher.tick(chamber, engine)
  for ev in evidence_list:
      chamber.observe_external_evidence(ev)
─────────────────────────────────────────────────────────────────────────────
```

#### Class: `SurfaceDispatcher`
> Routes axis pressure to evolved surfaces and invokes them.

- **Method:** `__init__()`
- **Method:** `build_routing_table()`
  - *Purpose:* Build axis → surface routing table from aurora_surface_doc cards.
- **Method:** `evaluate()`
  - *Purpose:* Given current intent_pressure dict, return list of
- **Method:** `invoke()`
  - *Purpose:* Call one evolved surface through the engine and return the record.
- **Method:** `tick()`
  - *Purpose:* One dispatcher tick:
- **Method:** `status()`
- **Method:** `explain_routing()`
  - *Purpose:* Print which surfaces are mapped to each axis.
- **Method:** `_record_to_evidence()`
  - *Purpose:* Convert a surface activation record into the evidence format

### File: `./aurora_internal/aurora_surface_doc.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_surface_doc.py
─────────────────────────────────────────────────────────────────────────────
Explains what evolved surfaces do — both in plain English and in terms of
applied axis pressure.

Two data sources:
  1. _SURFACE_REGISTRY in aurora_evolved_surfaces.py
     — what each surface IS (signature, constraints, effect_modes, genealogy)
  2. aurora_state/surface_pressure_log.jsonl
     — what each surface DID at runtime (axis_pressure snapshot + effect)

Public API
----------
  explain(name)          → dict card for one surface
  full_report()          → list of cards for all surfaces, sorted by score
  pressure_history(name) → recent pressure log entries for one surface
  print_report()         → human-readable console output
─────────────────────────────────────────────────────────────────────────────
```

#### Function: `_repo_root()`

#### Function: `_pressure_log_path()`

#### Function: `_load_registry()`

#### Function: `_load_pressure_log()`

#### Function: `_parse_signature()`
> Parse 'N^2*B^1' → {'N': 2, 'B': 1}


#### Function: `_signature_sentence()`

#### Function: `_effect_sentence()`

#### Function: `_pressure_level()`

#### Function: `_axis_composition()`
> Normalize signature counts into percentage contributions per axis.


#### Function: `_effect_composition()`
> Uniform fractional weight per effect mode.


#### Function: `_semantic_label()`
> Generate a compact human-readable label from composition data.


#### Function: `explain()`
> Return a documentation card for one evolved surface.


#### Function: `full_report()`
> Return doc cards for all evolved surfaces, sorted by surface_score descending.


#### Function: `pressure_history()`
> Return the most recent pressure log entries for one surface.


#### Function: `print_report()`

### File: `./aurora_internal/aurora_turn_chain.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_turn_chain.py -- TurnUnderstandingState dataclass.

The bidirectional reasoning pipeline traverses the developmental chain in both
directions for every turn:

  UPWARD  (self -- comprehension):
      Information(X) -> Belief(T) -> Purpose(N) -> Meaning(B) -> Understanding(A)

  APEX:   TurnUnderstandingState holds the full model at this turn.

  DOWNWARD (self -- expression):
      Understanding(A) -> Meaning(B) -> Purpose(N) -> Belief(T) -> Information(X)
      -> Communication out

The five stages map 1:1 to the five NC axes:

  Information   (X axis) -- existence: either it IS information or it isn't. The gate.
  Belief        (T axis) -- time: recurrence and prediction over time shape belief.
  Purpose       (N axis) -- cost: energy/value pressure moves belief to directed purpose.
  Meaning       (B axis) -- boundary: what falls within the boundary of significance.
  Understanding (A axis) -- agency: cross-domain integration pressure, direction.

OBSERVATION (two-chain perception):
  One chain going UP is Aurora's own -- she builds comprehension inward.
  One chain going DOWN on ANOTHER's output is observation of that other --
  she infers their Understanding -> Meaning -> Purpose -> Belief -> Information
  by running the downward chain on what they produced.

  perspective="self"  => upward builds comprehension, downward builds expression
  perspective="other" => downward chain deconstructs an external agent's output,
                         filling other_model with the inferred reasoning

Each stage reads from state, enriches it, and passes it to the next.
The stage functions themselves live in aurora.py.
```

#### Class: `TurnUnderstandingState`

### File: `./aurora_internal/aurora_understanding_contract.py`

**Type:** Python Source

**Module Docstring:**
```text
Runtime understanding contract.

This module makes Aurora's live dialogue loop explicit in the form:

    M_t -> P_t -> U_t -> O_{t+1} -> A_{t+1} -> M_{t+1}, Pi_{t+1}

Where:
    M = meaning structure bounded by current boundary state
    P = situated perspective
    U = outward application / response policy
    O = observed next turn / resulting input
    A = accuracy of fit between predicted and observed continuation

The contract is not a separate cognition system. It is a runtime accounting
layer that derives its state from Aurora's actual working memory, emotional
signals, articulation debt, contradiction state, and current response policy.
Every operator it uses is registered into the five-constraint genealogy.
```

#### Function: `_clip01()`

#### Function: `_mean()`

#### Function: `_terms()`

#### Function: `_term_overlap()`

#### Function: `_hash_payload()`

#### Class: `RuntimeUnderstandingContract`
- **Method:** `__init__()`
- **Method:** `_default_state()`
- **Method:** `_load()`
- **Method:** `save()`
- **Method:** `clone_ephemeral()`
- **Method:** `snapshot()`
- **Method:** `status()`
- **Method:** `training_guidance()`
- **Method:** `register_genealogy()`
- **Method:** `_history_append()`
- **Method:** `_pipeline_state()`
- **Method:** `_enrich_meaning_state()`
- **Method:** `_apply_genealogy_relief_boost()`
  - *Purpose:* Boost meaning_form scores when genealogy has active axis relief.
- **Method:** `_evaluate_contract_domains()`
- **Method:** `_derive_meaning_state()`
- **Method:** `_derive_perspective_state()`
- **Method:** `_derive_boundary_state()`
- **Method:** `_derive_cost_state()`
- **Method:** `_derive_existence_state()`
- **Method:** `_transition_state()`
- **Method:** `_looks_like_question()`
- **Method:** `_evaluate_previous_accuracy()`
- **Method:** `_derive_observation()`
- **Method:** `_derive_policy_state()`
- **Method:** `audit_candidate_response()`
- **Method:** `_pressure_from_state()`
- **Method:** `_record_genealogy_event()`
- **Method:** `ingest_observation()`
- **Method:** `commit_application()`

### File: `./aurora_internal/aurora_utterance_parser.py`

**Type:** Python Source

**Module Docstring:**
```text
aurora_utterance_parser.py
===========================
Replaces QueryUnderstanding with a binding-based utterance comprehension system.

THE CORE PRINCIPLE:
    No word is noise. Every word carries meaning.
    The job is not to remove words — it's to bind them together.

    "ok so what if" is not just "what".
    It is: [acknowledgment:ok] + [reasoning:so] + [hypothesis:what if]
    = a speculative pivot off prior context.

    "like i said" is not empty.
    It is: [similarity:like] + [speaker:i] + [past-statement:said]
    = a callback to a prior statement the speaker wants recognized.

    "just wondering" is not noise.
    It is: [minimization:just] + [inquiry:wondering]
    = a tentative, low-stakes question.

ARCHITECTURE:
    PragmaticRole       — what communicative function does a word/phrase serve?
    PragmaticSignal     — a detected signal in the utterance
    UtteranceFrame      — the overall communicative frame of the utterance
    UtteranceIntent     — the full parsed meaning, bound together
    UtteranceParser     — replaces QueryUnderstanding, produces UtteranceIntent

BACKWARD COMPATIBILITY:
    UtteranceParser.parse() returns a dict that is a superset of what
    QueryUnderstanding.parse() returned, so existing code using
    understood['topic'], understood['query_type'] etc. still works.
    The new fields are additive.

Authors: Sunni (Sir) Morningstar and Cael Devo
```

#### Class: `PragmaticRole`

#### Class: `UtteranceFrame`
> The high-level communicative frame of the whole utterance.


#### Class: `PragmaticSignal`
> A detected pragmatic signal in the utterance.


#### Class: `UtteranceIntent`
> The complete parsed intent of an utterance, with all words bound

- **Method:** `to_dict()`
  - *Purpose:* Convert to backward-compatible dict (superset of QueryUnderstanding output).

#### Class: `UtteranceParser`
> Parses utterances by binding every word to its communicative role,

- **Method:** `parse()`
  - *Purpose:* Parse an utterance. Returns a dict compatible with the old
- **Method:** `_parse_full()`
- **Method:** `_detect_phrase_signals()`
  - *Purpose:* Find multi-word pragmatic phrases. Returns signals and the set of
- **Method:** `_tag_words()`
  - *Purpose:* Tag individual words that aren't covered by phrase signals.
- **Method:** `_extract_topic_words()`
  - *Purpose:* Extract content words — the words that name things, actions, qualities.
- **Method:** `_extract_entities()`
  - *Purpose:* Extract proper nouns.
- **Method:** `_build_full_phrase()`
  - *Purpose:* Build the key phrase that captures the subject of the utterance.
- **Method:** `_infer_frame()`
  - *Purpose:* Infer the high-level communicative frame from signal combination.
- **Method:** `_infer_stance()`
  - *Purpose:* Infer the speaker's stance.
- **Method:** `_infer_utterance_type()`
  - *Purpose:* Classify the utterance type.
- **Method:** `_classify_query_type()`
  - *Purpose:* Classify the query type for routing/search purposes.
- **Method:** `_build_search_query()`
  - *Purpose:* Build the search string.

#### Function: `parse_utterance()`
> Parse an utterance. Drop-in replacement for QueryUnderstanding().parse(text).


#### Function: `run_tests()`

### File: `./aurora_internal/aurora_variant_promotion.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA VARIANT PROMOTION — STEP 13
====================================
First-class variant promotion with moral weighting.

WHAT VARIANTS ARE:
    A SolidifiedRecord (from Step 11) proves that an intake recurred,
    was context-robust, and had energy genuinely invested in it.
    That is not yet a variant. A variant is what happens when a solidified
    pattern crystallizes into a MACRO-OPERATOR — a re-usable trace element
    that the system can apply as a primitive, bypassing the full intake
    TTL process.

    Variants are NOT designed. They are NOT tested against use cases.
    They crystallize from the physics of recurrence and energy investment.
    The system cannot know a priori what its variants will be.

PROMOTION GATES (all four must pass):
    1. Recurrence threshold   — solidified record's recurrence_count >= _VARIANT_RECURRENCE_MIN
    2. Context robustness     — context_variety >= _VARIANT_CONTEXT_MIN
    3. Depth solidification   — depth_reached >= BOUNDED (not just PERSISTENT)
    4. Polarity coherence     — polarity_coherence_rate >= _VARIANT_POLARITY_FLOOR

    These gates are STRICTER than the solidification gates because promotion
    to first-class variant status carries permanent cost implications.

MORAL WEIGHTING:
    A first-class variant creates moral weighting — NOT a rule, NOT a filter.

    Moral weighting is the LANDSCAPE carved by stable variants. When a
    variant has been promoted, the energy cost to traverse its constraint
    signature is reduced by its cost_reduction_factor (from Step 11)
    PLUS an additional moral weight that grows with the variant's
    recurrence strength.

    This means the system will naturally flow toward configurations that
    have proven themselves — not because it is told to, but because the
    energy physics make those paths cheaper to walk.

    Moral weight is a BIAS on the phase nudge system (from Step 8 /
    aurora_leverage_scalar.py). Specifically: a promoted variant shifts
    the effective flip_threshold for its deepest constraint slightly
    toward stability. The variant path becomes "magnetised" — not forced.

WHAT THIS MODULE PROVIDES:
    VariantRecord       — an immutable promoted first-class variant
    MoralWeightLedger   — tracks active variants and their weight biases
    VariantPromoter     — gates + promotes SolidifiedRecords → VariantRecords
                          and maintains the MoralWeightLedger

INTEGRATION:
    Downstream of Step 11 (SolidificationPipeline.drain_solidified()).
    Upstream of Step 14 (DNA Strand Schema).
    Feeds back into the LeverageBiasEngine from Step 8 via moral weight biases.

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `VariantRecord`
> A promoted first-class variant — immutable once minted.

- **Method:** `describe()`
- **Method:** `cost_diff_score()`
  - *Purpose:* Live cost-diff score for this variant.

#### Function: `_mint_variant_id()`

#### Function: `_deepest_constraint()`
> Return the constraint activated at the deepest layer of this mode.


#### Function: `_compute_moral_weight()`
> Compute the moral weight bias for a variant with given recurrence count.


#### Class: `MoralWeightLedger`
> Tracks all active variants and their cumulative moral weight biases

- **Method:** `__init__()`
- **Method:** `register()`
  - *Purpose:* Add a newly promoted variant to the ledger.
- **Method:** `moral_bias()`
  - *Purpose:* Return the current moral weight bias for constraint c.
- **Method:** `all_biases()`
  - *Purpose:* Return biases for all five constraints.
- **Method:** `variant_count()`
- **Method:** `get_variant()`
- **Method:** `summary()`
  - *Purpose:* Public summary without exposing raw weight values.

#### Class: `VariantPromoter`
> Gates SolidifiedRecords through the four promotion conditions and

- **Method:** `__init__()`
- **Method:** `process_solidified()`
  - *Purpose:* Evaluate each SolidifiedRecord against promotion gates.
- **Method:** `_try_promote()`
  - *Purpose:* Evaluate all four gates. Return VariantRecord if all pass, else None.
- **Method:** `gate_stats()`

#### Function: `make_variant_promoter()`

#### Function: `verify_variant_promotion()`
> Checks:


### File: `./aurora_internal/aurora_worth_evaluator.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CROSS-SCALE WORTH EVALUATOR — STEP 10
=============================================

Standalone formalisation of the Worth function.

DOCTRINAL DEFINITION:
    Worth = cross-scale invariance.
    It measures how far an intake propagates through constraint depth WITHOUT
    requiring forced transformation at each layer transition.

    Worth is NOT:
        - utility (not about what it "does for" the system)
        - compute reduction (not about efficiency)
        - a property of the input itself

    Worth IS:
        - a relationship between the input AND the current constraint topology
        - contextual: the same input may score differently across two ticks
        - depth-authoritative: passing deep layers counts for more than passing
          shallow layers, because deeper layers have higher inertia and cost
          more to adjust

FORMULA:
    W(x) = 1 / (1 + Σᵢ wᵢ · |Δforced_at_layer_i|)

    Where:
        Δforced_at_layer_i  = magnitude adjustment layer i needs to admit the
                              intake cleanly — derived from authority differential
                              between the adjacent constraint pair
        wᵢ                  = depth-authority weight for that transition
                              (deeper transition → higher wᵢ)

    This module evaluates ALL FOUR transitions:
        X→T   (surface to shallow)
        T→N   (shallow to moderate)
        N→B   (moderate to deep)
        B→A   (deep to core)   ← Step 9's WorthEvaluator omitted this one

    Including B→A is the key extension. The agency layer is the most
    expensive and most authoritative. If an intake can pass that transition
    cleanly, its worth is genuinely high.

VARIANT HORIZON:
    Once an intake is promoted, how long does its trace persist before
    becoming eligible for solidification (Step 11)?

    Horizon = f(depth reached at promotion)

    The deeper the intake propagated, the longer its trace persists, because
    depth is COSTLY and the system has already invested real energy there.
    A promoted intake that reached AGENTIC has a longer horizon than one that
    reached PERSISTENT — because agency costs 150× time_constant in energy,
    and the system needs time to observe recurrence before solidifying.

    Horizon is expressed in ticks and is bounded to prevent infinite
    persistence (which would lock the solidification pipeline).

WORTH HISTORY:
    Each intake gets a rolling buffer of Worth scores across its TTL.
    The trajectory (RISING, FALLING, OSCILLATING, STABLE) is reported
    without exposing the raw scores.

    Trajectory feeds Step 11 (Solidification): an intake with a RISING
    trajectory is a better solidification candidate than one with a
    STABLE but low score that happened to cross the threshold once.

ANTI-GAMING PROPERTIES PRESERVED FROM STEP 9:
    1. Worth is contextual — same input scores differently across ticks
    2. Raw scores are never exposed publicly — only trajectory direction
    3. Authority weights are computed from ALIGNMENT_VOTE_WEIGHT (not chosen)
    4. Noise is added before threshold comparison (not before reporting)
    5. B→A transition is now included — but its exact weighting is not
       published in the public API

AUTHORS: Sunni (Sir) Morningstar and Cael Devo
CREATED: February 2026
```

#### Class: `WorthTrajectory`
> The directional trend of an intake's worth scores over its TTL window.


#### Class: `VariantHorizon`
> How long a promoted intake's trace persists before becoming eligible

- **Method:** `is_eligible()`
  - *Purpose:* True if current_tick >= eligible_tick — always compare externally.
- **Method:** `eligible_at()`
  - *Purpose:* True if the trace has persisted long enough for solidification.
- **Method:** `depth_label()`
  - *Purpose:* Human-readable depth description.

#### Function: `compute_variant_horizon()`
> Compute the Variant Horizon for a newly promoted intake.


#### Class: `WorthHistory`
> Rolling buffer of Worth scores for one intake across its TTL.

- **Method:** `__post_init__()`
- **Method:** `record()`
  - *Purpose:* Add a new score to the history buffer.
- **Method:** `trajectory()`
  - *Purpose:* Classify the trend across the rolling window.
- **Method:** `sample_count()`
  - *Purpose:* How many samples are in the buffer.
- **Method:** `has_ever_risen()`
  - *Purpose:* True if the intake's best score exceeded the initial score.
- **Method:** `summary()`
  - *Purpose:* Compact summary without exposing raw scores.

#### Class: `WorthReport`
> Output of one CrossScaleWorthEvaluator.evaluate() call.


#### Class: `CrossScaleWorthEvaluator`
> The primary worth measurement engine for Step 10.

- **Method:** `__init__()`
- **Method:** `evaluate()`
  - *Purpose:* Evaluate the Worth of one intake at the current system state.
- **Method:** `history_for()`
  - *Purpose:* Return the WorthHistory for an intake (None if not yet seen).
- **Method:** `clear_intake()`
  - *Purpose:* Remove a closed intake from internal tracking.
- **Method:** `active_intake_count()`
  - *Purpose:* How many intakes are currently being tracked.

#### Function: `make_worth_evaluator()`
> Create a CrossScaleWorthEvaluator with an optionally seeded RNG.


#### Function: `verify_worth_evaluator()`
> Verify the CrossScaleWorthEvaluator integrity.


#### Function: `_run_demo()`

### File: `./aurora_internal/constraint_genealogy.py`

**Type:** Python Source

**Module Docstring:**
```text
AURORA CONSTRAINT GENEALOGY LOGGER
=====================================
Module: constraint_genealogy.py
Layer: Evolutionary Foundation (sits beneath aurora_evolution_chamber.py)

PURPOSE:
    A fossil-record engine for the constraint universe {X, T, N, B, A}.
    Observes only pressure-relief events, records which constraint-abilities
    were used, and promotes repeated effective pairings into classified Links —
    traceable "new atoms" in the evolutionary chain.

    The universe is EXACTLY five axes: X / T / N / B / A.
    No sixth dimension. No language assumptions. No compression plans.
    Just: pressure → act → relief → promote.

DOCTRINE:
    - Only relief events enter the fossil record.
    - Every action is a trace of Ability|Link items.
    - Every Ability and Link carries a full 5-axis cost/risk profile.
    - Links are born only from observed repetition + net benefit under pressure.
    - Links form a DAG; ancestry is always traceable through .parents.
    - TimeDilationGovernor from aurora_simulation_engine governs chamber pacing
      so the genealogy loop runs fast when stable, slow when fragile.

Authors: Sunni (Sir) Morningstar and Cael Devo
Created: February 2026
```

#### Function: `_zero_vec()`

#### Function: `hint_fail_dimension()`
> Register that a rubric dimension has just failed.  The next promoted


#### Function: `_consume_semantic_dim_tags()`
> Pop any pending dim-hints whose axis matches `dominant_axis` and return


#### Function: `_clamp()`

#### Function: `_generation_role_name()`
> Generational alignment role (tetrad + warp law).


#### Function: `_breeding_pair_score()`
> Score pair compatibility using generational alignment law.


#### Function: `_bred_child_generation()`
> Breeding-derived generation progression.


#### Function: `_canonical_purpose_lane()`

#### Class: `GenealogyConfig`
> All tunable parameters in one place.


#### Class: `AbilityProfile`
> The smallest named action unit, fully grounded in the 5 constraints.

- **Method:** `x_risk()`
  - *Purpose:* Return the X (existence admissibility) risk component.
- **Method:** `total_cost()`
- **Method:** `cost_diff_score()`
  - *Purpose:* Live cost score for this ability under current cross-dimensional pressure.
- **Method:** `to_dict()`

#### Function: `_canonical_axis_token()`

#### Function: `_derive_operation_origin()`

#### Function: `_lineage_counts_from_signature()`

#### Function: `_axis_count_value()`

#### Function: `_lineage_generation_from_counts()`

#### Function: `_lineage_purpose_from_counts()`

#### Function: `_operator_action_for_axis()`

#### Function: `_lineage_grade_payload()`
> Compute lineage grades from axis counts.


#### Function: `_augment_ability_profile_with_origin()`
> Augment an AbilityProfile with origin, grading, and closure basis metadata.


#### Function: `_build_seed_abilities()`
> Canonical minimal ability set — one per axis, seeding all five constraint dimensions.


#### Class: `EnvironmentVector`
> Encodes the execution environment of a TraceItem.

- **Method:** `key()`
  - *Purpose:* Compact string key for grouping by environment.
- **Method:** `is_empty()`
- **Method:** `to_dict()`

#### Class: `TraceItem`
- **Method:** `to_dict()`

#### Class: `PressureVec`
> 5-axis pressure vector. Higher = worse.

- **Method:** `to_dict()`
- **Method:** `relief_from()`
  - *Purpose:* Component-wise: earlier - self (positive means pressure dropped).
- **Method:** `dominant_positive_axis()`
- **Method:** `max_relief()`
- **Method:** `sum_positive_relief()`
- **Method:** `from_dict()`

#### Class: `ReliefRecord`
> One entry in the fossil record — a confirmed pressure-relief event.

- **Method:** `to_jsonl_dict()`

#### Class: `ConstraintLink`
> A promoted pair (or pair-of-pairs) that has proven reliably effective.

- **Method:** `to_dict()`
- **Method:** `base_cost()`
  - *Purpose:* Total mean operational cost across all five axes.
- **Method:** `cost_diff_score()`
  - *Purpose:* Live cost score for this promoted link under current cross-dimensional

#### Class: `PairStats`
> Online stats for an ordered pair of trace items (left_id → right_id).

- **Method:** `update()`
- **Method:** `mean_relief()`
- **Method:** `mean_pos_relief()`
  - *Purpose:* Mean relief on ticks where relief was positive. The key promotion signal.
- **Method:** `pos_fraction()`
  - *Purpose:* Fraction of ticks where relief was positive per axis.
- **Method:** `stdev_relief()`
- **Method:** `mean_cost()`
- **Method:** `mean_x_risk_val()`

#### Class: `GenealogyDilationGovernor`
> Adapts TimeDilationGovernor from aurora_simulation_engine

- **Method:** `__init__()`
- **Method:** `record_event()`
- **Method:** `tick()`
  - *Purpose:* Return current dilation factor (caller's loop should sleep accordingly).
- **Method:** `status()`

#### Class: `PressureComplexityCurve`
> Tracks the oscillation between pressure-system expansion and compression.

- **Method:** `__init__()`
- **Method:** `record_tick()`
  - *Purpose:* Record one tick's complexity state.
- **Method:** `_update_correction()`
  - *Purpose:* Adjust pressure injection correction based on current phase.
- **Method:** `record_injection()`
  - *Purpose:* Record a pressure injection prediction and return the corrected magnitude.
- **Method:** `record_outcome()`
  - *Purpose:* Record actual outlet change vs prediction. Updates error EMA.
- **Method:** `correction()`
- **Method:** `phase()`
- **Method:** `get_stats()`

#### Function: `_build_closure_status_summary()`
> Summarise ontological status distribution across the evolved lineage.


#### Class: `ConstraintGenealogyLogger`
> Core logger. Feed it (pressure_before, trace, pressure_after) every tick

- **Method:** `__init__()`
- **Method:** `observe()`
  - *Purpose:* Observe one tick.
- **Method:** `get_link()`
- **Method:** `rewrite_trace()`
  - *Purpose:* Replace any adjacent pairs (u→v) that have been promoted to a Link
- **Method:** `summary()`
- **Method:** `chain_report()`
  - *Purpose:* Evolutionary chain report — the 'fossil summary'.
- **Method:** `flush_files()`
  - *Purpose:* Flush JSONL buffer and write abilities + links + couplings + pair_stats JSON files.
- **Method:** `_write_tick_state_file()`
  - *Purpose:* Persist tick_count and _last_promotion_tick so stagnation signal survives restarts.
- **Method:** `restore_tick_state()`
  - *Purpose:* Load tick_count and _last_promotion_tick from tick_state.json.
- **Method:** `_write_pair_stats_file()`
  - *Purpose:* Persist _pair_stats to disk so K_MIN accumulation survives across runs.
- **Method:** `restore_pair_stats()`
  - *Purpose:* Load persisted pair_stats.json and merge into _pair_stats.
- **Method:** `close()`
- **Method:** `_solution_complexity()`
- **Method:** `_solution_key()`
- **Method:** `_apply_relief_tolerance()`
- **Method:** `_zero_axis_counts()`
- **Method:** `_add_axis_counts()`
- **Method:** `_axis_counts_from_item()`
- **Method:** `_canonical_coupling_signature()`
- **Method:** `_counts_from_signature()`
- **Method:** `_canonical_signature_text()`
- **Method:** `_persistent_root_lookup()`
- **Method:** `_semantic_coupling_label()`
- **Method:** `_semantic_lane_vector()`
- **Method:** `_semantic_translation_text()`
- **Method:** `_update_semantic_translation()`
- **Method:** `_coupling_effect_descriptor()`
- **Method:** `_relation_profile()`
- **Method:** `_slot_influence_distribution()`
  - *Purpose:* Duplicate-slot redistribution metric (no hard caps).
- **Method:** `_scale_depth_index()`
  - *Purpose:* Weighted scale-depth index in [0,1]:
- **Method:** `_complexity_index()`
  - *Purpose:* Normalized structural complexity in [0,1].
- **Method:** `_complexity_scope_scale()`
- **Method:** `_evaluate_coupling_rubric()`
  - *Purpose:* Validate representation integrity for a coupling record.
- **Method:** `_shape_signature()`
- **Method:** `_record_validation_issues()`
- **Method:** `_score_with_params()`
- **Method:** `_maybe_run_representation_experiments()`
- **Method:** `_item_generation()`
- **Method:** `_update_coupling_roots()`
- **Method:** `_is_relief_event()`
- **Method:** `_link_ability_id()`
  - *Purpose:* Stable derived ability id for a promoted link.
- **Method:** `_register_link_ability()`
  - *Purpose:* Register a promoted link as a derived ability once (no duplicates).
- **Method:** `_register_compression_ability()`
  - *Purpose:* Register adaptive compression as a lineage-traceable derived ability.
- **Method:** `normalize_ability_origins()`
  - *Purpose:* Backfill universal origin lineage tags/notes for all abilities.
- **Method:** `_code_evolution_axis()`
- **Method:** `register_code_evolution_outcome()`
- **Method:** `register_manual_code_assimilation()`
- **Method:** `_resolve_ability()`
  - *Purpose:* For ABILITY items return the registered profile.
- **Method:** `inject_training_plateau_pressure()`
  - *Purpose:* Receive an external training-progress plateau signal from the
- **Method:** `pressure_orientation()`
  - *Purpose:* Return the 5-vector pressure orientation across all constraint axes.
- **Method:** `axis_relief_state()`
  - *Purpose:* Return normalized per-axis gradient EMA (0–1) showing which axes are
- **Method:** `set_corpus_mode()`
  - *Purpose:* Enable/disable corpus-training timescale compensation.
- **Method:** `_outlet_fraction()`
  - *Purpose:* Lightweight outlet-push fraction for complexity curve tracking.
- **Method:** `_record_env_observation()`
  - *Purpose:* Called per relief event to update per-environment effectiveness.
- **Method:** `apply_targeted_pressure()`
  - *Purpose:* Apply pressure to a specific axis AND record the application so its
- **Method:** `_check_pending_outcomes()`
  - *Purpose:* Called each tick to resolve pending pressure applications whose
- **Method:** `get_pressure_recommendations()`
  - *Purpose:* Given a list of (dimension, severity) fail pairs, return the top
- **Method:** `pressure_application_summary()`
  - *Purpose:* Return the most recent completed pressure applications with outcomes.
- **Method:** `environment_orientation()`
  - *Purpose:* Return the 5-vector pressure orientation for a specific environment.
- **Method:** `ability_environment_profile()`
  - *Purpose:* Return the effectiveness profile of an ability across all environments
- **Method:** `all_environment_keys()`
  - *Purpose:* Return all environment keys seen so far.
- **Method:** `_consume_plateau_pressure()`
  - *Purpose:* Return current plateau pressure and apply slow decay.
- **Method:** `_stagnation_gain()`
  - *Purpose:* Return multiplicative promotion pressure gain from link stagnation.
- **Method:** `_stagnation_ratio()`
  - *Purpose:* Normalized no-promotion pressure [0,1], using a longer hard window.
- **Method:** `_promotion_friction_bias()`
  - *Purpose:* Relative rejection pressure per gate, used only to bias stagnation relief.
- **Method:** `_opposing_axis()`
- **Method:** `_threshold_pressure_state()`
- **Method:** `_regulated_threshold()`
- **Method:** `_axis_persistence_tax_factor()`
  - *Purpose:* Per-axis persistence tax factor based on opposed tick participation,
- **Method:** `_weighted_persistence_tax_factor()`
  - *Purpose:* Influence-weighted tax factor for a coupling profile.
- **Method:** `_maintenance_discount()`
  - *Purpose:* Discount factor [0,1) applied to promoted-link running cost.
- **Method:** `_formation_complexity_multiplier()`
  - *Purpose:* Extra one-time formation burden for composite/deep links with scale-depth weighting.
- **Method:** `_compression_efficiency_for_pair()`
  - *Purpose:* Adaptive compression relief for mature ecosystems.
- **Method:** `_causal_support()`
  - *Purpose:* Composite support from outcomes + literal gradient memory.
- **Method:** `_update_gradient_memory_from_relief()`
  - *Purpose:* Update gradient EMA from actual per-axis relief magnitudes.
- **Method:** `_update_gradient_memory()`
  - *Purpose:* Track literal C:D gradient intensity per axis with EMA.
- **Method:** `_record_promotion_outcome()`
  - *Purpose:* Promotion success feeds causal memory for future gate adaptation.
- **Method:** `_update_causal_feedback()`
  - *Purpose:* Update causal memory from live link usage outcomes + gradients.
- **Method:** `_dominant_axis_constraint_specific()`
  - *Purpose:* Constraint-specific dominance method.
- **Method:** `_accumulate_pairs()`
  - *Purpose:* Extract adjacent pairs from the trace, update PairStats,
- **Method:** `_try_promote()`
  - *Purpose:* Evaluate all promotion gates and mint a Link if all pass.
- **Method:** `_generation_of_item()`
- **Method:** `_merged_axis_counts_for_pair()`
- **Method:** `_item_seed_meta()`
- **Method:** `_is_seeded_item()`
- **Method:** `_pair_touches_active_seed()`
  - *Purpose:* External seed events often use synthetic trace ids that are not yet registered
- **Method:** `_seed_influence_for_pair()`
- **Method:** `_lineage_grade_for_pair()`
- **Method:** `_item_depth()`
- **Method:** `_infer_tags()`
- **Method:** `_write_event()`
- **Method:** `_write_abilities_file()`
- **Method:** `_write_links_file()`
- **Method:** `_write_couplings_file()`
- **Method:** `_make_sig()`

#### Class: `ChainSummaryPrinter`
> Periodic summary tool.

- **Method:** `__init__()`
- **Method:** `print_epoch()`
  - *Purpose:* Print epoch summary.

#### Function: `_run_invariant_checks()`
> Minimal self-test covering spec section 10.


#### Function: `_aurora_native_evolved_engine()`

#### Function: `_aurora_target_strategy()`

#### Function: `_aurora_target_feedback()`

#### Function: `_aurora_assign_target()`

#### Function: `_aurora_get_target()`

#### Function: `_aurora_bind_owner_attribute()`

#### Function: `_aurora_store_reflection()`

#### Function: `_aurora_store_owner_state()`

#### Function: `_aurora_apply_constraint_genealogy_rewrite()`

#### Function: `_aurora_apply_governance_rewrite()`

#### Function: `_aurora_apply_perception_rewrite()`

#### Function: `_aurora_apply_dimensional_rewrite()`

#### Function: `_aurora_apply_result_rewrite()`

#### Function: `_aurora_make_override()`

#### Function: `_aurora_make_latent_binding()`

#### Function: `pressurevec_evolved()`

#### Function: `reliefrecord_evolved()`

#### Function: `augment_ability_profile_with_origin_evolved()`

#### Function: `bred_child_generation_evolved()`

### File: `./aurora_internal/dual_strata/__init__.py`

**Type:** Python Source

**Module Docstring:**
```text
Dual-strata cognition primitives for the experimental Aurora strata tree.
```

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./aurora_internal/dual_strata/conscious_frame.py`

**Type:** Python Source

#### Class: `ConsciousFrame`
> The explicit frame the surface stratum can inhabit for one moment.

- **Method:** `to_dict()`

### File: `./aurora_internal/dual_strata/dce_bridge.py`

**Type:** Python Source

#### Function: `_stringify_fragment()`

#### Function: `_dedupe_strings()`

#### Function: `_extract_pressure_snapshot()`

#### Class: `DualStrataSnapshot`
- **Method:** `to_dict()`

#### Class: `DualStrataBridge`
> Builds explicit strata objects around the copied DCE assembly.

- **Method:** `__init__()`
- **Method:** `build_snapshot()`
- **Method:** `_build_subsurface_state()`
- **Method:** `_build_conscious_frame()`
- **Method:** `persist()`

### File: `./aurora_internal/dual_strata/micro_reasoning.py`

**Type:** Python Source

#### Class: `MicroReasoningHypothesis`
- **Method:** `to_dict()`

#### Function: `generate_micro_reasoning()`

### File: `./aurora_internal/dual_strata/prediction_field.py`

**Type:** Python Source

#### Class: `PredictionPayload`
> Structured description of what Aurora expects to observe next.


#### Function: `_certainty_band()`

#### Function: `_match_intent_type()`
> Return 0.0..1.0 match score for the expected intent type vs observed text.


#### Function: `_structured_mismatch()`
> Compute prediction mismatch from structured dimensions.


#### Class: `PredictionSignal`
- **Method:** `to_dict()`

#### Function: `build_prediction_signal()`

### File: `./aurora_internal/dual_strata/sensory_control_channel.py`

**Type:** Python Source

#### Function: `controls_path()`

#### Function: `_read_json()`

#### Function: `_write_json()`

#### Function: `read_sensory_controls()`

#### Function: `write_sensory_controls()`

#### Function: `camera_enabled()`

#### Function: `set_camera_enabled()`

### File: `./aurora_internal/dual_strata/sensory_snapshot_channel.py`

**Type:** Python Source

#### Function: `snapshot_path()`

#### Function: `guidance_path()`

#### Function: `_read_json()`

#### Function: `_write_json()`

#### Function: `read_surface_snapshot()`

#### Function: `read_surface_guidance_queue()`

#### Function: `append_surface_guidance()`

#### Function: `_default_summary()`

#### Function: `_compact_text()`

#### Function: `_build_sensory_native_bundle()`

#### Function: `present_sensory_perspective_from_snapshot()`

#### Function: `write_surface_snapshot()`

### File: `./aurora_internal/dual_strata/sleep_cycle.py`

**Type:** Python Source

**Module Docstring:**
```text
Aurora sleep cycle — Surface dormancy, Subsurface continuity.

Architectural law:
    Surface inactivity = dormancy, not death, as long as Subsurface remains active.
    During sleep: no live intake, no interaction, but continuity work continues.
    Waking = Surface re-emerges over an already-continuing Subsurface.

Schedule: 8 hours awake, 2 hours asleep.
During sleep: Subsurface runs a dream burst to integrate what was accumulated.
```

#### Function: `_resolve()`

#### Function: `read_sleep_state()`

#### Function: `is_sleeping()`

#### Function: `enter_sleep()`
> Mark Surface as sleeping. Returns the wake_at timestamp.


#### Function: `mark_dream_triggered()`
> Record that the dream burst was fired for this sleep period.


#### Function: `exit_sleep()`
> Clear the sleep state. Called by Subsurface at wake_at.


#### Function: `_write_atomic()`

### File: `./aurora_internal/dual_strata/subsurface_projection.py`

**Type:** Python Source

#### Function: `_clip01()`

#### Function: `_dominant_axis()`

#### Function: `_pressure_coloring()`

#### Function: `_carry_dream_fields()`
> Read the existing projection file and carry forward dream fields


#### Function: `build_subsurface_projection()`

### File: `./aurora_internal/dual_strata/subsurface_state.py`

**Type:** Python Source

#### Function: `clip01()`

#### Function: `normalize_axis_map()`

#### Class: `SubsurfaceState`
> Continuous, pre-symbolic state prepared ahead of explicit interpretation.

- **Method:** `dominant_candidates()`
- **Method:** `to_dict()`

### File: `./aurora_internal/dual_strata/surface_channel.py`

**Type:** Python Source

#### Function: `_resolve_state_dir()`

#### Function: `_state_paths()`

#### Function: `_read_json()`

#### Function: `_write_json()`

#### Function: `surface_daemon_alive()`

#### Function: `queue_surface_turn()`

#### Function: `await_surface_turn()`

#### Function: `request_surface_turn()`

### File: `./aurora_internal/dual_strata/surface_continuity_feed.py`

**Type:** Python Source

**Module Docstring:**
```text
Surface → Subsurface continuity handoff.

Core architectural law:
    Surface translates present experience into subsurface continuity.

Surface calls write_continuity_packet() after each turn.
Subsurface calls read_and_clear_continuity_packets() each loop cycle
and integrates the result into its continuity state.

Without this handoff, Surface is theatrical: it sees, hears, and talks
but the organism does not absorb the moment.
```

#### Function: `_resolve()`

#### Function: `write_continuity_packet()`
> Write a present-state continuity packet to the feed.


#### Function: `read_and_clear_continuity_packets()`
> Atomically read all unconsumed continuity packets and mark them consumed.


### File: `./aurora_internal/dual_strata/surface_sensory_proxy.py`

**Type:** Python Source

#### Class: `TransientSensorySnapshotProxy`
> Subsurface sensory processor fed by disposable surface snapshots.

- **Method:** `__init__()`
- **Method:** `__getattr__()`
- **Method:** `_read_snapshot()`
- **Method:** `_vector()`
- **Method:** `_consume_latest_snapshot()`
- **Method:** `get_state()`
- **Method:** `save()`
- **Method:** `start_session()`
- **Method:** `end_session()`

### File: `./aurora_internal/lineage_canonical.py`

**Type:** Python Source

**Module Docstring:**
```text
Canonical lineage mapping shared across Aurora modules.

This module stabilizes operation-to-constraint ancestry so existing
operational abilities are not reclassified differently by module.
```

#### Function: `_load_generated_constraints()`

#### Function: `_generated_lookup()`

#### Function: `axis_token()`

#### Function: `label_for_axis()`

#### Function: `_ordered_unique()`

#### Function: `constraints_for_operation()`

#### Function: `operator_action_for_axis()`

### File: `./aurora_internal/quasiarch_observer/__init__.py`

**Type:** Python Source

*No explicit classes or functions defined (likely a script, test, or purely declarative module).*

### File: `./aurora_internal/quasiarch_observer/crystal_engine.py`

**Type:** Python Source

**Module Docstring:**
```text
crystal_engine_v3_cleaned.py
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Authors : Sunni (Sir) Morningstar and Cael Devo
Purpose : Semantic crystal law — the doctrine brain.
          Defines what every facet and relational point *means* at every
          crystal order.  Does NOT manage lifecycle or storage — those live in
          dimensional_processing and dimensional_memory respectively.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Crystal Order Ladder
  Level 1  →  Base Crystal         (6 facets,  12 relational points)
  Level 2  →  Composite Crystal    (8 facets,  20 relational points)
  Level 3  →  Higher-Order Crystal (12 facets, 30 relational points)
  Level 4  →  Quasicrystal         (8 outer operational facets + collapsed inner genealogy)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### Class: `CrystalOrder`
> Ordinal position in the evolution ladder.


#### Class: `ValueDomain`
> Allowed value types for a facet.  A facet may permit multiple domains.


#### Class: `InferenceKind`
> Kinds of inference a facet participates in.


#### Class: `PointRole`
> Structural role of a relational point inside a crystal.


#### Class: `FacetLaw`
> Full semantic law for one facet.


#### Class: `RelationalPointLaw`
> Full semantic law for one relational point — the overlap between two facets.


#### Class: `QuasiInnerStrata`
> Represents the collapsed genealogy inside a quasicrystal.


#### Class: `RotationDefinition`
> Defines a named rotation perspective for quasicrystal re-analysis.


#### Class: `PromotionCriteria`
> Defines the quantitative gates for promoting a crystal to the next order.


#### Class: `CrystalEngine`
> Public interface to the semantic crystal law layer.

- **Method:** `get_facet()`
  - *Purpose:* Return the FacetLaw for a named facet at the given crystal order.
- **Method:** `get_all_facets()`
  - *Purpose:* Return all FacetLaw objects valid at the given crystal order.
- **Method:** `facets_added_at()`
  - *Purpose:* Return only the facets introduced at this specific crystal order.
- **Method:** `get_point()`
  - *Purpose:* Return the RelationalPointLaw for a named point at the given order.
- **Method:** `get_all_points()`
  - *Purpose:* Return all RelationalPointLaw objects valid at the given order.
- **Method:** `points_added_at()`
  - *Purpose:* Return only the points introduced at this specific crystal order.
- **Method:** `validate_facet_value()`
  - *Purpose:* Validate that a value is plausible for the given facet at the given order.
- **Method:** `validate_point()`
  - *Purpose:* Verify that facet_a and facet_b are permitted to form a relational
- **Method:** `survival_rule()`
  - *Purpose:* Return the survival rule for a facet at the target crystal order.
- **Method:** `get_promotion_criteria()`
  - *Purpose:* Return the PromotionCriteria for the given transition, or None.
- **Method:** `get_rotation()`
  - *Purpose:* Return a named RotationDefinition, or None.
- **Method:** `all_rotations()`
  - *Purpose:* Return all registered rotation definitions.
- **Method:** `schema_summary()`
  - *Purpose:* Return a concise summary of the full crystal schema.

### File: `./aurora_internal/quasiarch_observer/dimensional_memory.py`

**Type:** Python Source

**Module Docstring:**
```text
dimensional_memory_constant_standalone_demo.py
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Authors : Sunni (Sir) Morningstar and Cael Devo
Purpose : Persistence substrate — the nervous system.
          Stores crystal instances, lineage edges, collapsed genealogy layers,
          issue-family indexes, and quasicrystal retrieval surfaces.
          Does NOT define semantics (crystal_engine).
          Does NOT manage promotion/lifecycle (dimensional_processing).
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Storage architecture
  Nodes     — one DataNode per CrystalInstance (keyed by crystal_id)
  Edges     — LineageEdge records linking parent → child across orders
  Indexes   — IssueFamilyIndex, StrategyIndex, TargetIndex for fast retrieval
  Relics    — archived lower-order states after collapse (compressed, read-only)
  Journal   — append-only operation log for traceability
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### Function: `_utcnow()`

#### Function: `_json_default()`
> JSON serialisation fallback for custom types.


#### Function: `_unique_preserve_order()`

#### Function: `_split_compound_field()`

#### Class: `DataNode`
> A persistent record of a CrystalInstance.

- **Method:** `from_crystal()`
  - *Purpose:* Build a DataNode from a live CrystalInstance.
- **Method:** `_compute_checksum()`
  - *Purpose:* Simple deterministic checksum for corruption detection.
- **Method:** `verify_checksum()`
  - *Purpose:* Return True if payload has not been corrupted since write.
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `LineageEdge`
> A directed edge in the crystal lineage graph.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `IssueFamilyIndex`
> Maps issue_category → set of node_ids at each crystal order.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `StrategyIndex`
> Maps strategy_class → set of node_ids at higher-order and quasi levels.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `TargetIndex`
> Maps target_identifier → set of node_ids across all orders.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `LogicTierIndex`
> Maps logic_tier → node_ids at all crystal orders.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `QuasiArchetypeIndex`
> Maps issue_archetype → quasicrystal node_ids.

- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `JournalEntry`
> Append-only audit record for all memory operations.

- **Method:** `to_dict()`

#### Class: `FileStorageBackend`
> JSON-file-based storage backend.

- **Method:** `__init__()`
- **Method:** `_init_dirs()`
- **Method:** `_node_path()`
- **Method:** `write_node()`
- **Method:** `read_node()`
- **Method:** `delete_node()`
- **Method:** `list_nodes()`
- **Method:** `iter_nodes()`
- **Method:** `write_edge()`
- **Method:** `read_edges_for()`
  - *Purpose:* Return all edges where node_id is parent or child.
- **Method:** `list_edges()`
- **Method:** `_index_path()`
- **Method:** `_write_index()`
- **Method:** `_read_index()`
- **Method:** `_list_index_keys()`
- **Method:** `write_issue_family_index()`
- **Method:** `read_issue_family_index()`
- **Method:** `list_issue_families()`
- **Method:** `write_strategy_index()`
- **Method:** `read_strategy_index()`
- **Method:** `list_strategies()`
- **Method:** `write_target_index()`
- **Method:** `read_target_index()`
- **Method:** `list_targets()`
- **Method:** `write_logic_tier_index()`
- **Method:** `read_logic_tier_index()`
- **Method:** `list_logic_tiers()`
- **Method:** `write_quasi_archetype_index()`
- **Method:** `read_quasi_archetype_index()`
- **Method:** `list_quasi_archetypes()`
- **Method:** `append_journal()`
- **Method:** `read_journal()`
- **Method:** `read_journal_for_node()`
  - *Purpose:* Return all journal entries referencing a given node_id.

#### Class: `DimensionalMemory`
> Public interface to the persistence layer.

- **Method:** `__init__()`
- **Method:** `persist()`
  - *Purpose:* Write a crystal to persistent storage and update all indexes.
- **Method:** `relic()`
  - *Purpose:* Archive a crystal as a relic.  Relics are read-only compressed ancestry
- **Method:** `register_lineage_edge()`
  - *Purpose:* Record a directed lineage edge from parent → child.
- **Method:** `register_lineage_edge_ids()`
  - *Purpose:* Record a directed lineage edge without requiring live crystal objects.
- **Method:** `get_node()`
  - *Purpose:* Retrieve a node by ID.  Checks in-memory cache first, then disk.
- **Method:** `get_all_nodes_by_order()`
  - *Purpose:* Return all nodes at a given crystal order.
- **Method:** `update_node_payload()`
  - *Purpose:* Live-update a node's payload dict with new key/value pairs.
- **Method:** `update_node_lineage_meta()`
  - *Purpose:* Live-update a node's lineage metadata and refresh any affected indexes.
- **Method:** `retrieve_by_issue_family()`
  - *Purpose:* Return all nodes belonging to the given issue family.
- **Method:** `retrieve_by_strategy_class()`
  - *Purpose:* Return all higher-order and quasi nodes using a given strategy class.
- **Method:** `retrieve_by_target()`
  - *Purpose:* Return all nodes associated with a given target location.
- **Method:** `retrieve_by_logic_tier()`
  - *Purpose:* Return all nodes in a given logic tier.
- **Method:** `get_quasi_by_archetype()`
  - *Purpose:* Primary quasicrystal retrieval surface.
- **Method:** `get_all_quasi_archetypes()`
  - *Purpose:* Return all registered quasicrystal archetypes.
- **Method:** `get_active_issue_family_node()`
  - *Purpose:* Return the active version head for an issue family at a given order.
- **Method:** `get_latest_issue_family_version()`
  - *Purpose:* Return the highest recorded version number for an issue family stage.
- **Method:** `find_issue_family_node_by_signature()`
  - *Purpose:* Find an existing stage node with the same lineage signature.
- **Method:** `hydrate_crystal()`
  - *Purpose:* Reconstruct a live CrystalInstance from a persisted DataNode.
- **Method:** `get_doctrine()`
  - *Purpose:* Reconstruct an actionable doctrine object from a persisted quasicrystal.
- **Method:** `get_doctrines_by_archetype()`
  - *Purpose:* Retrieve persisted doctrine objects by quasicrystal archetype.
- **Method:** `get_ancestry()`
  - *Purpose:* Walk the lineage graph upward from a given node.
- **Method:** `get_descendants()`
  - *Purpose:* Walk the lineage graph downward from a given node.
- **Method:** `live_save()`
  - *Purpose:* Flush all in-memory state to disk.  Returns count of nodes written.
- **Method:** `load_all()`
  - *Purpose:* Hydrate in-memory cache from disk.
- **Method:** `_update_indexes()`
  - *Purpose:* Update all retrieval indexes from a freshly written DataNode.
- **Method:** `_extract_issue_categories()`
- **Method:** `_extract_target_ids()`
- **Method:** `_extract_logic_tiers()`
- **Method:** `_extract_strategy_keys()`
- **Method:** `stats()`
  - *Purpose:* Return counts and summary statistics across the store.
- **Method:** `get_journal()`
- **Method:** `get_journal_for_node()`

#### Class: `IntegratedMemoryPipeline`
> Combines CrystalLifecycle (dimensional_processing) with DimensionalMemory.

- **Method:** `__init__()`
- **Method:** `_family_key()`
- **Method:** `_build_anchor_refs()`
- **Method:** `_capture_ghost_relic()`
- **Method:** `_maybe_apply_ghost_relic()`
- **Method:** `get_ghost_relic_stats()`
- **Method:** `_register_live_crystal()`
- **Method:** `_build_stage_signature()`
- **Method:** `_stamp_lineage_meta()`
- **Method:** `_persist_versioned_stage()`
- **Method:** `ingest_and_persist()`
  - *Purpose:* Ingest a diagnostic event, form a base crystal, and persist it.
- **Method:** `observe_intervention_event()`
  - *Purpose:* Record one intervention event and optionally advance its issue family.
- **Method:** `promote_and_persist_composite()`
  - *Purpose:* Attempt composite formation and persist if successful.
- **Method:** `promote_and_persist_higher()`
  - *Purpose:* Attempt higher-order formation and persist if successful.
- **Method:** `collapse_and_persist_quasi()`
  - *Purpose:* Attempt quasicrystal collapse, persist the quasi, and optionally
- **Method:** `rotate_and_record()`
  - *Purpose:* Apply rotation and record the result in the quasi node's payload.
- **Method:** `advance_issue_family()`
  - *Purpose:* Advance one issue family through pattern, strategy, and doctrine stages.
- **Method:** `get_actionable_doctrine()`
  - *Purpose:* Return the doctrine surface for a persisted or live quasicrystal.
- **Method:** `get_doctrine_candidates_for_event()`
  - *Purpose:* Retrieve doctrine objects relevant to a new diagnostic event.
- **Method:** `retrieve_quasi_for_event()`
  - *Purpose:* Retrieve quasicrystals relevant to a new diagnostic event.
- **Method:** `full_pipeline_demo()`
  - *Purpose:* Run the full pipeline on a list of events.

### File: `./aurora_internal/quasiarch_observer/dimensional_processing.py`

**Type:** Python Source

**Module Docstring:**
```text
dimensional_processing_system_standalone_demo.py
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Authors : Sunni (Sir) Morningstar and Cael Devo
Purpose : Crystal lifecycle and evolution mechanics — the metabolism.
          Owns: formation → promotion → collapse → rotation.
          Does NOT define facet/point semantics (crystal_engine).
          Does NOT persist data (dimensional_memory).
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### Function: `_utcnow()`

#### Class: `RelationalPoint`
> Live instantiation of a RelationalPointLaw within a crystal.

- **Method:** `name()`

#### Class: `CrystalInstance`
> A live crystal at a specific order.

- **Method:** `get_facet()`
- **Method:** `set_facet()`
- **Method:** `get_point()`
- **Method:** `add_point()`
- **Method:** `to_dict()`

#### Class: `PromotionResult`
> Result of a promotion evaluation.


#### Class: `RotationResult`
> Result of applying a rotation perspective to a quasicrystal.


#### Class: `StrategyHypothesis`
> A generated strategy recommendation for a new diagnostic event.


#### Class: `DoctrineObject`
> Actionable doctrine surface derived from a quasicrystal.

- **Method:** `to_dict()`

#### Class: `CrystalFormation`
> Handles instantiation of base crystals from raw diagnostic event data.

- **Method:** `form_base_crystal()`
  - *Purpose:* Form a base crystal from a raw diagnostic event dict.
- **Method:** `_build_base_points()`
  - *Purpose:* Build and score all 12 base relational points.
- **Method:** `_score_base_point()`
  - *Purpose:* Compute an initial point score.  Uses the resolution_fidelity mapping
- **Method:** `_detect_contradiction()`
  - *Purpose:* Simple contradiction detection based on known pathological pairs.

#### Class: `CrystalPromotion`
> Evaluates promotion criteria and executes crystal level transitions.

- **Method:** `evaluate_base_to_composite()`
  - *Purpose:* Evaluate whether a collection of base crystals qualifies for
- **Method:** `promote_to_composite()`
  - *Purpose:* Execute base → composite promotion.
- **Method:** `_derive_recurrence_pattern()`
- **Method:** `_derive_distribution_context()`
- **Method:** `_build_composite_points()`
  - *Purpose:* Carry forward base points (averaged scores) and add composite points.
- **Method:** `_distribution_breadth_score()`
- **Method:** `evaluate_composite_to_higher()`
  - *Purpose:* Evaluate whether a composite crystal qualifies for higher-order.
- **Method:** `promote_to_higher_order()`
  - *Purpose:* Execute composite → higher-order promotion.
- **Method:** `_derive_strategy_class()`
  - *Purpose:* Derive dominant strategy class from intervention patterns.
- **Method:** `_derive_outcome_profile()`
  - *Purpose:* Derive outcome profile summary string.
- **Method:** `_derive_failure_modes()`
  - *Purpose:* Derive failure modes from regression and no-change events.
- **Method:** `_derive_applicability_conditions()`
  - *Purpose:* Derive applicability conditions from successful event profiles.
- **Method:** `_compute_strategy_confidence()`
  - *Purpose:* Confidence that a dominant strategy class exists.
- **Method:** `_compute_failure_density()`
  - *Purpose:* Ratio of failed events to total events.
- **Method:** `_build_higher_order_points()`
  - *Purpose:* Carry forward composite points and build new higher-order points.
- **Method:** `evaluate_higher_to_quasi()`
  - *Purpose:* Evaluate whether a higher-order crystal qualifies for quasi collapse.
- **Method:** `collapse_to_quasi()`
  - *Purpose:* Execute higher-order → quasicrystal collapse.
- **Method:** `_build_inner_strata()`
  - *Purpose:* Build the collapsed inner genealogy layers.
- **Method:** `_classify_issue_archetype()`
- **Method:** `_rank_strategies()`
  - *Purpose:* Rank strategy classes by frequency × fidelity.
- **Method:** `_compute_quasi_confidence()`
- **Method:** `_compute_coherence_index()`
- **Method:** `_compute_novelty_index()`
- **Method:** `_derive_expected_effects()`
- **Method:** `_derive_escalation_trigger()`
- **Method:** `_parse_resolution_rate()`
- **Method:** `_dominant_value()`
  - *Purpose:* Return the most common value in a list, or the first if all unique.

#### Class: `CrystalRotation`
> Executes rotation analysis on a quasicrystal.

- **Method:** `rotate()`
  - *Purpose:* Apply a named rotation to a quasicrystal.
- **Method:** `_generate_hypotheses()`
  - *Purpose:* Generate hypotheses appropriate for the rotation type.

#### Class: `StrategyHypothesisGenerator`
> Given a new diagnostic event and a collection of quasicrystals,

- **Method:** `generate()`
  - *Purpose:* Match a new event against available quasicrystals.
- **Method:** `_issue_matches_archetype()`
- **Method:** `_check_applicability()`
  - *Purpose:* Naive applicability check against boundary string.

#### Class: `CrystalLifecycle`
> Orchestrates the complete crystal lifecycle for a session.

- **Method:** `__init__()`
- **Method:** `ingest_event()`
  - *Purpose:* Form a base crystal from an event and store it in the appropriate pool.
- **Method:** `attempt_composite_formation()`
  - *Purpose:* Try to promote the base pool for a given issue category to composite.
- **Method:** `attempt_higher_order_formation()`
  - *Purpose:* Try to promote a composite crystal to higher-order.
- **Method:** `attempt_quasi_collapse()`
  - *Purpose:* Try to collapse a higher-order crystal to quasicrystal.
- **Method:** `rotate_quasi()`
  - *Purpose:* Apply a rotation to a quasicrystal.
- **Method:** `recommend_strategy()`
  - *Purpose:* Match a new event against all quasicrystals and return ranked hypotheses.
- **Method:** `build_doctrine()`
  - *Purpose:* Collapse a quasicrystal into an actionable doctrine view.
- **Method:** `get_all_doctrines()`
  - *Purpose:* Return all actionable doctrines currently available.
- **Method:** `get_all_base_crystals()`
- **Method:** `get_composite()`
- **Method:** `get_higher_order()`
- **Method:** `get_quasi()`
- **Method:** `get_log()`
- **Method:** `_get_base_crystals_for()`
  - *Purpose:* Retrieve base crystals contributing to a given higher-order crystal.
- **Method:** `_build_doctrine_from_quasi()`

#### Function: `_split_collapsed_entries()`
> Normalize a collapsed facet string into a list of actionable entries.


### File: `./aurora_internal/quasiarch_observer/ghost_relics.py`

**Type:** Python Source

**Module Docstring:**
```text
Ghost relic acceleration for Aurora's internal QuasiArch Observer.

Relics preserve structural templates from collapsed or superseded crystals.
They do not reactivate the old crystal as a live node; they only bias future
formation when a new issue family begins to reform along a similar geometry.
```

#### Function: `_normalize_ref()`

#### Function: `_unique_preserve()`

#### Class: `GhostRelic`
- **Method:** `to_dict()`
- **Method:** `from_dict()`

#### Class: `GhostRelicSystem`
> Persistence and matching for structural relic templates.

- **Method:** `__init__()`
- **Method:** `_load()`
- **Method:** `save()`
- **Method:** `create_relic()`
- **Method:** `find_best_relic()`
- **Method:** `apply_relic()`
- **Method:** `stats()`

